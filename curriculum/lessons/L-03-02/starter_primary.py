def classify_readings(readings, threshold):
    results = []
    # TODO: loop over every value in readings.
    # Append "alert" when value >= threshold; otherwise append "clear".
    # Return results after the loop has finished.
    return results


readings = [3, 8, 5]
results = classify_readings(readings, 5)
print(results)
