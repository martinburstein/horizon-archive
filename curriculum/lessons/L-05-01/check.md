# L-05-01 answer and remediation reference

The machine key is `answer_key.json`; reference files prove both passing paths.

- Resolve identity/scope before mutations.
- Confirm the correct project before choosing a model.
- Deploy or reuse a named deployment and wait for successful provisioning.
- Treat portal interaction as a bounded smoke test.
- Keep project endpoint, deployment name, and credential conceptually separate.
- Confirm ownership and scope before cleanup.

Run `python validate_portal_orientation.py --self-test` for all eight checkpoints, blank rejection, catalog-versus-deployment, and destructive-cleanup probes.
