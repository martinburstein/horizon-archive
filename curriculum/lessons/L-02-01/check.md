# L-02-01 Mastery Check

## Activity 1 reasoning

1. Written reviews → detect sentiment → sentiment labels: **text analysis**.
2. Spoken tour → convert speech → live text captions: **speech**.
3. Written brief → create visual content → new image: **computer vision and image generation**.
4. Request plus inventory access → decide and use tools → completed order and notice: **agentic AI**.
5. Invoice image/document → locate named values → fixed record: **information extraction**.
6. Photograph → interpret visible content → descriptions: **computer vision and image generation**.

## Terminal-state retrieval

1. Editable source is the layer you change.
2. Runtime output is produced by Run.
3. Saved mastery evidence records limited outcome metadata without needing the free-form answer.
4. Close/reopen can resume the same temporary working session; scene transition ends that session. Mastery evidence is separate and persists.

## Twelve-point closed-note gate

The deterministic primary and fresh-retry items and answer keys are in `exercise.json`. Award one point per item on the active 12-item form.

Pass only when both conditions are true:

- score is at least 10/12; and
- no critical contrast item is missed.

Critical misconception tags:

- `generative-is-agentic`
- `vision-is-extraction`
- `source-is-output`
- `working-state-is-mastery-evidence`

Store only item correctness, attempts, hint level, confidence, tags, and mastery state. Do not persist the learner's free-form source or response text.

For the reload and scene-round-trip distinction, use [Saved Evidence Is Prior Progress](saved-evidence-resume-guide.md). Reconstructed finalized correctness remains prior assessed progress; it is not a new attempt, retained private response, mastery acknowledgement, exam credit, readiness guarantee, or external authority.
