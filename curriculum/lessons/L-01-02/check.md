# L-01-02 Retrieval and Mastery Key

## Retrieval answers

1. `3` is a number; `"3"` is a string containing one text character.
2. No. Assignment stores a value. `print()` produces visible output.
3. `ROUTE VERIFIED`, because the later print uses the latest assigned value.
4. Variable use demonstrates storage and reuse. Hardcoded output can look correct while bypassing the target skill.

## Expected primary output

```text
DROWNED ARCHIVE
LOCAL SURFACE 3
```

## Expected transfer output

```text
DROWNED ARCHIVE
ROUTE VERIFIED 3
```

Only the second line changes because only `signal_label` is reassigned, and the first print reads `site_name`.

## Mastery rule

Require:

- primary validator: 8/8;
- transfer validator: 8/8;
- both transfer lines predicted before Run;
- all four retrieval answers correct;
- no critical misconception tag unresolved.

The reference files are remediation artifacts, not first-attempt templates.

