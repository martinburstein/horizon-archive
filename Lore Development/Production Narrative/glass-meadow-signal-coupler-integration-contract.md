# Glass Meadow Signal-Coupler Integration Contract

Status: production-ready surface contract for the first visible Terminal. This file is an implementation overlay on `glass-meadow-dual-node-interaction-packet.md`; where the older packet describes a freestanding Petal body, translated Petal writing, or a physical Petal state change, this contract supersedes it. Lesson order, evidence, Route Marker gating, safe dismissal, and recovery behavior remain unchanged.

Runtime alignment:

- scene: `meadow`; hotspot ID: `primary`
- exercise: `terminal-l0101-independent-run`; lesson: `L-01-01`
- production art: `Concept Art Book/production-pixel/AB-01/signal-coupler/production/`
- playable overlay: `640 x 360`, six frames, fixed body, screen-only animation

## Naming and evidence boundary

`Signal Coupler` is an internal production label. It does not confirm a Builder name, a signal's sender, a native occupation, or the artifact's complete function.

Use these player-facing terms:

- initial objective: `Terminal`, the Pilot's broad working class;
- hotspot/accessibility label: `field-linked Terminal`, a description of visible continuity;
- Pilot observation after inspection: `one exposed part of a wider system`, explicitly an inference;
- curriculum workspace: `First Signal`, the human lesson name.

Do not present `signal coupler`, `diagnostic organ`, `data bus`, `network`, `buried Machine`, or `control surface` as confirmed player-facing fact. Those remain production shorthand until later evidence earns a narrower reading.

## Visible anatomy: observation, not explanation

The player may observe:

- a low asymmetric body interrupted by cold glass fins;
- paired sleeved channels that continue through both side edges and/or pass beneath the field;
- one separate conduit entering the substrate beside the body;
- a broad laminated front projection with no tube attached to it;
- an off-center clipped membrane cycling through six abstract states;
- a body, channels, fins, collars, projection, ground contact, lighting, and shadow that remain identical throughout the loop.

The player may not yet conclude what the channels carry, how far they travel, whether they join the visible Route Marker, whether the membrane is language, whether the cycle is a warning, whether the object initiated the opening signal, or whether any active agent remains.

The front projection is not a tongue, ramp, path, tray, keyboard, or human approach surface in player-facing copy. Describe it only as a `front laminate` if inspection requires a noun. Nothing branches from it.

## Animation and causality lock

The six membrane states are ambient evidence, not conversation. They cycle before approach, while the First Signal workbench is open, after validation, after acknowledgement, on return to the completed meadow, and in reduced-motion form as one representative still.

Only membrane pixels may change. No line may claim that the housing brightens, settles, wakes, pulses, answers, confirms, remembers, recognizes, accepts, rewards, or displays a completion mark. First Signal completion is stored by the human expedition layer. The only required physical progression response is the separate Route Marker changing from prerequisite-locked to awake after acknowledgement.

Temporal correlation is permitted as evidence: the Route Marker changes after a verified transfer. Intent, audience, and direct causation remain unresolved.

## First-45-second discoverability

The approved opening objective remains:

- `SYSTEM // EXPEDITION STATE`: `Objective: Find a Terminal in the Glass Meadow.`
- persistent text: `OBJECTIVE // FIND A TERMINAL`

The coupler's scale and cross-frame channels make it the first visual anchor. Do not add a beacon, arrow, label, projected text, voice, or new glow to explain it. Pointer focus and keyboard focus may expose the accessible name `field-linked Terminal`; this is interface mediation, not native labeling.

The first inspection should revise the player's category from `standalone object` to `exposed system part` without explaining the larger system. That is the full narrative payload of the new anatomy in the opening.

## Production copy deck

### Arrival hold

Do not name the artifact before the player inspects or focuses it. The Chapter I arrival line and initial objective already carry the allowed opening facts.

### `LOOK AT` before acknowledgement

`PILOT // FLIGHT RECORDER`:

> Cold glass fins rise from a dark housing. Paired channels continue under the field and beyond sight.

Optional second inspection:

> Not a console. One exposed part of something I can't see.

The second line is Pilot inference, not Scene fact.

### `TALK TO`

`PILOT // FLIGHT RECORDER`:

> First signal. Mine, not yours.

`SCENE // SENSOR RECORD`:

> No voice separates from the wind. The membrane continues its prior cycle.

Talking changes no world, exercise, evidence, or animation state.

### `USE` first open

`EXPEDITION LINK // LOCAL TOOLING`:

> First Signal active. Edit the file, run it, then review the output.

`SCENE // SENSOR RECORD`:

> Six abstract states repeat on the membrane. No text resolves.

Do not render a `LOCAL SURFACE // PROVISIONAL TRANSLATION` line for this screen. The visible topology has not been translated. The dark Python workspace is human expedition mediation reached through the contact, not an interface visibly written into the artifact.

### Close, reopen, and reload

Preserve the existing System-owned session/evidence copy. Replace body-response clauses with:

- close unfinished, `SCENE // SENSOR RECORD`: `The workbench closes. The membrane cycle continues unchanged.`
- reopen, `SYSTEM // EXPEDITION STATE`: `First Signal restored. Code, result, and current hint level are unchanged.`
- reload, `SYSTEM // EXPEDITION STATE`: `Application session restarted. First Signal opens from a clean working file.`
- reload evidence boundary, `SYSTEM // EXPEDITION STATE`: `Attempt and hint-use evidence remain; learner code was not saved.`

No state is the object forgetting or remembering.

### Valid run before acknowledgement

- `EXPEDITION LINK // LOCAL TOOLING`: `Output verified. Review all three lines, then acknowledge completion.`
- `SCENE // SENSOR RECORD`: `The membrane keeps its prior cycle. The Route Marker remains dark.`

### First Signal acknowledgement

- `EXPEDITION LINK // LOCAL TOOLING`: `First Signal mastery evidence recorded. Learner code was not retained.`
- `SCENE // SENSOR RECORD`: `The coupler remains unchanged. The Route Marker gains a pale core and one short groove.`
- `PILOT // FLIGHT RECORDER`: `The small node changed after the transfer. That supports a sequence, not an audience.`

Do not show a translated confirmation on the coupler membrane.

### After acknowledgement

`LOOK AT`, `SCENE // SENSOR RECORD`:

> The same six-state membrane cycle continues. The side channels remain beyond sight.

`USE`, `SYSTEM // EXPEDITION STATE`:

> First Signal is complete. The awake Route Marker owns the next lesson.

`TALK TO`, `PILOT // FLIGHT RECORDER`:

> One output verified. I won't turn that into a conversation.

These actions do not reopen First Signal, repaint the coupler, reset evidence, or change the Route Marker.

### Return to completed meadow

The coupler restores in the same ambient screen cycle or reduced-motion still. This is visual continuity, not a fresh response. Retained completion belongs to expedition evidence; no new local-surface line is emitted.

## Runtime reconciliation handoff

The following current strings conflict with the production asset and this surface contract:

| Current runtime copy or label | Required replacement |
|---|---|
| hotspot `Petal terminal` | `field-linked Terminal` |
| `A dormant interface waits inside the crystal bloom. Wake it with one line of Python.` | `Cold glass fins rise from a dark housing. Paired channels continue under the field and beyond sight.` |
| `The bloom answers in your own alphabet... already listening for you.` | `Output verified. The membrane keeps its prior cycle; reception remains unknown.` |
| `A low marker remains dark. The Petal Terminal must confirm...` | `A low marker remains dark. First Signal must be acknowledged at the field-linked Terminal.` |
| `The route marker refuses the link...` | `Route lesson locked. Acknowledge First Signal at the field-linked Terminal.` |
| `The Petal Terminal is complete...` | `First Signal is complete. The separate Route Marker now carries the active lesson.` |
| `Nothing here has a mouth. Something still seems to hear you.` | Use the two owner-separated `TALK TO` lines above. |

The runtime's animated image remains decorative to assistive technology because the hotspot supplies the operable name. If a nonvisual description of the artifact is exposed later, it must mention the visible interconnection and stable-body/screen-only cycle without claiming hidden function.

## Validation gates

- The player can classify the target as a Terminal without being told it is standalone, sentient, addressed to humans, or a confirmed signal source.
- Paired channels and the separate ground conduit support `larger system` as a Pilot inference while leaving destination and contents unknown.
- The clean front laminate never receives a tube or human-use explanation.
- All six frames have an identical body; narrative progression never depends on a body animation that does not exist.
- Only the screen changes, and its abstract topology is never treated as readable writing.
- First Signal completion changes expedition evidence and Route Marker availability, not the coupler's ambient cycle.
- Copy ownership remains distinct among Pilot, Scene, Expedition Link, and System.
- LOOK, USE, TALK, close, reopen, reload, acknowledgement, completed inspection, and return remain recoverable and spoiler-safe.
- No line confirms the Machine's nature, a Builder occupation, sender, audience, consciousness, disappearance, or prior human contact.

