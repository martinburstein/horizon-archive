import test from"node:test";import assert from"node:assert/strict";import p from"../../curriculum/lessons/L-06-01/reference_primary_answers.json"with{type:"json"};import t from"../../curriculum/lessons/L-06-01/reference_transfer_answers.json"with{type:"json"};import{evaluateObjectiveLedgerScenario,objectiveLedgerPrimary,objectiveLedgerTransfer,sanitizeObjectiveLedgerEvidence}from"../src/objectiveLedgerExercise.js";test("both ledger forms cover every objective exactly once and pass 30/30",()=>{for(const[form,ss,aa]of[["primary",objectiveLedgerPrimary,p],["transfer",objectiveLedgerTransfer,t]]){assert.equal(ss.length,15);assert.equal(new Set(ss.map(s=>s.topic)).size,15);assert.equal(ss.reduce((n,s)=>n+evaluateObjectiveLedgerScenario(s.id,aa[s.id],form).score,0),30);}});test("confidence cannot forge mastery and private data is stripped",()=>{const s=sanitizeObjectiveLedgerEvidence({exerciseId:"EX-L0601-OBJECTIVE-LEDGER",masteryStatus:"mastered",confidence:"high",examItemText:"x",credential:"x",endpoint:"x",serviceData:"x",notes:"x",externalActionRequest:"x",response:"x",choices:"x",freeText:"x"});assert.notEqual(s.masteryStatus,"mastered");for(const k of["examItemText","credential","endpoint","serviceData","notes","externalActionRequest","response","choices","freeText"])assert.equal(k in s,false);});
