import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";import test from "node:test";import { CAPPED_RETURN_PRISM_REGISTRY,auditCappedReturnPrism,deriveCappedReturnPrismState } from "../src/threeCurrentHosts.js";const decoded={complete:true,naturalWidth:1920,naturalHeight:1080};
test("Host 24 freezes accepted source provenance copy and six layouts",()=>{assert.deepEqual(auditCappedReturnPrism(),{source:true,provenance:true,layouts:true,copy:true});assert.equal(CAPPED_RETURN_PRISM_REGISTRY.layouts.length,6);});
test("Host 24 fails closed on registry mutation",()=>{for(const mutate of[c=>{c.source.bytes++;},c=>{c.provenance.sha256="0".repeat(64);},c=>{c.layouts.pop();}]){const copy=structuredClone(CAPPED_RETURN_PRISM_REGISTRY);mutate(copy);assert.equal(deriveCappedReturnPrismState({registry:copy,decodedImage:decoded}),"hidden");}});
test("Host 24 requires exact Full HD decode",()=>{assert.equal(deriveCappedReturnPrismState({decodedImage:decoded}),"available");assert.equal(deriveCappedReturnPrismState({decodedImage:null}),"hidden");});
test("Three Current Reach selects Host 24 for non-PY states with Host 23 and released fallback",()=>{const source=fs.readFileSync(path.resolve("src/ThreeCurrentReach.jsx"),"utf8");assert.match(source,/!host23NativeActive/);assert.match(source,/host24NativeActive \? cappedReturnPrismImage : host23NativeActive \? threeThroatReplicaBloomImage : worldImage/);assert.match(source,/data-capped-return-prism-source=/);});
test("Host 24 preserves forms actions focus and return scene",()=>{const source=fs.readFileSync(path.resolve("src/ThreeCurrentReach.jsx"),"utf8");assert.match(source,/renderForm/);assert.match(source,/state\.availableActions/);assert.match(source,/focusIntent/);assert.match(source,/showThreeCurrentReach/);});
