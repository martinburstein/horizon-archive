# Chapter II Safe-Return Accessibility Gate

**Verdict: PASS — the live-evidence blocker is closed.** The reversible Chapter II -> completed Meadow -> Chapter II path now passes native geometry, discoverability, pointer/USE activation, focus transfer, chapter status, both-side reload, state restoration, privacy, mastery, non-color, reduced-motion, and exact containment gates. No runtime correction is required.

This is an integration PASS, not a WCAG conformance claim. The approved preview could not synthesize Tab movement, and no human screen reader was attached; physical keyboard traversal and actual spoken announcement order remain manual risks.

## Tested modes and viewports

- Rebuilt approved in-app preview at exact `320 x 240`.
- Rebuilt approved in-app preview at `640 x 480`.
- Pointer activation of the persistent lower return action.
- LOOK AT and USE behavior on the secondary ridge hotspot.
- Direct return, re-departure, and one reload/resume on each side.
- Current source/test contracts for native button order, focus, live status, state preservation, privacy, and mastery.
- Focused tests and production build: pass per coordinator evidence.

## Round-trip stages

1. **Chapter II discovery — healthy.** LOOK AT the ridge produces explicit direction to the persistent lower `Return: Glass Meadow` action. The ridge remains a secondary world target and the named lower action remains available without relying on the transient label or color.
2. **Exact `320 x 240` ridge geometry — healthy.** The page is exactly `clientWidth = scrollWidth = 320` and `clientHeight = scrollHeight = 240`; the scene is `(0,0)..(320,180)`. The ridge is `(0,125.4375)..(48,179.1875)`, exceeding `44 x 44`. Its visible 8 px/8 px label is `(3.8,121.7875)..(44.2,175.3875)`, wholly inside the scene. Source keeps the focus outline inset 3 px.
3. **Exact `320 x 240` lower return action — healthy.** The action is `(210,187.2)..(316,211.2)`, exactly 106 x 24 px, with 8 px/8 px type, a 24 px minimum, square corners, and no shadow. It remains inside the 60 px lower band and the exact page bounds.
4. **`640 x 480` ridge and action — healthy.** The scene is `(2.725,2.0375)..(637.275,358.975)`. The ridge is 95.175 x 106.6 px. Its label stays within the scene at `(10.2625,243.525)..(90.3625,349.825)` with 8 px/8 px type. The return action is 210.1875 x 47.5875 px and retains 8 px type plus the 24 px minimum contract.
5. **Return activation and focus — healthy.** Both the lower return button and USE on the ridge restore the completed/crowned Meadow, populate the polite Chapter I status, and focus the destination-aware forward departure action. The restored completed marker measures 69 x 83.125 px at narrow presentation and does not regress to an earlier state.
6. **Returned-side reload — healthy.** Reload/resume preserves exact `320 x 240` containment, the completed Meadow, Chapter I status, and forward-departure focus. The corrected resume path now repopulates the Chapter I announcement instead of clearing it.
7. **Re-departure and Chapter II reload — healthy.** Re-departure revisits Chapter II without skipping ahead, populates the Chapter II status, and focuses the primary Workload Terminal. Chapter II reload/resume preserves its status, primary focus, return action, and page containment.
8. **Mastery, privacy, recovery, color, and motion — healthy.** The navigation patch remains strictly gated by completed Meadow route mastery and contains no scoring/evidence fields. Existing evidence survives unchanged while private working source, traceback, output, notes, and temporary session containers are not reconstructed. A safe forward route always remains. Text names every navigation state independently of color, focus is visibly inset, and no animation or timed input was added.

## Closed blocker and remaining manual risks

- **Live geometry/reload blocker — closed.** Both target rectangles, both label rectangles, page containment, return mechanisms, focus targets, statuses, completed-marker restoration, and reload sides are now observed.
- **Physical keyboard traversal — manual risk.** The approved backend did not synthesize Tab movement. DOM order, native buttons, Enter/Space semantics, and deterministic focus wiring remain covered by source/tests, but one physical keyboard pass is still required before a conformance claim.
- **Screen-reader spoken order — manual risk.** Live status content and resulting active elements are verified, but NVDA/equivalent speech sequencing was not observed.
- **Additional conformance risks:** Windows forced colors, 200% zoom, and physical switch-control remain unobserved.

## Handoff

Accept and preserve the current safe-return implementation: exact ridge geometry, contained 8 px label, 24 px named lower action, non-color wording, inset focus, Chapter I/II status restoration, deterministic focus, completed-marker state, evidence isolation, and direct motionless round trip. No Coder change is required. Schedule one human keyboard plus NVDA/equivalent pass, forced-colors check, 200% zoom check, and physical switch-control observation before making an accessibility-conformance claim.
