# Opening objective, cultivated coupler, and SIM-03 accessibility review — 2026-07-13

**Outcome: PASS after a bounded runtime correction.** The opening objective no longer obstructs the Glass Meadow, the cultivated signal coupler remains correctly represented by scene and interaction semantics, and SIM-03 now clears its invalid-field, focus, native-control, strict-mastery, privacy, and exact-frame gates. A fresh live narrow check found and closed one additional clipped-resume defect. This is not an accessibility-conformance claim.

## Opening objective and cultivated coupler

- The first-arrival objective is visible in the established lower dialogue band and uses its existing polite status behavior. Fresh entry and an incomplete saved return restore the same bounded guidance without adding a scene overlay.
- The complete field-linked Terminal remains unobstructed. Its focusable hotspot owns the action name and state; the production coupler picture, including the new cultivated route organs, remains decorative and pointer-transparent inside the already-described first-person landscape. The image does not create a duplicate keyboard stop or duplicate alternative text.
- Reduced-motion source selection still replaces the six-frame membrane loop with the static production PNG. The body and cultivated organs remain identical in every frame; no new motion or interaction was introduced by the art revision.

## SIM-03 corrections

- Every failed entry prerequisite now marks its native control with `aria-invalid="true"` and associates a specific visible repair message. The shared source-freshness failure describes both related date inputs rather than silently blaming one date.
- The polite, atomic gate summary reports the number of failed prerequisites while the associated list provides the details. One-scenario scoring retains independent decision/reason invalid states, specific descriptions, neutral System scoring, Teacher remediation, and blank retry.
- Invalid submission moves focus to the first failed prerequisite. A valid gate moves focus to the optional timer toggle; next-item, blank-retry, and final-summary transitions move focus to the new meaningful control or heading. The final acknowledgement is disabled until confidence is chosen.
- Checkboxes and radios in this workspace now render at least `24 x 24 px`; native number/date/select controls retain their `24 px` floor and visible inset focus. Timing and confidence remain optional and separate from correctness.
- Required scene progression retains the established `continueButtonRef` and post-SIM-01 focus priority. The adjacent optional SIM-03 launch/resume is reachable in DOM order without competing for automatic focus, and opening it still begins the dialog's deterministic focus lifecycle. At exact `320 x 240`, the resume action's prior rectangle was `x=56..244, y=235.56..259.56` and was clipped by the fixed host. The corrected compact lower-band treatment retains the status text for assistive technology, hides only its narrow visual duplicate, and contains the resume action at `x=6..231.5, y=211..235` with a `24 px` height.

## Live evidence

- At `640 x 480`, the page and scroll extent are exactly `640 x 480`; the dialog is `x=10.64..629.34, y=9.97..350.06`; and the single scrolling workspace is contained at `x=11.64..628.34, y=153.73..349.06`.
- At exact `320 x 240`, the page and scroll extent are exactly `320 x 240`; the dialog is `x=4..316, y=4..175`; and its workspace is contained at `x=5..315, y=77..174`.
- The incomplete default gate exposes eight invalid native controls because the shared source-freshness check covers two dates. All eight have existing descriptions and dimensions of at least `24 x 24 px`; focus lands on the L-06-03 readiness select.
- A valid gate focuses the optional timer. A deliberate one-dimension item miss exposes exactly one invalid select with associated feedback and a polite `1/2` status. `Retry with blank choices` clears both working choices and focuses the first blank select.
- Closing at narrow size produces the fully visible `Resume Final Confidence · item 1/12` action above. The focused live run used reduced-motion emulation and reported zero page or console errors.

## Validation

- Focused opening, SIM-03, and coupler tests: `18/18` passed.
- Complete game unit suite: `232/232` passed.
- Production build passed with only the existing large-chunk advisory.
- Fresh focused Playwright pass covered invalid and valid entry gates, native date/number/select/checkbox controls, error associations, focus transitions, one-scenario remediation, blank retry, exact `640 x 480` and `320 x 240` containment, the longest resume label, reduced motion, and runtime errors.
- A coordinator full-E2E follow-up caught an initial optional-action focus-priority regression. The final source contract explicitly leaves the optional button unreferenced and focuses required scene progression through `continueButtonRef`; the focused regression protects this exact ordering.

## Remaining manual risk

Human NVDA/equivalent spoken order, physical keyboard and switch-control traversal, Windows forced colors, and 200% browser zoom remain unobserved. The dialog focus trap and complete DOM order are source- and automated-focus-confirmed, but those platform observations are still required before any conformance claim.

**Status: PASS — bounded opening/SIM-03 accessibility gate cleared after correction.**
