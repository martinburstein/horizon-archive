# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`EVIDENCE LOCALIZATION CORRECTION COMPLETE / RETURN TO
FRESH MISSION / FRCE-003-v1-VR-12`**

Release state: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Exact next owner: **fresh Mission Captain**

Exact diagnostic-control candidate:
`ce7c9abbaf1d0ffad8c1031f0398750676d4970e`

Exact immutable content candidate:
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`

Validation control: `4cd7fbf31291671dd28c0743b44a7c49aaad82bb`

Accepted evidence predecessor:
`ca89a679195c11d441a76e6c02983a6436f2ccb2`

Read the Mission Captain profile in full, `FRSH-003-v1-VR-12`, current
`FRAB-003-v1`, complete `FRCE-003-v1-VR-12`, operative `FRSH-003-v1-VR-07`,
and the exact committed E2E/FRRC/static-test controls at candidate `ce7c9ab`.
Independently inspect whether the correction exactly satisfies VR-12 without
changing product, candidate lineage, thresholds, summary/verifier semantics,
or the one-run rule.

Combat committed exactly three control files. The E2E now synchronously writes
machine-owned `first-run-live-diagnostic.json` before focus/layout aggregate
throws, preserves all six raw layout records, emits exhaustive deterministic
required-versus-emitted checks with actual/expected/pass/owner, records every
sorted false path by layout, and uses concise failure transport. FRRC-002
freezes diagnostic ownership, failure capture/retention, cleanup, non-release
status, and no verifier after a failed E2E. The existing static test proves
write/throw/summary separation without changing the focused count.

Combat proof passed exact ancestry/three-file/product-blob boundaries,
`git diff --check`, JSON parse, `node --check`, and the exact focused command
`68/68` (`207.1096ms`). Combat ran no E2E, diagnostic execution, preview,
external root, related/full/validator/build, browser, served request, PBA/media,
summary, verifier, or cleanup command.

Mission may perform only read-only candidate/diff/blob/static inspection and
issue one versioned `HOLD` or `READY` adjudication routing the earliest exact
owner. Mission may not execute or authorize work implicitly. Any later sole
E2E requires explicit new versioned Mission authority and must preserve the
frozen product `a91763e`, validation `4cd7fbf`, predecessor evidence
`ca89a679`, all VR-07 thresholds, external-root containment, and the
diagnostic's failure-side-only status.

Do not inspect, enumerate, reuse, mutate, move, or delete the disclosed
predecessor root. Protected repository QA, PDF, training, browser/profile/save,
hidden lore, media, and user state remain forbidden. Do not repair product,
run E2E, begin Quartermaster/Image/Intelligence, add Host 06-15/City/RP-013/
successor/post-ending content, create a reveal or schedule, advance maturity,
or call `FIRST RUN COMPLETE`.
