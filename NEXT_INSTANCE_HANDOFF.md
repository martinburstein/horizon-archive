# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`HOLD / PRE-CALL BUILT-IN DATA-URL MATERIALIZATION
ADAPTER UNAVAILABLE / ORDINALS 2-3 PRESERVED / MISSION ADJUDICATION NEXT`**

Martin's controlling decision: **`Authorized new Drowned Media for Host 06`**

Current Quartermaster return: `FRCA-005-v2 / HOLD`

Prior Quartermaster return: `FRCA-005-v1 / HOLD`

Current Work Order / viability: `FRWO-005-v3` / `FRVE-005-v3`

Prior viability / effective Science variances: `FRVE-005-v2` /
`FRVE-005-v2-VR-01` / `FRVE-005-v2-VR-02`

Base shell / effective Mission variances: `FRSH-005-v1` /
`FRSH-005-v1-VR-01` / `FRSH-005-v1-VR-02` /
`FRSH-005-v1-VR-03` / `FRSH-005-v1-VR-04` /
`FRSH-005-v1-VR-05` / `FRSH-005-v1-VR-06`

Combat functional close: `FRCE-005-v1-VR-04 / PRODUCTION FUNCTIONAL`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Frozen candidate tree:
`09da6293d72c8123b8d9673bd8e41329338e8d13`

Immutable accepted-media manifest: `FRAM-001-v1`

Manifest file SHA-256:
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`

Manifest identity: exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next owner: **one fresh Mission Captain / `mission_captain`**

## Quartermaster result

Quartermaster independently proved exact synchronized intake at
`1352d08316cd813e1770d118d850bde397156fcb`, exact frozen candidate/tree,
immutable manifest file and tuple identity, absent product raster/provenance,
and the unchanged null/false registry, seven null copy slots, and one null alt
slot.

Quartermaster then proved that the exact adapter frozen by
`FRSH-005-v1-VR-06` is unavailable in the installed built-in tool boundary:

- the built-in generator is callable inside a non-Node V8 tool-orchestration
  isolate with no filesystem primitive;
- that isolate could validate one direct `image_url` value but could not
  create or stream to `attempt-02.png`;
- every filesystem-capable tool is a separate transport, so carrying the full
  data URL/base64 to shell, patch, or Node-REPL would create the explicitly
  forbidden second full-payload transport copy;
- reading/parsing `output_hint` would violate the exact opaque-field rule; and
- managed discovery, browser download, emitted image, or silent CLI/API
  fallback is forbidden.

Because the shell requires parser/adapter identity before the call and makes
an unavailable primitive fail closed, Quartermaster did **not** invoke ordinal
`2`. No result, payload, temp child, candidate, image, pixel, product,
provenance, registry, copy, test, build, E2E, or reveal exists from this stage.

## Exact ordinal and residual state

- Total ordinal domain remains exactly `{1,2,3}`.
- Ordinal `1` remains permanently consumed by the historical opaque
  result-path stop.
- Ordinal `2` remains unconsumed and may not run under the current HOLD.
- Ordinal `3` remains unconsumed and conditional on a future lawful ordinal-2
  objective rejection plus exact temp cleanup.
- Built-in calls in `FRCA-005-v2`: exact `0`.
- Conservative possible ordinal-associated managed residual count remains
  exact `1`, associated only with ordinal `1`.
- The residual remains **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
  NON-GATING / OPAQUE BUILT-IN MANAGED RESIDUAL`** and was not inspected or
  changed.
- VR-65 remains separate, opaque, non-gating, and inaccessible.

No role may infer that this HOLD consumed ordinal `2`, produced a candidate,
or authorized output-hint parsing, managed discovery/cleanup, second payload
transport, CLI/API fallback, or a generator call.

## Exact next action

One fresh Mission Captain reads this handoff; complete `FRCA-005-v2`; the full
Mission profile; complete `FRWO-005-v3`; complete `FRVE-005-v3`; complete
prior `FRVE-005-v2` and effective variances; complete `FRSH-005-v1` through
`FRSH-005-v1-VR-06`; and the installed image-generation skill's built-in
save-path and no-silent-CLI-fallback rules.

Mission issues exactly one versioned `FIRST RUN SHELL READY`, `REVISE`, or
`HOLD` adjudication for the unavailable same-isolate materialization adapter.
It must decide whether the question requires:

1. a complete Operations -> Science -> Mission return for a narrowly explicit
   managed-output pathname/result-discovery contract that never infers, lists,
   or broadly cleans the managed parent; or
2. a terminal request for Martin's explicit CLI/API fallback authorization,
   because authorization of new Drowned media does not silently authorize the
   skill's fallback mode.

Mission cannot create the missing primitive, use `output_hint`, consume
ordinal `2`, invoke generation, discover/clean managed output, transport a
payload, authorize CLI/API for Martin, inspect pixels, import media, write
copy/provenance, begin Image, run E2E, reveal, advance maturity, close an OPEN
record, inspect VR-65, schedule, automate, release, or call `FIRST RUN
COMPLETE`.

## Preserved gates and state

Inherited functional gates remain frozen and were not replayed: focused
`50/0`, legacy static-contract `29/0`, learning/privacy `17/0`, related
`58/0`, validators `40/40`, cold full `979/0/0`, production and TD-012
fixture builds PASS, production PBA JavaScript `1,676,508`, CSS `119,394`,
modules `217`, accepted media `17 / 37,410,731`, source maps `0`, served
preflight PASS, owned process/port cleanup PASS, and `git diff --check` PASS.
Complete E2E remains correctly unrun.

Maturity remains unchanged. `FR-03` remains continuity `FR2`; physical-host
expression `FR0 - 1 accepted shared compression / 1 exact / 10 missing`;
learning `FR2`; behavior/save/recovery `FR1`; content `FR2`; presentation
`FR3`; and prior bounded release proof `FR4`.

All thirteen inherited process records remain separate and **OPEN**: VR-17,
VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage
4, VR-60 Call 9, and VR-61. The separate Commandant filename/search-scope
record remains **OPEN**. This stage closes none.

Repository QA quarantine, protected PDF, training directory, real browser /
profile/save, hidden lore, user work, accepted-media pixels, managed/temp
parents, and opaque residuals remain protected. No media operation, preview,
Markdown image, screenshot, reveal, or publication occurred.
