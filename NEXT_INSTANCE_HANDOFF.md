# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`HOLD / FRVE-005-v1 / BUILT-IN MANAGED-INGRESS CONTRACT NOT AUTHORIZED`**

Martin's controlling decision: **`Authorized new Drowned Media for Host 06`**

Decision authority: `FRPB-001-v2`

Continuity authority: `FRCL-004-v2`

Selected Work Order: `FRWO-005-v1 / Stranded Lens Cradle - One New Source and Fixed Lesson Integration`

Science return: `FRVE-005-v1 / HOLD / BUILT-IN MANAGED-INGRESS CONTRACT NOT AUTHORIZED`

Exact next owner: **fresh Operations Planning Major / `operations_planning_major`**

Immediate control: **one bounded `FRWO-005` revision or `HOLD` decision only**

Science source inspected: `22dd9de3f8e6241cb6c3c38a8e31c2c925876ab0`

Current control source: **the dedicated Science HOLD commit containing this handoff; verify exact local `HEAD` before Operations action**

Remote synchronization reference: `origin/main` remains `d6e2386a4a00fa829f246d6c32545232d56c6fcc`; the planning HOLD chain, Commandant reopen, Colonel lock, Operations Work Order, and Science HOLD are intentionally unpushed.

Founding product baseline: `FRPB-001-v1 / FIRST RUN VISION BASELINE`

Reopened product baseline: `FRPB-001-v2 / BOUNDED HOST 06 NEW-MEDIA AUTHORITY`

Current continuity: `FRCL-004-v2 / CONTINUITY LOCK`

Closed Work Order: `FRWO-004-v1`, withdrawn by `FRWO-004-v1-OPH-01 / HOLD`

Historical Science return: `FRVE-004-v1 / HOLD / SOURCE GEOMETRY NOT PROVED`

Released authority: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Current planning controls: `FRRM-005-v1` / `FRSB-005-v1`

Released candidate identities:

```text
geometry=8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97
transport=6c64eb354b7dbb467df5725e2cae4eb67092ddc7
product=a91763e28d488f31f8cf7d40ece0b2682246ba9b
validation=4cd7fbf31291671dd28c0743b44a7c49aaad82bb
diagnostic=2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
evidence=ca89a679195c11d441a76e6c02983a6436f2ccb2
```

## Science decision

`FRVE-005-v1` is **HOLD**, not `POLISH VIABILITY READY`. The built-in image generator does not expose a caller-selected destination, exact format, exact dimensions, or deterministic one-output contract. Its managed `generated_images` location is outside the Horizon workspace and may lawfully serve as one-file, tool-owned ingress without weakening the zero-workspace-entry, no-import, and no-reveal guarantees, but only after Operations freezes that path explicitly. The literal current Work Order instead requires the first generated byte to land in a fresh GUID OS-temporary child and forbids draft/cache placement elsewhere. Science may not silently reinterpret that contract.

This is a **bounded Operations staging and cleanup variance**, not a request for new Martin authority. Martin already authorized exactly one bounded Host 06 source. The proposed revision adds no source, canon, route, lesson, attempt, reveal, access, world response, or ending authority.

The second blocker is the accepted-media manifest gate. Current released controls prove only aggregate accepted media `17 / 37,410,731`; they do not provide all seventeen exact repository paths, byte lengths, and SHA-256 values. Science is prohibited from inspecting media. Operations must move creation of that exact manifest to a read-only pre-generation Combat/manifest gate. No generation may start until the manifest is frozen.

## Exact bounded Operations question

Operations may revise `FRWO-005` only if it freezes all of the following without weakening any existing boundary:

1. One built-in call produces one output at one exact unique tool-returned pathname beneath the managed generated-images location outside the Horizon repository. The output pathname must be returned directly; parent-directory enumeration is forbidden. An unknown, missing, multiple, or in-repository output stops the attempt opaque.
2. The exact returned file is copied to one fresh GUID-named OS-temporary evaluation child. Exact byte length and SHA-256 must match managed ingress to temporary evaluation copy before any rubric check.
3. The exact managed-ingress pathname is deleted immediately after identity proof. Cleanup targets only that exact file. Cleanup failure, missing exact path, or uncertain ownership stops the cycle opaque; it does not authorize parent listing, retry, reveal, import, or workspace entry.
4. Rejected temporary candidates are deleted by exact pathname only and never enter the Horizon workspace, source control, preview, browser, report, or reveal. Attempt results record only pass/fail rubric facts, exact bytes/hash for the selected candidate if any, and opaque failure classification.
5. Attempts remain sequential, one output per call, maximum three total. Output that is not exact opaque sRGB PNG `3840x2160`, exceeds `12,000,000` bytes, or fails any physical/composition/non-overlap/source guard consumes an attempt and is rejected. No CLI/API fallback is authorized.
6. Before attempt one, a read-only Combat/manifest gate records the exact repository-relative path, exact byte length, and SHA-256 for every one of the immutable accepted seventeen media files. The gate must reconcile to `17 / 37,410,731`; mismatch stops before generation. Media pixel inspection remains forbidden.
7. At most one passing candidate is selected. Only its byte-identical copy may enter `Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png`; only its provenance may enter `Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md`. No other new raster or generated draft may enter the workspace.
8. All exact `FRVE-005-v1` physical rubric, six-layout no-crop mapping, source guard, sole L02-03 routing, meaning/accessibility, PBA, validation, no-reveal, rollback, role, canon, learning, save, privacy, world-response, and shared-ending boundaries remain unchanged.

If Operations cannot freeze both the managed-ingress protocol and the pre-generation accepted-media manifest gate, it must issue one terminal HOLD to Martin. If it can, it issues one versioned revised Work Order and returns to a fresh Science context. Mission does not begin from this handoff.

## Frozen conditional envelope

- Candidate: exact opaque sRGB PNG `3840x2160`, maximum `12,000,000` bytes.
- Attempts: maximum three, sequential, exactly one output per attempt.
- Accepted workspace delta: at most one raster path plus its provenance path; no rejected draft workspace entry.
- Media result: maximum `18` files and exactly `37,410,731 + selectedBytes`, never above `49,410,731`.
- JavaScript: affected entry maximum `1,679,393` bytes; global maximum `1,703,258` bytes.
- CSS: affected entry maximum `119,547` bytes; global maximum `119,672` bytes.
- Modules: affected entry maximum `218`; global maximum `222`.
- Runtime: candidate decode <=`250 ms` cold and <=`100 ms` warm; local image-ready <=`750 ms`; attributable CLS <=`0.01` and total CLS <=`0.05`; activation handler <=`2 ms`; sampled task <=`100 ms`.
- Validation calls: focused <=`30 s`, related <=`60 s`, full <=`60 s`, each build <=`60 s`, and one E2E call <=`180 s`.

Current released PBA remains JavaScript `1,667,393`, CSS `119,247`, modules `217`, media `17 / 37,410,731`, and maps `0`.

## Preserved classifications and protected state

All thirteen inherited records remain separate and **OPEN**: VR-17, VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4, VR-60 Call 9, and VR-61. The Commandant-stage broad First Run filename/search-scope record remains one additional separate OPEN record. Science retains the recorded VR-24, VR-23/Commandant-search, and VR-17 process-only recurrences. None is waived, merged, closed, cured, renumbered, or candidate evidence.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY / NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**. Its contents and meaning remain unknown. No residual was accessed, inferred, retried, cleaned, moved, or deleted.

No protected QA file content was opened or used as evidence. All disclosed user, protected, and residual paths remain untouched. The seventeen accepted media files remain immutable. Science performed no image generation, edit, variation, import, copy, movement, deletion, selection, preview, publication, or reveal.

## Exact next action

Fresh Operations reads in full `AGENTS.md`, this handoff, `FIRST_RUN_AGENT_WORKFLOW.md`, the canonical registry, the complete Operations Planning Major profile, `FRPB-001-v2`, `FRCL-004-v2`, `FRWO-005-v1`, and `FRVE-005-v1`. It then issues exactly one bounded revised `FRWO-005` or one HOLD. No Mission, Reconnaissance, Tactical Operations, Combat, Quartermaster, Image Specialist, Intelligence, media tool, preview, reveal, schedule, automation, push, or release action begins from this artifact.
