# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`POLISH VIABILITY READY / FRVE-005-v2 / MANAGED
ONE-FILE INGRESS AND PRE-GENERATION IMMUTABLE 17-FILE MANIFEST GATE VIABLE`**

Martin's controlling decision: **`Authorized new Drowned Media for Host 06`**

Decision authority: `FRPB-001-v2`

Continuity authority: `FRCL-004-v2`

Selected Work Order: `FRWO-005-v2 / Stranded Lens Cradle - One New Source and
Fixed Lesson Integration`

Superseded Science return: `FRVE-005-v1 / HOLD`

Current Science envelope: `FRVE-005-v2 / POLISH VIABILITY READY`

Exact next owner: **fresh Mission Captain / `mission_captain`**

Immediate control: **one bounded `FRWO-005-v2` shell integration only**

Science source inspected: `4c7c8a0d0d238f07db09871061ec05224fb75266`

Current control source: **the dedicated Science commit containing this
handoff; verify exact local `HEAD` before Mission action**

Remote synchronization reference: `origin/main` remains
`d6e2386a4a00fa829f246d6c32545232d56c6fcc`; the planning chain and this
Science READY remain intentionally unpushed.

Released authority: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Planning controls: `FRRM-005-v2` / `FRSB-005-v2`

## Science adjudication

All twelve `FRWO-005-v2` viability questions pass conditionally. The installed
built-in generator's managed `$CODEX_HOME/generated_images/...` location can
serve as exactly one tool-owned ingress before byte-identical transfer to one
fresh GUID-named OS-temp evaluation child. Unknown, missing, multiple,
ambiguous, unowned, nonlocal, in-repository, containment-uncertain, identity-
mismatched, or cleanup-unverifiable state consumes the ordinal and stops the
cycle opaque without discovery, fallback, retry, import, or reveal.

A literal reference traversal from exact runtime entry
`horizon-archive-game/src/main.jsx` yields exactly seventeen unique accepted-
media paths without directory enumeration or media-byte read. `FRVE-005-v2`
freezes those exact literals plus schema
`horizon-archive.first-run.accepted-media-manifest/v1`: Combat hashes raw bytes
from one read-only handle, records same-stream byte count and lowercase SHA-256,
and hashes a canonical NUL/LF tuple stream in fixed ordinal order. The gate must
reconcile exactly to `17 / 37,410,731` before generation. Combat receives no
pixel, generation, selection, import, edit, or cleanup authority.

Every retained `PHY-01..12`, source-band, centered six-layout no-crop,
hotspot/label/center/nonoverlap, fail-closed source/state, sole unchanged
`L02-03`, meaning/accessibility, numeric media/PBA/runtime, provenance,
cleanup/no-reveal, validation, and exact rollback requirement is frozen in
`FRVE-005-v2`.

## Frozen numeric envelope

- Candidate: exact opaque sRGB PNG `3840 x 2160`, maximum `12,000,000` bytes.
- Attempts: maximum three, sequential, exactly one built-in output per call.
- Accepted baseline: immutable `17 / 37,410,731` with exact literal
  path/byte/SHA tuples and canonical tuple-set digest.
- Conditional result: exact `18` files and `37,410,731 + selectedBytes`, never
  above `49,410,731`.
- JavaScript: affected `<=1,679,393`; global `<=1,703,258`.
- CSS: affected `<=119,547`; global `<=119,672`.
- Modules: affected `<=218`; global `<=222`; source maps `0`.
- Runtime: decode `<=250ms` cold / `<=100ms` warm; image-ready `<=750ms`;
  attributable CLS `<=0.01`, total CLS `<=0.05`; activation `<=2ms`; sampled
  task `<=100ms`.
- Validation calls: focused `<=30s`, related `<=60s`, full `<=60s`, each build
  `<=60s`, and one complete E2E `<=180s` with no retry.

Released PBA remains JavaScript `1,667,393`, CSS `119,247`, modules `217`,
media `17 / 37,410,731`, and maps `0`.

## Preserved maturity and protected state

No maturity cell, host inventory, candidate identity, accepted-media tuple, or
release proof advances. `FR-03` remains `1 accepted shared compression / 1
exact / 10 missing`; the forty-host inventory remains `6 exact / 1 accepted
shared compression / 32 missing / 1 unadvanced Witness expression`.

All thirteen inherited records remain separate and **OPEN**: VR-17, VR-23,
VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The Commandant filename/search-scope record remains
one additional separate **OPEN** record. All prior and Science-recorded
process-only recurrences remain recorded; none is waived, merged, closed,
cured, renumbered, or candidate evidence.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**. Its contents and meaning remain
unknown. No protected QA content, accepted-media byte, user/browser/save path,
or residual was opened, inspected, inferred, retried, cleaned, moved, or
deleted.

No image generation, edit, variation, import, selection, preview, publication,
reveal, build, browser, E2E, save, external service, cleanup, schedule,
automation, production, push, release, or manifest byte read occurred.

Focused read-only source/state/access contract tests passed `30/30`; exact
literal runtime traversal yielded `17` media paths from `165` followed source
files without directory enumeration or media-byte read. The official Microsoft
Learn AI-901 objectives remain the April 15, 2026 set; the page reports last
update July 13, 2026.

## Exact next action

One fresh Mission Captain reads in full `AGENTS.md`, this handoff,
`FIRST_RUN_AGENT_WORKFLOW.md`, the canonical registry, the complete Mission
profile, `FRPB-001-v2`, `FRCL-004-v2`, complete `FRWO-005-v2`, complete
`FRVE-005-v1`, and complete `FRVE-005-v2`, plus only its exact lawful current
controls.

Mission issues exactly one new versioned `FIRST RUN SHELL READY`, `REVISE`, or
`HOLD` artifact. It must copy the seventeen literal paths exactly; freeze the
machine manifest schema/hash semantics, exact managed root and call-result
adapter, prompt version, scalar containment/identity/cleanup controls, role
partition, budgets, validation, rollback, and no-reveal boundary; and preserve
all canon, learning, route, save, world, ending, process, and protected-state
controls.

No Reconnaissance, Tactical, Combat, Quartermaster, Image Specialist,
Intelligence, manifest byte read, media tool, candidate inspection, production,
reveal, schedule, automation, push, release, maturity update, OPEN-record
closure, residual access, or `FIRST RUN COMPLETE` begins from this handoff.
