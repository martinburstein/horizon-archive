# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`PRODUCTION FUNCTIONAL / FRCE-007-v1 / HOST 07 INERT
FAIL-CLOSED / FIVE CLI ATTEMPTS UNCONSUMED`**

Exact next owner: **one fresh Quartermaster / `quartermaster`**

Combat candidate `5164437a199ec101cb638ad0599715e77d563a72`, tree
`02c2cbbdfacad67c8cee86eeba8172ab2b450e81`, is functionally green. Full suite
`987/987`, related `64/64`, production `219` modules, TD-012 `57`, PBA
JS `1,687,754`, CSS `119,394`, maps `0`, accepted media `17/37,410,731`, served
root/deep/JS/CSS `200`, and port `4173` clear. Candidate fields remain null and
no Host 07 import exists.

Martin authorized the installed imagegen CLI fallback for exactly five total
Host 07 attempts. `{H7-1,H7-2,H7-3,H7-4,H7-5}` remain unconsumed. Read the full
Quartermaster profile and imagegen skill, `FRSH-007-v1`, `FRDT-007-v1`,
`FRPX-007-v1`, `FRCE-007-v1`, all five immutable prompts, exact source guard,
accepted-media manifest, and CLI/custody/measurement law.

Prove Git/source/prompt/CLI/key-boolean/manifest/scratch/product absence. Create
only the frozen scratch root immediately before H7-1. Launch one exact CLI
process for H7-1. Continue to the next fixed ordinal only after objective
rejection plus exact identity-conditioned cleanup. Nonzero CLI exit stops.
First PASS stops. No sixth call or rerun exists.

Privately validate exact ordinary/one-link/bytes/SHA/PNG/decode, ten physical
predicates, FRVE normalized geometry, six layouts, accessibility, and truthful
alt. Never reveal a rejected candidate. On PASS, import byte-identically,
write provenance, populate only declared fields/copy/alt, run the content
validation ladder, and issue `PRODUCTION CONTENT COMPLETE`; otherwise HOLD.

Preserve exact Hosts 01-06/media, L02-03, L03-01/L03-02, one-path rail,
learning/privacy/save/offline/accessibility rules, invariant world, RP-012,
`successor=null`, every OPEN process record, both filename-enumeration records,
protected exclusions, and VR-65. Do not inspect protected paths or reveal Host
08/later work.
