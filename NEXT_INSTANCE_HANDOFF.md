# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`POLISH VIABILITY READY / FRVE-005-v10 / PT11
DICTIONARY METHOD DEFECT REPRODUCED / FORWARD-COMPATIBLE PARSER 15 OF 15 /
MARTIN APPROVED ONE FURTHER ATTEMPT / FRESH OPERATIONS NEXT`**

Exact next owner: **one fresh Operations Planning Major /
`operations_planning_major`**

## Science result

The parser called `.Contains(key)` on generic dictionaries returned by
PowerShell 5.1's JSON deserializer; the host cannot resolve that method. Exact
case-sensitive key-set membership fixes the deterministic PT11 defect.
Official ImagesResponse fields and API compatibility also require tolerating
bounded future metadata while still rejecting non-null URL/revised-prompt,
extra outputs, type confusion, duplicates, malformed/canonical-payload
failures, and wrong known echoes. The corrected semantic fixture passed
15/15 with zero API/media activity.

## Exact next action

Fresh Operations issues one separately versioned finite no-retry attempt under
Martin's explicit approval, with fresh paths, the dictionary-safe parser,
corrected 4K budgets, and all unchanged Host 06 controls. Route fresh Mission
for new retained sources, synthetic parser proof, a credential-cleared fixture,
and a new shell. No current call is authorized.

All protected paths/state, canon, route, lesson, save/privacy, MH-40, RP-012,
null deltas, and `successor=null` remain unchanged. No reveal is authorized.
