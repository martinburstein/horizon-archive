# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`FIRST RUN SHELL READY / STRICT PNG DATA-URL INGRESS
AND BOUNDED OPAQUE BUILT-IN MANAGED RESIDUAL POLICY FROZEN / QUARTERMASTER
ORDINAL 2 NEXT`**

Martin's controlling decision: **`Authorized new Drowned Media for Host 06`**

Current Work Order / viability: `FRWO-005-v3` / `FRVE-005-v3`

Prior viability / effective Science variances: `FRVE-005-v2` /
`FRVE-005-v2-VR-01` / `FRVE-005-v2-VR-02`

Base shell / effective Mission variances: `FRSH-005-v1` /
`FRSH-005-v1-VR-01` / `FRSH-005-v1-VR-02` /
`FRSH-005-v1-VR-03` / `FRSH-005-v1-VR-04` /
`FRSH-005-v1-VR-05` / `FRSH-005-v1-VR-06`

Combat functional close: `FRCE-005-v1-VR-04 / PRODUCTION FUNCTIONAL`

Prior Quartermaster return: `FRCA-005-v1 / HOLD`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Frozen candidate tree:
`09da6293d72c8123b8d9673bd8e41329338e8d13`

Immutable accepted-media manifest: `FRAM-001-v1`

Manifest file SHA-256:
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`

Manifest identity: exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next owner: **one fresh Quartermaster / `quartermaster`**

## Mission decision

`FRSH-005-v1-VR-06` reaches `FIRST RUN SHELL READY` for the prospective
strict PNG data-URL correction authorized by `FRWO-005-v3` and independently
validated by `FRVE-005-v3`. It replaces only the effective shell's superseded
direct-`output_hint` managed-path ingress, managed-file-to-temp identity,
mandatory managed-file deletion, and ordinal-1 opaque-stop continuation rules.
Every other shell field and effective variance remains exact.

Ordinal `1` remains permanently consumed and is not candidate or
`PHY-01..12` evidence. The remaining ordinal set is exactly `{2,3}` in that
order, with maximum future calls `2`. Quartermaster begins at ordinal `2`.
Ordinal `3` is permitted only after ordinal `2` becomes a fully identified
objective rejection and its exact temp candidate plus GUID child are deleted
and proved absent. A transport/identity/allocation/write/cleanup stop, a
selected ordinal-2 candidate, or any other terminal condition forbids ordinal
`3`. No fourth call or retry exists.

Each consumed ordinal is conservatively charged at most one possible opaque
tool-owned managed residual. Ordinal `1` already counts as possible residual
`1`; consuming ordinals `2` and `3` raises that conservative count to at most
`2` and `3`. Maximum ordinal-associated count is `3`. Every possible residual
is exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY / NON-GATING /
OPAQUE BUILT-IN MANAGED RESIDUAL`** with zero project, candidate, provenance,
evidence, reuse, reveal, rollback, canon, maturity, or release authority. No
role may discover, inspect, infer, copy, move, delete, clean, or prove absence
of it. VR-65 remains separate and inaccessible.

## Exact next action

One fresh Quartermaster reads this handoff; complete `FRWO-005-v3`; complete
`FRVE-005-v3`; complete prior `FRVE-005-v2` and its effective variances;
complete `FRSH-005-v1` through `FRSH-005-v1-VR-06`; complete
`FRDT-005-v1`; complete `FRPX-005-v1`; complete `FRCE-005-v1-VR-04`;
complete `FRCA-005-v1`; the full Quartermaster profile; immutable
`FRAM-001-v1`; and the installed built-in image-generation save-path policy.

Quartermaster first independently proves exact Git synchronization, frozen
candidate/tree, immutable manifest file and tuple identity, ordinal/residual
counters, absent product raster/provenance, inert null/false registry, seven
null copy slots, one null alt slot, and protected-state separation. It then
creates one fresh GUID-in-D-format OS-temp child and predeclares only absent
`attempt-02.png`.

Quartermaster invokes ordinal `2` once with exact prompt
`HOST06-GEN-PROMPT-v1`, built-in generation only, and this exact argument
shape:

```text
{ prompt: HOST06-GEN-PROMPT-v1 }
```

Both image-input fields are omitted. No reference/conversation/accepted-media
image, edit, variation, CLI, REST/API, browser, extension, alternate generator,
manual file, upscale, derivative, conversion, or fallback is permitted.

The one direct non-array result object must have one direct own data property
`image_url` whose primitive string is exactly one canonical
`data:image/png;base64,` value. Every other field, including `output_hint`, is
opaque and unread. Total string length is at most `16,000,022` UTF-16 code
units: exact 22-code-unit prefix plus at most `16,000,000` canonical RFC 4648
payload characters, decoding to at most `12,000,000` bytes. No split, slice,
substring, replace, regex, `atob`, full-payload `Buffer.from`, or second
transport copy is allowed.

After one index-only grammar/padding/pad-bit validation and decoded-length
proof, one fixed-buffer streaming decoder of at most `196,608` bytes writes
each quartet once to create-new `attempt-02.png`, counts decoded bytes, and
computes lowercase SHA-256. An independent exact-file stream must match byte
length and SHA before any PNG, decode, pixel, or composition work. Result,
base64, decoder, and buffer references are then released; this never claims
managed cleanup.

The exact temp candidate must then pass signature/chunk bounds and CRCs; one
first `IHDR` of `3840 x 2160`, 8-bit truecolor color type `2`, compression
`0`, filter `0`, interlace `0`; one pre-IDAT `sRGB`; only the narrowly exact
optional `gAMA`, `cHRM`, or `pHYs`; consecutive nonempty `IDAT`; one final
empty `IEND`; no `tRNS`, alpha, ICC, text, EXIF, time, APNG, unknown/private
chunk, or trailing byte; one complete zlib stream with exactly `24,885,360`
structural scanline bytes and filter bytes `0..4`; exact byte cap; and direct
isolated-browser natural decode at `3840 x 2160` with no preview or emission.

Only then may Quartermaster evaluate original-resolution `PHY-01..12`, exact
source bands, centered six-layout no-crop retention, physical/activation/
label/center/anchor/nonoverlap/target mapping, `1/64 CSS px` floor lattice,
focus stability, accessibility, owner-correct meaning, and all unchanged
content gates. The complete rubric and exact prompt are frozen in
`FRSH-005-v1-VR-06`.

If ordinal `2` is a fully identified objective rejection, Quartermaster
retains only permitted bounded scalars, deletes and proves absent only exact
`attempt-02.png` and its exact empty GUID child, and may then repeat the same
unchanged contract once with fresh child and `attempt-03.png`. If ordinal `3`
rejects, exact cleanup is followed by `HOLD / ALL LAWFUL ORDINALS CONSUMED /
NO MEDIA CANDIDATE`. Any opaque transport/identity/write/allocation stop or
cleanup uncertainty is terminal and authorizes no later call.

At most the first fully passing candidate may be copied create-new and byte-
identically to:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
```

Independent product length/SHA must equal selected temp identity before the
only permitted `PROVENANCE.md` is written. Provenance includes bounded
tool/prompt/ordinal/gate/product/manifest/residual scalars but no data URL,
payload, base64, result, `output_hint`, managed/temp path, rejected bytes/hash/
pixels, opaque identity, or cleanup claim about managed output. Quartermaster
then populates only the predeclared source/geometry/protected/six-layout
registry, seven `FRPX05_COPY` slots, and one factual alt slot; performs exact
selected temp/child cleanup; validates the content candidate in the frozen
order; and issues one versioned `PRODUCTION CONTENT COMPLETE` or `HOLD`
ledger plus one synchronized next handoff.

No payload, temp candidate, result, managed output, rejected or selected bytes,
preview, Markdown image, screenshot, contact sheet, cycle reveal, or
publication may be emitted to the user or any report. The selected raster may
enter the repository only through the byte-identical product path above.

## Preserved gates and state

The production data URL is transient and may not enter source, configuration,
provenance, Git, build output, browser storage, logs, reports, previews, or
reveal. Static source/diff/build proof must find no literal PNG data URL or
selected base64 payload. Result caps remain JavaScript `<=1,679,393` affected
/ `<=1,703,258` global; CSS `<=119,547` / `<=119,672`; modules `<=218` /
`<=222`; resulting media exact `18` and `37,410,731 + selectedBytes`, at most
`49,410,731`; source maps `0`; and every decode/ready/CLS/task/test/build/E2E
cap in the effective shell unchanged.

Inherited functional gates remain frozen and were not replayed: focused
`50/0`, legacy static-contract `29/0`, learning/privacy `17/0`, related `58/0`,
validators `40/40`, cold full `979/0/0`, production and TD-012 fixture builds
PASS, production PBA JavaScript `1,676,508`, CSS `119,394`, modules `217`,
accepted media `17 / 37,410,731`, source maps `0`, served preflight PASS,
owned process/port cleanup PASS, and `git diff --check` PASS. Complete E2E
remains correctly unrun and later has exactly one invocation with no retry.

Maturity remains unchanged. `FR-03` remains continuity `FR2`; physical-host
expression `FR0 - 1 accepted shared compression / 1 exact / 10 missing`;
learning `FR2`; behavior/save/recovery `FR1`; content `FR2`; presentation
`FR3`; and prior bounded release proof `FR4`.

All thirteen inherited process records remain separate and **OPEN**: VR-17,
VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The separate Commandant filename/search-scope record
remains **OPEN**. Mission closes none.

Repository QA quarantine, protected PDF, training directory, real browser /
profile/save, hidden lore, user work, accepted-media pixels, managed/temp
parents, and opaque residuals remain protected. No accepted-media path or
pixel is generation input; no accepted byte is edited, replaced, relabeled,
deleted, regenerated, or repaired.

## Mission stage close

Mission changed only `FRSH-005-v1-VR-06` and this synchronized handoff.
Mission did not call a generator, inspect/decode a payload or pixel, inspect or
clean a managed output, import media, write product copy/provenance, alter the
frozen candidate or immutable manifest, run test/build/preview/E2E, reveal,
advance maturity, close an OPEN record, inspect VR-65, schedule, automate,
release, or call `FIRST RUN COMPLETE`.

Quartermaster ordinal `2` becomes active only after the enclosing Mission
commit is pushed and exact `HEAD == origin/main == remote main`
synchronization is proved.
