# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`POLISH VIABILITY READY / FRVE-005-v12 / SOLE F1 /
HOST06-GEN-PROMPT-v3 CENTER-CORE / REQUIRED F1 IDENTITY + 30 MB SOURCE CAP +
FRAGMENT/APPROACH EVIDENCE CORRECTIONS / NO API OR MEDIA / FRESH MISSION
NEXT`**

Exact next owner: **one fresh Mission Captain / `mission_captain`**

Read the mandatory intake and Mission profile in full, then read complete
`FRWO-005-v15`, `FRVE-005-v12`, `FRCA-005-v12`, `FRVE-005-v11`,
`FRSH-005-v1-VR-43`, current release map/scoreboard, `FRPB-001-v2`,
`FRCL-004-v2`, `FRDT-005-v1`, `FRDT-005-v1-VR-01`, `FRPX-005-v1`,
`FRPX-005-v1-VR-01`, all five retained v12/E1 controls, and only the exact
current Host 06 runtime/test controls they cite.

Issue one separately versioned F1 shell and five new retained v13/F1 sources.
Copy the complete `HOST06-GEN-PROMPT-v3` from `FRWO-005-v15` byte-exactly and
prove strict ASCII/UTF-8 without BOM, LF/no terminal LF, exact `5,386` bytes /
SHA-256
`1c55c9c5d6dbbf7c83a0fcadde4180e974db051dc9f883542aadf3ea2f9b8a1f`.
Retain only the nine literal F1 paths from the Work Order/viability envelope,
the sole attempt domain `{F1}`, exactly one future `SendAsync`, and no F2,
retry, relaunch, fallback, alternate endpoint/model/transport, edit,
variation, reference input, second output, crop repair, reconstruction, or E1
reuse.

Freeze all three accepted-only runtime corrections explicitly:

1. exact `source.attemptId="F1"` everywhere, with the obsolete numeric
   `attemptOrdinal` field/predicate removed rather than forged;
2. selected source exact integer `1..30,000,000` bytes, accepted media exact
   `18`, aggregate `<=67,410,731`, with all decode/image-ready/CLS/task/offline
   budgets still conjunctive; and
3. replacement of the broader historical relation guard with the exact v3
   relation envelope/size/center/top/bottom/corridor predicates, plus positive
   source and six-layout evidence for the complete fragment group, at least
   three individually qualifying fragment rectangles, and the dry approach,
   including exact bands, each fragment width `>=.021`, `390px` legibility,
   relation join without activation entry, mapped centers/retained areas, and
   protected intersections/separations. The old `protected.host05Cue` field
   cannot substitute for these positive facts.

Preserve the PowerShell 5.1 generic-dictionary-safe parser, strict whole-JSON
guard, bounded response/Base64/decoded PNG, temp-only native helper, handle-
bound identity, create-new staging, atomic no-replace materialization,
technical-first private no-reveal review, exact decision grammar, provenance,
identity-conditioned cleanup, one-path rail, exact Host 05 gate, sole
unchanged `L02-03`, lesson ownership, privacy/save/offline behavior, surface
canon, Hosts 07+ nonexpression, null deltas, shared RP-012 ending, and
`successor=null`.

Before `FIRST RUN SHELL READY`, prove strict encoding, exact byte length/SHA,
and parser-zero for all five new sources; prompt identity in the retained
carrier; exactly one `SendAsync`; F1-only attempt fields/paths; the 15-case
Windows PowerShell 5.1 semantic suite; exactly one credential-cleared fixture
with one child and zero credential reads, request constructions,
`SendAsync`/API sends, or F1 consumption; and exact absence of all nine fresh
paths before and after the fixture. Commit the shell, five sources, and this
synchronized handoff; push at the workflow gate and prove
`HEAD == origin/main`.

Mission performs no production invocation, credential read, request
construction, API send, F1 consumption, media or pixel operation, import,
preview, reveal, product population, browser, E2E, maturity advance, release,
later role, later host, or completion action.

Preserve the Operations protected-filename enumeration variance as separate
and **OPEN** without repeating, querying, using, closing, merging, waiving,
curing, or renumbering it. Do not access protected user state, accepted-media
pixels, historical attempts, old roots, opaque residuals, repository QA
quarantine, or hidden lore.
