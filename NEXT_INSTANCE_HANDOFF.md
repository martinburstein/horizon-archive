# Horizon Archive - Skyscraper Workflow Handoff

Last updated: **2026-07-26**

## Start here

Read `AGENTS.md`, this file, `SKYSCRAPER_AGENT_WORKFLOW.md`, and
`Skyscraper Agent Profiles/README.md` in full.

The authorized Skyscraper automation is active on a three-hour cadence.
TD-005 is in a sequential return chain; do not restart a completed stage or
deploy a Marine before the Mission `SHELL READY` push gate.

## Current local state

- Active test drive: `TD-005`
- Latest completed stage: `7 - returned Office of Science Administrator`
- Product/world authorities: `GDB-TD005-v1`, `WNMP-TD005-v1`
- Historical HOLD chain:
  `CFS-TD005-v1`, `VE-TD005-v1`, `MC-TD005-HOLD-v1`
- Current floor stack: `CFS-TD005-v2`
- Current viability envelope: `VE-TD005-v2`
- Budget authority: `PBA-TD005-v1`
- Selected slice: `TD-005-RP005-MANYFOLD-RETURN-v1`
- Campaign address: `RP-005 / SC-06 / MF-00-MF-30`
- Route contract: `TD005-RTA-001`
- Science disposition:
  **`VIABILITY READY - ROUTE, RECORD, EVIDENCE, RECOVERY, ACCESSIBILITY, AND BUDGET VALIDATED`**
- Operations return commit:
  `9d3829563d783f9069332417f79bae360d9b9114`
- Science return commit: the dedicated local commit containing
  `VE-TD005-v2`, `PBA-TD005-v1`, `scripts/validate_td005_budget.py`,
  metrics, this handoff, and current readiness control is authoritative.
- Shell identifier: **not issued**
- Synchronization: **local-only until Mission's `SHELL READY` push gate**
- Marine deployment: **forbidden**

## Frozen route and predecessor contract

`TD005-RTA-001` is the only fresh Pilot-owned action from exact released
TD-004 `TR-40 / SC-05` into one transient zero-evidence
`MF-00 ARRIVE + IDLE / SC-06`.

The normal adapter must use the released TD-004 key, version, strict
sanitizer, adapter, raw read-back, shell/controller identities, and scene
resolver. Every dependency, private-clearing boundary, owner/action/modality,
active group, and opaque fresh unused token validates before consumption.

One accepted token mounts MF-00 once. It writes no record, grants no evidence,
reward, access, permission, identity, authority, or external action, replays
nothing, changes no world state, and preserves continuation, null deltas, and
null successor.

TD-004 raw bytes remain immutable across entry, RP-005 save attempts,
recovery, restore, and return. The apparent common return remains
non-traversable. Destinationless notation remains optional, zero-evidence,
non-routing, and neither necessary nor sufficient for entry.

## Frozen RP-005 record

- key: `horizon-archive-rp005-manyfold-return-save-v1`
- version: `rp005.manyfold-return-save.v1`
- shell: `SS-RP005-MANYFOLD-RETURN-v1`
- controller: `rp005.manyfold-return-controller.v1`
- exactly ten ordered top-level keys, nine ordered note keys, and eight
  ordered finalized evidence records as specified by `VE-TD005-v2`
- exact private/transient denylist
- one atomic replacement at the RP-005 key only
- raw and sanitized strict read-back
- exact prior-byte/absence rollback plus unchanged TD-004 byte verification
- exact saved re-entry to replay-free heading-first MF-30
- unsaved/invalid re-entry to exact TR-40 with fresh intent required

Four physical observations, strict `PY-012`, strict independent
`RP005-TEXT-01 / AI901-D1-O5`, and both explanations remain separate
conjuncts. Scene, route, frequency, presentation, focus, modality, timing,
Tour, execution, and save/restore display remain zero-credit.

## Accessibility, offline, world, and hard-stop controls

- seven equivalent modalities with validation before token consumption;
- one active owner/group, semantic heading, one polite atomic status,
  deterministic focus, field-associated errors, and `>=44px` targets;
- exact DPR-1 desktop `1920x1080`, laptop `1366x768`, narrow `390x844`,
  and effective-zoom `768x900` review;
- forced colors, reduced motion, grayscale, responsive containment/reflow,
  exact scene/alt identity, and closed storage-free fixture;
- offline/no-authority/no-exam-guarantee and no-credit Tour;
- invariant SC-05/SC-06 and no live service, credential, endpoint, external
  action, access, ranking, correction, sampling, or response;
- exact returns to Three-Current Reach and City Threshold; no new direct
  Calibration Margin or Civic shortcut; and
- hard stop at MF-30 before any RP-006 route, RP-013, successor, reward,
  authority, world response, or post-ending content.

## Production budget

`PBA-TD005-v1` is a one-time non-compounding authority:

- JavaScript `<=1,310,110` bytes;
- CSS `<=89,408` bytes;
- modules `<=192`;
- at most two new runtime image assets totaling `<=8,388,608` bytes;
- total runtime media `<=29,924,731` bytes;
- no new audio, font, video, or network-fetched payload;
- build `<=60s`, focused `<=30s`, related/full `<=60s`,
  complete E2E `<=180s`, and sampled main-thread task `<=100ms`.

The two image roles are the SC-06 panorama and registered forensic detail.
The allowance never permits visual degradation. Maximum-quality first-person
photorealism, direct import, provenance, accessibility, responsive crops,
grayscale review, and placeholder retirement remain mandatory.

The executable validator aggregates every chunk, hashes all media, requires
every accepted TD-004 media identity to remain, and fails closed on excess or
disallowed assets.

## Validation evidence

- official AI-901/Foundry/Azure Language review: aligned;
- RP-005 mapping validator: PASS;
- focused: `38/38 PASS`;
- related: `58/58 PASS`;
- full game: `806/806 PASS`;
- production build: `183` modules / `11.13s`;
- baseline budget: PASS;
- deliberate module, build, missing-release-timing, new-audio, and
  three-image probes: FAIL as required.

Normal RP-005 remains unimplemented. Passing Science evidence does not claim
runtime SC-06 integration or release.

## Protected boundaries

Never open the hidden-lore vault. Never inspect or mutate Martin's browser
storage or campaign save. Never inspect, alter, stage, move, delete, or
commit `Art Of No Mans Sky Book Scan.pdf` or
`Simplilearn Training Files/`. The archived two-team workflow remains
inactive.

## Exact next action

**Mission Captain re-entry.**

Read the Mission profile, `GDB-TD005-v1`, `WNMP-TD005-v1`,
`CFS-TD005-v2`, `VE-TD005-v2`, `PBA-TD005-v1`, the historical Mission
HOLD, released TD-004 reconciliation/source, frozen RP-005 contracts, and
this handoff.

If every boundary is conflict-free, replace the historical HOLD with one
versioned `SS-RP005-MANYFOLD-RETURN-v1` `SHELL READY` contract that freezes
the route, immutable predecessor, state-to-scene matrix, MF graph, exact
record/atomic recovery, evidence firewall, accessibility/responsive fixture,
offline/Tour/world limits, budget, two runtime image roles, validation ladder,
variance protocol, Marine ownership, and exact Reconnaissance Sergeant
handoff.

Commit, push at the `SHELL READY` gate, verify `HEAD == origin/main`, and only
then deploy Reconnaissance Sergeant.

If any conflict remains, return it to the earliest responsible owner. Do not
issue a partial or implied shell.
