# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`FIRST RUN SHELL READY / CORRECTED FOUR-STAGE COMPLETE
DIAGNOSTIC-CONTROL VERIFICATION / FRSH-003-v1-VR-53`**

Stage and release remain HOLD.

Exact next owner: **fresh Combat Engineer**

Immediate control: `FRSH-003-v1-VR-53`

Mission source: `1c491a9471112d3d0b1eeef95aaebb7a62bec565`

Geometry correction / diagnostic transport / report lineage remains:
`8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97` /
`6c64eb354b7dbb467df5725e2cae4eb67092ddc7` /
`0ac9023037873004f7bd5d75c16f80953d770a4d`.

VR-52 is accepted as **`DISPOSABLE SHA-256 CORRECTION CONTROL PASS / ROOT
DELETED / NO LIVE`**. All root, identity, UTF-8, base64, two-hash,
uppercase/lowercase shape, recompute-equality, disposal, cleanup, and
`nativeExit=0` predicates passed. This is execution-control evidence under the
existing VR-50 OPEN item, not a candidate finding or a new OPEN classification.

Fresh-child capacity remains unavailable; Mission context reuse is disclosed
and non-evidence.

Combat may execute the four separate bounded VR-53 stages only: (1) create and
prove one fresh root, construct its base64 token, and hash it with the exact
VR-52 disposable SHA mechanism; (2) re-prove the token and launch/ready the two
owned previews; (3) invoke exactly one complete E2E and take the mutually
exclusive failure-transport or success-summary/verifier branch; and (4)
unconditionally stop only transported PIDs, clear both ports, and delete only
the revalidated transported root.

Exactly one E2E invocation remains. Success requires diagnostic
`checkInventoryExact=true`, `failureCount=0`, `browserClosed=true`, exactly one
summary, exactly one verifier PASS, and exact cleanup. Failure requires full
canonical base64 transport before cleanup, no verifier, and no rerun. A pre-
E2E failure/rejection spends no E2E invocation but exhausts this shell and
activates only mandatory cleanup.

Cumulative accepted gates, frozen candidates/invariants, and all ten OPEN
classifications remain exact. No product/media/protected change, build, test,
validator, PBA, served-identity rerun, downstream stage, or release action is
authorized.
