# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`WORK ORDER READY / FRWO-005-v5 / MARTIN-AUTHORIZED
IMAGEGEN CLI/API FALLBACK / FRESH SCIENCE REQUIRED`**

Martin's controlling decision: **`Authorize imagegen CLI/API fallback for Host
06. The API key is set.`**

Current Work Order: `FRWO-005-v5`

Superseded Science HOLD / Work Order: `FRVE-005-v4` / `FRWO-005-v4`

Planning controls: `FRRM-005-v5` / `FRSB-005-v5`

Base shell / effective prior Mission variances: `FRSH-005-v1` through
`FRSH-005-v1-VR-07`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Frozen candidate tree:
`09da6293d72c8123b8d9673bd8e41329338e8d13`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next owner: **one fresh Office of Science Administrator /
`office_of_science_administrator`**

## Operations decision

Martin explicitly selected CLI/API option 2 from the v4 Science HOLD.
`FRWO-005-v5` withdraws the rejected built-in managed-directory creator-binding
route and freezes one bounded explicit fallback using only:

```text
C:\Users\marti\.codex\skills\.system\imagegen\scripts\image_gen.py
HOST06-IMAGEGEN-CLI-v1
SHA-256 c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05
35,266 bytes
model gpt-image-2
```

Remaining ordinals are exactly `2` and `3`, sequential, one output each,
maximum two calls. Ordinal `1` is permanently consumed and never reused.
The unchanged `HOST06-GEN-PROMPT-v1` is passed with no augmentation, no image
input, no edit/reference/variation, exact `3840x2160`, high quality, opaque
PNG, and exact `--out`. No `--force`, `--out-dir`, batch, SDK/runner, script
edit, managed-output discovery, `output_hint`, or output-hint dependence exists.

The predeclared fresh absent evaluation root is:

```text
C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-cli-1ecc6811-5521-4244-8dcd-a8adbb0f0393
```

Exact attempt paths are `attempt-02.png` and `attempt-03.png` under that root.
Rejected attempts never enter the workspace, are never revealed or reused,
and require exact-path candidate plus empty-root cleanup. At most the first
fully passing source may be copied byte-identically to the existing sole Host
06 product path and accompanied by the existing sole `PROVENANCE.md` path.

No generation or API call occurred. `OPENAI_API_KEY` presence was verified as
boolean `true`; no key value or derivative was printed or retained.

## Preserved outcome and state

The bounded outcome remains exact Host 05 mastery -> lens-like fragment
handoff -> one weathered lens visibly resting inside one tilted conformal
cradle, dry and above current waterline, with at least two stress/load contacts,
at least two drainage seams, restrained horizon catch, and same-basin context
-> sole unchanged `L02-03` -> unchanged next Drowned boundary.

Maturity remains unchanged: `FR-03` continuity `FR2`; physical-host expression
`FR0 - 1 accepted shared compression / 1 exact / 10 missing`; learning `FR2`;
behavior/save/recovery `FR1`; content `FR2`; presentation `FR3`; prior bounded
release proof `FR4`.

The accepted media remain immutable at exact `17 / 37,410,731`; the canonical
tuple digest is unchanged. The strict PNG/chunk/CRC/opaque-sRGB/
`3840x2160`/`<=12,000,000`/decode gates, original-resolution `PHY-01..12`,
six-layout crop/hotspot/mapping/accessibility gates, PBA/media/performance caps,
sole unchanged `L02-03`, privacy/save/recovery, one-path rail, RP-012,
`successor=null`, all thirteen OPEN records, the separate Commandant record,
ordinal-1 opaque residual, and VR-65 remain exact and protected.

## Exact next action

One fresh Office of Science Administrator reads `FRWO-005-v5` and the
synchronized authorities, independently pins the exact CLI SHA/contract/help/
output behavior, repeats only boolean key presence, and validates the argument
vector, predeclared target containment/absence, create-new/race/partial-write,
one-call/no-retry, technical PNG/CRC/decode, cleanup, ordinal/residual,
privacy/offline/performance/no-reveal, and rollback contracts using only
non-generative, non-API fixtures.

Science issues exactly one new versioned `POLISH VIABILITY READY`, `REVISE`,
or `HOLD` artifact. If actual CLI help/source behavior contradicts the frozen
contract or fail-closed exact-target semantics cannot be proved without a
script edit, wrapper, or wider discovery, Science must HOLD.

Science may not invoke generation or the API; allocate the live attempt root;
inspect pixels; access the real managed directory or opaque residuals; import
media; write copy/provenance; authorize Quartermaster; run E2E; reveal; advance
maturity; close an OPEN record; inspect VR-65; schedule; automate; push;
release; or call `FIRST RUN COMPLETE`.
