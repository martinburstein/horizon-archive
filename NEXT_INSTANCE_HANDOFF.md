# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`POLISH VIABILITY READY / FRVE-005-v11 / SOLE E1 /
CENTER-SAFE PROMPT V2 / REQUIRED E1 IDENTITY + 30 MB RUNTIME RECONCILIATION /
ZERO API OR MEDIA`**

Exact next owner: **one fresh Mission Captain / `mission_captain`**

Read `AGENTS.md`, this handoff, `FIRST_RUN_AGENT_WORKFLOW.md`, `First Run Agent
Profiles/README.md`, and `First Run Agent Profiles/mission-captain.md` in full.
Then read complete `FRWO-005-v13`, `FRVE-005-v11`, `FRCA-005-v11`,
`FRVE-005-v10`, `FRSH-005-v1-VR-42`, `FRPB-001-v2`, `FRCL-004-v2`, current
release map/scoreboard, complete Host 06 treatment/blueprint controls, and only
the exact retained v11/runtime controls cited by the Science envelope.

Issue one separately versioned E1 `FIRST RUN SHELL READY`, `REVISE`, or `HOLD`
artifact and five new retained v12/E1 sources. Freeze the exact
`HOST06-GEN-PROMPT-v2` bytes/SHA from `FRVE-005-v11`; exactly one future
`SendAsync`; no E2/retry; the fresh literal v12 paths; dictionary-safe bounded
response parser; native identity/materialization/PNG/private-review/no-reveal/
provenance/cleanup controls; and the full validation ladder. Explicitly require
the later inert runtime, tests, provenance, PBA, and E2E identities to replace
the obsolete historical `{2,3}` / 12 MB source contract with exact
`attemptId="E1"` / `1..30,000,000` bytes without altering historical ledgers.

Before shell READY, prove strict UTF-8/no BOM, exact length/SHA and parser-zero
for all five retained sources; prompt identity; the 15-case Windows PowerShell
5.1 semantic suite; exactly one credential-cleared fixture with zero request/
send/E1 activity; and exact absence of all nine fresh paths. Commit shell,
sources, and synchronized handoff; push at the Mission gate; prove
`HEAD == origin/main`.

Mission performs no production invocation, credential read, API send, E1
consumption, media creation/inspection/import, product population, reveal,
later role/stage, E2E, maturity advance, or later-address work. Do not revive
D1, add E2/retry, access protected/old/residual paths, close an OPEN
classification, weaken any physical/layout/accessibility gate, skip Host 06,
or call `FIRST RUN COMPLETE`. Preserve the Operations protected-filename
enumeration variance as separate and OPEN without repeating or using it.
