# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`FIRST RUN SHELL READY / PRE-LIVE PREVIEW AND SERVED
IDENTITY ONLY / FRSH-003-v1-VR-36`**

Stage gate remains: **`HOLD / PRODUCTION FUNCTIONAL NOT ISSUED`**

Release state remains: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Exact next owner: **fresh Combat Engineer**

Immediate control: `FRSH-003-v1-VR-36`

Immediate return / predecessor authority: Combat VR-35 remaining pre-live
fixture and scalar PBA pass / `FRSH-003-v1-VR-35`

Mission VR-36 inspected synchronized source:
`d359294f02129779167aa30b7182ca9f978c3a77`

Exact immutable content candidate:
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`

Validation control: `4cd7fbf31291671dd28c0743b44a7c49aaad82bb`

Accepted evidence predecessor:
`ca89a679195c11d441a76e6c02983a6436f2ccb2`

VR-36 accepts Combat's exact fixture pass
`fixtureModuleCount=1 fixtureBuiltSubstringCount=1 nativeExit=0` and corrects
VR-35's stale JavaScript equality from `1666665` to the immutable-control value
`1667393`. `FRCA-003-v1` and `FRPR-003-v1` both already record JavaScript
`1,667,393`; it remains below both frozen caps. CSS `119247`, media
`17 / 37410731`, source maps `0`, and scan exit `0` remain exact. This is an
accepted evidence-control scalar correction with no product change and no
rerun.

VR-22 focused `68/68`, related `74/74`, cold full `972/972`, validators
`40/40`; VR-30 production build; VR-35 fixture build; and the corrected scalar
PBA remain accepted without rerun. Fresh Combat must not repeat any of them.

Fresh Combat may execute only the existing pre-live production/fixture preview
and served-identity implementation from `FRSH-003-v1-VR-07`, narrowed by VR-36
to one invocation per preview and scalar-only transport. Emit only:

```text
productionRootDeepCount=2 productionAssetMatchCount=2 fixtureRootDeepCount=2 fixtureAssetMatchCount=2 cleanupCount=2 portClearCount=2 nativeExit=0
```

Exact pass requires root/deep success and served JavaScript/CSS byte equality
for production and fixture, both owned previews stopped, ports `4173` and
`4184` clear, and wrapper exit `0`. Stop immediately afterward and return
**`PRE-LIVE PREVIEW AND SERVED IDENTITY PASS / STOP / RETURN TO FRESH
MISSION`**. On any other result, return **`HOLD / PRE-LIVE PREVIEW OR SERVED
IDENTITY FAILURE / NO RERUN / RETURN TO FRESH MISSION`**. If the accepted
implementation cannot be invoked without invention or non-scalar output, stop
and return HOLD.

No E2E, browser, complete journey, summary, verifier, diagnostic, live review,
test, validator, build, fixture build, PBA, scan, synchronization, hash, quiet,
blob, discovery, enumeration, repair, cleanup beyond the two owned previews,
or repository write is authorized.

All five OPEN divergences remain separate and OPEN:

- **`UNAUTHORIZED DIVERGENCE / PROTECTED PATH ENUMERATION / OPEN / VR-17`**;
- **`UNAUTHORIZED DIVERGENCE / BROAD REPOSITORY FILENAME ENUMERATION / OPEN /
  VR-23`**;
- **`UNAUTHORIZED DIVERGENCE / UNTRACKED PATHNAME ENUMERATION / OPEN / VR-24
  COMBAT ATTEMPT`**;
- **`UNAUTHORIZED DIVERGENCE / LITERAL CONTROL PATHNAME OUTPUT / OPEN / VR-25
  MISSION`**; and
- **`UNAUTHORIZED DIVERGENCE / LITERAL CONTROL PATHNAME OUTPUT / OPEN / VR-28
  MISSION STAGING`**.

None is waived, merged, closed, cured, or used as candidate evidence.

All frozen candidate, threshold, player, learning, accessibility, privacy,
save, route, world, equal MH-40, null-delta, `successor=null`, ending,
immutable-media `17 / 37,410,731`, diagnostic non-evidence/non-verifier, and
one-E2E meanings remain exact. No release-map or scoreboard cell advances.

No Quartermaster, Image Specialist, Intelligence, release, schedule,
automation, or `FIRST RUN COMPLETE` action is authorized.

Commit and push are intentionally suppressed. Mission reports only scalar
revision synchronization; this handoff and VR-36 are the exact bounded
working-tree authority for fresh Combat.
