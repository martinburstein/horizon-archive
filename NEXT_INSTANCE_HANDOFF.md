# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`FIRST RUN SHELL READY / FRSH-005-v1 / COMBAT
PRE-GENERATION MANIFEST PUSH GATE THEN QUARTERMASTER BOUNDED BUILT-IN
GENERATION`**

Martin's controlling decision: **`Authorized new Drowned Media for Host 06`**

Decision authority: `FRPB-001-v2`

Continuity authority: `FRCL-004-v2`

Selected Work Order: `FRWO-005-v2 / Stranded Lens Cradle - One New Source and
Fixed Lesson Integration`

Science authority: `FRVE-005-v2 / POLISH VIABILITY READY`

Current production contract: `FRSH-005-v1 / FIRST RUN SHELL READY`

Released authority: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Planning controls: `FRRM-005-v2` / `FRSB-005-v2`

Exact next owner: **fresh Reconnaissance Sergeant /
`reconnaissance_sergeant`**

Immediate control: **one bounded `FRSH-005-v1` Directorial Treatment only**

Mission source inspected:
`21a0ed9bf9e9c6d04da0c92a47907d029171d17d`

Mission commit / synchronization: **reported by the Mission release message;
verify exact local `HEAD == origin/main` before Recon action**

## Shell result

The five-role planning chain is conflict-free. `FRSH-005-v1` freezes one
player-visible Host 05 -> Host 06 -> unchanged `L02-03` outcome and permits no
Host 07 or later work.

The production sequence is exact:

1. Recon locks attention, same-basin reorientation, the fragment-led dry-reach
   discovery, horizon catch, silence, and unresolved stranded/correct
   uncertainty. It generates nothing.
2. Tactical freezes the exact state/action/source switch, focus, recovery,
   semantic order, six-layout measurement schema, access, and malformed
   fallbacks. Candidate scalars remain null.
3. Combat creates the inert fail-closed Host 06 registry and conditional
   source/legacy-launcher boundary, then freezes a code candidate. It computes
   the raw-byte manifest for exactly the seventeen shell literals, requires
   `17 / 37,410,731` plus the canonical tuple digest, commits its evidence,
   pushes, and proves synchronization. Combat generates and inspects no pixels.
4. Only after Combat's passing push gate, Quartermaster may execute maximum
   three sequential built-in calls, exactly one output each, with no image
   input or edit/variation. Each exact managed return is copied byte-identically
   to one predeclared GUID-temp candidate and the managed file is immediately
   deleted and proved absent before rubric inspection. Quartermaster alone
   rejects/selects, imports at most one passing raster plus `PROVENANCE.md`,
   populates predeclared scalars, and writes final copy in seven slots.
5. Image Specialist may change code/configuration presentation only around the
   immutable selected bytes. It may not generate, edit, vary, export, import,
   replace, or reveal media.
6. Fresh Intelligence independently validates the exact candidate, runs or
   corroborates the frozen ladder, classifies every variance, and alone may
   release or advance maturity.

## Frozen candidate and media identities

Do not collapse these identities: Combat code commit; immutable seventeen-
tuple baseline; attempt prompt/ordinal/tool-returned path; byte-identical temp
evaluation file; selected raster bytes at the exact product path;
Quartermaster content commit; Image presentation commit; and Intelligence
release candidate. None becomes canon or maturity evidence before independent
release.

The two and only two media-owned workspace paths are:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md
```

The candidate must be an exact opaque 8-bit sRGB PNG `3840 x 2160`, at most
`12,000,000` bytes. The selected runtime result is exactly `18` media files and
`37,410,731 + selectedBytes`, never above `49,410,731`. Every pre-existing
accepted path/byte/SHA tuple remains immutable.

## Frozen presentation, behavior, and release boundary

The complete relation stays inside the shell's source bands and uses one
centered no-crop `<img>` at all six layouts. `PHY-01..12`, physical/activation/
label/center/nonoverlap mapping, `44 x 44 CSS px` targets, exact accessible
name/label states, keyboard/pointer/touch parity, focus recovery, forced-color
`3px Highlight`, reduced motion, and equivalent meaning are mandatory.

Host 06 remains absent until exact sanitized Host 05 mastery, exact selected
source/provenance identity, finite passing measurements, and natural-dimension
decode all pass. The generic model-choice launcher remains unchanged only in
Combat's inert `enabled=false` state; after lawful source enablement it is
absent and never returns on source/decode failure. Sole Host 06 USE opens the
unchanged `L02-03`; LOOK/TALK write nothing; completed USE is read-only.

Learning/evaluator/remediation/sanitizer, privacy, save schema, route, Demo
Tour, offline/no-authority behavior, world nonresponse, READY/NOT YET READY
parity, null deltas, shared ending, and `successor=null` remain exact. No
runtime external request, dependency, source map, reveal, publication, or
automation is allowed.

## Numeric and validation envelope

- JavaScript: affected `<=1,679,393`; global `<=1,703,258`.
- CSS: affected `<=119,547`; global `<=119,672`.
- Modules: affected `<=218`; global `<=222`; source maps `0`.
- Decode `<=250ms` cold / `<=100ms` warm; image-ready `<=750ms`.
- Attributable CLS `<=0.01`; total CLS `<=0.05`; activation `<=2ms`;
  sampled task `<=100ms`.
- Focused `<=30s`, related/full/build `<=60s`, and exactly one complete E2E
  `<=180s` with no retry.

`FRRC-003-v1` is the new command manifest; `FRRC-001-v1` and `FRRC-002-v1`
remain immutable. `FRAM-001-v1` is the immutable seventeen-tuple manifest.
The shell freezes ordered integrity, source-rubric, six-layout, focused,
learning/privacy, related, validator/full, build/PBA/served, one-E2E, cleanup,
and Git gates plus exact rollback to released product candidate
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`.

## Preserved process and protected state

All thirteen inherited records remain separate and **OPEN**: VR-17, VR-23,
VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The Commandant filename/search-scope record remains
one additional separate **OPEN** record. All prior and Mission-recorded
process-only recurrences remain recorded. None is waived, merged, closed,
cured, renumbered, or candidate evidence.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**. Its contents and meaning remain
unknown. The repository QA quarantine, protected PDF, training directory,
browser/profile/save state, hidden lore, user work, accepted-media bytes, and
opaque residuals remain protected except for Combat's exact seventeen literal
read-only byte-stream gate.

Mission performed no image generation, edit, variation, selection, import,
copy, movement, deletion, publication, reveal, build, browser, E2E, save,
external service, cleanup, schedule, automation, production, release,
maturity update, OPEN-record closure, or residual access.

## Exact next action

One fresh Reconnaissance Sergeant reads in full `AGENTS.md`, this handoff,
`FIRST_RUN_AGENT_WORKFLOW.md`, the canonical registry, the complete Recon
profile, and complete `FRSH-005-v1`, then reads `FRWO-005-v2`, `FRVE-005-v2`,
the exact Drowned/Host 05 runtime state, and only the relevant current visual
authorities.

Issue exactly one versioned Directorial Treatment for `FRSH-005-v1`. Define
the attention path, felt transition from exact Host 05, same-basin continuity,
fragment-led approach, dry reach, lens/horizon catch, physical uncertainty,
silence, and handoff into unchanged `L02-03` without changing the frozen
prompt, geometry envelope, candidate rubric, role partition, or protected
boundaries.

No Tactical, Combat, Quartermaster, Image, Intelligence, manifest byte read,
media tool, candidate inspection, generation, selection, import, runtime
implementation, reveal, schedule, automation, maturity update, OPEN-record
closure, residual access, release, or `FIRST RUN COMPLETE` begins from this
handoff.
