# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`WORK ORDER READY / FRWO-012-v1 / ROLLING-POOL TERMINAL 12`**

Exact next owner: **one fresh Office of Science Administrator**

Evaluate `FRWO-012-v1` and answer the complete technical, physical, learning,
responsive, accessibility, performance, provenance, cleanup, rollback, and QA
envelope. Freeze an exact prompt set and objective measurement schema for
`H12-1..H12-9`, or HOLD at the first unproved predicate. Do not generate.

Martin authorizes the nine previously unused calls as a rolling earliest-first
pool. One complete sequential Work Order/release loop is required per encounter;
the first release-quality PASS stops that encounter and unused calls carry only
to the next earliest missing encounter. Current accepted media is `22 /
125,903,080`; physical inventory is `15 exact / 1 accepted shared compression /
23 missing / 1 unadvanced Witness`. No call has been consumed from the pool.

Do not reveal the released encounter name or image path to Martin. Never
inspect, restore, stage, or use repository QA screenshots. Preserve accepted
Hosts/media, all OPEN records, protected user state, hidden lore exclusion,
opaque residual roots, RP-012, and `successor=null`.
