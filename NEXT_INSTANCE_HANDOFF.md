# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`FIRST RUN SHELL READY / FRSH-003-v1-VR-09`**

Exact next owner: **Quartermaster**

Mission adjudicated VR-08 as a pre-live command-syntax failure and issued
`FIRST RUN SHELL READY / FRSH-003-v1-VR-09`. `New-Item` in this PowerShell
supports `-Path` and `-ItemType` but not `-LiteralPath`. The proposed root never
existed; no browser, E2E, summary, verifier, served request, or tracked change
occurred. Prior preview PIDs are stopped and ports `4173` / `4184` are clear.

Exact next action: read `First Run Agent Profiles/quartermaster.md` in full,
then read this handoff, complete
`Production Pipeline/First Run/FIRST_RUN_SHELL_VARIANCE_FRSH-003-v1-VR-09.md`,
complete `FRSH-003-v1-VR-08`, and complete current `FRCA-003-v1`.

Change no tracked file. Repeat exact Git ancestry/blob/identity preflight from
the synchronized VR-09 source. Set terminating errors first; validate one new
canonical GUID child as an exact direct child of resolved OS temp and outside
the repository; require it not to exist; then create it once with
`New-Item -ItemType Directory -Path <validated-exact-path> -ErrorAction Stop`.
Resolve and revalidate it after creation and before any write or preview.

Only after root containment passes may Quartermaster inject exact content
candidate `a91763e28d488f31f8cf7d40ece0b2682246ba9b`, evidence candidate
`ca89a679195c11d441a76e6c02983a6436f2ccb2`, and the exact new root; start
owned previews; prove served identity; and invoke exactly one complete
FRRC-002 E2E, one generated summary, and one verifier. Revalidate exact
containment before deleting only that root with supported
`Remove-Item -LiteralPath ... -Recurse -Force -ErrorAction Stop`.

Any preflight/path/create/preview/served/E2E/summary/verifier/cleanup failure is
`HOLD` with no rerun. Product, content, test, copy, probe, manifest, media,
presentation, thresholds, learning/save/route/ending/maturity remain frozen.
Image Specialist and Intelligence remain blocked until synchronized
Quartermaster `PRODUCTION CONTENT` PASS. Do not inspect protected paths, touch
prior roots, perform media work, create a reveal/schedule, or call
`FIRST RUN COMPLETE`.
