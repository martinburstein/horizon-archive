# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`FIRST RUN SHELL READY / ONE DIAGNOSTIC-CONTROL
VERIFICATION / FRSH-003-v1-VR-13`**

Release state: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Exact next owner: **fresh Combat Engineer**

Exact diagnostic-control candidate:
`ce7c9abbaf1d0ffad8c1031f0398750676d4970e`

Exact immutable content candidate:
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`

Validation control: `4cd7fbf31291671dd28c0743b44a7c49aaad82bb`

Accepted evidence predecessor:
`ca89a679195c11d441a76e6c02983a6436f2ccb2`

Read the Combat Engineer profile in full, complete
`FRSH-003-v1-VR-13`, operative `FRSH-003-v1-VR-07`, diagnostic contract
`FRSH-003-v1-VR-12`, complete `FRCE-003-v1-VR-12`, current
`FRAB-003-v1`, and the exact committed `FRRC-002-v1` manifest, E2E, and static
test at candidate `ce7c9ab`.

Mission independently accepted the exact three-file correction. Candidate
ancestry and file scope, frozen product/validation/evidence blobs,
`git diff --check`, manifest JSON/13-entry/one-E2E identity, and `node --check`
passed. The exact focused command independently passed `68/68` in
`166.7833ms`. Diagnostic schema/path inventory, six-layout ordering, exhaustive
false-path grouping, write-before-throw ordering, unchanged layout
conjunction, concise failure transport, summary separation, verifier exclusion,
and no-retry semantics satisfy VR-12.

Run the complete deterministic ladder from the beginning without changing any
tracked implementation/control file. Any deterministic failure is `HOLD` and
stops before E2E. Only after every gate passes, create one previously
nonexistent GUID direct child inside resolved OS temp and outside the
repository, prove exact containment, and invoke the complete E2E exactly once
with product `a91763e` and probe `ce7c9ab`.

On E2E failure, timeout, abort, or absent summary: `HOLD`, no rerun, no
verifier. You may read only the machine diagnostic in the exact owned root to
record every failure path/value; it is never summary, verifier input, release
evidence, or a retry oracle. On E2E success, require exactly one
`first-run-live-summary.json`, then run exactly one independent verifier with
that summary only. Any verifier, identity, evidence-count, or cleanup failure
is `HOLD` with no rerun.

Clean only exact owned PIDs, ports, browser, and the fresh contained root.
Never inspect, enumerate, reuse, mutate, move, or delete the disclosed VR-11
predecessor root. On complete PASS, issue `DIAGNOSTIC-CONTROL VERIFICATION PASS
/ RETURN TO FRESH INTELLIGENCE / FRCE-003-v1-VR-13`; on failure, issue `HOLD /
NO RERUN / FRCE-003-v1-VR-13` and return to fresh Mission. Commit the Combat
report plus this handoff, push, and prove synchronization.

Do not repair product, change thresholds/identity/summary/verifier semantics,
touch product/test/manifest/E2E/media/curriculum/save/route/ending/maturity,
use real user state, begin Quartermaster/Image, expand Host 06-15 or City,
create RP-013/successor/post-ending content, create a reveal/schedule, advance
maturity, or call `FIRST RUN COMPLETE`.
