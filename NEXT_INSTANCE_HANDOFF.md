# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`FIRST RUN SHELL READY / FRSH-005-v1-VR-43 / SOLE
E1 / CENTER-SAFE PROMPT V2 / EXACT ATTEMPT-ID + 30 MB RECONCILIATION / ONE
SEND / NO RETRY / ZERO API OR MEDIA / FRESH QUARTERMASTER`**

Exact next owner: **one fresh Quartermaster / `quartermaster`**

Read `AGENTS.md`, this handoff, `FIRST_RUN_AGENT_WORKFLOW.md`, `First Run Agent
Profiles/README.md`, and `First Run Agent Profiles/quartermaster.md` in full.
Then read complete `FRWO-005-v13`, `FRVE-005-v11`, `FRCA-005-v11`,
`FRSH-005-v1-VR-43`, complete Host 06 treatment/blueprint/functional/manifest
authorities cited by the shell, all five retained v12/E1 sources, and only the
exact current runtime/test controls they cite.

Independently prove `HEAD == origin/main == remote main`, tracked-clean Git,
strict UTF-8/no BOM, exact length/SHA, and parser-zero for the five retained
sources; exact prompt v2 `5,139 /
561a82faa45aed45d2fca50bfb189bfe6e315682ca1289882361939f79455801`;
one carrier `SendAsync`; exact `attemptId=E1`; no E2/retry; immutable
`FRAM-001-v1`; null-first runtime; mandatory later `attemptId="E1"` and
selected-source `1..30,000,000` / aggregate `<=67,410,731` reconciliation;
and exact absence of all nine fresh paths. Do not rerun Mission's sole
credential-cleared fixture.

Invoke only this command, exactly once:

```text
C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoLogo -NoProfile -NonInteractive -File "Production Pipeline\First Run\HOST06_V12_E1_STDIN_PARENT.ps1"
```

E1 is consumed at its sole `SendAsync` regardless of result. If the carrier
reaches `HOST06_REVIEW_READY|attemptId=E1`, inspect only the exact identity-
proved target privately at original resolution and adjudicate every technical,
`PHY-01..12`, prompt-v2 band, six-layout, and accessibility predicate. Write
only the exact create-new decision grammar. Any false or uncertain predicate
is objective rejection; there is no E2, retry, crop/edit/variation, alternate,
or reveal.

Only conjunctive ACCEPT may import the byte-identical raster/provenance and
unlock the bounded content sequence. Replace obsolete runtime
`attemptOrdinal` / `{2,3}` with exact `attemptId="E1"`; replace the 12 MB guard
with exact `1..30,000,000` and media aggregate `<=67,410,731`; populate only
the predeclared registry/geometry/six-layout/copy/alt fields; write
`enabled=true` last; and run the full shell ladder through exactly one E2E.
Issue one versioned `PRODUCTION CONTENT COMPLETE` or `HOLD` artifact, commit,
and synchronize the handoff. Do not begin Image or Intelligence.

Quartermaster may not reveal E1, access protected/old/residual paths, inspect
accepted-media pixels before lawful selection, revive D1, add E2/retry, change
lesson/route/save/world/ending, express Host 07, advance maturity, close an
OPEN classification, or call `FIRST RUN COMPLETE`. Preserve the Operations
protected-filename enumeration variance as separate and OPEN without
repeating or using it.
