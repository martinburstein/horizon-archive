# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`DIRECTORIAL LOCK / FRDT-005-v1 / TACTICAL PLAYER
EXPERIENCE BLUEPRINT ONLY`**

Martin's controlling decision: **`Authorized new Drowned Media for Host 06`**

Decision authority: `FRPB-001-v2`

Continuity authority: `FRCL-004-v2`

Selected Work Order: `FRWO-005-v2 / Stranded Lens Cradle - One New Source and
Fixed Lesson Integration`

Science authority: `FRVE-005-v2 / POLISH VIABILITY READY`

Production contract: `FRSH-005-v1 / FIRST RUN SHELL READY`

Directorial authority: `FRDT-005-v1 / DIRECTORIAL LOCK`

Released authority: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Planning controls: `FRRM-005-v2` / `FRSB-005-v2`

Recon source inspected:
`f345f10f215ef4407d724fa30af968f7a597cd3d`

Recon commit / synchronization: **reported by the Recon release message; no
push required by `FRSH-005-v1`**

Exact next owner: **fresh Tactical Operations Specialist /
`tactical_operations_specialist`**

Immediate control: **one bounded `FRSH-005-v1` Player Experience Blueprint
only**

## Directorial result

`FRDT-005-v1` locks one sober observation-and-uncertainty interpretation:

1. Exact Host 05 mastery returns the unchanged accepted basin for one calm
   beat; no success spectacle or world response occurs.
2. The additional Host 06 source reads as a short clockwise same-basin
   reorientation, not an arrival, teleport, portal, cutaway, or second basin.
3. At least three lens-like fragments in the Host 05-side band lead along one
   continuous dry material approach toward, but not into, the activation.
4. One visibly weathered lens rests inside one tilted conformal cradle as the
   sole discovery center. Stress/load contacts, drainage seams, dry clearance,
   and multi-era material handoffs make the relation physically credible
   without establishing purpose.
5. The ruler-straight horizon/reflection catches inside the lens through
   natural optics. It is not a glow, signal, eye, beacon, answer, reward,
   activation, invitation, or recognition.
6. The Pilot provisionally names the relation and preserves the unresolved
   stranded/correct hypothesis; Scene states physical fact; Suit/System owns
   compatibility and recovery; the 901 Teacher owns the unchanged lesson;
   Machine and Builders own no line.
7. `LOOK AT` observes; `TALK TO` ends in complete nonresponse; sole deliberate
   `USE` enters the unchanged `L02-03`. Miss, mastery, reload, return, and
   completed read-only revisit leave the relation and world invariant.

The adjacent contrast is fixed: Host 05 is low, distributed, repeated, and
spread across two mineral lips; Host 06 is singular, nested, obliquely tilted,
and centered around a horizon-holding local body. This avoids another reskinned
hotspot cadence while preserving exact same-basin material continuity.

## Tactical questions to freeze

Tactical must answer all of these inside one implementation-neutral but exact
blueprint:

1. What exact pure state/source guard leaves Host 06 absent until sanitized
   Host 05 mastery, exact source/provenance identity, finite passing
   measurements, and natural-dimension decode all pass?
2. What exact source switch replaces the accepted Host 05 view only for the
   lawful post-mastery reach, preserves the accepted source for Hosts 04/05 and
   Demo Tour, and prevents the generic model-choice launcher from returning on
   later source/decode failure?
3. What exact state graph maps absent model-choice evidence to `available`,
   exact `remediation_required` to remediation, `in_progress` /
   `primary_complete` / `transfer_complete` to in progress, exact `mastered`
   to complete, and every other condition to hidden?
4. What exact `LOOK AT`, `TALK TO`, sole `USE`, and completed read-only `USE`
   actions preserve write-free observation/silence, one-hit activation, no
   duplicate launcher, and the unchanged `L02-03` entry?
5. What exact DOM source order, native semantics, labels, polite announcement,
   focus landing, Terminal inertness, Close/Escape return, miss/remediation,
   primary/transfer/explanation/mastery recovery, reload, Meadow return, and
   malformed fallback implement the directorial attention path without a
   sensory-only cue?
6. What exact null-first schema and later measurement contract freeze
   `source`, `physical`, `activation`, `label`, `protected`, and all six
   `layouts` without supplying candidate coordinates before Quartermaster?
7. What exact centered transform, floor lattice, source/DOM identity, physical
   center, activation containment, target, label, anchor, retention,
   nonoverlap, and before/after-focus stability proof applies at desktop,
   laptop, narrow, effective `200%`, retained `320x180`, and retained
   `320x240`?
8. What exact accessible name/alternative-text slot, keyboard/pointer/touch/
   switch parity, forced-color `3px Highlight`, reduced-motion behavior, and
   no-color/no-sound/no-motion meaning keep the six layouts equivalent?
9. What exact seven-slot semantic ledger hands Quartermaster unseen,
   available, in-progress, remediation, complete, returned, and next-boundary
   meaning without writing final prose or turning the relation into an answer
   key, invitation, reward, purpose claim, or world response?
10. What exact Combat-ready acceptance matrix preserves the prompt, candidate
    identity layers, pre-generation seventeen-file manifest gate, role order,
    no-reveal boundary, PBA caps, validation ladder, cleanup, rollback,
    process records, and protected state?

## Frozen candidate and media identities

Do not collapse Combat code candidate; immutable seventeen-tuple baseline;
attempt prompt/ordinal/tool-returned path; byte-identical temporary evaluation
file; selected raster bytes at the exact product path; Quartermaster content
commit; Image presentation commit; and Intelligence release candidate. None is
canon or maturity evidence before independent release.

The two and only two media-owned workspace paths remain:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md
```

The selected candidate remains exact opaque 8-bit sRGB PNG `3840 x 2160`, at
most `12,000,000` bytes. The selected runtime result remains exactly `18`
media files and `37,410,731 + selectedBytes`, never above `49,410,731`.
Every pre-existing accepted path/byte/SHA tuple remains immutable.

Tactical reads no media byte, candidate, managed output, OS-temp candidate, or
manifest tuple. Candidate-specific source and geometry values remain `null`.

## Frozen presentation, behavior, and hard stop

The complete relation stays inside the shell's exact source bands and uses one
centered no-crop `<img>` at all six layouts. `PHY-01..12`, physical/activation/
label/center/nonoverlap mapping, `44 x 44 CSS px` targets, exact accessible
name/label states, pointer/touch/Enter/Space/switch parity, focus recovery,
forced-color `3px Highlight`, reduced motion, and equivalent meaning remain
mandatory.

Host 06 remains absent until exact sanitized Host 05 mastery and complete
source proof. The generic model-choice launcher remains byte/behavior unchanged
only in Combat's later inert `enabled=false` state; after lawful enablement it
is absent and never returns on source/decode failure. Sole Host 06 `USE` opens
unchanged `L02-03`; `LOOK AT` and `TALK TO` write nothing; completed `USE` is
read-only.

Learning/evaluator/remediation/sanitizer, privacy, save schema, route, Demo
Tour, offline/no-authority behavior, world nonresponse, READY/NOT YET READY
parity, null deltas, shared ending, and `successor=null` remain exact. No
runtime external request, dependency, source map, reveal, publication,
schedule, automation, or maturity advance is allowed.

The hard stop remains immediately after exact `L02-03` mastery. No Host 07 or
later expression, Crown change, Witness work, City repair, later-host work,
Measured Horizon literary work, new branch, lesson, reward, access, identity,
authority, response, successor, RP-013, or post-ending content is authorized.

## Numeric and validation envelope

- JavaScript: affected `<=1,679,393`; global `<=1,703,258`.
- CSS: affected `<=119,547`; global `<=119,672`.
- Modules: affected `<=218`; global `<=222`; source maps `0`.
- Decode `<=250ms` cold / `<=100ms` warm; image-ready `<=750ms`.
- Attributable CLS `<=0.01`; total CLS `<=0.05`; activation `<=2ms`;
  sampled task `<=100ms`.
- Focused `<=30s`, related/full/build `<=60s`, and exactly one complete E2E
  `<=180s` with no retry.

`FRRC-003-v1` remains the new command manifest; `FRRC-001-v1` and
`FRRC-002-v1` remain immutable. `FRAM-001-v1` remains the immutable
seventeen-tuple manifest after Combat's later passing gate. Tactical does not
create, populate, hash, or execute either new artifact.

## Preserved process and protected state

All thirteen inherited records remain separate and **OPEN**: VR-17, VR-23,
VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The Commandant filename/search-scope record remains
one additional separate **OPEN** record. All prior process-only recurrences
remain recorded. None is waived, merged, closed, cured, renumbered, or
candidate evidence.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**. Its contents and meaning remain
unknown. The repository QA quarantine, protected PDF, training directory,
browser/profile/save state, hidden lore, user work, accepted-media bytes,
managed/temp roots, and opaque residuals remain protected.

Recon performed no image generation, edit, variation, candidate inspection,
selection, import, copy, movement, deletion, publication, reveal, manifest
byte read, runtime implementation, test, build, preview, browser, E2E, save,
external service, cleanup, schedule, automation, production, release, maturity
update, OPEN-record closure, or residual access.

## Exact next action

One fresh Tactical Operations Specialist reads in full `AGENTS.md`, this
handoff, `FIRST_RUN_AGENT_WORKFLOW.md`, the canonical registry, the complete
Tactical profile, complete `FRSH-005-v1`, and complete `FRDT-005-v1`, then
reads only the exact current runtime/state/focus/return and shell authorities
required by that profile.

Issue exactly one versioned Player Experience Blueprint for `FRSH-005-v1`.
Freeze the exact Host 05-to-06 state/action/source switch, generic-launcher
conditional absence, semantic order, focus, announcement, recovery, malformed
fallbacks, six-layout centered mapping, null candidate-measurement schema,
accessibility, seven-slot content ledger, and Combat-ready acceptance matrix.

No Recon revision, Combat, Quartermaster, Image, Intelligence, manifest byte
read, media tool, candidate inspection, generation, selection, import,
runtime implementation, final prose, reveal, schedule, automation, maturity
update, OPEN-record closure, residual access, release, or `FIRST RUN COMPLETE`
begins from this handoff.
