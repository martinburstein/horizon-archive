# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`MISSION REVISE / DATA-URL PAYLOAD INGRESS REQUIRES
OPERATIONS WORK ORDER REVISION / ORDINAL 1 CONSUMED / NO MEDIA CANDIDATE`**

Martin's controlling decision: **`Authorized new Drowned Media for Host 06`**

Work Order / viability / effective Science variances: `FRWO-005-v2` /
`FRVE-005-v2` / `FRVE-005-v2-VR-01` / `FRVE-005-v2-VR-02`

Base shell / effective Mission variances: `FRSH-005-v1` /
`FRSH-005-v1-VR-01` / `FRSH-005-v1-VR-02` /
`FRSH-005-v1-VR-03` / `FRSH-005-v1-VR-04` /
`FRSH-005-v1-VR-05`

Combat functional close: `FRCE-005-v1-VR-04 / PRODUCTION FUNCTIONAL`

Quartermaster return: `FRCA-005-v1 / HOLD`

Mission return: `FRSH-005-v1-VR-05 / REVISE`

Immutable accepted-media manifest: `FRAM-001-v1`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Frozen candidate tree:
`09da6293d72c8123b8d9673bd8e41329338e8d13`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next owner: **fresh Operations Planning Major /
`operations_planning_major`**

## Mission adjudication

The built-in tool contract exposes one direct `image_url` string plus an
optional `output_hint`. A future exact `data:image/png;base64,...` payload can
in principle be strictly decoded into one predeclared fresh GUID OS-temp
candidate without parsing the hint, discovering the managed parent, using a
fallback, or revealing an image.

Mission cannot authorize that route as a narrow result-adapter variance. The
effective Work Order and Science envelope require a unique local absolute
pathname returned directly by the tool, a known tool-managed file, a managed-
to-temp byte/SHA identity copy, and immediate deletion/proven absence of that
exact managed file. A payload-first route instead makes the data URL the
ingress authority, materializes the first known file in OS temp, compares
decoded payload identity to that file, and leaves any tool-managed output
unknown and intentionally untouched.

Those are changes to `FRWO-005-v2` and `FRVE-005-v2`, not merely Mission's
adapter spelling. `FRSH-005-v1-VR-05` therefore returns to Operations as the
earliest owner. Any accepted Operations revision must then receive fresh
Science viability review before a fresh Mission shell may prospectively
authorize Quartermaster ordinal 2.

## Exact next action

One fresh Operations Planning Major reads this handoff, complete
`FRSH-005-v1-VR-05`, complete `FRCA-005-v1`, complete `FRWO-005-v2`, complete
`FRVE-005-v2`, and the effective shell through `FRSH-005-v1-VR-04`, then
issues exactly one versioned `WORK ORDER READY`, `REVISE`, or `HOLD` artifact
adjudicating whether future ordinals may use one exact tool-returned PNG data
URL as the sole ingress authority while every managed output remains opaque
and undiscovered.

If accepted, the prospective contract must preserve ordinal 1 as permanently
consumed; maximum three total ordinals; no accepted-media input/edit; exact
single result/payload shape; literal `data:image/png;base64,` grammar; strict
base64 decode; one create-new GUID OS-temp candidate; decoded-payload/temp
byte-and-SHA identity; pre-pixel PNG/sRGB/dimension/size/metadata/decode gates;
discarded payload/result references; no managed-parent parsing, listing,
search, inference, discovery, or cleanup; exact temp cleanup; one selected
source maximum; no reveal; and every fixed canon/learning/state/save/route/
world/ending/protected-state boundary.

Operations may not call the generator, inspect or expose a result, recover or
clean ordinal 1, authorize Quartermaster directly, decode a payload, inspect
pixels, import media, write copy/provenance, begin Image, run E2E, update
maturity, close an OPEN record, inspect VR-65, reveal, schedule, automate,
release, or call `FIRST RUN COMPLETE`.

## Preserved state

Ordinal 1 remains consumed and is neither candidate evidence nor a
`PHY-01..12` rejection. Its non-path hint, `image_url`, managed output, and
other result details remain opaque and uninspected. No Host 06 media/content
candidate exists. No Quartermaster attempt 2 is authorized.

The accepted-media baseline remains exact `17 / 37,410,731`; product/test
code, immutable manifest, candidate/tree, registry/source/geometry/protected/
layout scalars, seven copy slots, and alt slot remain unchanged. Complete E2E
remains correctly unrun.

Inherited functional gates remain frozen and were not replayed: focused
`50/0`, legacy static-contract `29/0`, learning/privacy `17/0`, related `58/0`,
validators `40/40`, cold full `979/0/0`, production and TD-012 fixture builds
PASS, production PBA JavaScript `1,676,508`, CSS `119,394`, modules `217`,
accepted media `17 / 37,410,731`, source maps `0`, served preflight PASS,
owned process/port cleanup PASS, and `git diff --check` PASS.

Maturity remains unchanged. `FR-03` remains continuity `FR2`; physical-host
expression `FR0 - 1 accepted shared compression / 1 exact / 10 missing`;
learning `FR2`; behavior/save/recovery `FR1`; content `FR2`; presentation
`FR3`; and prior bounded release proof `FR4`.

All thirteen inherited process records remain separate and **OPEN**: VR-17,
VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The separate Commandant filename/search-scope record
remains **OPEN**. This Mission stage's broad initial status and tracked
filename locator outputs remain process-only recurrences under applicable
still-OPEN records and close nothing.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**, unknown and inaccessible. The
ordinal-1 managed result remains separate and is not merged with VR-65.

Repository QA quarantine, protected PDF, training directory, real browser /
profile/save, hidden lore, user work, accepted-media pixels, managed/temp
parents, and opaque residuals remain protected.
