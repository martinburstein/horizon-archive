# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`WORK ORDER READY / FRWO-005-v15 / SOLE F1 /
HOST06-GEN-PROMPT-v3 CENTER-CORE CORRECTION / NO API OR MEDIA / FRESH SCIENCE
NEXT`**

Exact next owner: **one fresh Office of Science Administrator /
`office_of_science_administrator`**

Read the mandatory intake and Science profile in full, then read
`FRWO-005-v15`, `FRWO-005-v14`, `FRCA-005-v12`, `FRWO-005-v13`,
`FRVE-005-v11`, `FRSH-005-v1-VR-43`, current release map/scoreboard,
`FRPB-001-v2`, `FRCL-004-v2`, `FRDT-005-v1`, `FRDT-005-v1-VR-01`,
`FRPX-005-v1`, and `FRPX-005-v1-VR-01` at their exact current sections.

Independently evaluate exactly one fresh paid attempt domain `{F1}`. Reproduce
the frozen `HOST06-GEN-PROMPT-v3` identity `5,386` ASCII/UTF-8 bytes / SHA-256
`1c55c9c5d6dbbf7c83a0fcadde4180e974db051dc9f883542aadf3ea2f9b8a1f`.
Prove or reject the substantially inset center-safe geometry: complete relation
inside `x=[.36,.64], y=[.32,.68]`, width `.24-.27`, height `.25-.29`, center
`x=.50-.51, y=.48-.50`, top `>=.33`, bottom `<=.65`; complete fragment group
inside `x=[.27,.38], y=[.62,.80]` with at least three distinct fragments each
`>=.021` source width; dry approach inside `x=[.27,.41], y=[.54,.82]`; and all
essential reading inside `x=[.27,.64], y=[.32,.82]` while retaining every
physical, layout, accessibility, learning, privacy, save, offline, performance,
identity, parser, provenance, cleanup, canon, route, and ending gate.

Use only the fresh literal F1 paths in `FRWO-005-v15`. If viable, issue one
new versioned `FRVE-005-v12 / POLISH VIABILITY READY` and hand one fresh
Mission Captain the exact prompt/path/attempt/validation envelope. If any
predicate cannot coexist, issue `HOLD`; do not widen a band, weaken a physical
fact, substitute responsive repair, or invent a different prompt.

Science performs no credential read, request construction, API call, media or
pixel operation, import, preview, reveal, implementation, Image work, E2E,
maturity advance, or release. F1 is the complete attempt domain and begins
only at a later shell-authorized sole `SendAsync`; there is no F2, retry,
relaunch, fallback, edit, variation, reference input, second output, alternate
transport/model/endpoint, or reuse of E1 or earlier attempts.

Preserve the one-path rail, exact Host 05 gate, sole unchanged `L02-03`, lesson
ownership, surface canon, null deltas, shared RP-012 ending, and
`successor=null`. Preserve the Operations protected-filename enumeration
variance as separate and OPEN without repeating, using, closing, merging,
waiving, curing, or renumbering it. Do not access protected user state,
accepted-media pixels, historical attempts, old roots, opaque residuals,
repository QA quarantine, or hidden lore.
