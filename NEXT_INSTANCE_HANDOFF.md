# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`WORK ORDER READY / FRWO-005-v2 / MANAGED ONE-FILE
INGRESS AND IMMUTABLE MEDIA-MANIFEST GATE FROZEN`**

Martin's controlling decision: **`Authorized new Drowned Media for Host 06`**

Decision authority: `FRPB-001-v2`

Continuity authority: `FRCL-004-v2`

Selected Work Order: `FRWO-005-v2 / Stranded Lens Cradle - One New Source and
Fixed Lesson Integration`

Superseded Work Order: `FRWO-005-v1 / WORK ORDER READY`

Science return adjudicated: `FRVE-005-v1 / HOLD / BUILT-IN MANAGED-INGRESS
CONTRACT NOT AUTHORIZED`

Exact next owner: **fresh Office of Science Administrator /
`office_of_science_administrator`**

Immediate control: **one bounded `FRWO-005-v2` viability revalidation only**

Operations source inspected: `04e00139cdc1dcc723179b7235a7a1dc507906cb`

Current control source: **the dedicated Operations revision commit containing
this handoff; verify exact local `HEAD` before Science action**

Remote synchronization reference: `origin/main` remains
`d6e2386a4a00fa829f246d6c32545232d56c6fcc`; the planning HOLD chain,
Commandant reopen, Colonel lock, Operations Work Orders, Science HOLD, and this
Operations revision are intentionally unpushed.

Released authority: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Current planning controls: `FRRM-005-v2` / `FRSB-005-v2`

Released candidate identities:

```text
geometry=8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97
transport=6c64eb354b7dbb467df5725e2cae4eb67092ddc7
product=a91763e28d488f31f8cf7d40ece0b2682246ba9b
validation=4cd7fbf31291671dd28c0743b44a7c49aaad82bb
diagnostic=2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
evidence=ca89a679195c11d441a76e6c02983a6436f2ccb2
```

## Operations adjudication

`FRWO-005-v2` lawfully accepts the bounded Science staging/cleanup variance
without expanding Martin's one-source authority. It is a new versioned Work
Order, not a silent amendment of v1. It changes only the two Science blockers:

1. the staging/cleanup contract may use exactly one unique managed output path
   returned by one built-in tool call beneath the shell-frozen tool-owned
   `generated_images` root outside the Horizon repository, with rejected-
   candidate retention narrowed to bounded scalar pass/fail facts; and
2. Combat owns a read-only, pre-generation immutable manifest gate for the
   exact seventeen accepted-media literal paths, byte lengths, and SHA-256
   values.

All `FRWO-005-v1` scope and exact `FRVE-005-v1` conditional requirements
otherwise remain binding.

## Frozen managed-ingress boundary

- Maximum three calls total, sequential, exactly one returned raster per
  built-in call; no accepted/conversation/reference image input and no edit or
  variation mode.
- No CLI, REST/API, browser, alternate generator, manual-file, or other
  generation fallback.
- The tool must directly return one exact unique local absolute managed output
  path. No managed-parent listing, glob, search, enumeration, or discovery is
  permitted.
- Unknown, missing, nonlocal, multiple, ambiguous, unowned, or in-repository
  output, unavailable exact managed root, or containment uncertainty consumes
  the ordinal and stops the cycle opaque with no retry or fallback.
- Copy only the exact returned file to one predeclared `attempt-0N.png` inside
  one fresh GUID-named OS-temp evaluation child proven outside repository and
  protected roots. Byte length and SHA-256 must match managed ingress and temp
  copy before any rubric or pixel check.
- Immediately after identity proof, delete only the exact managed returned
  file and prove it absent. Missing-before-cleanup state, uncertain ownership,
  cleanup failure, or unverifiable absence stops the cycle opaque; it never
  authorizes parent listing, retry, reveal, import, or broader cleanup.
- A rejected temporary candidate consumes its ordinal, is deleted by exact
  pathname, and never enters workspace, source control, runtime, preview,
  browser, report, Markdown, publication, or reveal. Rejected attempts retain
  only bounded scalar pass/fail facts and rejection codes.
- At most one passing candidate may be copied byte-identically into
  `Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png`;
  only its provenance may enter
  `Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md`.
  No other generated raster or draft may enter the workspace.

## Frozen accepted-media manifest boundary

Before attempt one, Mission must freeze exactly seventeen repository-relative
accepted-media literal paths. Combat may use read-only byte-stream operations
on only those paths to record repository-relative path, byte length, and
SHA-256. Parent enumeration, recursive discovery, globbing, preview, decode,
render, thumbnail, metadata interpretation, and pixel inspection are
forbidden.

The gate must reconcile exactly to `17 / 37,410,731`. Missing, duplicate,
unknown, extra, inaccessible, symlink/reparse ambiguity, out-of-repository
resolution, byte/SHA drift, count/sum mismatch, or inability to establish the
literal list stops before generation. A passing manifest is the immutable
accepted-media baseline for every later media/PBA, build, rollback, and
Intelligence gate. Combat receives no generation, selection, import, edit,
cleanup, or pixel-review authority from this gate.

## Frozen conditional envelope

- Candidate: exact opaque sRGB PNG `3840 x 2160`, maximum `12,000,000` bytes.
- Attempts: maximum three, sequential, exactly one output per built-in call.
- Accepted workspace delta: at most one raster path plus its provenance path;
  no rejected draft workspace entry.
- Media result: exact `18` files and `37,410,731 + selectedBytes`, never above
  `49,410,731`.
- JavaScript: affected entry maximum `1,679,393` bytes; global maximum
  `1,703,258` bytes.
- CSS: affected entry maximum `119,547` bytes; global maximum `119,672` bytes.
- Modules: affected entry maximum `218`; global maximum `222`; source maps `0`.
- Runtime: candidate decode <=`250 ms` cold and <=`100 ms` warm; local image-
  ready <=`750 ms`; attributable CLS <=`0.01` and total CLS <=`0.05`;
  activation handler <=`2 ms`; sampled task <=`100 ms`.
- Validation calls: focused <=`30 s`, related <=`60 s`, full <=`60 s`, each
  build <=`60 s`, and one E2E call <=`180 s` with no retry.

Current released PBA remains JavaScript `1,667,393`, CSS `119,247`, modules
`217`, media `17 / 37,410,731`, and maps `0`.

All exact `FRVE-005-v1` `PHY-01..12`, source-band, six-layout centered no-crop,
hotspot/label/center/nonoverlap, meaning/accessibility, fail-closed state, sole
unchanged `L02-03` routing, provenance, PBA, validation, no-reveal, rollback,
role, canon, learning, privacy, save, route, world-response, and shared-ending
boundaries remain unchanged.

## Preserved classifications and protected state

All thirteen inherited records remain separate and **OPEN**: VR-17, VR-23,
VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The Commandant-stage broad First Run filename/search-
scope record remains one additional separate OPEN record. All previously
recorded process-only recurrences remain recorded. This Operations stage adds
no closure, waiver, merge, cure, renumbering, or candidate evidence.

The initial broad worktree status and exact-ID tracked First Run locator/path
output in this Operations stage are retained only as recurrences under still-
OPEN VR-24, still-OPEN VR-23, and the separate Commandant search-scope record.
No disclosed untracked or protected path was opened or used as evidence.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**. Its contents and meaning remain
unknown. No protected QA content, accepted media, user/browser/save path, or
residual was opened, inspected, inferred, retried, cleaned, moved, or deleted.

No image generation, edit, variation, import, selection, preview, publication,
reveal, game, test, build, browser, save, external service, cleanup, schedule,
automation, production, push, or release operation occurred.

## Exact next action

One fresh Office of Science Administrator reads in full `AGENTS.md`, this
handoff, `FIRST_RUN_AGENT_WORKFLOW.md`, the canonical registry, the complete
Science profile, `FRPB-001-v2`, `FRCL-004-v2`, complete `FRWO-005-v2`, and
complete `FRVE-005-v1`, plus only its exact lawful current controls.

Science independently revalidates the managed one-file ingress, exact literal
seventeen-path Combat manifest gate, and every retained conditional envelope
requirement. It issues exactly one new versioned `POLISH VIABILITY READY`,
`REVISE`, or `HOLD` artifact for `FRWO-005-v2`.

No Mission, Reconnaissance, Tactical Operations, Combat, Quartermaster, Image
Specialist, Intelligence, media tool, candidate inspection, preview, reveal,
schedule, automation, push, or release action begins from this artifact.
