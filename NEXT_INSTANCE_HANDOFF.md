# Next Instance Handoff

Workflow: `FIRST_RUN_AGENT_WORKFLOW.md`

Current disposition: **`FIRST RUN SHELL READY / FRSH-007-v1 / HOST 07
IMAGEGEN CLI / FIVE TOTAL ATTEMPTS`**

Exact next owner: **one fresh Reconnaissance Sergeant /
`reconnaissance_sergeant`**

Martin explicitly authorized the installed imagegen CLI fallback for Host 07
with five total attempts. The new sequential ledger is
`{H7-1,H7-2,H7-3,H7-4,H7-5}`, all unconsumed. The exact bundled CLI is
`image_gen.py` at `35266` bytes / SHA-256 `c2cdb052...d20b05`; the API key is
present locally and remains secret. No call, candidate, import, implementation,
maturity advance, or reveal exists.

Mission issued the conflict-free shell. `{H7-1..H7-5}` remain unconsumed;
exact CLI/options, absent GUID scratch root, candidate/product paths, sequential
ledger, custody/cleanup, physical/layout/accessibility/state/learning budgets,
role ownership, validation, rollback, protected boundaries, and no reveal are
frozen.

Read the full Recon profile, `FRSH-007-v1`, current planning chain, exact Host
06 release, game on paper, accepted visual controls, and only needed runtime
sources. Freeze one directorial interpretation and five complete independent
prompt files/texts before H7-1. Issue DIRECTORIAL LOCK/REVISE/HOLD and route
fresh Tactical on PASS. Do not generate or inspect media.

Preserve exact Hosts 01-06 and accepted media, sole `L02-03`, Host 07 ownership
of unchanged `L03-01 + L03-02`, the one-path rail, learning/privacy/save/
offline/accessibility rules, invariant world, RP-012, `successor=null`, every
OPEN process record, both filename-enumeration records, protected exclusions,
and VR-65. Do not implement or reveal Host 08 or later work.
