# First Run Shell Variance Reissue - One Corrected Diagnostic-Control Verification

Variance ID: `FRSH-003-v1-VR-47`

Disposition: **`FIRST RUN SHELL READY / ONE CORRECTED DIAGNOSTIC-CONTROL
VERIFICATION / FRSH-003-v1-VR-47`**

Stage gate remains: **`HOLD / PRODUCTION FUNCTIONAL NOT ISSUED`**

Release state remains: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / governing shell: `FRWO-003-v1` / `FRSH-003-v1`

Mission source inspected:
`6f2651f86e994d040c3edb5edcdaa5083dca7114`

Geometry evidence-source correction candidate:
`8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97`

Diagnostic transport / report lineage:
`6c64eb354b7dbb467df5725e2cae4eb67092ddc7` /
`0ac9023037873004f7bd5d75c16f80953d770a4d`

Exact immutable product / validation / diagnostic / evidence identities:

```text
a91763e28d488f31f8cf7d40ece0b2682246ba9b
4cd7fbf31291671dd28c0743b44a7c49aaad82bb
2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
ca89a679195c11d441a76e6c02983a6436f2ccb2
```

Recorded: **2026-08-10**

## Context reuse and independent candidate adjudication

Fresh-child capacity remains unavailable. Root expressly authorized this
already-active Mission Captain context to independently adjudicate VR-46 and
issue this bounded shell. The reuse is disclosed, is not candidate evidence,
and waives no boundary.

Mission independently inspected the exact candidate. The delta contains only
`playtest/e2e-playthrough.mjs` and the existing Sixfold static test. The E2E
harness contains live-connectivity, owner-document, finite, and empty-source
guards; a shared geometry epoch; two-frame settle; and all nine required source
names. Static control covers shared epoch, disconnected source, and empty
source. Neither failing layout is hardcoded.

Mission accepts the exact authorized proof: Node syntax PASS; exact frozen
focused aggregate `68/68` PASS; two-file allowlist/integrity PASS; no E2E,
preview, browser, root, build, broader test, validator, PBA, served identity,
summary, verifier, product, content, media, protected, or user-state action.
The correction is technically ready for one live diagnostic verification.

## New command-selection divergence

Before reading the frozen aggregate, Combat ran the modified static file alone
and obtained `9/9`. That standalone subset was outside VR-46's exact command
authority. Mission classifies:

**`UNAUTHORIZED DIVERGENCE / VALIDATION COMMAND SELECTION / OPEN / VR-46
COMBAT`**.

The `9/9` result is not acceptance evidence and is not merged with `68/68`.
It changed no state and ran no live or broader gate. The later exact focused
aggregate and candidate-integrity proof remain lawful and sufficient; no
additional static rerun is required. The prior seven OPEN classifications
remain separate, producing eight OPEN classifications total.

## Exact one-live-verification authority

Exact next owner is a **fresh Combat Engineer** if capacity permits; otherwise
context reuse must be disclosed.

Run one fail-closed wrapper only:

1. Create exactly one new direct-child OS-temp root named
   `horizon-archive-frrc002-<lowercase-D-GUID>`. Apply the frozen normalized
   direct-parent, temp-descendant, bidirectional repository-exclusion,
   anchored-name, pre-nonexistence, and disclosed-predecessor-distinctness
   checks before creation and deletion. Emit no path.
2. Require exact ports `4173/4184` clear. Launch the manifest production and
   TD-012 fixture previews once each with hidden PassThru-owned PIDs and obtain
   root readiness `200/200`. Inherit VR-42 served identity; do not rerun it.
3. Run exactly one complete E2E with timeout `180000ms`:

```text
node playtest/e2e-playthrough.mjs
```

   Use immutable product candidate
   `a91763e28d488f31f8cf7d40ece0b2682246ba9b`, diagnostic/probe candidate
   `2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc`, the exact fresh root, and frozen
   PBA/media environment flags. The executing harness lineage must include
   geometry correction `8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97`
   and diagnostic transport `6c64eb354b7dbb467df5725e2cae4eb67092ddc7`.
   Capture and suppress the complete stream. No second E2E is authorized.
4. Require exactly one diagnostic, `checkInventoryExact=true`, integer failure
   count, and owned-lifecycle `browserClosed=true`.
5. On E2E failure or positive failure count, require no summary; run the exact
   manifest diagnostic transport once before cleanup; require its complete,
   canonical, ordinal-sorted, count-consistent base64 failure paths/layouts;
   retain it in memory; run no verifier; and authorize no rerun.
6. Only on E2E exit `0` and failure count `0`, require exactly one summary with
   all six layouts, full rail, both equal MH-40 outcomes, null outcome, zero
   runtime errors, offline/runtime-request/media/PBA gates, Host 05 `<=2ms`,
   sampled task `<=100ms`, exact identities, and pass true. Then invoke the
   exact manifest summary verifier once, capture/suppress output, and require
   verifier PASS.
7. In an unconditional final block, stop both owned preview PIDs, require both
   exact ports clear, retain any failure transport, re-prove root boundaries,
   delete only the fresh root, require it absent, then emit one scalar line.

Use the exact VR-45 scalar schema and mode semantics. `success` requires E2E
exit `0`, diagnostic inventory exact/failure `0`, browser closed, one summary,
one verifier PASS, and exact cleanup. `failure-transport` requires nonzero E2E
or positive failure count, inventory exact, browser closed, no summary or
verifier, one complete transport PASS, and exact cleanup. Any other result is
control failure.

Return only one of:

- **`ONE CORRECTED DIAGNOSTIC-CONTROL VERIFICATION PASS / STOP / RETURN TO
  FRESH MISSION`**;
- **`CORRECTED COMPLETE E2E FAILURE / FAILURE TRANSPORT PASS / DIAGNOSTIC ONLY
  / HOLD / NO VERIFIER / NO RERUN / RETURN TO FRESH MISSION`**; or
- **`HOLD / CORRECTED DIAGNOSTIC-CONTROL FAILURE / NO RERUN / RETURN TO FRESH
  MISSION`**.

No retry, diagnosis, repair, second E2E/extractor/verifier, product/media/
protected action, build, test, validator, PBA, served-identity rerun, or post-
cleanup command is authorized.

## Preserved boundaries

The cumulative VR-22 tests/validators, VR-30 production build, VR-35 fixture
build/corrected PBA, VR-42 formal served identity, and VR-46 static correction
proof remain accepted. No product change, `PRODUCTION FUNCTIONAL`, release, or
maturity advance is authorized.

All eight OPEN divergences remain separate and OPEN: the prior seven from
VR-17, VR-23, VR-24, VR-25, VR-28, VR-39, and VR-41, plus VR-46 validation
command selection. None is waived, merged, closed, cured, or used as candidate
evidence.

Diagnostic output remains non-release, forbidden verifier input, and no-retry.
All frozen player, learning, privacy, save, accessibility, route, world,
MH-40, null-delta, `successor=null`, ending, media, and one-E2E meanings remain
exact. No downstream stage, release, schedule, automation, reveal, or `FIRST
RUN COMPLETE` action is authorized.

Mission Captain signs **`FIRST RUN SHELL READY / ONE CORRECTED DIAGNOSTIC-
CONTROL VERIFICATION / FRSH-003-v1-VR-47`**.
