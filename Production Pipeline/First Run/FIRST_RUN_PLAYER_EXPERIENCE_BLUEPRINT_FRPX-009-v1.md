# Horizon Archive First Run Player Experience Blueprint - Host 09

Blueprint ID: `FRPX-009-v1`

Stage / owner: Tactical Operations Specialist / `tactical_operations_specialist`

Disposition: **`PLAYER EXPERIENCE READY / HOST 09 NULL-FIRST / FIVE CANDIDATE RECORDS`**

Shell / treatment: `FRSH-009-v1` / `FRDT-009-v1`

Source inspected: `d1f07e3f`

## State and replacement graph

Current release: lawful Host 08 plus strict `L03-03` mastery exposes generic
Text Analysis, then generic Speech Workloads. Inert Combat state has
`source.enabled=false` and changes nothing. Accepted state requires the same
predecessor plus exact source/provenance/measurement/decode lawfulness; one
committed predecessor paint then replaces the source with Host 09, focuses one
native target once, emits one factual polite notice, and irreversibly removes
both generic launchers. Later guard failure hides Host 09 and never restores a
bypass.

State derives from sanitized Text Analysis and Speech evidence:

- absent predecessor/mastery or malformed source -> `hidden`;
- both evidence absent/lawful -> `available`;
- Text or Speech remediation -> `remediation_required`;
- lawful Text progress, Text mastered with Speech incomplete, or lawful Speech
  progress -> `in_progress`;
- both strict masteries -> `complete`.

LOOK and TALK write nothing. USE opens/resumes Text Analysis until mastered,
then Speech Workloads until mastered; complete USE is read-only. Close/Escape
returns focus to Host 09 when lawful, otherwise Host 08 or first lawful control.
Reload/return/resume reconstructs from sanitized evidence and never replays the
arrival notice.

## Null-first registry

Combat declares source `{enabled:false,path:null,sha256:null,byteLength:null,
width:null,height:null,format:null,color:null,attemptId:null}`, separate null
provenance, rectangles for family/cavities/pressure/reflectionPair/nonreturn/
dryRim/wetExclusion/coupling/semanticTarget/label/protected regions, six null
layout records, seven null final-purpose copy slots, and null alt.

Accepted attempt ID is exactly `H9-1`..`H9-5`; path, prompt/hash, CLI hash,
bytes/SHA, `3840x2160`, opaque PNG, provenance, geometry, layouts, and decode
must agree. No ordinal scalar exists. Source cap is `1..30000000` and aggregate
media `<=100136520`.

## Interaction, copy, and access

There is exactly one selected image and one native Host 09 button. Pointer,
touch, Enter, Space, speech-by-name, and switch-like activation converge on one
click path with mixed-input duplicate-open protection. Accessible name is
`${verb.toLowerCase()} Flooded Choir, ${state}`. Source order is image, target,
scene status, LOOK/TALK/USE, factual dialogue, existing navigation. Hidden
state has no DOM, focus, name, announcement, credit, or save presence.

One nonpersisted transition token performs one animation-frame recheck, one
source replacement, one `preventScroll:true` focus, and one polite factual
notice. Reduced motion preserves committed order without decorative travel.
Forced colors preserves target/focus outline without image-derived meaning.
Effective-200% and narrow layouts have no horizontal escape.

Quartermaster owns seven copy slots: unseen physical materials; available
provisional Pilot name plus Suit compatibility; Text in-progress boundary;
Speech in-progress boundary; missed exact modality with answer-free
remediation; complete finalized human evidence/world unchanged; returned
unchanged relation plus nextBoundary only current visual-analysis continuation.
Alt factually names multiple water-filled stone cavities, selected reflections,
one dark nonreflective cavity, incoming material continuity, and dry rim—never
voice, song, purpose, correctness, or response.

## Geometry and private decision record

Every selected source carries every normalized rectangle and six exact layout
records from `FRVE-009-v1`. Each stores viewport, rendered/visible source,
retained area, mapped physical/semantic/label/protected intersections, logical
order, and focus bounds. All thresholds fail closed.

Each launched attempt has a bounded record: attemptId, immutable prompt path/
bytes/SHA, CLI identity, launched, exit, ordinary/one-link/length/SHA, strict
PNG/decode/opacity, technical/physical/layout/accessibility booleans,
allowlisted reason codes (`TECHNICAL`, `PHYSICAL`, `LAYOUT`, `ACCESSIBILITY`),
identity reproof, cleanup, and terminal disposition. Rejected records retain no
path/hash/pixels/description. First PASS stops later calls.

## Combat acceptance matrix

Combat proves: null-first values; five-ID source/provenance guard; independent
fail-close for source/provenance/geometry/layout/decode; Host08+L03-03 sole
predecessor; both generic launchers visible iff `enabled!==true`; enabled-
invalid never restores either; hidden zero-presence; single image/target;
LOOK/TALK zero-write; USE exact Text-then-Speech sequence and complete read-only;
exact dual-evidence state mapping; one transition/focus/notice; lawful fallback
focus; close/reload recovery; single dispatch; six-layout thresholds; 44px
target; forced-color/reduced-motion; no Demo Tour node/credit; no Host09 save/
private field; prompt/CLI ledger; failure stop; rejection cleanup; PASS stop and
byte-identical import; no media evidence; accepted Hosts 01-08 immutable; no
Host 10/canon expansion; RP-012/successor; PBA/tests/builds/served/E2E/cleanup.

Combat may change only dedicated Host 09 registry/state/view/copy/styles,
required App integration, focused tests, and functional report. It must not
populate selected values, generate, inspect, import, or reveal media.

Tactical signs **`PLAYER EXPERIENCE READY / FRPX-009-v1 / ROUTE FRESH COMBAT`**.

