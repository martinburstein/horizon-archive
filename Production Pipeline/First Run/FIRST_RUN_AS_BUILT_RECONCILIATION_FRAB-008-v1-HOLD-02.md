# First Run As-Built Reconciliation Hold - Host 08 E2E Timing

Package ID: `FRAB-008-v1-HOLD-02`

Disposition: **`HOLD / REQUIRED CORRECTION / E2E ARRIVAL TIMING`**

The sole release E2E ran once under fresh external root
`horizon-archive-host08-release-907bee2e-bce9-4dd5-886d-5e43cb445164` and
stopped after `52.724s` before Host 08 activation. The corrected harness still
waited for the prior Teacher dialogue after Control Flow mastery. Host 08's
lawful arrival effect immediately replaces that transient line, so the old
wait timed out. This is a deterministic harness timing mismatch; no product,
state, lesson, media, canon, or accessibility failure was observed.

The owned preview listener was stopped, port `5174` is clear, and the exact
external QA root and its generated captures were deleted and proved absent.
Repository QA was untouched and unused.

Classification: **`REQUIRED CORRECTION / MISSION THEN COMBAT / HARNESS ONLY`**.
Mission must adjudicate one corrected external-root E2E after Combat removes
only the obsolete pre-arrival Teacher-line wait. No other retry or product
change is authorized by this HOLD.

Intelligence signs **`HOLD / ROUTE FRESH MISSION`**.
