# First Run Shell Variance Reissue - Four-Stage Fresh Contained Corrected Diagnostic-Control Verification

Variance ID: `FRSH-003-v1-VR-50`

Disposition: **`FIRST RUN SHELL READY / FOUR-STAGE FRESH CONTAINED CORRECTED
DIAGNOSTIC-CONTROL VERIFICATION / FRSH-003-v1-VR-50`**

Stage gate remains: **`HOLD / PRODUCTION FUNCTIONAL NOT ISSUED`**

Release state remains: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Stage / stable agent: Mission Captain / `mission_captain`

Mission source inspected:
`57e8b48e2a84de83f9c1330ac43d054b5c82c1d7`

Geometry correction / diagnostic transport / report lineage:

```text
8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97
6c64eb354b7dbb467df5725e2cae4eb67092ddc7
0ac9023037873004f7bd5d75c16f80953d770a4d
```

Exact immutable product / validation / diagnostic / evidence identities:

```text
a91763e28d488f31f8cf7d40ece0b2682246ba9b
4cd7fbf31291671dd28c0743b44a7c49aaad82bb
2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
ca89a679195c11d441a76e6c02983a6436f2ccb2
```

Recorded: **2026-08-10**

## Context reuse and VR-49 adjudication

Fresh-child capacity remains unavailable. Root expressly authorized this
Mission Captain context to adjudicate VR-49 and issue the next bounded shell.
The reuse is disclosed, is not candidate evidence, and waives no boundary.

The submitted VR-49 fail-closed wrapper was rejected by the tool-safety layer
before PowerShell execution. Mission accepts the exact report that no root was
generated or created and no port, process, preview, browser, E2E, diagnostic,
extractor, summary, verifier, or cleanup action occurred. Every live invocation
count is zero.

Mission classifies **`EXECUTION-CONTROL REJECTION / TOOL SAFETY / NON-EXECUTED
/ NON-EVIDENCE / NO E2E / VR-49`**. This is neither a candidate/product finding
nor an unauthorized divergence and creates no tenth OPEN classification. It
does not spend the one-E2E budget. VR-49 authority is nevertheless exhausted;
its monolithic shape may not be reformulated or retried.

The accepted VR-48 **`ATOMIC FRESH-ROOT CONTROL PASS / ROOT DELETED / NO LIVE
ACTION`** remains exact. Its normalization, name, direct-parent, containment,
repository exclusion, predecessor distinction, nonexistence, create-once,
post-resolve identity, empty-root deletion, and `nativeExit=0` evidence is not
rerun or weakened.

## Cumulative accepted controls

The following immutable gates remain accepted without rerun:

- VR-22 focused 68, related 74, full 972, and validator 40 controls;
- VR-30 production build;
- VR-35 fixture build and corrected PBA: one module, one built asset, zero
  native assets, JS `1667393`, CSS `119247`, media 17 / `37410731`, maps 0,
  scan 0;
- VR-42 formal production/fixture served identity;
- VR-44 transport control JSON/Node/focused-68 proof; and
- VR-46 exact two-file live-DOM epoch correction Node/focused-68/integrity
  proof for candidate `8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97`.

VR-45's 114 geometry paths remain diagnostic evidence of the superseded
evidence-source epoch. They are not product/layout findings and are not
verifier input.

## Tool-policy-compatible staged orchestration

Exact next owner is a **fresh Combat Engineer** if capacity permits; otherwise
context reuse must be disclosed. Use four independent, bounded PowerShell
calls in the exact order below. Do not submit a wrapper that combines two or
more stages. Each call suppresses command/object/error/body/path output and
emits only its ordered scalar line, except the established safe base64 evidence
transport in Stage 3.

Every scalar from a completed stage must be copied exactly into the next
stage. Do not recover state by directory listing, pathname enumeration,
process enumeration, port-owner discovery, wildcard search, or inference.

If any stage is rejected by tool policy or fails, the shell is exhausted and
no same-context reformulation or rerun is authorized. If Stage 1 completed,
Stage 4 remains independently authorized and mandatory after any later failure
or rejection. If Stage 2 created either preview, its emitted numeric owned PID
must be passed to Stage 4. A control rejection before Stage 3 execution does
not spend the E2E budget but does not permit reuse of this shell.

### Stage 1 - create, prove, and transport one root identity

Run one small atomic root-only call using the exact proven VR-48 protocol:

1. normalize OS temp and repository;
2. generate one lowercase `horizon-archive-frrc002-<GUID>` direct child;
3. prove anchored leaf, exact direct parent, strict temp containment,
   bidirectional repository exclusion, predecessor distinction, and
   nonexistence;
4. create exactly once with `New-Item -ItemType Directory -Path $candidate
   -ErrorAction Stop`, suppressing the returned object; and
5. same-call resolve with `Resolve-Path -LiteralPath -ErrorAction Stop` and
   re-prove exact identity plus every post-create predicate.

Retain the root rather than deleting it. Encode the normalized resolved root's
UTF-8 bytes as standard base64 and compute lowercase SHA-256 over those exact
UTF-8 bytes. Emit no raw path or GUID. Emit exactly:

```text
stage=1 rootControlPass=<0|1> preExists=<0|1> createAttemptCount=<0|1> createExit=<0|1> postExists=<0|1> rootRetained=<0|1> failureRootCleanupAttempt=<0|1> failureRootDeleted=<0|1> rootTokenB64=<base64-or-empty> rootTokenSha256=<64-lowercase-hex-or-empty> nativeExit=<0|1>
```

Exact PASS is stage 1; every VR-48 pre/post predicate true; `preExists=0`;
create attempt 1, exit 0; post exists/root retained 1/1; a nonempty canonical
base64 token whose decoded UTF-8 bytes hash to the emitted digest; failure
cleanup attempt/deleted 0/0; and native exit 0. On failure after lawful
creation, attempt exact empty-root deletion in `finally`, report the attempt
and deletion scalars, emit empty token/digest, and stop. No port or live action
is allowed in Stage 1.

### Stage 2 - re-prove root, launch, own, and ready previews

Run one preview-only call. Accept the exact Stage 1 token and digest as literal
inputs; base64-decode once, reject invalid UTF-8 or NUL/control characters,
normalize once, recompute the digest, and require exact digest equality.
Re-prove directory existence, anchored leaf, direct OS-temp parent, strict
temp containment, bidirectional repository exclusion, and predecessor
distinction before any port query.

Require exact ports 4173 and 4184 clear. Start production on 4173 and fixture
on 4184 once each, hidden and with exact `PassThru` ownership, using the proven
direct Node/Vite launcher forms. Require each returned PID to be positive,
live, and the exact listener owner for its assigned port without fallback
ownership discovery. Run bounded suppressed `Invoke-WebRequest
-UseBasicParsing` root readiness and require HTTP 200 for both.

Emit exactly:

```text
stage=2 rootTokenMatch=<0|1> prePortsClearCount=<0..2> prodLaunchAttempts=<0|1> fixtureLaunchAttempts=<0|1> prodPid=<positive-integer-or-0> fixturePid=<positive-integer-or-0> ownedListenerCount=<0..2> readiness200Count=<0..2> inCallFailurePidCleanupCount=<0..2> previewStagePass=<0|1> nativeExit=<0|1>
```

Exact PASS requires root-token match 1, pre-ports clear 2, launch attempts 1/1,
two positive owned PIDs, owned listener count 2, readiness 200 count 2,
in-call failure cleanup count 0, preview-stage pass 1, and native exit 0. On a
started-preview failure, stop only
the locally returned owned PID or PIDs in the same call when safe, emit their
numeric values and the count stopped or already absent through
`inCallFailurePidCleanupCount`, then proceed
only to Stage 4. Never recover or substitute a PID by enumerating processes or
listeners. No E2E, deep/assets identity, build, PBA, test, validator, summary,
or verifier action is allowed in Stage 2.

### Stage 3 - exactly one E2E and one mutually exclusive evidence branch

Run one E2E/evidence call. Accept the exact Stage 1 root token/digest and Stage
2 numeric PIDs as literal inputs. Repeat the root token decoding, digest check,
and full containment/identity proof. Require both positive PIDs live, require
each to remain the exact owner of its assigned port, and require one bounded
root HTTP 200 readiness check per preview. Do not launch or replace a process.

Only after all Stage 3 preconditions pass, invoke exactly once:

```text
node playtest/e2e-playthrough.mjs
```

Use the FRRC-002 environment contract, the immutable product identity
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`, validation identity
`4cd7fbf31291671dd28c0743b44a7c49aaad82bb`, diagnostic/probe identity
`2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc`, geometry correction
`8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97`, transport candidate
`6c64eb354b7dbb467df5725e2cae4eb67092ddc7`, the exact decoded retained QA
root, and the frozen PBA flags. Apply the existing 180000 ms timeout. Do not
pipe, truncate, duplicate, or asynchronously defer diagnostic evidence.

Synchronously read exactly one diagnostic after the E2E process ends while the
root still exists. Capture `checkInventoryExact`, `failureCount`, and
`browserClosed`.

If E2E exit is nonzero, inventory is not exact, `failureCount` is positive, or
`browserClosed` is not true, do not create a summary and do not invoke the
verifier. Invoke the accepted diagnostic transport exactly once with its 60000
ms timeout, synchronously capture the full canonical compact sorted
`failurePaths` and `failuresByLayout` safe base64 plus lifecycle scalars, and
emit that evidence before Stage 4.

Only if E2E exit is 0, inventory is exact, failure count is 0, and browser is
closed may the call emit exactly one complete summary and invoke exactly one
verifier. The summary must contain all six layouts, the full rail, both MH-40
outcomes, null-delta/runtime/offline/performance controls, and every frozen
accessibility/save/route/ending meaning. Exact success additionally requires
exactly one verifier PASS. Diagnostic evidence is forbidden verifier input.

Emit exactly one stage scalar plus the conditionally authorized summary or
failure transport:

```text
stage=3 rootTokenMatch=<0|1> ownershipPass=<0|1> readiness200Count=<0..2> e2eAttemptCount=<0|1> e2eExit=<integer-or-NA> checkInventoryExact=<0|1|NA> failureCount=<integer-or-NA> browserClosed=<0|1|NA> transportAttemptCount=<0|1> transportExit=<integer-or-NA> summaryCount=<0|1> verifierAttemptCount=<0|1> verifierPassCount=<0|1> stageOutcome=<PRECONDITION_HOLD|DIAGNOSTIC_HOLD|SUCCESS> nativeExit=<0|1>
```

Exactly one complete E2E invocation is permitted. If its process begins or
returns, the budget is consumed regardless of outcome. No retry, repair,
alternate subset, second browser, second diagnostic pass, extra transport,
second summary, or second verifier is permitted.

### Stage 4 - independently authorized unconditional exact cleanup

After any completed Stage 1, run exactly one cleanup-only call even if Stage 2
or Stage 3 failed or a later call was rejected by tool policy. Pass the exact
root token/digest and any numeric PIDs returned by Stage 2. Decode and re-prove
the root exactly as above. Treat only the two transported positive PIDs as
owned; do not discover or substitute process ownership.

Stop each transported owned PID once if still live. Require ports 4173 and
4184 clear without stopping any unowned listener. Delete only the transported
root after revalidating exact identity, direct parent, strict temp containment,
bidirectional repository exclusion, predecessor distinction, and leaf shape.
Require root absence. Do not target an alternate, inferred, unresolved,
repository, predecessor, enumerated, or wildcard path.

Emit exactly:

```text
stage=4 rootTokenMatch=<0|1> ownedPidCount=<0..2> pidStopAttemptCount=<0..2> ownedPidsStoppedOrAbsentCount=<0..2> portsClearCount=<0..2> rootCleanupAttemptCount=<0|1> rootDeleted=<0|1> cleanupPass=<0|1> nativeExit=<0|1>
```

Exact cleanup PASS requires token match 1, every transported owned PID stopped
or already absent, ports clear 2, root cleanup attempt/root deleted 1/1,
cleanup pass 1, and native exit 0. A cleanup failure remains HOLD and must
return its scalars plus the still-valid root token/digest to Mission; no
alternate cleanup or retry is authorized under this shell.

## Exact return dispositions

Success requires Stage 1 PASS, Stage 2 PASS, Stage 3 `SUCCESS` with E2E attempt
1/exit 0, diagnostic inventory exact/failure 0/browser closed, summary 1,
verifier attempt 1/PASS 1, and Stage 4 exact cleanup PASS. Return:

**`ONE COMPLETE CORRECTED E2E PASS / DIAGNOSTIC INVENTORY EXACT / FAILURE 0 /
BROWSER CLOSED / SUMMARY 1 / VERIFIER 1 PASS / EXACT CLEANUP / STOP / RETURN TO
FRESH MISSION`**.

A diagnostic outcome returns **`HOLD / DIAGNOSTIC FAILURE / FULL TRANSPORT
CAPTURED / NO VERIFIER / NO RERUN / EXACT CLEANUP RESULT / RETURN TO FRESH
MISSION`**. A pre-E2E rejection/failure returns **`HOLD / EXECUTION CONTROL /
NO E2E / NO RERUN / EXACT CLEANUP RESULT / RETURN TO FRESH MISSION`**.

Combat may not declare `PRODUCTION FUNCTIONAL`, advance maturity, or authorize
a downstream stage.

## Preserved boundaries and OPEN classifications

No product, content, media, asset, lore, curriculum, save, route, ending,
accessibility, privacy, or release change is authorized. Every frozen product,
player, learning, privacy, save, accessibility, route, world, MH-40,
null-delta, `successor=null`, ending, media, and one-E2E meaning remains exact.

All nine classifications remain separate and OPEN:

1. `UNAUTHORIZED DIVERGENCE / PROTECTED PATH ENUMERATION / OPEN / VR-17`;
2. `UNAUTHORIZED DIVERGENCE / BROAD REPOSITORY FILENAME ENUMERATION / OPEN /
   VR-23`;
3. `UNAUTHORIZED DIVERGENCE / UNTRACKED PATHNAME ENUMERATION / OPEN / VR-24
   COMBAT ATTEMPT`;
4. `UNAUTHORIZED DIVERGENCE / LITERAL CONTROL PATHNAME OUTPUT / OPEN / VR-25
   MISSION`;
5. `UNAUTHORIZED DIVERGENCE / LITERAL CONTROL PATHNAME OUTPUT / OPEN / VR-28
   MISSION STAGING`;
6. `UNAUTHORIZED DIVERGENCE / EXACT-PORT LISTENER OWNERSHIP RECOVERY / OPEN /
   VR-39`;
7. `UNAUTHORIZED DIVERGENCE / PARSER DIAGNOSTIC SCALAR OUTPUT / OPEN / VR-41`;
8. `UNAUTHORIZED DIVERGENCE / VALIDATION COMMAND SELECTION / OPEN / VR-46
   COMBAT`; and
9. `REQUIRED CORRECTION / EXECUTION CONTROL / PRE-ROOT SCALAR TRANSPORT / OPEN
   / VR-47`.

None is waived, merged, closed, cured, or admitted as candidate evidence by
this shell. The VR-49 rejection is separately recorded as non-executed/non-
evidence and does not alter the nine OPEN classifications.

Diagnostic evidence remains non-release, forbidden verifier input, and no-
retry. No build, test, validator, PBA, served-identity rerun, product/media/
protected action, downstream stage, or release action is authorized.

Mission Captain signs **`FIRST RUN SHELL READY / FOUR-STAGE FRESH CONTAINED
CORRECTED DIAGNOSTIC-CONTROL VERIFICATION / FRSH-003-v1-VR-50`**.
