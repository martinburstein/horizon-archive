# Horizon Archive First Run Functional Report - Host 07 Sediment Abacus

Report ID: `FRCE-007-v1`

Stage / owner: Combat Engineer / `combat_engineer`

Disposition: **`PRODUCTION FUNCTIONAL / HOST 07 INERT FAIL-CLOSED INTEGRATION / FIVE CLI ATTEMPTS UNCONSUMED`**

Shell / blueprint: `FRSH-007-v1` / `FRPX-007-v1`

Code candidate: `5164437a199ec101cb638ad0599715e77d563a72`

Tree: `02c2cbbdfacad67c8cee86eeba8172ab2b450e81`

Date: **2026-08-12**

## Implementation

Combat added `src/sedimentAbacus.js`, integrated its future selected-source
phase in `src/App.jsx`, and added `test/sedimentAbacus.test.js`. Candidate
source/provenance/measurements/copy/alt remain null and `enabled=false`; no
Host 07 image import exists and released behavior remains active.

The pure guard requires exact lawful Host 06, sanitized strict L02-03 mastery,
selected H7-1..H7-5 source/provenance/prompt/CLI identity, complete normalized
measurements, and exact 3840x2160 decode. It rejects malformed evidence and
invalid source, geometry, layout, provenance, or decode independently.

`enabled!==true` alone owns the released generic Structured Packets launcher.
After accepted-only enablement, later failure cannot restore the bypass. The
selected phase owns one image, one native Host 07 target, LOOK physical facts,
TALK silence, sole USE through unchanged L03-01 then L03-02, read-only complete
USE, one animation-frame transition, deterministic focus/notice, and safe
fallback. It adds no save family, private field, evidence minting, or Demo Tour
credit.

## Validation

- Focused Host 07 + Host 06 + L03-01 + L03-02: `28/28` PASS after the positive
  lawful-registry case was added.
- Related model-choice/privacy/Demo Tour/return/focus/Host 05-07 set: `64/64`
  PASS.
- Complete release validator: `987/987/0` PASS; RP-002 contract, E2E syntax,
  whitespace and patch integrity PASS in `31.1s`.
- Production build: PASS, `219` modules, `8.19s`; clean rebuild PASS, `219`
  modules, `6.87s`.
- TD-012/RP-012 fixture build: PASS, `57` modules, `0.74s`.
- PBA: JavaScript `1,687,754`, CSS `119,394`, modules `219`, source maps `0`;
  all `FRVE-007-v2` pre-generation caps PASS.
- Accepted media remains exact `17 / 37,410,731`; no media changed.
- Served root/deep/JavaScript/CSS returned `200`; served bytes equal build
  (`1,687,754` JS / `119,394` CSS). Exact owned PID `16048` stopped and port
  `4173` proved clear.
- `git diff --check` PASS; tracked worktree/index clean after candidate commit.

Candidate-bearing media decode/performance, live selected six-layout review,
selected served-media identity, and complete isolated Host06->Host07 E2E are
correctly deferred until Quartermaster selection. The inert branch is covered
without pretending that absent media passed content gates.

## Quartermaster placeholders

Quartermaster alone may populate the exact declared source/provenance/relation/
semantic/label/handoff/approach/depth-band/nodule/protected/layout/copy/alt
fields after a passing candidate. Accepted attempt ID is exactly H7-1..H7-5.
Prompt byte/hash and CLI hash checks are frozen in the source guard.

All five prompts remain immutable and all five ordinals remain unconsumed. The
scratch and product paths remain absent. Combat made zero CLI/API/credential/
candidate/media operation and revealed nothing.

## Hard stop, variance, and rollback

Rollback removes only the three exact code/test changes. Accepted Hosts 01-06,
media, lessons, rail, RP-012, and `successor=null` remain exact. No Host 08 or
later surface was added.

Recon's protected-QA filename enumeration remains OPEN PROCESS ONLY, without
content access or product evidence. Every earlier OPEN record, both enumeration
records, protected exclusions, and VR-65 remain separate and unchanged.

The first served-preview launch expression was rejected by command policy
before process start; the ordinary repository preview command then produced the
recorded PASS. This is a non-product process note, not a functional variance.

## Exact Quartermaster handoff

Use one fresh Quartermaster context. Read the full profile, imagegen skill,
shell, treatment, blueprint, this report, all five prompt files, immutable CLI,
source guard, accepted-media manifest, and exact custody/measurement rules.
Prove synchronized source and absence, create only the frozen scratch root,
then consume H7-1 first. Continue only after objective rejection plus exact
identity cleanup. First PASS stops; nonzero CLI exit stops; no sixth call.
Privately review candidates and do not reveal rejected media.

Combat signs **`PRODUCTION FUNCTIONAL / FRCE-007-v1 / ROUTE FRESH QUARTERMASTER`**.
