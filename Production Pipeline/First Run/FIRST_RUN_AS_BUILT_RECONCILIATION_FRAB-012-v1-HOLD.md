# FIRST RUN AS-BUILT RECONCILIATION / FRAB-012-v1-HOLD

## Disposition

`HOLD / REQUIRED CORRECTION / COMBAT E2E HARNESS`

Shell `FRSH-012-v1`; content candidate `6faa828c`; presentation candidate
`89c06514`. Selected H12-1 and its physical/runtime evidence remain accepted
candidate facts, not a release. H12-2..H12-9 remain unused; no further call is
authorized until this Work Order releases.

Independent gates passed: exact selected-media identity and scratch absence;
40/40 validators; focused Host12 `5/5`; full suite `1028/1028`; production/
fixture/production builds `231/57/231`; JS `1,741,218`, CSS `120,598`, maps0;
real Edge six-layout responsive/forced-color/reduced-motion PASS; accepted media
candidate `23 / 140,647,485`.

The sole clean-start E2E stopped after Client Boundaries mastery because the
historical harness waited for generic button `Start SDK Route Chooser`. Exact
selected-source behavior lawfully retires that generic launcher and presents
the native Host12 hotspot instead. This is a stale E2E navigation/selector
contract, not evidence that the product route failed. Nevertheless the shell
requires one complete journey, so release and maturity advance are forbidden.

Classification: `REQUIRED CORRECTION / COMBAT TEST HARNESS`. Combat must update
the sanctioned E2E at the Host12 boundary to activate the native available
hotspot and continue the unchanged SDK -> Single Agent sequence; preserve the
pre-selection generic path, client-boundary ordering, focus, state, no-credit,
and all other journey assertions. Add a focused harness assertion, rerun only
the affected focused gate, commit/push `PRODUCTION FUNCTIONAL`, then return to
fresh Intelligence for a new sole complete E2E. Product/media/copy/CSS changes
are not authorized.

The failed owned preview PID/listener were stopped, port 5174 is clear, and the
exact external QA root was identity-bounded, deleted, and proved absent. No QA
artifact was imported or used beyond the bounded failure localization. No image
or encounter name was revealed to Martin.

Exact next owner: one Combat Engineer.
