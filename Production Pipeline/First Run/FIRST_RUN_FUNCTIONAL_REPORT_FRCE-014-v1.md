# FIRST RUN FUNCTIONAL REPORT / FRCE-014-v1

## Disposition

`PRODUCTION FUNCTIONAL / INERT HOST 14 SLOT / QUARTERMASTER READY`

Shell `FRSH-014-v1`; treatment `FRCT-014-v1`; blueprint `FRPX-014-v1`;
released best `FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e`.

Combat implemented the smallest complete null-first functional candidate. The
new `waterlineLedger.js` owns a recursively frozen disabled registry, strict
non-coercing source/provenance/copy/decode/predecessor/geometry lawfulness,
six source-derived responsive layouts, physical mutation rejection, sanitized
lesson ordering, one atomic native-active selector, inverse generic-launcher
guards, and a source-derived minimum-44-CSS-pixel semantic hotspot. `App.jsx`
routes the native semantic action, focus, announcement, session/reopen state,
and malformed-state fallback through that one selector; exactly the Objective
Ledger and Remediation Planner generic launchers are inverse-guarded. Minimal
CSS adds containment, focus, textual/non-color state distinctions, forced
colors, and no motion dependency. The sanctioned E2E file contains the bounded
native-first ownership/count hook required for later source-enabled proof.

The registry source remains `enabled:false` with null identity, provenance,
geometry, copy, layouts, and runtime URL. Therefore the native selector cannot
activate, the released image stays mounted, and both released generic
launchers remain functional. No source, raster, provenance, product-media
import, private review, or presentation treatment exists in this candidate.

## Verification

- predecessor reproduction before edit: `24/24/0`;
- focused functional/responsive/projection/curriculum tests: `31/31/0`;
- related predecessor/terminal/route/served-identity tests: `63/63/0`;
- cold full suite: `1047/1047/0`;
- exact manifest-order curriculum/readiness validators: `40/40`, exit `0`;
- production and TD-012 fixture builds: `234 / 57` modules, both PASS;
- null-first production PBA observations: JS `1,762,475 <= 1,785,000`, CSS
  `121,983 <= 122,000`, maps `0`, emitted files `27`, no new raster;
- accepted-media authority remains exactly `24 / 154,163,567`;
- prompt identities `8/8`, generator/FRAM/FRFC/FRPBA identities `4/4`, and
  key-presence-only preflight PASS; scratch, source, provenance, partial, and
  call-ledger paths all absent;
- TD010 and TD011 fresh-build served-identity checks each PASS; ports
  `4173/4184/5174` were clear when checked.

The first combined related run exposed fixture-build `dist` contention in its
two served-identity tests (`61/63`). FRCV replanned once: rebuild production,
run both served checks independently, then run the exact related set with
deterministic single-test concurrency. It passed `63/63`; no test or threshold
was weakened. A later logged production build encountered one transient
OneDrive `EPERM` while clearing `dist/assets`; no owned preview process or port
existed, and the single bounded rebuild passed. Neither event formed a
same-family plateau.

The selected-source PBA, 28-file selected inventory, private physical review,
real browser layouts, runtime request proof, full source-enabled E2E journey,
and rollback/release holdout are source-dependent and are not claimed. The
external proof root contains only the successful null-first production build
log for Quartermaster continuation. Repository QA, screenshots, browser
profiles/saves, protected PDFs/training, hidden lore, archives, residual roots,
and media pixels were not read or modified.

## Quartermaster convergence handoff

Populate only after custody, private physical, decode, identity, and responsive
gates pass: source URL/import and exact source identity; provenance identity;
three separate material histories; irregular deposition band crossing all
three histories; at least three upward service seams; center target/relation;
label anchor; dry approach; waterline; protected regions; six derived layouts;
final name, alt, state/announcement copy, and exact registry fields. Then
change the selected build/count expectations from the null-first `27`-file
baseline to the shell-required selected inventory, exercise the bounded E2E
ownership hook across every owned lesson entry/re-entry, and run the complete
source-dependent PBA/served/browser/E2E/cleanup cascade.

Remaining unknowns are source bytes/transport, private physical credibility,
exact measurements, integrated browser/accessibility/performance evidence,
source-enabled E2E coverage, and independent release proof. Stop at the first
complete PASS; any source failure leaves this inert functional commit and the
released generic path as the exact rollback.

Generation calls used: `0`; media actions: `0`; browser/E2E actions: `0`;
authorized call pool remaining: `32` (`8` in the initial earned tranche).

Exact next owner: one Quartermaster.
