# Horizon Archive First Run Polish Viability Envelope

Envelope ID: `FRVE-005-v7`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`HOLD / EXACT PS5.1 NATIVE-IDENTITY FIXTURE DID NOT EXECUTE /
NO HELPER RESULT OR CLEANUP POSTCONDITION MAY BE INFERRED`**

Date: **2026-08-11**

Science source inspected:
`39b0ae964db61488044e4e61ea670b653ffed749`

Planning controls: `FRRM-005-v7` / `FRSB-005-v7`

Prior Science envelope: `FRVE-005-v6`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Science decision

Science read the active intake, workflow, registry, complete Science profile,
current handoff, complete `FRWO-005-v7`, complete `FRWO-005-v6`, complete
`FRVE-005-v6`, current release map and scoreboard, effective Host 06 controls,
and the installed image-generation skill plus its Image API and network
references. Starting `HEAD` was exactly
`39b0ae964db61488044e4e61ea670b653ffed749`.

The host shell independently reported Windows PowerShell
`5.1.26100.8875`, Desktop edition, CLR `4.0.30319.42000`, and a 64-bit
process. Science then attempted to start the exact bounded fixture body in an
explicit Windows PowerShell 5.1 child process. Process creation returned
Windows `Access is denied` before the fixture body produced any result.

Therefore no admissible evidence exists for exact source-byte extraction,
`Add-Type` compilation, emitted DLL length/SHA, byte-only assembly load,
reflection surface, x64 struct layout, native return schema, one-link and hard-
link behavior, invalid/closed/native-false failure behavior, stable identity
across reopen and no-replace move, substitution/reparse rejection, identity-
conditioned deletion, or exact post-fixture cleanup. The command emitted no
fixture evidence object. Science does not infer execution, success, or cleanup
from intent or from the absence of output.

`FRWO-005-v7` requires every one of those predicates before `POLISH VIABILITY
READY`. This invocation consequently issues **`HOLD`**. This is an evidence-
availability HOLD, not a finding that the frozen helper source is defective.
No Operations, Mission, product, canon, or maturity correction has been
established.

Variance classification: **`REQUIRED CORRECTION / FRESH SCIENCE MUST RUN THE
EXACT FIXTURE DIRECTLY IN THE ALREADY-CONFIRMED PS5.1 HOST AND PROVE CLEANUP`**.

## Preserved v6 evidence and unavailable fresh proof

`FRVE-005-v6` remains the prior accepted evidence for the exact prospective
endpoint/model/seven-member request, one-item `data[0].b64_json`, boolean-only
environment credential boundary, TLS reachability, single-send/no-redirect
HttpClient posture, strict bounded UTF-8/JSON/base64 parser, `CreateNew`,
`Flush(true)`, same-volume two-argument `File.Move` no-replace behavior, and
exact-path cleanup. This interrupted v7 stage neither contradicts nor promotes
that evidence.

Fresh v7 corroboration did not run. In particular, this stage did not re-read
the credential even as a boolean, open a TCP/TLS connection, serialize a
request, exercise parser vectors, allocate the live root, or run disposable
file materialization fixtures. Direct Image API calls remain exact `0` and no
request was constructed or sent.

The installed authoritative references still describe generation at `POST
/v1/images/generations`, `gpt-image-2`, flexible `3840x2160`, high-quality
opaque PNG output, and one `data[]` item carrying `b64_json`. Reference support
is contract evidence only and cannot replace the missing host fixture.

## State, media, ordinal, and product invariants

- Ordinal `1` remains permanently consumed, opaque, inaccessible, and never
  retried or cleaned.
- Ordinals `2` and `3` remain unconsumed and unavailable.
- Direct API calls remain exact `0`.
- Conservative managed-residual count remains exact `1`, associated only with
  historical ordinal `1`; VR-65 remains separate and inaccessible.
- `FRAM-001-v1` remains immutable at exact `17 / 37,410,731`, file SHA-256
  `a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
  canonical digest
  `c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`.
- The frozen inert code candidate
  `02d957e9d69dc7986928a391c37f899784f73ea5` and tree
  `09da6293d72c8123b8d9673bd8e41329338e8d13` remain unchanged.
- `FR-03` remains exactly `1 accepted shared compression / 1 exact / 10
  missing`; no maturity or inventory cell advances.

The Host 05 -> dry same-basin Host 06 -> sole unchanged `L02-03` -> unchanged
next Drowned boundary; exact prompt; strict PNG/CRC/decode and `PHY-01..12`
predicates; six-layout crop/mapping/accessibility gates; privacy, save,
offline, PBA, performance, no-reveal, rollback, one-path canon, RP-012,
`successor=null`, and all OPEN records remain unchanged and unreachable under
this HOLD.

No generation, Image API request, media read/hash/decode, pixel inspection,
live-root allocation, product import, provenance, runtime/test change, E2E,
reveal, maturity update, OPEN-record closure, residual access, schedule,
automation, push, release, or `FIRST RUN COMPLETE` occurred.

## Exact fresh Science handoff

One fresh Office of Science Administrator starts from this HOLD and the current
synchronized authorities. It must invoke the fixture **directly in the already
confirmed Windows PowerShell 5.1 host**, without spawning a nested shell, and
must complete every `FRWO-005-v7` predicate. It must freeze the emitted DLL
byte length/lowercase SHA, load only those bytes, prove the exact native and
managed surface and x64 layout, exercise every required exclusive-handle,
link-count, failure, reparse, substitution, move-race, and cleanup branch,
restore the exact helper and disposable fixture roots to proved absence, and
then freshly corroborate the unchanged v6 gates with zero API sends.

Only a completed evidence set may issue `POLISH VIABILITY READY`. A repeated
execution or cleanup failure remains `HOLD`. No Mission follows this artifact.

Office of Science Administrator signs **`HOLD / FRVE-005-v7`** from exact
source `39b0ae964db61488044e4e61ea670b653ffed749`.
