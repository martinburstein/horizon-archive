# Horizon Archive First Run Work Order - Host 09 Compact-Island Retry

Work Order ID: `FRWO-009-v3`

Stage / stable agent: Operations Planning Major / `operations_planning_major`

Disposition: **`WORK ORDER READY / FRESH FIVE-ATTEMPT COMPACT-ISLAND PASS`**

Predecessor closeout: `FRWO-009-v2` / `FRCA-009-v1`

Address: `FR-03 / Host 09`, immediately after released Host 08.

Date: **2026-08-12**

## Authorization and rationale

Martin explicitly authorized one fresh five-attempt run. Earliest-first order
keeps the pass on Host 09; Host 10 cannot begin while Host 09 remains the first
missing encounter. The new ledger is exactly `{H9-6,H9-7,H9-8,H9-9,H9-10}`.
It does not reopen, reuse, edit, crop, reference, vary, or import any rejected
`H9-1..H9-5` source.

The prior run proved the concept and technical transport viable. All five
candidates were physically coherent but framed the complete relation too large
or too low for the frozen responsive envelope. This retry changes only the
generation composition strategy: distant establishing view, deliberately
small central encounter island, broad quiet margin on all four sides, and no
foreground-dominant relation. Canon, physical predicates, lesson ownership,
state, runtime, and acceptance thresholds remain unchanged.

## Exact attempt boundary

Science and Mission must freeze five independent strict-ASCII prompts and one
new absent GUID OS-temp root with literal `h9-6.png` through `h9-10.png`
children. Each call remains the approved installed CLI with `gpt-image-2`,
`3840x2160`, high quality, opaque PNG, no augmentation, one output, sequential
execution, and no reference/input/edit/mask/batch/internal retry.

An ordinal is consumed when its CLI process launches. Nonzero exit stops.
Objective rejection enables the next ordinal only after exact identity-bound
deletion and absence proof. First PASS stops all later calls. There is no
sixth fresh call and no eleventh cumulative Host 09 call.

## Compact-island correction

The authored complete family should target source `x=.34..66`, `y=.34..66`,
width `.27..32`, height `.24..30`, center `x=.49..51`, `y=.48..52`. Every
required cavity center, reflection pair, no-return case, incoming continuity
center, and dry observation-rim center must sit inside `x=.31..69`,
`y=.30..72`. The image must contain visible unused terrain margin left, right,
above, and below the complete relation. A close view, foreground-dominant
formation, edge-reaching rim, or essential relation outside this island is an
objective `LAYOUT` rejection.

The released acceptance maximum remains family within `x=.24..76`,
`y=.20..78`, with `>=95%` retention and all essential centers across the six
layouts. The tighter generation target is a safety margin, not a canon or
runtime geometry change.

## Preserved product and validation boundary

At most one passing byte-identical raster plus provenance may enter the exact
existing Host 09 product path. The current product and provenance are absent;
accepted media remains `19 / 70,136,520`; the `FRCE-009-v1` registry remains
inert and null-first. Accepted-only integration must populate exact geometry,
copy, factual alt, native state, dual unchanged lesson flow, focus/recovery,
responsive evidence, and E2E traversal.

All `FRWO-009-v1`, `FRVE-009-v1`, `FRSH-009-v1`, continuity, technical PNG,
Cloud-safe custody, accessibility, PBA, test/build/served/E2E, cleanup,
rollback, no-reveal, protected-state, OPEN-record, VR-65, RP-012, and
`successor=null` controls continue unless freshly superseded by Science or
Mission. Hosts 01-08/media remain immutable; Host 10 remains absent.

Operations signs **`WORK ORDER READY / FRWO-009-v3 / ROUTE FRESH SCIENCE`**.
