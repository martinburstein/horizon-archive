# FIRST RUN SHELL / FRSH-011-v1

## Disposition

`FIRST RUN SHELL READY / TEN PRIVATE HOST 11 ATTEMPTS / FIRST PASS STOPS`

Source release is `FRAB-010-v1` at
`41931b8678e88e90fa4e700a68e7f1e5c9729900`. Planning authorities are
`FRPB-001-v9`, `FRCL-011-v1`, `FRWO-011-v1`, and `FRVE-011-v1`. This shell
owns only `FR-03 / Host 11` and existing `L05-01 + L05-02`.

## Fixed player-visible outcome

The Host 10 reflected path reaches one distinct mineral receiving wall. Moving
borrowed daylight crosses mineral film, enters at least three embedded
apertures, and returns along at least two changed physical angles. A broad,
continuous dry apron reaches one off-axis service throat. The wall does not
emit its main light and never becomes writing, image, UI, screen, theater,
answer, address, or world response.

LOOK is physical and write-free. TALK is exact nonresponse. USE enters only
the first incomplete owned lesson. Completion is read-only and changes no
light, wall, aperture, route, water, access, meaning, or later state.

## Generation, custody, and objective PASS

Only sequential `{H11-1..H11-10}` exist. First complete PASS ends the ledger.
Each call uses the bundled CLI `35,266` /
`c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05`
with exact prompt identity from `FRVE-011-v1`, `generate`, `gpt-image-2`,
`3840x2160`, high, opaque PNG, exact output, and `--no-augment`. No force,
batch, reference, edit, variation, internal retry, alternate model, or alternate
transport.

Scratch root and children, product raster, and provenance are exactly the
literals in `FRWO-011-v1`. Quartermaster privately reviews only an identity-
proved ordinary nonreparse output. A rejected candidate receives bounded
bytes/SHA, physical/technical flags, measurements, derived layout verdict, and
reasons; then is identity-deleted and proved absent before any later ordinal.
No rejected pixel, derivative, reference, reveal, or workspace copy exists.

Technical PASS is opaque RGB PNG, exact `3840x2160`, strict decode, 1..30 MB.
Physical PASS is the entire receiving-wall/light/film/apertures/changed-return/
dry-apron/off-axis-throat sentence with every `FRCL-011-v1` rejection absent.
Responsive PASS derives actual cover projection at all six layouts with >=.95
relation retention, all essential centers and throat visible, contained native
target >=44 CSS px, and zero protected overlap, then corroborates it in real
Edge. No arbitrary source-coordinate band may reject a complete derived PASS.

At most one passing byte-identical raster plus `PROVENANCE.md` enters the
workspace. Import is selection, not release.

## Role ownership and runtime contract

- Recon locks one physical reading order without adding spectacle or meaning.
- Tactical locks state, native actions, focus, layout, recovery, copy, and QA.
- Combat builds a null-first fail-closed registry and tests. Before selection,
  released generic behavior remains available. Exact source enablement retires
  the two generic launchers permanently even on later decode/layout failure.
- Quartermaster alone calls, reviews, selects, imports, measures, records
  provenance, and populates accepted-source fields.
- Image Specialist changes runtime code/configuration only around the unchanged
  selected raster; never image bytes or reveal.
- Intelligence independently validates and alone releases.

Runtime acceptance requires `attemptId` matching exact `H11-1`..`H11-10`, no
ordinal surrogate, source bytes `1..30000000`, lowercase SHA-256, exact
dimensions/opacity, final copy/alt/provenance, measured physical/semantic
rectangles, and all six layout records. One native button converges pointer,
keyboard, touch, and switch-like entry. Focus returns after LOOK, TALK, lesson
exit, remediation, mastery, reload, and route return. No prompt, candidate,
answer, media identity, or review state is persisted.

## Validation, budgets, and rollback

Budgets and validation are exactly `FRWO-011-v1` / `FRVE-011-v1`. Combat must
pass focused/related/full tests, all validators, production/fixture builds,
PBA/media inventory, served identity, and push exact code candidate. After
selection, Quartermaster proves private acceptance/cleanup and commits content.
Image proves six-layout runtime presentation. Intelligence runs isolated
responsive browser, clean-start E2E through both owned lessons and unchanged
MH-40, final budgets, cleanup, candidate identity, and sync.

Before import rollback is exact scratch cleanup. Before release after import,
rollback removes only the new raster/provenance and returns the registry to
null. Cleanup, identity, technical, physical, responsive, accessibility,
budget, canon, lesson, or protected-boundary failure stops and returns to the
earliest owner.

Accepted Hosts 01-10 and all accepted media are immutable. Never inspect,
restore, stage, or use repository QA screenshots, Martin's real browser/profile/
save, protected PDF/training, hidden lore, or opaque residual roots. No branch,
packet, lesson, reward, access, identity, authority, dialogue, world response,
RP-013, post-ending content, or successor.

## Mission signature

`FIRST RUN SHELL READY / FRSH-011-v1`

Exact next owner: one Reconnaissance Sergeant.
