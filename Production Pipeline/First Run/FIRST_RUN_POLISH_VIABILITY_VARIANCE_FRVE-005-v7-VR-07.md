# Horizon Archive First Run Polish Viability Variance

Variance ID: `FRVE-005-v7-VR-07`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`HOLD / COMPACT CURRENT-SHELL PARENT ABORTED ON
CASE-INSENSITIVE AUTOMATIC PID VARIABLE COLLISION BEFORE CREDENTIAL CLEAR,
COMMAND-LINE MEASUREMENT, OR CHILD ENTRY / NO RETRY / FRVE-005-v7-VR-07`**

Date: **2026-08-11**

Science source inspected:
`8844bc13c9cf9fb1deedede8566fca2621ababb5`

Mission correction: `FRSH-005-v1-VR-09`

Prior Science return / decisive viability: `FRVE-005-v7-VR-06` /
`FRVE-005-v7-VR-03`

Quartermaster return: `FRCA-005-v3`

Effective shell: `FRSH-005-v1` through `FRSH-005-v1-VR-09`

Effective treatment / blueprint: `FRDT-005-v1-VR-01` /
`FRPX-005-v1-VR-01`

Functional report: `FRCE-005-v1-VR-05`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Science decision

Science read the complete active intake, workflow, registry, full Science
profile, `FRVE-005-v7-VR-06`, `FRVE-005-v7-VR-05`,
`FRSH-005-v1-VR-09`, `FRVE-005-v7-VR-04`, `FRCA-005-v3`, complete
`FRWO-005-v7`, decisive `FRVE-005-v7-VR-03`, the complete effective shell,
treatment, blueprint, `FRCE-005-v1-VR-05`, immutable `FRAM-001-v1`, and the
exact pre-helper execution boundary. Starting `HEAD` was exact
`8844bc13c9cf9fb1deedede8566fca2621ababb5`. The three disclosed untracked
protected items were left untouched.

Science selected the authorized compact transport: the already-running
current PowerShell host as the parent, with exact direct invocation of the
frozen child executable and argument array. It introduced no encoded parent,
child source file, stdin, diagnostic file, alternate environment transport,
nested parent shell, or file redirection.

One orchestration-template parse was rejected before `shell_command` was
called because an outer JavaScript template interpreted a PowerShell escape.
No command string, parent process, fixture, environment operation, path
operation, or child existed in that rejected preparation. Science corrected
only that outer quoting defect before beginning the one authorized parent.

The one compact current-shell parent then began. It read the two frozen code
fences in memory and computed the launcher identity into a local object. On
the next assignment it used the concise local name `$pid` for the pre-helper
identity object. PowerShell variable names are case-insensitive, so `$pid`
collided with the read-only automatic `$PID` variable and the parent stopped
with exact local failure:

```text
Cannot overwrite variable PID because it is read-only or constant.
FullyQualifiedErrorId: VariableNotWritable
```

The stop occurred before the identity assertions, before the conservative
child command-line measurement, before controlled-path definitions and
preconditions, before removal of the prospective credential from the parent
environment, before the task-specific environment value was set, before any
`$ErrorActionPreference='Continue'` capture scope, and before any native
child invocation. The exact launcher and pre-helper were not executed.

The explicit no-retry boundary prohibited repairing the parent variable name
or launching a second parent in this heartbeat. Science therefore issues
**`HOLD`** and does not route to Mission or Quartermaster.

Variance classification: **`REQUIRED CORRECTION / SCIENCE COMPACT PARENT
VARIABLE NAMESPACE / CASE-INSENSITIVE AUTOMATIC PID COLLISION / OPEN`**.

## Evidence available and unavailable

Available evidence:

- the exact source and complete controlling intake were verified before the
  attempted parent;
- the selected parent transport was the current shell and contained no
  encoded parent, source file, stdin, diagnostic file, or alternate
  environment transport;
- the parent failure is exact, local, and precedes every child, credential,
  controlled-path, request, API, and ordinal operation; and
- an independent post-failure exact-path check proves every controlled helper,
  live, ordinal, raster, and provenance target absent.

Unavailable because the parent stopped before fixture entry:

- accepted in-fixture recomputation of launcher `2001 / 96feaf7e...` and
  pre-helper `976 / 5cd257c9...` identities;
- a measured conservative direct-child command line below the Windows host
  limit;
- exact 64-bit Windows PowerShell Desktop `5.1.26100.8875` child success;
- silent ordered `PH01..PH08` success and exact root creation/cleanup;
- every locally inducible first-failure record and exhaustive class/FQID
  allowlist bounding;
- literal `UNLISTED` replacement;
- exact `640`-byte acceptance and missing, extra, reordered, unknown-
  predicate, multiline, non-ASCII, over-`640`-byte, zero-record, and multiple-
  record collapse; and
- launcher-owned, identity-conditioned nonrecursive cleanup after a
  post-root failure.

Prepared parent source and planned assertions are not fixture evidence.

## Final root, ordinal, product, and protected-state proof

Independent exact-path checks after the stopped parent proved:

```text
helperRootAbsent=true
helperDllAbsent=true
liveRootAbsent=true
ordinal2StageAbsent=true
ordinal2TargetAbsent=true
ordinal3StageAbsent=true
ordinal3TargetAbsent=true
productRasterAbsent=true
productProvenanceAbsent=true
```

The parent stopped before credential clearing but never read a credential.
Credential reads, request constructions, `SendAsync` calls, direct Image API
sends, and ordinals consumed are therefore each exact `0`. Child invocations
and fixture retries are exact `0`. Historical ordinal `1` remains permanently
consumed, opaque, inaccessible, and unchanged. Ordinals `2` and `3` remain
unstarted and unconsumed.

No helper was compiled or loaded. No helper root, DLL, live root, request,
response, raster, provenance, media byte, or pixel was created or opened.
Repository QA quarantine, the protected PDF, training tree, Martin's browser/
profile/save, hidden lore, accepted-media pixels, OS-temp parent, ordinal-1
residual, managed directory, user work, VR-65, and every opaque residual
remained untouched. All inherited OPEN records remain separate and OPEN.

## Required fresh Science correction

One fresh Office of Science Administrator begins from the committed handoff
and runs exactly one new credential-cleared, no-request fixture through the
same compact current-shell parent transport. Before the parent fixture begins,
its task-specific local variable names must be checked case-insensitively
against PowerShell automatic, preference, constant, read-only, and common
system variables. The pre-helper identity object must use the unambiguous
task-specific name `$prehelperIdentity`; `$pid` and every case variant of
`$PID` are forbidden.

The parent must still measure a conservative rendering of the exact child
executable plus argument array and prove it safely below both `8191` and
`32767` characters before the first child. It must scope
`$ErrorActionPreference='Continue'` around only each exact native child
invocation, capture the one stderr record and `$LASTEXITCODE` in memory, and
immediately restore the prior preference. It may not suppress, rewrite,
discard, or file-redirect stderr.

The frozen launcher, pre-helper, environment name, executable, child argument
order, controlled paths, predicates, schema, allowlists, and cleanup rules do
not change. The one fixture must independently repeat exact identities and
full success, then complete every locally inducible first-failure, allowlist,
`UNLISTED`, malformed/oversized-collapse, ownership-conditioned cleanup, and
final-absence assertion. Only a PASS may issue a new `POLISH VIABILITY READY`
and return to one fresh Mission Captain. Failure remains a precise Science
`HOLD` with no retry.

## Maturity, changes, and hard stops

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`.

Science changed no product, test, runtime, lesson, save, media, manifest,
candidate, map, scoreboard, maturity, process classification, residual,
schedule, or automation state. The only repository changes authorized for
this stage are this variance and synchronized `NEXT_INSTANCE_HANDOFF.md`.

Science did not run a fixture child, call the API, inspect media/pixels,
compile/load the helper, allocate the live root, consume an ordinal, change
product/tests, run a build/browser/E2E, reveal, advance maturity, close an
OPEN record, access a residual or VR-65, schedule, automate, push, release, or
call `FIRST RUN COMPLETE`.

Office of Science Administrator signs **`HOLD /
FRVE-005-v7-VR-07`** from exact source
`8844bc13c9cf9fb1deedede8566fca2621ababb5`.
