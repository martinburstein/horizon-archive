# FIRST RUN FUNCTIONAL REPORT / FRCE-009-v5

## Disposition

`PRODUCTION FUNCTIONAL / FIVE FRESH IDENTITIES / CORRECTED RESPONSIVE GATE`

## Candidate

- Extended the inert Host 09 prompt identity allowlist through exactly
  `H9-16..H9-20`; `H9-21` remains rejected.
- Preserved the null-first source, provenance, measurement, copy, alt, route,
  lesson, and save graph. No image or product path was populated.
- Preserved the released derived projection and browser-computed responsive
  gate; no source-center preference was reintroduced.

## Validation

- Focused and adjacent suite: `14/14 PASS`.
- Isolated Edge fixture: `6/6 layouts PASS`, forced colors `1`, reduced motion
  `1`.
- Production build: PASS, JavaScript `1,711,659` bytes, CSS `119,757` bytes,
  source maps `0`.
- Accepted media remains `19 / 70,136,520` bytes.

## Boundaries

No API, credential, image, pixel, product, provenance, browser-profile, save,
or protected-path action occurred. Pre-existing modified QA screenshots were
left unstaged, uninspected, unrestored, and unused.

## Handoff

One fresh Quartermaster may execute the five sequential private attempts under
FRSH-009-v5. The first PASS stops the ledger. Every rejection requires the
bounded evidence record and exact identity-conditioned deletion before the
next attempt.
