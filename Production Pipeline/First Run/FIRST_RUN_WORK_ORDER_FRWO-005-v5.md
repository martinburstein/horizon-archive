# Horizon Archive First Run Work Order

Work Order ID: `FRWO-005-v5`

Title: **Stranded Lens Cradle - Explicit CLI/API Source and Fixed Lesson Integration**

Stage / stable agent: Operations Planning Major / `operations_planning_major`

Disposition: **`WORK ORDER READY / MARTIN-AUTHORIZED IMAGEGEN CLI/API
FALLBACK / PREDECLARED EXACT OS-TEMP OUTPUT / FRESH SCIENCE REQUIRED`**

Date: **2026-08-11**

Operations source inspected:
`796262d392e49e66cd635b4b016624dd5138f606`

Martin's latest controlling instruction: **`Authorize imagegen CLI/API fallback
for Host 06. The API key is set.`**

Reopened baseline / continuity: `FRPB-001-v2` / `FRCL-004-v2`

Superseded Work Order / decisive return: `FRWO-005-v4` / `FRVE-005-v4`

Effective prior shell / returns: `FRSH-005-v1` through
`FRSH-005-v1-VR-07` / `FRCA-005-v2`

Released First Run predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Frozen candidate tree:
`09da6293d72c8123b8d9673bd8e41329338e8d13`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Planning controls: `FRRM-005-v5` / `FRSB-005-v5`

First-run address: `FR-03 / Chapter II - Drowned Archive / Host 06`

Learning owner: existing sole unchanged `L02-03`
model/deployment/configuration work

Exact next recipient: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Operations adjudication

Martin explicitly selects option `2` from the `FRVE-005-v4` HOLD: CLI/API
mode with a predeclared output target, followed by a new Operations -> Science
-> Mission sequence. This is the missing external authority. Operations accepts
it without widening the product, canon, ordinal, asset, or release boundary.

`FRWO-005-v5` supersedes `FRWO-005-v4`. It replaces only the rejected built-in
managed-directory creator-binding route with one explicit invocation of the
installed immutable `imagegen` CLI for each still-lawful ordinal. The CLI owns
the API response decode and writes directly to the one exact `--out` path.
There is no `$CODEX_HOME/generated_images` discovery, `output_hint`, result
object transport, data-URL transport, SDK runner, one-off script, browser
download, extension, manual file, or alternate generator.

The v4 Science counterexample remains historically correct for the built-in
managed-directory route and is not cured or reinterpreted. That route is
withdrawn. The real managed directory and the possible ordinal-1 managed
residual remain opaque and untouched.

The installed CLI help is compatible with the authorized shape: `generate`
accepts explicit `--model`, `--prompt`, `--n`, `--size`, `--quality`,
`--background`, `--output-format`, and `--out`; the installed reference states
that an existing target fails unless `--force` is supplied. The current script
implementation performs an existence check and then a direct byte write rather
than exposing a separate atomic create-new flag. Science must fixture-prove the
exact absent-target/no-`--force` and race boundary below. If it cannot establish
fail-closed create-new behavior in the fresh owned GUID root, it must issue
`HOLD`; no role may edit the script or substitute a runner.

This planning stage establishes no candidate and authorizes no live call.
Fresh Science must validate the exact installed CLI, output-target semantics,
PNG gates, cleanup, and retained product envelope. Fresh Mission must then
issue a new versioned shell before ordinal `2` may run.

## Preserved bounded outcome, route, and maturity

The one player-visible outcome remains exactly:

```text
exact Host 05 / Sixfold Weir mastery
-> lens-like fragment handoff
-> one distinct local dry Host 06 / Stranded Lens Cradle
-> sole unchanged L02-03 entry and loop
-> unchanged next Drowned learning boundary
```

The source must show exactly one visibly weathered lens resting inside one
tilted conformal cradle on a dry, locally reachable same-basin continuation
above the current waterline. It must show at least two distinct load or stress
contacts, at least two continuous drainage seams, the material fragment
handoff from Host 05, and a restrained horizon or reflected-horizon catch.
The relation remains distinct from live water, Host 05, a return-like ridge,
the Crown, the distant suspended Tidal Lens, and any second lens/cradle
candidate.

Entry begins only after exact sanitized Host 05 / `L02-02` mastery and the
changed boundary hard-stops immediately after exact `L02-03` mastery. Exact
Host 05 -> Host 06 -> unchanged `L02-03`, the next Drowned boundary, the one
on-foot rail, shared RP-012 ending, READY/NOT YET READY dignity, all null
deltas, and `successor=null` remain unchanged. There is no Host 07, RP-013,
branch, reward, access, identity, authority, world response, successor, or
post-ending content.

Maturity remains unchanged. `FR-03` remains continuity `FR2`; physical-host
expression `FR0 - 1 accepted shared compression / 1 exact / 10 missing`;
learning `FR2`; behavior/save/recovery `FR1`; content `FR2`; presentation
`FR3`; and prior bounded release proof `FR4`. Only Intelligence may later
record `1 accepted shared compression / 2 exact / 9 missing` from accepted
as-built evidence.

## Frozen installed CLI identity and mode

Only this installed immutable script may be used:

```text
C:\Users\marti\.codex\skills\.system\imagegen\scripts\image_gen.py
```

Operations observed these non-secret identity scalars:

```text
Host 06 CLI contract ID: HOST06-IMAGEGEN-CLI-v1
SHA-256: c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05
byte length: 35,266
last-write UTC: 2026-08-09T15:46:11.7575005Z
model: gpt-image-2
```

The script exposes no user-facing intrinsic `--version` in the inspected
`generate --help`; for this Work Order, `HOST06-IMAGEGEN-CLI-v1` means exactly
the literal installed path plus the SHA-256 and byte length above. Science and
Mission must independently pin this contract ID, SHA, help surface, model, and
relevant output implementation before authority becomes active. Any drift,
missing script, changed help, unsupported parameter, or unresolved script
identity is `HOLD`; no install, upgrade, downgrade, copy, patch, or edit is
authorized.

`OPENAI_API_KEY` was present in the Operations process environment. Only a
boolean presence check occurred. No value, prefix, length, digest, fragment,
or derived credential was printed or retained. Science and the future
invoking role may repeat only the boolean presence gate. A missing key is a
pre-call HOLD that consumes no ordinal. The key never enters a command line,
report, provenance, log, source, Git, browser, save, or runtime.

## Predeclared exact OS-temp evaluation targets

The one fresh GUID-in-D-format evaluation root is predeclared, outside the
repository and absent at this Operations stage:

```text
C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-cli-1ecc6811-5521-4244-8dcd-a8adbb0f0393
```

The only permitted attempt targets under that root are:

```text
ordinal 2: C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-cli-1ecc6811-5521-4244-8dcd-a8adbb0f0393\attempt-02.png
ordinal 3: C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-cli-1ecc6811-5521-4244-8dcd-a8adbb0f0393\attempt-03.png
```

Operations proved by exact-path existence checks only that the root and both
targets were absent. It did not create, list, or mutate the OS-temp parent.
Science must freeze scalar parent resolution, canonical containment, no-follow
ordinary-directory/file identity, protected-root separation, permissions,
target absence, race controls, and exact cleanup for this literal root.

The invocation must use the active ordinal's literal target through `--out`.
`--out-dir` and default output naming are forbidden. `--force` is forbidden.
No other file may exist in the evaluation root. The inactive ordinal target
must remain absent. A pre-existing root/target, wrong identity, nonordinary
entry, reparse/link ambiguity, path drift, inability to prove containment, or
unexpected child is a pre-call HOLD that consumes no ordinal.

## Exact future CLI invocation contract

The exact prompt remains `HOST06-GEN-PROMPT-v1` from
`FRSH-005-v1-VR-06`, byte-for-byte and semantically unchanged. There is no
reference, edit, variation, image input, accepted-media input, prompt
augmentation, local repair, post-process, or derivative.

For ordinal `2`, and only conditionally ordinal `3`, the future role invokes
the installed script directly with the platform Python interpreter and these
effective arguments:

```text
generate
--model gpt-image-2
--prompt <exact HOST06-GEN-PROMPT-v1 text>
--n 1
--size 3840x2160
--quality high
--background opaque
--output-format png
--out <exact active ordinal path above>
--no-augment
```

The following are omitted or forbidden: `--prompt-file`, `--out-dir`,
`--force`, `--dry-run`, `--augment`, `--output-compression`, `--moderation`,
`--downscale-max-dim`, `--downscale-suffix`, `generate-batch`, `edit`, every
image/mask/input-fidelity argument, and every unlisted option. Mission must
freeze the exact safe argument-vector primitive so the prompt is one argument
without shell interpolation or command-string execution.

Exactly one API request and one output are allowed per active ordinal. Calls
are sequential. There is no CLI automatic retry, wrapper retry, SDK retry
loop, parallel request, alternate path, or output discovery. The CLI's own
single API response decode and exact `--out` write are the sole candidate
transport. Standard output/error may be observed only for exit status,
declared-path confirmation, and bounded non-secret diagnostics; it may not be
used to discover another output, credential, payload, or result field.

## Frozen ordinal, failure, and cleanup ledger

- Total ordinal domain remains exactly `{1,2,3}`.
- Ordinal `1` remains permanently consumed and is never reused, retried,
  recovered, discovered, inspected, cleaned, or associated with CLI mode.
- Ordinal `2` remains unconsumed and unavailable until new Science and Mission
  both accept this v5 contract.
- Ordinal `3` remains unconsumed and may run only after ordinal `2` becomes a
  fully identified objective source rejection and exact attempt-02 cleanup is
  proved.
- Maximum future calls is exactly `2`; at most one output per call; no fourth
  call, replacement ordinal, or parallel call exists.
- Built-in calls in `FRCA-005-v2` remain exact `0`; Science v4 calls remain
  exact `0`; Operations v5 CLI/API calls remain exact `0`.
- Conservative possible ordinal-associated managed residual count remains
  exact `1`, associated only with historical built-in ordinal `1`. CLI
  ordinals `2` and `3` do not use or increase the managed residual count.
- The ordinal-1 residual remains **`DEFERRED LIMITATION / RELEASE-PROCESS
  ONLY / NON-GATING / OPAQUE BUILT-IN MANAGED RESIDUAL`** and untouched.
- VR-65 remains separate, opaque, non-gating, and inaccessible.

A nonzero CLI exit, API error, missing output, multiple outputs, output at any
other path, target race/overwrite ambiguity, partial write, identity drift,
unexpected sibling, technical-gate failure, or cleanup uncertainty consumes
only the active ordinal and stops according to Science's frozen failure class.
No output discovery, default-path search, SDK inspection, result-field parsing,
broad cleanup, later ordinal, or fallback follows an opaque failure.

An exact output that is fully identity-proved at the active path and then
fails PNG, decode, `PHY-01..12`, composition, mapping, accessibility, or
meaning is an objective source rejection. Delete only that exact candidate,
prove it absent, and retain no rejected bytes, digest, pixels, path, preview,
or reuse artifact. Only successful exact cleanup may authorize ordinal `3`.
If ordinal `3` is rejected, exact cleanup is mandatory and the result is
`HOLD / ALL LAWFUL ORDINALS CONSUMED / NO MEDIA CANDIDATE`.

After any rejection or selected import, delete only the exact active attempt
path and then the exact empty predeclared GUID root, and prove both absent.
No parent, sibling, glob, recursive, pattern, retry, unknown-file, ordinal-1,
managed-directory, VR-65, or alternate cleanup is permitted.

## Technical, physical, and selected-import gates

Only the exact CLI-created active path may enter the inherited candidate
ladder. Before pixel review it must pass:

- exact path/handle identity, ordinary one-link non-reparse file, stable length
  and lowercase SHA-256, no sibling/output ambiguity, and byte length
  `1..12,000,000`;
- exact PNG signature, checked chunk order and lengths, CRC for every chunk,
  exactly one first `IHDR` with `3840 x 2160`, 8-bit color type `2`, required
  `sRGB`, forbidden alpha/transparency/metadata/animation/trailing bytes, exact
  zlib/scanline structure, and isolated browser decode at natural
  `3840 x 2160`; and
- no conversion, repair, normalization, alternate decode, screenshot, preview,
  export, crop, upscale, post-process, or derivative.

Only then may the original-resolution `PHY-01..12`, frozen source bands,
centered six-layout presentation, retained relation/approach, physical /
activation / label / center / anchor / nonoverlap / target mapping, native
`44 x 44 CSS px` target, focus stability, effective `200%`, forced color,
reduced motion, and no-sensory-only-meaning gates run.

At most the first fully passing source may be selected. It may be copied once,
byte-identically and create-new, to the existing sole product path:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
```

Require equal byte length and lowercase SHA-256 between the exact temp source
and product. Only then may the existing sole provenance path be created:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md
```

Provenance must record `FRWO-005-v5`, the later effective shell, exact
`HOST06-IMAGEGEN-CLI-v1` path/SHA/byte length, model `gpt-image-2`, exact
argument contract and no-input/no-edit/no-variation declaration, consumed
ordinals and dispositions, active output basename, selected length/SHA,
technical/CRC/decode and `PHY`/layout/accessibility results, immutable manifest
digest, exact product path, accepted-media unchanged declaration, and exact
temp cleanup. It contains no API key, API response, base64, result object,
rejected bytes/hash/pixels, hidden diagnostics, or prompt secret.

The product/test/media/copy delta, seven copy slots, one factual alt slot,
fail-closed Host 06 registry, generic-launcher removal rule, exact `L02-03`
evaluator/progression/remediation/transfer/explanation/ownership/confidence/
privacy/save behavior, and permitted production files remain inherited from
the effective shell. No accepted-media path or pixel is generation input or a
mutation target.

## Performance, offline, accessibility, and validation radius

All inherited caps remain exact:

| Measure | Affected cap | Global/result cap |
| --- | ---: | ---: |
| emitted JavaScript | `<=1,679,393` | `<=1,703,258` |
| emitted CSS | `<=119,547` | `<=119,672` |
| production modules | `<=218` | `<=222` |
| selected raster | `1..12,000,000` bytes | exact `3840 x 2160` opaque PNG |
| resulting media | exact `18` and `37,410,731 + selectedBytes` | `<=49,410,731` bytes |
| source maps | `0` | `0` |
| cold/warm decode | `<=250ms / <=100ms` | direct browser decode |
| cold local image ready | `<=750ms` | one same-origin request; zero external |
| attributable / total CLS | `<=0.01 / <=0.05` | reserved `16:9` slot |
| Host 06 activation / sampled task | `<=2ms / <=100ms` | same |
| focused / related / cold full | `<=30s / <=60s / <=60s` | same |
| production / fixture build | `<=60s` each | same |
| complete E2E | one invocation `<=180s` | no retry |

Generation is a bounded production-time external API operation only. The
shipped product remains local/offline with one same-origin selected-image
request and zero external requests. No dependency, lockfile, endpoint,
credential, service worker, telemetry, runtime model call, or external runtime
asset is added.

The later validation order remains: authority/script/argument/target/manifest
gates; Science synthetic CLI dry-run and output-target fixtures without live
generation; exact technical PNG/decode gates; original-resolution physical and
six-layout/accessibility gates; focused Host 05-to-06 and complete unchanged
`L02-03` learning/privacy/recovery proof; related rail and ending regression;
validators `40/40`, cold full, production and TD-012 fixture builds, PBA,
served/offline/decode/task/CLS; exactly one later complete E2E without retry;
owned cleanup; candidate freeze; and Git synchronization.

## Required Science adjudication

One fresh Office of Science Administrator must independently validate this
contract without invoking generation, the API, or pixel review. It must:

1. pin `HOST06-IMAGEGEN-CLI-v1`, exact installed path, SHA-256, byte length,
   help surface, model support, and relevant output implementation;
2. repeat only the boolean API-key presence check and freeze credential
   non-disclosure;
3. prove by dry-run and synthetic non-image fixtures the exact safe argument
   vector, prompt non-augmentation, `n=1`, `3840x2160`, `quality=high`, opaque
   PNG request, exact `--out`, no default/out-dir/force/downscale/edit/batch,
   and one-call/no-retry semantics;
4. prove the literal GUID root and both exact attempt paths are outside every
   protected root, ordinary/no-follow, absent before use, bounded to only the
   active target, and exactly cleanable without parent enumeration;
5. fixture-test existing-target refusal, the script's existence-check/write
   interval, race/overwrite and partial-write behavior, exact output identity,
   zero/multiple/unexpected output, CLI/API error, and every cleanup branch;
6. freeze exact candidate size/hash, strict PNG/chunk/CRC/sRGB/opacity/
   dimension/inflate/browser-decode implementation and limits;
7. preserve the physical, learning, privacy, save, accessibility, performance,
   offline, PBA, validation, no-reveal, rollback, ordinal, residual, and
   protected-state contracts; and
8. issue exactly one new versioned `POLISH VIABILITY READY`, `REVISE`, or
   `HOLD` artifact.

If actual help/source behavior contradicts the frozen invocation, if the
installed identity cannot be pinned, or if exact create-new/race/cleanup
semantics cannot be established without editing the script, using a wrapper,
or widening file discovery, Science must issue `HOLD`. It may not silently
change model, quality, size, prompt, options, output path, script, runner, or
ordinal policy.

## Rollback, protected state, and process records

Rollback remains bounded to the exact new selected raster, `PROVENANCE.md`,
bounded Host 06 product/config/test/E2E delta, release-command delta, and cycle
reports after exact identity proof. It never touches the CLI script, ordinal
`1`, the real managed directory, any opaque residual, accepted media, Hosts
04/05, curriculum, save schema, dependencies, Host 07+, later rail, user or
untracked work, browser/profile/save, repository QA quarantine, protected PDF,
training directory, VR-65, or a parent root.

All thirteen inherited process records remain separate and **OPEN**: VR-17,
VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage
4, VR-60 Call 9, and VR-61. The separate Commandant filename/search-scope
record remains **OPEN**. This stage closes, cures, merges, waives, or renumbers
none.

Repository QA quarantine, `Art Of No Mans Sky Book Scan.pdf`, `Simplilearn
Training Files/`, real browser/profile/save, hidden lore, user work, accepted-
media pixels, the real managed directory, ordinal-1 residual, OS-temp parent,
and VR-65 remain protected. The exact predeclared GUID root is inactive and
absent until a later shell authorizes the active ordinal.

## Validation performed and exact Science handoff

Operations verified exact starting `HEAD`
`796262d392e49e66cd635b4b016624dd5138f606`; read the active workflow,
registry, complete Operations profile, current handoff, complete
`FRWO-005-v4`, `FRVE-005-v4`, current release map/scoreboard, applicable
Host 06 baseline/continuity/effective-shell/return controls, and the installed
`imagegen` skill plus its CLI/API references. Operations inspected the actual
installed `generate --help`, script identity, relevant output code, API-key
presence boolean, and exact target absence.

No generator or API call, dry-run, prompt execution, OS-temp allocation,
managed-directory access, result/output discovery, media read/hash/decode,
pixel inspection, import, copy/provenance, product/test change, build, browser,
preview, E2E, reveal, maturity update, OPEN-record closure, VR-65 access,
schedule, automation, push, release, or `FIRST RUN COMPLETE` occurred.

One fresh Office of Science Administrator / `office_of_science_administrator`
reads this complete Work Order and synchronized authorities, performs only the
non-generative/non-API adjudication above, and issues one new versioned
`POLISH VIABILITY READY`, `REVISE`, or `HOLD` artifact.

Science may not invoke generation or the Image API; inspect pixels; allocate
the live attempt root; import media; write copy/provenance; authorize Mission
or Quartermaster; run E2E; reveal; advance maturity; close an OPEN record;
inspect VR-65 or the real managed directory; schedule; automate; push;
release; or call `FIRST RUN COMPLETE`.
