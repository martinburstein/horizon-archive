# First Run Polish Viability Envelope — The Third Coupling

Envelope ID: `FRVE-002-v1`

Stage: Office of Science Administrator / `office_of_science_administrator`

Work Order: `FRWO-002-v1 / The Third Coupling`

Baseline / lock: `FRPB-001-v1` / `FRCL-002-v1`

Release map / scoreboard: `FRRM-002-v1` / `FRSB-002-v1`

Science source inspected: `80b83044d185645a485c79c396561a1c8541f53a`

Released First Run predecessor: `FRAB-001-v1 / FIRST RUN PASS RELEASED`

Accepted product/test candidate: `d37103b46f257cb61ef99f76413611ee31d0fab2`

Budget authority: `PBA-TD012-v1`

Date: **2026-08-10**

## Disposition

**`POLISH VIABILITY READY / FRVE-002-v1`**

The immutable Glass Meadow plate can honestly carry one distinct Fracture
Nursery relationship without a raster change. Its original generation record
explicitly requests rejected cloudy growths, salvage, repairs, cutting
collars, feed channels, annealing, and underground routing. The accepted
integrated edit preserves that source plate while adding only the already
registered central field coupling and right Route Marker. The low-left
foreground therefore is not an invented object: it is existing repair-stock
material authored into the source plate and already preserved in the runtime
master.

One exact normalized target is viable at `left 0%`, `top 52%`, `width 24%`,
`height 48%`, with the same geometry at narrow and effective `200%`. It covers
the visually coherent low-left bulbs, sleeves, rejected glass, collars, fused
edges, and local channels while remaining disjoint from both existing targets.
This registration is a relationship over visible source material, not a claim
that a newly fabricated kiosk or responding object exists.

The existing optional `L01-03` machinery can be rehoused behind this target's
sole `USE` path without changing its evaluator, learning owner, evidence,
privacy, route, save schema, Demo Tour, later rail, or ending. Mission may issue
a shell if it adopts every fixed requirement below. No implementation begins
under this envelope.

## Independent inspection basis

- Read the complete active workflow, registry, Science profile, synchronized
  handoff, `FRWO-002-v1`, `FRPB-001-v1`, guarded revalidation, `FRAB-001-v1`,
  `FRCL-002-v1`, `FRRM-002-v1`, and `FRSB-002-v1`.
- Read the exact Chapter I game-on-paper sequence, the applicable surface-lore
  and voice rules, the current gameplay/curriculum/readiness/visual controls,
  accessibility requirements, `PBA-TD012-v1`, and the complete `L01-03`
  exercise/evidence contract.
- Inspected current `App.jsx`, `pixelMeadow.js`, `calibrationExercise.js`,
  `sceneTransition.js`, Terminal focus behavior, relevant styling, focused
  tests, both Meadow provenance records, and the accepted runtime master.
- Read-only visual inspection confirmed one coherent low-left repair-stock
  grouping in the accepted `1672 x 941` plate.
- Accepted master identity is `2,688,247` bytes, SHA-256
  `6B27AC8233A5C29583450064DA3D51C504E16A513759D52E5651B9C60EC031D2`.
- Fresh current focused baseline passes `24/24` across calibration, Meadow
  geometry, Route Marker, transition, and return in `121.721 ms` Node time.
- No volatile AI-901 assertion or learning change is introduced. The named
  `foundry-azure-source-priority` skill is unavailable in this session; no
  external source was needed because `L01-03`, its empty objective list, and
  `PY-007` ownership remain byte- and meaning-fixed.
- No user save/profile/browser, hidden lore, protected path, media mutation,
  image operation, or reveal was used.

## Answers to the eight Work Order questions

### 1. Provenance, crop, and non-overlap

**PASS.** The source provenance explicitly authored `rejected cloudy growths,
salvage, repairs`, annealing, cutting collars, and routing into Glass Meadow.
The integrated provenance says the edit preserves the prior plate, camera,
horizon, field, exposure, and composition and identifies only the added central
Terminal and right marker. Registering the already-visible low-left grouping as
the canonical repair-stock relationship consumes existing authored geometry;
it does not manufacture a third artifact in prose.

The fixed candidate rectangle is:

| Target | Left | Top | Right | Bottom | Source-pixel bounds at `1672 x 941` |
| --- | ---: | ---: | ---: | ---: | --- |
| Fracture Nursery | `0%` | `52%` | `24%` | `100%` | `x 0-401.28`, `y 489.32-941` |
| First Signal | `25%` | `11.1111%` | `75%` | `100%` | `x 418-1254`, `y 104.56-941` |
| Route Marker | `77.0313%` | `53.3333%` | `98.5938%` | `99.7222%` | `x 1287.96-1648.49`, `y 501.87-938.39` |

The Nursery ends one percentage point before First Signal begins, a `16.72`
source-pixel gap. First Signal ends `2.0313` percentage points before Route
Marker begins. Pairwise intersection area is exactly zero. The plate ratio is
`1672/941`; the runtime scene is `16/9` with centered `object-fit: cover`, so
only a sub-pixel-equivalent vertical edge is cropped and the entire horizontal
Nursery band survives every required layout.

| Scene width | Scene height at `16/9` | Nursery rendered size | Gap to First Signal |
| ---: | ---: | ---: | ---: |
| `1920` | `1080` | `460.8 x 518.4` | `19.2` |
| `1366` | `768.375` | `327.84 x 368.82` | `13.66` |
| `390` | `219.375` | `93.6 x 105.3` | `3.9` |
| `768` effective `200%` | `432` | `184.32 x 207.36` | `7.68` |

Even the retained `320 x 180` canonical fixture yields a `76.8 x 86.4`
target, above the `44 x 44` minimum. Tactical must remeasure actual DOM/source
mapping rather than accept percentages alone. Any crop, style, or layout that
reduces source overlap below `0.85`, removes the target center from this source
band, or creates any positive pairwise target overlap is a fail-closed `HOLD`.

The scene image's broad alt must remain truthful and neutral; it may describe
visible low repair stock but may not call the raster itself interactive or
responsive. The active button owns the stable assistive name **`Fracture
Nursery coupling`** plus action and non-color state.

### 2. Exact state derivation and optional route

**PASS with one derived, nonpersisted state.** A pure derivation may expose:

```text
routeMarkerMastery != mastered       -> hidden/inert (no button, no affordance)
routeMarkerMastery == mastered
  and calibrationMastery == null     -> available
  and calibrationMastery in progress -> in_progress
  and calibrationMastery mastered    -> complete
```

Both inputs must pass their existing sanitizers. Forged IDs or malformed
mastery fail closed. The derivation is view state only and may live beside
`MEADOW_PIXEL_HOTSPOTS`; it is not a new save field, evidence class, route
gate, or completion key.

`completed`, `pendingSceneId`, and the current `pendingAdvance=true` Meadow hold
remain exact after Route Marker mastery. The Nursery button alone stays usable
inside that hold, and the Meadow verbs may remain usable solely to select
`LOOK AT`, `TALK TO`, or `USE` for the Nursery. First Signal and Route Marker
remain nonactive completed surfaces. The existing `Depart: Drowned Archive`
action remains enabled before, during, after, or without calibration. Returning
from Drowned derives the same Nursery state from sanitized existing evidence
and performs no write.

Detection may mount one polite Suit/System state announcement after exact Route
Marker mastery. It is a report of newly recognized compatibility, not a field
change. It creates no evidence, no extra required activation, and never repeats
on re-render.

### 3. Sole in-world calibration entry

**PASS.** Remove the generic departure-row calibration launcher completely.
Exactly one campaign activation remains: `USE Fracture Nursery coupling`.
`LOOK AT` reports material observation; `TALK TO` reports silence; neither
opens a Terminal or writes evidence. `USE` opens the unchanged
`EX-L0103-CALIBRATION-DEBUG` workspace only when the sanitized Route Marker is
mastered.

The current evaluator, starters, diagnoses, two strict `8/8` forms, `4/4`
retrieval, confidence, misconception tags, remediation, unlimited retry,
visible exit, and exact evidence allowlist remain byte/meaning exact.
`calibrationMastery` remains the only durable record. Source, traceback,
output, choices, notes, dialogue, focus, and detection are never persisted.
Close/reopen in the same scene may preserve the in-memory session; full reload,
departure, or return reconstructs clean working content from sanitized mastery.

If mastery is already complete, `USE` must not reopen a fresh scored form or
downgrade mastery. It may report the completed local record or mount a bounded
read-only summary using existing evidence, but it must create zero attempt,
check, hint, confidence, route, or save change. Demo Tour remains separately
named, zero-credit, and cannot use Nursery appearance or interaction to mint
campaign state.

### 4. Focus and recovery ownership

**PASS under this deterministic focus table.** No focus landing is evidence.

| Event | Required focus / recovery |
| --- | --- |
| Route Marker mastery detects Nursery | Nursery hotspot after the Route Terminal unmounts; polite detection announcement once |
| `LOOK AT` or `TALK TO` Nursery | Nursery hotspot remains focused; dialogue live region reports the owner-correct result |
| `USE` Nursery | Terminal title receives initial focus; modal containment and Escape behavior remain exact |
| Exit/Escape or failed-practice close | Connected, enabled Nursery trigger; if unavailable through fail-closed sanitation, `Depart: Drowned Archive` |
| Active failure/remediation | Existing actual-failed control/status inside Terminal; no focus escape or forced departure |
| Mastery acknowledgement | Nursery hotspot, then owner-correct completion announcement; departure remains next reachable action |
| Reload with unfinished `calibrationMastery` | Nursery hotspot; clean session reconstruction only after deliberate `USE` |
| Reload unstarted or mastered | `Depart: Drowned Archive`; Nursery remains next in canonical scene focus order and discoverable |
| Drowned return with unfinished calibration | Nursery hotspot |
| Drowned return unstarted or mastered | Departure action; Nursery remains available/discoverable |
| Malformed route or calibration evidence | Existing earliest safe fallback; no Nursery, no credit, no skipped prerequisite |

Pointer, touch, keyboard, Enter/Space, and switch-like semantic activation call
the same handler once. Repeated-key and simultaneous activation cannot create
multiple attempts. Disabled/hidden targets never receive focus. The source
order remains First Signal, Route Marker, Fracture Nursery, then adventure
controls, preserving canonical discovery order rather than visual-left order.

### 5. Required meanings and voice owners

Quartermaster owns exact final prose. The shell must require these meanings:

| State | Required meaning | Owner |
| --- | --- | --- |
| Pre-detection | Low rejected/fractured repair stock is physically present; no active affordance or implied invitation | Scene |
| Detected | The suit can now classify one compatible local coupling; the material did not move, wake, open, or answer | Suit/System |
| Optional, unstarted | Existing scored calibration is available here and the Drowned route is already open | Suit/System |
| In progress / remediation | Human working copy has a bounded gap; route stays open; only actual misses are named | System / 901 Teacher |
| Mastered | Existing `L01-03` evidence is finalized; every crack and the field remain unchanged | System plus Scene fact |
| Declined / departed | No refusal, loss, shame, or state change; the Pilot deliberately follows the pre-existing ridge | Pilot / Scene |
| Returned | The same repair stock and coupling remain; unfinished optional work can be resumed from this host | Scene / Suit/System |
| Chapter turn | Three unlike local bodies shared the expedition's familiar interface; the familiarity came from human tooling, not Builder/Machine speech | Pilot |

Machine and Builders own no line. Copy may not say healed, earned route,
accepted user, expected visitor, invitation, reward, approval, mission, native
Python, or response. The current phrase `crowned passage earned` may not survive
if it implies calibration earned the already-open route; final copy must state
route independence unambiguously.

### 6. CSS, performance, and asset budget

**PASS only fail-closed.** Current accepted evidence is JS `1,661,383 /`
`1,703,258`, CSS `119,662 / 119,672`, modules `216 / 222`, and media exact
`17 / 37,410,731`, leaving `41,875` JS bytes, `10` CSS bytes, and `6` modules.

- Prefer existing hotspot, status, focus, dialog, forced-color, reduced-motion,
  and responsive rules.
- Any added CSS must be offset by removing only demonstrably redundant or
  equivalent CSS in the same candidate. Aggregate emitted CSS must remain
  `<=119,672`; an overage of one byte is `HOLD`, not a waiver.
- Aggregate emitted JS must remain `<=1,703,258`; production modules `<=222`;
  build `<=60s`; focused `<=30s`; related/full `<=60s`; E2E `<=180s`; sampled
  main-thread task `<=100ms`.
- No dependency, network request, service worker, Python/WASM runtime, source
  map, or runtime media may be added. Media remains the exact accepted hash set
  with zero new/changed/moved/removed files.
- No style reduction may weaken target size, focus, contrast, containment,
  forced colors, reduced motion, or any unrelated surface.

Any cap failure, raster need, crop export, overlay cutout, invisible semantic
button, or inability to keep the three targets disjoint returns `HOLD`.

### 7. Focused, related, and full regression proof

**PASS with mandatory new evidence.** At minimum:

1. Pure geometry/state tests assert the exact rectangle above, `>=44px` at all
   four required layouts plus `320 x 240`, `>=0.85` source-band overlap, center
   containment, and zero pairwise target overlap.
2. State tests cover forged/malformed Route Marker and calibration evidence;
   pre-Marker absence; exact post-Marker availability; unfinished and mastered
   derivation; no new save key; and no availability credit.
3. App contracts cover LOOK/TALK/USE ownership, exactly one active calibration
   path, generic-launcher absence, completed-use non-regression, departure
   availability in every calibration state, and no activation of First Signal
   or Route Marker during the post-Marker hold.
4. Calibration tests retain the exact `8/8 + 8/8 + 4/4`, actual-miss-only
   remediation, answer-free retry, unlimited attempts, safe exit, same-scene
   session, full-reload clean reconstruction, and strict evidence sanitizer.
5. Save/privacy tests enumerate the full campaign projection and prove only
   existing `calibrationMastery` changes; source, traceback, output, choices,
   notes, detection, focus, and content sentinels never appear in storage.
6. Return tests cover skip-departure, Drowned write-free return, unfinished
   resume from Nursery, mastered return, exact redeparture, `completed` and
   `pendingSceneId`, and zero cross-credit.
7. Demo Tour tests prove Nursery cannot add a scene, boundary, score, mastery,
   campaign completion, or saved field and that the Tour remains zero-credit.
8. Opening, First Signal, Route Marker, Drowned entry, Witness, City bridge,
   later rail, and both MH-40 outcomes remain exact in related/full tests.
9. One and only one complete production E2E first proves the route can be
   skipped, enters Drowned, returns write-free, completes optional calibration
   from Nursery after at least one real miss/exit/reload recovery, redeparts,
   and reaches both equal MH-40 outcomes with zero runtime errors.
10. Live affected-state review covers `1920x1080`, `1366x768`, `390x844`, and
    `768x900` effective `200%`; keyboard, pointer, target, source mapping,
    non-color states, forced color, reduced motion, focus, announcements,
    overflow, and longest copy. Automation is not human AT certification.

No second overlapping whole-game journey may be claimed. Existing tests may be
strengthened but not replaced by string-presence-only proof.

### 8. Canonical release-command manifest

**PASS if Mission authorizes one checked-in manifest before production.** The
canonical location is:

`Production Pipeline/First Run/FIRST_RUN_RELEASE_COMMAND_MANIFEST_FRRC-001-v1.json`

It is a release-control artifact, not runtime code. Mission owns its first
authorization; Combat may instantiate it exactly; Quartermaster/Image may only
append their named focused tests without changing command meaning; Intelligence
must execute or independently reproduce every release entry from the frozen
candidate.

The manifest must contain explicit `id`, `workdir`, argument-array `command`,
timeout, expected exit, owner, output/port ownership, prerequisites, and cleanup
for these entries:

- `focused`: exact nonempty file list including calibration, Meadow geometry,
  Nursery, Route Marker, transition, return, privacy, and Tour tests;
- `related`: exact nonempty early-rail/opening/Drowned/Witness/City and affected
  accessibility/save tests;
- `full`: `node --test` from `horizon-archive-game`;
- `validators`: sorted enumeration of exactly the current `40`
  `curriculum/**/validate*.py` files, each invoked with `--self-test`; any count
  other than `40` requires explicit reconciliation before execution;
- `production-build`: `npm run build`;
- `fixture-build`: `npm run build:td012-fixture`;
- `production-preview`: exact `vite preview`, `127.0.0.1`, strict owned port
  `4173`, production `dist`;
- `fixture-preview`: exact TD-012 config
  `review-fixtures/td012-measured-horizon/vite.config.js`, `127.0.0.1`, strict
  owned port `4184`—never generic preview defaults;
- `served-identity`: root, one deep fallback, emitted JS, and emitted CSS for
  both previews return `200`; documents/fallback and served/disk bytes match;
- `complete-e2e`: one owned production preview, one isolated QA directory,
  `HORIZON_ARCHIVE_URL`, `HORIZON_ARCHIVE_QA_DIR`, and exactly one invocation of
  `playtest/e2e-playthrough.mjs`;
- `pba-media`: aggregate every emitted JS/CSS chunk, exact module count, exact
  accepted media inventory/hash, forbidden runtime asset scan, and all caps;
  and
- `cleanup-identity`: stop only recorded owned PIDs, verify ports `4173` and
  `4184` clear, remove only exact manifest-created fixture/QA outputs after
  resolved-path containment checks, report any blocked cleanup honestly, run
  `git diff --check`, protected-boundary checks, and record exact candidate
  commit before and after the ladder.

The manifest must not contain an empty test-name filter, validator invocation
without `--self-test`, generic fixture preview, broad process kill, broad temp
delete, real browser/profile/save path, or protected path. Shell commands that
diverge from the checked-in manifest do not supply release evidence until the
manifest is versioned and the exact gate rerun.

## Reused systems and permitted production surface

Reused unchanged in authority: accepted Meadow master; normalized hotspot
renderer; current Scene/Pilot/Suit dialogue ownership; existing Route Marker
mastery; `pendingAdvance` and `pendingSceneId`; existing calibration evaluator,
session, sanitizer, save projection, Terminal shell, focus containment, and
Demo Tour isolation; completed-Meadow return patch; Drowned departure and
return; full later rail and MH-40.

Mission may permit changes only to:

- `horizon-archive-game/src/App.jsx`;
- `horizon-archive-game/src/pixelMeadow.js` for exact geometry and pure derived
  view state;
- `horizon-archive-game/src/sceneTransition.js` only for bounded presentation,
  return, or focus derivation without route/state change;
- `horizon-archive-game/src/calibrationExercise.js` only if wiring/name reuse is
  unavoidable; evaluator, content, thresholds, and sanitizer meaning are
  patch-forbidden;
- `horizon-archive-game/src/styles.css` only under exact aggregate cap and with
  no unrelated presentation regression;
- focused/related tests and the single existing E2E;
- `FRRC-001-v1` release manifest; and
- current First Run reports and synchronized controls.

Curriculum source, lesson JSON/Markdown, save schema/version, dependencies,
canonical story/lore, Drowned/later controllers, TD-012 fixture behavior,
accepted media, real storage, hidden lore, and protected paths are
patch-forbidden.

## Hard stops and variance routing

Return `HOLD` to Operations/Science for any new media need, unsupported source
reinterpretation, crop loss, overlap, invisible host, CSS/JS/module/time overage,
save-schema change, new dependency, route/calibration coupling, privacy leak,
Demo Tour credit, or inability to run one isolated E2E. Return canon/voice
conflict to Colonel, Work Order expansion to Operations, and cross-discipline
ambiguity to Mission.

Rollback is the bounded `FRWO-002-v1` implementation, tests, manifest, and
reports. The accepted `FRAB-001-v1` product/test candidate remains the exact
recoverable baseline. No migration is required because no new durable field is
authorized.

## Maturity impact

None in Science. `FR-02` remains at its current scoreboard values until
accepted as-built evidence exists. This envelope proves feasibility; it does
not create a host, release content, or advance `FR2`, `FR3`, or `FR4`.

## Exact Mission handoff

Read `First Run Agent Profiles/mission-captain.md` in full, then read
`FRWO-002-v1`, this `FRVE-002-v1`, `FRPB-001-v1`, guarded revalidation,
`FRAB-001-v1`, `FRCL-002-v1`, `FRRM-002-v1`, and `FRSB-002-v1`. Independently
verify the fixed `0/52/24/48` Fracture Nursery rectangle against the immutable
master/provenance and reconcile every Work Order requirement with this
envelope.

Issue exactly one versioned `FIRST RUN SHELL READY`, `REVISE`, or `HOLD` for
`FRWO-002-v1`. If ready, lock the sole in-world `USE` entry, pure view-state
derivation, route/save/privacy/focus/accessibility/PBA contracts, exact
permitted files, the `FRRC-001-v1` command-manifest requirement, one complete
non-overlapping E2E, hard stops, and rollback boundary before handing Recon one
exact production edge.

Do not write final prose, implement production, alter learning/save/route/
world/ending/media, advance maturity, begin Recon, inspect protected state, or
perform an image/media/reveal operation inside Mission.
