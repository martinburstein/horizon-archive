# Horizon Archive First Run Player Experience Blueprint - Sediment Abacus

Blueprint ID: `FRPX-007-v1`

Stage / owner: Tactical Operations Specialist / `tactical_operations_specialist`

Disposition: **`PLAYER EXPERIENCE READY / HOST 07 NULL-FIRST / FIVE CLI CANDIDATE RECORDS`**

Shell / treatment: `FRSH-007-v1` / `FRDT-007-v1`

Work Order / viability: `FRWO-007-v2` / `FRVE-007-v2`

Source inspected: `fdb1a4a8e450a47e4159eb7138bc2d4812b5aed9`

Date: **2026-08-12**

## Result

The shell, treatment, current Host 06 implementation, and existing Structured
Packets (`L03-01`) and Control Flow (`L03-02`) exercises are compatible.
Combat can build an inert Host 07 integration without inventing state, order,
copy purpose, focus, recovery, evidence, geometry, or candidate behavior.

All candidate fields remain null and all five generation ordinals remain
unconsumed. No media was generated, read, imported, rendered, or revealed.

## Current and target graphs

```text
CURRENT RELEASED
Host 06 + L02-03 mastered
  -> accepted Host 06 source remains
  -> generic Start Structured Packets command appears
  -> unchanged L03-01 -> unchanged L03-02

COMBAT INERT CANDIDATE
source.enabled=false
  -> released Host 06 and generic command remain exact
  -> no Host 07 image, node, name, focus, notice, action, save, or credit

LATER ACCEPTED CANDIDATE
exact Host 06 lawful + exact L02-03 mastery
  + selected source/provenance/measurement/decode PASS
  -> one committed Host 06 paint
  -> direct same-basin replacement with Host 07 source
  -> focus Sediment Abacus once; one factual Suit notice
  -> generic Structured Packets launcher absent
  -> LOOK physical/write-free; TALK silence; USE opens unchanged L03-01
  -> exact L03-01 mastery makes unchanged L03-02 eligible
  -> exact L03-02 mastery leaves Host 07 read-only and preserves next rail
```

No Host 06 hotspot is projected onto Host 07. Host 06 and Host 07 never coexist
on the wrong source. The cross-source logical order is Host 06, Host 07, then
the existing return/navigation controls.

## Null-first registry

Combat declares these immutable shapes and populates no selected value:

```text
source = { enabled:false, path:null, sha256:null, byteLength:null,
           width:null, height:null, format:null, color:null, attemptId:null }
provenance = { sha256:null, byteLength:null, promptId:null, cliSha256:null }
relation = { x:null, y:null, width:null, height:null,
             centerX:null, centerY:null }
semanticTarget = { x:null, y:null, width:null, height:null }
labelAnchor = { x:null, y:null, width:null, height:null,
                insetOuterCss:3, insetTextCss:5 }
sedimentHandoff = { x:null, y:null, width:null, height:null }
dryApproach = { x:null, y:null, width:null, height:null }
depthBands = null
noduleGroup = { x:null, y:null, width:null, height:null }
protected = { host06:null, liveWater:null, returnRoute:null, crown:null,
              tidalLens:null, host08:null }
layouts = { desktop:null, laptop:null, narrow:null, effective200:null,
            retained320x180:null, retained320x240:null }
copy = { unseen:null, available:null, inProgress:null, missed:null,
         mastered:null, returned:null, nextBoundary:null }
alt = null
```

Accepted `attemptId` must be exactly one of `H7-1` through `H7-5`; there is no
ordinal scalar. Source is exact 3840x2160 opaque sRGB PNG, byte length
`1..30000000`, lowercase SHA-256, exact product path, and byte/SHA equality to
provenance. Prompt ID/hash must match the selected frozen prompt. Aggregate
accepted media must remain `<=83554983` bytes.

## Pure guard and irreversible legacy switch

```text
predecessorPass = Host06 is lawful
  && sanitized modelChoiceEvidence is exact mastered

sourceIdentityPass = source.enabled===true
  && exact path/dimensions/format/color/attemptId/bytes/SHA
  && provenance byte/SHA/prompt/CLI identity equality

measurementPass = exact FRVE-007-v2 relation/center/semantic/label/handoff/
  approach/depthBands/noduleGroup/protected/layout schema and thresholds

decodePass = imported image complete && naturalWidth===3840
  && naturalHeight===2160

host07Lawful = predecessorPass && sourceIdentityPass
  && measurementPass && decodePass

legacyStructuredLauncherVisible = source.enabled !== true
```

`source.enabled` is irreversible candidate mode, not lawfulness. Before it is
true, the released generic launcher remains exact. Once true, that launcher is
absent even if any later source, provenance, decode, measurement, or evidence
guard fails. Failure retains the last safe accepted Host 06 view, makes Host 07
wholly absent, cancels transition/focus tokens, and never restores a bypass.

## View states

State derives only after `host07Lawful` passes.

| Sanitized evidence | State | Player result |
| --- | --- | --- |
| Structured Packets absent | `available` | observation and sole USE |
| L03-01 remediation | `remediation_required` | USE resumes exact remediation |
| L03-01 in progress/primary/transfer | `in_progress` | USE resumes clean boundary |
| L03-01 mastered; L03-02 not mastered | `in_progress` | USE opens unchanged Control Flow |
| both L03-01 and L03-02 mastered | `complete` | observation; USE read-only |
| malformed/forged/contradictory | `hidden` | no node, focus, notice, action, credit |

```text
H0 hidden -> no Host 07 surface
H1 available -> LOOK/TALK remain H1; USE opens L03-01
H2 in_progress -> USE resumes lawful current lesson (L03-01 then L03-02)
H3 remediation_required -> USE resumes actual answer-free remediation
H4 complete -> LOOK/TALK invariant; USE reports finalized status without write
```

Observation, focus, candidate review, image load, provisional naming, and
dialogue never mint evidence.

## Source order, transition, focus, and announcement

At every layout, Host 07 phase order is: one selected image; one Host 07 native
button; scene status; native LOOK/TALK/USE controls; factual dialogue; existing
return/navigation. There is no hidden, disabled, duplicate, zero-size, or
offscreen Host 07 substitute.

Exact Host 06 + L02-03 mastery sets one nonpersisted transition token only when
`source.enabled===true`. After Terminal unmount and one committed Host 06 paint,
the next animation frame rechecks `host07Lawful`, replaces the source, consumes
the token, and focuses Host 07 with `preventScroll:true`. One polite live-region
notice follows: local compatible surface and expedition availability only.
It never claims discovery, response, answer, invitation, reward, or mastery.

Reload, return, resize, decode repetition, hover, focus traversal, forced color,
and reduced-motion changes do not replay the notice. A failed recheck cancels
the token and settles focus on Host 06 or the first lawful scene control.

## Actions and recovery

| Event | Eligibility | Result | Focus | Writes |
| --- | --- | --- | --- | --- |
| Select verb | no modal | native pressed state | selected verb | none |
| LOOK AT Host 07 | H1-H4 | visible sediment/channel/nodule facts | Host 07 | none |
| TALK TO Host 07 | H1-H4 | complete silence/nonresponse | Host 07 | none |
| USE Host 07 | H1-H3, L03-01 incomplete | existing `openStructuredPackets()` once | Terminal title | none on open |
| USE Host 07 | H2/H3, L03-01 mastered | existing `openControlFlow()` once | Terminal title | none on open |
| USE Host 07 | H4 | allowlisted completion status | Host 07 | zero |
| Close/Escape | lesson open | close only; private active input cleared per existing rule | Host 07 if lawful, else Host 06, else first lawful control | none |
| Return/reload/resume | existing gate | sanitized evidence reconstructs only lawful state | first connected lawful target | none |

All world targets remain native buttons. Pointer, touch, Enter, Space,
screen-reader/speech-by-name, and switch activation converge on the one click
path. Pending intent blocks mixed-input double open. Unavailable states are
absent, never color-only disabled dispatchers.

Accessible name: `${verb.toLowerCase()} Sediment Abacus, ${state}` where state
is `available`, `in progress`, `remediation required`, or `complete`.

## Copy and alt ownership

Quartermaster may populate the seven declared copy slots only after a passing
candidate, using this purpose ledger:

- `unseen`: visible irregular nodules, graded sediment, three channel depths,
  dry approach; no interpretation;
- `available`: Pilot provisional name plus Suit compatibility only;
- `inProgress`: clean sanitized boundary and unchanged physical scene;
- `missed`: one unresolved checked learning dimension and answer-free Terminal
  guidance only;
- `mastered`: finalized evidence and unchanged world;
- `returned`: same unchanged field and sanitized boundary restoration;
- `nextBoundary`: only the already-existing continuation.

Alt is one factual sentence naming irregular mineral nodules distributed across
at least three eroded channel depths on a dry reachable shelf. It omits
position-only instructions, literal-abacus meaning, correctness, purpose,
world response, hidden lore, and Host 08.

## Six-layout geometry

Every selected registry must carry the six exact layout records. Source is
3840x2160, centered, with full-source visible rectangle. Each record includes
viewport, rendered/visible source, retained area, relation/semantic/label,
handoff/approach/depth-band/nodule-group mappings, protected intersections,
logical order, and before/after focus rectangles.

Acceptance repeats all `FRVE-007-v2` thresholds: center-safe corridor, relation
size/center, three nonoverlapping depth bands and span, connected handoff and
dry approach, `>=95%` relation retention, all essential centers visible,
semantic target `>=44x44`, physical center contained, zero protected overlap,
label/focus separation `>=8px`, stable forced-color outline, no horizontal
escape, and factual alt parity at desktop, laptop, narrow, effective 200%,
320x180, and 320x240.

## Private candidate record

Quartermaster owns one bounded record per launched attempt:

```text
attemptId, promptFile, promptBytes, promptSha256, cliBytes, cliSha256,
processLaunched, exitCode, candidateOrdinary, candidateOneLink,
candidateByteLength, candidateSha256, pngStrict, decoded, opaque,
technicalPass, physicalPass, layoutsPass, accessibilityPass,
reasonCodes[], identityReproofPass, cleanupPass, terminalDisposition
```

Reason codes are allowlisted: `TECHNICAL`, `PHYSICAL`, `LAYOUT`,
`ACCESSIBILITY`. A rejected record retains no candidate path/hash/pixels or
visual description. Nonzero CLI exit is not objective rejection and stops.
Objective rejection plus exact cleanup alone enables the next fixed prompt.
First PASS stops the remaining ledger. No sixth attempt exists.

Selected record additionally contains product path, exact byte/SHA equality,
provenance identity, accepted media count/bytes, registry population, and later
validation results. It contains no credential or rejected-candidate facts.

## Acceptance matrix for Combat

Combat must prove at minimum:

1. null-first registry and provenance contain no selected values;
2. source guard accepts only H7-1..H7-5 and exact product identity;
3. malformed source/provenance/measurement/decode each hides Host 07;
4. exact Host 06 plus strict L02-03 mastery is the sole predecessor;
5. legacy Structured Packets launcher is visible iff `enabled!==true`;
6. enabled-but-invalid never restores the generic launcher;
7. hidden state creates no DOM/name/focus/notice/action/save/credit;
8. selected phase contains one image and one Host 07 target only;
9. Host 06 target is absent from Host 07 source;
10. LOOK and TALK write zero evidence;
11. USE routes L03-01 before L03-02 with unchanged evaluators;
12. L03-01 remediation/in-progress/mastered mapping is exact;
13. L03-02 is ineligible before strict L03-01 mastery;
14. complete USE is read-only;
15. one transition token, one animation frame, one focus, one notice;
16. guard loss cancels transition and uses lawful fallback focus;
17. close/Escape returns focus to the connected lawful target;
18. reload/return reconstructs from sanitized evidence only;
19. repeated/mixed input opens at most one Terminal;
20. six layout schemas and objective thresholds fail closed;
21. semantic target is native and at least 44x44;
22. forced-color and reduced-motion preserve meaning/focus;
23. Demo Tour receives no Host 07 node or credit;
24. no Host 07 localStorage/save family/private response field exists;
25. candidate ledger accepts only fixed prompt/hash/CLI identities;
26. nonzero CLI exit cannot authorize the next attempt;
27. rejection requires bounded code plus exact cleanup;
28. PASS stops later attempts and selected import is byte-identical;
29. media, observation, naming, focus, and review mint zero evidence;
30. accepted Hosts 01-06/media remain byte-identical;
31. no branch/reward/access/authority/world response/hidden lore is added;
32. RP-012 and `successor=null` remain exact;
33. performance/PBA/maps/dependency budgets pass;
34. focused, related, full, validator, build, served, and E2E gates pass;
35. all scratch paths are absent and owned processes/ports are clear.

## Regression radius, hard stop, and variances

Regression covers Host 06, model choices, Structured Packets, Control Flow,
route/focus/save/privacy/Demo Tour/ending, responsive presentation, accepted
media identity, builds, and served output. Combat may change only Host 07
registry/route/state/view/copy/styles and focused tests authorized by the shell;
it may not populate selected source values or generate media.

Any ambiguity in order, state, source identity, geometry, evidence, focus,
candidate custody, cleanup, protected state, or attempt ledger is HOLD.

Recon's protected-QA filename enumeration remains **OPEN PROCESS ONLY** with no
content access/evidence. Every earlier OPEN record, both enumeration records,
protected exclusions, and VR-65 remain separate and unchanged.

## Exact Combat handoff

Use one fresh Combat Engineer context. Read the full Combat profile, shell,
treatment, this blueprint, all five immutable prompt files, current Host 06 and
learning sources/tests, and exact validation budgets. Implement only inert
null-first Host 07 code/tests, run the non-media ladder, issue a dedicated
functional report, commit/push at the Combat gate, and route fresh
Quartermaster. Do not generate, inspect, import, or reveal media.

Tactical signs **`PLAYER EXPERIENCE READY / FRPX-007-v1 / HOST 07 NULL-FIRST /
ROUTE FRESH COMBAT`**.
