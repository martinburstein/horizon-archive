# Horizon Archive First Run Shell - Host 07 Sediment Abacus

Shell ID: `FRSH-007-v1`

Stage / stable agent: Mission Captain / `mission_captain`

Work Order: `FRWO-007-v2`

Disposition: **`FIRST RUN SHELL READY / HOST 07 SEDIMENT ABACUS /
IMAGEGEN CLI / FIVE TOTAL SEQUENTIAL ATTEMPTS`**

Date: **2026-08-12**

Shell source: `b036165f`

Released predecessor: `FRAB-005-v1`, product candidate `57b7b396`

Baseline / continuity / viability: `FRPB-001-v4` / `FRCL-006-v2` +
`FRCL-006-v1` / `FRVE-007-v2`

Current maturity: Host 07 missing; `FR-03` physical `FR0`

Target: one host-level exact/released Sediment Abacus relationship while
`FR-03` remains physical `FR0` until Hosts 08-15 are complete.

## Fixed player-visible outcome

After exact Host 06 and unchanged `L02-03`, graded sediment leads along the
existing dry clockwise shelf to one distributed irregular mineral-nodule
relationship across several wash-channel depths. The Pilot provisionally names
it `Sediment Abacus`; LOOK reports physical facts, TALK remains silent, and
sole USE enters unchanged `L03-01`, followed by exact `L03-02` eligibility.

No lesson, branch, route opening, reward, access, authority, world response,
Machine/Builder dialogue, hidden-lore answer, Host 08 reveal, RP-013, or
successor is added.

## Exact generation authority

The only generator is:

```text
C:\Users\marti\.codex\skills\.system\imagegen\scripts\image_gen.py
bytes=35266
sha256=c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05
python=3.12.10
openai=3.0.0
```

Every call uses:

```text
python <script> generate
  --prompt-file <exact prompt>
  --model gpt-image-2
  --size 3840x2160
  --quality high
  --output-format png
  --out <exact ordinal candidate>
  --no-augment
```

`n=1` is the CLI default. No `--force`, batch, edit, input image, mask,
downscale, background, compression, moderation override, alternate model,
direct REST/SDK runner, script edit, or internal retry is permitted. API-key
presence is checked as a boolean; value secrecy is absolute.

## Exact scratch and product paths

The shell-owned root is frozen and currently absent:

```text
C:\Users\marti\AppData\Local\Temp\
horizon-archive-host07-cli-8f1d3d2b-672d-4682-a5c4-1f87a6ed3097
```

Its only permitted candidate children are `h7-1.png` through `h7-5.png`.
Quartermaster creates the root immediately before the first eligible call,
proves ordinary/non-reparse containment and empty child set, and deletes it
nonrecursively after final cleanup.

Product paths are exactly:

```text
Visual Direction/Production Masters/2026-08-12-first-run-host07/
  host07-sediment-abacus-master-v1.png
  PROVENANCE.md
```

Both are currently absent. Import is create-new/no-replace after acceptance.

## Attempt ledger and prompt ownership

Domain is `{H7-1,H7-2,H7-3,H7-4,H7-5}`, all unconsumed. Recon owns five final
prompt texts and directorial interpretation; Mission requires all five prompt
files/hashes be frozen before H7-1. Tactical may tighten measurable composition
language through the prompt files before Combat freeze, but may not change
physical meaning. After Combat candidate freeze, prompts are immutable.

One ordinal becomes consumed when its CLI process is launched. A nonzero exit
returns to Science/Mission and authorizes no next call. Objective rejection
plus exact identity cleanup enables the next ordinal. First PASS stops the
ledger. No sixth call or rerun exists. Rejected candidate pixels and paths are
never reused, referenced, revealed, or published; only bounded reason codes may
inform already-predeclared later prompts.

## Candidate acceptance

Quartermaster privately validates each candidate in this order:

1. exact path/ordinary/non-reparse/one-link identity and sole-child set;
2. `1..30,000,000` bytes, SHA-256, strict static PNG, CRC/chunks,
   `3840x2160`, RGB/RGBA, fully opaque, local decode;
3. all ten physical predicates in `FRWO-007-v1` and every rejection condition
   in `FRCL-006-v1`;
4. `FRVE-007-v2` normalized relation/center/handoff/approach/depth-band/group/
   semantic/label/protected records and center-safe thresholds;
5. six-layout `>=95%` retention, all essential centers visible, semantic target
   `>=44x44`, zero protected overlap, label/focus separation `>=8px`, and
   truthful equivalent alt; and
6. exact candidate identity reproof before rejection delete or selected copy.

Any ambiguity rejects. Aesthetic quality never overrides a failed predicate.

## State and interaction contract

Combat builds inert null-first Host 07 registry/source/provenance/measurement/
copy/alt records. Exact Host 06 + `L02-03` mastery is the sole predecessor.
Before enablement Host 07 has no DOM/focus/name/action/evidence/save presence.
Accepted-only enablement is irreversible; any later source/provenance/decode/
measurement failure remains fail-closed and does not resurrect generic
continuation.

LOOK is visible-fact-only and evidence-free. TALK is silent. USE enters exact
`L03-01`; `L03-02` requires exact current `L03-01` mastery. Completed USE is
read-only. Return/reload/resume restore truthful local state. All existing
learning thresholds, evidence firewall, remediation, transfer, sanitization,
privacy, Demo Tour, save, offline, and no-exam-guarantee rules remain exact.

## Role sequence and freedoms

1. **Recon**: freeze one premium first-person Drowned interpretation and five
   complete prompts; no generation.
2. **Tactical**: freeze interaction/state/focus/recovery/copy-slot, six-layout,
   semantic/label, and private candidate-review blueprint; no generation.
3. **Combat**: implement inert fail-closed Host 07 runtime/tests, freeze code
   candidate and prompt/CLI identities, run non-media gates, commit/push; no
   generation.
4. **Quartermaster**: sole owner of CLI calls, private review, rejection
   cleanup, selection, byte-identical product import, provenance, accepted-only
   field population, content gates, and `PRODUCTION CONTENT COMPLETE`.
5. **Image Specialist**: no generation/edit/replacement; polish only code/config
   presentation using the selected unchanged source.
6. **Intelligence**: independently validate exact as-built candidate, release
   or reject, update maturity/inventory, and synchronize NEXT.

No role may skip or overlap.

## Permitted runtime scope

After Tactical lock, Combat may change only the exact Host 07 registry/route/
state/view/copy/styles and focused test files named in its report, plus later
selected Host 07 product/provenance paths. Reuse current Structured Packets and
Control Flow; do not add dependencies or alter accepted media. Recon/Tactical/
planning docs and dedicated stage reports are permitted per workflow.

## Performance and accessibility

All exact `FRVE-007-v2` budgets apply: candidate/aggregate media, JS/CSS/
modules/maps, decode/ready/handler/CLS, test/build/E2E times, semantic `44px`,
keyboard/pointer/touch/switch equivalence, focus/announcement, six layouts,
effective `200%`, forced color, reduced motion, and truthful factual alt.

## Validation ladder

- authority, Git, CLI/dependency/key boolean, prompt/hash, ledger, scratch and
  product absence, accepted-media immutability preflight;
- focused Host 07 + Structured Packets + Control Flow + Host 06 tests;
- related route/focus/save/privacy/ending tests and validators;
- full product tests, production + fixture builds, PBA/maps/media inventory;
- served root/deep/JS/CSS/new-media/fixture identity;
- isolated nonoverlapping E2E Host06->Host07 primary/remediation/transfer/
  return/reload/complete;
- desktop/narrow/effective-200%, keyboard/focus/forced-color/reduced-motion
  review;
- exact scratch/rejected-output cleanup, process shutdown and ports clear;
- dedicated stage commits and Mission/Combat/Intelligence push gates; and
- Intelligence ancestry/synchronization proof.

## Definition of done

Done requires one selected byte-identical raster + provenance, exact physical
and layout measurements, accepted-only runtime source, complete interaction and
learning route, all validation gates, no rejected/scratch residue, no protected
access, Intelligence release, inventory advancement of Host 07 only, and
`HEAD == origin/main`.

If no candidate passes, return HOLD with all five consumed/rejected/clean and
no product. Transport/auth/quota/tool failure stops at its active ordinal and
returns for adjudication rather than spending another.

## Rollback, protected state, and variance

Rollback touches only exact Host 07 additions and shell-owned scratch. Accepted
Hosts 01-06/media are immutable. Never enumerate/access protected QA, PDF,
training, Martin browser/profile/save, hidden lore, opaque residuals, prior
attempt roots, or VR-65. Every OPEN record and both filename-enumeration records
remain separate.

Route product/canon/order/science/shell/direction/interaction/code/content/
presentation/release variances to their earliest named owner. Any sixth-call,
fallback, secret, protected, cleanup, identity, or ledger ambiguity is HOLD.

## Exact Recon handoff

Use one fresh Reconnaissance Sergeant context. Read its full profile, this
shell, all current planning artifacts, exact Host 06 release, game on paper,
accepted visual controls, and only needed runtime sources. Issue one
`DIRECTORIAL LOCK`, `REVISE`, or `HOLD` with five complete prompt texts/files
and synchronized NEXT. Do not generate or inspect media.

Mission signs **`FIRST RUN SHELL READY / FRSH-007-v1 / HOST 07 CLI /
FIVE TOTAL ATTEMPTS / ROUTE FRESH RECON`**.
