# First Run Shell - Sixfold Weir

Shell ID: `FRSH-003-v1`

Disposition: **`FIRST RUN SHELL READY / FRSH-003-v1`**

Stage / owner: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-003-v1` / `FRVE-003-v1`

Planning authorities: `FRPB-001-v1`, `FRPB-001-v1-GR-01`,
`FRPB-001-v1-GR-02`, `FRCL-003-v1`, `FRRM-003-v1`, `FRSB-003-v1`

Mission source inspected: `810c9558072bcbe47126ad1066f9dd62408227b9`

Released First Run predecessor: `FRAB-002-v1 / FIRST RUN PASS RELEASED`

Accepted product/test baseline:
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`

Budget authority: `PBA-TD012-v1`

Date: **2026-08-10**

## Disposition and felt improvement

The complete planning stack is compatible and the bounded pass is ready for
production. After mastering the grounded Host 04 Workload Sort relationship,
the player returns to the unchanged basin and notices six unequal foreground
branch relations crossing two dry mineral lips. The Pilot names the repeated
relationship `Sixfold Weir`; the suit confirms one separate compatible local
surface. Deliberate `USE` opens the already-existing `L02-02` responsible-AI
work through that relationship. Miss, remediation, safe exit, reload,
transfer, explanation, mastery, return, and continuation change only the
expedition's existing private-free evidence.

This shell adds one distinct Host 05 expression and one physical handoff. It
adds no lesson, exercise, score, route, save field, checkpoint, world response,
media asset, access, reward, identity, authority, ending, or successor.

## Planning reconciliation and immutable-plate decision

Mission freezes the Science result without reinterpretation:

1. The accepted Drowned master remains exact `1672 x 941`, `2,727,857` bytes,
   SHA-256
   `AFA0008E3F1E0CDACB2B9E58F14E9F676729EAB9E8725A58C87D73AC489C08ED`.
2. Accepted provenance authors intake, separation, distribution, return flow,
   maintenance bypasses, submerged counterparts, process structures at several
   heights, and grounded local coupling relationships. The selected region is
   therefore an existing foreground process relationship, not a new object
   fabricated by interface copy.
3. Host 05 geometry is exact and identical at canonical and narrow layouts:

   ```text
   left: "45%"
   top: "75%"
   width: "20%"
   height: "25%"
   narrow: identical
   ```

4. The source rectangle is `x 752.4-1086.8 / y 705.75-941`; its center is
   exact `55% / 87.5%` or `x 919.6 / y 823.375`. The center must remain on the
   dry foreground six-branch relationship.
5. Host 05 is horizontally disjoint from canonical Host 04 by `10%` /
   `167.2` source pixels and from narrow Host 04 by `1%` / `16.72` source
   pixels. It is disjoint from both Meadow-return rectangles and vertically
   disjoint from the upper-field Tidal Lens. Pairwise target intersection area
   is exact zero.
6. Centered `16/9` `object-fit: cover` retains full source width and removes
   only `0.5` source pixel vertically. Host 05 source-band retention is exact
   `0.998299681190223` at desktop/laptop/effective-`200%` and
   `0.997874601487779` at narrow and retained `320` fixtures.
7. Expected rendered target sizes are `384 x 270`,
   `273.2 x 192.09375`, `78 x 54.84375`, `153.6 x 108`, and at least
   `64 x 45` in both retained `320` fixtures. Production must remeasure the
   actual DOM and source mapping; arithmetic alone is not release evidence.
8. The broad scene alternative text remains a neutral landscape description.
   Only the semantic active control may own Host 05 action/state language.

Any shift off the dry center, use of a submerged counterpart as one of the six
reachable branches, positive overlap, target below `44 x 44`, retention below
`0.95`, lost center, clipped label, horizontal escape, or naming of the Tidal
Lens as Host 05 is a fail-closed `HOLD`. No raster, vector, crop export,
replacement, variation, import, or overlay workaround is authorized.

## Address, maturity, entry, completion, and hard stop

- **Address:** `FR-03 / Chapter II - Drowned Archive / Host 05` only.
- **Current `FR-03`:** continuity `FR2`; physical-host expression `FR0 - 1
  accepted compression / 11 missing`; learning `FR2`; behavior/save/recovery
  `FR1`; final-purpose content `FR2`; presentation `FR3`; prior bounded
  release proof `FR4` applies only to accepted predecessor packages.
- **Target evidence:** one exact distinct Host 05 relationship and one
  final-purpose Host 04-to-05 handoff, leaving physical-host expression at
  `FR0 - 1 accepted compression / 1 exact / 10 missing`. Existing continuity,
  learning, behavior/save/recovery, content, and presentation remain at least
  exact to current levels. Intelligence alone may update maturity or release.
- **Clean entry:** unchanged package/opening/Meadow reaches the released
  Drowned arrival, Host 04, and write-free Meadow return.
- **Changed active boundary:** begins only after sanitized exact Host 04 /
  `L02-01` mastery closes its Terminal and makes Host 05 actionable.
- **Completion:** Host 05 has one honest target, final state/copy slots, sole
  campaign `L02-02` entry, complete recovery, and unchanged next continuation.
- **Permitted exits:** Terminal close, Escape, actual-miss remediation, primary
  acknowledgement, reload/resume, write-free Meadow return/redeparture,
  completed read-only revisit, and the current forward continuation.
- **Hard stop:** the unchanged next Drowned learning boundary after exact
  `L02-02` mastery. No Host 06 expression or parity, later Drowned change,
  Crown turn, Witness change, City repair, later host, or ending content is in
  scope.
- **Pacing:** one short observation/approach between existing learning
  clusters; zero new scored work and no changed `L02-02` duration.

## Fixed Host 05 state contract

Register one distinct `sixfold-weir` secondary hotspot after Host 04 in source
and semantic order. Its geometry is the exact `45/75/20/25` rectangle above.

Availability is a pure, sanitized, nonpersisted view derivation:

```text
sanitizeWorkloadEvidence(...).masteryStatus != mastered
  -> hidden interface
workload mastered + no valid responsibleAIEvidence
  -> available
workload mastered + responsibleAIEvidence in_progress,
  primary_complete, or transfer_complete
  -> in_progress
workload mastered + responsibleAIEvidence remediation_required
  -> remediation_required
workload mastered + responsibleAIEvidence mastered
  -> complete
```

`hidden interface` means the six foreground relations remain visible in the
immutable plate, but there is no Host 05 DOM node, focus stop, stable label,
status, announcement, action, or credit. Forged IDs, unknown forms or statuses,
malformed correctness, noncontiguous evidence, or a forged Host 04 prerequisite
fail closed through the existing sanitizers.

No `sixfoldWeir`, detection, focus, dialogue, hotspot state, session object, or
active-host key may enter persistence. Existing `completed`, `pendingSceneId`,
Drowned completion, `workloadEvidence`, `responsibleAIEvidence`, and every
later evidence field retain their current schema and meaning.

Exact Host 04 mastery may mount one polite Suit/System detection only after the
Host 04 Terminal unmounts. It creates no evidence, claims no material change,
and cannot repeat on render, return, or reload.

## Sole in-world `L02-02` entry

- Remove the generic post-Host-04 `Start/Resume Responsible AI` launcher.
- Exactly one campaign activation remains: **`USE Sixfold Weir`** after
  sanitized Host 04 mastery.
- `LOOK AT` observes the dry physical relationship. `TALK TO` records silence.
  Neither opens a Terminal, writes evidence, changes focus ownership beyond
  its lawful return, or alters route/world state.
- `USE` opens or reconstructs the existing `EX-L0202-RESPONSIBLE-AI` session.
  Pointer, touch, Enter, Space, and switch-like semantic activation dispatch
  the same nonduplicated intent once.
- Completed `USE` is read-only. It may report finalized existing local evidence
  but cannot open fresh scored work, add an attempt/hint/check, request new
  confidence, change explanation state, downgrade mastery, or change save,
  route, world, readiness, or ending state.
- Demo Tour remains separate and zero-credit. Discovery, focus, observation,
  dialogue, or connection grants no mastery or readiness evidence.

## Immutable learning, evidence, privacy, and save firewall

The complete existing `L02-02` contract remains byte- and meaning-fixed:

- six principles; six primary and six genuinely fresh transfer scenarios;
- four independently checked dimensions per scenario and strict `24/24` per
  form;
- all-principles coverage, misconception routing, progressive answer-free
  remediation, closed-note explanation, confidence, and human ownership;
- objective `AI901-D1-O1` and current exercise/activity/assessment/skill
  owners; and
- `responsibleAIEvidence` as the sole durable `L02-02` record.

Curriculum, scenario bank, answer key, evaluator, remediation, thresholds,
sanitizer, objective mapping, and evidence meaning are patch-forbidden. No new
AI-901 factual claim is authored by this shell.

Durable evidence may retain only the existing sanitizer output: known IDs,
form, per-scenario dimension booleans, bounded attempt/hint telemetry,
confidence, allowlisted misconception tags, and mastery status. Choices,
scenario responses, explanation text, free-form reasoning, feedback, working
index, private controls, open hint, dialogue, focus, detection, content
sentinels, timing, modality, credentials, endpoints, payloads, and runtime
state never enter the save.

Same-session close/reopen may retain the current in-memory session. Full reload,
return, or redeparture mounts no active session; a clean lawful boundary is
created only after deliberate `USE`, from sanitized existing evidence. Save
schema/version, atomic checkpoint behavior, `completed`, `pendingSceneId`,
route order, return, recovery, and all later evidence remain unchanged.

## Fixed route, world, offline, and ending boundaries

- Host 04 remains the one accepted shared Tide-Sort compression and sole
  `L02-01` entry.
- After exact `L02-02` mastery, the existing `L02-03` continuation remains the
  next lawful action. It gains no Host 06 name, hotspot, affordance, or parity.
- Drowned-to-Meadow navigation and redeparture remain write-free and cannot
  mint, erase, replay, or cross-credit Host 04 or Host 05 evidence.
- Hosts 06-15 and the full Crown approach remain grouped deferrals. Witness,
  City, hosts 17-38, and the Measured Horizon literary close remain untouched.
- The City `successor packet` / `staging boundary` contradiction remains open
  and is not repaired or softened by this pass.
- Normal play remains local/offline. Add no dependency, lockfile change,
  network request, service worker, source map, account, sign-in, telemetry,
  credential, endpoint, SDK/model/agent call, Python/WASM runtime, upload,
  clipboard, download, external storage, or external action.
- READY and NOT YET READY retain equal dignity and the same horizon.
  `cityStateDelta=null`, `worldStateDelta=null`, `externalStateDelta=null`,
  `authorityDelta=null`, and `successor=null` remain exact. There is no RP-013
  or post-ending content.

## Content meanings and voice ownership

Quartermaster owns exact final prose. The shell freezes only these meanings:

| State | Required meaning | Owner |
| --- | --- | --- |
| unseen as interface | Six unequal foreground branch relations already divide the flow; no active interface or invitation is claimed | Scene |
| available | Pilot notices the repeated relation; Suit confirms one separate compatible local surface | Pilot, then Suit/System |
| in progress | Human expedition work is unfinished and may resume privately; water and branch geometry remain unchanged | System plus Scene fact |
| missed | Only the actual responsible-AI dimension is unresolved; help is bounded and answer-free | 901 Teacher/System |
| mastered | Existing `L02-02` evidence is finalized; branches, basin, Crown, and route do not respond | System plus Scene fact |
| returned | The same physical relation remains; sanitized evidence reconstructs only the lawful boundary | Scene plus Suit/System |
| next boundary | Pilot chooses the pre-existing material trail; the retained continuation claims no Host 06 parity | Pilot / Scene / System boundary |

Machine and Builders own no line. Final copy may not call the structure a
native six-principle diagram, invitation, expected visitor, lesson apparatus,
healed route, unlocked path, earned access, approval, reward, identity,
authority, mission, response, memory, recognition, or prior-human trace.

## Focus, semantic order, announcements, and recovery

Actual source/tab order is Host 04, Host 05 when eligible, the existing
secondary Meadow return only where currently ordered, adventure controls, and
the unchanged continuation. CSS visual order is not proof.

| Event | Required focus / recovery owner |
| --- | --- |
| Host 04 mastery / Host 05 detection | Host 05 hotspot after Host 04 Terminal unmount; one polite Suit announcement |
| `LOOK AT` / `TALK TO` | Host 05 remains focused; the existing live region reports owner-correct meaning |
| `USE` | Existing responsible-AI Terminal title receives initial focus; background is inert |
| Primary miss / remediation | Actual failed field/status and existing ordered controls; no scene focus jump |
| Primary acknowledgement | Terminal closes; Host 05 restores focus; next `USE` reconstructs fresh transfer |
| Transfer to explanation | Existing in-dialog order; no close or focus escape |
| Close / Escape | Connected enabled Host 05 trigger; if sanitation hides it, Host 04; otherwise the scene's first lawful fallback |
| Mastery acknowledgement | Host 05 trigger, then unchanged next continuation is the next lawful action |
| Reload unfinished | Host 05 receives scene recovery focus; clean session mounts only after `USE` |
| Reload mastered | Existing next continuation receives recovery focus; completed Host 05 remains discoverable/read-only |
| Meadow return / redepart unfinished | Navigation writes nothing; Drowned re-entry focuses Host 05 |
| Meadow return / redepart mastered | Navigation writes nothing; existing next continuation receives focus |
| Malformed evidence | Earliest safe sanitized Drowned boundary; no Host 05, skip, replay, or credit |

Focus is zero evidence. Hidden/disabled surfaces receive no focus. Repeated-key,
pointer/keyboard crossover, or simultaneous activation cannot dispatch twice.
Modal background remains inert; close and Escape restore focus exactly once.

## Responsive and visual-access contract

Prove the exact affected states at:

- desktop `1920 x 1080`;
- laptop `1366 x 768` with `1366 x 768.375` scene;
- narrow `390 x 844` with `390 x 219.375` scene;
- effective `200%` `768 x 900` with `768 x 432` scene;
- retained `320 x 180`; and
- retained `320 x 240` viewport with `320 x 180` scene.

At all six layouts, actual measurements must prove exact Host 05 source bounds,
contained center, correct canonical/narrow registration, source-band retention
at or above `0.95`, exact zero Host04/return overlap, target at least
`44 x 44`, contained accessible label/status, and no horizontal overflow.

Keyboard, pointer, touch, and switch-like semantic activation preserve the
same action. Longest final copy reflows without clipping, target occlusion,
required page-level horizontal scroll, or lost hierarchy. State and focus are
not color-only. Forced colors retain visible `:focus-visible`, target identity,
and state. Reduced motion removes nonessential animation/transition without
changing order or meaning. Sound, hover, color, motion, timing, and scenic
recognition are never the sole carrier of meaning. Automated evidence is not
human assistive-technology certification.

## Presentation and immutable-media contract

Recon may define attention, pacing, distance, silence, and the felt turn across
the existing Drowned master. Tactical freezes the state/action/focus/layout
blueprint. Combat implements authorized code and tests. Quartermaster fills the
owned final-purpose copy slots. Image Specialist may review or adjust only
code/configuration-level composition, crop use, existing sound, lighting/color
treatment, motion, typography, hierarchy, spacing, focus, forced-color, and
reduced-motion behavior.

No role may generate, edit, replace, vary, crop-export, import, move, remove,
publish, or reveal an image or other media asset; alter media bytes; add a
runtime media request; or treat concept art as integrated evidence. Accepted
runtime media remains exact `17 files / 37,410,731 bytes`, with zero new,
changed, moved, or removed files.

## Performance budgets and fail-closed caps

Released predecessor evidence is JavaScript `1,663,664`, CSS `118,981`, and
`216` production modules. Both the narrower pass cap and global PBA must pass:

| Measure | `FRWO-003` pass cap | Global hard cap |
| --- | ---: | ---: |
| aggregate JavaScript | `<=1,675,664` | `<=1,703,258` |
| aggregate CSS | `<=119,281` | `<=119,672` |
| production modules | `<=217` | `<=222` |
| accepted runtime media | exact `17 / 37,410,731` | exact same |
| new runtime media | exact `0 / 0` | exact same |
| focused / related / cold full | `<=30s / <=60s / <=60s` | same |
| production / fixture build | `<=60s` each | same |
| complete E2E | `<=180s` | same |
| sampled Host 05 activation | `<=2ms` | total sampled task `<=100ms` |

Prefer existing hotspot, state, focus, modal, forced-color, reduced-motion, and
containment styles. Any local or global overage is `HOLD`; no quality waiver,
budget borrowing, image workaround, or accessibility reduction is permitted.

## Exact permitted files and systems

Product/test changes are limited to:

- `horizon-archive-game/src/App.jsx` for bounded Host 05 state/action/focus
  integration and removal of the generic launcher;
- optional new `horizon-archive-game/src/drownedArchive.js` for exact immutable
  geometry, pure sanitized view-state derivation, and copy slots only;
- `horizon-archive-game/src/sceneTransition.js` only for exact Drowned/Meadow
  focus-return presentation, with no navigation or save meaning change;
- `horizon-archive-game/src/styles.css` only when existing rules cannot satisfy
  target/focus/containment and only under the narrow cap;
- existing focused/related tests for Workload Sort, responsible AI, restore,
  return, focus, scene transition, Demo Tour, world-plate integration, and
  later-rail/ending invariants;
- optional new `horizon-archive-game/test/sixfoldWeir.test.js`;
- the single existing `playtest/e2e-playthrough.mjs`;
- new
  `Production Pipeline/First Run/FIRST_RUN_RELEASE_COMMAND_MANIFEST_FRRC-002-v1.json`;
- the current role's exact First Run report and `NEXT_INSTANCE_HANDOFF.md`;
  Intelligence may also update the release map, scoreboard, and changelog.

`responsibleAIExercise.js` is regression-only. Curriculum, scenario/answer
assets, evaluator, remediation, sanitizer, save schema/version, dependencies,
lockfiles, canonical story/lore, accepted media, Host 06-15 content/controllers,
Witness, City, later rail, Measured Horizon product code, real storage, hidden
lore, and every path not listed above are patch-forbidden. A downstream role
must request a variance before touching any additional file.

The pre-existing untracked repository
`Production Pipeline/First Run/QA/` quarantine is protected. No role may name
it as an output, enumerate or inspect it, use it as evidence, alter it, stage
it, move it, or delete it. The protected PDF and training directory remain
equally outside scope.

## Role freedoms and ownership

- **Recon:** define one compelling physical attention path and emotional turn
  over the existing plate; no final prose, exact interaction graph, code,
  media operation, or reveal.
- **Tactical:** freeze semantic order, pure states, action graph, focus,
  announcements, responsive/source mapping, recovery, copy slots, and live
  measurement contract; no final prose or implementation.
- **Combat:** implement exact host/state/action/focus wiring, remove the generic
  launcher, create tests/E2E/manifest, and preserve explicit content slots.
- **Quartermaster:** write final owner-correct copy and accessible labels inside
  the frozen slots; change no behavior, learning, route, save, world, or media.
- **Image Specialist:** review and only if necessary adjust permitted runtime
  code/configuration presentation. A substantiated no-change pass is valid.
- **Intelligence:** begin from a fresh posture, independently run or reproduce
  the manifest ladder, reconcile the immutable candidate, advance maturity
  only from accepted as-built evidence, and release or return without repair.

Each owner writes one dedicated report/commit. Combat and Intelligence are
required push gates after this Mission push.

## `FRRC-002-v1` release-command manifest contract

`FRRC-001-v1` remains immutable historical evidence for `FRWO-002-v1`.
Combat must create a new checked-in `FRRC-002-v1` JSON manifest for this Work
Order. Every command entry must state a versioned schema, `id`, `workdir`,
argument-array `command`, timeout, expected exit, owner, prerequisites,
output/port ownership, and exact cleanup.

The manifest must include nonempty entries for:

1. `focused`: explicit Host 04/05 geometry, state, action, focus, responsible-AI,
   restore, return, privacy, and world-plate tests.
2. `related`: explicit opening/Meadow/Drowned/Witness/City/later-rail/ending,
   Demo Tour, save, and accessibility regressions.
3. `full`: cold `node --test` from `horizon-archive-game`.
4. `validators`: exact sorted repository inventory of exactly `40`
   `curriculum/**/validate*.py` paths, every invocation with `--self-test`.
5. `production-build`: exact `npm run build`.
6. `fixture-build`: exact `npm run build:td012-fixture`.
7. `production-preview`: strict owned PID and `127.0.0.1:4173`.
8. `fixture-preview`: exact TD-012 fixture Vite configuration, strict owned PID
   and `127.0.0.1:4184`; generic preview defaults are forbidden.
9. `served-identity`: root/deep/JS/CSS HTTP success and served-to-disk byte
   identity for both previews.
10. `complete-e2e`: exactly one invocation of the existing production journey,
    using the exact external QA root contract below.
11. `live-summary-verify`: one machine verifier for the E2E-produced summary.
12. `pba-media`: every emitted JS/CSS chunk, exact module count, narrow/global
    caps, exact media hash inventory, forbidden asset/request scan, and task
    sample.
13. `cleanup-identity`: exact owned PID/port/temp cleanup, diff/ancestry checks,
    and immutable candidate identity.

### Fresh external QA root

The execution coordinator must resolve the operating system temporary parent,
then create one new GUID-suffixed child directory under that parent. The child
must not exist before creation. Before every write or delete, resolve both
paths and prove all of the following:

- the child is outside the repository root;
- the child is strictly inside the resolved OS temporary parent;
- the child is the exact root created by this manifest execution; and
- neither path is the repository QA quarantine or any protected/user path.

Pass the exact absolute child path to the sole E2E as
`HORIZON_ARCHIVE_QA_DIR`. After evidence capture, repeat the containment proof
and delete only that exact owned child. Broad globbing, recursive parent
cleanup, repository QA access, real browser/profile/save access, or cleanup of
an unresolved path is forbidden. A blocked exact cleanup is reported and is a
release disposition question; it never licenses a broader delete.

### Machine-owned live summary

The sole E2E must emit `first-run-live-summary.json` inside its owned external
QA root. The runner resolves and freezes the exact candidate once before
preview start, passes that same identity to E2E and verifier, and rejects any
candidate drift. The summary is evidence only, never durable campaign state or
a reveal.

The machine verifier must fail unless the JSON includes:

- schema, `FRWO-003-v1`, `FRSH-003-v1`, exact frozen candidate, and
  `runtimeErrors=false`;
- exactly six layout IDs: desktop, laptop, narrow, effective-`200%`, retained
  `320x180`, and retained `320x240`;
- measured viewport/scene dimensions for every layout;
- per-layout Host 05 DOM rectangle, source bounds, center containment,
  source-band retention, Host04/return overlap areas, target-size result,
  label containment, and horizontal-overflow result;
- focus order/restoration results for detection, LOOK, TALK, USE, close,
  Escape, miss, primary acknowledgement, mastery, reload, return, and next
  continuation;
- forced-color and reduced-motion computed-style results;
- both narrow and global PBA results, media identity, and runtime-request
  result; and
- one fail-closed aggregate `pass` computed by the run.

The verifier prints one concise JSON result and rejects missing or extra layout
IDs, missing keys, nonfinite measurements, candidate mismatch, any false
required result, runtime errors, or a handwritten/non-E2E summary.

## Executable validation ladder

Every gate records the manifest entry, exact candidate, elapsed time, result,
and honest unavailable evidence.

1. **Integrity:** exact ancestry and bounded diff; permitted paths; protected
   boundaries; game-on-paper and Drowned-master hashes; full accepted media
   inventory; forbidden-token and forbidden-change scans.
2. **Focused (`<=30s`):** exact `45/75/20/25`, source bounds/center, both
   retention values, all six target sizes, `>=44`, zero overlap, pre-mastery
   DOM absence, pure state derivation, no new save key, LOOK/TALK/USE, sole
   `L02-02` entry, generic-launcher absence, read-only completed `USE`, modal
   inertness, announcements, and all focus fallbacks.
3. **Related (`<=60s`):** exact responsible-AI primary miss, progressive
   remediation, fresh transfer, explanation, ownership, confidence, mastery,
   sanitizer, close/Escape, clean restore; write-free Meadow return/redeparture;
   unchanged `L02-03`, later Drowned sequence, Tour, opening, Witness, City,
   later rail, and ending.
4. **Learning/privacy (`<=60s`):** cold full product suite; exactly `40` sorted
   validator `--self-test` invocations; complete save projection enumeration;
   private/transient sentinel rejection; no cross-credit or new objective claim.
5. **Build/budget/identity:** clean production and exact TD-012 fixture builds;
   narrow/global PBA; immutable media; offline/runtime-request scan; HTTP
   preflight; root/deep/JS/CSS served-to-disk identity.
6. **One E2E (`<=180s`):** one and only one non-overlapping production journey
   from clean start to MH-40. It proves Host 04, deliberate Host 05, a real
   `L02-02` miss, remediation, safe exit, reload, fresh transfer, explanation,
   mastery, completed read-only `USE`, unchanged next Drowned boundary and
   later rail, both equal MH-40 outcomes, null deltas, `successor=null`, and
   zero runtime errors. A second whole-game journey is forbidden evidence.
7. **Machine live review:** verify the exact E2E summary for all six layouts,
   input modes, focus, target/label containment, non-color state, forced color,
   reduced motion, and no overflow. No manually authored substitute passes.
8. **Cleanup:** stop only recorded owned PIDs, prove ports `4173` and `4184`
   clear, remove only the exact owned external GUID root after repeated
   containment checks, preserve repository QA/protected state, and report any
   exact cleanup limitation.
9. **Identity:** run `git diff --check`; freeze candidate before later control
   commits; prove dedicated stage commits and `HEAD == origin/main` at Mission,
   Combat, and Intelligence push gates.

## Definition of done

One provenance-honest, dry, nonoverlapping Sixfold Weir exists at exact
`45/75/20/25`; no interface exists before sanitized Host 04 mastery; pure state
and sole in-world `USE` rehouse unchanged `L02-02`; the generic launcher is
gone; LOOK/TALK/USE and every focus/recovery path preserve their owner; final
copy lands the repeated-relationship discovery without response; no new
persistence exists; later Drowned order, route, world, media, offline behavior,
and ending remain exact; all focused/related/full/validator/build/PBA/served/
single-E2E/live-summary/cleanup/Git gates pass; and Intelligence accepts only
the exact immutable candidate.

String presence, an invisible duplicate control, renamed generic launcher,
arithmetic without live source mapping, manually written live JSON, or test
depth without player-visible physical expression is insufficient.

## Rollback, hard stops, and variance routing

Rollback baseline is exact released product/test candidate
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`. The bounded rollback removes only
the Host 05 product/test delta, the FRWO-003 E2E delta, new `FRRC-002-v1`, and
FRWO-003 downstream reports; restores shell-permitted product/test paths to
their exact baseline blobs; and deletes only files first introduced by this
bounded patch. It does not reset the repository, rewrite prior First Run
history, modify `FRRC-001-v1`, touch any protected/untracked path, or require a
save migration.

Record every mismatch as `ACCEPTED IMPROVEMENT`, `REQUIRED CORRECTION`,
`MASTERPLAN UPDATE`, `DEFERRED LIMITATION`, or `UNAUTHORIZED DIVERGENCE` and
return it to the earliest owner. No downstream role silently changes the shell.

Return canon/voice conflict to Colonel; address/order/scope expansion to
Operations; learning/evidence/privacy/save/accessibility/performance/offline/
manifest ambiguity to Science through Mission; cross-discipline ambiguity to
Mission; and release classification to Intelligence.

Immediate `HOLD` applies to unsupported six-branch identity, lost dry center,
crop/overlap/target failure, duplicate `L02-02` entry, evaluator/sanitizer/
learning change, private persistence, save-schema change, Host 06 promotion,
route/world/ending change, dependency/network use, any narrow/global overage,
external-root/live-summary failure, inability to run one isolated E2E, hidden
lore, real browser/profile/save access, protected-path conflict, image/media/
reveal work, new lesson/branch/reward/access/identity/authority/response,
successor, RP-013, post-ending content, or destructive/broad cleanup.

## Mission signature and exact Recon handoff

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-003-v1`** from source
`810c9558072bcbe47126ad1066f9dd62408227b9`.

### Change history and shell-gate proof

- `FRSH-003-v1` is the initial shell for `FRWO-003-v1`; it does not revise
  `FRSH-001-v1` or `FRSH-002-v1`, which remain historical authorities for their
  accepted Work Orders.
- Mission changed no product, test, learning, story, save, media, map,
  scoreboard, maturity, or automation file.
- The dedicated Mission commit introduces only this shell and the synchronized
  Recon handoff. Mission pushes `main` and reports the resolved commit plus
  equality of `git rev-parse HEAD` and `git rev-parse origin/main`; the hash
  cannot be embedded in the file that first creates it.

Reconnaissance Sergeant must read this shell, `FRWO-003-v1`, `FRVE-003-v1`,
the synchronized handoff, current Drowned runtime/canon/provenance, and exact
Host 04-to-05 source state. Issue exactly one Directorial Treatment for
`FRSH-003-v1` defining the physical attention path, felt discovery, basin
reorientation, approach rhythm, silence, and existing-media concept. Do not
write final prose, freeze the exact interaction/focus implementation, edit
product/test/media, generate or reveal an image, alter learning/route/save/
world/ending, advance maturity, or begin Tactical.
