# First Run Shell Variance - Host 08 Corrected E2E Arrival Timing

Variance ID: `FRSH-008-v1-VR-02`

Disposition: **`FIRST RUN SHELL READY / ONE CORRECTED E2E`**

Mission accepts `FRAB-008-v1-HOLD-02`. Combat may remove exactly the obsolete
post-Control-Flow Teacher-dialogue wait from `playtest/e2e-playthrough.mjs`.
The harness must still wait for the native Host 08 target/focus, prove its
available state, activate unchanged L03-03 through USE, prove read-only Host 08
completion, then continue the unchanged rail.

After syntax/focused/full/build PASS, one fresh Intelligence Officer may run
exactly one corrected E2E in a new explicit external QA root. No further retry
is authorized without another Mission variance. Product, media, runtime, copy,
lesson, state, save, canon, presentation, and protected QA remain immutable.
The prior external root is absent and port 5174 is clear.

Mission signs **`FIRST RUN SHELL READY / ROUTE COMBAT THEN INTELLIGENCE`**.
