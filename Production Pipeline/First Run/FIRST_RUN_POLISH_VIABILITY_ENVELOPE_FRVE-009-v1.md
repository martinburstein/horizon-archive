# Horizon Archive First Run Polish Viability Envelope - Host 09

Envelope ID: `FRVE-009-v1`

Stage / stable agent: Office of Science Administrator / `office_of_science_administrator`

Work Order: `FRWO-009-v1`

Disposition: **`POLISH VIABILITY READY / HOST 09 / FIVE CLI ATTEMPTS AVAILABLE`**

Date: **2026-08-12**

Exact next owner: **one fresh Mission Captain / `mission_captain`**

## Tool and custody decision

Science accepts the bounded CLI plan. The installed generator is exact:
`image_gen.py`, `35,266` bytes, SHA-256
`c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05`.
The host has Python `3.12.10`, OpenAI SDK `3.0.0`, and a nonempty API-key
boolean. An exact `gpt-image-2`, `n=1`, `3840x2160`, high-quality opaque-PNG,
no-augmentation dry run exited `0` and created no output. Science made no live
request, read no credential value, generated no image, and reviewed no media.

The attempt domain is exactly `{H9-1..H9-5}`. Calls are sequential and
independent with no reference/input, edit, variation, batch, alternate runner,
script mutation, or sixth call. A technical failure consumes and stops. An
objective rejection enables the next ordinal only after exact deletion and
absence proof. First PASS ends all generation.

Mission must freeze a new absent GUID child of the OS temp directory and five
literal children `h9-1.png` through `h9-5.png`. Each candidate is one ordinary,
non-reparse, one-link file at its predeclared path, identity-bound by canonical
path, volume/file ID, length, and lowercase SHA-256 before and after private
review. The selected copy uses create-new/no-replace byte identity. Rejected
candidates have zero canon/reuse/reveal authority. Final scratch cleanup is
exact-file deletion followed by nonrecursive empty-root deletion.

The OneDrive product parent may contain Cloud Files reparse components. Mission
must reuse the previously proven Cloud-safe exact literal path adapter:
canonical FinalPath, volume/FileId identity, Cloud tag/state completeness,
one-link ordinary leaf semantics, create-new/no-replace selected copy, flush,
byte/SHA re-observation, and exact accepted-only product identity. It may not
enumerate a parent, repair/hydrate accepted media, or weaken fail-closed
cleanup.

## Candidate and geometry envelope

A lawful candidate is one static `3840x2160` PNG with valid signature/chunks/
CRC/IHDR, RGB or RGBA, fully opaque, no external reference, and
`1..30,000,000` bytes. Accepted inventory is at most `20 / 100,136,520` bytes.

Private review records normalized source rectangles for:

- complete cavity family and at least three individually distinct openings;
- incoming pressure-bearing continuity from the predecessor material family;
- at least two ambient reflected-light openings;
- one pressure-related cavity with bounded no-optical-return;
- continuous dry annular rim and wet exclusion;
- distributed coupling, semantic target, label, and protected regions.

The complete family stays within `x=.24..76`, `y=.20..78`; incoming continuity
center within `x=.30..46`, `y=.48..68`; dry rim within `y=.66..88`; target
center within `x=.46..58`, `y=.43..61`. At least `95%` family retention, all
three cavity centers, reflection pair, nonreturn cavity, incoming continuity,
and dry rim survive all six centered-cover layouts. Target is `>=44x44`,
contains the distributed coupling center, and overlaps neither water/cavity
voids nor protected predecessor/return/Crown regions. Label remains contained
with `>=8px` separation. Any mouth/face/performer/voice/interface cue,
submerged access, missing relation, crop ambiguity, or lesson-answer cue is an
objective rejection.

## State, lessons, privacy, and accessibility

Combat may build only a null-first fail-closed registry and UI state. Exact
released Host 08 plus strict `L03-03` mastery is the sole predecessor. The
current generic Text Analysis and Speech Workloads launchers remain only before
Host 09 is ever enabled. Accepted enablement removes them irreversibly; later
source/provenance/decode/measurement failure hides Host 09 and never restores a
bypass.

States are hidden, available, in progress, remediation required, and complete,
derived from sanitized strict `L04-01` and `L04-02` evidence. USE launches Text
Analysis until mastered, then Speech Workloads until mastered, then becomes
read-only. LOOK writes nothing; TALK is silent. The source, focus, selection,
sound treatment, and presentation mint no evidence, audio, transcript,
credential, telemetry, save field, reward, identity, authority, access, answer,
or world response. Existing answer-free remediation, confidence, evidence
sanitization, save/reload recovery, Demo Tour isolation, and privacy remain.

Pointer, keyboard, touch, native activation, focus return, Escape/close,
announcement, `390px`, effective-200%, forced-color, reduced-motion, and
global `44px` targets converge on the same action. The no-optical-return case
must be conveyed visually and in factual alt/copy; essential meaning may not
depend on audio.

Focused predecessor/Text/Speech tests pass `19/19` in `109.5058ms`.

## Performance and validation

Final ceilings: affected/global JS `<=1,705,500 / 1,730,000`, CSS
`<=119,950`, modules `<=223`, maps `0`, no dependency/lockfile change; decode
`<=300ms` cold / `<=120ms` warm, handler `<=2ms`, attributable/whole CLS
`<=.01/.05`, complete E2E `<180s`.

The ladder is authority/prompt/tool/key-boolean/path/ledger preflight; focused
Host08/Host09/Text/Speech tests; related route/save/privacy/accessibility/
ending regressions; validators/full suite; production/TD-012 builds; minified
PBA and exact media inventory; served root/deep/JS/CSS/media/fixture identity;
six-layout/keyboard/focus/forced-color/reduced-motion review; and one isolated
clean-start external-root E2E through Host 09 and both MH-40 outcomes.

Before the first browser run the shell must enumerate every initial and re-entry
launcher used by the harness and bind each to native Host 09. The external QA
root must be newly created outside the repository, ordinary, non-reparse,
empty, and identity-cleaned afterward. Repository QA is never a target. CSS is
measured after minification before presentation commit.

Rollback removes only exact Host 09 additions and shell scratch. All accepted
Hosts/media, route, lessons, saves, ending, protected state, opaque residuals,
and later gaps remain untouched.

All OPEN records, protected-QA records, filename-enumeration records, and VR-65
remain separate. Science signs **`POLISH VIABILITY READY / FRVE-009-v1 / ROUTE
FRESH MISSION / ZERO ATTEMPTS CONSUMED`**.

