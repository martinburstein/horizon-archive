# Horizon Archive First Run Polish Viability Variance

Variance ID: `FRVE-005-v7-VR-15`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`HOLD / FILELESS READTOEND PARENT SOURCE REACHED THE FIXED
BOOTSTRAP BUT FAILED SCRIPTBLOCK PARSE BEFORE PARENT BODY OR CHILD / REJECTED
UNBOUNDED BOOTSTRAP ERROR / NO RETRY / FRVE-005-v7-VR-15`**

Date: **2026-08-11**

Science source inspected:
`d4ac6006f9a2b09b498d89fe9b1f0843108f78b8`

Mission correction: `FRSH-005-v1-VR-17`

Prior Science return: `FRVE-005-v7-VR-14`

Quartermaster return: `FRCA-005-v4`

Effective shell: `FRSH-005-v1` through `FRSH-005-v1-VR-17`

Effective treatment / blueprint: `FRDT-005-v1-VR-01` /
`FRPX-005-v1-VR-01`

Functional report: `FRCE-005-v1-VR-05`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Mission Captain / `mission_captain`

## Science decision

Science read the complete active intake, workflow, registry, full Science
profile, complete `FRSH-005-v1-VR-17`, complete `FRVE-005-v7-VR-14`,
complete `FRSH-005-v1-VR-16`, complete `FRVE-005-v7-VR-13`, complete
`FRSH-005-v1-VR-15`, and the exact effective classifier, carrier, controlled-
path, Work Order, shell, candidate, manifest, release-map, and scoreboard
controls cited there. Starting `HEAD` was exact
`d4ac6006f9a2b09b498d89fe9b1f0843108f78b8`.

Science compiled one document-independent parent source in memory with only
the two immutable Base64 runtime carriers from `FRSH-005-v1-VR-16`. It was
strictly ASCII, LF-only, final-LF, exact `47,147` characters, and lowercase
ASCII SHA-256:

```text
2426e45f62842f7c5c9c314e280e4c4dc384ffa518eacaf230223a93ec85283e
```

Science started exact Windows PowerShell 5.1 once through the frozen
`FRSH-005-v1-VR-17` transport. The exact fixed argument scalar remained
`151` characters. Standard output and standard error asynchronous drains
started before the one `StandardInput.Write(parentSource)` call; Science then
closed input, waited for the process, and retained both streams and exit.

The bootstrap read the nonempty complete source but
`[scriptblock]::Create($s)` rejected its syntax. The process returned exit
`1`, exact zero-byte stdout, and unbounded PowerShell parse-error formatting
on stderr. The parent body never compiled or began, so its bounded catch and
`SCIENCE_PARENT_STOP` record were unavailable. The parse text is rejected
evidence and is not reproduced here. The first localized syntax class was a
PowerShell generic static method invocation written without required token
separation after unary `-not`; Mission must repair the parent-source grammar
as a whole and re-prove a bounded parse result before a later fresh Science
context can run.

No production child began. This context therefore proves
`childInvocations=0`, not the required one-child PT06 result. Under the exact
one-run contract, Science performed no second parent invocation, no transport
probe, no corrected invocation, and no child retry. Quartermaster,
generation/API, and ordinal `2` remain unauthorized.

Variance classification: **`REQUIRED CORRECTION / SCIENCE PARENT SOURCE
GRAMMAR REJECTED BY FIXED BOOTSTRAP BEFORE BOUNDED PARENT CONTROL BEGAN /
OPEN`**.

## Exact retained activity and absence evidence

The parent body did not begin. It neither read nor changed `OPENAI_API_KEY`.
No child environment, request, endpoint call, response, Base64 response,
media byte, image, or pixel was created or inspected. Exact counters are:

```text
childInvocations=0
credentialValueReads=0
requestConstructions=0
sendAsyncCalls=0
directSends=0
ordinalsConsumed=0
```

Independent exact-path postflight after the sole parent process ended proved:

```text
helperRootAbsent=true
helperDllAbsent=true
liveRootAbsent=true
ordinal2StageAbsent=true
ordinal2TargetAbsent=true
ordinal2DecisionAbsent=true
ordinal3StageAbsent=true
ordinal3TargetAbsent=true
ordinal3DecisionAbsent=true
productRootAbsent=true
productRasterAbsent=true
productProvenanceAbsent=true
scienceFixtureRootsAbsent=true
```

Historical ordinal `1` remains consumed, opaque, inaccessible, and unchanged.
Ordinals `2` and `3` remain unstarted and unconsumed. Direct sends remain `0`.
The temporary outer runner was removed before commit; the parent source never
existed as a file, and no disposable fixture root or file remains.

## Protected state, maturity, and exact Mission handoff

The repository QA quarantine, protected PDF, training tree, Martin's real
browser/profile/save, hidden lore, accepted-media bytes/pixels, OS-temp parent,
ordinal-1 residual, real managed directory, user work, VR-65, and every opaque
residual remained untouched. All inherited OPEN records remain separate and
OPEN. This HOLD closes, cures, merges, waives, renumbers, accesses, or
reclassifies none.

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`.

Science changed no product, test, runtime, lesson, save, media, manifest,
candidate, map, scoreboard, maturity, process classification, residual,
schedule, or automation state. The only repository changes retained for this
stage are this variance and synchronized `NEXT_INSTANCE_HANDOFF.md`. Science
does not push.

One fresh Mission Captain / `mission_captain` reads this complete HOLD,
complete `FRSH-005-v1-VR-17`, complete `FRVE-005-v7-VR-14`, complete
`FRSH-005-v1-VR-16`, complete `FRVE-005-v7-VR-13`, complete
`FRSH-005-v1-VR-15`, and all exact cited controls. Mission issues exactly one
versioned `FIRST RUN SHELL READY`, `REVISE`, or `HOLD` decision that repairs
the parent-source grammar before bootstrap invocation while preserving the
two immutable carriers, fixed 105-character ReadToEnd bootstrap, 151-character
arguments, one-write/close, asynchronous dual-stream capture, unchanged
production child, PT06 semantic mapping, controlled-path checks, and no-retry
rules.

Mission may not reinterpret this parse failure as parent or child evidence;
rerun this Science context; route to Quartermaster; execute generation/API;
allocate the live root; read a credential value; construct/send a request;
call `SendAsync`; consume ordinal `2`; inspect media/pixels; change product/
tests; run E2E; reveal; advance maturity; close an OPEN record; access a
residual or VR-65; schedule; automate; push from Science; release; or call
`FIRST RUN COMPLETE`.

Office of Science Administrator signs **`HOLD / FILELESS PARENT SCRIPTBLOCK
PARSE FAILURE / CHILD INVOCATIONS ZERO / NO RETRY / FRVE-005-v7-VR-15`** from
exact source `d4ac6006f9a2b09b498d89fe9b1f0843108f78b8`.
