# First Run Shell - Host 06 v8 A1

Shell ID: `FRSH-005-v1-VR-34`

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-005-v8` / `FRVE-005-v8`

Disposition: **`FIRST RUN SHELL READY / EXACTLY ONE FRESH A1 OFFICIAL-ENVELOPE IMAGE API ATTEMPT / COMPLETE PRODUCTION CARRIER RETAINED / NO A2 OR RETRY / FRSH-005-v1-VR-34`**

Date: **2026-08-12**

Mission source: `d3a267f140b84592b4deff41d5767114e420ce44`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Planning controls: `FRRM-005-v8` / `FRSB-005-v8`

First-run address: `FR-03 / Chapter II - Drowned Archive / Host 06`

Current / target maturity: physical-host expression remains `FR0 - 1 accepted shared compression / 1 exact / 10 missing`; the bounded target after independent release is `1 accepted shared compression / 2 exact / 9 missing`. Mission advances no maturity.

Exact next owner: one fresh Quartermaster / `quartermaster`

## Mission decision

Mission accepts the complete `FRVE-005-v8` PASS and issues one conflict-free production contract for the sole fresh v8 paid-attempt domain `{A1}`. `A1` begins at its sole authoritative `SendAsync` and is consumed regardless of outcome. There is no `A2`, retry, redirect, alternate request, continuation, fallback, edit, variation, second output, or later attempt under this shell.

The complete executable carrier and launcher are retained in the repository before authorization:

```text
launcher=Production Pipeline/First Run/HOST06_V8_A1_LAUNCHER.ps1
launcher bytes=2,007
launcher SHA-256=1e54a46d1492bb37e59d430885aeba2e2f2b950786744eebfe3e46dda1564f54

carrier=Production Pipeline/First Run/HOST06_V8_A1_PRODUCTION_CARRIER.ps1
carrier bytes=33,638
carrier SHA-256=81e82f1b4f6bd936c4002f969ea6896533673ef7d03b4eef23b7953e7fcbe491

deterministic construction record=Production Pipeline/First Run/BUILD_HOST06_V8_A1_CARRIER.ps1
```

Quartermaster must independently reproduce both byte identities immediately before launch. Any mismatch is terminal `HOLD / PT00_SOURCE_IDENTITY`; it may not repair or reconstruct production source in the live attempt. The builder is provenance and reproducibility evidence only and is not production authority.

Mission performed no credential read, request construction, API send, carrier/launcher/helper execution, temp allocation, media read, image generation, image review, import, reveal, runtime/product change, test/build, maturity update, release, or record closure.

## Player-visible vertical-quality contract

The only eventual player-visible outcome is:

```text
exact Host 05 / Sixfold Weir mastery
-> lens-like fragment handoff
-> one distinct local dry Host 06 / Stranded Lens Cradle
-> sole unchanged L02-03 entry and loop
-> unchanged next Drowned learning boundary
```

The accepted source must show exactly one visibly weathered lens resting inside one tilted conformal cradle on a dry, reachable, above-water continuation of the same Drowned basin. It requires at least two load/stress contacts, at least two continuous drainage seams, one dry approach, Host 05 fragment continuity, and a restrained horizon or reflected-horizon catch. Host 06 must remain distinct from live water, Host 05, a return-like ridge, the Crown, the distant suspended Tidal Lens, and any second lens/cradle candidate. Text, watermark, border, human presence/control, prior-human trace, lesson diagram, answer cue, purpose certainty, hidden-lore answer, invitation, reward, activation, recognition, authority, or world response is forbidden.

Entry remains exact sanitized Host 05 / `L02-02` mastery. LOOK and TALK are write-free. USE enters only the sole unchanged `L02-03`; completed USE remains read-only. Remediation is answer-free. Retry, return, resume, deterministic save/recovery, privacy clearing, offline/no-authority behavior, Demo Tour no-credit, chapter order, and hard stop remain unchanged. The shell ends after exact `L02-03` mastery and authorizes nothing at Host 07 or later.

## Exact request and transport

Quartermaster may execute the retained source once. The request has exactly seven serialized members in this order:

```text
model=gpt-image-2
prompt=HOST06-GEN-PROMPT-v1
n=1
size=3840x2160
quality=high
background=opaque
output_format=png
```

`HOST06-GEN-PROMPT-v1` is the exact ASCII/UTF-8 prompt retained literally in the production carrier: `4,099` bytes, SHA-256 `efd8c7f8b5ca6c0ec9e16ac82cdb008921a480e549b3b0b1b4b767f79e323179`. It begins `Use case: stylized-concept` and ends `watermark; border; crop-dependent essential fact.` No byte or request member may change.

Transport is exactly native Windows PowerShell 5.1/.NET `HttpClient`, one non-streaming `POST https://api.openai.com/v1/images/generations`, environment-only process `OPENAI_API_KEY`, in-memory Bearer header, boolean-only pre-send credential presence, `AllowAutoRedirect=false`, `ResponseHeadersRead`, exact ten-minute timeout, one in-memory body, and at most one `SendAsync`. No SDK, CLI, Python, browser, package install, URL output, managed-output discovery, output hint, image input, alternate endpoint/model/transport, header/body logging, or credential persistence exists.

## Fresh owned paths and identity boundary

The only helper/live attempt paths are literal:

```text
helperRoot=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-v8-5fbbd31e-8b50-4cb4-a0d3-c2f0d4b9e8aa
helperDll=<helperRoot>\Host06FileIdentity.dll
liveRoot=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-v8-3f7d8a21-76c5-4d4e-9641-b2f5e73a019c
A1Stage=<liveRoot>\.attempt-A1-95a70af8-f9d0-49dc-87dc-89675212ed35.stage
A1Target=<liveRoot>\attempt-A1.png
A1Decision=<liveRoot>\.attempt-A1.review-v1
```

All must be absent before launch. Collision, reparse point, path ambiguity, or identity mismatch is terminal. The native helper remains exact `HOST06-FILE-IDENTITY-PSNET-v1`: source `1,693 / 98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97`; current-run DLL `4,096 / 39e85b32b7f8437c2b5732e26093ca5bd9a9182b048c411e9dc5660ba03f10c9`. It exposes only handle-bound read identity, is loaded in memory, and must prove five scalars, one link, no reparse point, exact size, exclusive reobservation, and exact cleanup.

Decoded bytes use `FileMode.CreateNew`, one bounded write, `Flush(true)`, close, exclusive reopen, tuple/hash equality, same-directory atomic no-replace move, and final-target identity proof. Cleanup is identity-conditioned and limited to the exact owned file and empty exact owned root; it may never target a parent, sibling, glob, recursive root, managed directory, v7 path, opaque residual, repository path, or protected path.

## Official response and failure contract

Every completed HTTP response records only numeric status `100..599`, normalized lower-case media type without parameters or `absent`, bounded response byte count or `cap-exceeded`, terminal gate, `sendStarted`, and for non-success only a strict secret-safe diagnostic or `diagnostic-unavailable`. Success requires exact `200` and `application/json`.

The carrier parses one complete strict UTF-8 JSON object, rejects BOM, malformed UTF-8/JSON, trailing content, duplicate keys at every layer, and unknown members. Its exact top allowlist is `created background data output_format quality size usage`; `created` and `data` are required. `created` is a nonnegative integral signed-64-bit JSON number. `data` is exactly one array with exactly one object containing only required canonical RFC 4648 `b64_json`. `url`, `revised_prompt`, second image/payload, unknown member, invalid padding/pad bits, or noncanonical base64 rejects. Complete response is `1..16,500,000` bytes; base64 is `4..16,000,000` characters; decoded payload is `1..12,000,000` bytes and must round-trip canonically.

When present, request echoes must equal `background=opaque`, `output_format=png`, `quality=high`, and `size=3840x2160`. Optional `usage` admits only nonnegative integral `input_tokens`, `output_tokens`, `total_tokens`, and optional detail objects containing only nonnegative integral `text_tokens` and `image_tokens`. All metadata is non-authority and is cleared with response/base64 references after decode.

For non-success only, the strict error projection admits exactly one top-level `error` object with optional string `type`, `code`, `param`, `message`, capped at `128/128/128/512` UTF-8 bytes and `1,024` total. Unknown, duplicate, nested, non-string, control, oversized, malformed, or unsafe content yields `diagnostic-unavailable`. Bearer/key-like substrings are redacted. No key, header, request JSON, response body/JSON, base64, generated byte, exception/stack, account/project identifier, request ID, or unknown field may be retained or reported.

## Review, content, and product gate

Before any human or visual review, Quartermaster must prove exact PNG signature, bounded chunk structure/order/length, valid CRC, one `IHDR`, exact `3840x2160`, 8-bit truecolor type 2, opaque sRGB/no alpha, bounded complete inflate, independent decode, and absence of disallowed metadata. Failure closes A1 and triggers exact cleanup without revealing the image.

Only after technical PASS may Quartermaster privately inspect the quarantined A1 source and measure every frozen `PHY-01..12` physical predicate, source-retention band, six-layout crop, hotspot/label/center/nonoverlap mapping, keyboard/pointer/touch/semantic activation, focus, announcement, reflow, effective `200%`, forced-color, reduced-motion, performance, offline, save/privacy, and accessibility predicate. It writes exact `A1Decision` only after all evidence exists. A failed predicate must write REJECT with exact allowed code and closes v8 after cleanup. A passing source writes ACCEPT only with technical/physical/layout/accessibility all true and `codes=NONE`.

Only sole fully passing A1 may be imported byte-identically with create-new semantics to:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md
```

The product raster, provenance, and their directory must be absent before import. No replacement exists. Provenance is limited to authorized request/model/source identity, accepted byte identity, helper identities, objective review scalars, validation results, and cleanup. It excludes secrets, headers/bodies/base64, diagnostics, rejected identity/pixels, and hidden metadata. Registry/copy/alt remain null-first until successful import.

After import, Quartermaster must apply only the already-approved Host 06 registry/provenance/measurement/copy scalars, connect the existing behavior to this accepted source without changing `L02-03`, and run focused tests, related regression, curriculum/readiness validators, full product tests, clean production and fixture builds, served identity, isolated preview, PBA, and one bounded non-overlapping E2E using sanctioned fixtures. It may not use Martin's browser/profile/save. Full success yields one Quartermaster `PRODUCTION CONTENT COMPLETE` commit and push; any failure yields exact HOLD/rollback and no downstream handoff.

## Production-role freedoms

- Quartermaster may make only the exact A1 call, objective acceptance/rejection, byte-identical product import, provenance, and already-approved Host 06 content/registry integration required above.
- Image Specialist, only after Quartermaster PASS, may polish runtime code/configuration presentation around the unchanged accepted raster: crop, composition, lighting treatment, color, sound, motion, typography, hierarchy, focus, responsive containment, forced color, and reduced motion. It may not generate, edit, replace, vary, re-encode, import, or reveal media.
- Reconnaissance, Tactical, and Combat predecessor controls `FRDT-005-v1-VR-01`, `FRPX-005-v1-VR-01`, and `FRCE-005-v1-VR-05` remain accepted construction constraints; this shell does not reopen their completed implementation except for the exact accepted-source registration already planned.

## Exact delta classification

All v8 bytes are classified:

1. `ACCEPTED IMPROVEMENT`: v7 ordinal loop is replaced by sole string attempt `A1` and fresh literal v8 paths.
2. `REQUIRED CORRECTION`: the data-only regex parser is replaced by the strict official `ImagesResponse` structural parser and duplicate-key guard accepted by `FRVE-005-v8`.
3. `REQUIRED CORRECTION`: status, media type, bounded byte count, stable gate, send state, and strict sanitized non-success diagnostic are added to the failure record.
4. `REQUIRED CORRECTION`: provenance names `FRWO-005-v8`, this shell, and sole A1.
5. `ACCEPTED IMPROVEMENT`: launcher path literals move only to the fresh v8 helper/live roots.
6. Retained unchanged: exact prompt/request, endpoint/model/options, pre-response transport, native helper source and behavior, handle identity, create-new/flush/move, review handshake, product paths, byte-identical import, manifest identity, cleanup, validation, canon, learning, state, accessibility, and release boundaries.

Any other byte, semantic, path, model, prompt, option, transport, output, product, or role delta is `UNAUTHORIZED DIVERGENCE` and terminal.

## Validation, definition of done, and rollback

Mission parser-checked both retained scripts, compiled the exact embedded strict JSON guard in memory, passed minimal and documented-sibling success fixtures, rejected duplicate top/item keys and trailing content, confirmed sole-A1 markers and absence of v7 attempt names, and independently confirmed all eight file paths plus product root absent. This was zero-send, zero-credential, zero-temp, zero-media validation.

Definition of done for Quartermaster is either:

- `PRODUCTION CONTENT COMPLETE`: exactly one A1 send, one fully passing source, exact product/provenance/integration, all required gates passing, owned temp paths absent, dedicated commit pushed, and handoff to one fresh Image Specialist; or
- exact terminal `HOLD`: A1 consumed or pre-send gate failed, no retry, no rejected media/product residue or reveal, precise secret-safe evidence, and handoff to the earliest owner.

Rollback never resets the repository. Before accepted import it deletes only exact identity-proved A1-owned material and empty roots. After a later failed product operation it removes only create-new A1-owned product/provenance whose identities are proven. It does not touch pre-existing media, runtime, user work, v7 paths/residuals, or protected state.

## Immutable and protected boundaries

Repository QA quarantine, `Art Of No Mans Sky Book Scan.pdf`, `Simplilearn Training Files/`, Martin's real browser/profile/save, accepted-media bytes/pixels, v7 paths and ordinals, managed or opaque residuals, VR-65, hidden lore, and unrelated work are inaccessible. The immutable accepted-media manifest remains `17 / 37,410,731`, file SHA-256 `a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`, tuple digest `c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99` until a fully passing A1 import is truthfully added.

The one-path rail, sole unchanged `L02-03`, no-cross-credit, privacy/evidence ownership, equal-dignity MH-40 outcomes, shared RP-012 ending, all null deltas, and `successor=null` are immutable. No branch, packet, lesson, answer, reward, access, identity, authority, world response, Builder/Machine dialogue, Host 07+, RP-013, successor, or post-ending content is authorized.

## Exact Quartermaster handoff

One fresh Quartermaster reads the complete required intake and profile; this complete shell; complete `FRVE-005-v8`, `FRWO-005-v8`, `FRSH-005-v1-VR-33`, `FRVE-005-v7-VR-29`, and `FRCA-005-v5`; complete retained launcher/carrier/builder; exact prompt; current official OpenAI Image API authority; `FRRM-005-v8` / `FRSB-005-v8`; retained `FRDT-005-v1-VR-01`, `FRPX-005-v1-VR-01`, `FRCE-005-v1-VR-05`, manifest, blueprint, functional, treatment, and validation controls.

It reproduces source identities and path absence, then executes exactly one A1 through the retained launcher with the retained carrier supplied only through the exact process environment variable expected by the launcher. It reveals no key, request/response body, base64, generated byte, or rejected image. It stops at the first terminal result and writes one dedicated content ledger plus synchronized handoff.

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-005-v1-VR-34 / ONE FRESH A1 / COMPLETE BYTE-FROZEN OFFICIAL-ENVELOPE CARRIER / NO RETRY / QUARTERMASTER NEXT`**.
