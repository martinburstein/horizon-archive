# FIRST RUN SHELL / FRSH-012-v1

## Disposition

`FIRST RUN SHELL READY / NINE ROLLING-POOL CALLS / FIRST PASS STOPS`

Source release is `FRAB-011-v1` at
`b84e2425c7fd6f2f2f582ab2a606d084ed0aafe2`. Planning authorities are
`FRPB-001-v10`, `FRCL-012-v1`, `FRWO-012-v1`, and `FRVE-012-v1`. This shell
owns only `FR-03 / Host 12` and the existing `L05-03 + L05-04` sequence.

## Fixed player-visible outcome

The prior encounter's submerged returns reach one fused upper crown standing
dry on a high slab. A continuous dry approach reaches that crown. At least
three bypass seams rise from present water into it; at least two sealed sample
chambers remain closed above water; and at least two drowned returns visibly
converge below or behind the accessible structure. Water makes the lower
junction physically unreachable. No lock, warning, swim, descent, chamber
opening, console, display, answer, or world response is created.

LOOK is physical and write-free. TALK is exact nonresponse. USE enters only the
first incomplete owned lesson. Completion remains read-only and changes no
water, crown, seam, chamber, return, route, access, meaning, or later state.

## Generation, custody, and objective PASS

Only sequential `{H12-1..H12-9}` exist locally. First complete PASS ends this
ledger; unused calls remain in the rolling pool but cannot move until this full
release loop completes. Each call uses the bundled CLI `35,266` /
`c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05`
with its exact prompt from `FRVE-012-v1`, `generate`, `gpt-image-2`,
`3840x2160`, high, opaque PNG, exact output, and `--no-augment`. No force,
batch, reference, edit, variation, internal retry, alternate model, or alternate
transport.

Scratch root/children, product raster, and provenance are exact `FRWO-012-v1`
literals. Quartermaster privately reviews only an identity-proved ordinary
nonreparse output. A rejection records bounded identity, flags, measurements,
derived layout verdict, and reasons, then identity-deletes the output and proves
absence before another call. No rejected pixel, derivative, reference, reveal,
or workspace copy exists.

Technical PASS is opaque RGB PNG, exact `3840x2160`, strict decode, 1..30 MB.
Physical PASS is the full dry-approach/crown/seams/sealed-chambers/drowned-
returns sentence with every `FRCL-012-v1` rejection absent. Responsive PASS
derives real cover projection at all six layouts with >=.95 relation retention,
all required centers visible, contained native target >=44 CSS px, zero
protected overlap, and real Edge corroboration. No arbitrary absolute source
band may reject a complete derived PASS.

At most one passing byte-identical raster plus `PROVENANCE.md` enters the
workspace. Import is selection, not release.

## Role ownership and runtime contract

- Recon locks one physical reading order without adding spectacle or meaning.
- Tactical locks state, native actions, focus, layout, recovery, copy, and QA.
- Combat builds a null-first fail-closed registry/tests. Before selection,
  released generic behavior remains. Exact source enablement retires affected
  generic launchers even on later decode/layout failure.
- Quartermaster alone calls, privately reviews, selects, imports, measures,
  records provenance, and populates accepted-source fields.
- Image Specialist changes runtime code/configuration only around the unchanged
  selected raster; never image bytes or reveal.
- Intelligence independently validates and alone releases/carries the pool.

Runtime acceptance requires exact `H12-[1-9]` attempt ID, no surrogate ordinal,
source bytes 1..30000000, lowercase SHA-256, exact dimensions/opacity, final
copy/alt/provenance, measured physical/semantic rectangles, and all six layout
records. One native button converges pointer, keyboard, touch, and switch-like
entry. Focus returns after LOOK, TALK, lesson exit, remediation, mastery, reload,
and route return. The intermediate existing client-boundary lesson retains its
current order and ownership. No prompt, candidate, answer, media identity, or
review state is persisted.

## Validation, budgets, and rollback

Budgets and validation are exactly `FRWO-012-v1` / `FRVE-012-v1`. Combat passes
focused/related/full tests, validators, production/fixture builds, PBA/media,
served identity, and pushes the exact code candidate. After selection,
Quartermaster proves acceptance/cleanup and commits content. Image proves six-
layout runtime presentation. Intelligence runs isolated real-browser evidence,
clean-start E2E through both owned lessons and unchanged MH-40, final budgets,
cleanup, candidate identity, and sync.

Before import, rollback is scratch cleanup. Before release after import,
rollback removes only new raster/provenance and restores the null registry.
Identity, technical, physical, responsive, accessibility, cleanup, budget,
canon, lesson, or protected-boundary failure stops and returns earliest-owner.

Accepted Hosts 01-11 and media remain immutable. Never inspect, restore, stage,
or use repository QA screenshots, Martin's browser/profile/save, protected PDF/
training, hidden lore, or opaque residual roots. No branch, packet, lesson,
reward, access, identity, authority, dialogue, world response, RP-013, post-
ending content, or successor.

## Mission signature

`FIRST RUN SHELL READY / FRSH-012-v1`

Exact next owner: one Reconnaissance Sergeant.
