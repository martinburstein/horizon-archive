# Horizon Archive First Run Work Order - Responsive Evidence Repair

Work Order ID: `FRWO-009-v7`

Stage / owner: Operations Planning Major / `operations_planning_major`

Disposition: **`WORK ORDER READY / ZERO-COST RESPONSIVE-GATE CORRECTION`**

Address: `FR-03 / Host 09`, with a bounded evidence audit of released Hosts 07
and 08 because they share the same layout-record implementation.

## Problem and outcome

The current gate conflates directorial source composition with browser
responsiveness. Its six layout records are caller-supplied attestations; tests
populate perfect booleans instead of deriving crop, projection, DOM target, or
focus evidence. Production renders `3840x2160` sources into a `16:9` world, so
the prior center bands are not evidence of crop safety.

This pass replaces the attestation boundary with deterministic source-to-world
projection, an actual `44px` hotspot floor, and browser-computed geometry/CSS
checks. Physical/canon checks remain exact. Source-center bands become
directorial scoring only and cannot reject a candidate unless an actual crop,
access, focus, label, protected overlap, or physical failure is proved.

All fifteen prior attempts remain consumed and deleted. Their technical facts
remain valid; their `LAYOUT` dispositions become `UNVERIFIED / DEFECTIVE GATE`
except the explicit release-envelope PASS, which is reclassified
`INVALID REJECTION`. No deleted candidate is revived, reused, or regenerated.

## Scope and hard stop

Permitted: dedicated projection utility; Host09 validator/tests/styles;
browser-responsive fixture; evidence/correction reports; release map,
scoreboard, changelog, and handoff. Hosts07/08 receive read-only code/evidence
audit only unless an actual live browser defect is observed. No accepted-media
byte, image, copy, canon, lesson, route, save, or ending change is permitted.

Completion requires deterministic projection tests for all six layouts, real
browser `getBoundingClientRect()` and `getComputedStyle()` evidence, physical
negative tests, adjacent audit, focused/full/build gates, corrected ledgers,
and synchronized release controls. No generation or future-attempt authority
exists.

Operations signs **`WORK ORDER READY / FRWO-009-v7 / ROUTE SCIENCE`**.
