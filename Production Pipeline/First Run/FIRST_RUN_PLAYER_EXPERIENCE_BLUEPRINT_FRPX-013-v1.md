# FIRST RUN PLAYER EXPERIENCE BLUEPRINT / FRPX-013-v1

## Disposition

`PLAYER EXPERIENCE READY / ACCEPTED-ONLY NATIVE HOST 13`

Authorities: `FRSH-013-v1`, `FRDT-013-v1`, source release `FRAB-012-v1`.

## State and replacement graph

Current: predecessor mastery -> generic Text and Speech Patterns -> generic
Visual Patterns -> later Objective Ledger. Before source selection, behavior
remains identical.

After exact source enablement: predecessor mastery -> Host 13 `available` ->
USE opens Text and Speech Patterns -> strict mastery -> USE opens Visual
Patterns -> strict mastery -> `complete` read-only USE -> unchanged Objective
Ledger. Whenever selected source enablement is true, only the two affected
generic launchers are absent even if later decode/measurement fails; malformed
enabled state fails closed. No earlier or later launcher moves.

## Registry and native interaction

Combat creates one null-first registry with source/provenance, complete
relation, dry approach, span array, paired-collar array, load-contact array,
residue, waterline, semantic target, label, protected regions, six layout
records, copy slots, and alt. Lawfulness requires exact H13 attempt ID,
dimensions, opacity, size/SHA, complete source-contained physical geometry,
and derived layouts.

One native hotspot owns LOOK/TALK/USE. LOOK announces only dry access, spans,
collars, loading contacts, residue, and waterline. TALK announces complete
nonresponse. USE dispatches Text and Speech first, then Visual Patterns, and is
read-only after complete. Pointer, Enter/Space, touch, and switch-like
activation converge. One paint after predecessor mastery presents and focuses
once. Terminal exit, primary, remediation, mastery, reload, and route return
restore focus when lawful. Escape closes only the active terminal layer.

States are `hidden` for unmet predecessor or source/provenance/decode/layout/
evidence malformation; `available`; `in_progress`; `remediation_required`;
`complete`; and a copy-only `returned` presentation of the same complete
state. No state changes world art, sound, motion, water, access, or residue.

## Responsive, accessibility, and copy

Scene uses centered production cover projection. Semantic target derives from
measured source geometry, is clamped, and remains >=44x44 CSS px. Six records
cover desktop, laptop, narrow, effective-200%, 320x180, and 320x240: complete
relation retention >=.95; approach/span/collar/contact/residue/waterline centers
visible; target contained; physical center included; label/focus separated;
protected overlap zero. DOM order is art -> hotspot -> status/terminal. Forced
colors adds a visible border; reduced motion removes transition only; 200%
preserves meaning and source order.

Quartermaster fills final-purpose slots: `name`, `unseen`, `available`,
`textSpeechInProgress`, `visualInProgress`, `missed`, `mastered`, `returned`,
`nextBoundary`, and `alt`. Copy cannot imply writing, music, speech, code,
maker, intention, answer, correctness, reward, authority, access, or response.

## Combat acceptance matrix

Tests prove null-first registry; eight prompt identities; exact H13 attempt ID
and 30 MB/aggregate guards; source/provenance fail-close; complete relation and
dry approach; >=3 spans; paired collars for every span; varied orientations;
>=3 load contacts; explicit residue and waterline; production-derived six-
layout evidence ignores caller attestations; source enablement retires only the
two generic owned launchers; predecessor strict mastery; all states; Text/
Speech-then-Visual routing; complete read-only USE; LOOK/TALK write-free;
decode failure; focus/reload/Escape; target semantics; all copy slots;
immutable accepted media; unchanged Objective Ledger and MH-40.

Quartermaster adds selected-byte, private physical measurement/decision,
cleanup, and provenance evidence. Image adds runtime presentation only.
Intelligence independently reruns the release ladder. No variance exists.

Exact next owner: one Combat Engineer.
