# First Run Shell - Stranded Lens Cradle

Shell ID: `FRSH-005-v1`

Disposition: **`FIRST RUN SHELL READY / FRSH-005-v1`**

Stage / owner: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-005-v2` / `FRVE-005-v2`

Planning authorities: `FRPB-001-v2`, `FRCL-004-v2`, `FRRM-005-v2`,
`FRSB-005-v2`

Superseded planning return: `FRVE-005-v1 / HOLD`

Mission source inspected: `21a0ed9bf9e9c6d04da0c92a47907d029171d17d`

Released First Run predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Released product/test baseline:
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`

Released PBA authority: JavaScript `1,667,393`, CSS `119,247`, production
modules `217`, accepted media `17 / 37,410,731`, source maps `0`

Date: **2026-08-10**

## Disposition and felt improvement

The complete planning stack is compatible. This shell authorizes one bounded
vertical-quality pass at `FR-03 / Host 06`.

After exact Host 05 / Sixfold Weir mastery, the accepted Drowned plate gives
way to one additional same-basin first-person view. A lens-like fragment trail
leads across an already-dry local reach to one weathered lens resting inside
one tilted conformal cradle. Drainage seams and stress-control contacts make
the relation physically credible while the horizon catches in the lens. The
Pilot provisionally names it `Stranded Lens Cradle` and preserves the central
uncertainty: stranded by a former phase, or exactly where it belongs. The sole
deliberate `USE` enters the unchanged `L02-03` model/deployment/configuration
work. Miss, answer-free remediation, safe exit, reload, return, transfer,
explanation, mastery, and continuation alter only the expedition's existing
sanitized evidence.

The shell authorizes at most one new canonical raster and its provenance. It
adds no second Host 06 view, Host 07 expression, lesson, scenario, score,
route, save field, checkpoint, world response, access, reward, identity,
authority, ending, successor, RP-013, or post-ending content.

## Planning reconciliation

Mission accepts the two v2 cures without reopening product or continuity:

1. The built-in tool may first place one exact output beneath the fixed
   tool-owned managed root before one byte-identical copy enters a fresh
   GUID-named OS-temporary evaluation child.
2. Combat, not Science or Mission, owns a distinct read-only pre-generation
   gate over exactly seventeen literal accepted-media paths.

`FRVE-005-v1` remains the exact historical HOLD against first-byte OS-temp and
an absent per-file accepted-media manifest. `FRWO-005-v2` and `FRVE-005-v2`
supersede those two execution controls only. Every retained physical,
composition, source, state, learning, privacy, accessibility, budget,
provenance, cleanup, validation, rollback, route, world, ending, and protected-
state boundary remains binding.

Quartermaster generation **may occur after Combat's inert slot** and only
after all of the following are true: Combat's code/test candidate is frozen;
the immutable seventeen-file manifest gate passes; the manifest evidence is
committed; Combat's dedicated push succeeds; and exact
`HEAD == origin/main`. Combat receives no generation, pixel-review, selection,
import, or media-cleanup authority. A failed or unavailable Combat gate ends
the pass before attempt one and consumes no generation ordinal.

## Address, maturity, entry, completion, and hard stop

- **Address:** `FR-03 / Chapter II - Drowned Archive / Host 06` only.
- **Current `FR-03`:** continuity `FR2`; physical-host expression `FR0 - 1
  accepted shared compression / 1 exact / 10 missing`; learning `FR2`;
  behavior/save/recovery `FR1`; final-purpose content `FR2`; presentation
  `FR3`; bounded prior release proof `FR4` for accepted packages.
- **Target evidence:** one exact Host 06 plus final-purpose Host 05 -> Host 06
  -> unchanged `L02-03` continuity. Intelligence alone may later record `1
  accepted shared compression / 2 exact / 9 missing`; the address-level cell
  remains `FR0`.
- **Clean entry:** the unchanged package/opening/Meadow route reaches the
  accepted Drowned source, exact Host 04, and exact Host 05.
- **Changed boundary:** begins only after exact sanitized Host 05 /
  `L02-02` mastery.
- **Completion:** exactly one accepted Host 06 source, lawful metadata,
  final-purpose seven-slot copy, sole `L02-03` entry, recovery, and unchanged
  next Drowned boundary are independently proved.
- **Permitted exits:** Terminal Close, Escape, actual-miss remediation, primary
  acknowledgement, reload/resume, write-free Meadow return/redeparture,
  completed read-only revisit, and the unchanged current forward boundary.
- **Hard stop:** immediately after exact `L02-03` mastery. No Host 07 or later
  physical expression, Crown change, Witness work, City repair, later-host
  work, or Measured Horizon literary content.
- **Pacing:** one short observation and reorientation beat. No new scored work,
  mandatory detour, wait, or changed lesson duration.

No planning, manifest, attempt, candidate, import, commit, or review advances
maturity. Intelligence alone may release the exact as-built candidate.

## Frozen accepted source target and media delta

The two and only two new media-owned workspace paths are:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md
```

At most one passing raster may enter the first path. It must be the exact
selected managed/temp identity copied byte-for-byte: `PNG`, `3840 x 2160`,
exact `16:9`, opaque 8-bit sRGB, browser-decodable, no alpha/transparency,
readable text, executable metadata, or more than `12,000,000` bytes. Only that
raster's bounded provenance may enter the second path. No rejected candidate,
alternate export, crop, derivative, sidecar, contact sheet, thumbnail, or
additional generated output may enter the workspace.

Every pre-existing accepted media byte remains immutable. The selected source
is an additional same-basin Host 06 view; it does not replace, edit, vary,
relabel, or claim parity for the accepted Drowned master or any other accepted
asset.

## Exact built-in call and result adapter

Prompt ID: **`HOST06-GEN-PROMPT-v1`**

Quartermaster must invoke only the built-in `image_gen__imagegen` tool. Each
ordinal uses exactly this argument object and no other field:

```text
{ prompt: HOST06-GEN-PROMPT-v1 }
```

`referenced_image_paths` and `num_last_images_to_include` are omitted. No
conversation image, accepted media, edit target, variation, CLI, REST/API,
browser, extension, alternate generator, manual file, upscale, post-process,
or fallback is permitted. Calls are sequential, exactly one output per call,
and capped at three total. A prompt revision is a new ordinal and must remain
within the exact versioned prompt below; no fourth call exists.

The result adapter accepts exactly one tool result with exactly one local
managed output. It reads the tool-returned `output_hint` only as the candidate
absolute Windows pathname; it never calls `generatedImage`, renders the
returned `image_url`, embeds Markdown, or uses any other result field as a
path. `output_hint` must resolve to one unique existing regular file strictly
beneath:

```text
C:\Users\marti\.codex\generated_images
```

The managed root is literal and may be resolved as a scalar, never listed.
Absent `output_hint`, a non-path hint, more than one path, a relative/nonlocal
path, a directory, link/reparse ambiguity, an unavailable root, a path outside
that root, a path inside the repository or a protected root, uncertain
ownership, or any extra/ambiguous managed output consumes the ordinal and
stops the whole pass opaque. There is no discovery, parent listing, search,
fallback, inspection, import, reveal, or another call.

### `HOST06-GEN-PROMPT-v1` exact text

```text
Use case: stylized-concept
Asset type: final runtime 16:9 first-person environment plate for Horizon Archive Host 06
Primary request: Create one premium contemporary, maximum-quality cinematic science-fiction environment view of the Stranded Lens Cradle in the existing Drowned Archive basin. Show one visibly weathered lens resting inside one tilted conformal cradle on an already-dry, above-water local reach. The scene must preserve quiet scientific uncertainty about whether the lens was stranded by a former phase or is exactly where it belongs.
Scene/backdrop: One local next reach of the same enormous flooded Builder phase-processing basin, continuous with the prior Sixfold Weir rather than a second basin, teleport, cutaway, or lesson diagram. Keep a ruler-straight horizon and reflected horizon, restrained atmosphere, and physically legible old process routing.
Subject: The complete lens-and-cradle relation is the single discovery center. At least 80 percent of the visible lens body lies within the projected inner cradle. Tilt the cradle principal axis 12 to 35 degrees from image horizontal. Show at least two distinct load or stress-control contacts coupling lens, cradle, and supporting fabric; at least two continuous drainage seams leading away toward basin drainage; at least 2 percent of source height of dry clearance above live water; and one continuous dry material approach. Show at least three lens-like fragments leading from the Host 05 side toward but not into the activation region. Make the horizon or its reflection cross at least 25 percent of the visible inner lens width.
Style/medium: Feature-film environment credibility and flagship current-generation game key art; photorealistic materials, physically convincing smoky phase glass, ceramic ribs, mineral deposition, corrosion, abrasion, dust, inclusions, refraction, internal reflection, caustics, repair seams, and several visible stewardship eras. Builder beauty must read as accumulated functional civic construction, engineered ecology, maintenance, revision, graceful failure, and nonhuman process rather than generic fantasy ornament or human industry.
Composition/framing: Exact wide 3840 by 2160 landscape intent, first-person eye level, centered full-source presentation with no required crop. Keep the top 10 percent free of essential physical fact. Place the complete Host 06 relation wholly within normalized x 0.34 to 0.70 and y 0.30 to 0.74, with a tight relation rectangle width 0.30 to 0.36 and height 0.30 to 0.40. Place the Host 05 fragment approach wholly within x 0.04 to 0.30 and y 0.56 to 0.88. Reserve same-basin distant context within x 0.70 to 0.98 and y 0.08 to 0.56. Live water may occupy x 0.68 to 0.98 and y 0.62 to 0.92 but must not intersect the relation or dry approach. Keep the relation distinct from live water, the Host 05 cue, a return-like ridge, the Crown, the distant suspended Tidal Lens, and any second lens/cradle candidate.
Lighting/mood: Restrained natural basin light, horizon reflection caught by the inner lens, sober discovery, deep atmospheric scale, no magical glow, spectacle cue, invitation, reward, activation flash, or world response.
Text: No text.
Constraints: No image input. No protagonist, body, hands, shadow, reflection, portrait, companion, ship, person, face, human path, human control, label, glyph, symbol, interface, overlay, beacon, readable mark, prior-human trace, or native educational graphic. The Machine and Builders do not speak, react, recognize, reward, authorize, invite, judge, explain, move, heal, aim, drain, illuminate, or open anything because of the player. Preserve physical ambiguity and surface-safe canon.
Avoid: Empty ring; lens beside, behind, through, or in front of an aperture; submerged or water-filled cradle; inaccessible scenic landmark; duplicate lens/cradle relation; the distant Tidal Lens as Host 06; generic ruins; terrestrial industrial shorthand; arbitrary fantasy ornament; answer key; model or deployment diagram; purpose certainty; hidden-lore answer; watermark; border; crop-dependent essential fact.
```

The prompt requests but cannot itself prove dimensions, format, count,
composition, canon, or acceptance. Every output must pass the exact objective
gate below.

## Managed ingress, temporary identity, cleanup, and selection

Before each lawful call, Quartermaster resolves the OS temporary parent with
the shell-approved platform primitive and creates exactly one previously absent
direct child named:

```text
horizon-archive-frwo005-<fresh GUID in D format>
```

The adapter proves the child strictly inside the resolved OS temp parent and
outside the repository, managed root, protected/user/browser/save roots, and
the opaque VR-65 root without listing any parent. Only
`attempt-0N.png`, where `N` is the current ordinal `1..3`, is predeclared
inside that child.

For each ordinal, execute in this exact order:

1. Call the built-in tool once and accept only the exact result adapter above.
2. Copy only the exact returned managed file to the exact predeclared temp
   pathname. Do not move or rename the managed file.
3. From read-only handles, record managed and temp byte lengths and lowercase
   SHA-256 values. Require exact equality before any decode, metadata, or pixel
   inspection.
4. Immediately delete only the exact returned managed pathname and prove that
   exact file absent. Missing-before-cleanup state, failure, or unverifiable
   absence stops the whole pass opaque before rubric review.
5. Only after managed cleanup passes, inspect the exact temp candidate against
   PNG/color/dimension/byte/metadata/decode plus `PHY-01..12`, composition,
   mapping, and accessibility predicates.
6. On rejection, retain only ordinal, prompt ID, available tool identity,
   bounded pass/fail facts, and rejection codes. Do not retain rejected bytes,
   hash, pathname, pixels, prompt secret, or hidden diagnostics. Delete only
   the exact temp candidate, prove it absent, then delete the exact empty GUID
   child nonrecursively and prove it absent. Cleanup failure stops opaque and
   forbids another attempt.
7. At the first passing candidate, select at most that one. Copy it
   byte-identically to the exact product raster path; prove temp/product bytes
   and lowercase SHA-256 equal; write only the exact provenance path; then
   delete the exact temp candidate and exact empty GUID child and prove both
   absent.
8. Any identity, product-copy, provenance, candidate cleanup, or root cleanup
   failure stops opaque and triggers bounded rollback of product ingress. It
   creates no acceptance, import, canon, maturity, publication, or reveal.

No recursive or pattern cleanup, parent enumeration, retry of a failed
ordinal, fourth attempt, second selection, later-host reuse, accepted-media
input, preview of a rejected output, Markdown image, cycle reveal,
publication, or sharing is permitted.

## Candidate identity separation

These identities are intentionally different and must never be collapsed:

| Identity | Exact meaning | Authority |
| --- | --- | --- |
| Combat code candidate | Git commit containing only inert Host 06 behavior/tests before media ingress | May run the manifest gate; no media/canon authority |
| Accepted-media baseline | Seventeen frozen path/byte/SHA tuples plus `entriesSha256` at the Combat candidate | Immutable negative-control evidence only |
| Attempt identity | Prompt ID plus ordinal and exactly one tool-returned managed pathname | Noncanonical; consumes one call |
| Temp evaluation identity | `attempt-0N.png` bytes proven equal to that ordinal's managed output | Temporary rubric evidence only |
| Selected raster identity | One passing temp byte length/SHA copied to the exact product path | Eligible content candidate; not yet accepted canon or release |
| Quartermaster content candidate | Git commit containing selected raster, provenance, final copy, and populated predeclared metadata | Candidate only |
| Image presentation candidate | Later Git commit after code/config-only presentation review | Candidate only |
| Intelligence release candidate | Exact frozen as-built commit and evidence chain independently reconciled | Only Intelligence may accept and advance maturity |

Copying, hashing, naming, committing, pushing, rendering, or passing a subset of
gates never promotes one identity into another.

## Objective source rejection and selection

Quartermaster rejects at original resolution when any one stable predicate
fails:

1. `PHY-01`: no single tight rectangle contains the complete lens/cradle.
2. `PHY-02`: less than `80%` of visible lens lies within the projected inner
   cradle, or the lens reads beside/behind/through/in front of an empty ring.
3. `PHY-03`: cradle principal-axis tilt is outside `12-35 degrees`.
4. `PHY-04`: fewer than two distinct stress/load contacts couple lens, cradle,
   and supporting fabric.
5. `PHY-05`: fewer than two continuous drainage seams lead toward basin
   drainage without implying player-caused drainage.
6. `PHY-06`: relation intersects live water, lacks `2%` source-height dry
   clearance, or lacks one continuous dry-material approach.
7. `PHY-07`: fewer than three lens-like fragments cue from the Host 05 side
   toward but not into activation.
8. `PHY-08`: horizon/reflection crosses less than `25%` of visible inner-lens
   width before interface.
9. `PHY-09`: not first-person, or shows a cutaway, diagram, teleport, second
   basin, body, hand, shadow, reflection, companion, ship, human path/control,
   text, glyph, face, or prior-human trace.
10. `PHY-10`: Host 06 cannot be separated from live water, Host 05 cue,
    return-like ridge, Crown, distant Tidal Lens, or a second candidate.
11. `PHY-11`: same-basin continuity is not independently legible through
    water, ceramic ribs, smoky phase glass, drainage, corrosion/mineral
    deposition, repair seams, and old process routing.
12. `PHY-12`: the scene resembles a native lesson diagram, indicates a best
    answer, or implies response, recognition, reward, access, authority,
    invitation, purpose certainty, or hidden lore.

One failure gives zero product, reuse, canon, maturity, publication, or reveal
authority. If all three attempts fail, disposition is `HOLD`; no compromise,
composite, local repair, crop, edit, or alternate source is allowed.

## Frozen source bands, centered composition, and crop

The selected raster uses normalized source coordinates:

| Band | Required envelope |
| --- | --- |
| top status-safe context | `x=[0,1], y=[0,.10]`; no essential physical fact |
| complete Host 06 relation | wholly inside `x=[.34,.70], y=[.30,.74]` |
| tight relation rectangle | width `.30-.36`; height `.30-.40` |
| Host 05 fragment approach | wholly inside `x=[.04,.30], y=[.56,.88]` |
| same-basin distant context | `x=[.70,.98], y=[.08,.56]` |
| live-water context | may occupy `x=[.68,.98], y=[.62,.92]`; no relation/dry-approach intersection |

One `<img>` presents the selected raster at every required layout with
`object-fit: cover` and exact `object-position: 50% 50%`. Because source and
world frame are exact `16:9`, the full `x=[0,3840], y=[0,2160]` remains
visible at desktop `1920x1080`, laptop `1366x768`, narrow `390x844`, effective
`200%` `768x900`, retained `320x180`, and the `320x180` world inside
`320x240`. For rendered world width `Rw`, scale is `Rw/3840`; height is
`2160*scale`. Non-centered override, ratio drift, crop, responsive duplicate,
alternate source, or source derivative is rejection.

The complete relation and Host 05 cue each retain at least `99.5%` area and
their measured centers in all six layouts. The relation is at least `.30 x
.30` of source. Essential fact outside the bands or a need for UI-created
physical fact, raster/vector repair, synthetic geometry, alternate crop, or a
fourth attempt is rejection.

## Hotspot, center, label, nonoverlap, and live mapping

Combat creates, and Quartermaster may populate, only this predeclared schema:

```text
source = { enabled=false, path=null, sha256=null, byteLength=null,
           width=null, height=null, format=null, color=null,
           attemptOrdinal=null }
physical = { x=null, y=null, width=null, height=null,
             centerX=null, centerY=null }
activation = { x=null, y=null, width=null, height=null }
label = { insetOuterCss=3, insetTextCss=5 }
protected = { host05Cue=null, liveWater=null, returnLikeRidge=null,
              crown=null, tidalLens=null, secondLensCandidate=null }
layouts = { desktop=null, laptop=null, narrow=null, effective200=null,
            retained320x180=null, retained320x240=null }
```

`physical` is the smallest source-pixel rectangle containing the complete
relation. Its arithmetic center must lie inside activation with at least `4
CSS px` clearance in all layouts. Activation contains physical, is no more
than `1.50x` its area, and is at least `44 x 44 CSS px`. Label outer/text
bounds are activation inset by exact `3/5 CSS px`, fully contained without
scroll, clipping, or overflow. Physical and activation have zero area
intersection with every present protected rectangle and at least `96` source
pixels / `8 CSS px` mapped edge separation. Every absent protected feature is
recorded exactly as `absent`.

Mapping uses the centered transform, unrounded DOM rectangles, and a strict
`1/64 CSS px` floor lattice for live evidence. Machine gates prove source/DOM
identity, center and label containment, target size, anchor retention,
nonoverlap, source order, and geometry stability before and after focus.

## Combat pre-generation inert slot

Combat extends the current Drowned system with one inert Host 06 registry.
Before Quartermaster, all candidate fields are exactly the null/false schema
above. No Host 06 raster import exists, Host 06 has no DOM node, name,
announcement, focus stop, action, state, or credit, and the accepted runtime
remains `17 / 37,410,731`.

The existing generic post-Host-05 model-choice launcher remains byte/behavior
unchanged only while `source.enabled !== true`. Once Quartermaster lawfully
populates and enables the selected source contract, that generic launcher is
absent. If enabled metadata, import, decode, or measurement later fails, Host
06 remains absent **and the generic bypass does not return**. Such a candidate
cannot release; Host 05 remains the safe boundary.

Combat must implement the fail-closed guard but may not prefill candidate
coordinates, invent copy, import any raster, inspect pixels, run a generator,
or make Host 06 available. The Combat stage ends with the inert registry,
focused functional tests, exact manifest gate, candidate/evidence commits,
push, and synchronization before Quartermaster attempt one.

## Immutable seventeen-file manifest gate

Artifact path:

```text
Production Pipeline/First Run/FIRST_RUN_ACCEPTED_MEDIA_MANIFEST_FRAM-001-v1.json
```

Schema ID: `horizon-archive.first-run.accepted-media-manifest/v1`

Mission copies these seventeen repository-relative POSIX strings exactly, in
this ordinal order. They are Combat's only accepted-media byte-read authority:

```text
Visual Direction/Production Masters/2026-07-15-glass-meadow-integrated-terminal/glass-meadow-integrated-terminal-master-v1.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/city-threshold-access-master.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/city-threshold-boundary-master.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/city-threshold-overview-master.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/drowned-archive-master.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/witness-corridor-master.png
Visual Direction/Production Masters/2026-07-16-civic-record-district-arrival/civic-record-district-arrival-master-v1.png
Visual Direction/Production Masters/2026-07-26-rp004-three-current-runtime-master/sc05-three-current-panorama-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-27-rp005-manyfold-return-runtime/sc06-manyfold-return-detail-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-27-rp005-manyfold-return-runtime/sc06-manyfold-return-panorama-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-29-rp006-interval-works-runtime/sc07-interval-works-crosssection-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-29-rp006-interval-works-runtime/sc07-interval-works-panorama-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-29-rp007-braided-verge-runtime/sc08-braided-verge-contact-detail-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-29-rp007-braided-verge-runtime/sc08-braided-verge-panorama-runtime-master-v1.webp
Visual Direction/Production Masters/2026-08-01-rp008-offset-reach-runtime/sc09-offset-reach-panorama-runtime-master-v4.webp
Visual Direction/Production Masters/2026-08-01-rp008-offset-reach-runtime/sc09-offset-reach-relation-detail-runtime-master-v1.webp
curriculum/lessons/L-05-07/evidence/basin_audio.wav
```

The JSON object has exactly these semantic fields:

```text
schema: "horizon-archive.first-run.accepted-media-manifest/v1"
workOrder: "FRWO-005-v2"
shell: "FRSH-005-v1"
releaseBaseline: "a91763e28d488f31f8cf7d40ece0b2682246ba9b"
candidateHead: exact 40-character lowercase Combat code-candidate commit
pathBase: "repository-root"
hashAlgorithm: "SHA-256"
hashEncoding: "lowercase-hex"
entries: exactly 17 { ordinal, path, byteLength, sha256 }
count: 17
totalBytes: 37410731
entriesSha256: canonical tuple-stream SHA-256
```

`ordinal` is integer `1..17`; `byteLength` is a nonnegative base-10 JSON
integer; `sha256` is 64 lowercase hexadecimal characters. JSON whitespace and
key order are not identity-bearing. In frozen ordinal order, canonical bytes
for each entry are:

```text
UTF8(path) + 0x00 + ASCII(decimal byteLength, no sign/leading zero) +
0x00 + ASCII(lowercase sha256) + 0x0A
```

Concatenate all seventeen records with no BOM or extra bytes, SHA-256 the
stream, and encode lowercase hex. Each file hash is over exact raw bytes from
one opened read-only handle; byte length is the number of bytes read from that
same handle. Before read, reject a reparse point in any literal path component,
resolve the opened handle inside the exact repository root, prohibit write and
delete sharing, and reject before/after identity or length change. Never
decode, render, thumbnail, preview, interpret metadata, or inspect pixels.

The gate fails before generation on missing/inaccessible/duplicate/unknown/
extra path, ordinal drift, reparse ambiguity, out-of-root resolution, tuple
drift, malformed hash, count other than `17`, total other than `37,410,731`,
or canonical-set mismatch. A failure consumes no attempt and authorizes no
repair or media mutation.

Combat first freezes its code candidate commit, then computes the manifest
against that candidate and records that exact commit in `candidateHead`.
The later manifest/evidence commit does not change product/runtime bytes.
Combat pushes both bounded commits and proves synchronization before
Quartermaster. Later Quartermaster media/PBA, Image, build, rollback, and
Intelligence gates recompute the same tuples and `entriesSha256` exactly.

## Fail-closed Host 06 state and source guard

Host 06 exists only when every predicate passes:

```text
sanitizeResponsibleAIEvidence(input)?.masteryStatus === "mastered"
&& source.enabled === true
&& source.path === "Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png"
&& source.width === 3840
&& source.height === 2160
&& source.format === "png"
&& source.color === "opaque-srgb-8"
&& source.byteLength > 0 && source.byteLength <= 12000000
&& source.sha256 equals the selected provenance record
&& source.attemptOrdinal is integer 1..3
&& every required physical/activation/protected/layout scalar is finite or the exact permitted "absent"
&& every physical/composition/access predicate passes
&& the imported image decodes at naturalWidth=3840 and naturalHeight=2160
```

Then absent sanitized model-choice evidence yields `available`; exact
`remediation_required` yields `remediation_required`; exact `in_progress`,
`primary_complete`, or `transfer_complete` yields `in_progress`; exact
`mastered` yields `complete`; every other state yields `hidden`.
Raw/private responses, focus, observation, media load, detection, dialogue,
and candidate naming are never inputs.

When Host 06 becomes lawful, the selected source replaces the accepted
Drowned view only for the post-Host-05 local reach. The accepted Drowned master
remains exact for Host 04/05 and Demo Tour. Host 04/05 hotspots do not remap
onto the new raster. The current write-free Meadow return remains available
through its existing inventory/navigation control; the old source-local return
ridge hotspot does not migrate to an unproved region of the new raster.

No new durable field, save version, migration, route flag, detection flag,
scene mutation, or source identity enters persistence. Missing static import
fails build. Missing/mismatched decode or metadata leaves Host 06 absent and
Host 05 safe without restoring the generic bypass. An unavailable boundary
cannot release.

## Sole unchanged `L02-03` routing and learning firewall

- Exactly one campaign activation remains after enablement: **`USE Stranded
  Lens Cradle`** in `available`, `in_progress`, or
  `remediation_required`.
- That activation alone calls existing `openModelChoiceExercise()`.
- `LOOK AT` writes nothing and reports only physical fact. `TALK TO` writes
  nothing and preserves silence/uncertainty. Neither opens the Terminal or
  alters state.
- Completed `USE` is read-only: it may report finalized allowlisted evidence
  but cannot add an attempt/hint/check, request confidence, downgrade mastery,
  or change save, route, world, readiness, or ending.
- Pointer, touch, Enter, Space, and switch-like native activation converge
  once on the same intent. Focus, image load, observation, naming, label
  visibility, or source selection grants no mastery or navigation credit.
- Demo Tour remains separate and zero-credit.

The existing `L02-03` exercise JSON, scenario bank, answer key, eight primary
scenarios, eight transfer scenarios, options, evaluator, strict `16/16 +
16/16 + 2/2` progression, actual-dimension remediation, blank retry,
closed-note explanation, ownership/confidence, sanitizer, allowlisted
`modelChoiceEvidence`, private clearing, save projection, restore, and next
structured-packet boundary remain byte- and behavior-exact. No cross-credit,
objective reassignment, new factual claim, answer, hint answer, threshold, or
AI-901 promise is authorized.

## Meaning, labels, accessibility, and focus

Quartermaster owns final prose in exactly seven slots:

| State | Required meaning | Owner |
| --- | --- | --- |
| unseen as interface | The scene predates the player; no compatibility, invitation, or discovery credit | Scene |
| available | Observable lens-inside-cradle fact, provisional name, unresolved stranded/correct hypothesis, and local compatibility only | Scene + Pilot + Suit |
| in progress | Expedition-owned work can be reconstructed; physical scene remains invariant | System + Scene |
| remediation required | Only the actually missed model/deployment/configuration tradeoff needs answer-free repair | 901 Teacher |
| complete | Allowlisted evidence is finalized; lens, basin, Crown, route, and world remain unchanged | System + Scene |
| returned | The same relation remains; sanitized evidence restores only the lawful boundary | Scene + Suit |
| next boundary | The existing structured-packet continuation is available without Host 07 expression | Pilot + System |

Machine and Builders own no line. Copy may not assert purpose, correctness,
lesson embodiment, expected visitor, response, memory, recognition, reward,
access, approval, identity, authority, mission, prior-human trace, or hidden
lore.

The fixed accessible host name is `Stranded Lens Cradle`. Native button labels
are exactly `${verb in lower case} Stranded Lens Cradle, ${state}`, with state
one of `available`, `in progress`, `remediation required`, or `complete`.
`hidden` means no node, name, status, announcement, focus stop, action, or
credit. Image alternative text states only the first-person observable lens,
tilted cradle, dry waterline separation, fragments, drainage, stress contacts,
and surrounding basin; it never states purpose, correctness, invitation,
lesson answer, or Builder intent.

| Event | Required focus/recovery |
| --- | --- |
| Host 05 mastery and lawful source enablement | Host 06 after the accepted Host 05 Terminal unmount; one polite Suit/System announcement |
| LOOK/TALK | Host 06 remains focused; existing live region reports owner-correct meaning |
| USE | Existing model-choice Terminal title receives focus; background is inert |
| Miss/remediation | Actual failed field/status and existing ordered controls; no scene jump |
| Primary acknowledgement | Terminal closes to Host 06; next USE reconstructs fresh transfer |
| Transfer/explanation | Existing dialog order; no focus escape |
| Close/Escape | Connected enabled Host 06; otherwise Host 05; otherwise first lawful scene control |
| Mastery | Host 06, then unchanged next continuation is the next lawful action |
| Reload unfinished | Host 06 scene recovery; clean session mounts only after USE |
| Reload mastered | Existing next continuation; completed Host 06 remains discoverable/read-only |
| Meadow return/redeparture | Write-free; re-entry focuses Host 06 when lawful, otherwise Host 05 |
| Malformed evidence/source | Earliest safe Host 05 boundary; no bypass, replay, or credit |

Hidden/disabled controls receive no focus. Repeated-key and mixed-input
activation cannot dispatch twice. Keyboard, pointer, touch, and switch-like
activation remain equivalent. Focus and meaning are not color-, sound-,
animation-, timing-, hover-, or sight-only. Forced colors retain a `3px
Highlight` focus outline. Reduced motion removes nonessential transition and
animation without changing state or order. No audio or timed state is
required. Automated evidence is not human assistive-technology certification.

## Save, privacy, route, world, offline, and ending

- No Host 06 source/coordinate/detection/focus/dialogue/session flag enters the
  save. Existing allowlisted atomic save/version/restore behavior remains
  exact and malformed state fails closed.
- Only sanitized `modelChoiceEvidence` is durable for `L02-03`. Private
  choices, response text, explanation prose, free-form reasoning, working
  index, focus, transient dialogue, media path diagnostics, credentials,
  endpoints, and generation data are never persisted.
- Close/reopen in one session may use existing in-memory behavior. Reload,
  return, or redeparture reconstructs only from sanitized evidence after
  deliberate USE.
- Host 04 remains accepted shared compression and sole `L02-01`; Host 05
  remains exact and sole `L02-02`; Host 06 becomes sole `L02-03` only after
  complete enablement. The unchanged next structured-packet boundary follows.
- Hosts 07-15, Witness Host 16, the City contradiction, Hosts 17-38, and the
  Measured Horizon literary close remain separate and unadvanced.
- The basin, lens, cradle, water, Crown, route, Machine, and Builders never
  react, invite, judge, reward, recognize, authorize, answer, move, heal, aim,
  drain, illuminate, or open because of discovery or learning.
- Normal runtime remains local/offline with one same-origin selected-image
  request and zero external requests. Add no dependency, lockfile change,
  network service, endpoint, model/agent call, account, credential, telemetry,
  service worker, download, clipboard, external storage, or Python/WASM
  runtime.
- READY and NOT YET READY retain equal dignity, review access, and the same
  horizon. `cityStateDelta=null`, `worldStateDelta=null`,
  `externalStateDelta=null`, `authorityDelta=null`, and `successor=null` stay
  exact. No branch, RP-013, successor, or post-ending content exists.

## Performance and product-budget caps

| Measure | Affected cap | Global/result cap |
| --- | ---: | ---: |
| emitted JavaScript | `<=1,679,393` | `<=1,703,258` |
| emitted CSS | `<=119,547` | `<=119,672` |
| production modules | `<=218` | `<=222` |
| selected raster | `<=12,000,000` | exact `3840 x 2160` PNG |
| resulting media | exact `18` and `37,410,731 + selectedBytes` | `<=49,410,731` bytes |
| source maps | `0` | `0` |
| cold/warm decode | `<=250ms / <=100ms` | direct browser decode |
| cold local image ready | `<=750ms` | one same-origin request, zero external |
| attributable/total CLS | `<=0.01 / <=0.05` | reserved `16:9` slot |
| Host 06 activation | `<=2ms` | sampled task `<=100ms` |
| focused/related/cold full | `<=30s / <=60s / <=60s` | same |
| production/fixture build | `<=60s` each | same |
| complete E2E | one invocation `<=180s` | no retry |

Any local/global overage is `HOLD`; there is no budget borrowing, quality
waiver, accessibility reduction, media compression/edit, source-map waiver,
or dependency workaround.

## Role partition and ordered production milestones

1. **Reconnaissance Sergeant:** lock one observation-and-uncertainty
   interpretation, attention path, and same-basin reorientation. No prompt
   change, generation, final prose, code, asset handling, or reveal.
2. **Tactical Operations Specialist:** freeze state/action/source switch,
   semantic order, focus, recovery, six-layout mapping, candidate-measurement
   slots, accessible order, and malformed fallbacks. Candidate values remain
   null. No final prose, generation, import, or implementation.
3. **Combat Engineer:** implement the inert fail-closed registry, conditional
   source/legacy-launcher boundary, tests, release commands, and read-only
   manifest gate. Generate or inspect no pixels. Freeze product candidate,
   record manifest, commit, push, and prove synchronization.
4. **Quartermaster:** only after the Combat push gate, execute at most three
   built-in ordinals through the exact managed-ingress protocol; reject
   objectively; select at most one pass; import exactly one raster plus
   `PROVENANCE.md`; populate only predeclared scalar/source slots; and write
   final copy in seven slots. No behavior, lesson, route, save, world, or
   ending change.
5. **Image Specialist:** code/configuration presentation only around the
   immutable selected bytes—composition within the centered no-crop contract,
   lighting/color treatment, hierarchy, typography, spacing, focus,
   forced-color, reduced-motion, and existing sound only. No generation,
   edit, variation, export, import, replacement, media-byte change, or reveal.
   A substantiated no-change pass is valid.
6. **Intelligence Officer:** fresh independent release posture; recompute
   manifests and identities, run/corroborate the exact ladder, classify every
   variance, release or return the immutable candidate, and alone update
   maturity/map/scoreboard. No repair, generation, reveal, or residual access.

Every role writes one dedicated report and at least one dedicated commit.
Production cannot reorder these milestones. A variance request pauses the
affected edge and returns to its earliest owner; it never silently changes the
shell.

## Exact permitted files and systems

Product/test/media changes are limited to:

- `horizon-archive-game/src/App.jsx` for bounded source switching, Host 06
  state/action/focus integration, and conditional generic-launcher removal;
- `horizon-archive-game/src/drownedArchive.js` for inert registry, pure guard,
  measurement/config slots, and copy slots; no additional production module is
  authorized beyond the selected raster import;
- `horizon-archive-game/src/styles.css` only if existing presentation cannot
  satisfy centered source, target, focus, containment, forced-color, or
  reduced-motion gates and only under the CSS cap;
- existing focused/related tests for Host 04/05, model choice, restore,
  return, focus, canonical frame, Demo Tour, accepted world plates, later rail,
  and ending, plus optional
  `horizon-archive-game/test/strandedLensCradle.test.js`;
- existing `playtest/e2e-playthrough.mjs` for the one complete journey;
- `Production Pipeline/First Run/FIRST_RUN_RELEASE_COMMAND_MANIFEST_FRRC-003-v1.json`;
- `Production Pipeline/First Run/FIRST_RUN_ACCEPTED_MEDIA_MANIFEST_FRAM-001-v1.json`;
- the two exact media-owned paths above;
- the current role's exact First Run report and `NEXT_INSTANCE_HANDOFF.md`;
  Intelligence may also update release map, scoreboard, and changelog.

`modelChoiceExercise.js` and the complete `L02-03` curriculum tree are
regression-only. Save schemas/versions, dependencies, `package.json`, lockfile,
canonical story/lore, every pre-existing accepted media/provenance file,
Hosts 07+, Witness, City, later rail, Measured Horizon product code, real
storage, hidden lore, and every unlisted path are patch-forbidden.

Protected user state remains outside scope: the repository
`Production Pipeline/First Run/QA/` quarantine, `Art Of No Mans Sky Book
Scan.pdf`, `Simplilearn Training Files/`, real browser/profile/save state,
hidden lore, and every opaque residual. VR-65's published token/digest may be
used only as a scalar do-not-touch identity; it may not be decoded, listed,
resolved, opened, inferred, retried, cleaned, moved, renamed, or deleted.

## Release-command and manifest supersession

`FRRC-001-v1` and `FRRC-002-v1` remain immutable historical command manifests.
Combat creates new `FRRC-003-v1` for `FRWO-005-v2` / `FRSH-005-v1`; it does
not revise or overwrite either predecessor. `FRAM-001-v1` is the first
machine-readable accepted-media tuple manifest for this Work Order and is
immutable after its Combat evidence commit. A later correction requires a new
version and variance, never an in-place edit.

Every `FRRC-003-v1` command entry has a versioned schema, unique ID, exact
working directory, argument-array command or exact platform primitive,
timeout, expected exit, owner, prerequisites, input/output ownership, candidate
identity, and exact cleanup. It must include nonempty entries for:

1. authority/ancestry/protected-boundary and game-on-paper hash;
2. accepted-media manifest creation and later exact recomputation;
3. original-resolution PNG/color/metadata/decode and `PHY-01..12` review;
4. six-layout geometry/source/center/anchor/label/nonoverlap/focus stability;
5. focused Host 05-to-06 state/action/focus/decode-failure tests;
6. full current model-choice miss/remediation/retry/transfer/explanation/
   ownership/confidence/mastery/privacy/restore tests;
7. related route, Demo Tour, later rail, outcomes, null deltas, and ending;
8. exact sorted `40/40` curriculum/readiness `--self-test` validators and cold
   full product suite;
9. production and exact TD-012 fixture builds, PBA, served identity,
   offline/same-origin, decode/task/CLS;
10. production/fixture previews with exact owned PIDs and ports `4173`/`4184`;
11. exactly one complete E2E and one machine summary verifier; and
12. exact process/artifact cleanup, candidate freeze, diff, and Git identity.

Unknown commands, broad discovery, overlapping E2E, retry, generic preview
defaults, real profile/save, repository QA, or undeclared output is forbidden.

## Executable validation ladder

All candidate-bearing gates run in this order and record exact candidate,
manifest entry, elapsed time, result, and honest unavailable evidence:

1. **Integrity:** authority and ancestry; bounded diff; permitted paths;
   protected boundaries; game-on-paper hash; exact `FRAM-001-v1` tuples and
   digest; selected bytes/digest; provenance; attempt count; containment,
   identity, cleanup, forbidden-path, and no-reveal scalars.
2. **Source acceptance:** original-resolution `PHY-01..12`; PNG chunks;
   `3840x2160`; opacity/color/metadata/byte cap; direct decode.
3. **Geometry/access:** all six layouts; source/physical/activation/label/
   center/anchor/retention/nonoverlap/target mapping and before/after-focus
   stability.
4. **Focused (`<=30s`):** inert pre-generation slot; pre-Host-05 absence;
   source guard; exact order; LOOK/TALK/sole USE; conditional legacy-launcher
   removal; state; focus; Close/Escape; return; reload; malformed evidence;
   missing import/build failure; decode/metadata failure.
5. **Learning/privacy (`<=60s`):** every current model-choice case plus one
   actual miss; actual-dimension answer-free remediation; blank retry; fresh
   transfer; closed-note explanation; ownership/confidence; mastery;
   private-free persistence and exact restore.
6. **Related (`<=60s`):** Hosts 04/05; Meadow return; unchanged next boundary;
   no Host 07; Witness/City/later rail; Demo Tour; both MH-40 outcomes; null
   deltas; `successor=null`.
7. **Full/validators (`<=60s` each):** exact sorted `40/40` validators and cold
   full suite.
8. **Build/PBA/served (`<=60s` each build):** production and TD-012 fixture;
   exact JS/CSS/module/media/map caps; selected source and served-to-disk
   identity; root/deep/JS/CSS HTTP preflight; offline/same-origin requests;
   decode, activation/task, and CLS budgets.
9. **One E2E (`<=180s`, no retry):** exactly one non-overlapping clean-start
   production journey to MH-40 including Hosts 04/05/06, one real `L02-03`
   miss/recovery/mastery, Close/Escape, reload/return, completed read-only USE,
   unchanged rail, equal outcomes, null deltas, `successor=null`, zero runtime
   errors, one summary, and one verifier.
10. **Cleanup/Git:** stop exact owned processes, prove ports clear, delete only
    exact declared success artifacts and owned temp roots under repeated
    containment checks, leave protected/opaque residuals untouched, freeze
    candidate, and prove required stage synchronization.

No candidate preview or E2E may begin before a source passes objective review,
enters only the exact product path, and managed/temp cleanup succeeds. No
rejected image ever enters browser, preview, report, Markdown, source control,
or E2E. No role creates a cycle reveal.

## Definition of done

One exact provenance-honest selected raster expresses the complete dry
lens-inside-tilted-cradle relation and passes every objective and six-layout
gate; the seventeen accepted tuples remain immutable; all managed/temp paths
are exactly cleaned; Host 06 is absent until exact Host 05 mastery and complete
source proof; one native accessible Host 06 becomes sole `L02-03` entry; the
generic launcher is absent after enablement; all state, learning, save,
privacy, return, route, world, offline, accessibility, performance, and ending
invariants pass; one raster and one provenance record are the only media
delta; Image performs code/config-only review; exactly one E2E and complete
cleanup/Git gates pass; and fresh Intelligence accepts the immutable as-built
candidate.

Prompt intent, self-reported tool success, a renamed generic launcher,
decorative scenery, invisible duplicate routing, arithmetic without live
mapping, a separately exported crop, synthetic geometry, a planning artifact,
or partial tests are insufficient.

## Rollback boundary

Rollback baseline is exact released product candidate
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`. Bounded rollback removes only:

- the exact new selected raster and `PROVENANCE.md`;
- bounded Host 06 product/config/test/E2E changes;
- `FRRC-003-v1`, `FRAM-001-v1`, and cycle production reports; and
- files first introduced by this candidate after exact path and digest proof.

Restore pre-existing permitted product/test paths by explicit path to their
exact baseline blobs. Do not reset the repository, rewrite planning history,
or erase unrelated later commits. Rollback never touches accepted media or
provenance, Hosts 04/05, curriculum, save schema, dependencies, Host 07+,
Witness, City, later rail, Measured Horizon, user/untracked work,
browser/profile/save, protected roots, repository QA quarantine, a managed file
not returned by the exact call, or an opaque residual. Failed managed/temp
cleanup remains an opaque stopped condition and never licenses parent
enumeration or recursive deletion.

## Variance, no-reveal, and hard-stop routing

Every mismatch is immediately classified as `ACCEPTED IMPROVEMENT`, `REQUIRED
CORRECTION`, `MASTERPLAN UPDATE`, `DEFERRED LIMITATION`, or `UNAUTHORIZED
DIVERGENCE`. No downstream role silently changes this shell.

Return product/media authority to Commandant; canon/voice/ending conflict to
Colonel; address/order/scope to Operations; systems/learning/evidence/privacy/
save/accessibility/performance/offline/manifest/cleanup ambiguity to Science;
cross-discipline ambiguity to Mission; direction to Recon; interaction/focus/
responsive mapping to Tactical; code/state/build defects to Combat;
copy/content/asset/provenance defects to Quartermaster; runtime hierarchy/
focus/forced-color/reduced-motion presentation to Image; and release evidence,
maturity, or synchronization to Intelligence.

Immediate `HOLD` applies to more than one canonical source, more than three
calls, accepted-media input/edit, CLI/API/fallback, managed-parent listing,
unknown tool path, identity mismatch, cleanup weakening/failure, derivative,
second host, source-band/crop/hotspot/target failure, duplicate `L02-03` entry,
learning/evaluator/sanitizer change, private persistence, save/dependency/
network change, route/world/ending change, cap overage, E2E retry, protected or
residual access, hidden lore, real browser/profile/save, reveal/publication,
new lesson/branch/reward/access/identity/authority/response, successor, RP-013,
post-ending content, or destructive/broad cleanup.

No generated or accepted candidate is emitted through `generatedImage`,
Markdown, screenshot, contact sheet, cycle reveal, publication, or sharing.
Internal accepted-candidate validation is not publication authority.

## Preserved OPEN process records and recurrences

All thirteen inherited records remain separate and **OPEN**: VR-17, VR-23,
VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The Commandant broad First Run filename/search-scope
record remains one additional separate **OPEN** record. All prior recurrences
remain recorded. None is waived, merged, closed, cured, renumbered, or
candidate evidence.

This Mission stage records these process-only recurrences without using them
as product/media/candidate evidence:

- the initial broad `git status --short` re-emitted the three already-disclosed
  untracked pathnames, a recurrence under still-OPEN VR-24;
- the initial broad control locator and later current-control search emitted
  repository filenames, recurrences under still-OPEN VR-23 and the separate
  Commandant search-scope record; and
- a nonrecursive First Run control-directory listing re-emitted the protected
  repository QA directory name but did not enumerate or open its contents; it
  remains a process-only protected-path/search-scope recurrence under still-
  OPEN VR-17/VR-23 and the separate Commandant record.

No protected QA file content, media byte, PDF, training content, real browser/
profile/save, hidden lore, or opaque residual was opened or used. No image
generation, edit, variation, selection, preview, import, copy, move, deletion,
publication, reveal, build, browser, E2E, save, external service, cleanup,
schedule, automation, production, or release operation occurred in Mission.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**. Its contents and meaning remain
unknown. It remains inaccessible and is not canon, media, candidate, maturity,
or release evidence.

## Git stage and push rules

- **Mission:** commit only this shell and the synchronized Recon handoff; push
  `main`; prove exact `HEAD == origin/main` before Recon.
- **Recon:** dedicated treatment commit; no required push.
- **Tactical:** dedicated blueprint commit; no required push.
- **Combat:** freeze the inert code candidate, then the manifest/evidence
  commit; push `main`; prove `HEAD == origin/main` before Quartermaster attempt
  one. No media exists in either Combat commit.
- **Quartermaster:** dedicated content/asset commit after exact selection and
  cleanup; no independent push required. A failed/opaque attempt may instead
  commit only a no-media HOLD report when safe.
- **Image:** dedicated code/config presentation report/commit; no independent
  push required; a substantiated no-change report is valid.
- **Intelligence:** fresh reconciliation, candidate/evidence freeze, release
  commit, push `main`, and prove `HEAD == origin/main == remote main` before
  release handoff.

No role stages unrelated user work. Automation and schedules remain disabled.

## Mission signature and exact Recon handoff

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-005-v1`** from source
`21a0ed9bf9e9c6d04da0c92a47907d029171d17d`.

### Change history and shell-gate proof

- `FRSH-005-v1` is the initial shell for `FRWO-005-v2`. It does not revise
  prior shells or revive withdrawn `FRWO-004-v1`.
- Mission changed no product, test, runtime, learning, story, save, media,
  manifest, map, scoreboard, maturity, process classification, residual,
  automation, or schedule file.
- The dedicated Mission commit introduces only this shell and the synchronized
  Recon handoff. The resolved commit and push equality are reported after the
  commit because the file cannot contain the hash of the commit that first
  contains itself.

One fresh Reconnaissance Sergeant / `reconnaissance_sergeant` must read this
shell in full, the synchronized handoff, `FRWO-005-v2`, `FRVE-005-v2`, current
Drowned runtime/canon, exact Host 05 completion state, and only the relevant
visual authorities. Issue exactly one versioned Directorial Treatment for
`FRSH-005-v1` defining attention, same-basin reorientation, fragment approach,
dry-reach discovery, horizon catch, unresolved stranded/correct uncertainty,
silence, and the transition into unchanged `L02-03`.

Recon may not change the frozen prompt, generate or inspect a candidate,
create final prose, freeze exact interaction/focus implementation, edit
product/test/media, alter learning/route/save/world/ending, advance maturity,
close an OPEN record, access a residual, create a reveal/schedule, begin
Tactical, or call `FIRST RUN COMPLETE`.
