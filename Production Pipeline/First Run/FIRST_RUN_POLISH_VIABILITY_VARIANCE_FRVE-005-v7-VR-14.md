# Horizon Archive First Run Polish Viability Variance

Variance ID: `FRVE-005-v7-VR-14`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`HOLD / DISPOSABLE PARENT RETURNED EXIT ZERO WITH NO BOUNDED
RESULT OR STOP RECORD / CHILD INVOCATION CARDINALITY NOT RETAINED / NO RETRY /
FRVE-005-v7-VR-14`**

Date: **2026-08-11**

Science source inspected:
`2652788736289a18bf1c896bffe8a007ef9fc181`

Mission correction: `FRSH-005-v1-VR-16`

Prior Science return: `FRVE-005-v7-VR-13`

Quartermaster return: `FRCA-005-v4`

Effective shell: `FRSH-005-v1` through `FRSH-005-v1-VR-16`

Effective treatment / blueprint: `FRDT-005-v1-VR-01` /
`FRPX-005-v1-VR-01`

Functional report: `FRCE-005-v1-VR-05`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Mission Captain / `mission_captain`

## Science decision

Science read the complete active intake, workflow, registry, full Science
profile, complete `FRSH-005-v1-VR-16`, complete `FRVE-005-v7-VR-13`,
complete `FRSH-005-v1-VR-15`, and the exact current classifier, controlled
path, Work Order, shell, candidate, manifest, release-map, and scoreboard
controls cited there. Starting `HEAD` was exact
`2652788736289a18bf1c896bffe8a007ef9fc181`.

Science constructed one document-independent child-bearing parent source in
memory. The parent source contained exactly the canonical launcher and
combined Base64 scalar values frozen by `FRSH-005-v1-VR-16`; the parent
itself contained no repository read, Markdown extraction, fence, line range,
Git-object read, source slice, alternate carrier, or runtime reconstruction.
The parent source included the required carrier/hash/decode, prefix/tail
slice, strict UTF-8, parser, argv/environment, Windows-limit, stage-order,
PT06 classifier-mutation, credential-clear, one-child, postflight, zero-
activity, and bounded-result assertions.

Science started the disposable parent process once using redirected standard
input as its source transport. The parent process returned exact exit `0`,
but captured stdout was zero bytes and captured stderr was zero bytes. It
therefore returned neither the sole accepted
`SCIENCE_HOST06_COMBINED_RESULT_V1` record nor an allowlisted bounded
`SCIENCE_PARENT_STOP` record. No output exists from which Science can prove
that SR01 completed, that the child began, or that child invocation
cardinality was exactly `0` or `1`.

Science separately proved only that PowerShell 5.1 redirected `-Command -`
transport can emit and capture a short bounded control record. That control
does not reconstruct the missing run result and does not prove how the
36,060-character carrier source was ingested. The failure is therefore
localized to the disposable-parent source/result transport boundary, not to
the frozen launcher, combined bytes, production child, or PT06 classifier.

Because the production child may have begun before the missing result, the
unchanged no-retry rule applies. Science performed no second parent or child
run and does not reinterpret exact exit `0` plus empty capture as acceptance.
Quartermaster, generation/API, and ordinal `2` remain unauthorized.

Variance classification: **`REQUIRED CORRECTION / DISPOSABLE PARENT SOURCE
TRANSPORT OR RESULT RETENTION RETURNED EMPTY CAPTURE / OPEN`**.

## Exact retained activity and absence evidence

Science did not read `OPENAI_API_KEY`. The parent source's only credential
operation before a possible child was removal of `OPENAI_API_KEY` from its
process environment without reading its value. Any possible production child
therefore inherited credential absence and could reach no request
construction or send stage. Science itself constructed no request, called no
API, sent no request, consumed no ordinal, and accessed no response, Base64
payload, media byte, image, or pixel.

The exact parent result counters are not retained. Safe outer facts are:

```text
credentialValueReads=0
requestConstructions=0
sendAsyncCalls=0
directSends=0
ordinalsConsumed=0
childInvocations=UNPROVEN_0_OR_1
```

Independent exact-path postflight after the sole parent process ended proved:

```text
helperRootAbsent=true
helperDllAbsent=true
liveRootAbsent=true
ordinal2StageAbsent=true
ordinal2TargetAbsent=true
ordinal2DecisionAbsent=true
ordinal3StageAbsent=true
ordinal3TargetAbsent=true
ordinal3DecisionAbsent=true
productRootAbsent=true
productRasterAbsent=true
productProvenanceAbsent=true
scienceFixtureRootsAbsent=true
```

Historical ordinal `1` remains consumed, opaque, inaccessible, and unchanged.
Ordinals `2` and `3` remain unstarted and unconsumed. No disposable parent
file or Science fixture root was created; the in-memory parent and its outer
capture were released before commit.

## Protected state, maturity, and exact Mission handoff

The repository QA quarantine, protected PDF, training tree, Martin's real
browser/profile/save, hidden lore, accepted-media bytes/pixels, OS-temp
parent, ordinal-1 residual, real managed directory, user work, VR-65, and
every opaque residual remained untouched. All inherited OPEN records remain
separate and OPEN. This HOLD closes, cures, merges, waives, renumbers,
accesses, or reclassifies none.

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`.

Science changed no product, test, runtime, lesson, save, media, manifest,
candidate, map, scoreboard, maturity, process classification, residual,
schedule, or automation state. The only repository changes authorized and
retained for this stage are this variance and synchronized
`NEXT_INSTANCE_HANDOFF.md`. Science does not push.

One fresh Mission Captain / `mission_captain` reads this complete HOLD,
complete `FRSH-005-v1-VR-16`, complete `FRVE-005-v7-VR-13`, complete
`FRSH-005-v1-VR-15`, and all exact effective controls cited there. Mission
issues exactly one versioned `FIRST RUN SHELL READY`, `REVISE`, or `HOLD`
decision for a parent source transport that can carry the immutable
`36,060`-character combined Base64 scalar and retain exactly one bounded
result without repository-document runtime extraction. Mission may not
reinterpret this run as child evidence; authorize a retry from this Science
context; route to Quartermaster; execute generation/API; allocate the live
root; read a credential value; construct/send a request; call `SendAsync`;
consume ordinal `2`; inspect media/pixels; change product/tests; run E2E;
reveal; advance maturity; close an OPEN record; access a residual or VR-65;
schedule; automate; push from Science; release; or call
`FIRST RUN COMPLETE`.

Office of Science Administrator signs **`HOLD / EMPTY PARENT CAPTURE /
CHILD INVOCATION CARDINALITY UNPROVEN / NO RETRY /
FRVE-005-v7-VR-14`** from exact source
`2652788736289a18bf1c896bffe8a007ef9fc181`.
