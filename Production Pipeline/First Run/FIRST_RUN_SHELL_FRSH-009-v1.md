# Horizon Archive First Run Shell - Host 09 Flooded Choir

Shell ID: `FRSH-009-v1`

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-009-v1` / `FRVE-009-v1`

Disposition: **`FIRST RUN SHELL READY / HOST 09 / IMAGEGEN CLI / FIVE SEQUENTIAL ATTEMPTS`**

Date: **2026-08-12**

Shell source: `bde3ffe0`

Released predecessor: `FRAB-008-v1`, release commit
`6f1f09575dfb47486b7b233d56b0c6afb771f12f`

Baseline / continuity: `FRPB-001-v6` / `FRCL-009-v1`

## Fixed outcome

After exact Host 08 and strict `L03-03` mastery, the player reaches a dry rim
beside one pressure-linked family of differently sized water-filled cavities.
At least two openings reflect ambient light and one pressure-related cavity
returns no optical reflection. The distributed coupling is off-axis and
physical. LOOK is write-free, TALK is silent, and USE owns unchanged `L04-01`
then `L04-02`. The compact visual continuation after both masteries remains.
Host 10 stays absent.

No mouth, face, body, singer, voice, performance, instrument, communication,
recognition by the world, answer cue, message, reward, access, authority,
reaction, route creation, branch, hidden-lore answer, later-host reveal,
RP-013, or successor may be added. `Choir` remains Pilot metaphor only.

## Exact generator and ledger

Only this installed CLI may run:

```text
C:\Users\marti\.codex\skills\.system\imagegen\scripts\image_gen.py
bytes=35266
sha256=c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05
python=3.12.10
openai=3.0.0
```

Each call is `python <script> generate --prompt-file <exact prompt> --model
gpt-image-2 --size 3840x2160 --quality high --output-format png --background
opaque --out <exact ordinal path> --no-augment`. `n=1` is default. No force,
batch, edit, reference/input, mask, variation, alternate model, direct runner,
script edit, internal retry, reveal, or sixth call exists. Key presence may be
tested only as a boolean; its value remains secret.

Ledger `{H9-1..H9-5}` is initially unconsumed. An ordinal is consumed when its
CLI process launches. Nonzero exit stops. Objective rejection plus exact
identity-conditioned cleanup enables the next ordinal. First PASS stops all
later calls.

## Frozen prompts

```text
H9-1  HOST09_GEN_PROMPT_H9-1.txt  3076  298287c29b477f8741ca2971dc2e56f28a4bf0a6ba3a4fb3bd68c528db5cc3ce
H9-2  HOST09_GEN_PROMPT_H9-2.txt  2789  80a7735cefa83fe04df03e37ed3cb5487b370e9dad7ffb8576f24585ce4e4409
H9-3  HOST09_GEN_PROMPT_H9-3.txt  2915  62a5b09d0bac3a88f7f69b24fdc82bd46c4822c173d0e69eb73bcc5f4392c207
H9-4  HOST09_GEN_PROMPT_H9-4.txt  2634  8c5c440981b09f2de0dd21b2133cd651b6827cbd80d38bf062092347b3f4036a
H9-5  HOST09_GEN_PROMPT_H9-5.txt  2867  5e868de1da500fc1176ee739e340a6aa3f3ae4cacac1a0c50f7d49c1d502247f
```

All are strict ASCII, LF-only, complete independent prompts. Downstream may
tighten runtime geometry/copy but may not mutate prompt bytes.

## Custody and product paths

Fresh absent root:

```text
C:\Users\marti\AppData\Local\Temp\horizon-archive-host09-cli-9390d9ee-92b2-4602-b688-bb6cf59f0d2e
```

Its only possible children are `h9-1.png` through `h9-5.png`. Quartermaster
creates it immediately before the first call, proves ordinary/non-reparse,
contained, and empty, and deletes it nonrecursively after candidate cleanup.

Only one accepted byte-identical source and provenance may enter:

```text
Visual Direction/Production Masters/2026-08-12-first-run-host09/
  host09-flooded-choir-master-v1.png
  PROVENANCE.md
```

Both product paths are currently absent. Import is create-new/no-replace using
the exact Cloud-safe literal adapter; no parent enumeration or accepted-media
hydration/repair is allowed.

## Private acceptance

Quartermaster privately checks exact pathname/volume/file identity, ordinary/
non-reparse/one-link status, sole-child set, `1..30,000,000` bytes and SHA,
strict CRC-valid `3840x2160` opaque RGB/RGBA PNG, local decode, all
`FRCL-009-v1` predicates, and all `FRVE-009-v1` geometry/layout gates.

Required normalized measurements include complete family, at least three
opening rectangles, incoming continuity, reflection pair, nonreturn cavity,
dry rim, wet exclusion, distributed coupling, target, label, and protected
regions. All six layouts retain `>=95%` plus every essential center. Target is
`>=44x44` and avoids voids/water/protected regions; label separation is
`>=8px`. Ambiguity rejects. Aesthetics cannot override a predicate.

Rejected pixels are never reused, referenced, published, or revealed. Only
bounded reason codes may guide the already-frozen next prompt. Identity is
reproved immediately before deletion or selected copy.

## Runtime state and roles

Combat first builds inert null-first Host 09 source/provenance/geometry/copy/
alt/state/tests. Exact Host 08 plus `L03-03` is the sole predecessor. Before
enablement generic Text Analysis and Speech launchers remain. Accepted-only
enablement removes them irreversibly; later guard failure hides Host 09 without
restoring bypasses.

USE opens unchanged Text Analysis until mastered, then unchanged Speech
Workloads until mastered, then is read-only. State is hidden, available, in
progress, remediation required, or complete from sanitized evidence only.
LOOK/focus/selection/presentation mint nothing. All inputs, focus, Escape,
return/reload, reflow, effective-200%, forced color, reduced motion, and
assistive naming remain equivalent.

1. Recon freezes one directorial interpretation; no generation.
2. Tactical freezes flow/state/focus/recovery/copy/measurement blueprint.
3. Combat implements inert runtime/tests, freezes code/prompts, runs non-media
   gates, commits/pushes; no generation.
4. Quartermaster alone calls CLI sequentially, reviews privately, deletes
   rejects, imports first PASS, writes provenance/accepted-only fields, and
   runs content gates.
5. Image Specialist changes only runtime code/config presentation with
   selected bytes immutable.
6. Intelligence independently validates and alone releases/matures Host 09.

## Budgets, release proof, rollback

Accepted media `<=20 / 100,136,520`; affected/global JS `<=1,705,500 /
1,730,000`; CSS `<=119,950`; modules `<=223`; maps `0`; no dependency change;
decode `<=300ms` cold / `<=120ms` warm; handler `<=2ms`; CLS `<=.01/.05`; full
E2E `<180s`.

Run focused Host08/Host09/Text/Speech tests; related route/save/privacy/
accessibility/ending tests; validators/full suite; production and TD-012
builds; minified PBA/media; served identities; six-layout/input/focus/forced-
color/reduced-motion review; and one clean-start Host09-to-MH40 E2E in a fresh
explicit external QA root. The harness must statically inventory and bind
every initial and re-entry launcher to native Host 09 before browser start.
Repository QA quarantine is never accessed.

Rollback removes only exact Host 09 additions and scratch. Accepted Hosts
01-08/media, lessons, saves, route, ending, protected state, opaque residuals,
and later gaps are immutable.

Done requires one byte-identical selected raster/provenance, exact measurements
and copy, native dual-lesson flow, all gates, no rejected/scratch residue,
independent release, and synchronized main. If no candidate passes, close HOLD
with all consumed attempts rejected and cleaned. Tool/auth/quota failure stops.

All variances return to the earliest owner. Preserve every OPEN record and
VR-65. Mission signs **`FIRST RUN SHELL READY / FRSH-009-v1 / ROUTE FRESH
RECON`**.

