# First Run Shell - Center-Safe Host 06 E1

Shell ID: `FRSH-005-v1-VR-43`

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-005-v13` / `FRVE-005-v11`

Predecessor release / asset result: `FRAB-003-v1 / FIRST RUN PASS RELEASED` /
`FRCA-005-v11 / D1 OBJECTIVE LAYOUT REJECTION`

Mission source inspected:
`598b0c3a9c15e332ed2f28f1fe024266b70531af`

Disposition: **`FIRST RUN SHELL READY / SOLE E1 / CENTER-SAFE PROMPT V2 /
EXACT ATTEMPT-ID AND 30 MB RECONCILIATION / ONE SEND / NO RETRY / PRIVATE
NO-REVEAL REVIEW`**

Date: **2026-08-12**

Exact next owner: one fresh Quartermaster / `quartermaster`

## Integrated decision

The Work Order, Science envelope, D1 ledger, retained directorial treatment,
player-experience blueprint, inert functional candidate, immutable accepted-
media manifest, and current null-first runtime are compatible under two
mandatory pre-product corrections:

1. a selected source is identified by exact string `attemptId="E1"`; the
   obsolete runtime `attemptOrdinal` member and historical `{2,3}` predicate
   must be removed rather than repurposed; and
2. selected-source bytes are exact integer `1..30,000,000`, with accepted
   media exact `18` and aggregate bytes `<=67,410,731`; the obsolete 12 MB
   source/product condition must be removed from the runtime, tests,
   provenance contract, PBA, served identity, and E2E evidence.

Historical ledgers remain unchanged. These corrections create no source,
player value, canon, or maturity while the registry remains inert. A later
accepted E1 must satisfy every decode, image-ready, CLS, activation, task,
offline, and layout gate independently; the larger finite cap is not a quality
waiver.

Mission independently recomputed the Science witness formula. The laptop
relation height is `261.2475 CSS px`, not the envelope table's transcription
`261.225`; all frozen source bands, witness inputs, minimum targets, and
viability conclusions remain unchanged. Acceptance uses the exact formula and
unrounded geometry, never the transcribed table cell.

## Frozen retained v12/E1 sources

All five files are strict UTF-8 without BOM, ASCII-only, LF-terminated as
declared by `.gitattributes`, and parse with zero Windows PowerShell AST errors:

```text
BUILD_HOST06_V12_E1_CARRIER.ps1
  16,529 / 3b7bf6ac44122e7b297453603a449ee6f344f6a6f3dccef1390bdbc4eba96927
HOST06_V12_E1_PRODUCTION_CARRIER.ps1
  36,230 / 639ca116dd083019bdcd63471beae7b9e352cf29c1ac9166aa8e8aa4f13f7739
HOST06_V12_E1_LAUNCHER.ps1
  2,639 / 6af193c4bd1abac6605f90b603ddb6d5084f5a0c9f1001b7a7899c00f38e7a1d
HOST06_V12_E1_STDIN_PARENT.ps1
  2,880 / f9ca38a6822b8ab17df00073d36d80674ca8280b89fd6002dd97baa95a567f0f
HOST06_V12_E1_STDIN_FIXTURE_CONTROLLER.ps1
  2,202 / fb4e5e9a3e5482376c413ec358f52940575f7efd5fa26485c7d8eabac5eb3ec1
parserErrors=0 for all five
```

The builder independently verifies the exact five v11 baseline identities,
mechanically derives the v12/E1 family, proves the generated carrier's exact
prompt and domain, and executes the dictionary-safe synthetic parser suite.
It is retained evidence, not production ingress. Quartermaster invokes neither
the builder nor the fixture.

## Address, current maturity, and felt result

First-run address is exact `FR-03 / Drowned Archive / Host 05 mastery -> Host
06 Stranded Lens Cradle -> sole unchanged L02-03`.

Current physical expression remains `FR0 - 1 accepted shared compression / 1
exact / 10 missing`. The target after complete independent release would be `1
accepted shared compression / 2 exact / 9 missing`; intent, API success,
technical validity, selection, import, or a product commit cannot advance that
cell. Intelligence alone records release maturity.

After exact Host 05 mastery, the player follows at least three materially
related fragments along one continuous dry same-basin reach toward one
complete weathered lens held within one tilted conformal cradle. The relation,
approach, and horizon catch remain legible as one restrained physical sentence
at all six layouts. Optional `LOOK AT`, silent `TALK TO`, and deliberate sole
`USE` retain their frozen behavior into unchanged `L02-03`. The world never
responds.

## Sole E1 authority and hard stop

The complete request domain is exactly `{E1}`. E1 begins at the carrier's sole
future `SendAsync` and is consumed by every outcome. There is no E2, retry,
relaunch, second output, fallback, alternate endpoint/model/transport, CLI,
SDK runner, edit, variation, reference input, crop repair, responsive
derivative, reconstruction, or reuse of D1.

Quartermaster may invoke only this command, exactly once, after independently
passing every preflight gate:

```text
C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoLogo -NoProfile -NonInteractive -File "Production Pipeline\First Run\HOST06_V12_E1_STDIN_PARENT.ps1"
```

No other v12 source is an execution entry. Any HTTP, transport, parser,
identity, path, materialization, PNG, physical, layout, accessibility,
provenance, cleanup, protected-boundary, or evidence failure closes E1 and the
Work Order fail-closed. Failure does not authorize another request or later-
address work.

## Frozen `HOST06-GEN-PROMPT-v2`

The carrier contains the exact Science-frozen prompt: strict ASCII / UTF-8
without BOM, LF between content lines, no terminal LF, exact **`5,139` bytes**,
SHA-256 **`561a82faa45aed45d2fca50bfb189bfe6e315682ca1289882361939f79455801`**.
It differs from v1 only in the one frozen responsive-composition line and may
not be paraphrased, supplemented, negatively prompted, or amended from review.

The request is exactly one non-streaming `POST` to
`https://api.openai.com/v1/images/generations` with seven members:
`model=gpt-image-2`, the exact prompt, `n=1`, `size=3840x2160`, `quality=high`,
`background=opaque`, and `output_format=png`. Authorization is process-
environment-only `OPENAI_API_KEY`; only boolean nonblank presence may be
reported. No credential, request body, prompt body, response body, Base64,
private diagnostic, or hidden metadata may persist.

## Exact fresh paths

Only these nine literal paths are controlled:

```text
helper root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-v12-7b3e6b6c-f96d-4f5a-91f9-a02be46e560b
helper DLL=<helper root>\Host06FileIdentity.dll
live root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-v12-15ae0b5e-04a0-43eb-b6be-0f8ab94f9cc9
stage=<live root>\.attempt-E1-053311a9-3849-42d4-bada-3a7f44c3044c.stage
target=<live root>\attempt-E1.png
decision=<live root>\.attempt-E1.review-v1
product root=C:\Users\marti\OneDrive\Desktop\Horizon Archive\Visual Direction\Production Masters\2026-08-10-first-run-host06
product raster=<product root>\host06-stranded-lens-cradle-master-v1.png
product provenance=<product root>\PROVENANCE.md
```

All nine must be absent before the invocation. Old v7-v11 roots, D1, managed
output, parent/sibling enumeration, opaque residuals, repository QA quarantine,
accepted-media pixels, and protected/user state are inaccessible and cannot
supply evidence.

## Helper, response, identity, and materialization gates

The helper remains temp-only C# compilation with exact source `1,693 /
98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97`.
Its only native primitive is `GetFileInformationByHandle` over an already-open
exclusive `SafeFileHandle`. Current-run source/DLL length/SHA, reflection and
P/Invoke shape, one link, no reparse, exact size, no helper close/path/write
method, exact DLL deletion, and nonrecursive empty helper-root deletion must
pass before credential access.

Transport remains redirect-disabled, `ResponseHeadersRead`, ten-minute
timeout, exactly one `SendAsync`, bounded `65,536`-byte reads, response
`1..40,500,000`, canonical Base64 `4..40,000,000`, and decoded PNG
`1..30,000,000` bytes.

The PowerShell 5.1 response parser remains dictionary-safe and case-sensitive:
whole-document strict JSON syntax plus duplicate-key rejection; nonnegative
integral `created`; exactly one data dictionary; one string `b64_json`; exact
known request echoes when present; null-or-absent `url` / `revised_prompt`;
nonnegative known usage integers; and bounded future metadata only after the
whole JSON guard. Canonical Base64 round-trip and decoded-length equality are
mandatory.

Materialization remains create-new stage, one bounded write, `Flush(true)`,
handle-bound identity, exclusive reopen/SHA, same-file pre-move identity,
same-directory no-replace move, stage absence, and final-target identity. Any
collision, alias, substitution, drift, reparse point, extra link, or cleanup
doubt stops fail-closed. A candidate path exists only after these gates pass.

## Technical and private objective review

Review is technical-first, private, and no-reveal. Quartermaster may inspect
only the exact identity-proved E1 target at original resolution while the
carrier waits. It may not render through Markdown, attach a screenshot, export
a preview, retain a rejected pixel, publish a hash/path, or expose the source
to Martin or another surface.

Technical PASS requires exact PNG signature, bounded chunks, allowed types and
order, valid CRC, one IHDR at `3840x2160`, 8-bit truecolor opaque sRGB,
methods zero, exact sRGB ordering and only allowed gamma/chromaticity/physical
metadata, no alpha/palette/transparency/ICC/text/EXIF/time/animation/private or
unknown chunk, consecutive nonempty IDAT, one final empty IEND, no trailing
bytes, one complete zlib stream, exact inflated scanlines/filter bytes,
isolated-browser decode, opacity, and byte caps.

Physical review requires all inherited `PHY-01..12` plus exact v2 bands:

- one complete lens/cradle relation wholly inside `x=.34..70, y=.30..74`;
  tight width `.30..34`, height `.30..38`, center x `.51..53`;
- visible lens nested fraction `>=.80`; cradle tilt `12..35` degrees;
- at least two distinct load/stress contacts and two continuous drainage
  seams;
- dry clearance `>=.02` source height and one continuous dry approach;
- at least three individually legible non-token fragments wholly inside
  `x=.18..32, y=.58..84`, retaining distinct silhouettes at 390 px;
- approach wholly inside `x=.18..40, y=.52..88`, visibly joining fragments to
  the relation without entering activation;
- all essential reading inside `x=.18..70, y=.30..88`; top ten percent free of
  essential fact; same-basin distant context and water only in their bands;
- natural horizon/reflection across `>=.25` inner-lens width; and
- first-person, same-basin, one candidate, no human trace, text, watermark,
  native lesson diagram, purpose claim, or world response.

The decision record is create-new and exact:

```text
HOST06_REVIEW_V1|attemptId=E1|decision=ACCEPT|technical=true|physical=true|layouts=true|accessibility=true|codes=NONE\n
```

Any false or uncertain predicate requires truthful `REJECT` with objective
`PHY-xx`, `LAYOUT`, or `ACCESSIBILITY` code under the carrier grammar. A reject
is deleted under proven identity and has zero canon, product, reuse, prompt,
maturity, screenshot, publication, or reveal authority.

## Runtime, content, and interaction contract after ACCEPT only

Before a passing source, the current runtime remains exact null-first: no
import, DOM node, name, focus stop, notice, action, alt, copy, geometry, or
credit. After ACCEPT and byte-identical import/provenance only, Quartermaster
may populate the already declared Host 06 registry, geometry, six layouts,
factual alt, and seven owner-correct `FRPX05_*` slots. It must replace
`source.attemptOrdinal` with `source.attemptId`, set exact `"E1"`, raise the
source guard to `1..30,000,000`, update focused tests/evidence, and write
`source.enabled=true` last.

Host 05 mastery remains the sole Host 06 gate. `LOOK AT` is physical and
write-free. `TALK TO` is immediate complete silence and write-free. Available,
in-progress, and remediation `USE` call existing `openModelChoiceExercise()`
once. Completed `USE` is read-only. The generic launcher remains exact while
`enabled!==true` and is absent irreversibly after lawful enablement, including
later fail-close.

The source/DOM/focus sequence remains one committed unchanged Host 05 paint,
direct source replacement, logical Host 05 -> Host 06 -> Return order, one
Host 06 `preventScroll:true` focus landing, and one polite Suit/System notice
without replay. Close, Escape, actual miss, primary acknowledgement, fresh
transfer, reload, Meadow return, malformed evidence, and mastered return use
the frozen blueprint recovery order.

Quartermaster owns final wording only after selection. Scene owns observable
physical fact; Pilot owns provisional naming and stranded/correct uncertainty;
Suit/System owns compatibility and interface; the 901 Teacher owns only the
unchanged learning. Machine and Builders own no line.

## Learning, privacy, save, route, and ending invariants

The existing `L02-03` eight primary plus eight transfer scenarios, strict
`16/16 + 16/16 + 2/2`, evaluator, actual-dimension answer-free remediation,
blank retry, fresh transfer, closed-note explanation, ownership, confidence,
and evidence rules remain byte/behavior unchanged. No lesson or answer is
encoded in the scene.

Only sanitized `modelChoiceEvidence` may persist. Credential, response,
Base64, private session, free-form work, focus, timing, media diagnostics,
candidate, attempt, or review state may not enter save or telemetry. Save
schema/version/projection, malformed-state fail-close, no-cross-credit, and
Demo Tour zero-credit isolation remain exact. Packaged runtime has one same-
origin selected-image request and zero external requests, services,
credentials, authority, telemetry, or account coupling.

The one-path rail, Hosts 04/05, Hosts 07+, later rail, READY/NOT YET READY
parity, MH-40, shared RP-012 ending, and exact null deltas remain unchanged:
`cityStateDelta=null`, `worldStateDelta=null`, `externalStateDelta=null`,
`authorityDelta=null`, and `successor=null`. No branch, packet, lesson, answer,
reward, access, identity, authority, recognition, Machine/Builder dialogue,
world response, prior-human trace, hidden-lore answer, RP-013, or post-ending
content is permitted.

## Six-layout and accessibility contract

One selected `<img>` uses `object-fit:cover`, `object-position:50% 50%`, and
the full `3840x2160` source at exactly: desktop `1920x1080`; laptop
`1366x768.375`; narrow `390x219.375`; effective-`200%` `768x432`; retained
`320x180`; and centered `320x180` inside `320x240`. No alternate, duplicate,
derivative, zoom, responsive source, or overlay may repair a source fact.

Each exact layout record retains source, physical, semantic, label, protected,
order, focus, and post-import live fields. Unrounded measurements govern; the
diagnostic lattice is `floor(value*64)/64` and cannot create a pass. Source-
center and relation/approach retention are `>=.995`; activation contains the
complete relation and center, area ratio is `<=1.50`, center clearance is
`>=4 CSS px`, target is `>=44x44 CSS px`, protected intersections are zero,
and separation is `>=96` source pixels / `>=8 CSS px` mapped.

Pointer, touch, Enter, Space, speech-by-name, and switch-like use one native
click path. Focus remains stable within `1/64 CSS px`; forced colors use exact
`3px solid Highlight`; reduced motion removes nonessential timing with equal
state/order/focus/meaning. There is no horizontal escape, clipped required
text, sensory-only meaning, or fabricated alternative text.

## Budgets and provenance

The immutable accepted-media baseline remains exact `17 / 37,410,731` with
manifest digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`.
Only an accepted E1 may yield exact `18` media and aggregate
`37,410,731 + selectedBytes <=67,410,731`.

Affected/global JavaScript remain `<=1,679,393 / 1,703,258`; CSS
`<=119,547 / 119,672`; modules `<=218 / 222`; source maps `0`; cold/warm
decode `<=250ms / <=100ms`; image ready `<=750ms`; attributable/total CLS
`<=.01 / <=.05`; activation `<=2ms`; sampled task `<=100ms`; complete E2E
`<=180s`. A budget failure is HOLD and authorizes no compression, edit,
replacement, retry, or alternate.

Provenance is create-new and records Work Order, this shell, helper source and
dynamic DLL identities, transport, endpoint, model/options, exact prompt-v2
ID/bytes/SHA, exact `attemptId=E1`, selected bytes/SHA, objective conjunctive
PASS, immutable manifest identity, product path, and helper/live cleanup. It
contains no credential, response, Base64, hidden metadata, rejected identity,
or private diagnostic.

## Ordered validation ladder

1. Prove exact authority, patch scope, five retained source identities,
   parser-zero, prompt v2 identity, one `SendAsync`, E1-only strings, forbidden
   old domain absence, Git synchronization, and all nine path predicates.
2. Corroborate the Windows PowerShell `5.1.26100.8875` generic-dictionary
   semantic suite `15/15`; do not rerun the one already completed Mission
   credential-cleared fixture. Independently verify its recorded one child,
   zero credential/request/send/E1 activity, and postflight absence.
3. Prove the pre-generation inert runtime and tests still null-first. Before
   any candidate-bearing product edit, freeze exact required replacement
   `attemptId="E1"`, no `attemptOrdinal`, source `1..30,000,000`, aggregate
   `<=67,410,731`, immutable manifest, no import/DOM/name/action, and unchanged
   launcher behavior.
4. Invoke only the parent once. Prove strict response/Base64/identity/
   materialization, then technical PNG/decode and private original-resolution
   physical/band/layout/accessibility review with exact decision grammar.
5. After ACCEPT only, prove byte-identical import/provenance and populate only
   registry/geometry/layout/copy/alt/test fields; run focused Host 05 -> Host 06
   -> L02-03, learning/privacy/save, and adjacent-route regressions.
6. Run applicable validators, cold full product tests, clean fixture and
   production builds, PBA, served identity, and exactly one isolated
   non-overlapping no-retry E2E. Review all six layouts plus live keyboard,
   focus, forced-color, reduced-motion, containment, and runtime errors; emit
   one machine summary and one independent verifier result.
7. Stop only owned browser/process/ports and restore owned QA fixtures. Prove
   helper, DLL, live, stage, target, decision, and accepted-temp absence; exact
   product identity after acceptance; tracked-clean Git; and synchronized
   workflow-gate identity.

Static strings and arithmetic cannot substitute for live DOM absence, one-hit
dispatch, inertness, focus, decode, served identity, pixel geometry,
performance, or cleanup proof. Candidate-bearing E2E occurs only once after
all earlier gates.

## Permitted files, rollback, and role boundaries

Quartermaster may execute only the retained parent and, after ACCEPT, change
only the exact product raster/provenance, current Host 06 registry and its
direct App/CSS integration, focused Host 06 tests/evidence, and the versioned
Quartermaster report/handoff required by the workflow. Any broader code or
behavior need is a variance to the earliest owner, not silent scope.

Post-rejection cleanup owns only the proven E1 target/decision and empty live
root. Post-import rollback owns only the new Host 06 raster/provenance,
registry/copy/alt/measurement changes, focused tests/evidence, and an empty
product root. No repository reset, recursive broad cleanup, accepted-media
change, planning-history erasure, protected-state access, or user-state change
is permitted.

Mission performs no production invocation, credential read, request
construction, `SendAsync`, E1 consumption, media creation/inspection/import,
product population, preview, browser, E2E, release, maturity advance, later
role, later host, or completion operation.

## Validation evidence and variance ledger

Mission validated strict encoding, exact length/SHA, and parser-zero for all
five retained sources; exact prompt `5,139 / 561a82...`; exactly one carrier
`SendAsync`; and exact E1 fields/paths. The generated-carrier semantic suite
passed `15/15` in Windows PowerShell `5.1.26100.8875` over generic
`Dictionary<String,Object>`, including future metadata, null optional fields,
known echoes/usage, URL/revised-prompt rejection, extra output, missing/wrong
payload, fractional numeric fields, wrong size, duplicate key, and
noncanonical Base64.

Exactly one credential-cleared fixture passed at `PT06_CREDENTIAL_GATE` with
one child, zero credential reads, request constructions, `SendAsync`, API
sends, or E1 consumption. Independent preflight and postflight proved all nine
fresh paths absent. The six-layout synthetic witness passed with relation
center `.51`, water separation `.03/.03`, activation/physical area ratio
`1.06158088235294`, `54.6px` fragment band at 390 px, and full-source
retention `1` at all six layouts.

The Operations process variance remains separate and **OPEN**:
**`UNAUTHORIZED DIVERGENCE / FULL UNTRACKED GIT STATUS ENUMERATED PROTECTED
FILENAMES`**. Mission did not repeat, query, use, waive, merge, cure, close, or
renumber it. All Git status checks in this stage use `--untracked-files=no`.
VR-65 remains separate, opaque, inaccessible, and non-gating.

Repository QA quarantine, protected PDF, training tree, Martin's real browser/
profile/save, hidden lore, accepted-media pixels, old v7-v11 paths, D1,
managed-output roots, opaque residuals, and unlisted user state were not
opened, listed, searched, inferred, changed, moved, or deleted.

## Exact Quartermaster handoff

One fresh Quartermaster reads the full active intake and Quartermaster profile,
then complete `FRWO-005-v13`, `FRVE-005-v11`, `FRCA-005-v11`, this shell,
retained treatment/blueprint/functional/manifest authorities, all five exact
v12/E1 sources, and only the current runtime/test controls they cite.

Quartermaster independently proves exact synchronized Git identity, the five
source identities/parser-zero, prompt identity, one-send/E1-only domain,
dictionary-safe parser, immutable manifest, inert runtime, required future
`attemptId`/30 MB correction, and nine-path absence. It then invokes only
`HOST06_V12_E1_STDIN_PARENT.ps1` once, conducts the private technical-first
review, and either closes on truthful cleanup/HOLD or completes the strict
accepted-source content sequence. No reveal is authorized.

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-005-v1-VR-43 / SOLE
E1 / CENTER-SAFE PROMPT V2 / EXACT ATTEMPT-ID + 30 MB RECONCILIATION / ONE
SEND / NO RETRY / PRIVATE REVIEW / FRESH QUARTERMASTER NEXT`**.
