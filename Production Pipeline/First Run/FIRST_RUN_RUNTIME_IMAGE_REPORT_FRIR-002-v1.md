# First Run Runtime Image Report

Report: `FRIR-002-v1`

Loop / shell: `HA-RI-001-v1` / `FRSH-016-v1`

Asset / address: `host-15` / `FR-03`

Disposition: **INTEGRATION CANDIDATE / FOCUSED PASS / HOST 16 NEXT**

The selected Full HD production master is now the native Crown Counterpoise
world plate at the existing post-Host-14 boundary. Its bytes are unchanged.
Source/provenance law, decoded dimensions, responsive geometry, semantic
target, card alternative text, focus behavior, lesson order, and atomic native
vs generic launcher selection are bound in the runtime. A missing, malformed,
undecodable, or predecessor-incomplete source preserves the existing generic
launcher path.

Fresh evidence:

- exact master `1920x1080`, `Format24bppRgb`, `2,460,258` bytes, SHA-256
  `fc7c8104eca3f365a03d481a964d2bbf1cf0f37b7578e204b3914479e8642e88`;
- focused Host 15 and predecessor-boundary checks `16/16`;
- production build PASS at `237` modules;
- bundle caps PASS at JS `1,774,391`, CSS `121,983`, source maps `0`; and
- emitted raster is byte-identical to the production master.

Evidence reuse:

- related `63/63`, full `1047/1047`, validators `40/40`, and unchanged
  route/save/privacy/ending evidence remain `REUSED` from `FRCE-015-v1`;
- no broad suite, fixture, served journey, full E2E, or repository QA capture
  was rerun; and
- the final aggregate independent release cascade remains reserved.

No lesson, scoring, evidence, save, route, dialogue authority, world response,
ending, image bytes, generation call, or reveal changed. Decision:
`STOP_SUCCESS` for this asset iteration. Host 16 is the next earliest item.
