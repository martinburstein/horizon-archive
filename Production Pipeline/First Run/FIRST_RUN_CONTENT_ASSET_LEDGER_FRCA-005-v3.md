# First Run Content and Asset Ledger - Direct API Pre-Call HOLD

Ledger ID: `FRCA-005-v3`

Stage / stable agent: Quartermaster / `quartermaster`

Disposition: **`HOLD / PRE-HELPER ONE-PROCESS HOST FAILURE / ORDINALS 2-3
PRESERVED / NO MEDIA CANDIDATE / FRCA-005-v3`**

Work Order / decisive viability: `FRWO-005-v7` /
`FRVE-005-v7-VR-03`

Effective shell: `FRSH-005-v1` through `FRSH-005-v1-VR-08`

Effective treatment: `FRDT-005-v1` through `FRDT-005-v1-VR-01`

Effective blueprint: `FRPX-005-v1` through `FRPX-005-v1-VR-01`

Functional predecessor: `FRCE-005-v1-VR-05 / PRODUCTION FUNCTIONAL`

Quartermaster intake source:
`38feede08cce1bb200e8aedeb46cb7b81264eac4`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Date: **2026-08-11**

Exact next recipient: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Quartermaster decision

Quartermaster stopped before helper-root creation and before ordinal `2`.
The final bounded Windows PowerShell `5.1.26100.8875`, 64-bit, one-process
host reached execution through a task-specific inherited environment string
containing only the nonsecret script text. It returned only the permitted
stable result:

```text
TERMINAL_FAILURE / STABLE_LOCAL_FAILURE
helperRootAbsent=true
liveRootAbsent=true
```

The failure occurred before the exact helper root existed. Therefore the
frozen DLL was not compiled or loaded, the environment credential was not
read by the production sequence, the live root was not allocated,
`SendAsync` did not begin, and no API ordinal was consumed. The shell makes a
helper or execution-control failure terminal and does not permit Quartermaster
to retry it. Quartermaster therefore issues **`HOLD`**.

No exception dump, diagnostic body, source text, credential derivative,
request, response, base64, image bytes, native tuple, or opaque value is
retained. Science receives only the stable failure stage and exact absence
booleans.

## Intake and pre-call proof

Before the host action, Quartermaster independently proved:

- exact `HEAD == origin/main == remote main == 38feede08...`;
- tracked worktree and index deltas absent;
- immutable `FRAM-001-v1` file SHA, count, stored byte total, and canonical
  tuple digest exact without opening accepted-media pixels;
- selected-source guard exact for ordinal `2|3`, with ordinal `1` failing
  closed;
- product raster and `PROVENANCE.md` absent;
- source, provenance, physical, activation, protected, layout, seven copy,
  and factual-alt slots exact null-first except frozen label insets `3/5`;
- exact helper and live roots absent; and
- historical ordinal `1` remained consumed and inaccessible while ordinals
  `2` and `3` remained unconsumed.

Quartermaster read the complete required workflow, registry, profile,
`FRWO-005-v7`, decisive `FRVE-005-v7-VR-03`, effective shell, treatment,
blueprint, `FRCE-005-v1-VR-05`, `FRAM-001-v1`, prior content return,
Host 06 continuity/game-on-paper controls, runtime registry/tests, active
photorealistic charter, Premium Art Style Guide, the installed `imagegen`
skill, and its required Image API reference before any production action.

## Pre-call host transport outcomes

The following were pre-call execution-host construction outcomes, not Image
API ordinals:

1. an orchestration JavaScript template parse rejected the first wrapper
   before a shell or PowerShell process began;
2. Windows rejected an encoded-command launch because the command line was
   too long, before `powershell.exe` began;
3. an stdin transport returned without executing the multiline production
   block and left both controlled roots absent; and
4. the corrected inherited-environment ScriptBlock transport reached exact
   PowerShell 5.1 but failed closed with `STABLE_LOCAL_FAILURE` before helper
   root creation.

None read the API key, constructed or serialized a live request, invoked
`SendAsync`, allocated the live root, created an attempt path, or consumed an
ordinal. The first three were rejected before the production host ran. The
fourth is the terminal shell control failure that causes this HOLD.

## Ordinal, cleanup, content, and asset ledger

| Fact | Result |
| --- | --- |
| Historical ordinal `1` | consumed, opaque, inaccessible, unchanged |
| Ordinal `2` | **not started; unconsumed** |
| Ordinal `3` | **not started; conditional and unconsumed** |
| Direct Image API sends | exact `0` |
| Helper root / DLL | absent / absent |
| Live root / ordinal stage-target pairs | absent / all absent |
| Candidate bytes, hash, path, pixels, preview | none |
| Selected raster / `PROVENANCE.md` | absent / absent |
| Registry / copy / alt delta | none; exact null-first state retained |
| Accepted media | immutable `17 / 37,410,731` |
| Managed residual | conservative exact `1`, historical ordinal `1` only |
| VR-65 | separate, opaque, inaccessible, untouched |

No PNG/CRC/IHDR/decode, physical, crop/mapping, accessibility, focused,
related, full, validator, build, PBA, served, preview, or E2E gate ran because
no lawful candidate existed. No rejected or selected image exists and no image
was displayed, rendered, embedded, screenshotted, published, or revealed.

## Variance, protected state, and maturity

Classification: **`REQUIRED CORRECTION / PRE-HELPER ONE-PROCESS EXECUTION
CONTROL / STABLE LOCAL FAILURE / OPEN`**.

The exact helper/API contract itself is not weakened. This HOLD grants no
authority to change helper source, DLL identity, endpoint, model, prompt,
request options, parser, staging, identity, cleanup, ordinal, media, crop,
acceptance, or no-reveal controls. A retry requires fresh Science
adjudication and a new Mission shell variance.

The repository QA quarantine, protected PDF, training tree, Martin's real
browser/profile/save, hidden lore, accepted-media pixels, OS-temp parent,
ordinal-1 residual, managed directory, user work, and VR-65 were not opened,
enumerated for content, changed, or targeted. All inherited OPEN records and
the separate Commandant search-scope record remain separate and OPEN.

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`.

## Exact Science return

One fresh Office of Science Administrator / `office_of_science_administrator`
reads this complete ledger, the synchronized handoff, complete
`FRWO-005-v7`, decisive `FRVE-005-v7-VR-03`, complete effective shell,
treatment, blueprint, `FRCE-005-v1-VR-05`, immutable `FRAM-001-v1`, and the
exact pre-helper host boundary.

Science issues exactly one versioned `POLISH VIABILITY READY`, `REVISE`, or
`HOLD` artifact adjudicating the retained `STABLE_LOCAL_FAILURE` before helper
root creation. It may authorize a corrected no-file one-process host only
through a new Mission shell variance. It may not infer or replay the failed
diagnostic, compile/invoke the helper, allocate the live root, read a
credential, send the API, consume ordinal `2`, inspect media, change product,
run E2E, reveal, advance maturity, close an OPEN record, access a residual or
VR-65, schedule, automate, push, release, or call `FIRST RUN COMPLETE`.
