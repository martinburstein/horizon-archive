# First Run Content and Asset Ledger

Ledger ID: `FRCA-015-v1`

Shell: `FRSH-015-v1`

Build identity: `398a64131fa804223cfbdfc88f2865242bb3e02a`

Disposition: **HOLD / RETURN TO MISSION FOR REPRESENTATION REPLAN**

## Scope and preserved state

Quartermaster executed only the frozen `FRM15` G01 guide-conditioned built-in
render graph. No source was selected, imported, activated, served, or exposed
to the player. `horizon.waterline-ledger.v2` remains disabled and null-first;
the two generic launchers, accepted inventory (`24` files / `154163567` bytes),
runtime behavior, copy, lessons, save, privacy, ending, and product tree remain
unchanged.

Forbidden repository QA images, Martin browser/profile/save state, hidden lore,
archives, PDFs/training material, opaque residual roots, unrelated dirt, prior
rejected pixels, and accepted existing media pixels were not inspected or used.

## Preflight and deterministic controls

- `HEAD == origin/main == 398a64131fa804223cfbdfc88f2865242bb3e02a`.
- Frozen guide spec, guide control, custody control, transform control, and all
  four prompt byte/hash identities matched `FRSH-015-v1`.
- Python `3.12.10`, Pillow `12.2.0`, disk, ports, custody absence, inert v2
  source, accepted manifest, product-scope cleanliness, and proof reserve passed.
- Guide synthetic self-test, validation, build, and verify passed: `4/4`
  deterministic guide operations.
- Custody and transform synthetic self-tests passed, including mutation and
  nonbinary-mask rejection.
- G01 produced and privately input-checked the exact ordered structure,
  material-ID, and protection/layout guides. Their hashes are respectively
  `974dba59c839c2671d47282c30c0a472e4892de6dd76a278a70a36082379dade`,
  `014628650fa332347f6726c804426f1952dbc1246d07a4a068c4ac60ffb22d2e`,
  and `ccd198f6bfa09ec485fbe482b93720011d4710de1d3a2627b7a22a340562d91e`.

## Built-in call ledger and custody

| Call | Frozen operation | Effect | Custody disposition |
| --- | --- | --- | --- |
| `FRM15-C01` | base render from exact R01 prompt plus ordered G01 references | returned one artifact | reconciled only within the known built-in output root, adopted create-new as `FRM15-R01`, inspected, rejected |
| `FRM15-C02` | exact E01 material/process edit from current best plus ordered G01 references | returned one artifact | adopted create-new as `FRM15-E01`, inspected, rejected |
| `FRM15-C03` | exact E02 trace edit from current best plus ordered G01 references | network transport failure | consumed; known output directory reconciliation found no new artifact; no retry |

`FRM15-R01` was `2744979` bytes with SHA-256
`518393d52bc0671c98838cca5f70c912d441e1bec3618eabfa6b9edbb04c6e9d`.
`FRM15-E01` was `2714889` bytes with SHA-256
`554e87aeaa83e94d37658b00857feded6ce048fc4394b47a161194d69261332e`.
Before cleanup the frozen custody verifier passed at five nodes, retained node
bytes `5491701`, and ledger SHA-256
`087d59a0c4e893078f41abd0ea297943b821027d5717d29a84416280d820b5b6`.

## Private hard vectors

Both decoded as recognized RGB PNG with no alpha and no forbidden metadata at
`1672x941`; strict selected dimensions were not claimed and normalization was
not spent on rejected semantics.

`FRM15-R01`:

- PASS: continuous dry foreground; lateral receded water; no biology, writing,
  UI, answer/message/response, protagonist, glow/activation, reward/access, or
  world-change cue.
- FAIL `FACE_CONVEX_BARRIER`: no low nonhuman concave face.
- FAIL `PROCESS_3_INCOMPLETE`: three processes were not independently
  identifiable by construction evidence.
- FAIL `PAIRWISE_INTERLOCK_INCOMPLETE`: horizontal/vertical overlap and two
  interlocks/islands for every pair were absent.
- FAIL `TRACE_REACTION_INCOMPLETE`: the trace did not prove continuity across
  all three processes with three distinct substrate reactions.
- FAIL `SEAM_DAG_INCOMPLETE`: three unequal nonparallel upward recessed seams
  plus a branch/merge were not all unambiguous.
- FAIL `BARRIER_IMPOUNDMENT`, `HUMAN_INFRASTRUCTURE`, and
  `NATURAL_GEOLOGY_SUBSTITUTION`.

`FRM15-E01`:

- PASS: the E01 material/process edit visibly improved process separation and
  interdigitation without regressing dry foreground, lateral water, or the
  clean anti-writing/anti-answer/anti-reward vector; it became search-best.
- FAIL `FACE_CONVEX_BARRIER`, `PROCESS_3_INCOMPLETE`,
  `PAIRWISE_INTERLOCK_INCOMPLETE`, `TRACE_REACTION_INCOMPLETE`, and
  `SEAM_DAG_INCOMPLETE`.
- FAIL `BARRIER_IMPOUNDMENT`, `HUMAN_INFRASTRUCTURE`, and
  `NATURAL_GEOLOGY_SUBSTITUTION`.

One hard failure rejects a candidate, so displacement/removal, one-box alias,
color-only, guide-leak, center-crop, crop-shift, mask-edge, six-layout,
accessibility, served, and E2E gates were correctly not advanced.

## Stop, budget, and causal diagnosis

```yaml
guide_families: {used: 1, executable_ceiling: 1, hard_max: 2}
stochastic_calls: {used: 3, initial: 3, earned_unused: 2, reserve_unused: 1}
deterministic_operations: {used: 4, hard_max: 12}
private_candidate_inspections: {used: 2, hard_max: 8}
selected_source: null
product_imports: 0
browser_or_e2e: 0
final_product_and_release_proof: FULL_PROTECTED
```

`C02` proved the material edit mechanism can improve one mandatory group, but
the G01 family persistently resolves the target as a convex engineered/natural
barrier rather than the required low nonhuman concave constructed-process
sentence. `C03` produced no immediately preceding candidate, so `C04/C05`
cannot satisfy their earned-call condition. The reserve is barred because more
than one localized hard defect remains. Another same-family call has low
expected marginal player value and would not lawfully address the categorical
topology/substitution failures.

## Cleanup and limitation

The two rejected raster identities and vectors were recorded above before
exact hash-reproved leaf deletion from custody. Rejected custody raster
residual count is zero. G01 remains as four custody files / `32541` bytes;
the append-only ledger preserves the rejected node identities and pre-cleanup
verified chain. The two now-empty leaf directories could not be removed because
OneDrive denied the nonrecursive directory deletion; they contain zero files or
bytes and are explicitly recorded as harmless residual containers. Tool-output
originals were not deleted. Product/media rollback is the unchanged null-first
state; no product cleanup or revert is required.

Limitation: no accepted major source exists for the bounded slot, so production
content is not complete. Copy, alt text, geometry, provenance, registry source
fields, inventory count, and source-dependent test expectations remain
unpopulated rather than guessing from guide controls or rejected pixels.

## Exact handoff

Return to the Mission Captain for a versioned variance/replan that changes the
structural/material representation mechanism and, if still warranted, supplies
a new frozen guide specification and shell. Do not retry `C03`, spend
`C04-C06`, reuse rejected pixels, or activate the v2 source under the current
shell. Image Specialist receives no content-complete candidate.
