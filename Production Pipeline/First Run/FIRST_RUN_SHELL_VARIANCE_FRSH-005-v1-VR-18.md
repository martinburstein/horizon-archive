# First Run Shell Variance - Disposable Parent Grammar Localization

Variance ID: `FRSH-005-v1-VR-18`

Disposition: **`FIRST RUN SHELL READY / DISPOSABLE PARENT GRAMMAR
LOCALIZATION-ONLY PASS FROZEN / NO BOOTSTRAP, CHILD, OR API / FRESH SCIENCE
REQUIRED / FRSH-005-v1-VR-18`**

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / current Science return: `FRWO-005-v7` /
`FRVE-005-v7-VR-15`

Effective prior shell: `FRSH-005-v1` through `FRSH-005-v1-VR-17`

Quartermaster return: `FRCA-005-v4`

Effective treatment / blueprint: `FRDT-005-v1-VR-01` /
`FRPX-005-v1-VR-01`

Functional report: `FRCE-005-v1-VR-05`

Mission source inspected:
`d0d183dc1d7182089d42819f4ea9a9ed5f5bf571`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Date: **2026-08-11**

Exact next recipient: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Mission adjudication

Mission accepts `FRVE-005-v7-VR-15` only as an honest pre-body parser HOLD.
The sole `47,147`-character source reached the frozen ReadToEnd bootstrap, but
`ScriptBlock.Create` rejected it before the parent body or child began. Its
unbounded host parser formatting is rejected evidence. It cannot be copied,
reinterpreted, resumed, repaired, or followed by a child in that completed
Science context.

The retained evidence localizes at least one grammar class: a generic static
invocation was written without token separation after unary `-not`. The exact
failed parent source and its construction expression were disposable and are
not retained in the repository. Therefore Mission cannot honestly claim that
repairing one reported token repairs the whole source, nor can it freeze an
exact corrected source identity from current evidence.

Mission introduces one syntax-localization-only Science subgate. Fresh Science
constructs one prospective complete parent candidate in memory, validates its
non-secret carrier and text identities, parses it locally without creating or
invoking a script block, and preserves either bounded syntax tuples or the
exact parser-clean candidate as a canonical Base64 carrier in its report. That
preserved carrier gives the next Mission an inspectable, immutable source from
which a later one-run shell may be decided.

This variance changes no Windows PowerShell executable, `FRSH-005-v1-VR-17`
bootstrap, `151`-character arguments, production launcher, combined source,
pre-helper, production tail, child classifier, credential rule, API request,
PNG predicate, ordinal rule, product path, or cleanup semantics.

Variance classification: **`REQUIRED CORRECTION IN PROGRESS / DISPOSABLE
PARENT SOURCE WAS NOT RETAINED / BOUNDED PARSER-ONLY LOCALIZATION AND EXACT
CANDIDATE CARRIER REQUIRED BEFORE ANY FURTHER EXECUTION`**.

## Frozen parser-only Science pass

Fresh Science builds exactly one prospective parent candidate as one nonempty
`System.String` in memory. It may correct only disposable-parent grammar and
result-classification code. It may not alter either immutable runtime carrier
or any decoded production byte. Before parsing, Science proves:

```text
strictAscii=true
lfOnly=true
finalLf=true
launcherCarrierOccurrences=1
combinedCarrierOccurrences=1
launcherCarrierAsciiLength=2668
launcherCarrierAsciiSha256=02c0219793c0aeb6e8e989aac9f9ee52373598cdbc2a33da4f2c54b069796af9
combinedCarrierAsciiLength=36060
combinedCarrierAsciiSha256=c8926687184ecff422bb29fe26a7a9e3bd3d6273c75fceb56746734de89b19f5
```

The parent candidate must preserve the exact closed SR01 allowlist, one-child
maximum, exact PT06 diagnostic-to-`CREDENTIAL_ABSENT` mapping, all zero
counters, controlled-path postflight facts, and no-retry rules from
`FRSH-005-v1-VR-16` and `FRSH-005-v1-VR-17`. It contains no credential value,
response, media byte, path discovery result, residual value, or opaque value.

Science then performs exactly one local grammar operation:

```powershell
$tokens = $null
$parseErrors = $null
[void][System.Management.Automation.Language.Parser]::ParseInput(
  $parentCandidate,
  [ref]$tokens,
  [ref]$parseErrors
)
```

This operation runs in the Science controller process. Science does not call
`ScriptBlock.Create`, `Invoke`, `&`, dot-source, `powershell.exe`,
`Process.Start`, `Start-Process`, or any equivalent execution path. It does
not decode or invoke the launcher or combined source. It creates no helper,
child, fixture, source file, output file, live root, ordinal path, product
path, request, response, raster, provenance, image, or pixel.

Science records the candidate's exact ASCII character count, ASCII byte count,
lowercase SHA-256, final-LF fact, parser error count, and token count. It does
not record token text.

If parser errors exist, the report records only these bounded scalar fields for
at most the first `16` errors in parser order:

```text
ordinal=<1..16>
errorId=<parser ErrorId ASCII token>
startLine=<positive integer>
startColumn=<positive integer>
endLine=<positive integer>
endColumn=<positive integer>
```

The report also records exact total error count and `reportedErrorCount`.
No message, source text, source excerpt, offending token, path, stack, command
echo, exception rendering, or unbounded stderr is retained. Any non-ASCII
`ErrorId`, nonpositive coordinate, inverted extent, or report beyond the first
`16` errors is rejected. Science returns `HOLD / PARENT GRAMMAR ERRORS
LOCALIZED / MISSION REPAIR REQUIRED`, commits only its variance and handoff,
does not push, and routes to one fresh Mission Captain.

If and only if parser error count is exact `0`, Science additionally canonical-
Base64-encodes the complete ASCII parent candidate and records the uninterrupted
carrier in one fenced block in its variance. It proves exact RFC 4648 grammar,
decode/re-encode equality, decoded byte count equal to candidate ASCII byte
count, decoded SHA equal to candidate SHA, strict ASCII, LF-only, final-LF,
and parser-zero after decoding the recorded carrier. No alternate carrier or
source representation is authoritative.

A zero-error result is only **`POLISH VIABILITY READY / DISPOSABLE PARENT
GRAMMAR SUBGATE PASSED / MISSION IMMUTABLE-CARRIER FREEZE REQUIRED`**. It is
not credential, child, classifier, transport, API, generation, media, product,
or release evidence. Science commits only its variance and synchronized
handoff, does not push, and routes to one fresh Mission Captain. Mission must
independently inspect and freeze that exact carrier before any later fresh
Science execution can be authorized.

Science performs no retry. A construction failure, identity failure, parser
exception, nonexact report, missing carrier on parser-zero, or any execution
attempt is `HOLD`.

## Unchanged production identities and semantic contract

The immutable runtime identities remain exact:

```text
launcher 2001 / 96feaf7e62fa89e8c80cc46d38425d465cf845ffbd426405a75c73c056314212
prefix 976 / 5cd257c94bcd70b8d6ada4e0b561b2a14ed52fd9459146b1269dc93ce1bdc7d1
tail 26068 / e9815ce8f988bca0f1f9bd14a7ba50cfa4935b3d7a47492c95bfa90986b7070c
combined 27044 / 015dfd96befad29793892f1e15dc9ff4362ff8cec0ae4ce7b9c45b5da9e125f3
helper source 1693 / 98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97
helper DLL 4096 / 39e85b32b7f8437c2b5732e26093ca5bd9a9182b048c411e9dc5660ba03f10c9
```

The later one-run acceptance contract remains unchanged and is not exercised
here:

```text
outcome=ACCEPTED_NO_REQUEST_STOP
earliestStage=PT06_CREDENTIAL_GATE
code=CREDENTIAL_ABSENT
childExit=87
childInvocations=1
credentialValueReads=0
requestConstructions=0
sendAsyncCalls=0
directSends=0
ordinalsConsumed=0
helperRootAbsent=true
helperDllAbsent=true
liveRootAbsent=true
ordinal2StageAbsent=true
ordinal2TargetAbsent=true
ordinal2DecisionAbsent=true
ordinal3StageAbsent=true
ordinal3TargetAbsent=true
ordinal3DecisionAbsent=true
productRootAbsent=true
productRasterAbsent=true
productProvenanceAbsent=true
scienceFixtureRootsAbsent=true
```

The production child diagnostic still has no `code` field. Product-root,
product-raster, and provenance absence remain independent parent postflight
facts. Nothing in this syntax-only variance authorizes a child or establishes
the semantic result above.

## Preserved boundaries, validation, rollback, and handoff

Historical ordinal `1` remains consumed, opaque, inaccessible, and unchanged.
Ordinals `2` and `3` remain unstarted and unconsumed. Mission credential value
reads, request constructions, `SendAsync` calls, direct sends, ordinals
consumed, parent executions, and child invocations are exact `0`. No API,
generation, response, media byte, image, pixel, product raster, or provenance
exists.

The player address remains `FR-03 / Chapter II - Drowned Archive / Host 06`.
The exact Host 05 handoff, one dry same-basin Stranded Lens Cradle, sole
unchanged `L02-03`, next Drowned boundary, one-path rail, both MH-40 outcomes,
equal dignity, all null deltas, shared RP-012 ending, and `successor=null`
remain immutable. No branch, packet, lesson, Host 07, reward, access, identity,
authority, world response, hidden-lore answer, successor, RP-013, or
post-ending content exists.

The immutable manifest and accepted-media bytes/pixels remain untouched.
Repository QA quarantine, protected PDF, training tree, Martin's real browser/
profile/save, hidden lore, OS-temp parent, ordinal-1 residual, real managed
directory, user work, VR-65, and every opaque residual remain protected. All
inherited OPEN records remain separate and OPEN.

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`.

Mission changes only this variance and `NEXT_INSTANCE_HANDOFF.md`. Validation
is read-only authority review, exact contract comparison, diff, and Git.
Mission runs no parser candidate, bootstrap, parent, child, helper, fixture,
API, generation, media, pixel, product, test, build, browser, or E2E operation.

Rollback removes only this variance and restores the immediately preceding
handoff by explicit content. It never resets the repository or touches
product, media, controlled paths, protected state, or user work.

After synchronization, one fresh Office of Science Administrator /
`office_of_science_administrator` reads the complete active intake, full
Science profile, this complete variance, complete `FRVE-005-v7-VR-15`,
complete `FRSH-005-v1-VR-17`, complete `FRSH-005-v1-VR-16`, and the exact
cited effective controls. It performs only the parser-localization pass above,
commits its variance plus synchronized handoff, and does not push.

Science may not execute the candidate; invoke a bootstrap, parent, child, or
helper; read or change a credential; route to Quartermaster; generate or call
an API; allocate the live root; construct a request; call `SendAsync`; consume
ordinal `2`; inspect media/pixels; change product/tests; run E2E; reveal;
advance maturity; close an OPEN record; access a residual or VR-65; schedule;
automate; push; release; or call `FIRST RUN COMPLETE`.

Mission Captain signs **`FIRST RUN SHELL READY / PARENT GRAMMAR
LOCALIZATION-ONLY SUBGATE / NO EXECUTION / FRESH SCIENCE REQUIRED /
FRSH-005-v1-VR-18`** from exact source
`d0d183dc1d7182089d42819f4ea9a9ed5f5bf571`.
