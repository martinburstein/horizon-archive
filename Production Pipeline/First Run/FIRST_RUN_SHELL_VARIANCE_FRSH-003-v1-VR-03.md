# First Run Shell Variance Routing - Browser-Resolved Host 05 Contract

Variance ID: `FRSH-003-v1-VR-03`

Disposition: **`HOLD / RETURN TO SCIENCE THEN TACTICAL / FRSH-003-v1-VR-03`**

Stage / owner: Mission Captain / `mission_captain`

Governing shell: `FIRST RUN SHELL READY / FRSH-003-v1`

Prior Mission reissue: `FIRST RUN SHELL READY / FRSH-003-v1-VR-02`

Science clarification under review: `POLISH VIABILITY READY /
FRVE-003-v1-VR-01`

Tactical clarification under review: `PLAYER EXPERIENCE READY /
FRPX-003-v1-VR-01`

Functional evidence: `HOLD / VR-02 LIVE CONTRACT VARIANCE / FRCE-003-v1`

Work Order: `FRWO-003-v1 / Sixfold Weir`

Mission control source inspected:
`17d0d268b707cb27d560ba2f4085b8313c42862b`

Frozen repaired candidate:
`7e85154abd8dbf116c4bb84ca66afd859903d750`

Frozen product/test/manifest predecessor:
`a9776e337f1820776864a5690332c364d0fb2556`

Frozen corrected harness predecessor:
`bf58e528bc6ce4088f81f2c782ce2895259ab9fd`

Released rollback baseline:
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`

Date: **2026-08-10**

## Decision

Mission sustains the cycle `HOLD`. The VR-02 repair proved the player-facing
minimum-size, containment, rendered-image mapping, forced-color focus-visible,
reduced-motion, overlap, retention, and identity outcomes, but the live run
also proved that four frozen acceptance assumptions do not describe the
browser-resolved product.

Those contradictions are planning-contract defects. They do not authorize a
product reorder, another product repair, a tolerance, rounding, a second live
run, or release acceptance. The earliest owners are:

1. **Office of Science Administrator** for percentage-layout quantization and
   the inner-content versus outer-border label coordinate convention; then
2. **Tactical Operations Specialist** for the released action order and the
   actual reverse-tab graph.

Science is the sole exact next owner. Tactical may begin only after Science
issues a compatible `READY`. Mission must reconcile both returns before Combat
may edit or execute anything. Quartermaster and every later role remain
blocked, and no maturity cell advances.

## Live facts accepted only as variance evidence

Mission accepts the following observations as truthful evidence of the frozen
candidate, not as a passing release ladder:

- Repaired candidate `7e85154abd8dbf116c4bb84ca66afd859903d750`
  preserves physical declarations `left 45% / top 75% / width 20% / height
  25%`, all seven placeholders, source media, learning, save, route, later
  rail, and ending.
- Focused `68/68`, related `74/74`, cold full `972/972`, validators `40/40`,
  builds `217/57`, JavaScript `1,666,665`, CSS `119,247`, modules `217`, media
  `17 / 37,410,731`, and served identities `2/2` passed before live execution.
- The one authorized E2E completed the full journey and failed closed only at
  its final six-layout aggregate. It was not rerun; no machine live summary
  was emitted and no verifier ran.
- `.scene-world-content` and `.scene-art` have equal live boxes. The actual
  rendered-image source transform, source-anchor containment, retention above
  `0.95`, exact zero overlap, and no horizontal overflow pass.
- The retained semantic activation is exact `62.796875 x 44`, bottom
  anchored. Its visible label is contained, has no scroll overflow, and
  measures `56.796875 x 38` inside the `62.796875 x 44` button.
- Genuine keyboard `Tab` arrival produces Host 05 `:focus-visible` with exact
  `3px solid` browser-resolved system `Highlight`. Reduced-motion and target
  identity remain stable.
- At desktop, raw arithmetic predicts physical `x 888.5671875 / w
  285.73125`, while the browser-resolved DOM box is `x 888.5625 / w
  285.71875`. The CSS percentage declarations remain exact even though those
  two coordinate representations are not byte-identical.
- The label's inline `2px` positioning is inside the button's `1px` border,
  producing a measured `3px` inset from the outer border. At retained size the
  button is `x 144.296875 / y 134.625 / w 62.796875 / h 44` and the label is
  `x 147.296875 / y 137.625 / w 56.796875 / h 38`.
- The existing enabled action order is `LOOK AT -> USE -> TALK TO`, not the
  VR-02 expected `LOOK AT -> TALK TO -> USE`.
- One genuine `Shift+Tab` from recovered Host 05 does not focus inventory
  `Return: Glass Meadow`. The following genuine `Tab` returns to Host 05 and
  proves its focus-visible treatment, but the asserted predecessor is false.

Cleanup is accepted as complete. These facts do not convert the failed
aggregate into a pass and do not consume or create another E2E authority.

## Four-variance adjudication

| Live finding | Classification | Earliest owner / required result |
| --- | --- | --- |
| Browser-quantized physical DOM box differs from unquantized percentage arithmetic | `REQUIRED CONTRACT CORRECTION - OPEN` | Science must distinguish exact authoring registration from exact browser-resolved measurement without tolerance or rounding. |
| Inline `2px` label position measures `3px` from the outer edge of a `1px` bordered button | `COORDINATE CLARIFICATION - OPEN` | Science must name the reference box and freeze the corresponding exact containment dimensions. |
| Existing enabled order is `LOOK AT -> USE -> TALK TO` | `BLUEPRINT CORRECTION - OPEN` | Tactical must record the actual released order; no player-facing reorder is authorized. |
| Assumed inventory-return reverse-tab predecessor is false | `BLUEPRINT CORRECTION - OPEN` | Tactical must record the actual focus graph and genuine key sequence that returns to Host 05; programmatic focus is forbidden. |
| Retained semantic activation is exact `44px` high | `REQUIRED CORRECTION - CLOSED` | Preserve the VR-02 bottom-anchored semantic repair exactly. |
| Label is contained with no scroll overflow | `PLAYER-FACING THRESHOLD - PASSING FACT` | Preserve containment; coordinate naming may change, geometry may not. |
| Genuine Host 05 `:focus-visible` is exact `3px solid Highlight` | `PLAYER-FACING THRESHOLD - PASSING FACT` | Preserve real keyboard proof and exact computed treatment. |
| Learning/save/route/world/media/later rail/ending | `PRESERVED` | No change authorized or inferred. |

## Frozen player-facing contract

No planning return may weaken, round, exchange, or reinterpret any of these
outcomes:

- physical Host 05 declarations remain exact `45/75/20/25`, identical at
  canonical and narrow, on the same dry six-branch relationship;
- semantic activation remains bottom anchored, width/left unchanged, and
  actually `>=44 x 44 CSS px` at every required layout;
- source registration begins at the actual `.scene-art` content box under the
  real cover/object-position transform, retains `>=0.95`, contains the nominal
  source anchor, and has exact zero Host 04/return overlap;
- visible identity/state remains wholly contained with no scroll or page
  overflow;
- forced colors remain active before genuine keyboard traversal, Host 05 must
  match `:focus-visible`, and the computed outline remains exact `3px solid`
  system `Highlight`; reduced-motion durations/delays remain zero;
- physical order remains Host 04, Host 05, return ridge; released action
  behavior remains `LOOK AT`, sole `USE`, and silent `TALK TO` with no action
  reorder or dispatch introduced for test convenience;
- pure sanitized availability, completed read-only `USE`, generic-launcher
  removal, and all focus/recovery/announcement meanings remain unchanged;
- the seven meaning slots remain exactly `FRPX03_UNSEEN_INTERFACE`,
  `FRPX03_AVAILABLE`, `FRPX03_IN_PROGRESS`, `FRPX03_MISSED`,
  `FRPX03_MASTERED`, `FRPX03_RETURNED`, and `FRPX03_NEXT_BOUNDARY`;
- `L02-02`, evaluator, sanitizer, evidence/privacy, save, Host 04, `L02-03`,
  route, later rail, both equal MH-40 outcomes, null deltas, and
  `successor=null` remain unchanged; and
- PBA, offline, performance, immutable-media, external GUID-root,
  machine-summary, verifier, cleanup, and single-E2E gates remain exact.

The arithmetic nominal rectangle may remain a diagnostic record, but it may
not override the actual browser-resolved DOM box or be used to waive a failed
player-facing threshold. Conversely, browser quantization may not be called a
failure when exact percentage declarations and every frozen physical/source
invariant remain truthful. Science must define the fail-closed distinction.

## Exact Science return

Science must issue one `FRVE-003-v1-VR-02` as `POLISH VIABILITY READY` or
`HOLD`, documentation only. Its one implementable, non-weakened goal is:

> Freeze an exact acceptance model that keeps the authored physical Host 05
> registration at `45/75/20/25`, measures its actual browser-resolved box and
> source mapping at full precision, preserves the exact bottom-anchored
> `>=44 x 44` semantic activation and contained `56.796875 x 38` label, and
> distinguishes inner `2px` positioning from the resulting outer-border
> `3px` inset without tolerance, rounding, geometry change, or threshold
> relaxation.

Science must state, for every required layout:

1. which invariants are proven from the exact CSS declarations and computed
   styles;
2. which rectangles are browser-resolved DOM facts and how source registration
   derives from those actual rectangles;
3. how equality/failure is evaluated without comparing a quantized DOMRect to
   an independently unquantized arithmetic expectation;
4. whether `2px` names the label's inner positioning edge and `3px` names the
   expected outer-border distance, including the exact `1px` border and
   retained `56.796875 x 38` label result; and
5. how the contract continues to fail closed for any real `44px`, retention,
   overlap, source-anchor, label-containment, or overflow defect.

Science may not authorize code/test/harness/manifest edits, another E2E,
product geometry, CSS/module/media changes, copy, learning/save/route/ending,
or a tolerance. If an exact browser-resolved contract cannot preserve all
frozen outcomes, Science must return `HOLD`.

## Tactical return after Science READY

Only after compatible Science `READY`, Tactical must issue one
`FRPX-003-v1-VR-02`, documentation only. Tactical must:

- freeze the actual enabled semantic order as Host 05, `LOOK AT`, `USE`,
  `TALK TO`, followed by whatever focusable controls the live DOM truthfully
  exposes; preserve the action meanings and authorize no reorder;
- identify the actual lawful element reached by genuine `Shift+Tab` from
  recovered Host 05 and the exact genuine keyboard sequence that returns to
  Host 05;
- require forced colors and reduced motion to be active before traversal,
  forbid pointer or programmatic target focus as acceptance evidence, and
  preserve exact Host 05 `:focus-visible` plus `3px solid Highlight` proof;
- keep actual `.scene-art` source mapping and Science's resolved coordinate
  convention; and
- update the acceptance/summary field contract so it records observed action
  order, predecessor identity, exact keys, active element, focus-visible,
  computed outline, and unchanged identity/state without assuming a cyclic
  inventory-return predecessor.

Tactical must derive the real sequence from read-only current controls and the
accepted live record. It may not run a browser/E2E, change product/test/
harness/manifest, invent a predecessor, or simplify the tab graph by changing
player behavior.

## Authorization, validation, and rollback

Current authorization stops after Mission documentation. Combat may not edit
product, tests, E2E, manifest, report, summary, or verifier and may not run any
test, build, preview, browser, validator, verifier, or complete journey.
Quartermaster, Image Specialist, and Intelligence may not begin.

Candidate `7e85154abd8dbf116c4bb84ca66afd859903d750` remains frozen. Product
predecessor `a9776e337f1820776864a5690332c364d0fb2556`, harness predecessor
`bf58e528bc6ce4088f81f2c782ce2895259ab9fd`, released rollback
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`, all seven placeholders, and
`FRRC-002-v1` external-root/live-summary/single-E2E gates remain exact. This
decision executes no rollback.

Mission read the current handoff, its profile, both prior shell variance
decisions, the Science and Tactical VR-01 clarifications, and complete latest
Combat evidence. It made no product, test, harness, manifest, media, copy,
learning, save, route, world, ending, maturity, map, scoreboard, automation,
schedule, user-state, protected-path, or hidden-lore change and ran no product
validation.

## Mission signature and exact next action

Mission Captain signs **`HOLD / RETURN TO SCIENCE THEN TACTICAL /
FRSH-003-v1-VR-03`** from source
`17d0d268b707cb27d560ba2f4085b8313c42862b`.

The **sole exact next action** is Office of Science Administrator: read this
decision, `FRVE-003-v1-VR-01`, `FRSH-003-v1-VR-02`,
`FRPX-003-v1-VR-01`, and complete `FRCE-003-v1`; then issue exactly one
`FRVE-003-v1-VR-02` `POLISH VIABILITY READY` or `HOLD` satisfying the exact
Science goal above. Do not change or execute product/test/harness/manifest,
run E2E, authorize Tactical/Combat/Quartermaster, weaken a threshold, or touch
copy/media/learning/save/route/world/ending/maturity/protected/user state.
