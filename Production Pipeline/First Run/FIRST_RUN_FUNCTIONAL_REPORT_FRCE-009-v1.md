# Horizon Archive First Run Functional Report - Host 09

Report ID: `FRCE-009-v1`

Stage / stable agent: Combat Engineer / `combat_engineer`

Disposition: **`PRODUCTION FUNCTIONAL / NULL-FIRST HOST 09 / FIVE ATTEMPTS UNCONSUMED`**

Shell / blueprint: `FRSH-009-v1` / `FRPX-009-v1`

Source: `237abb01`

## Implementation

Added one dedicated inert `floodedChoir.js` registry/controller, focused tests,
and minimal App/CSS integration. Source, provenance, copy, alt, geometry, six
layouts, decode, attempt, and media URL remain null/false. No generated file is
imported. Runtime therefore renders the released Host 08 and existing generic
Text/Speech launchers exactly as before.

The future accepted path is fail-closed: exact Host 08 plus sanitized `L03-03`,
five prompt identities, path/bytes/SHA/provenance, source dimensions/format,
complete physical rectangles, six-layout records, and decoded image must all
agree. Enabled is the irreversible bypass switch; enabled-invalid hides the
native host and never restores generic Text or Speech buttons.

The declared native USE flow opens unchanged Text Analysis until strict
mastery, then unchanged Speech Workloads, then becomes read-only. LOOK/TALK
write no evidence. Dual-evidence state mapping, no save/private host field,
forced-color focus, semantic target, and exact null-first absence are tested.

## Validation

```text
focused Host08/Host09/Text/Speech = 24/24 PASS
production build = 223 modules PASS
full suite after production build = 999/999 PASS
minified JS = 1,706,407 bytes PASS
minified CSS = 119,635 bytes PASS
source maps = 0
accepted media unchanged = 19 / 70,136,520
```

An initial concurrent full-suite/build/fixture invocation produced one
`dist/assets` missing-directory failure because the fixture build replaced the
production output while the full suite was reading it. This was a gate-order
race, not a product defect. The required sequential production-build then full-
suite rerun passed `999/999` and is the accepted evidence.

No browser/E2E ran because no selected source exists. No credential, live
generation, candidate, pixel, product media, or protected path was accessed.

## Placeholder and rollback ledger

Quartermaster alone may populate the declared source/provenance/rectangles/
layouts/copy/alt after the first private PASS. Image Specialist may alter only
runtime presentation code/config with selected bytes immutable. Rollback is the
exact new controller/test plus bounded App/CSS/report/handoff delta.

Hosts 01-08, all accepted media, lessons, saves, route, RP-012,
`successor=null`, protected QA, OPEN records, and VR-65 remain unchanged.

Combat signs **`PRODUCTION FUNCTIONAL / FRCE-009-v1 / ROUTE FRESH QUARTERMASTER`**.

