# Horizon Archive First Run Work Order - Host 08 Severed Relay Spine

Work Order ID: `FRWO-008-v1`

Stage / stable agent: Operations Planning Major / `operations_planning_major`

Disposition: **`WORK ORDER READY / HOST 08 SEVERED RELAY SPINE / IMAGEGEN CLI / FIVE TOTAL SEQUENTIAL ATTEMPTS MAXIMUM`**

Baseline / continuity / predecessor: `FRPB-001-v5` / `FRCL-008-v1` /
`FRAB-007-v1`

Address: `FR-03 / Host 08`, immediately after released Host 07 and before
missing Host 09.

## Outcome and maturity

After exact Host 07 mastery, the player follows the interrupted wash-channel
continuity to one dry reachable nonhumanoid fallen field element. Its stacked
service laminates have collapsed sideways; one external continuity ends in a
fracture and a distinct continuity remains materially pressure-bearing toward
the basin wall. LOOK is physical/write-free, TALK silent, and sole USE owns
unchanged `L03-03` Offline Client Bridge.

Current: Host 08 missing; `FR-03` physical `FR0`, one accepted compression /
three exact / eight missing. Target: Host 08 exact/released while address-level
physical maturity remains `FR0` and Hosts 09-15 remain missing.

## Exact five-attempt CLI boundary

Domain is `{H8-1,H8-2,H8-3,H8-4,H8-5}`, all unconsumed. The only later
generator is the installed `image_gen.py`, `35266` bytes, SHA-256
`c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05`,
`generate`, `gpt-image-2`, `n=1`, `3840x2160`, high-quality opaque PNG,
no augmentation, no force.

Calls are sequential. Nonzero exit consumes and stops. Objective rejection
plus exact identity-conditioned cleanup alone enables the next fixed prompt.
First PASS stops. No sixth call, rerun, batch, edit, input/reference image,
mask, alternate model, direct REST/SDK runner, script edit, or reveal exists.

Mission must freeze one fresh GUID OS-temp root and exact `h8-1.png` through
`h8-5.png` children. Root/current/later/product absence, ordinary/nonreparse/
one-link identity, sole-child set, strict technical review, candidate hash,
identity reproof, exact rejection deletion, selected create-new/no-replace
copy, and final nonrecursive root deletion are mandatory. Any ambiguity stops.

## Product and runtime scope

At most one accepted byte-identical raster and provenance may enter:

```text
Visual Direction/Production Masters/2026-08-12-first-run-host08/
  host08-severed-relay-spine-master-v1.png
  PROVENANCE.md
```

Combat first builds null-first fail-closed Host 08 source/provenance/geometry/
copy/alt/state tests. Exact Host 07 plus strict `L03-01` and `L03-02` mastery
is the sole predecessor. Before accepted enablement the existing generic Client
Bridge launcher remains. Accepted enablement removes it irreversibly; invalid
later guards hide Host 08 without restoring a bypass. Sole USE opens unchanged
`L03-03`; completed USE is read-only. After `L03-03`, the current generic Text
Analysis continuation remains unchanged and Host 09 remains absent.

## Required physical and presentation evidence

A candidate must prove all `FRCL-008-v1` relations plus normalized rectangles
for the complete laminate relation, fracture continuity, pressure continuity,
Host 07 handoff, dry outer-ridge approach, damaged exclusion volume, semantic
target, label, and protected regions. All essential relations must be visible
and distinct across desktop, laptop, narrow, effective-200%, 320x180, and
320x240 centered-cover layouts.

Minimum acceptance: `>=95%` complete-relation retention; both continuity
centers, fracture, approach, and physical center visible; native target
`>=44x44`; label contained with `>=8px` focus/protected separation; zero target
overlap with damaged volume/water/Host07/return route/Crown; factual alt; forced
color and reduced-motion parity. Any ambiguity is objective rejection.

## Performance, validation, rollback

Candidate is strict static RGB/RGBA fully opaque PNG, `1..30,000,000` bytes.
Accepted aggregate media `<=83,320,925`. Affected/global JS `<=1,702,345 /
1,726,000`, CSS `<=119,900`, modules `<=222`, maps `0`, no dependency change;
decode cold `<=300ms`, warm `<=120ms`, handler `<=2ms`, full E2E `<180s`.

Required ladder: authority/prompt/CLI/key-boolean/path/ledger preflight;
focused Host07/Host08/ClientBridge tests; route/save/privacy/accessibility and
ending regressions; full tests and validators; production/TD-012 builds; PBA,
media and served identity; isolated external-QA-root E2E through Host 08 and
MH-40; six-layout/keyboard/focus/forced-color/reduced-motion review; exact
cleanup, ports, ancestry, and Intelligence release.

Rollback removes only exact Host 08 additions and shell-owned scratch. Hosts
01-07/media, lessons, saves, route, world, ending, and protected state are not
targets.

## Science questions

Science must independently prove CLI/dependency/custody viability; five-prompt
schema; exact physical/geometry/layout thresholds; Client Bridge ownership and
privacy; null-first state; performance/media budgets; validation; rollback;
and explicit external QA root enforcement. It must issue READY, REVISE, or HOLD
with zero live API/media activity.
