# Horizon Archive First Run Functional Report - Host 08

Report ID: `FRCE-008-v1`

Stage / owner: Combat Engineer / `combat_engineer`

Shell / blueprint: `FRSH-008-v1` / `FRPX-008-v1`

Disposition: **`PRODUCTION FUNCTIONAL / HOST 08 INERT CANDIDATE / ROUTE QUARTERMASTER`**

Code candidate: `c3050ce1523ae3a02d3a0a6a94a31bdbaa0788d0`

Tree: `1bea688884f51c75cfb778cc9bd895b4da379661`

## Implementation

Added `severedRelaySpine.js`, focused tests, and the minimal App integration.
The registry is null-first and disabled; source URL, provenance, geometry,
layouts, copy, and alt contain no selected value. The released generic Client
Bridge remains visible. The accepted-only guard requires lawful Host 07,
sanitized strict `L03-01`/`L03-02`, exact five-attempt source/provenance,
measurements, layouts, and decode. Enablement is the irreversible legacy switch
even when later guards fail. No Host 08 image import, DOM target, focus,
announcement, evidence write, or save field exists yet.

Prompt identities were independently frozen exactly as Recon recorded. Combat
made no CLI/API call, generated or inspected no media, populated no selected
field, and consumed zero attempts.

## Validation

- Focused Host 06/07/08/Client Bridge: `20/20` PASS in `152.5279ms`.
- Full product: `993/993`, zero fail/skip/cancel, `34.399s` PASS.
- Production build: `221` modules, `8.96s`; JS `1,695,180`, CSS `119,490`.
- TD-012 fixture build: `57` modules, `1.83s`.
- Accepted media unchanged: `18 / 53,320,925`; no source maps or dependency
  change.

All measured values remain inside `FRSH-008-v1` budgets. E2E and media decode
are correctly deferred until an accepted source exists; no protected QA path
was inspected, restored, staged, or used.

## Quartermaster placeholders

Quartermaster alone may launch `{H8-1..H8-5}` sequentially, privately validate,
identity-clean rejects, stop on first PASS, create the exact product raster and
provenance, and populate the predeclared source/provenance/geometry/layout/copy/
alt fields. Runtime activation then requires focused/full/build/PBA/served/E2E
revalidation. No sixth attempt exists.

The OPEN protected-QA capture-write variance and every other OPEN/process
record remain unchanged. Rollback is the exact Host 08 code/test/report delta.

Combat signs **`PRODUCTION FUNCTIONAL / FRCE-008-v1 / ROUTE FRESH QUARTERMASTER`**.
