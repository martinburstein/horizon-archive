# First Run Shell Variance - Direct Image API and Native Handle Identity

Variance ID: `FRSH-005-v1-VR-08`

Disposition: **`FIRST RUN SHELL READY / DIRECT IMAGE API, EXACT PS5.1 NATIVE
HANDLE IDENTITY, AND FULL FRESH PRODUCTION SEQUENCE FROZEN /
FRSH-005-v1-VR-08`**

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / decisive viability: `FRWO-005-v7` /
`FRVE-005-v7-VR-03`

Prior v7 viability history: `FRVE-005-v7` / `FRVE-005-v7-VR-01` /
`FRVE-005-v7-VR-02`

Preserved direct-API evidence: `FRWO-005-v6` / `FRVE-005-v6`

Base shell / effective prior Mission returns: `FRSH-005-v1` through
`FRSH-005-v1-VR-07`

Planning controls: `FRPB-001-v2` / `FRCL-004-v2` / `FRRM-005-v7` /
`FRSB-005-v7`

Mission source inspected:
`7b10633bd1f6664103d50bbe648a2c0c0ffafc61`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Released product/test baseline:
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Frozen candidate tree:
`09da6293d72c8123b8d9673bd8e41329338e8d13`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Date: **2026-08-11**

Exact next recipient: one fresh Reconnaissance Sergeant /
`reconnaissance_sergeant`

## Mission decision

The planning stack is conflict-free. `FRVE-005-v7-VR-03` supplies the exact
native-handle, one-link, race, substitution, and cleanup proof missing from
`FRVE-005-v6`; the earlier v7 HOLDs remain honest evidence-availability
history and are not rewritten. Martin's explicit Host 06 direct Image API
authorization remains controlling. Mission therefore issues **`FIRST RUN
SHELL READY`**.

This variance supersedes only the withdrawn built-in-tool/data-URL/managed-
directory ingress paths and the associated direct-to-Quartermaster shortcuts.
The sole live production transport is now `HOST06-IMAGE-API-PSNET-v1` plus the
exact `HOST06-FILE-IDENTITY-PSNET-v1` helper. `output_hint`, managed-output
discovery, the built-in generation call, CLI, Python SDK, packages, browser,
extension, alternate endpoint/model/parser, response URL, data URL, reference,
edit, variation, repair, upscale, post-process, and fallback remain forbidden.

Every unchanged player-facing, canon, learning, privacy, save, accessibility,
performance, offline, presentation, provenance, validation, rollback,
no-reveal, process, maturity, and protected-state clause in `FRSH-005-v1`,
`FRSH-005-v1-VR-06`, `FRWO-005-v6`, and `FRWO-005-v7` remains exact. Where an
old shell clause requires built-in transport or a managed residual for a
future ordinal, this variance replaces only that transport/residual clause.
Historical ordinal `1` and its conservative residual remain unchanged.

Production resumes through every required role in exact order: fresh Recon,
fresh Tactical, fresh Combat preflight, Quartermaster direct-API generation
and content, Image runtime presentation, then fresh Intelligence. The frozen
inert candidate and prior direction/blueprint/function evidence are inputs,
not authority to skip a role.

## Bounded player-visible outcome, address, and maturity

The only player-visible outcome remains:

```text
exact Host 05 / Sixfold Weir mastery
-> lens-like fragment handoff
-> one distinct local dry Host 06 / Stranded Lens Cradle
-> sole unchanged L02-03 entry and loop
-> unchanged next Drowned learning boundary
```

- **Address:** `FR-03 / Chapter II - Drowned Archive / Host 06` only.
- **Entry:** only after exact sanitized Host 05 / `L02-02` mastery.
- **Hard stop:** immediately after exact `L02-03` mastery.
- **Current maturity:** continuity `FR2`; physical expression `FR0 - 1
  accepted shared compression / 1 exact / 10 missing`; learning `FR2`;
  behavior/save/recovery `FR1`; content `FR2`; presentation `FR3`; prior
  bounded release proof `FR4`.
- **Possible accepted result:** Intelligence alone may later record `1
  accepted shared compression / 2 exact / 9 missing`; no planning, request,
  attempt, source, import, commit, or presentation step advances maturity.

There is no Host 07 expression, new lesson, route, checkpoint, save field,
branch, reward, access, identity, authority, world response, successor,
RP-013, or post-ending content. Both MH-40 outcomes retain equal dignity, the
shared RP-012 ending, all null deltas, and `successor=null`.

## Frozen native helper and production-host contract

Helper contract ID: `HOST06-FILE-IDENTITY-PSNET-v1`.

The exact C# source remains the fenced source in `FRWO-005-v7`: UTF-8 without
BOM, LF line endings including final LF, exact `1,693` bytes, lowercase
SHA-256:

```text
98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97
```

The only helper root and output are:

```text
C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-725b75e4-8083-4df5-9a80-a0301b8f00dd
C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-725b75e4-8083-4df5-9a80-a0301b8f00dd\Host06FileIdentity.dll
```

In the one bounded 64-bit Windows PowerShell `5.1.26100.8875` production
process, Quartermaster must extract the exact source in memory, verify its
length/SHA, and compile only through `Add-Type -TypeDefinition`, CSharp, exact
sole `-OutputAssembly`, and `-ErrorAction Stop`. No source file is written.
Pre-existing root/DLL, warning, diagnostic, type output, extra child, or any
other compiler output stops before a live root or API request.

The compiled DLL must be an ordinary, one-link, non-reparse exact `4,096`-byte
file with lowercase SHA-256:

```text
39e85b32b7f8437c2b5732e26093ca5bd9a9182b048c411e9dc5660ba03f10c9
```

Load only those verified bytes through `Assembly.Load(byte[])`. Reflection
must prove the sole public static `Read(SafeFileHandle) -> UInt64[]` surface,
the sole private `GetFileInformationByHandle` import from `kernel32.dll` with
`SetLastError=true`, no third declared method, exact x64 structure size `52`,
and offsets `0,4,12,20,28,32,36,40,44,48`. The returned five unsigned values
remain volume serial, 64-bit file index, link count, attributes, and 64-bit
size. Null, invalid, closed, native-false, non-five-value, zero/multiple-link,
reparse-bit `0x400`, or size mismatch fails closed.

Before any live-root allocation or API request, reverify the DLL bytes and an
exclusive handle snapshot, delete only the identity-proved exact DLL, prove it
absent, delete only the exact empty helper root nonrecursively, and prove it
absent. The verified in-memory type may remain only for this bounded process.
No parent, sibling, glob, recursion, pattern, compiler/package cache,
repository path, live attempt root, product path, ordinal-1 residual, managed
directory, or VR-65 is a cleanup target. Helper cleanup uncertainty consumes
no ordinal but is terminal.

The helper accepts only the owning `FileStream.SafeFileHandle` while the exact
stream remains open with `FileShare.None`. It never receives a raw/borrowed or
duplicated handle, a path-opened shared handle, or a disposed handle; it never
owns or closes a handle and exposes no path, string, create, write, move, copy,
link, delete, enumeration, process, network, registry, environment, reflection,
or arbitrary-native-call surface.

## Exact direct Image API call

Transport contract ID: `HOST06-IMAGE-API-PSNET-v1`.

The only request is one `POST` to:

```text
https://api.openai.com/v1/images/generations
```

For the active ordinal, the serialized JSON object has exactly seven members:

```json
{
  "model": "gpt-image-2",
  "prompt": "<exact HOST06-GEN-PROMPT-v1 text below>",
  "n": 1,
  "size": "3840x2160",
  "quality": "high",
  "background": "opaque",
  "output_format": "png"
}
```

There is no eighth member: no image/mask/input fidelity, moderation override,
compression, response format, user, stream, metadata, seed, or SDK/tool
option. Use installed framework assemblies only: `System.Net.Http`,
`System.Web.Extensions`, and `System.Runtime.Serialization`. Serialize one
predeclared ordered in-memory object; never interpolate prompt or secret into a
command or literal JSON string.

Read `OPENAI_API_KEY` only from the process environment. Before the request,
only nonempty boolean presence may be observed. Construct only
`Authorization: Bearer <value>` through `AuthenticationHeaderValue`; set
`Content-Type: application/json; charset=utf-8` and `Accept: application/json`.
Never emit, log, persist, hash, count, slice, transcribe, or retain the key,
header, credential derivative, request object, or serialized request.

Use one `HttpClientHandler` with `AllowAutoRedirect=false`, one
`HttpRequestMessage`, one `HttpClient` with `00:10:00` timeout, and exactly one
`SendAsync(..., ResponseHeadersRead)`. There is no retry handler, proxy
override, parallel send, fallback, or second send for an ordinal. Once
`SendAsync` begins, that ordinal is consumed regardless of outcome.

### `HOST06-GEN-PROMPT-v1` exact text

The request prompt is exact ASCII/UTF-8 length `4,099`, LF-normalized, with
SHA-256 `efd8c7f8b5ca6c0ec9e16ac82cdb008921a480e549b3b0b1b4b767f79e323179`:

```text
Use case: stylized-concept
Asset type: final runtime 16:9 first-person environment plate for Horizon Archive Host 06
Primary request: Create one premium contemporary, maximum-quality cinematic science-fiction environment view of the Stranded Lens Cradle in the existing Drowned Archive basin. Show one visibly weathered lens resting inside one tilted conformal cradle on an already-dry, above-water local reach. The scene must preserve quiet scientific uncertainty about whether the lens was stranded by a former phase or is exactly where it belongs.
Scene/backdrop: One local next reach of the same enormous flooded Builder phase-processing basin, continuous with the prior Sixfold Weir rather than a second basin, teleport, cutaway, or lesson diagram. Keep a ruler-straight horizon and reflected horizon, restrained atmosphere, and physically legible old process routing.
Subject: The complete lens-and-cradle relation is the single discovery center. At least 80 percent of the visible lens body lies within the projected inner cradle. Tilt the cradle principal axis 12 to 35 degrees from image horizontal. Show at least two distinct load or stress-control contacts coupling lens, cradle, and supporting fabric; at least two continuous drainage seams leading away toward basin drainage; at least 2 percent of source height of dry clearance above live water; and one continuous dry material approach. Show at least three lens-like fragments leading from the Host 05 side toward but not into the activation region. Make the horizon or its reflection cross at least 25 percent of the visible inner lens width.
Style/medium: Feature-film environment credibility and flagship current-generation game key art; photorealistic materials, physically convincing smoky phase glass, ceramic ribs, mineral deposition, corrosion, abrasion, dust, inclusions, refraction, internal reflection, caustics, repair seams, and several visible stewardship eras. Builder beauty must read as accumulated functional civic construction, engineered ecology, maintenance, revision, graceful failure, and nonhuman process rather than generic fantasy ornament or human industry.
Composition/framing: Exact wide 3840 by 2160 landscape intent, first-person eye level, centered full-source presentation with no required crop. Keep the top 10 percent free of essential physical fact. Place the complete Host 06 relation wholly within normalized x 0.34 to 0.70 and y 0.30 to 0.74, with a tight relation rectangle width 0.30 to 0.36 and height 0.30 to 0.40. Place the Host 05 fragment approach wholly within x 0.04 to 0.30 and y 0.56 to 0.88. Reserve same-basin distant context within x 0.70 to 0.98 and y 0.08 to 0.56. Live water may occupy x 0.68 to 0.98 and y 0.62 to 0.92 but must not intersect the relation or dry approach. Keep the relation distinct from live water, the Host 05 cue, a return-like ridge, the Crown, the distant suspended Tidal Lens, and any second lens/cradle candidate.
Lighting/mood: Restrained natural basin light, horizon reflection caught by the inner lens, sober discovery, deep atmospheric scale, no magical glow, spectacle cue, invitation, reward, activation flash, or world response.
Text: No text.
Constraints: No image input. No protagonist, body, hands, shadow, reflection, portrait, companion, ship, person, face, human path, human control, label, glyph, symbol, interface, overlay, beacon, readable mark, prior-human trace, or native educational graphic. The Machine and Builders do not speak, react, recognize, reward, authorize, invite, judge, explain, move, heal, aim, drain, illuminate, or open anything because of the player. Preserve physical ambiguity and surface-safe canon.
Avoid: Empty ring; lens beside, behind, through, or in front of an aperture; submerged or water-filled cradle; inaccessible scenic landmark; duplicate lens/cradle relation; the distant Tidal Lens as Host 06; generic ruins; terrestrial industrial shorthand; arbitrary fantasy ornament; answer key; model or deployment diagram; purpose certainty; hidden-lore answer; watermark; border; crop-dependent essential fact.
```

## Response, base64, staging, identity, and no-replace move

Require HTTP `200`, JSON content type, and any declared `Content-Length`
inside `1..16,500,000`. Read the body in chunks no larger than `65,536` into
one memory buffer capped at `16,500,000`, rejecting before an over-cap write.
Decode with strict UTF-8. Accept exactly one JSON document: top-level object,
exactly one `data` array, count `1`, one object, exactly one primitive string
`b64_json`, and no URL, duplicate, second payload, unknown payload-bearing
member, deep/nonfinite/oversized structure, or trailing non-whitespace.

Require `b64_json` length `4..16,000,000`, divisible by four, canonical RFC
4648 alphabet/padding/pad bits, and decoded-length arithmetic `1..12,000,000`.
Call one in-memory base64 decoder exactly once. On transport/non-200/parser/
decode failure retain only ordinal, stable local failure code, optional integer
HTTP status, and cleanup booleans. Never retain response body, API diagnostic,
exception dump, JSON, base64, headers, prompt, or secret.

The exact live root and only active pairs are:

```text
root: C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-6eae8313-1407-492d-af7c-675051ab8e08
ordinal 2 stage: C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-6eae8313-1407-492d-af7c-675051ab8e08\.attempt-02-5f858a43-216c-4344-a4ce-0bfbcd042bcb.stage
ordinal 2 target: C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-6eae8313-1407-492d-af7c-675051ab8e08\attempt-02.png
ordinal 3 stage: C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-6eae8313-1407-492d-af7c-675051ab8e08\.attempt-03-63422fd8-bf71-489b-921a-5acb0fca7357.stage
ordinal 3 target: C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-6eae8313-1407-492d-af7c-675051ab8e08\attempt-03.png
```

After response decode: prove the inactive pair and active pair absent; create
only the exact active stage using `FileMode.CreateNew`, `FileAccess.Write`,
`FileShare.None`; write the decoded array once; call `Flush(true)` while the
exclusive write handle remains open; and call the helper on that same owning
handle. Require one link, no reparse bit, exact size, and freeze its volume/
file-index tuple without emitting it.

Close, then exclusively reopen the stage read-only. Require the same tuple,
one link, no reparse bit, exact length, and decoded-byte lowercase SHA-256
identity. Run the strict technical gate on those exact bytes. Immediately
before move, take a second snapshot from that same open stage handle and
require unchanged identity. Close validation handles, re-prove target absence,
then invoke same-directory two-argument `File.Move(stage,target)` exactly once.
It is no-replace: a destination race fails and neither destination nor stage
may be overwritten or adopted.

Prove stage absent. Exclusively open the target read-only and require the same
tuple, one link, no reparse bit, length, and SHA. Only this identity-proved
target may enter physical review. A pathname claim never substitutes for a
handle snapshot. Any API, parser, staging, write, flush, close, identity,
substitution, link, reparse, move, destination-race, or cleanup failure is
terminal with no retry and no later ordinal.

## Ordinal, cleanup, rejection, and selection ledger

- Total ordinal domain is exactly `{1,2,3}`.
- Ordinal `1` is permanently consumed, opaque, inaccessible, and never
  retried, recovered, discovered, inspected, or cleaned.
- Ordinal `2` is the only first live request under this shell.
- Ordinal `3` is permitted only after ordinal `2` becomes a fully identified
  objective source rejection after final target identity, complete technical,
  physical, crop/mapping, and accessibility review, and exact target/root
  cleanup.
- Maximum future API calls is `2`, sequential, one request and one output per
  ordinal. There is no retry, fourth/replacement/parallel call, or alternate
  transport.
- Any transport/parser/file/identity/cleanup failure after `SendAsync` consumes
  the active ordinal and stops; it never authorizes ordinal `3`.
- Conservative managed-residual count remains exact `1`, associated only with
  historical ordinal `1`. Direct API ordinals create no managed output.

Cleanup may delete an active stage or target only after an exclusive reopened
handle snapshot matches its frozen active tuple, link count `1`, no reparse
bit, and expected size. Delete only that exact identity-proved file, prove it
absent, delete only the exact empty live root nonrecursively, and prove it
absent. If identity cannot be proved, do not delete; cleanup uncertainty is
terminal. Never enumerate or target the OS-temp parent, a sibling, unknown
entry, glob/pattern, ordinal-1 residual, managed directory, product path,
VR-65, or protected/user path.

An objective rejection retains only ordinal, prompt/contract IDs, gate stage,
stable rejection codes, and cleanup booleans. Rejected bytes, hash, path,
pixels, screenshot, diagnostic, response, or preview retain zero authority and
must not enter a report, repository, browser, Markdown, reveal, later prompt,
or later host. After exact rejection cleanup, ordinal `3` begins from a newly
created and revalidated same literal root with its distinct pair.

At most the first fully passing target may be selected and copied once,
byte-identically with create-new semantics, to:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
```

Require target/product length and lowercase SHA equality. Only then create:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md
```

Provenance records `FRWO-005-v7`, this effective shell,
`HOST06-FILE-IDENTITY-PSNET-v1`, source length/SHA, compiled DLL length/SHA,
successful one-link/identity checks and helper cleanup,
`HOST06-IMAGE-API-PSNET-v1`, exact endpoint/model/seven request options,
prompt ID/no-input declaration, consumed ordinals/dispositions, selected
attempt basename, selected length/SHA, technical/physical/layout/accessibility
results, immutable manifest digest, exact product path, accepted-media
unchanged declaration, and exact temp cleanup. It contains no helper/DLL bytes,
native tuple values, key/header, request/response JSON, base64, rejected bytes/
hash/path/pixels, diagnostics, exceptions, or hidden/opaque data.

At most one raster plus this one provenance file may enter the workspace. No
alternate, derivative, crop, thumbnail, contact sheet, or second source is
authorized. No role emits a candidate through `generatedImage`, Markdown,
screenshot, preview export, cycle reveal, publication, or sharing.

## Technical, physical, crop, mapping, and accessibility gates

Before any pixel review, the exact target must pass: PNG signature; checked
big-endian chunk length/type/order and CRC; exactly one first `IHDR` length
`13` with `3840x2160`, bit depth `8`, truecolor type `2`, compression/filter/
interlace `0`; exactly one valid pre-IDAT `sRGB`; only the constrained `gAMA`
`45455`, exact `cHRM` tuple, and nonzero valid `pHYs`; no alpha, `tRNS`,
palette, ICC/profile, text, EXIF, time, animation, private/unknown chunks, or
trailing bytes; consecutive nonempty IDAT; one final empty IEND; one complete
zlib stream with no compressed trailing data; exact `24,885,360` scanline
structural bytes and valid filter byte `0..4` per scanline; and clean isolated
browser decode at natural `3840x2160`. No repair, conversion, alternate decode,
screenshot, preview, export, or derivative exists.

Only then apply all `PHY-01..12` predicates unchanged. The complete single
lens/cradle must be nested at least `80%`, cradle tilt `12..35` degrees, with
at least two load/stress contacts, at least two continuous drainage seams,
at least `2%` source-height dry clearance and continuous dry approach, at
least three Host 05-side fragments, and horizon/reflection crossing at least
`25%` of inner-lens width. Reject non-first-person or human trace, ambiguity,
water intersection, broken same-basin continuity, second candidate, diagram/
answer behavior, response, recognition, reward, access, authority, invitation,
purpose certainty, or hidden lore.

Preserve the exact source bands: top-safe `y=[0,.10]`; relation wholly within
`x=[.34,.70], y=[.30,.74]`, tight rectangle width `.30-.36` and height
`.30-.40`; Host 05 approach within `x=[.04,.30], y=[.56,.88]`; distant context
within `x=[.70,.98], y=[.08,.56]`; live water only within the allowed right/
lower band without relation/approach intersection.

Use one `<img>`, `object-fit:cover`, exact `object-position:50% 50%`, no crop,
alternate, responsive duplicate, or derivative at desktop `1920x1080`, laptop
`1366x768`, narrow `390x844`, effective-`200%` `768x900`, retained
`320x180`, and the `320x180` world inside `320x240`. Require at least `99.5%`
relation/approach retention and retained centers; exact unrounded DOM mapping
with `1/64 CSS px` floor-lattice diagnostics; physical/activation/label center,
containment, area, target, separation, nonoverlap, source order, and before/
after-focus stability. Activation is at least `44x44 CSS px`, contains the
physical center with `4 CSS px` clearance, and is no more than `1.50x` physical
area. Label insets remain exact `3/5 CSS px`; present protected features have
zero intersection and at least `96` source pixels / `8 CSS px` mapped edge
separation; absent features are exactly `absent`.

Keyboard, pointer, touch, Enter, Space, speech-by-name, and switch-like native
activation converge once. Hidden means no node/name/status/announcement/focus/
action/credit. Normal and forced-color focus remain visible, with exact `3px
Highlight` in forced colors. Reduced motion removes nonessential motion without
changing state/order/focus/meaning. Alt and status text state observable fact
and owner-correct state without answer, purpose, invitation, or intent.
Automated evidence is not human assistive-technology certification.

## Runtime, learning, privacy, offline, and performance contract

The frozen null-first registry, source guard, conditional generic-launcher
removal, seven copy slots, one factual alt slot, exact action/focus/recovery
graph, and inert code candidate remain inputs. Fresh Recon and Tactical must
reconcile them to this direct-API shell; Combat may change code only if their
new exact artifacts prove a required bounded correction. Otherwise a
substantiated no-change preflight is valid.

Host 06 exists only after exact Host 05 mastery, selected source/provenance
identity, lawful finite measurements, and natural decode all pass. Once
`source.enabled===true`, the generic launcher stays absent even on later source
failure; failure leaves Host 05 safe and Host 06 absent. Sole `USE Stranded
Lens Cradle` opens unchanged `openModelChoiceExercise()` only in available,
in-progress, or remediation-required state. LOOK/TALK write nothing; completed
USE is read-only.

The exact `L02-03` eight primary/eight transfer scenarios, evaluator,
`16/16 + 16/16 + 2/2`, actual-miss answer-free remediation, blank retry,
fresh transfer, closed-note explanation, ownership/confidence, sanitizer,
private/transient clearing, allowlisted atomic save projection/restore, and
no-cross-credit behavior remain byte/behavior exact. Demo Tour remains
separate and zero-credit. No raw/private response, reasoning, focus, media,
credential, endpoint, generation, or diagnostic state is persisted.

The shipped product remains local/offline: one same-origin selected-image
request and zero external runtime requests. Add no dependency, package,
lockfile, service worker, telemetry, credential, external service, runtime
model call, Python/WASM runtime, or source map.

All caps remain exact: response `1..16,500,000`; base64 `4..16,000,000`;
selected raster `1..12,000,000`; JavaScript affected/global
`<=1,679,393 / <=1,703,258`; CSS `<=119,547 / <=119,672`; modules
`<=218 / <=222`; resulting media exact `18` and
`37,410,731 + selectedBytes <=49,410,731`; source maps `0`; cold/warm decode
`<=250ms / <=100ms`; cold local image-ready `<=750ms`; attributable/total CLS
`<=0.01 / <=0.05`; activation/task `<=2ms / <=100ms`; focused/related/cold
full `<=30s / <=60s / <=60s`; production/fixture build `<=60s` each; and one
complete E2E `<=180s` with no retry. Any overage is HOLD; no budget borrowing,
compression/edit, accessibility reduction, or dependency workaround exists.

## Required fresh role sequence and ownership

1. **Reconnaissance Sergeant:** issue a new versioned Directorial Treatment
   variance for this shell. Reconfirm the fragment-led same-basin
   reorientation, singular nested relation, restrained horizon catch,
   stranded/correct uncertainty, world silence, no-reveal boundary, and the
   transition into unchanged `L02-03`. No helper/API execution, final prose,
   code, media, or pixel action.
2. **Tactical Operations Specialist:** issue a new versioned Player Experience
   Blueprint variance. Reconcile `FRPX-005-v1` to the direct-API attempt/
   failure/selection ledger, exact source/DOM/focus/recovery order, null-first
   registry, six-layout/accessibility probes, and no-reveal behavior. No
   helper/API execution, prose, implementation, media, or pixel action.
3. **Combat Engineer:** perform a fresh non-generative/non-API preflight over
   the exact shell, treatment, blueprint, frozen inert candidate/tree,
   immutable `FRAM-001-v1`, absent product targets, null copy/alt/geometry
   slots, exact helper/API harness contract, permitted files, tests, and
   release-command needs. Implement only a bounded correction explicitly
   required by the fresh artifacts; otherwise issue a substantiated no-change
   `PRODUCTION FUNCTIONAL` preflight. Re-run proportionate focused/related/
   static/build gates if code/control changes, commit its report/handoff, push
   if product/control code changes or the handoff requires it, and prove exact
   synchronization before Quartermaster. No helper compile, live root, API,
   media, pixel, copy, provenance, or E2E.
4. **Quartermaster:** only after the fresh Combat gate and synchronization,
   execute the exact helper and direct API protocol in one bounded PS5.1
   process, beginning with ordinal `2`; perform technical and original-
   resolution content review; use ordinal `3` only after lawful objective
   rejection and exact cleanup; select at most one source; import exactly one
   byte-identical raster plus `PROVENANCE.md`; populate only predeclared
   scalars; write final-purpose copy in exactly seven slots and one factual alt;
   prove exact cleanup; issue `PRODUCTION CONTENT COMPLETE` or `HOLD`. No
   behavior/lesson/route/save/world/ending change and no reveal.
5. **Image Specialist:** after a lawful Quartermaster candidate, perform only
   code/configuration runtime presentation around immutable selected bytes:
   composition, hierarchy, lighting/color treatment, existing sound, motion,
   typography, spacing, focus, forced color, reduced motion, responsive
   containment, and performance. No generation, edit, variation, export,
   import, replacement, media-byte change, or reveal. A substantiated no-change
   result is valid.
6. **Intelligence Officer:** begin fresh, freeze the exact as-built candidate,
   independently recompute identities and manifests, execute/corroborate the
   full ladder, classify every variance, release or return the candidate, and
   alone update maturity/map/scoreboard/changelog and the next handoff. No
   repair, generation, residual access, or reveal.

No role may be silently skipped or reordered. A variance returns to the
earliest owner and pauses every downstream stage.

## Validation ladder and definition of done

Candidate-bearing gates run in this exact order:

1. authority, ancestry, shell/role artifacts, bounded diff, permitted paths,
   protected boundaries, frozen candidate/tree, immutable `FRAM-001-v1`,
   ordinals/residuals, absent targets, and helper/API contract identity;
2. helper source/compile/DLL/load/reflection/layout/handle and exact cleanup;
3. env-only boolean credential preflight, one-send request, response caps,
   strict UTF-8/JSON/base64, create-new stage, Flush, handle snapshots,
   no-replace move, exact cleanup, and selected identity;
4. strict PNG/chunk/CRC/IHDR/sRGB/opacity/dimension/inflate/browser decode,
   then original-resolution `PHY-01..12`;
5. six-layout source/physical/activation/label/center/anchor/retention/
   nonoverlap/target/accessibility and before/after-focus stability;
6. focused Host 05-to-06 source/state/action/focus/return/reload/malformed and
   complete current `L02-03` learning/privacy/save/restore proof;
7. related Hosts 04/05, Meadow return, unchanged next boundary, no Host 07,
   Witness/City/later rail, Demo Tour, both MH-40 outcomes, null deltas,
   RP-012, and `successor=null`;
8. exact sorted `40/40` validators, cold full suite, clean production and
   TD-012 fixture builds, PBA, served-to-disk identity, offline/same-origin,
   decode/image-ready/task/CLS, source-map, and no-inline-secret/payload proof;
9. exactly one non-overlapping clean-start production E2E `<=180s`, no retry,
   through Hosts 04/05/06, one real `L02-03` miss/remediation/retry/transfer/
   explanation/mastery, Close/Escape, reload/return, completed read-only USE,
   unchanged rail, equal MH-40 outcomes, null deltas, `successor=null`, zero
   external runtime requests, zero runtime errors, one summary, one verifier;
10. stop exact owned processes, clear owned ports, delete only declared
    identity-proved temp/success artifacts, leave protected/opaque state
    untouched, freeze candidate, prove bounded diff and Git synchronization.

Definition of done is one provenance-honest, fully passing, byte-identical
source at the sole raster path; one lawful provenance file; immutable prior
media; exact helper and attempt cleanup; complete runtime/content/presentation
contract; full validation including one E2E; and fresh Intelligence acceptance.
Prompt intent, API success, a partial parser pass, decorative similarity,
arithmetic without live mapping, a renamed generic launcher, partial tests, or
a planning artifact is insufficient.

## Permitted files, rollback, and protected state

The existing permitted product/test/report surface in `FRSH-005-v1` remains
exact. The only media-owned additions are the sole raster and provenance paths.
`modelChoiceExercise.js`, all `L02-03` curriculum, save schema/version,
dependencies, accepted-media files/manifest, Hosts 07+, later rail, canon, and
every unlisted path remain patch-forbidden. A fresh role may add only its exact
versioned report plus `NEXT_INSTANCE_HANDOFF.md`; Intelligence alone may update
release controls and changelog.

Rollback baseline is exact released candidate `a91763e...`. Bounded rollback
removes only the exact selected raster/provenance, bounded Host 06 product/
config/test/E2E delta, authorized cycle release-command delta, and cycle
reports after exact identity proof; restore pre-existing permitted blobs only
by explicit path to their frozen identities. Never reset the repository or
touch accepted media, Hosts 04/05, curriculum, save/dependencies, Host 07+,
later rail, user/untracked work, browser/profile/save, repository QA
quarantine, protected PDF, training tree, hidden lore, OS-temp parent,
ordinal-1 residual, managed directory, VR-65, or another parent root.

All thirteen inherited process records remain separate and **OPEN**: VR-17,
VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The separate Commandant filename/search-scope record
also remains **OPEN**. This shell closes, cures, merges, waives, or renumbers
none. VR-65 remains separate, opaque, non-gating, and inaccessible. Repository
QA quarantine, `Art Of No Mans Sky Book Scan.pdf`, `Simplilearn Training
Files/`, real browser/profile/save, hidden lore, accepted-media pixels, OS-temp
parent, live root before its exact stage, ordinal-1 residual, real managed
directory, user work, and every opaque residual remain protected.

## Mission validation, change history, and exact Recon handoff

Mission read the complete current intake, workflow, registry, profile,
`FRWO-005-v7`, all v7 Science artifacts through decisive
`FRVE-005-v7-VR-03`, `FRWO-005-v6`, `FRVE-005-v6`, synchronized release map
and scoreboard, complete effective shell through `FRSH-005-v1-VR-07`,
immutable `FRAM-001-v1`, reopened baseline/continuity, current Host 06
direction/blueprint/functional/content returns, and the exact Chapter II and
shared-ending canon sections. It verified starting `HEAD` `7b10633...`, frozen
candidate/tree, and manifest file identity without opening accepted-media
pixels.

`FRSH-005-v1-VR-08` is the prospective direct-API/native-identity production
contract for `FRWO-005-v7`. Mission changed no product, test, runtime, lesson,
save, media, manifest, candidate, map, scoreboard, maturity, process
classification, residual, schedule, or automation state. The dedicated
Mission commit contains only this variance and the synchronized handoff; its
commit/push identity is reported after commit because the artifact cannot
contain the hash of the commit that first contains itself.

Mission did not compile or invoke the helper; allocate a helper, fixture, or
live root; read the credential; construct/send an API request; consume ordinal
`2`; read/hash/decode media; inspect pixels; import an asset; write product copy
or provenance; change runtime/tests; run build/browser/E2E; reveal; advance
maturity; close an OPEN record; access a residual or VR-65; schedule; automate;
release; or call `FIRST RUN COMPLETE`.

Mission Captain signs **`FIRST RUN SHELL READY / DIRECT IMAGE API, EXACT
PS5.1 NATIVE HANDLE IDENTITY, AND FULL FRESH PRODUCTION SEQUENCE FROZEN /
FRSH-005-v1-VR-08`** from exact source `7b10633...`.

After this variance and `NEXT_INSTANCE_HANDOFF.md` are committed, pushed, and
exactly synchronized, one fresh Reconnaissance Sergeant /
`reconnaissance_sergeant` reads this complete shell, the synchronized handoff,
complete `FRWO-005-v7`, decisive `FRVE-005-v7-VR-03`, `FRDT-005-v1`,
`FRPX-005-v1`, frozen functional evidence, immutable `FRAM-001-v1`, and only
the relevant current Drowned visual/canon authorities. Recon issues exactly
one versioned `DIRECTORIAL LOCK`, `REVISE`, or `HOLD` artifact for this shell
and hands a passing lock to one fresh Tactical Operations Specialist.

Recon may not compile/invoke the helper, allocate a root, call the API,
consume an ordinal, inspect media/pixels, write final prose, edit product/tests,
begin Tactical, reveal, advance maturity, close an OPEN record, access a
residual, schedule, automate, release, or call `FIRST RUN COMPLETE`.
