# FIRST RUN FUNCTIONAL REPORT / FRCE-012-v1

## Disposition

`PRODUCTION FUNCTIONAL / INERT HOST 12 SLOT / QUARTERMASTER READY`

Shell `FRSH-012-v1`; blueprint `FRPX-012-v1`; source release `FRAB-011-v1`.
Code candidate is `12efa77a4aedf94e792de2479ef44d391ebe6243`.

Combat added `drownedSwitchyard.js`: exact null-first registry, nine prompt
identity guards, 30 MB cap, provenance/measurement/decode lawfulness, derived
six-layout projection, state derivation, hotspot projection, and generic SDK/
Single Agent retirement guard. `App.jsx` gates only those two legacy launchers
on `source.enabled!==true`; Client Boundaries remains unguarded and unchanged.
Because the selected source is false/null, released behavior is unchanged. No
raster import, geometry, copy, alt, or provenance field is populated.

Tests cover inert/null behavior, exact attempt domain and cap, full physical
minimums, derived responsive evidence, fail-closed state, launcher retirement,
and preservation of Client Boundaries. Focused Host11/Host12/SDK/Single Agent
suite passes `26/26`. Full suite passes `1027/1027/0`. Production and TD-012
fixture builds pass `230 / 57` modules. PBA passes JS `1,733,148`, CSS `120,356`,
current accepted media `22 / 125,903,080`, maps0. The production build contains
25 asset files and no new raster.

No generation, API call, selected-media inspection, candidate raster, product
source, provenance, prompt mutation, live preview, E2E, real profile/save, or
protected QA action occurred. The rolling pool remains nine; local ledger
`{H12-1..H12-9}` is unstarted. Rollback is the new module/test plus the two
launcher guards.

Quartermaster placeholder ledger: source/provenance; upper crown, dry approach,
bypass seams, sealed chambers, drowned returns, water boundary, semantic target,
label, protected regions, six layouts; nine final-purpose state slots plus name
and alt. Quartermaster populates only after first total PASS, keeps rejected
outputs private, stops at first PASS, and carries unused calls only after
Intelligence release.

Exact next owner: one Quartermaster.
