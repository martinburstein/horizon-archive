# First Run Shell — Unbroken Opening Rail

Shell ID: `FRSH-001-v1`

Disposition: **`FIRST RUN SHELL READY / FRSH-001-v1`**

Stage / owner: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-001-v1` / `FRVE-001-v1`

Planning authorities: `FRPB-001-v1`, `FRCL-001-v1`, `FRRM-001-v1`,
`FRSB-001-v1`

Mission source inspected: `e5d7891a72bb8d27a62d469007f4d7e8958b4fca`

Released predecessor: `TD-012 / SS-RP012-MEASURED-HORIZON-v1`

Budget authority: `PBA-TD012-v1`

Date: **2026-08-10**

## Disposition and felt improvement

The planning authorities are compatible and this pass is ready for production.
One local Windows launch will have one truthful identity and one uninterrupted
first journey: a finished three-beat opening enters the existing compact early
rail, Drowned and Witness remain physically silent, and the earned Witness
continuation lands directly at the already-operating City Threshold without an
obsolete credits stop.

This shell authorizes no physical-host expansion. Fracture Nursery remains the
next earliest missing expression, Drowned hosts 05-15 and hosts 17-38 remain
incomplete, and learning coverage is never physical-host parity.

## Planning reconciliation

Mission closes the remaining options as follows:

1. Opening save version `1` is retained with no new field, migration, beat,
   step, key, slot, or schema. Temporary source and DOM names are only a test
   migration.
2. Normal completion and exact completed-prefix resume enter
   `city-threshold-staging` directly. The old `ending`/credits product surface
   and City return-to-prologue-credits action are removed, not repurposed.
3. Direct City entry synchronously writes the existing campaign projection as
   opening `playing`, exact three-scene completion, and
   `pendingSceneId=null`; its separate City save is unchanged on arrival.
4. A canonical launcher/guide are added. The old launcher remains only as a
   truthful quoted-path, exit-code-preserving compatibility wrapper.
5. Quartermaster owns exact final prose. This shell freezes meaning, ownership,
   slots, rejected assertions, and acceptance proof—not final wording.
6. Existing accepted media and current semantic/focus/responsive/accessibility
   systems are sufficient; no new presentation or asset system is authorized.

Science's v1/no-credits decisions therefore supersede the Work Order's optional
versioned-opening and repurposed-credits alternatives. No conflict remains.

## Address, maturity, entry, and hard stop

- **Address:** `FR-00 -> FR-01 -> FR-02 -> FR-03 -> FR-04 -> initial FR-05`.
- **Current:** covered behavior `FR1`; identity/opening/early continuity content
  contains `FR0`; affected presentation has predecessor `FR2`-`FR3` evidence.
- **Target evidence:** covered continuity/content sufficient for `FR2`, affected
  presentation sufficient for `FR3`, unchanged functional behavior. Only
  Intelligence may record `FR4`.
- **Clean entry:** canonical local Windows launcher through title, Slot 01,
  bounded character name, and prologue beat `0`.
- **Resume entry:** exact sanitized opening/early v1 state, exact later City or
  Civic restore, or exact completed early prefix under the order below.
- **Completion:** initial City Threshold is visible, durably resumable, and
  focused before its first required world action; no predecessor is minted.
- **Permitted exits:** existing safe title/cancel/close, Meadow return,
  Terminal retry/return, Demo Tour return, forward rail, and released later
  write-free returns only.
- **Hard stop for changed behavior:** initial City Threshold. Civic Record,
  Calibration Margin, RP-004 through RP-012, MH-40, outcomes, remediation,
  returns, and shared ending are regression-only.
- **Pacing:** zero new mandatory exercise and no net new mandatory activation.

## Fixed route and opening state

Normal play is exactly:

`FR-00 -> title -> create/resume Slot 01 -> character-name -> prologue[0..2] -> chapter-reveal -> meadow -> ruins -> automaton -> city-threshold-staging`

The unchanged continuation is:

`city-threshold-staging -> verified RP-001 predecessor -> RP-002 ... -> MH-40`

Preserve `OPENING_SAVE_VERSION=1`, `LOCAL_SAVE_SLOT_ID="expedition-01"`,
`PROLOGUE_BEAT_COUNT=3`, key `horizon-archive-prologue-v1`, resumable steps,
ordinal `prologueBeat`, bounded name sanitation, and activation de-duplication.
Existing v1 beats `0/1/2` resume on the same beat; `chapter-reveal` and
`playing` resume unchanged. Legacy state without `opening` retains the bounded
`Pilot / playing / beat 2` migration; malformed slot/name/step fails closed.

The three beats keep their roles: pattern without sender; a reversible mortal
approach without mission; and a ruler-straight repeated horizon without road or
landing claim, followed by Chapter I. Remove all player-visible temporary,
story-pass, placeholder, frozen-demo, playable-slice, and later-work-held
claims. Replace `TEMPORARY_PROLOGUE_*` markers/source identities with stable
final-purpose names without persisting them.

One pointer, touch, Enter, Space, switch-like, speech, or screen-reader
activation advances at most one state. Modality changes no state or evidence.

## Early rail and post-Witness contract

- Meadow behavior, First Signal, Route Marker, optional calibration, learning,
  returns, recovery, and Fracture Nursery's missing status remain unchanged.
- Drowned retains one accepted compressed host and current learning. Its success
  may report local work and the pre-existing dry Crown outflow/service
  continuity only; Crown, basin, and route do not change.
- Witness retains a separate inert fallen assembly and grounded active Evidence
  Terminal. Fallen-assembly `LOOK AT`, `TALK TO`, and `USE` never open an
  exercise, move/couple the assembly, change evidence, or imply attention,
  tracking, listening, rejection, pulse, voice, or world response.
- Witness success and pending resume report suit/local evidence only; assembly
  and world remain silent and unchanged.
- After exact Witness learning, `completed` is exactly
  `["meadow", "ruins", "automaton"]`. `pendingSceneId="automaton"` remains
  valid until the earned continuation is activated. Reload before activation
  restores Witness once, presents the same non-response completion state,
  focuses continuation, and replays no evidence.

## Durable City frontier and resume order

The earned Witness continuation atomically clears pending acknowledgement,
enters City staging, and persists opening version `1` / step `playing`, the
exact completed prefix, `pendingSceneId=null`, and existing sanitized early
evidence only. It writes no City completion, RP-001 predecessor, lesson
evidence, route-creation event, city response, world delta, or extra action.

`city-threshold-staging` must be covered by the existing campaign save effect
or one equivalently testable pure projection; it may not be passed to
`createOpeningProgress` as an unknown step. The separate
`horizon-archive-rp001-staging-v1` value is absent or byte-identical on arrival.
Only existing Python, CUM-01, safety, and explicit local-anchor commit derives
the verified predecessor.

Resume order is fixed:

1. reject malformed or noncontiguous early state fail-closed;
2. honor an accepted later City Threshold/Civic Record restore;
3. restore exact incomplete opening or pending early scene;
4. route an exact completed early prefix without a later record to initial City,
   including legacy saves formerly sent to credits; and
5. let the existing City sanitizer select entry, first incomplete learning
   boundary, or exact completed overview.

No arrival, response, success effect, credits beat, route creation, learning
submission, predecessor event, or evidence may replay.

## Windows package contract

- Add `PLAY_HORIZON_ARCHIVE.cmd` as canonical documented entry.
- Preserve npm discovery, Node/npm prerequisite guidance, optional local
  dependency preparation, build, local preview on `4173`, quoted paths, stop
  guidance, and nonzero error propagation.
- The canonical launcher may silently invoke existing `npm run demo` behavior;
  title/output identify the complete product, not a demo.
- `PLAY_HORIZON_ARCHIVE_DEMO.cmd` becomes a truthful wrapper calling the
  canonical launcher through quoted `%~dp0` and preserving its exit code. It
  may not recurse, reimplement the build, or print stale claims.
- Add canonical `PLAY_HORIZON_ARCHIVE.md`. Reduce `PLAYABLE_DEMO.md` to a
  truthful compatibility pointer with no slice/freeze/held-work claim.
- Preserve the exact separate player-facing name `Demo Tour` and zero-credit
  meaning. `package.json` remains unchanged.

## Content and voice slots

| Slot | Owner | Required result | Forbidden result |
| --- | --- | --- | --- |
| launcher/guide | Product/System | complete local game; honest prerequisites/stop | demo, frozen slice, held later rail |
| prologue | Pilot/Scene/Suit by line | mortal curiosity, unassigned compatibility, reversible approach | sender, invitation, mission, production status |
| Drowned success | Scene/Suit | work complete; Crown/basin unchanged; existing outflow | route appears or lesson creates road |
| fallen `LOOK AT` | Scene/Pilot | inert observation; distinct Terminal | tracking, attention, privileged interface |
| fallen `TALK TO` | Scene/Pilot/Suit | no response; separate Terminal is relevant | pulse, listening, speaker response |
| fallen `USE` | Scene/Pilot/Suit | no coupling/response; Terminal usable | rejection or joint response |
| Witness success/resume | Suit/System | local evidence complete; world silent | lens opening, voice, continuity statement |
| City arrival | Scene/Suit | cycles already operating; local entry status | acknowledgement, verdict, predecessor claim |

No Machine/Builder line exists. Candidate game-on-paper lines require bounded
Quartermaster review; no novella bulk paste or answer-bearing literary copy.

## Learning, privacy, Tour, later-route, and ending firewall

- No learning source/catalog/objective/answer/evaluator/threshold/owner,
  evidence key, attempt rule, remediation route, readiness rule, or forty-owner
  mapping changes.
- Package/opening/copy/navigation/focus/credits removal/presentation contribute
  zero mastery and zero remediation input.
- Working code, prose choices, notes, credentials, endpoints, payloads,
  responses, private identity, timing, focus, modality, and tokens are transient.
- Existing early sanitizers, later atomic records, verified predecessor, and
  write-free returns remain byte/meaning equivalent.
- Demo Tour retains `horizon-archive-demo-tour-v1`, allowlisted cursor, explicit
  no-credit confirmation, zero campaign/City/evidence mutation, and exact First
  Signal unfinished-practice resume.
- READY/NOT YET READY remain equal local evidence outcomes and converge on the
  same unchanged Measured Horizon. `successor=null`; no RP-013, reward, access,
  authority, exam promise, world response, or post-ending content.

## Focus, accessibility, and responsive contract

All affected actions remain native semantic controls and all supported
activation modalities converge on one nonduplicated intent.

| State | Required landing |
| --- | --- |
| slot/name/prologue/Chapter I reveal | one unique page heading; one-hit advance |
| Meadow/Drowned/Witness arrival/resume | atomic scene announcement plus first required hotspot or earned continuation |
| direct initial City | one unique City heading/status before first world action; no removed-control focus |
| partial City restore | overlay heading or first incomplete control; all background inert while modal |
| completed City/later return | exact forward/return action; no credits action |

Each changed state has one `main`, one unambiguous heading, one polite atomic
status owner where state changes, unique IDs/names, visible `3px`-equivalent
focus, and no hidden duplicate primary action. Modal cancel/close restores focus
deterministically.

Prove `1920x1080`, `1366x768`, `390x844`, and `768x900` at effective `200%`,
plus retained `320px` contracts. Controls are at least `44 x 44 CSS px`; opening
primary controls remain `48px`. Longest final copy/status/actions wrap with no
horizontal overflow, clipped required text, overlap, or viewport-locked
scrolling. Grayscale/forced colors retain meaning; reduced motion removes
nonessential motion; no forced audio/timer is added. Automated semantics are
not a claimed human AT session.

## Presentation and immutable media

Recon may direct attention, rhythm, silence, and concept-level composition.
Tactical may define semantic order/focus/containment/existing crop use. Combat
may implement authorized code/config behavior. Quartermaster may finalize text
and approved non-image content. Image Specialist may adjust only authorized
code/config composition, lighting/color treatment, existing sound, motion,
typography, hierarchy, spacing, focus, containment, forced color, and reduced
motion.

No role may generate, edit, replace, vary, import, or publish an image; change
an accepted media byte; add a board/media/reveal/runtime request; or treat
concept art as integrated evidence. Current imports and exact runtime media are
the only media authority.

## Performance, offline, and authority limits

| Measure | `PBA-TD012-v1` cap |
| --- | ---: |
| JavaScript | `1,703,258` bytes |
| CSS | `119,672` bytes |
| modules | `222` |
| runtime media | exact `17 / 37,410,731` bytes |
| new media | `0 / 0 bytes` |
| build / focused / related / full | `<=60s / <=30s / <=60s / <=60s` |
| complete E2E | `<=180s` |
| sampled main-thread task | `<=100ms` |

Predecessor headroom is `43,224` JS bytes, `73` CSS bytes, and `7` modules.
CSS is optional and must be net-neutral or offset by obsolete credits rules.
No requirement may be weakened to meet budget.

Normal play remains local and adds no account, Azure resource, credential,
endpoint, SDK/model/agent call, upload, telemetry, runtime service, dependency,
or external action. Existing Node/npm preparation is the only package-network
prerequisite behavior allowed.

## Exact permitted files and systems

Product/test changes are limited to:

- `PLAY_HORIZON_ARCHIVE.cmd`;
- `PLAY_HORIZON_ARCHIVE_DEMO.cmd`;
- `PLAY_HORIZON_ARCHIVE.md`;
- `PLAYABLE_DEMO.md`;
- `horizon-archive-game/src/App.jsx`;
- `horizon-archive-game/src/CityThresholdStaging.jsx`;
- `horizon-archive-game/src/styles.css` only for a proven affected-state need;
- optional sole pure helper
  `horizon-archive-game/src/firstRunOpeningTransition.js`;
- `horizon-archive-game/test/openingFlow.test.js`;
- `horizon-archive-game/test/gameLogic.test.js`;
- `horizon-archive-game/test/sceneTransition.test.js`;
- `horizon-archive-game/test/sceneReturn.test.js`;
- `horizon-archive-game/test/demoTour.test.js`;
- `horizon-archive-game/test/cityThresholdExercise.test.js`;
- optional `horizon-archive-game/test/firstRunOpeningTransition.test.js`;
- new `horizon-archive-game/test/firstRunPackageContract.test.js`;
- `playtest/e2e-playthrough.mjs`; and
- the current owner's exact First Run report and
  `NEXT_INSTANCE_HANDOFF.md`; Intelligence may also update release map,
  scoreboard, and process changelog at release.

`openingFlow.js`, `gameLogic.js`, `sceneTransition.js`,
`cityThresholdExercise.js`, and `demoTour.js` are regression-only. The optional
helper may only project the completed-prefix/direct-City target; it creates no
new state, field, schema, or route authority.

Protected are `package.json`, dependency/lock files, learning sources and
evaluators, later controllers/components/schemas, story authorities, media,
and all paths outside the list. Never inspect or mutate Martin's real browser,
profile, save, hidden lore, `Art Of No Mans Sky Book Scan.pdf`, or
`Simplilearn Training Files/`.

## Role freedoms

- **Recon:** define one emotional/attention reading, pacing, silence, and
  existing-media concept; no exact controls/copy/code.
- **Tactical:** freeze the target graph, action/focus/announcement map, modal
  inertness, responsive order, resume/recovery, and copy-slot ledger.
- **Combat:** choose inline vs sole helper; remove obsolete credits; implement
  atomic direct-City persistence/focus, package wiring, tests, and E2E. Keep
  content placeholders explicit and semantics-safe.
- **Quartermaster:** finalize all package/prologue/Drowned/Witness/City copy and
  accessible names; change no mechanics or media.
- **Image Specialist:** review and only if needed adjust authorized runtime code
  for presentation; a no-change PASS is valid.
- **Intelligence:** independently validate, classify, update maturity from
  as-built evidence, and release or return without specialist repair.

Each production owner writes one dedicated report/commit. Combat is the
production push gate before Intelligence.

## Executable validation ladder

Every gate records command, candidate identity, elapsed time, result, and honest
unavailable evidence.

1. **Integrity:** exact authorities/source; `git diff --check`; only permitted
   paths; protected/media/user-state/no-image/no-reveal proof.
2. **Focused (`<=30s`):** six named focused tests plus package contract and
   optional helper test. Prove quoted launcher paths, missing npm, error code,
   nonrecursive wrapper; beats `0/1/2`, legacy/malformed/duplicate activation;
   completed-prefix routing/cleared pending; Drowned/Witness states; direct,
   partial, completed City restore; no credits control; Tour bytes; focus; and
   scoped obsolete-claim scans.
3. **Related (`<=60s`):** opening, game logic, scene transition/return,
   workload/evidence, Tour, Terminal focus, City, Civic entry/return, malformed
   state. String absence alone is insufficient.
4. **Learning/privacy (`<=60s`):** cold full `npm test`; all current curriculum
   and readiness validators; exact learning/evidence/save allowlists; no
   cross-credit, private persistence, Tour mutation, or owner-map change.
5. **Build/budget/identity:** clean production and TD-012 fixture builds;
   `scripts/validate_td012_budget.py --mode candidate` with fresh module count;
   exact media hashes; no dependency/runtime request; root/deep/JS/CSS HTTP
   preflight and served-to-disk identity.
6. **One E2E (`<=180s`):** replace the old `CREDITS_REACHED` stop in
   `playtest/e2e-playthrough.mjs` with one canonical-launcher-equivalent clean
   start through MH-40: final opening, wrong-answer/retry, pending Witness
   reload, direct City, immediate reload at cleared frontier, partial/completed
   City restore, exact predecessor, later rail, equal ending outcomes via
   proportionate sanctioned fixtures, no obsolete response/route/credits
   assertion, and zero runtime error. No overlapping second whole-game run.
7. **Live states:** desktop/laptop/narrow/effective-`200%`, keyboard,
   pointer/touch-equivalent, focus, announcements, targets, longest copy,
   containment, forced color, grayscale, reduced motion, requests/errors, and
   sampled tasks. Do not overclaim human AT.
8. **Cleanup:** restore/remove owned build/QA artifacts under Git authority;
   close owned contexts; clear ports `4173`, `4184`, and isolated E2E port;
   leave Martin's state untouched.
9. **Identity:** dedicated commits; Mission/Combat/Intelligence pushes; exact
   ancestry and `HEAD == origin/main` at every push gate.

Source/bundle scans reject temporary markers, slice/freeze claims, obsolete
credits copy, Drowned route appearance, and Witness tracking/listening/pulse/
rejection/voice in owned states. They must not reject speech curriculum or the
truth that the Evidence Terminal has no voice.

## Definition of done

Canonical and legacy launch are truthful/safe/equivalent; every affected v1
start/resume reaches a durably cleared direct City frontier with no predecessor
or evidence minted; all Drowned/Witness response slots are final and proven by
interaction; learning/privacy/Tour/later route/MH-40/equal outcomes/world/
`successor=null` remain unchanged; accessibility, performance, offline, media,
E2E, cleanup, and Git gates pass; every variance is classified; and
Intelligence accepts only the exact candidate.

## Rollback and variance routing

Rollback is the complete bounded product/test patch plus downstream reports.
Reverting it restores exact TD-012 behavior while preserving all v1/later
saves. No migration, media, dependency, learning, later-controller, or canon
change may make rollback stateful/destructive. User state is never evidence or
a rollback instrument.

Record every mismatch as `ACCEPTED IMPROVEMENT`, `REQUIRED CORRECTION`,
`MASTERPLAN UPDATE`, `DEFERRED LIMITATION`, or `UNAUTHORIZED DIVERGENCE`, and
return it to the earliest owner. No downstream role silently changes the shell.

Return through Mission to Science before any new/reordered beat, step, save
field/version/schema, route authority, dependency, mandatory action,
learning/evidence change, later state, PBA overage, replay/privacy/Tour/access
defect, or inability to complete direct City/MH-40 E2E. Return canon/voice to
Colonel, scope/order to Operations, and product promise to Commandant.

`HOLD` for hidden lore, Martin's browser/profile/save, protected-path conflict,
media-byte/image/reveal work, new host/lesson/branch, Machine/Builder speech,
world response, reward, access, identity, authority, exam promise, successor,
RP-013, post-ending content, or destructive/external action.

## Mission signature and exact Recon handoff

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-001-v1`** at source
`e5d7891a72bb8d27a62d469007f4d7e8958b4fca`.

### Change history and shell-gate proof

- `FRSH-001-v1` — initial shell; reconciles `FRWO-001-v1` and
  `FRVE-001-v1`; no prior or superseded First Run shell exists.
- Production build identity remains the released TD-012 predecessor because
  Mission changed no product, test, media, or build file.
- The dedicated Mission commit is the Git commit introducing this shell and
  synchronized Recon handoff. Mission must push `main` and report the resolved
  commit plus equality of `git rev-parse HEAD` and
  `git rev-parse origin/main`; that post-commit proof does not alter this
  construction contract.

Reconnaissance Sergeant must inspect the current package/opening/Meadow/
Drowned/Witness/direct-City surfaces and unchanged accepted media, then issue
exactly one Directorial Treatment for this shell. Recon may define the felt
arc, attention, pacing, silence, and concept-level existing-media treatment
only; it may not write final copy, exact interaction, code, media, or a reveal.
