# First Run Shell Variance Decision - Sixfold Weir Live Geometry

Variance ID: `FRSH-003-v1-VR-01`

Disposition: **`HOLD / RETURN TO SCIENCE / FRSH-003-v1-VR-01`**

Stage / owner: Mission Captain / `mission_captain`

Shell under adjudication: `FIRST RUN SHELL READY / FRSH-003-v1`

Work Order / viability: `FRWO-003-v1` / `FRVE-003-v1`

Player-experience blueprint: `FRPX-003-v1`

Functional evidence: `HOLD / LIVE LAYOUT CONTRACT VARIANCE / FRCE-003-v1`

Mission control source inspected:
`4ad1d44e67d1c2e44b03afefc1c47983c60adc74`

Frozen product/test/manifest candidate:
`a9776e337f1820776864a5690332c364d0fb2556`

Frozen corrected harness candidate:
`bf58e528bc6ce4088f81f2c782ce2895259ab9fd`

Released rollback baseline:
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`

Date: **2026-08-10**

## Decision

Mission sustains the live `HOLD`. `FRSH-003-v1` remains the governing shell,
but it is not presently implementable as one conflict-free acceptance
contract at the two retained `320` layouts. No downstream role may choose
which frozen requirement to relax.

The exact product candidate is preserved. The exact corrected harness is also
preserved. This decision authorizes no product, test, E2E, copy, content,
media, maturity, release, or Quartermaster work.

The earliest owner of the live geometry contradiction is the Office of
Science Administrator. Science must reconcile the theoretical target-size
claim in `FRVE-003-v1` with the accepted live measurement before Tactical
clarifies measurement execution. Mission will adjudicate the resulting two
planning responses before any Combat repair or further complete journey.

## Evidence accepted without release promotion

Mission accepts these facts as diagnostic evidence only:

- `a9776e337f1820776864a5690332c364d0fb2556` is the exact bounded product,
  test, E2E, and manifest candidate exercised live.
- `bf58e528bc6ce4088f81f2c782ce2895259ab9fd` descends directly from that
  candidate and changes only two intended Host 05 E2E assertions in
  `playtest/e2e-playthrough.mjs`. It changes no product behavior, threshold,
  copy slot, manifest meaning, media, save, route, or ending.
- The seven exact placeholders remain frozen:
  `FRPX03_UNSEEN_INTERFACE`, `FRPX03_AVAILABLE`, `FRPX03_IN_PROGRESS`,
  `FRPX03_MISSED`, `FRPX03_MASTERED`, `FRPX03_RETURNED`, and
  `FRPX03_NEXT_BOUNDARY`.
- Focused `67/67`, related `74/74`, cold full `971/971`, validators `40/40`,
  builds `217/57`, JavaScript `1,666,377`, CSS `119,247`, modules `217`,
  immutable media `17 / 37,410,731`, and both served identities pass.
- The verification-only live return passed the former placeholder assertion,
  later rail, both equal MH-40 outcomes, zero-runtime-error checks, focus
  journey, and Host 05 `<=2ms` sample before failing closed at the live-layout
  aggregate.
- All six layouts retained source band above `0.95`, zero Host 04/return
  overlap, no horizontal overflow, and reduced-motion parity.
- The two retained layouts measured Host 05 at
  `62.796875 x 43.90625`. Height is `0.09375px` below the exact `44px`
  minimum, and the visible placeholder label escaped the measured target.
- Narrow, effective-`200%`, and retained source-center probes failed while
  measuring from `.scene-frame`; all six forced-color probes failed after
  programmatic focus followed a pointer-driven journey.
- No machine summary was emitted and no verifier ran. Therefore no complete
  live gate, release evidence, or maturity change exists.

These facts do not accept the candidate, authorize repair, waive the failed
measurements, or convert the failed E2E into release evidence.

## Frozen requirements and identities

The following remain exact and may not be weakened, rounded away, reworded as
advisory, or traded against one another:

- Host 05 physical registration `left 45% / top 75% / width 20% / height 25%`,
  identical canonical and narrow;
- source center `55% / 87.5%` or `x 919.6 / y 823.375` on the dry foreground
  six-branch relationship;
- source-band retention `>=0.95`, exact zero Host 04/return overlap, contained
  center, contained visible identity/state, no horizontal overflow, and an
  operable semantic target `>=44 x 44 CSS px` at all six layouts;
- one honest rendered-image source mapping under the actual `object-fit: cover`
  and `object-position: center 20%` behavior;
- keyboard-equivalent visible `:focus-visible` treatment in forced colors;
- the seven placeholders and all Scene/Pilot/Suit/System/901 Teacher meanings;
- pure sanitized state, sole campaign `USE`, completed read-only `USE`, and
  generic-launcher removal;
- unchanged `L02-02` content/evaluator/sanitizer/evidence/privacy/save,
  Host 04, `L02-03`, returns, route, later rail, equal MH-40 outcomes, null
  deltas, and `successor=null`;
- narrow/global PBA, offline, performance, immutable-media, external-temp,
  machine-summary, verifier, cleanup, and single-E2E gates; and
- exact product candidate `a9776e3`, exact corrected harness `bf58e52`, and
  rollback baseline `3e3da60`.

No rounding tolerance may treat `43.90625` as `44`. No probe correction may
hide a live product failure. No product adjustment may be made merely to make
an incorrect probe pass.

## Conflict decomposition and earliest-owner routing

### 1. Science owns the geometry viability contradiction

`FRVE-003-v1` predicts a retained scene of `320 x 180` and an exact
`20% x 25%` target of `64 x 45`. Live product evidence measures
`62.796875 x 43.90625` while the hotspot remains declared `45/75/20/25`.
The current frame has a size-consuming bottom border, and the hotspot's actual
containing box is not the theoretical `320 x 180` box used by Science.

This is a viability-contract contradiction, not Combat discretion. Science
must issue exactly one `FRVE-003-v1-VR-01` disposition:

- **`POLISH VIABILITY READY`** only if it defines one exact code-only,
  immutable-media solution that keeps `45/75/20/25` source registration,
  dry center, `>=0.95` retention, zero overlap, contained identity/state, and
  an actual `>=44 x 44` semantic target at all six layouts; or
- **`HOLD`** if those requirements cannot coexist inside the current shell,
  PBA, media, route, save, and presentation authority.

Science must explicitly distinguish and dimension all four coordinate spaces:

1. bordered `.scene-frame` border box;
2. the absolute hotspot containing block used by `.scene-world-content`;
3. the rendered `.scene-art` image content box and its actual cover crop; and
4. the Host 05 DOM activation and visible label/state rectangles.

For every required layout, Science must provide exact expected dimensions,
offsets, source transform, target rectangle, center, retention, overlap,
label/state containment, and minimum-size result. It must explain the
`320 x 180` theoretical-versus-live difference rather than masking it with a
tolerance.

The single implementable, non-weakened goal is:

> Preserve the exact `45/75/20/25` physical registration against the rendered
> immutable Drowned image while making the actual semantic Host 05 activation
> rectangle at least `44 x 44` at both retained layouts, with its visible
> identity/state contained and every crop, center, overlap, PBA, accessibility,
> and media gate still exact.

Science may authorize only the narrowest code/configuration presentation
mechanism needed to meet that goal. If it separates source registration from
the semantic activation rectangle, it must freeze the relationship between
them, prove that the expanded activation remains centered on the same honest
source relation, and prove exact zero overlap. If it instead changes the
containing-box mechanics, it must preserve the visible scene footprint, crop,
separator meaning, route hierarchy, and all unrelated surfaces. Science may
not prescribe final copy, change the registered source region, edit media,
relax `44px`, or authorize implementation/E2E.

### 2. Tactical owns two measurement-contract clarifications after Science

Only after a Science `READY` disposition, Tactical must issue one
`FRPX-003-v1-VR-01` clarification without product/test changes.

#### Rendered-image source mapping

The live source transform must originate from the actual rendered
`.scene-art.getBoundingClientRect()` and computed `object-fit` /
`object-position`, not from `.scene-frame.getBoundingClientRect()`. Tactical
must define the exact equations for cover scale, cropped source bounds, source
center, and Host 05 registration against the image content box. The probe must
also record frame, hotspot containing block, image, Host 04, Host 05, return,
and visible label/state rectangles so a corrected source probe cannot conceal
a target-size, overlap, or containment failure.

#### Keyboard-induced forced-color `:focus-visible`

The forced-color probe must enter forced-colors mode before focus verification,
clear the prior pointer-focused state, and reach Host 05 using actual keyboard
Tab/Shift+Tab navigation. It may not call `host.focus()` or use pointer
activation to create the asserted Host 05 focus. It must record:

- the preceding lawful focus owner and exact keyboard sequence;
- `document.activeElement` equal to Host 05;
- `element.matches(':focus-visible') === true`;
- `matchMedia('(forced-colors: active)').matches === true`;
- computed non-`none`, nontransparent visible outline at least `2px`, using
  `Highlight` or the browser's resolved system equivalent; and
- unchanged accessible name/state, target identity, semantic order, and
  reduced-motion result.

The same probe contract applies at all six layouts. Tactical may not replace
keyboard evidence with programmatic focus, weaken the outline requirement, or
claim that a corrected probe repairs the retained product geometry or label
failure.

## Return sequence and authorization state

The bounded return order is exact:

1. Office of Science Administrator: `FRVE-003-v1-VR-01` viability decision.
2. Tactical Operations Specialist: only after Science `READY`,
   `FRPX-003-v1-VR-01` probe clarification.
3. Mission Captain: reconcile both returns and issue a new versioned shell
   decision.
4. Combat Engineer: only after a later explicit Mission `READY`, apply one
   permitted correction and run the newly authorized validation ladder.

Current authorization is **STOP after Mission documentation**. Another
complete E2E is not authorized. Combat may not change product, tests, harness,
manifest, thresholds, placeholders, or reports. Quartermaster, Image
Specialist, and Intelligence may not begin. No maturity cell moves.

If Science returns `HOLD`, Tactical does not run and Mission continues the
cycle hold. If Science changes scope, source identity, media authority,
learning, save, route, world, ending, or any threshold, it is an unauthorized
divergence and returns to the earliest planning owner.

## Patch, content, media, and rollback firewall

This variance decision changes only Mission control documentation. It does not
modify or authorize changes to:

- product code, CSS, tests, E2E harness, command manifest, fixture, build, or
  runtime output;
- the seven Quartermaster placeholders or any player-facing copy;
- curriculum, evaluator, sanitizer, evidence, privacy, save, route, world,
  later rail, City contradiction, ending, or maturity controls;
- image/audio/media files, source requests, provenance, or asset inventory;
- protected/untracked paths, hidden lore, or Martin's browser/profile/save; or
- automation, schedule, reveal, successor, RP-013, or post-ending content.

Rollback remains the bounded return from exact candidate `a9776e3` plus exact
harness correction `bf58e52` to released baseline `3e3da60`. This decision
does not execute rollback and authorizes no broad reset, checkout, deletion,
or mutation.

## Validation and variance classification

- Confirmed current control `HEAD == origin/main == 4ad1d44` before editing.
- Confirmed `a9776e3` is the exact product candidate and `bf58e52` is its
  direct two-assertion harness descendant.
- Read current `FRVE-003-v1`, `FRSH-003-v1`, `FRPX-003-v1`, and complete
  `FRCE-003-v1` evidence.
- Reconciled the six exact live rows without rerunning a game, preview, build,
  test, validator, summary verifier, or E2E.
- Performed no product, test, media, content, maturity, user-state, protected-
  path, hidden-lore, or automation operation.

Variance classification:

| Finding | Classification | Owner / result |
| --- | --- | --- |
| Theoretical `45px` versus live `43.90625px` under exact `25%` height | `REQUIRED CORRECTION - OPEN` | Science viability return; no threshold relaxation |
| Narrow/effective/retained source-center failure from frame-box mapping | `REQUIRED CORRECTION - OPEN` | Tactical clarification after Science `READY` |
| Forced-color failure after programmatic focus in pointer modality | `REQUIRED CORRECTION - OPEN` | Tactical keyboard-induced probe after Science `READY` |
| Retained visible label escapes Host 05 target | `REQUIRED CORRECTION - OPEN` | Science must include containment in the implementable geometry contract |
| Stale post-primary/post-mastery placeholder assertions | `REQUIRED CORRECTION - CLOSED` | Exact harness `bf58e52`; no product or threshold change |
| Existing product behavior, learning, save, route, later rail, ending, media | `PRESERVED` | No change authorized or inferred |

## Mission signature and exact Science handoff

Mission Captain signs **`HOLD / RETURN TO SCIENCE /
FRSH-003-v1-VR-01`** from source
`4ad1d44e67d1c2e44b03afefc1c47983c60adc74`.

Office of Science Administrator must read this variance decision,
`FRVE-003-v1`, `FRSH-003-v1`, `FRPX-003-v1`, complete `FRCE-003-v1`, and the
exact current coordinate/style/runtime controls without changing them. Issue
exactly one `FRVE-003-v1-VR-01` `POLISH VIABILITY READY` or `HOLD` that solves
or rejects the one non-weakened goal above with exact six-layout math and a
bounded permitted mechanism.

Do not edit product/test/harness/manifest/media/copy, run E2E, change a
threshold, inspect protected/user state, begin Tactical, authorize Combat or
Quartermaster, alter learning/save/route/world/ending, advance maturity, create
a reveal/schedule, or call `FIRST RUN COMPLETE`.
