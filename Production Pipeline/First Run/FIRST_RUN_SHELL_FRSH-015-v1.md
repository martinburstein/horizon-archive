# FIRST RUN SHELL / FRSH-015-v1

## Disposition and identity

`FIRST RUN SHELL READY / GUIDE-CONDITIONED BUILT-IN MIGRATION / RECON NEXT`

Mission stage: `mission_captain` / `DESIGN -> MIGRATE -> OPERATE`.

```yaml
shell: FRSH-015-v1
work_order: FRWO-015-v1
baseline: FRPB-001-v13
continuity: FRCL-015-v1
viability: FRVE-015-v1
state_read: FRLS-SCI-004-v1
state_written: FRLS-MSN-005-v1
source_control_commit_read: d29d5e34319e469e9327a50f97f4fb968ea9d440
current_product: FRCE-014-v1@b4444afefe624ff231d986933a56c2003c0d8ac5
current_product_tree: 066077823b780ba83c5e71e7b478d117f3761dcc
best_and_committed: FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e
committed_product_tree: 612a7d53c4564563bfd0bdb7eeca110f715e5c82
current_maturity: FR0_missing_physical_expression
target: one_independently_released_bounded_pass_Intelligence_only
next_owner: reconnaissance_sergeant
```

Martin's explicit materially-different-mechanism authority permits this new
`FRM15` representation graph. It does not revive an `H14` ordinal, prompt,
candidate, shell, or unused call pool. The generic released path and inert
null-first product remain the rollback until Intelligence releases a superior
exact candidate.

## Independent Mission reconciliation

Mission found no product, canon, learning, privacy, accessibility, performance,
or release conflict among the five planning authorities. The only ownership
ambiguity was execution of the already-frozen deterministic guide control.
Mission resolves it without changing Science's bytes or verifier:

- Combat may validate and make only the shell-required fail-closed source-law,
  configuration, tests, and `FRPBA-015-v1` changes; if inspection proves no
  product delta is required, Combat issues `REUSED / NO PRODUCT DELTA`.
- Quartermaster executes the unchanged deterministic guide builder/verifier,
  views the guides, and owns all stochastic calls, custody, private selection,
  deterministic derivatives, accepted-only import, and provenance population.

This is a responsibility assignment, not a technical-envelope variance.
Quartermaster may not edit the guide controls or prompts. Combat may not build
or view the production guides or invoke image generation.

Mission independently verified:

| Control | Bytes | SHA-256 / result |
| --- | ---: | --- |
| `FRM15_GUIDE_SPEC_v1.json` | 3,859 | `d749d452bce7cc4747b25908ad4824275ba038debb7686e07c4a7502a5f6bf96` |
| `FRM15_GUIDE_CONTROL_v1.py` | 14,107 | `214a26f29443c91138cb4768fb54e40c9d7ce1980392cd6ecc20aae6a24a7037` |
| `FRM15_CUSTODY_DAG_v1.py` | 10,558 | `74ee796d87287ed7b8c4c281396a3fbdcc37897a472d89761fb5090234158329` |
| `FRM15_MEDIA_TRANSFORM_v1.py` | 6,400 | `cbc06354e448af6ece96d61bb5154c98c1b206ee05592db61f9f11256a59990f` |
| `FRM15_PROMPT_R01_v1.txt` | 3,050 | `5cf70f69727aa742045d9da6008c77f085894dfdd934f5493801170b08869cdd` |
| `FRM15_PROMPT_E01_MATERIAL_v1.txt` | 1,553 | `f56572f6812f1147f20bc468e5c56c4ce7b6c664ba4d3fd2656368ed27dc9877` |
| `FRM15_PROMPT_E02_TRACE_v1.txt` | 1,238 | `2c759d0c29a1c42218fd96e5005e2444a69b0bddcd5998f6667119ce5d9f2445` |
| `FRM15_PROMPT_E03_SEAMS_v1.txt` | 1,263 | `2e9dfdccd42f056d1ccd363b21811f9effd043ea0e381adc96cc0ced542e6f90` |
| Python / Pillow | N/A | `3.12.10 / 12.2.0` exact |
| guide synthetic self-test | N/A | PASS; base PASS, four adversarial mutations rejected, stable hashes |
| custody-DAG synthetic self-test | N/A | PASS; adoption/chain PASS and byte mutation rejected |
| transform synthetic self-test | N/A | PASS; deterministic normalize/composite and nonbinary-mask rejection |
| accepted baseline manifest | 5,512 | `7057dd8f24f46086e5591d326706d537876030959ce6e058bd7b826683b28c30`; `24 / 154,163,567` reused |
| current product identity | N/A | exact tree equals `FRCE-014-v1`; working product diff zero |

The synthetic self-tests used temporary self-deleting fixtures only. Mission
did not build or view a production guide, call `image_gen`, inspect candidate
media, import media, change product/runtime, or run a product test, build,
validator, browser, or E2E gate.

## Player contract and protected truth

The shell owns one bounded, dry, reachable, low nonhuman constructed physical
relationship at the exact earliest unresolved address and its existing two-
lesson native entry. The felt improvement is that three construction histories,
their pairwise interdigitation, one continuous trace with three substrate
reactions, and unequal branching recessed seams become physically legible
without changing the world.

Fixed behavior remains:

- exact predecessor completion enters the same dry adjacency;
- LOOK is write-free; TALK produces complete nonresponse;
- USE opens only the first incomplete already-owned lesson after every source,
  predecessor, provenance, geometry, layout, and decode guard passes;
- lesson order, scoring, actual-gap remediation, fresh transfer, explanation,
  close/Escape, reopen, reload, resume, return, privacy sanitation, Demo Tour
  isolation, and no-exam/no-authority language remain unchanged;
- failure and mastery change expedition-owned evidence/time only;
- the world never answers, moves, rewards, opens, identifies, approves, or
  remembers; and
- the single route, later boundary, equal RP-012 ending, and `successor=null`
  remain exact.

No guide, candidate, source name, encounter name, or media path may be exposed
to Martin. Repository QA/screenshots, Martin's browser/profile/save, hidden
lore, protected PDFs/training files, archives, rejected/deleted sources,
accepted-media pixels, opaque residual roots, and unrelated worktree dirt are
forbidden inputs and remain unopened and unchanged.

## Literal paths, identities, and custody

All paths below are literal and initially absent where marked. No environment
variable, glob, recursive discovery, broad deletion, alternate root, or path
substitution is authorized.

```yaml
repository_root: C:\Users\marti\OneDrive\Desktop\Horizon Archive
control_root: C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run
custody_root_initially_absent: C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_WORK_v1
guide_output_dir_initially_absent: C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_WORK_v1\G01
custody_ledger_initially_absent: C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_LEDGER_FRLG-015-v1.jsonl
private_acceptance_initially_absent: C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FIRST_RUN_PRIVATE_ACCEPTANCE_FRPA-015-v1.json
selected_provenance_initially_absent: C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FIRST_RUN_SELECTED_PROVENANCE_FRSP-015-v1.json
accepted_source_destination_initially_absent: C:\Users\marti\OneDrive\Desktop\Horizon Archive\Visual Direction\Production Masters\2026-08-13-first-run-host14\host14-waterline-ledger-master-v1.png
runtime_source_path: Visual Direction/Production Masters/2026-08-13-first-run-host14/host14-waterline-ledger-master-v1.png
pba_overlay_required: C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FIRST_RUN_PBA_FRPBA-015-v1.ps1
```

Quartermaster creates the custody root with one nonrecursive, no-replace
directory operation, then initializes the external ledger:

```powershell
$root = 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_WORK_v1'
$ledger = 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_LEDGER_FRLG-015-v1.jsonl'
if (Test-Path -LiteralPath $root) { throw 'custody root already exists' }
if (Test-Path -LiteralPath $ledger) { throw 'custody ledger already exists' }
New-Item -ItemType Directory -Path $root -ErrorAction Stop
python 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_DAG_v1.py' init --ledger $ledger --root $root
```

Every retained guide, returned raster, mask, and derivative is an immutable
ledger node. Each stochastic ordinal is consumed at launch and is recorded in
the adopted output node's `operation` object. If the built-in return is
ambiguous or lacks an identity-provable local file, the ordinal is recorded in
the Quartermaster report, all new effects stop, and the run returns
`STOP_SAFETY`; it is not silently retried or represented as a DAG node.

The built-in-return source must be an absolute local path explicitly returned
by the tool under its generated-images custody. An `http(s)` URL, data-only
image without a returned local file, relative path, missing path, changed
identity, reparse point, symlink, or multi-link file is not adoptable. On a
valid result, `FRM15_CUSTODY_DAG_v1.py adopt` copies bytes create-new to the
literal custody-root node path, then `add` records its node, ordered parents,
call ordinal, prompt identity, ordered references, tool mode, and adoption
hash. No output path is predicted before the tool returns.

## Deterministic guide contract

Quartermaster alone executes the exact unchanged controls after Combat and
the blueprint handoff:

```powershell
python 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_GUIDE_CONTROL_v1.py' build --spec 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_GUIDE_SPEC_v1.json' --out-dir 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_WORK_v1\G01'
python 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_GUIDE_CONTROL_v1.py' verify --spec 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_GUIDE_SPEC_v1.json' --out-dir 'C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_WORK_v1\G01'
```

The three expected guide hashes are exact:

```yaml
FRM15-G01-STRUCT.png: 974dba59c839c2671d47282c30c0a472e4892de6dd76a278a70a36082379dade
FRM15-G01-MAT.png: 014628650fa332347f6726c804426f1952dbc1246d07a4a068c4ac60ffb22d2e
FRM15-G01-PROT.png: ccd198f6bfa09ec485fbe482b93720011d4710de1d3a2627b7a22a340562d91e
```

Each is added as a `guide` node with no parents and the exact spec/control
hashes. `FRM15-G01-MANIFEST.json` is a control record. A PASS gives zero
candidate-acceptance credit. The exact material-ID palette, guide lines,
masks, labels, IDs, legends, and diagram structure must not survive in a
player-facing candidate.

Only guide family `G01` is executable in this shell. The hard program ceiling
remains two families, but `G02` requires a returned Science/Mission variance
that changes structural or material representation and supplies a new spec,
control identity, retention plan, and predicted measurement. Wording, palette,
seed, crop, or another call against unchanged guides cannot constitute G02.

## Built-in image-generation and edit graph

Only the built-in `image_gen` tool is permitted. No CLI, API key, endpoint,
model string, size/quality/output flag, batch, `n`, hidden retry, alternate
transport, or direct SDK/API call is authorized. Concurrency is one; each tool
call requests exactly one artifact and consumes exactly one `FRM15-C##`
ordinal when launched, including timeout or ambiguous completion.

Before `C01`, Quartermaster locally views all three exact guides in this order:
structure, material ID, protection/layout. Those views verify input roles and
do not judge or reveal a candidate. Base call `C01 -> FRM15-R01` uses the exact
bytes of `FRM15_PROMPT_R01_v1.txt` and this built-in request shape:

```yaml
tool: image_gen.imagegen
prompt: exact_UTF-8_text_of_FRM15_PROMPT_R01_v1.txt
referenced_image_paths:
  - C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_WORK_v1\G01\FRM15-G01-STRUCT.png
  - C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_WORK_v1\G01\FRM15-G01-MAT.png
  - C:\Users\marti\OneDrive\Desktop\Horizon Archive\Production Pipeline\First Run\FRM15_CUSTODY_WORK_v1\G01\FRM15-G01-PROT.png
num_last_images_to_include: OMIT
```

After adoption and technical inspection, Quartermaster privately views the
candidate and records the frozen hard vector. If it is not a complete PASS,
an edit may address exactly one diagnosed group. The current search-best must
be locally viewed before the edit. Edit calls use this ordered reference list:

```yaml
referenced_image_paths:
  - exact_absolute_path_of_current_search_best
  - exact_G01_structure_path
  - exact_G01_material_path
  - exact_G01_protection_path
num_last_images_to_include: OMIT
```

The prompt must be the exact bytes of precisely one matching frozen file:

- material/process/interdigitation defect -> `FRM15_PROMPT_E01_MATERIAL_v1.txt`;
- trace/substrate-reaction defect -> `FRM15_PROMPT_E02_TRACE_v1.txt`;
- seam/branch/merge defect -> `FRM15_PROMPT_E03_SEAMS_v1.txt`.

The current-best raster is the edit target; the three guides are immutable
references. The prompt repeats invariants. One call cannot repair two groups,
use an unrecorded reference, or regenerate the whole scene under an edit label.
Each returned edit is independently adopted before inspection. Node IDs are
`FRM15-E01` upward in launch order; call ordinals remain `FRM15-C02` upward.

Initial authority is exactly `C01` plus at most two diagnosed single-group
edits (`C02`, `C03`). Calls `C04` and `C05` are earned only when the immediately
preceding candidate strictly improves the lexicographic search-best vector
without regressing a passing hard item and the next frozen edit has one
localized predicted effect. `C06` is reserved for a candidate with exactly one
localized hard defect and every other technical, physical, adversarial,
layout, accessibility, provenance, and cleanup pre-import gate passing. Search
cannot consume `C06` or the final product-proof reserve.

## Deterministic transform and selected-source contract

Only `FRM15_MEDIA_TRANSFORM_v1.py` may create raster derivatives. Allowed
operations are exact `inspect`, `normalize`, and at most one binary-mask
`composite` as implemented by the frozen hash. Normalization performs
recognized-profile conversion to sRGB, fixed black alpha flattening, centered
Lanczos cover/crop to `3840x2160`, metadata stripping, and RGB PNG encoding.
Composite selects between two already-normalized `3840x2160` parents with one
exact `0/255` mask on a frozen guide/process boundary, then performs the same
clean encode.

Every effect creates a new file and DAG node. No in-place operation, repaint,
clone, content-aware fill, arbitrary crop, semantic geometry invention, or
crop-based concealment is allowed. Repeated normalization of unchanged input
must be byte-identical. Composite is optional and may preserve separately
passing regions only; it cannot invent missing meaning.

The first candidate to pass every pre-import gate becomes conceptual identity
`FRM15-A01`. It must be an ordinary strict-decode RGB PNG, exactly
`3840x2160`, opaque 8-bit sRGB intent, metadata-clean, `1..30,000,000` bytes,
and derived from the complete recorded DAG. The selected provenance record
uses `horizon.first-run.frm15-source-provenance.v1` and records the selected
node, all parents, exact bytes/SHA-256, prompt/control identities, operations,
review vector, and source/destination import identities.

After `FRM15-PA##` is a complete PASS and destination absence is re-proved,
Quartermaster imports the selected bytes atomically no-replace with the frozen
custody `adopt` command using the selected root file as `--source` and the
literal accepted destination as `--destination`. Source and destination
bytes/hash must match. A mismatch deletes only the newly created destination
after identity proof, restores null-first, and stops.

Retained custody is hard-capped at `16` files and `536,870,912` bytes. The v1
plan permits one four-file G01 directory (three guides plus manifest), at most
six adopted stochastic rasters, at most four retained raster derivatives, and
at most one binary mask: maximum `15` retained files. Ledger and acceptance/
provenance controls lie outside the custody root. No additional retained file
may be created when the count would exceed 16. The deterministic-operation
hard ceiling remains 12: four guide build/validation operations and at most
eight normalize/composite operations; file retention can stop this earlier.

## Private hard gates and best-state law

After technical/custody PASS, Quartermaster as a reviewer who did not author
the prompt records a conjunctive visible-pixel vector. It requires:

- low nonhuman concave face, continuous dry foreground, and lateral receded water;
- three processes identifiable by construction evidence rather than color;
- horizontal and vertical overlap and at least two interlocks/islands for each pair;
- one continuous irregular trace across all three and three substrate reactions;
- at least three unequal nonparallel upward recessed seams and one branch/merge;
- zero barrier/impoundment, human infrastructure/hardware, natural-geology or
  biological substitution, writing/UI/answer/message/response, protagonist,
  guide leakage, glow/activation, reward/access, or world-change cues.

One failed hard item rejects the candidate. Prompt text, guide correctness,
copy, alt text, bounding boxes, and generator self-assessment cannot rescue it.
Visible-pixel geometry must then pass displacement/removal, one-box alias,
color-only, guide-leak, center-crop, crop-shift, and mask-edge adversaries.

Six exact layouts remain `1920x1080`, `1366x768`, `390x844`, `768x900`
effective `200%`, `320x180`, and `320x240`. Relation retention is `>=.95`;
every essential center is visible; target is contained and `>=44x44` CSS px;
physical center is inside target; label is contained with `>=8px` focus
separation; protected overlap is zero. A declared mutation must reduce
retention by `>.05`, hide a center, shrink the target, or create protected
overlap and be rejected.

Keyboard, pointer, touch, semantic/switch-like activation share one action;
held/duplicate activation cannot double-dispatch. Visible focus and return,
announcement, narrow/effective-200% reflow, forced-color containment,
reduced-motion parity, no horizontal escape, and non-color/non-sound/non-motion
equivalence are hard gates. Human assistive-technology usability remains
unavailable and is not claimed.

Search-best order is lexicographic: fewer hard failures, then more complete
mandatory groups, then six-layout vector, then private preference only after
hard ties. Deadband is unchanged hard codes/group count, retention delta
`<.02`, target delta `<2px`, and unchanged center/protected counts. Current,
search-best, selected, and committed states are recorded separately. The first
full release-quality PASS stops all generation and edit calls.

Private candidate inspections are capped at eight: up to six search
inspections, one selected-source reconciliation, and one independent
Intelligence holdout. Guide input views are guide-control verification, not
candidate scoring. No candidate is inspected twice on unchanged bytes merely
because ownership changes.

## Runtime source-law and PBA migration

Combat is limited to the smallest source-law/config/test/PBA surface needed to
admit the new namespace while the registry remains disabled/null:

```yaml
runtime_schema: horizon.waterline-ledger.v2
source_identity: FRM15-A01
provenance_schema: horizon.first-run.frm15-source-provenance.v1
runtime_path: exact_frozen_path_above
source_format: png
source_color: opaque-srgb-8
source_dimensions: 3840x2160
source_bytes: 1..30000000
fallback: fail_closed_hidden_and_two_generic_launchers_active
save_boundary: no_source_identity_or_production_metadata_persisted
```

Absent, disabled, malformed, stale, wrong-path, wrong-ID, byte/hash/format/
decode/provenance mismatch, missing geometry, or invalid layout stays hidden.
No new dependency, service worker, runtime fetch, SDK, Python/WASM runtime,
endpoint, source map, audio, video, font, or remote request is permitted.

Combat creates `FRPBA-015-v1` as a minimal path/identity overlay of
`FRPBA-014-v1`, preserving the exact `FRAM-014-v1` baseline hash and requiring:

```yaml
selected_png_bytes: 1..30000000
accepted_media: exactly_25_files_and_at_most_184163567_bytes
decoded_rgba_working_set: <=33177600
javascript_bytes: <=1785000
css_bytes: <=122000
production_modules: <=236
source_maps: 0
emitted_runtime_files: 28
emitted_runtime_media: 25_exact_hashes_baseline_plus_selected
runtime_requests: local_only_one_new_selected_raster_zero_remote
source_activation_ms: <=1500
sampled_main_thread_task_ms: <=50
runtime_errors_or_unhandled_rejections: 0
```

Quartermaster may populate only exact accepted source identity, provenance,
visible-pixel-derived geometry/layout records, the existing frozen copy, and
runtime URL after pre-import PASS. It may not change behavior or copy meaning.

## Role sequence and freedoms

1. Recon issues `FRCT-015-v1`, validating/finalizing one directorial treatment
   from the locked physical sentence. No guides, media, product, browser, or
   generation action.
2. Tactical issues `FRPX-015-v1`, validating/finalizing source-space geometry,
   interaction, focus, crop, responsive, fallback, and recovery blueprint. No
   guides, media, product, browser, or generation action.
3. Combat issues `FRCE-015-v1`. It makes only the source-law/config/test/PBA
   changes above or reports `REUSED / NO PRODUCT DELTA`; source stays
   disabled/null. It may not build/view guides or call/inspect/import media.
4. Quartermaster creates/verifies/views guides, operates built-in generation
   and edits, owns custody/DAG/private review/allowed transforms, stops at first
   PASS, atomically imports only accepted bytes, populates frozen source data,
   and issues `FRCA-015-v1`.
5. Image Specialist acts only after accepted import and performs code/config-
   level runtime presentation using accepted media. It cannot generate, edit,
   replace, vary, composite, normalize, or reveal media bytes.
6. Intelligence starts fresh, freezes the exact candidate, executes the final
   proportional holdout, classifies variances, and alone releases or advances
   maturity.

Any downstream ambiguity or change to guide schema, prompts, references,
budget, transform, path, custody, physical facts, learning, copy, route, save,
world response, or ending returns to the earliest owner through Mission.

## Evidence-efficient proof cascade

Evidence is content-addressed. A role handoff is never an invalidation reason.
`FRCE-014-v1` focused `31/31`, related `63/63`, full `1047/1047`, validators
`40/40`, builds `234/57`, and null-first PBA are reused until their exact
verifier-relevant inputs change.

Run only this smallest affected cascade:

1. guide/control hash and synthetic self-tests;
2. guide build/verify and custody chain;
3. after Combat source-law changes, focused registry/projection tests only,
   plus affected related tests if the diff touches shared route/save/privacy;
4. private technical/physical/adversarial/source-layout acceptance;
5. after accepted source activation, focused and affected related tests;
6. one cold full suite only after the last behavior-affecting JS/config/test
   change; do not rerun it after media-only or CSS-only changes;
7. curriculum/readiness validators only if their exact inputs changed;
8. production and sanctioned fixture builds, `FRPBA-015-v1`, built hash/source
   identity, local request proof, six-layout sanctioned Edge review, and one
   clean-start E2E;
9. cleanup and exact candidate identity; then independent Intelligence
   holdout using the smallest fresh evidence that can fail independently.

If Intelligence receives the same JS/config/test hashes as the one cold full
run, it reuses that exact evidence rather than rerunning all tests. It must
still independently corroborate selected identity, PBA, served source,
six-layout/accessibility behavior, clean-start journey, cleanup, and rollback.
Every rerun outside this cascade must name the exact invalidated input and new
failure mode.

## Budget, plateau, cleanup, and rollback

```yaml
budget:
  guide_families: {hard_max: 2, executable_now: 1}
  stochastic_calls: {hard_max: 6, initial: 3, earned: 2, near_pass_reserve: 1}
  deterministic_operations: {hard_max: 12, guide: 4, normalize_composite: 8}
  private_candidate_inspections: {search: 6, selected: 1, intelligence: 1}
  retained: {files: 16, bytes: 536870912}
  concurrency: 1
  outputs_per_call: 1
  final_product_and_release_proof: FULL_PROTECTED
```

Two equivalent failures require a fresh causal diagnosis before another call.
Three non-improving same-family candidates force `REPLAN`,
`RETURN_TO_OWNER`, `HOLD`, or `STOP_LOW_MARGINAL_VALUE`; an equivalent fourth
is barred. Earned calls require verified vector improvement and one predicted
localized effect. The reserve requires exactly one remaining localized hard
defect. First full PASS stops. Stop when expected marginal player value is
below cost, risk, storage, regression, or cleanup exposure.

Before import, rollback leaves product/media unchanged and removes only exact
identity-proved tool/custody leaves allowed by the ledger. After import, every
product-bearing commit is dedicated; ordinary `git revert` in reverse order is
the rollback. Never reset, check out, overwrite, or delete user/unrelated
files. Restore the disabled/null v2 registry and exact generic launchers.

Rejected source identity and vector are recorded before deletion. A parent
needed by a retained derivative remains immutable. Final cleanup enumerates
only literal ledger-owned paths, proves containment and identity, deletes
leaves before parents, removes the root only when empty, and records residual
count zero. No glob or recursive delete is authorized. Ambiguous identity or
residual state is `STOP_SAFETY`, not cleanup by guess.

## Exact Recon handoff

Recon reads `FRPB-001-v13`, `FRCL-015-v1`, `FRWO-015-v1`, `FRVE-015-v1`, this
shell, and `FRLS-MSN-005-v1`. It issues only `FRCT-015-v1`, translating the
fixed physical sentence into a directorial treatment and validating guide/
render feel, hierarchy, material evidence, anti-human shorthand, camera,
light, and restraint. It may not create or view a guide/candidate, call
`image_gen`, modify product/media, run browser/E2E, change the budget, or
advance maturity.

```yaml
convergence_handoff:
  mode: DESIGN_to_OPERATE
  state_version_read: FRLS-SCI-004-v1
  state_version_written: FRLS-MSN-005-v1
  current_ref: FRWO-015-v1 / FRCE-014-v1_null_first@b4444afefe624ff231d986933a56c2003c0d8ac5
  best_ref: FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e
  committed_ref: FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e
  binding_gap_or_hypothesis: deterministic guides plus bounded built-in reference rendering and local edits can preserve low topology and constructed-process causality jointly
  action_kind: information
  predicted_effect: production can test the new representation with independently failing gates and finite earned continuation
  verifier_vector:
    planning_conflict: PASS_NONE_AFTER_ROLE_ASSIGNMENT
    frozen_control_hashes: PASS
    guide_self_test_and_adversaries: PASS
    custody_chain_and_mutation_sensitivity: PASS
    transform_determinism_and_mask_sensitivity: PASS
    exact_builtin_reference_graph: PASS_FROZEN_NO_CALL
    literal_custody_import_pba_rollback: PASS_FROZEN
    product_media_runtime_maturity_delta: ZERO
    product_evidence: REUSED_FRCE_014
  delta_vs_best: zero_product_zero_media_zero_maturity_positive_executable_shell_information
  budget_used:
    guide_families: 0
    stochastic_calls: 0
    deterministic_media_operations: 0
    private_candidate_inspections: 0
    product_tests_builds_browser_e2e: 0
  budget_remaining:
    guide_families: 2_hard_1_executable
    stochastic_calls: 6_hard_3_initial
    deterministic_media_operations: 12
    private_candidate_inspections: 8
    final_proof: FULL_PROTECTED
  remaining_uncertainty:
    - actual built-in result path, format, guide adherence, and edit locality
    - final physical meaning and private/adversarial classification
    - source geometry, six-layout, accessibility, performance, and E2E proof
    - future exact product candidate and Intelligence release
    - human assistive-technology usability study
  decision: GATHER_EVIDENCE
  next_owner: reconnaissance_sergeant
```

Mission signature:
**`FIRST RUN SHELL READY / FRSH-015-v1 / RECON NEXT`**.
