# FIRST RUN WORK ORDER / FRWO-012-v1

## Disposition

`WORK ORDER READY / ROLLING-POOL TERMINAL 12 PASS`

Address: `FR-03 / Host 12 / Drowned Switchyard`. Predecessor:
`FRAB-011-v1`. Current physical inventory is one accepted compression, seven
exact encounters, and four missing; target is one additional exact encounter
without promoting the address above `FR0`.

Execute up to nine sequential, one-output imagegen CLI calls allocated from the
rolling pool. Local ordinals are `{H12-1..H12-9}`. The first complete technical,
physical, responsive, and accessibility PASS stops the local ledger; every
unused call remains in the rolling pool for a later full First Run loop. Use
`gpt-image-2`, `3840x2160`, high, opaque PNG, `n=1`, no augmentation,
reference, edit, variation, batch, force, or internal retry. Science and Mission
must freeze the exact prompt set and CLI identity before generation.

Scratch root is the fresh initially absent owned OS-temp directory
`C:\Users\marti\AppData\Local\Temp\horizon-archive-host12-f88a32cc-bc96-48a6-9ec2-a0f3c725a2b5`.
Outputs are exact `h12-1.png` through `h12-9.png`. Product path is
`Visual Direction/Production Masters/2026-08-12-first-run-host12/host12-drowned-switchyard-master-v1.png`
plus `PROVENANCE.md`.

PASS requires an ordinary nonreparse opaque RGB PNG, exact `3840x2160`, <=30
MB, and strict decode. Physical review must prove one continuous dry high-slab
approach; one distinct fused upper crown wholly above present water; at least
three bypass seams visibly rising from water into the crown; at least two sealed
sample chambers above water and visibly closed; and at least two separate
drowned returns converging below or behind the accessible structure. No swim,
dive, descent, open chamber, console, portal, kiosk, readable glyph, projected
UI, answer, face, person, creature, ship, or intelligent response may appear.

Responsive proof derives actual `object-fit: cover` projection from measured
source rectangles at desktop, laptop, narrow, effective-200%, `320x180`, and
`320x240`. The complete physical sentence must retain >=95%; each crown, seam,
chamber, approach, and submerged-return center must remain visible; the target
must remain contained and >=44 CSS px; label/focus/protected overlap must be
zero; and real-browser corroboration must pass. No arbitrary absolute band may
override an otherwise complete derived-layout PASS.

Rejected evidence records local ordinal, bytes/SHA, technical/physical
booleans, measured rectangles, derived layout scalars, and bounded reasons,
then performs exact identity deletion and absence proof. Accepted-only
integration replaces the generic `L05-03 + L05-04` launchers with native
LOOK/TALK/USE, focus, recovery, decode, final copy/alt, provenance, and six-
layout evidence. Runtime attempt identity is `H12-n`, never a reused ordinal.

Budgets: accepted media <=23 /155,903,080; JS <=1,760,000; CSS <=121,200;
modules <=233; maps0. Focused, related, validators, full product, clean build,
served identity, isolated real-browser responsive, and one clean-start E2E are
required. Rejected media never enters the workspace.

Accepted Hosts 01-11 and media, lessons, evidence/privacy/save, one route,
RP-012, `successor=null`, protected QA, real browser/save, hidden lore, opaque
residuals, and later hosts are protected. Quartermaster alone may make frozen
calls and import the first passing raster. Image Specialist remains runtime
code/configuration only. Intelligence alone releases and carries unused calls.

Exact next owner: one Office of Science Administrator.
