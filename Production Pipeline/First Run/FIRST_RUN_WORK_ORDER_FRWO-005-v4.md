# Horizon Archive First Run Work Order

Work Order ID: `FRWO-005-v4`

Title: **Stranded Lens Cradle - One New Source and Fixed Lesson Integration**

Stage: Operations Planning Major / `operations_planning_major`

Disposition: **`WORK ORDER READY / BOUNDED BUILT-IN MANAGED-DIRECTORY
IDENTITY DISCOVERY / EXACT SOURCE CLEANUP / ORDINALS 2-3 PRESERVED`**

Date: **2026-08-11**

Operations source inspected:
`8a36ef773eb2d949b4f134b14d6d34670b74b5c6`

Reopened baseline / continuity: `FRPB-001-v2` / `FRCL-004-v2`

Superseded Work Order / viability: `FRWO-005-v3` / `FRVE-005-v3`

Mission return: `FRSH-005-v1-VR-07 / REVISE`

Quartermaster return: `FRCA-005-v2 / HOLD`

Released First Run predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Frozen candidate tree:
`09da6293d72c8123b8d9673bd8e41329338e8d13`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Planning controls: `FRRM-005-v4` / `FRSB-005-v4`

First-run address: `FR-03 / Chapter II - Drowned Archive / Host 06`

Learning owner: existing sole unchanged `L02-03`
model/deployment/configuration work

Exact next recipient: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Operations adjudication

Operations accepts `FRSH-005-v1-VR-07` as a lawful bounded Work Order return.
The same-isolate data-URL writer required by `FRWO-005-v3` is unavailable,
but the installed `imagegen` skill still prefers the built-in tool and states
that project-bound outputs are copied or moved from
`$CODEX_HOME/generated_images/...`. The narrow lawful replacement is one
built-in call bracketed by exact nonrecursive metadata snapshots of that one
managed directory, admission of exactly one uniquely new ordinary file by
canonical Windows identity, one exact-path stream copy/hash into a
predeclared create-new OS-temp candidate, and immediate deletion and absence
proof for only that identity-proved managed source.

This is a Work Order staging correction, not a Mission-only adapter patch.
`FRWO-005-v4` supersedes `FRWO-005-v3` and replaces only its direct-data-URL
materialization, opaque future-residual policy, and corresponding ingress /
cleanup failure rules. It does not rewrite the historical facts of ordinal
`1`, `FRCA-005-v2`, or `FRSH-005-v1-VR-07`.

The managed-directory delta is the sole candidate ingress authority.
`output_hint` is always opaque and unread. A direct canonical PNG data URL,
if independently available, is optional cross-check evidence only: it may
never name a path, create a file, supply project bytes, substitute for the
managed source, or make an otherwise ambiguous delta admissible. No CLI,
REST/API, browser download, extension, alternate generator, or manual-file
fallback exists.

The prospective contract is technically detailed but not yet production
authority. Fresh Science must prove its Windows identity, owner, timestamp,
race, no-follow, copy/hash, delete, absence, bounded-memory, privacy, and
fixture semantics. Fresh Mission must then issue a new shell before ordinal
`2` can run.

## Preserved bounded outcome, address, and maturity

The one player-visible outcome remains exactly:

```text
exact Host 05 / Sixfold Weir mastery
-> lens-like fragment handoff
-> one distinct local dry Host 06 / Stranded Lens Cradle
-> sole unchanged L02-03 entry and loop
-> unchanged next Drowned learning boundary
```

The changed boundary begins only after exact sanitized Host 05 / `L02-02`
mastery and hard-stops immediately after exact `L02-03` mastery. Entry, exit,
return, reload, recovery, checkpoint, pacing, player-time, route, world,
ending, and no-Host-07 boundaries remain those frozen by the effective shell.

Maturity remains unchanged. `FR-03` remains continuity `FR2`; physical-host
expression `FR0 - 1 accepted shared compression / 1 exact / 10 missing`;
learning `FR2`; behavior/save/recovery `FR1`; content `FR2`; presentation
`FR3`; and prior bounded release proof `FR4`. Only Intelligence may later
record `1 accepted shared compression / 2 exact / 9 missing` from accepted
as-built evidence.

## Frozen ordinal and residual state

- Total generation ordinal domain remains exactly `{1,2,3}`.
- Ordinal `1` remains permanently consumed by the historical opaque result-
  path stop and is never retried, recovered, discovered, inspected, or
  cleaned.
- Ordinal `2` remains unconsumed and unavailable until fresh Science and
  Mission both accept this exact v4 contract.
- Ordinal `3` remains unconsumed and conditional on a future lawful ordinal-2
  objective source rejection plus exact managed and temp cleanup.
- Built-in calls in `FRCA-005-v2` remain exact `0`.
- The current conservative possible ordinal-associated managed residual count
  remains exact `1`, associated only with ordinal `1`.
- A future ordinal whose one admissible managed source is deleted and proved
  absent adds no retained residual. Any post-call ambiguity before exact
  absence consumes that ordinal, stops opaque, and conservatively adds at most
  that ordinal's one possible residual; it never authorizes later discovery
  or cleanup.
- The ordinal-1 residual remains **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY
  / NON-GATING / OPAQUE BUILT-IN MANAGED RESIDUAL`** with zero candidate,
  provenance, evidence, reuse, reveal, rollback, maturity, or release
  authority.
- VR-65 remains separate, opaque, non-gating, and inaccessible.

No state changes in this planning stage: ordinals `2` and `3` are preserved,
and the conservative managed-residual count remains exactly `1`.

## Exact managed parent and bounded snapshot domain

Science must make this complete prospective adapter executable before Mission
may authorize a call:

1. Resolve trusted `$CODEX_HOME` once as a scalar without enumerating it,
   canonicalize it through a metadata-only ordinary-directory handle, and
   append only literal `generated_images`. Require the final normalized DOS
   path to equal exactly `C:\Users\marti\.codex\generated_images` for this
   candidate. The directory must exist, be an ordinary directory, and have no
   reparse, symlink, junction, mount, device, alternate-stream, placeholder,
   offline, sparse, or name-surrogate state.
2. Prove by canonical final path plus volume/file identity that the exact
   managed parent is outside the repository and every accepted-media, QA,
   protected PDF, training, browser/profile/save, user-work, OS-temp candidate,
   VR-65, opaque-residual, and other protected root. Never enumerate a parent,
   sibling, ancestor, fallback, or search root.
3. Resolve the OS temporary parent separately as one scalar without listing
   it. Create one fresh GUID-in-D-format direct child with create-new semantics,
   prove exact containment and protected-root separation, and predeclare only
   absent `attempt-02.png` or conditional `attempt-03.png`. The GUID child is
   outside both the repository and managed parent.
4. The snapshot implementation may enumerate only direct children of the
   exact managed parent, nonrecursively, and is bounded to at most `4096`
   entries. Each child name must be one direct component of `1..255` UTF-16
   code units with no separator, stream suffix, `.` or `..`. No child content
   is opened, read, hashed, decoded, rendered, previewed, or traversed.
5. The before snapshot is memory-only and admits ordinary direct files only.
   Any pre-existing directory/subdirectory, reparse point, symlink, junction,
   mount, device, alternate stream, sparse/offline placeholder, unknown type,
   hard-link count other than one, inaccessible metadata, duplicate canonical
   name, path escape, or more than `4096` children is a pre-call fail-closed
   HOLD that consumes no ordinal.
6. For each admitted pre-existing direct file, record only its exact child
   name, exact canonical final path, attributes and reparse tag, volume serial
   and 128-bit file ID, hard-link count, owner SID, creation UTC FILETIME,
   last-write UTC FILETIME, and byte length. These canonical IDs are obtained
   from metadata-only no-follow and normal-open handles whose identity and
   final path must agree. The bounded snapshot remains in memory and is never
   written to a repository, report, log, or provenance record.

Science may tighten the `4096`-entry or memory bound if synthetic proof shows
that is necessary, but it may not widen the traversal domain beyond exact
direct children or admit a non-ordinary child. If the exact managed parent
cannot satisfy this before-call shape, the route is non-viable and the pass
returns `HOLD / MARTIN DECISION REQUIRED` without consuming ordinal `2`.

## Exact one-call window and after-snapshot admission

For ordinal `2`, or conditional ordinal `3`, the future role must execute this
order exactly:

1. Complete all pre-call authority, candidate/tree, immutable-manifest,
   ordinal, residual, target-absence, parser/static, protected-root, temp-child,
   current-owner-SID, and before-snapshot gates.
2. Capture the lower UTC bound with the Science-frozen precise Windows
   FILETIME primitive only after the before snapshot is complete and
   immediately before invoking exactly one sequential built-in
   `image_gen__imagegen` call using unchanged `HOST06-GEN-PROMPT-v1` and with
   both image-input fields omitted.
3. Capture the upper UTC bound with the same primitive immediately after that
   one call returns, before result-field work, directory enumeration, content
   access, or another operation.
4. Take exactly one matching nonrecursive metadata-only after snapshot of the
   same exact managed directory using the identical schema and bounds.
5. Require every pre-existing direct file to remain present and metadata-
   identical across exact canonical path, type/tag, volume/file ID, hard-link
   count, owner SID, creation FILETIME, last-write FILETIME, and byte length.
   No rename, deletion, replacement, mutation, ownership drift, or identity
   drift is permitted.
6. Require exactly one new direct entry and no other delta. It must be one
   ordinary, non-reparse, one-link file under the exact managed parent, with
   no directory/subdirectory, symlink, junction, mount, device, alternate
   stream, sparse/offline placeholder, unknown type, or path/identity
   ambiguity. Its owner SID must equal the frozen current-owner SID.
7. Require both the new file's raw creation UTC FILETIME and raw last-write
   UTC FILETIME to fall inclusively inside the exact lower/upper call window.
   Science must prove clock source, timestamp resolution, comparison, and
   filesystem semantics without adding a tolerance that could admit another
   producer. If either timestamp cannot be tied unambiguously to the call,
   admission fails opaque.
8. A built-in implementation that creates a new direct subdirectory, writes
   below a child directory, changes any prior entry, creates zero or multiple
   entries, or produces any unproved entry fails opaque. No recursive or one-
   level child discovery is authorized to rescue that shape.

Any call-return error, snapshot error, zero/multiple/ambiguous/wrong-owner/
out-of-window entry, prior-entry delta, nonordinary child, or unresolved
identity consumes the active ordinal and stops opaque. It authorizes no later
ordinal, child traversal, content inspection, `output_hint` parsing, broad
cleanup, inference, or retry.

## Exact-path no-follow copy, identity, optional cross-check, and deletion

Only the one admissible new direct file may proceed:

1. Open only its exact canonical path through a Science-frozen Windows
   no-follow/reparse-safe primitive with read plus exact-file deletion rights
   and no write/delete sharing. Reprove on the opened handle the exact final
   path, parent containment, volume/file ID, owner SID, ordinary type/tag,
   link count `1`, creation/write window, and after-snapshot byte length.
   Any mismatch or replacement is an opaque stop.
2. Stream that one handle exactly once into only the predeclared create-new
   OS-temp `attempt-0N.png`, counting bytes and computing lowercase SHA-256.
   No repository or second destination receives bytes. No conversion,
   normalization, repair, export, derivative, second materialization, or
   full-file memory buffer exists.
3. Close the temp writer, independently stream the exact temp candidate once
   read-only, and require its byte length and lowercase SHA-256 to equal the
   managed-source stream identity. Reprove that the managed handle identity,
   owner, type, link count, timestamps, length, and path did not drift during
   copy/hash. Reads are bounded by the inherited `1..12,000,000` candidate
   byte cap; an over-cap source fails opaque before candidate inspection.
4. A direct primitive `image_url`, if present and lawfully accessible, is
   optional cross-check evidence only. It may be examined solely by the
   Science-frozen canonical PNG-data-URL grammar and fixed-buffer decoded
   byte-count/SHA procedure, with no file write or second materialization.
   Its derived length/SHA must equal the already-proved managed/temp identity.
   Absence or inaccessible optional data-URL cross-check does not defeat an
   otherwise admissible managed source; a present but malformed or mismatched
   cross-check fails opaque. No data URL is candidate transport.
5. `output_hint` and every path-like result field remain unread and opaque.
   They are never parsed, quoted, logged, retained, searched, or used for
   ingress, identity, cleanup, provenance, or evidence.
6. Immediately after managed/temp identity and any bounded optional cross-
   check pass, delete only the same identity-proved managed source by the
   Science-frozen handle-bound primitive. Close it and prove the exact path
   absent with an exact-path no-follow open only, without listing a parent.
7. Take one final matching nonrecursive metadata-only closing snapshot and
   require exact equality with the before snapshot. This proves the managed
   parent has no added, removed, renamed, changed, or subdirectory entry after
   removal of the one admitted source. Discard all snapshots after retaining
   only bounded pass/fail and ordinal/residual scalars.

There is no path-only delete after handle drift, directory delete, parent or
sibling operation, pattern/glob cleanup, recursive cleanup, retry cleanup, or
cleanup of ordinal `1`. Copy, hash, handle, optional-cross-check, delete,
absence, or closing-snapshot uncertainty is terminal opaque HOLD. Cleanup may
remove only an exact identity-proved temp candidate and its exact empty GUID
child; it may never delete an unproved managed entry or broaden discovery.

## Existing candidate gates after managed cleanup

Only a managed/temp byte-identical candidate whose exact managed source has
been deleted and proved absent, and whose closing snapshot equals the before
snapshot, may enter the inherited candidate ladder from `FRWO-005-v3` and the
effective shell:

- exact PNG signature, chunk/order/CRC allowlist, `IHDR 3840 x 2160`, opaque
  8-bit truecolor, required `sRGB`, forbidden metadata/transparency, exact
  inflate structure, `1..12,000,000` bytes, and direct isolated browser decode;
- original-resolution `PHY-01..12`, frozen source bands, centered six-layout
  presentation, physical/activation/label/center/anchor/retention/nonoverlap/
  target mapping, focus stability, effective `200%`, forced color, reduced
  motion, and no sensory-only meaning;
- at most the first passing source, byte-identical create-new product import,
  exact provenance, predeclared Host 06 registry/geometry, seven copy slots,
  one factual alt slot, exact temp cleanup, and bounded product rollback;
- sole unchanged `L02-03`, strict existing evaluator/progression,
  answer-free remediation, transfer, explanation, ownership/confidence,
  sanitizer, private clearing, save projection/restore, no cross-credit, and
  unchanged next boundary; and
- focused, learning/privacy, related, validators `40/40`, cold full, production
  and TD-012 fixture builds, PBA, served/offline/decode/task/CLS, exactly one
  later complete E2E without retry, owned cleanup, candidate freeze, and Git
  synchronization.

A fully identified source that fails a later PNG, decode, `PHY`, composition,
mapping, accessibility, or meaning predicate is an objective source rejection.
Only exact managed and temp cleanup may then permit conditional ordinal `3`.
All other post-call failures are opaque and forbid another ordinal.

The two and only two media-owned workspace paths remain:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md
```

No preview, `generatedImage`, Markdown image, screenshot, contact sheet, cycle
reveal, publication, or sharing exists at any point.

## Preserved roles, files, budgets, and invariants

The frozen prompt remains exactly `HOST06-GEN-PROMPT-v1` from the effective
shell. No prompt, reference, edit, variation, or image input changes. The
immutable accepted-media baseline remains exact and no accepted-media path or
pixel is a generation input or mutation target.

The product/test/media change set, seven copy meanings, one alt meaning,
inert source guard, generic-launcher removal rule, focus/recovery model,
save/privacy/offline rules, numeric JavaScript/CSS/module/media/source-map/
decode/task/CLS/test/build/E2E caps, exact permitted files, and rollback
boundary remain incorporated from `FRWO-005-v3` and the effective shell.
This v4 planning stage changes none of those files or values.

The role order resumes only after a new shell: Science, Mission, then the
already-complete Recon/Tactical/Combat evidence may be reused only if Mission
confirms it remains exact; Quartermaster begins at ordinal `2`; Image remains
code/configuration-only around immutable accepted bytes; Intelligence alone
may release or advance maturity. This Work Order does not directly authorize
Quartermaster or any production role.

## Required Science fixture adjudication

One fresh Office of Science Administrator must independently prove this
adapter with synthetic non-generative fixtures only. It must not enumerate or
touch the real managed parent and must cover at least:

- trusted scalar parent resolution, exact final-path/identity containment,
  protected-root separation, ordinary-directory and reparse rejection;
- empty, one-file, maximum-bound, over-bound, and pre-existing subdirectory /
  nonordinary before states;
- zero, exactly one, and multiple new entries; new subdirectory; rename,
  deletion, replacement, and metadata mutation of a prior entry;
- reparse, symlink, junction, mount, hard-link, device, alternate-stream,
  sparse/offline placeholder, wrong owner, path escape, and inaccessible
  metadata;
- raw creation and last-write FILETIME at, inside, and outside both call-window
  bounds, clock-resolution behavior, and ambiguity rejection;
- source replacement between after snapshot, handle open, copy, hash, delete,
  exact absence proof, and closing snapshot;
- no-follow open, canonical volume/128-bit file-ID equality, bounded streaming
  copy/hash, managed/temp mismatch, optional data-URL absent/match/malformed/
  mismatch, and proof that no data URL is written;
- handle-bound exact deletion, deletion failure, path replacement after
  deletion, exact absence, before-state restoration, exact temp cleanup, and
  every opaque-stop branch; and
- ordinal/residual accounting, offline/privacy/performance bounds, no result-
  hint use, no broad discovery/cleanup, no CLI/API fallback, and no reveal.

Science must freeze the exact Windows APIs/flags, handle-sharing and deletion
semantics, owner-SID primitive, canonical path/volume/file-ID representation,
FILETIME clock source and comparison, snapshot schema/memory bound, race /
TOCTOU controls, optional-cross-check implementation, copy/hash limits,
absence proof, closing-snapshot comparison, and temp cleanup. If any required
creator/owner, creation/write window, ordinary-file identity, race isolation,
handle-bound deletion, or exact restoration proof cannot be established
without broader discovery or result-field path parsing, Science must issue
`HOLD / MARTIN DECISION REQUIRED`. It may not silently select CLI/API.

## Rejection, rollback, protected state, and process records

Any need for a fourth call, nonsequential ordinal, more than one output, new
direct subdirectory, recursive discovery, `output_hint`, data-URL transport,
CLI/API fallback, broad cleanup, unproved delete, alternate generator,
accepted-media input/edit, derivative, second canonical source, lesson /
evaluator/save/dependency/network/route/world/ending change, protected or
residual access, hidden lore, real browser/profile/save, reveal, E2E retry, or
unsupported authority is `REVISE` or `HOLD` to the earliest owner.

Rollback remains bounded to the exact new selected raster, `PROVENANCE.md`,
bounded Host 06 product/config/test/E2E delta, release-command delta, and
cycle reports after exact identity proof. It never touches ordinal `1`, an
unproved future managed entry, accepted media, Hosts 04/05, curriculum, save
schema, dependencies, Host 07+, later rail, user/untracked work, browser /
profile/save, repository QA quarantine, protected PDF, training directory,
VR-65, or a parent root.

All thirteen inherited process records remain separate and **OPEN**: VR-17,
VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage
4, VR-60 Call 9, and VR-61. The separate Commandant filename/search-scope
record remains **OPEN**. This stage closes, cures, merges, waives, or renumbers
none.

Repository QA quarantine, `Art Of No Mans Sky Book Scan.pdf`, `Simplilearn
Training Files/`, real browser/profile/save, hidden lore, user work,
accepted-media pixels, managed/temp parents, opaque residuals, and VR-65
remain protected. The prospective exact managed-parent exception is inactive
until Science and Mission accept it.

## Validation performed and exact Science handoff

Operations verified exact starting `HEAD`
`8a36ef773eb2d949b4f134b14d6d34670b74b5c6`; read the active workflow,
registry, full Operations profile, current handoff, complete
`FRSH-005-v1-VR-07`, `FRCA-005-v2`, `FRWO-005-v3`, `FRVE-005-v3`, effective
shell through `FRSH-005-v1-VR-06`, current release map/scoreboard, and the
installed image-generation skill including built-in save-path and no-silent-
CLI-fallback rules. Operations adjudicated only the returned staging question.

No generator call, real managed-parent resolution/list/snapshot, result-field
inspection, data-URL transport, temp allocation, media read/copy/hash/delete,
pixel inspection, import, copy/provenance, product/test change, build, browser,
preview, E2E, reveal, maturity update, OPEN-record closure, VR-65 access,
schedule, automation, release, or push occurred.

One fresh Office of Science Administrator / `office_of_science_administrator`
reads this complete Work Order and the synchronized authorities, then issues
exactly one new versioned `POLISH VIABILITY READY`, `REVISE`, or `HOLD`
artifact. Science must answer every managed-parent, bounded-snapshot,
canonical-identity, owner, creation/write-window, race, no-follow, exact-copy /
hash, optional-data-URL-cross-check, handle-bound-delete, absence, restoration,
temp-cleanup, residual-accounting, privacy, performance, offline, no-reveal,
and rollback question above with synthetic non-generative fixtures.

Science may not invoke generation; resolve, enumerate, snapshot, or mutate the
real managed parent; parse `output_hint`; use a data URL as transport; inspect
pixels; import media; write copy/provenance; authorize Quartermaster; run E2E;
reveal; advance maturity; close an OPEN record; inspect VR-65; select CLI/API;
schedule; automate; push; release; or call `FIRST RUN COMPLETE`.
