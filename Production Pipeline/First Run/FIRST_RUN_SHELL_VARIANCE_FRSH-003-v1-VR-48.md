# First Run Shell Variance Reissue - Atomic Fresh-Root Control Localization Only

Variance ID: `FRSH-003-v1-VR-48`

Disposition: **`FIRST RUN SHELL READY / ATOMIC FRESH-ROOT CONTROL LOCALIZATION
ONLY / FRSH-003-v1-VR-48`**

Stage gate remains: **`HOLD / PRODUCTION FUNCTIONAL NOT ISSUED`**

Release state remains: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Stage / stable agent: Mission Captain / `mission_captain`

Mission source inspected:
`3329132b7b989db0ba2fe6d76dcfdf4f30acddca`

Geometry correction / diagnostic transport / report lineage:

```text
8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97
6c64eb354b7dbb467df5725e2cae4eb67092ddc7
0ac9023037873004f7bd5d75c16f80953d770a4d
```

Exact immutable product / validation / diagnostic / evidence identities:

```text
a91763e28d488f31f8cf7d40ece0b2682246ba9b
4cd7fbf31291671dd28c0743b44a7c49aaad82bb
2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
ca89a679195c11d441a76e6c02983a6436f2ccb2
```

Recorded: **2026-08-10**

## Context reuse and VR-47 adjudication

Fresh-child capacity remains unavailable. Root expressly authorized this
Mission Captain context to adjudicate VR-47 and issue the next bounded shell.
The reuse is disclosed, is not candidate evidence, and waives no boundary.

The initial monolithic wrapper was rejected by tool policy before execution.
Mission classifies it **`EXECUTION-CONTROL REJECTION / NON-EXECUTED /
NON-EVIDENCE / VR-47`**. It created no root or process and consumed no E2E,
extractor, or verifier invocation.

The smaller first stage then emitted no required root scalar. No root identity
was retained, no preview/browser/E2E ran, and the no-process cleanup branch
proved both ports clear. Mission accepts the return classification:

**`REQUIRED CORRECTION / EXECUTION CONTROL / PRE-ROOT SCALAR TRANSPORT / OPEN
/ VR-47`**.

This is not a geometry-correction, product, preview, browser, diagnostic, or
verifier finding. Candidate `8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97`
and its accepted Node/focused/integrity proof remain unchanged. Although the
one E2E was not invoked, VR-47 authority may not be reused or inferred.

Mission does not authorize a complete run until the root protocol itself has
produced one exact scalar in a small tool-compatible call.

## Exact atomic root protocol

Exact next owner is a **fresh Combat Engineer** if capacity permits; otherwise
context reuse must be disclosed. Execute exactly one PowerShell call containing
only root construction, proof, empty-root cleanup, and one scalar output:

1. Resolve OS temp and repository root with `[System.IO.Path]::GetFullPath`.
   Normalize both only by trimming trailing directory separators; preserve
   drive/root meaning.
2. Generate one leaf exactly `horizon-archive-frrc002-` plus a lowercase
   `Guid.ToString('D')`. Combine it with normalized OS temp and normalize the
   candidate with `GetFullPath`.
3. Before creation require: anchored leaf shape; candidate direct parent equals
   OS temp ordinal-ignore-case; candidate is a strict temp descendant;
   repository and candidate exclude each other in both directions; candidate
   differs from the disclosed predecessor root; and candidate does not exist.
4. Create exactly once with:

```powershell
New-Item -ItemType Directory -Path $candidate -ErrorAction Stop
```

   Assign and suppress the returned object. Do not use `-LiteralPath` with
   `New-Item`, an alternate creator, shell chaining, or a second attempt.
5. In the same call, resolve the created root with
   `Resolve-Path -LiteralPath -ErrorAction Stop`. Normalize once and require
   exact candidate equality, exact direct-parent equality, strict temp
   descendant, bidirectional repository exclusion, predecessor distinction,
   anchored leaf, and directory existence.
6. Freeze the resolved identity internally. Delete only that empty owned root
   in the same call with `[System.IO.Directory]::Delete($resolvedRoot,$false)`
   after all post-create checks pass. Require exact absence afterward. If a
   failure occurs after lawful creation, attempt that same empty-root deletion
   once in `finally`; never target an unresolved or alternate path.
7. Suppress all command/object/error/path output and emit exactly one ordered
   scalar line after cleanup:

```text
tempResolved=<0|1> repositoryResolved=<0|1> nameShapePass=<0|1> parentPass=<0|1> tempDescendantPass=<0|1> repositoryExcludedPass=<0|1> predecessorDistinctPass=<0|1> preExists=<0|1> createAttemptCount=<0|1> createExit=<0|1> postExists=<0|1> postResolvedMatch=<0|1> postParentPass=<0|1> rootIdentityRetained=<0|1> cleanupAttemptCount=<0|1> rootDeleted=<0|1> nativeExit=<0|1>
```

Exact PASS requires resolution/name/parent/descendant/exclusion/distinctness
all `1`; `preExists=0`; create attempt `1`, exit `0`; post exists/resolved/
parent/identity all `1`; cleanup attempt/deleted `1/1`; and native exit `0`.
Any other result is fail-closed. No path, GUID, exception, directory object, or
diagnostic may be emitted.

On exact PASS return **`ATOMIC FRESH-ROOT CONTROL PASS / ROOT DELETED / NO
LIVE ACTION / STOP / RETURN TO FRESH MISSION`**. Otherwise return **`HOLD /
ATOMIC FRESH-ROOT CONTROL FAILURE / NO LIVE ACTION / NO RERUN / RETURN TO
FRESH MISSION`**.

No second root attempt, port query, preview, browser, E2E, extractor, summary,
verifier, build, test, validator, PBA, served identity, product/media/protected
action, repository write, or post-scalar command is authorized. Mission alone
may later reissue one complete-run authority using a newly created root.

## Preserved boundaries

The cumulative VR-22 tests/validators, VR-30 production build, VR-35 fixture
build/corrected PBA, VR-42 formal served identity, and VR-46 correction proof
remain accepted. No product change, `PRODUCTION FUNCTIONAL`, release, or
maturity advance is authorized.

The eight prior OPEN divergences remain separate and OPEN: VR-17, VR-23,
VR-24, VR-25, VR-28, VR-39, VR-41, and VR-46. The VR-47 required execution-
control correction is a ninth separate OPEN classification. None is waived,
merged, closed, cured, or used as candidate evidence.

Diagnostic evidence remains non-release, forbidden verifier input, and no-
retry. Every frozen product, player, learning, privacy, save, accessibility,
route, world, MH-40, null-delta, `successor=null`, ending, media, and one-E2E
meaning remains exact. No downstream stage or release action is authorized.

Mission Captain signs **`FIRST RUN SHELL READY / ATOMIC FRESH-ROOT CONTROL
LOCALIZATION ONLY / FRSH-003-v1-VR-48`**.
