# First Run Polish Viability Envelope - Funded B1

Envelope ID: `FRVE-005-v9`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v9 / Stranded Lens Cradle - One Funded
Official-Envelope Image API Attempt`

Disposition: **`POLISH VIABILITY READY / ONE FUNDED B1 IS TECHNICALLY
VIABLE / NEW RETAINED V9 CARRIER AND COMPLETE MISSION SHELL REQUIRED / ZERO
CREDENTIAL READS, API SENDS, OR MEDIA ACTIVITY`**

Date: **2026-08-12**

Science source inspected:
`c0b9ca97e025ba6eb5e840728dab6dae9be3488f`

Planning controls: `FRRM-005-v9` / `FRSB-005-v9`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Mission Captain / `mission_captain`

## Science decision

Science accepts `FRCA-005-v8` exactly: v8 A1 is consumed, HTTP 429 is not
interpreted, v8 is closed, and no v8 executable or path may be reused. Martin's
explicit statement that funds were added and direction to try again supports
only the separately versioned v9 attempt `{B1}`. There is no B2 or retry.

Current official OpenAI documentation continues to support the frozen request:

- `gpt-image-2` supports `v1/images/generations` and paid Tier 1;
- the Image API supports one prompt-to-image generation;
- `3840x2160`, `quality=high`, opaque background, and PNG are supported; and
- GPT Image returns base64 image data in the official ImagesResponse envelope.

Authorities:

- https://developers.openai.com/api/docs/models/gpt-image-2
- https://developers.openai.com/api/docs/guides/image-generation
- https://developers.openai.com/api/docs/pricing

The v8 synthetic parser suite already proved 38 positive/adversarial cases,
and the retained v8 execution proved the request reached the official endpoint
with a completed JSON response. V9 changes no endpoint, model, prompt, request
options, parser, technical review, physical rubric, product path, runtime
behavior, or validation budget. Its only execution-domain changes are Martin's
funded authorization, `{B1}`, and fresh literal helper/live paths. Repeating
the same synthetic suite would add no new risk evidence; Mission must freeze
the new carrier and parser-check every retained source instead.

Science independently proved the exact nine v9 controlled paths absent:

```text
v9PathCount=9
presentCount=0
allAbsent=1
credentialReads=0
apiSends=0
```

No credential value or presence was observed. No request was constructed, no
network/API operation occurred, and no media, temp root, product, pixel,
browser, test, build, or runtime state was accessed or changed.

## Exact request, response, and secret envelope

Mission must retain a new v9 production carrier and parent. The carrier must
freeze exactly one non-streaming `POST` to
`https://api.openai.com/v1/images/generations` with exact
`HOST06-GEN-PROMPT-v1` (`4,099` ASCII/UTF-8 bytes, SHA-256
`efd8c7f8b5ca6c0ec9e16ac82cdb008921a480e549b3b0b1b4b767f79e323179`),
`model=gpt-image-2`, `n=1`, `size=3840x2160`, `quality=high`,
`background=opaque`, and `output_format=png`.

`OPENAI_API_KEY` is inherited from process environment only. Before B1, code
may observe only boolean nonblank presence; the value is used in memory only
as a Bearer credential and may never be printed, persisted, measured, hashed,
classified, returned, or included in diagnostics. Redirects remain disabled,
`ResponseHeadersRead` remains exact, timeout remains ten minutes, and one
`SendAsync` is the sole B1 boundary.

The strict response contract from `FRVE-005-v8` is adopted in full:

- strict UTF-8, no BOM/trailing/malformed input, duplicate detection at every
  admitted object layer;
- exact top-level allowlist `created background data output_format quality
  size usage`;
- required nonnegative integral `created` and exactly one `data` item with
  only canonical `b64_json`;
- optional exact request echoes and bounded nonnegative integral usage shapes;
- complete response `1..16,500,000` bytes, base64 `4..16,000,000` characters,
  decoded payload `1..12,000,000` bytes; and
- rejection of URL, revised prompt, unknown/duplicate/extra/nested/coerced/
  negative/fractional/overflow fields.

Every completed response durably retains only numeric status, normalized media
type or `absent`, bounded byte count or `cap-exceeded`, stable terminal stage,
send-started boolean, and the secret-safe at-most-1,024-byte non-success error
projection or `diagnostic-unavailable`. All body/JSON/base64/payload and
diagnostic working references are cleared. Headers, key, request body,
response body, generated bytes, exception/stack, request ID, and account or
project identity never enter a report or provenance.

## File, cleanup, and rollback envelope

Mission must freeze the exact v9 literals from `FRWO-005-v9` and prove all nine
absent immediately before Quartermaster. The v8 same-run helper pattern is
required without a prior-instance DLL hash: exact helper source, current-run
DLL length/SHA freeze, byte-only `Assembly.Load(byte[])`, exact reflection and
PInvoke/native surface, exclusive full-byte reobservation, one-link,
non-reparse, exact-size identity, exact DLL deletion, and nonrecursive deletion
of only the empty owned helper root before credential access.

Decoded payload materialization requires `FileMode.CreateNew`, one bounded
write, `Flush(true)`, close, exclusive reopen, handle-tuple equality,
one-link/non-reparse/size/SHA proof, same-directory atomic no-replace move,
stage absence, and final-target identity. Failure cleanup is limited to exact
identity-proved B1 files and the exact empty v9 root. No parent/sibling/glob/
recursive cleanup, alternate path, repair, or v7/v8 access is allowed.

REJECT leaves zero authority and no retained media. ACCEPT may create only the
single exact product raster and `PROVENANCE.md` with create-new semantics and
byte identity. A later validation failure rolls back only those exact newly
owned files and the exact newly empty product root, nonrecursively and after
identity proof. The 17 accepted-media baseline remains immutable.

## Technical, physical, accessibility, and state gates

Before any private visual judgment, the exact target must pass PNG signature,
bounded chunks, CRC/order, one IHDR, `3840x2160`, 8-bit truecolor type 2,
opaque sRGB/no alpha, permitted metadata only, bounded complete inflate,
independent decode, one IEND, and no trailing bytes, text, watermark, EXIF,
time, animation, private chunk, or unknown chunk.

Private Quartermaster review then requires all frozen `PHY-01..12`, including
one complete weathered lens nested at least `.80` inside one tilted conformal
cradle, tilt `12..35` degrees, at least two load/stress contacts, at least two
drainage seams, dry clearance and a continuous dry approach, at least three
Host 05-side fragments outside activation, a restrained horizon/reflection
crossing at least `.25` of lens width, same-basin continuity, and one candidate
distinct from all protected features. No human/text/answer/world-response or
authority meaning may appear.

The source remains one centered `cover` image across desktop, laptop, narrow,
effective-200%, retained-320x180, and retained-320x240 layouts. Relation and
approach centers and at least `.995` area must remain. Semantic target is at
least `44x44 CSS px`; physical center is contained with exact area/clearance,
label inset/containment, protected nonintersection and separation, stable
focus, `3px solid Highlight` forced-color focus, zero horizontal escape, and
reduced-motion parity.

State remains null-first and fail-closed until accepted import. Hidden means
no source/node/name/focus/notice/action/credit. Available/in-progress/
remediation/complete derive only from lawful source plus sanitized existing
`modelChoiceEvidence`. LOOK/TALK write nothing; sole USE opens unchanged
`L02-03`; completed USE is read-only. Save schema, evidence ownership,
no-cross-credit, Demo Tour, privacy clearing, recovery, offline/no-authority,
Host 04/05, return, later rail, MH-40 outcomes, RP-012, and `successor=null`
remain unchanged.

## Performance and validation ladder

The inherited caps remain: production JavaScript `<=1,679,393` affected and
`<=1,703,258` global, CSS `<=119,547` affected and `<=119,672` global,
modules `<=218` affected and `<=222` global, media `<=18` files and
`<=49,410,731` bytes, source maps `0`, cold/warm decode `<=250/100 ms`, image
ready `<=750 ms`, attributable/total CLS `<=.01/.05`, activation `<=2 ms`,
max task `<=100 ms`, focused/related/full gates `<=30/60/60 s`, builds
`<=60 s`, and one E2E `<=180 s`.

After accepted import only, run contract and identity gates; focused Host 06
and adjacent Host 05/model-choice/accessibility tests; learning/privacy and
related route regressions; validators and full suite; clean production and
fixture builds; PBA and served identity; one isolated no-retry E2E with one
summary and one independent verifier; six-layout/live review; browser/process/
port cleanup; exact rollback readiness; and Git identity/synchronization.

## Risks, hard stops, and Mission handoff

HTTP non-200, unexpected content type, response cap, strict parser failure,
unsafe diagnostic, Base64/PNG/file/identity/review/import/validation/cleanup
failure, or any missing scalar is terminal v9 HOLD. Objective image rejection
also closes v9. There is no B2.

Repository QA quarantine, protected PDF, training tree, Martin's real browser/
profile/save, accepted-media pixels, v7/v8 paths/residuals, opaque roots,
VR-65, hidden lore, and unrelated work remain inaccessible. All inherited OPEN
records remain separate and OPEN. No reveal is authorized.

Fresh Mission must build and retain a new v9 builder/carrier/launcher/stdin
parent/credential-cleared fixture controller, freeze strict byte/SHA and
parser-zero identities, run exactly one zero-credential/no-request fixture,
prove all nine paths absent afterward, and issue a complete B1-only
`FIRST RUN SHELL READY`, `REVISE`, or `HOLD`. Mission may not itself access the
credential, send the request, inspect media, or consume B1.

Office of Science Administrator signs **`POLISH VIABILITY READY /
FRVE-005-v9 / ONE FUNDED B1 / NEW RETAINED CARRIER REQUIRED / ZERO CREDENTIAL,
API, OR MEDIA ACTIVITY / FRESH MISSION NEXT`**.
