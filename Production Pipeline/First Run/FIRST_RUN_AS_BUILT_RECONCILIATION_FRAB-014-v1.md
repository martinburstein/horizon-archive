# First Run As-Built Reconciliation — FRAB-014-v1

Disposition: **RELEASED / 27 OF 27 RUNTIME IMAGE MASTERS / STOP_SUCCESS**

Date: **2026-08-14**

Exact released product candidate: `7b781abd`

Work Order: `FRSH-016-v1 / HA-RI-001`

## Released scope

The exact private production masters for Hosts 14 through 40 are integrated in
route order. Each host uses its own fail-closed source law, visible geometry,
semantic target, responsive presentation, focus and recovery behavior, and
provenance receipt. No route, lesson ownership, save, privacy, world-response,
reward, ending, or successor contract changes.

Final queue state is `0 queued / 0 integration_candidate / 27
integrated_released`. Every card and integration receipt agrees with that state.

## Exact aggregate proof

- Per-asset focused source, mapping, interaction, responsive, accessibility,
  and build evidence passes as recorded in the 27 integration receipts.
- The final exact candidate passes the serialized full product test suite.
- Curriculum and source validators pass `40 / 40`.
- Production build passes at `275` modules, JavaScript `1,827,959` bytes, CSS
  `122,252` bytes, and zero source maps.
- All 27 queue card, receipt, and master hashes reconcile. Every master is an
  exact `1920x1080` RGB PNG.
- The emitted binary set is exact at `52 / 52`: the 24-file accepted baseline,
  the released predecessor Host 06 source, and the 27 queue masters.
- Both serialized served-identity controls, `TD010` and `TD011`, pass against
  the exact rebuilt candidate.
- Complete clean-start E2E passes both READY and NOT YET READY equal-dignity
  outcomes in `78.555` seconds with zero runtime errors and no successor.

The final Host 17 presentation-only geometry correction did not invalidate the
curriculum-validator inputs, so the existing final aggregate `40 / 40` result
was reused. The full suite, build, served identity, and complete E2E were rerun
against the corrected exact candidate.

## Reconciled variances

- Stale fixture and source assertions were updated to the released 54-asset
  runtime shape.
- Shared served-build checks were serialized after proving their parallel
  failures were deterministic `dist` contention, not product failures.
- Complete E2E found a real Host 17 pointer-target overlap. The owned target was
  bounded away from its neighbor, a focused non-overlap regression was added,
  and the complete aggregate cascade passed afterward.

## Boundaries held

No image-generation or image-edit call occurred during integration. No
candidate or selected image was revealed in chat. Unrelated QA screenshots,
browser/profile/save data, hidden lore, PDFs, training material, and dirty user
files were neither inspected nor staged. Runtime integration made no new lore,
branch, lesson, reward, access, identity, authority, response, or post-ending
content.

## Final decision

`STOP_SUCCESS`. `FRAB-014-v1@7b781abd` is the committed runtime-image release.
There is no next image-queue or runtime-integration action.
