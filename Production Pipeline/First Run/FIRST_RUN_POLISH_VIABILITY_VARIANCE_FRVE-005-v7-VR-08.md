# Horizon Archive First Run Polish Viability Variance

Variance ID: `FRVE-005-v7-VR-08`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`POLISH VIABILITY READY / COMPACT CURRENT-SHELL PRE-HELPER
FIXTURE PASS / COMPLETE BOUNDED FAILURE RETENTION AND CLEANUP PROVED /
FRVE-005-v7-VR-08`**

Date: **2026-08-11**

Science source inspected:
`56bd994f67c8be35cb5d93cb343c128fec2e43ae`

Mission correction: `FRSH-005-v1-VR-09`

Prior Science returns / decisive viability: `FRVE-005-v7-VR-07` /
`FRVE-005-v7-VR-06` / `FRVE-005-v7-VR-05` /
`FRVE-005-v7-VR-03`

Quartermaster return: `FRCA-005-v3`

Effective shell: `FRSH-005-v1` through `FRSH-005-v1-VR-09`

Effective treatment / blueprint: `FRDT-005-v1-VR-01` /
`FRPX-005-v1-VR-01`

Functional report: `FRCE-005-v1-VR-05`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Mission Captain / `mission_captain`

## Science decision

Science read the complete active intake, workflow, registry, full Science
profile, `FRVE-005-v7-VR-07`, `FRVE-005-v7-VR-06`,
`FRVE-005-v7-VR-05`, `FRSH-005-v1-VR-09`,
`FRVE-005-v7-VR-04`, `FRCA-005-v3`, complete `FRWO-005-v7`, decisive
`FRVE-005-v7-VR-03`, the effective shell, treatment, blueprint,
`FRCE-005-v1-VR-05`, immutable `FRAM-001-v1`, and the exact pre-helper
execution boundary. Starting `HEAD` was exact
`56bd994f67c8be35cb5d93cb343c128fec2e43ae`. The three disclosed untracked
protected items remained untouched.

Science ran exactly one credential-cleared, no-request fixture through the
authorized compact current-shell parent. The parent used no encoded parent,
child source file, stdin, diagnostic file, alternate environment transport,
nested parent shell, or file redirection. Before fixture entry, every declared
parent-local name was checked case-insensitively against PowerShell automatic,
preference, constant, read-only, and common system names. The pre-helper
identity object was exact `$prehelperIdentity`; no `$pid` or case variant of
`$PID` was declared or assigned.

One outer JavaScript command-template submission was rejected before
`shell_command` because literal Markdown fence backticks terminated the outer
template. No command, parent, credential operation, environment operation,
path operation, or child existed. Science corrected only that orchestration
representation before beginning the sole authorized parent. This was not a
fixture execution or retry.

The parent independently extracted the frozen launcher and pre-helper in
memory, normalized only repository CRLF framing to the shell-frozen LF byte
domain, and proved:

```text
launcher byteLength=2001
launcher sha256=96feaf7e62fa89e8c80cc46d38425d465cf845ffbd426405a75c73c056314212
prehelper byteLength=976
prehelper sha256=5cd257c94bcd70b8d6ada4e0b561b2a14ed52fd9459146b1269dc93ce1bdc7d1
```

Both strings were UTF-8 without BOM, LF-only, and included the final LF. They
contained no `New-Item`; the exact root primitive remained
`[IO.Directory]::CreateDirectory($helperRoot)`.

Before the first child, the parent conservatively rendered the exact
`C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe` executable plus
the exact argument array
`-NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command
<launcher>`. The rendering was exact `2,156` characters, below both `8,191`
and `32,767`.

The parent removed `OPENAI_API_KEY` from its process environment without
reading it. Around only each exact native child invocation, it saved the prior
`$ErrorActionPreference`, set it to `Continue`, captured the native stderr
record and `$LASTEXITCODE` in memory, and immediately restored the prior
preference in `finally`. It did not suppress, rewrite, discard, or file-
redirect child stderr.

The exact success child exited `0` with no diagnostic output. It traversed
ordered `PH01..PH08`, proved exact Windows PowerShell Desktop
`5.1.26100.8875` and 64-bit process, created only the exact ordinary
non-reparse helper root, left the DLL and live root absent, and completed at
`PH08_ROOT_CREATE_COMPLETE`. The parent proved the root exact and empty,
deleted only that exact root nonrecursively with `Directory.Delete(path,
false)`, and proved it absent.

Science therefore resolves the open Science parent namespace, transport, and
capture corrections and issues **`POLISH VIABILITY READY`**. The frozen
Mission child contract is now positively proved in its complete required
fixture envelope. Mission may make one explicit Quartermaster retry decision;
Science does not route directly to Quartermaster or authorize production.

Variance classification: **`REQUIRED CORRECTION RESOLVED / SCIENCE COMPACT
PARENT VARIABLE NAMESPACE, COMMAND-LINE MEASUREMENT, NATIVE STDERR CAPTURE,
BOUNDED RETENTION, AND CLEANUP`**.

## Complete failure-retention fixture evidence

Within the same one fixture, exact frozen child executable and arguments were
used for every child. Science induced and accepted exactly one terminal
exit-`86`, single-record first failure for each stable predicate, in order:

```text
PH01_ENV_RETRIEVAL
PH02_PARSE_SUCCESS
PH03_INVOCATION_ENTRY
PH04_PS51_VERSION
PH05_X64_PROCESS
PH06_ROOT_ABSENT
PH07_ROOT_CREATE_ENTRY
PH08_ROOT_CREATE_COMPLETE
```

Every accepted record preserved the exact field order, ASCII/single-line
shape, allowlisted predicate, bounded class/FQID, and exact lowercase helper-
root, helper-DLL, and live-root absence booleans. Non-allowlisted synthetic
class and FQID values were replaced with literal `UNLISTED`; they were never
copied through.

The normalizer accepted one exact `640`-byte ordered ASCII record and replaced
its non-allowlisted class with `UNLISTED`. Each of these inputs collapsed to
exact `STABLE_LOCAL_FAILURE / diagnosticRecordRejected=true` plus freshly
observed exact absence booleans:

- zero records;
- multiple records;
- missing field;
- extra field;
- reordered fields;
- unknown predicate;
- multiline record;
- non-ASCII record; and
- exact `641`-byte oversized record.

The controlled PH08 negative branch created the root only with the exact
frozen primitive, set `RootCreated=true` and `RootOrdinary=true` only after
the ordinary/non-reparse checks passed, then failed at PH08. The frozen
launcher deleted only the exact empty ordinary root, nonrecursively and only
while the exact DLL was absent. The resulting record and independent parent
checks proved helper root, DLL, and live root absent. Cleanup uncertainty is
therefore absent.

## Final absence, zero activity, and protected state

The fixture's final checks and an independent post-fixture exact-path
corroboration proved:

```text
helperRootAbsent=true
helperDllAbsent=true
liveRootAbsent=true
ordinal2StageAbsent=true
ordinal2TargetAbsent=true
ordinal3StageAbsent=true
ordinal3TargetAbsent=true
productRasterAbsent=true
productProvenanceAbsent=true
```

Credential reads, request constructions, `SendAsync` calls, direct Image API
sends, and ordinals consumed were each exact `0`. Historical ordinal `1`
remains permanently consumed, opaque, inaccessible, and unchanged. Ordinals
`2` and `3` remain unstarted and unconsumed.

No helper was compiled or loaded. No DLL, child source file, live root,
request, response, raster, provenance, accepted-media stream, media byte, or
pixel was created or opened. Repository QA quarantine, the protected PDF,
training tree, Martin's browser/profile/save, hidden lore, accepted-media
pixels, OS-temp parent, ordinal-1 residual, managed directory, user work,
VR-65, and every opaque residual remained untouched. All thirteen inherited
process records and the separate Commandant filename/search-scope record
remain separate and OPEN.

## Preserved envelope, maturity, and exact Mission handoff

All helper/API, parser-after-response, ordinal, media, physical, crop/mapping,
accessibility, canon, lesson, privacy, save, offline, performance, PBA,
no-reveal, rollback, protected-state, and OPEN-record clauses in the effective
shell remain exact. The sole learning owner remains unchanged `L02-03`; no
evidence, authority, world response, reward, access, branch, or ending state
was created.

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`.

Science changed no product, test, runtime, lesson, save, media, manifest,
candidate, map, scoreboard, maturity, process classification, residual,
schedule, or automation state. The only repository changes authorized for
this stage are this variance and synchronized `NEXT_INSTANCE_HANDOFF.md`.

One fresh Mission Captain / `mission_captain` reads the complete current
intake, full Mission profile, this variance, `FRVE-005-v7-VR-07` through
`FRVE-005-v7-VR-05`, complete `FRSH-005-v1-VR-09`,
`FRCA-005-v3`, complete `FRWO-005-v7`, decisive
`FRVE-005-v7-VR-03`, complete effective shell, treatment, blueprint,
`FRCE-005-v1-VR-05`, immutable `FRAM-001-v1`, and the exact pre-helper
fixture evidence. Mission issues exactly one versioned `FIRST RUN SHELL
READY`, `REVISE`, or `HOLD` result making the explicit Quartermaster retry
decision.

Mission may not itself execute the helper, allocate the live root, read a
credential, call the API, consume an ordinal, inspect media/pixels, change
product/tests, run E2E, reveal, advance maturity, close an OPEN record, access
a residual or VR-65, schedule, automate, push, release, or call `FIRST RUN
COMPLETE`.

Office of Science Administrator signs **`POLISH VIABILITY READY /
FRVE-005-v7-VR-08`** from exact source
`56bd994f67c8be35cb5d93cb343c128fec2e43ae`.
