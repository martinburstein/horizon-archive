# FIRST RUN FUNCTIONAL REPORT / FRCE-013-v1

## Disposition

`PRODUCTION FUNCTIONAL / INERT HOST 13 SLOT / QUARTERMASTER READY`

Shell `FRSH-013-v1`; blueprint `FRPX-013-v1`; source release `FRAB-012-v1`.
Code candidate is `fb547bb64ee6b1f93b1399fe21ea4636e1aa95db`.

Combat added `tidalPatternLoom.js`: exact null-first registry, eight prompt
identity guards, 30 MB cap, provenance/measurement/decode lawfulness, complete
span/collar/contact geometry, production-derived six-layout projection, state
derivation, hotspot projection, and generic Text/Speech plus Visual Pattern
retirement guard. `App.jsx` gates only those two legacy launchers on
`source.enabled!==true`; predecessor and Objective Ledger remain unchanged.
Because selected source is false/null, released behavior is unchanged. No
raster import, geometry, copy, alt, provenance, decode, or image element exists.

Focused predecessor/Host13/pattern/projection suite passes `31/31`. Full suite
passes `1034/1034/0`. All curriculum/readiness validators pass `40/40`.
Production and TD-012 fixture builds pass `232 / 57` modules. PBA passes JS
`1,742,011`, CSS `120,695`, accepted media `23 / 140,647,485`, maps0. The
production build includes no new raster.

Two validation-harness corrections were required and changed no product: the
first validator lookup used the game directory rather than repository root;
the second naïve invocation did not supply each validator's `--self-test`
mode. The authoritative corrected run passed all 40. No test was weakened.

No generation, API call, selected-media inspection, candidate raster, product
source, provenance, live preview, E2E, real profile/save, or protected QA
action occurred. The rolling pool remains eight; `{H13-1..H13-8}` is unstarted.
Rollback is the new module/test, one style declaration, and two launcher guards.

Quartermaster placeholder ledger: source/provenance; relation, dry approach,
span array, paired-collar array, load-contact array, residue, waterline,
semantic target, label, protected regions, six layouts; nine final-purpose
state slots plus name and alt. Quartermaster populates only after first total
PASS, keeps rejected outputs private, stops at first PASS, and carries unused
calls only after Intelligence release.

Exact next owner: one Quartermaster.
