# First Run Shell - Distant Small Host 06 G1

Shell ID: `FRSH-005-v1-VR-45`

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-005-v17` / `FRVE-005-v13`

Predecessor result: `FRCA-005-v13 / F1 OBJECTIVE LAYOUT REJECTION`

Disposition: **`FIRST RUN SHELL READY / SOLE G1 / DISTANT SMALL CENTER
ISLAND PROMPT V4 / EXACT ATTEMPT-ID + 30 MB + POSITIVE GEOMETRY EVIDENCE /
ONE SEND / NO RETRY OR G2 / PRIVATE NO-REVEAL REVIEW`**

Date: **2026-08-12**

Exact next owner: one fresh Quartermaster / `quartermaster`

## Integrated decision

`FRWO-005-v17` and `FRVE-005-v13` are compatible with the retained treatment,
blueprint, inert functional candidate, and immutable accepted-media manifest.
This shell authorizes exactly one new paid generation domain `{G1}`. It does
not revive E1, F1, or any prior attempt and does not authorize G2, a retry,
relaunch, edit, variation, reference input, alternate transport, crop repair,
responsive derivative, or reveal.

The accepted-only runtime correction is exact: selected source uses
`attemptId="G1"` and no `attemptOrdinal`; source bytes are integer
`1..30,000,000`; accepted aggregate is at most `67,410,731`; and the old loose
geometry predicate is replaced by the exact v4 relation, fragment-group,
three-fragment, dry-approach, activation, and six-layout records. Until every
acceptance gate passes, the runtime remains null-first and unchanged.

## Frozen retained v14/G1 sources

All five sources are strict ASCII UTF-8 without BOM, LF/final-LF, and parse
with zero Windows PowerShell AST errors:

```text
BUILD_HOST06_V14_G1_CARRIER.ps1
  22,259 / 7c0e8393dd7c8608b8006165693cb1c0913b4b2516bb3cdfc2dc49d8d1816726
HOST06_V14_G1_PRODUCTION_CARRIER.ps1
  37,122 / 2b3884414360c773e487935fee16e71188d9cda9baa70f6738ab533703cb57f9
HOST06_V14_G1_LAUNCHER.ps1
  2,639 / 919f4764fd4fd04a7d2840f3a8c02c0e23f4ae2745676ef29916aa5b172883c3
HOST06_V14_G1_STDIN_PARENT.ps1
  2,880 / cc2813e371530d4031004b6ab541e123bb2c29d7944ddf6d949179b23af16990
HOST06_V14_G1_STDIN_FIXTURE_CONTROLLER.ps1
  2,202 / ea8e9c641b9908266b0ef8a02eb05f780bff3370eee1c94a6627c4685edd15fa
parserErrors=0 for all five
```

The builder and fixture are retained evidence, not production entry points.
Quartermaster invokes only the stdin parent, exactly once.

## Sole G1 execution authority

G1 begins at the carrier's sole future `SendAsync` and is consumed by every
outcome. Quartermaster may invoke only:

```text
C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoLogo -NoProfile -NonInteractive -File "Production Pipeline\First Run\HOST06_V14_G1_STDIN_PARENT.ps1"
```

Any HTTP, parser, identity, materialization, technical, physical, layout,
accessibility, provenance, cleanup, protected-boundary, or evidence failure
closes G1 fail-closed. There is no G2 or second request.

The request is one non-streaming `POST` to
`https://api.openai.com/v1/images/generations` with exactly
`model=gpt-image-2`, the frozen prompt, `n=1`, `size=3840x2160`,
`quality=high`, `background=opaque`, and `output_format=png`. Authorization is
environment-only `OPENAI_API_KEY`; only boolean presence may be reported.
Credential, request body, response body, Base64, private diagnostics, and
hidden metadata may not persist.

## Frozen prompt v4

The carrier contains exact `HOST06-GEN-PROMPT-v4`: strict ASCII / UTF-8
without BOM, no terminal LF, **`6,031` bytes**, SHA-256
**`a052c74d83f574fe6628651c2f5b5de54e655468d35b97d076cccd7b35df3086`**.
It differs from prompt v3 only in the frozen composition line and may not be
amended during review.

The full physical sentence is a deliberately small, distant center island:

- complete lens/cradle/support/extremities inside `x=.40..60,y=.39..61`;
  tight width `.16..18`, tight height `.17..20`, center x `.50..51`, center y
  `.49..51`, top `>=.40`, bottom `<=.60`;
- complete fragment group inside `x=.31..41,y=.58..72`, at least three
  individually legible fragments, each source width `>=.018`;
- one continuous dry approach inside `x=.31..43,y=.52..74`, visibly joining
  the fragments to the relation without entering activation;
- all essential reading inside `x=.31..60,y=.39..74`;
- distant same-basin context only inside `x=.70..96,y=.12..56` and water only
  inside `x=.72..98,y=.70..92`.

The Mission capacity witness uses relation `(.41,.405,.18,.19)`, activation
`(.40,.375,.20,.25)`, three `.019`-wide fragments, group
`(.315,.60,.06,.10)`, and approach `(.32,.54,.08,.18)`. Relation center is
`(.50,.50)`, activation/physical ratio is `1.46198830409357`, minimum
separation is `96` source pixels / `8 CSS px` at 320, and full-source retention
is `1` at all six layouts. These are capacity facts, not candidate evidence.

## Exact controlled paths

Only these nine paths are controlled:

```text
helper root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-v14-5aa24cb3-86e9-4a21-babf-1e00f708ef18
helper DLL=<helper root>\Host06FileIdentity.dll
live root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-v14-9783dcf8-7526-475e-b92e-d0abd931c7a3
stage=<live root>\.attempt-G1-f76e28bb-7e72-437b-ab45-b17c4b27c45e.stage
target=<live root>\attempt-G1.png
decision=<live root>\.attempt-G1.review-v1
product root=C:\Users\marti\OneDrive\Desktop\Horizon Archive\Visual Direction\Production Masters\2026-08-10-first-run-host06
product raster=<product root>\host06-stranded-lens-cradle-master-v1.png
product provenance=<product root>\PROVENANCE.md
```

All nine are absent at the gate. Old attempt roots, managed output, parent or
sibling enumeration, accepted-media pixels, opaque residuals, QA quarantine,
and protected/user state are inaccessible and cannot supply evidence.

## Technical and private review

The inherited helper, official ImagesResponse dictionary parser, canonical
Base64, create-new/flush/identity/no-replace materialization, strict PNG,
3840x2160 opaque sRGB decode, same-run dynamic DLL identity, and exact cleanup
gates remain mandatory. Response bytes are bounded `1..40,500,000`, Base64
`4..40,000,000`, and decoded PNG `1..30,000,000`.

Private original-resolution review requires technical PASS, every inherited
`PHY-01..12` relation predicate, exact v4 geometry, and all six responsive and
accessibility records. The candidate must show one weathered lens visibly
nested at least `.80` inside one tilted conformal cradle, two load/stress
contacts, two drainage seams, dry clearance and approach, the fragment handoff,
a restrained horizon catch, no human trace/text/watermark/lesson diagram, and
no alternate canon or world response.

The decision is create-new and exact:

```text
HOST06_REVIEW_V1|attemptId=G1|decision=ACCEPT|technical=true|physical=true|layouts=true|accessibility=true|codes=NONE\n
```

Any false or uncertain predicate requires truthful `REJECT` with `PHY-xx`,
`LAYOUT`, or `ACCESSIBILITY`. A rejected candidate is identity-deleted and has
zero canon, reuse, product, maturity, publication, screenshot, or reveal
authority.

## Accepted-only integration and validation

After ACCEPT and byte-identical import/provenance only, Quartermaster may
populate the already declared Host 06 registry, exact v4 source/layout
geometry, factual alt, and seven owner-correct copy slots. It removes
`attemptOrdinal`, adds `attemptId="G1"`, raises the source guard to 30 MB,
replaces historical geometry checks, updates focused tests/evidence, and sets
`source.enabled=true` last.

Host 05 mastery remains the only gate. `LOOK AT` stays physical and write-free;
`TALK TO` stays silent; available/in-progress/remediation `USE` enters the
sole unchanged `L02-03`; complete `USE` is read-only. Focus order, return,
reload, Meadow, Escape, reduced motion, forced colors, native input, privacy,
save, no-cross-credit, and generic-launcher behavior remain frozen.

Run focused Host 05/06/L02-03 and learning/privacy tests, adjacent-route
regressions, validators, cold full suite, clean fixture and production builds,
PBA, served identity, six-layout live review, and exactly one isolated
candidate E2E. Budgets remain JS `<=1,679,393/1,703,258`, CSS
`<=119,547/119,672`, modules `<=218/222`, maps `0`, accepted media exactly 18
and aggregate `<=67,410,731`, decode `<=250ms/100ms`, ready `<=750ms`, CLS
`<=.01/.05`, activation `<=2ms`, task `<=100ms`, and E2E `<=180s`.

Provenance records Work Order, shell, helper source/current DLL identity,
transport, endpoint/options, prompt v4 identity, `attemptId=G1`, selected
bytes/SHA, conjunctive review PASS, immutable manifest, product path, and
cleanup. It includes no credential, response, Base64, rejected identity, or
private diagnostic.

## Invariants and evidence

The immutable baseline remains `17 / 37,410,731`, manifest digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`.
Only accepted G1 may make `18` media and reduce the missing Host count.
Intelligence alone advances maturity.

The one-path rail, lesson ownership, Hosts 04/05 and 07+, `L02-03`, save and
privacy laws, READY/NOT YET READY parity, shared RP-012 ending, and exact null
deltas remain unchanged. No branch, packet, lesson, answer, reward, access,
identity, authority, recognition, Machine/Builder dialogue, world response,
hidden lore, RP-013, successor, or post-ending content is permitted.

Mission validation passed: five exact source identities and parser-zero;
prompt v4 exact; exactly one carrier `SendAsync`; generic-dictionary semantic
suite `15/15`; six-layout synthetic witness PASS; and one credential-cleared
fixture at `PT06_CREDENTIAL_GATE` with one child and zero credential reads,
request constructions, sends, API calls, or G1 consumption. All nine paths
were absent before and after.

The Operations process variance remains separately **OPEN**:
`UNAUTHORIZED DIVERGENCE / FULL UNTRACKED GIT STATUS ENUMERATED PROTECTED
FILENAMES`. Mission did not repeat, inspect, use, cure, close, or renumber it;
all status checks used `--untracked-files=no`. VR-65 remains opaque,
inaccessible, separate, and non-gating.

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-005-v1-VR-45 / SOLE
G1 / DISTANT SMALL CENTER ISLAND / ONE SEND / NO RETRY OR G2 / PRIVATE REVIEW
/ FRESH QUARTERMASTER NEXT`**.
