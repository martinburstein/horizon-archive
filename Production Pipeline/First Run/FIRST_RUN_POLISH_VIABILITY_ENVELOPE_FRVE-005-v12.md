# First Run Polish Viability Envelope - Center-Core Host 06 F1

Envelope ID: `FRVE-005-v12`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v15`

Disposition: **`POLISH VIABILITY READY / SOLE F1 / PROMPT V3 CENTER-CORE /
EXACT F1 RUNTIME IDENTITY + 30 MB SOURCE CAP + FRAGMENT/APPROACH EVIDENCE
CORRECTIONS / ZERO API OR MEDIA`**

Date: **2026-08-12**

Science source inspected: `192ba509c910cb260f31937c51f9cff12bdf1967`

Predecessor release / last asset result: `FRAB-003-v1 / FIRST RUN PASS
RELEASED` / `FRCA-005-v12 / E1 OBJECTIVE LAYOUT REJECTION`

Planning authorities: `FRPB-001-v2`, `FRCL-004-v2`, `FRWO-005-v15`,
`FRRM-005-v15`, `FRSB-005-v15`

Transport / parser baseline: `FRVE-005-v11`, `FRSH-005-v1-VR-43`, and the
five retained v12/E1 controls

Treatment / blueprint: `FRDT-005-v1`, `FRDT-005-v1-VR-01`,
`FRPX-005-v1`, `FRPX-005-v1-VR-01`

Exact next recipient: one fresh Mission Captain / `mission_captain`

## Science decision

The F1 center-core correction is technically, physically, educationally,
accessibly, and operationally viable. One complete lens/cradle relation,
three qualifying Host 05 fragments, one continuous dry approach, the natural
horizon catch, and separated same-basin water/context can coexist inside the
substantially inset source corridor without a crop, alternate, derivative,
overlay, or fabricated alternative description. At all six frozen layouts,
the smallest complete relation and a containing activation remain larger than
the `44x44 CSS px` target, the three fragments retain the frozen minimum
source width, and the protected separation floor can be met.

Science therefore issues `POLISH VIABILITY READY` for exactly one future
attempt domain `{F1}`. F1 begins at the carrier's sole future `SendAsync` and
is consumed by every result. There is no F2, retry, relaunch, fallback,
alternate endpoint/model/transport, edit, variation, reference input, second
output, crop repair, repositioning, reconstruction, or reuse of E1 or an
earlier attempt.

Three Mission/runtime corrections are mandatory before production:

1. **`REQUIRED CORRECTION / F1 RUNTIME SOURCE IDENTITY`** - the selected
   runtime source, provenance, guard, tests, measurements, PBA, served
   identity, and E2E must use exact `attemptId="F1"`. The obsolete numeric
   `attemptOrdinal` field and historical `{2,3}` predicate must be removed,
   never forged or repurposed. Historical ledgers remain unchanged.
2. **`REQUIRED CORRECTION / 4K PRODUCT BYTE ENVELOPE`** - a selected source
   remains exact integer `1..30,000,000` bytes, accepted media exact `18`, and
   aggregate media `<=67,410,731`. The current inert `12,000,000` selected-
   source guard must be replaced in the runtime, tests, provenance contract,
   PBA, served identity, and E2E. Decode, image-ready, CLS, task, and offline
   gates remain independent and conjunctive.
3. **`REQUIRED CORRECTION / F1 GEOMETRY, FRAGMENT, AND APPROACH EVIDENCE`** -
   the current inert measurement guard's broader historical relation bands
   must be replaced by the exact v3 relation envelope, size, center,
   top/bottom, and center-corridor predicates. The accepted-only runtime/
   configuration record must also add exact source-space evidence for
   `fragmentGroup`, exactly three-or-more individually qualifying fragment
   rectangles, and `dryApproach`, plus their mapped rectangles, centers,
   retained areas, and protected intersections/separations in each of the six
   layout records. The pure measurement guard, focused tests, live summary,
   PBA, served identity, and E2E must prove: every required fragment is wholly
   inside `x=[.27,.38], y=[.62,.80]`, each has width `>=.021` source width,
   the complete group remains legible at `390px`, the approach is wholly
   inside `x=[.27,.41], y=[.54,.82]`, it visibly joins the group to the lower-
   left relation edge without entering activation, and relation/group/
   approach retention is `>=.995`. The existing `protected.host05Cue` field
   cannot substitute for these distinct positive facts.

These are exact Work Order identity, performance, geometry, accessibility,
and validation corrections. They populate nothing before selection and do not
change current player behavior. Mission may not omit them or silently retain
the obsolete runtime contract.

## Official API basis

Official OpenAI documentation identifies `gpt-image-2` as supporting
`v1/images/generations`. The current Images API reference permits arbitrary
`WIDTHxHEIGHT` strings for `gpt-image-2` when the dimensions are divisible by
16 and the aspect ratio is between `1:3` and `3:1`; resolutions above
`2560x1440` are experimental and `3840x2160` is the maximum. It documents
opaque background, PNG output, high quality, and GPT image Base64 output.

Sources:

- https://developers.openai.com/api/docs/models/gpt-image-2
- https://developers.openai.com/api/reference/resources/images/methods/generate

This proves contract availability only. Account access, HTTP success,
response validity, candidate compliance, selection, performance, and release
remain downstream fail-closed gates.

## Frozen `HOST06-GEN-PROMPT-v3`

Science independently extracted the eleven prompt content lines from
`FRWO-005-v15` and recomputed strict ASCII / UTF-8 without BOM, LF separators,
and no terminal LF:

```text
HOST06-GEN-PROMPT-v3
bytes=5386
sha256=1c55c9c5d6dbbf7c83a0fcadde4180e974db051dc9f883542aadf3ea2f9b8a1f
ascii=true
cr=false
terminalLf=false
```

Mission must copy the complete prompt from `FRWO-005-v15` byte-exactly and
prove this identity in its retained carrier. A mismatch is `HOLD`; no role may
paraphrase, supplement, negatively prompt, seed, or amend it from review.

The request remains one non-streaming `POST` to
`https://api.openai.com/v1/images/generations` with exactly seven members:
`model=gpt-image-2`, this exact prompt, `n=1`, `size=3840x2160`,
`quality=high`, `background=opaque`, and `output_format=png`.

## Center-core six-layout witness

One unselected synthetic witness proves the conjunction has capacity:

```text
relation = x .38, y .355, width .25, height .27, center (.505,.49)
activation = x .38, y .34, width .25, height .29
fragment 1 = x .272..294, fragment 2 = x .302..324,
fragment 3 = x .332..354; each width .022; group y .64..78
dry approach = x .28..38, y .57..79; terminates at activation boundary
live water = x .70..98, y .66..92
distant context = x .68..96, y .12..56
horizon/reflection = restrained crossing through the inner lens near y .49
```

The relation lies inside `x=[.36,.64], y=[.32,.68]`; its `.25x.27` tight
rectangle and center satisfy every width/height/center/top/bottom predicate.
The three `.022`-wide fragments lie wholly inside the F1 band and map to
`8.58px` each at `390px`. The activation/relation area ratio is
`1.07407407407407`. Fragment-group-to-activation separation is `.026` source
width = `99.84` source pixels = `8.32 CSS px` at the smallest `320px` world.
Relation-to-water horizontal separation is `.07` source width = `268.8`
source pixels; the approach remains wholly left of water. The witness leaves
room for nested fraction `>=.80`, tilt `12..35` degrees, two contacts, two
drainage seams, dry clearance `>=.02` source height, and a natural horizon/
reflection crossing `>=.25` of the inner-lens width. Those remain candidate
facts, not inferred evidence.

| Layout | World | Relation | Activation | Each minimum fragment |
| --- | ---: | ---: | ---: | ---: |
| desktop | `1920x1080` | `480x291.6` | `480x313.2` | `40.32px` |
| laptop | `1366x768.375` | `341.5x207.46125` | `341.5x222.82875` | `28.686px` |
| narrow | `390x219.375` | `97.5x59.23125` | `97.5x63.61875` | `8.19px` minimum / `8.58px` witness |
| effective `200%` | `768x432` | `192x116.64` | `192x125.28` | `16.128px` |
| retained `320x180` | `320x180` | `80x48.6` | `80x52.2` | `6.72px` |
| centered `320x180` in `320x240` | `320x180` | `80x48.6` | `80x52.2` | `6.72px` |

Because all worlds are exact `16:9`, one full-source `object-fit:cover` /
`object-position:50% 50%` mapping retains the source at `1.0`; candidate and
live evidence must still prove `>=.995` for the relation, group, fragments,
and approach rather than inheriting this arithmetic.

## Request, response, parser, identity, and materialization envelope

Mission must create one new v13/F1 family from the retained v12/E1 baseline.
The E1 sources are read-only evidence, not executable F1 ingress. Preserve:

- environment-only `OPENAI_API_KEY`; only boolean nonblank presence reported;
- redirect disabled, `ResponseHeadersRead`, ten-minute timeout, one
  `SendAsync`, and no streaming;
- response `1..40,500,000` bytes in bounded `65,536`-byte reads;
- canonical Base64 `4..40,000,000` characters and decoded PNG
  `1..30,000,000` bytes;
- strict UTF-8/no BOM and whole-document JSON syntax/duplicate-key rejection;
- PowerShell 5.1 generic-dictionary-safe, case-sensitive key-set membership;
- nonnegative integral `created`, exactly one data dictionary, one string
  `b64_json`, exact known echoes when present, null-or-absent `url` and
  `revised_prompt`, nonnegative known usage integers, and bounded future
  metadata only after the strict whole-document guard;
- canonical Base64 round-trip and decoded-length equality; and
- disposal/clearing of response stream/bytes/text/object, Base64, decoded
  bytes, request JSON, prompt body, credential, and private diagnostics at the
  frozen boundaries.

The semantic parser must pass the same 15 positive/adversarial cases under
exact Windows PowerShell `5.1.26100.8875` generic dictionaries. No synthetic
suite may read a credential, construct/send a request, allocate controlled
paths, or touch media.

The helper remains temp-only C# compilation with exact source `1693 /
98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97`.
Its only native primitive is `GetFileInformationByHandle` over an already-open
exclusive `SafeFileHandle`; source/DLL identity, reflection/PInvoke shape, one
link, no reparse, exact size, no helper close/path/write method, and exact
DLL/empty-root cleanup must pass before credential access.

Materialization remains create-new stage, one bounded write, `Flush(true)`,
handle-bound identity, exclusive reopen/SHA, same-file pre-move identity,
same-directory no-replace move, stage absence, and final-target identity. Any
collision, alias, substitution, reparse, extra link, drift, or cleanup doubt
stops fail-closed.

## Exact fresh paths and attempt boundary

Mission must retain only these nine literal F1 paths:

```text
helper root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-v13-1a72c913-2a4c-4c2e-89a6-258748b7f27b
helper DLL=<helper root>\Host06FileIdentity.dll
live root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-v13-3da45298-46a7-4b77-80ee-199078ffcc82
stage=<live root>\.attempt-F1-81c2fa09-a857-4790-b9bd-e26312ea6b20.stage
target=<live root>\attempt-F1.png
decision=<live root>\.attempt-F1.review-v1
product root=C:\Users\marti\OneDrive\Desktop\Horizon Archive\Visual Direction\Production Masters\2026-08-10-first-run-host06
product raster=<product root>\host06-stranded-lens-cradle-master-v1.png
product provenance=<product root>\PROVENANCE.md
```

Science independently proved these exact nine predicates absent without
parent or sibling enumeration. Old v7-v12 roots, D1, E1, managed output,
opaque residuals, accepted-media pixels, protected paths, and user state are
inaccessible and cannot supply evidence.

F1 begins only at the one later shell-authorized `SendAsync` and is consumed
by HTTP failure, parser failure, candidate rejection, cleanup failure, or
success. No preflight, fixture, build, or review starts or retries F1.

## Technical, physical, layout, and provenance acceptance

Technical acceptance remains exact: PNG signature, bounded chunk lengths,
allowed types/order/CRC, one `3840x2160` IHDR, 8-bit truecolor opaque sRGB,
methods zero, valid sRGB ordering and only allowed gamma/chromaticity/physical
metadata, no alpha/palette/transparency/ICC/text/EXIF/time/animation/private or
unknown chunk, consecutive nonempty IDAT, final empty IEND, no trailing bytes,
one complete zlib stream, exact inflated scanlines/filter bytes, isolated
browser decode, opacity, and byte caps.

Private original-resolution physical selection requires all inherited
`PHY-01..12` plus the v3 predicates:

- one complete relation wholly inside `x=.36..64, y=.32..68`, width
  `.24..27`, height `.25..29`, center x `.50..51`, center y `.48..50`, top
  `>=.33`, bottom `<=.65`;
- visible lens nested `>=.80`, cradle tilt `12..35` degrees, at least two
  distinct load/stress contacts, at least two continuous drainage seams, and
  dry clearance `>=.02` source height;
- complete fragment group inside `x=.27..38, y=.62..80`, at least three
  distinct non-token fragments each width `>=.021` source width and legible at
  `390px`;
- continuous dry approach inside `x=.27..41, y=.54..82`, visibly joining
  fragment group to the lower-left relation edge without entering activation;
- every essential fact inside `x=.27..64, y=.32..82`; top twelve percent free
  of essential fact; distant context and water only in their permitted bands;
- natural horizon/reflection crossing `>=.25` of the inner-lens width; and
- first-person same basin, one candidate, no human trace, text, watermark,
  native lesson diagram, purpose claim, response, or later-host expression.

Selection is conjunctive. One selected `<img>` uses only `cover`, `50% 50%`,
and the full source at the six layouts. Activation contains the complete
relation/center, has area ratio `<=1.50`, center clearance `>=4 CSS px`, and
target `>=44x44 CSS px`; protected intersections are zero and separation is
`>=96` source pixels / `>=8 CSS px` mapped. Source-center and relation/group/
fragment/approach retention are `>=.995`. Pointer, touch, Enter, Space,
speech-by-name, and switch-like activation converge on one native click path;
focus remains stable within `1/64 CSS px`; forced colors use exact `3px solid
Highlight`; reduced motion preserves source/state/order/focus/meaning.

Review is technical-first, private, and no-reveal. ACCEPT requires exact
`technical=true|physical=true|layouts=true|accessibility=true|codes=NONE`.
Any false or uncertain predicate requires truthful objective rejection or
terminal failure. A rejected F1 has zero canon, product, reuse, prompt,
maturity, screenshot, publication, or reveal authority.

Only conjunctive ACCEPT may copy the source byte-identically to the exact
product raster and create provenance recording Work Order, shell, helper
source/dynamic DLL identities, transport, endpoint, model/options, exact
prompt-v3 ID/bytes/SHA, `attemptId=F1`, selected bytes/SHA, objective PASS,
immutable manifest identity, product path, and exact cleanup. Credential,
request/response bodies, Base64, hidden metadata, rejected identity, and
private diagnostics never persist.

## Learning, privacy, save, recovery, offline, and authority

- Host 05 mastery remains the sole Host 06 gate; lawful Host 06 remains the
  sole physical entry to unchanged `L02-03`.
- Runtime remains null-first until one accepted source/provenance, exact
  positive fragment/approach and relation measurements, six layouts, factual
  alt, and seven owner-correct copy slots are complete; `enabled=true` is
  written last and is not proof.
- `LOOK AT` is physical/write-free; `TALK TO` is immediate complete silence/
  write-free; available/in-progress/remediation `USE` calls the existing
  model-choice exercise once; completed `USE` is read-only.
- Existing `8+8`, `16/16 + 16/16 + 2/2`, evaluator, actual-dimension
  remediation, blank retry, fresh transfer, closed-note explanation,
  ownership, confidence, no-cross-credit, Demo Tour zero-credit, and evidence
  firewall remain byte/behavior unchanged.
- Only sanitized `modelChoiceEvidence` may persist. Credential, response,
  Base64, candidate, review state, free-form work, focus, timing, media
  diagnostics, and source diagnostics cannot enter save or telemetry.
- Save schema/version/projection, malformed-state fail-close, reload, Meadow
  return, Close/Escape focus, and authorized write-free returns remain exact.
- Packaged runtime has one same-origin selected-image request and zero external
  runtime requests, services, credentials, authority, telemetry, or account
  coupling.

Scene owns observable physical fact; Pilot owns provisional naming and
stranded/correct uncertainty; Suit/System owns compatibility/interface; the
901 Teacher owns only unchanged learning. Machine and Builders own no line.
The one-path rail, Hosts 04/05, Hosts 07+, later rail, READY/NOT YET READY
parity, MH-40, shared RP-012 ending, and exact null deltas remain unchanged:
`cityStateDelta=null`, `worldStateDelta=null`, `externalStateDelta=null`,
`authorityDelta=null`, and `successor=null`. No branch, packet, lesson,
answer, reward, access, identity, authority, recognition, response,
prior-human trace, hidden-lore answer, RP-013, or post-ending content is
permitted.

## Performance, validation, cleanup, and rollback

The immutable accepted-media baseline remains exact `17 / 37,410,731`,
canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`.
Accepted F1 may yield only exact `18` media and aggregate
`37,410,731 + selectedBytes <=67,410,731`.

Affected/global JavaScript remain `<=1,679,393 / 1,703,258`; CSS
`<=119,547 / 119,672`; modules `<=218 / 222`; source maps `0`; cold/warm
decode `<=250ms / <=100ms`; image ready `<=750ms`; attributable/total CLS
`<=.01 / <=.05`; activation `<=2ms`; sampled task `<=100ms`; complete E2E
`<=180s`. A miss is HOLD and authorizes no compression, edit, replacement,
retry, alternate, or responsive repair.

Validation order is fixed:

1. authority/patch scope; five retained F1 source identities/parser-zero;
   prompt v3 identity; one `SendAsync`; F1-only strings; forbidden old domain
   absence; synchronized Git; exact nine-path absence;
2. Windows PowerShell 5.1 generic-dictionary semantic suite `15/15`; exactly
   one credential-cleared fixture with one child, zero credential/request/
   send/F1 activity, and postflight absence;
3. inert runtime proof plus exact accepted-only `attemptId="F1"`, 30 MB source
   guard, exact v3 relation guard, fragment/group/approach schema and guards,
   immutable manifest, no import/DOM/name/action, and unchanged launcher;
4. invoke only the shell-named parent once; prove response/Base64/identity/
   materialization, technical PNG/decode, and private original-resolution
   physical/band/layout/accessibility review with exact decision grammar;
5. after ACCEPT only, prove byte-identical import/provenance and populate only
   registry/relation/fragments/approach/layout/copy/alt/test fields; run focused
   Host 05 -> Host 06 -> L02-03, learning/privacy/save, and adjacent-route
   regressions;
6. applicable validators, cold full tests, clean fixture/production builds,
   PBA, served identity, exactly one isolated non-overlapping no-retry E2E,
   six-layout/live keyboard/focus/forced-color/reduced-motion/error review,
   one machine summary, and one independent verifier; and
7. stop only owned browser/process/ports, restore owned QA fixtures, prove
   helper/DLL/live/stage/target/decision absence and exact product identity
   after acceptance, tracked-clean Git, and workflow-gate synchronization.

Static strings and arithmetic cannot substitute for actual candidate pixels,
DOM absence, one-hit dispatch, focus, live mapping, decode, served identity,
performance, or cleanup proof. Candidate-bearing E2E occurs once only after
all earlier gates.

Rejection cleanup owns only the identity-proved F1 target/decision and empty
live root. Post-import rollback owns only the new Host 06 raster/provenance,
bounded registry/copy/alt/measurement changes, focused tests/evidence, and an
empty product root. No repository reset, recursive broad cleanup, accepted-
media change, planning-history erasure, protected-state access, or user-state
change is permitted.

## Risks, evidence limits, and variance ledger

Prompt compliance is stochastic. A center-core prompt can still fail scale,
nesting, tilt, contacts, drainage, water clearance, fragment count/width/
legibility, approach continuity, horizon optics, same-basin continuity,
forbidden content, technical PNG, accessibility, or performance. Uncertainty
rejects and consumes F1. Prompt arithmetic, HTTP 200, or aesthetic strength is
not acceptance.

Science read the complete active intake, full Science profile, Work Orders
v13-v15, `FRCA-005-v12`, `FRVE-005-v11`, `FRSH-005-v1-VR-43`, reopened
baseline, continuity lock, current map/scoreboard, treatment/blueprint and
their direct-API variances, all five retained E1 controls, and exact current
null-first Host 06 runtime/test controls. It recomputed the prompt identity,
all witness geometry, six mapped layouts, minimum target and fragment scales,
separations, retention, and exact nine-path absence offline. The five retained
v12/E1 files matched their shell-frozen length/SHA identities, were ASCII/LF
with zero parser errors, and the carrier retained exactly one `SendAsync`,
redirect-disabled `ResponseHeadersRead`, the `40,500,000 / 40,000,000 /
30,000,000` response/Base64/decoded caps, dictionary-safe membership, strict
JSON guard, create-new/`Flush(true)` staging, and one no-replace move.
Science also extracted the retained carrier's strict guard into a fresh
Windows PowerShell `5.1.26100.8875` process and independently passed the
generic-dictionary semantic suite `15/15`, with zero credential reads,
request constructions, `SendAsync`/API sends, or media.

Science read no credential; constructed or sent no API request; invoked no
carrier/helper/fixture; allocated no helper/live/product root; created,
inspected, decoded, imported, rendered, or revealed no media; ran no product
test, build, preview, browser, E2E, cleanup, production, release, or maturity
operation. No candidate facts, selected bytes, actual pixel geometry,
technical image validity, fragment legibility, decode timing, served identity,
or live layout evidence exists or is claimed.

The Operations process variance remains separate and **OPEN**:
**`UNAUTHORIZED DIVERGENCE / FULL UNTRACKED GIT STATUS ENUMERATED PROTECTED
FILENAMES`**. Science did not repeat, query, use, close, merge, waive, cure, or
renumber it. All status checks were tracked-only. VR-65 remains separate,
opaque, inaccessible, and non-gating. Repository QA quarantine, protected PDF,
training tree, Martin's real browser/profile/save, hidden lore, accepted-media
pixels, historical attempts, old roots, managed output, opaque residuals, and
unlisted user state were not opened, listed, searched, inferred, changed,
moved, or deleted.

## Exact Mission handoff

One fresh Mission Captain reads the complete active intake, full Mission
profile, `FRWO-005-v15`, this complete `FRVE-005-v12`, `FRCA-005-v12`,
`FRVE-005-v11`, `FRSH-005-v1-VR-43`, current map/scoreboard, treatment/
blueprint controls, all five retained v12/E1 controls, and only the exact
current Host 06 runtime/test controls they cite.

Mission issues one separately versioned F1 shell and five new retained v13/F1
sources. It must preserve the literal paths, exact prompt-v3 identity, one-
send/no-retry domain, dictionary-safe response envelope, helper/identity/
materialization/PNG/private-review/no-reveal/cleanup/provenance contracts,
full validation ladder, and every canon/learning/save/world/ending boundary.
It must explicitly freeze all three required runtime corrections:
`attemptId="F1"` with no `attemptOrdinal`; selected-source
`1..30,000,000`; and exact v3 relation plus positive fragment/group/dry-
approach source and six-layout evidence.

Before `FIRST RUN SHELL READY`, Mission proves strict UTF-8/no BOM, exact byte
length and lowercase SHA-256, and parser-zero for all five sources; exact
prompt bytes/SHA in the retained carrier; exactly one `SendAsync`; the 15-case
PS5.1 semantic suite; exactly one credential-cleared fixture with zero
credential/request/send/F1 activity; and all nine paths absent. Mission then
commits the shell/sources/handoff, pushes at the workflow gate, and proves
`HEAD == origin/main`.

Mission performs no production invocation, credential read, request
construction, `SendAsync`, F1 consumption, media creation/inspection/import,
product population, browser, E2E, reveal, release, maturity advance, later
role, later host, or completion operation.

Office of Science Administrator signs **`POLISH VIABILITY READY /
FRVE-005-v12 / SOLE F1 / PROMPT V3 CENTER-CORE / REQUIRED F1 IDENTITY + 30
MB SOURCE CAP + FRAGMENT/APPROACH EVIDENCE CORRECTIONS / ZERO API OR MEDIA /
FRESH MISSION NEXT`**.
