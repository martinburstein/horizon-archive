# FIRST RUN FUNCTIONAL REPORT / FRCE-015-v1

## Disposition

`PRODUCTION FUNCTIONAL / V2 NULL-FIRST SOURCE LAW / QUARTERMASTER READY`

Combat stage: `combat_engineer` / `OPERATE`. Shell `FRSH-015-v1`; blueprint
`FRPX-015-v1`; current predecessor `FRCE-014-v1@b4444afe...`; best and
committed release `FRAB-013-v1@357ad6dc...`.

## Implementation

Combat migrated only the disabled source-law seam to
`horizon.waterline-ledger.v2`, exact selected identity `FRM15-A01`, and exact
provenance schema `horizon.first-run.frm15-source-provenance.v1`. The deeply
frozen registry remains disabled/null and keeps visible-pixel measurements
separate from noncanonical control geometry.

The source module now provides the one pure guide-core transform: guide canvas
`1536x1024`, final core `x=0,y=80,w=1536,h=864`, source `3840x2160`, scale
`2.5`, with no clamping, rounding, percentages, or out-of-core acceptance.
Exact control geometry covers target, center, label/insets, and all eight
protected regions. Lawfulness independently requires visible relation, dry
approach, three distinct history records with process evidence, six distinct
pairwise contacts intersecting their named histories, an irregular trace and
three on-trace reactions, waterline, three rising parentless seams, one joined
branch, non-aliased objects, exact source/provenance/decode identity, exact
copy, and caller-independent six-layout derivation. The narration/UI region is
accepted only as the label corridor; the target may overlap no protected
region. Any partial or malformed population keeps native hidden and the exact
generic fallback active.

`FRPBA-015-v1` is the minimal FRM15 path/identity overlay. It preserves the
accepted `FRAM-014-v1` baseline and numeric caps, verifies the selected source
and selected-provenance identities, and requires the selected build's exact
asset/raster inventory and hash set. It is intentionally source-dependent.

Changed files before this report/state: `waterlineLedger.js`, its focused test,
and `FIRST_RUN_PBA_FRPBA-015-v1.ps1`. `App.jsx`, CSS, route, lesson, copy,
Terminal, save, privacy, ending, dependencies, fixtures, and E2E were not
changed.

## Evidence economics and verification

Fresh evidence, because the v2 registry/source-law/test inputs changed:

- focused v2 source-law, transform, mutation, selector, responsive, and PBA
  overlay checks: `13/13/0`;
- production build: PASS, `234` modules;
- null-first observation: JS `1,765,598 <= 1,785,000`, CSS
  `121,983 <= 122,000`, maps `0`, asset files `27`, accepted raster media
  `24`, selected source/provenance absent;
- `FRPBA-015-v1` PowerShell syntax: PASS;
- diff/protected-boundary check: PASS; intended product/runtime paths only.

The first focused run exposed one test-fixture callback defect: passing the
rectangle predicate directly to `Array.every` supplied its index as the
optional source-width argument. One causal correction produced the final
`13/13`; no product threshold was weakened and no same-family plateau exists.

Reused evidence, because no shared route/save/privacy/curriculum/dependency/
fixture/runtime-presentation input changed:

- related `63/63`, cold full `1047/1047`, validators `40/40`, and fixture build
  `57` modules from `FRCE-014-v1@b4444afe...` are `REUSED`, not rerun or
  relabeled fresh;
- the committed release holdout remains `REUSED` for `FRAB-013-v1` only.

Selected-source PBA, guide build/view, custody chain, private pixel review,
source population/import, served identity, browser layouts, E2E, and release
holdout were not run and are not claimed. They require Quartermaster's future
accepted source. No guide or media was built, viewed, inspected, edited,
transformed, or imported; no image call occurred. Forbidden QA/screenshots,
browser/profile/save, lore, archives, PDF/training files, residual roots, and
accepted-media pixels remained unopened and unchanged.

## Quartermaster handoff

Quartermaster may now execute only the shell-frozen G01/custody/built-in graph.
After one independently passing flattened source exists, populate atomically:
source enable/path/`FRM15-A01`/bytes/hash/dimensions/format/color; exact
five-field runtime provenance projection; visible relation, dry approach,
three history bounds and process-evidence rectangles, all six distinct contact
rectangles, trace bounds/points/three reactions, waterline and seam DAG;
unchanged exact control geometry; six derived layout records; unchanged frozen
copy; and runtime URL/import. Do not copy guide controls into visible evidence.

Then run the source-dependent `FRPBA-015-v1`, focused and affected integration
checks, the one reserved cold full suite only after the final JS/config/test
change, selected production/fixture build and served identity, sanctioned
six-layout/browser/accessibility review, one clean-start E2E, cleanup, and the
independent holdout. Stop at the first complete PASS. Any absent, rejected,
partial, stale, aliased, mismatched, or malformed source leaves this v2
registry inert and preserves the exact two-generic rollback.

Budget used: `0` guide families, `0` stochastic calls, `0` deterministic media
operations, `0` private inspections, `0` browser/E2E actions. Full final-proof
reserve remains protected. Remaining uncertainty is actual guide/output
identity and adherence, visible physical measurements, accepted provenance
DAG, source-enabled layout/accessibility/performance/E2E evidence, human AT
usability, and independent release.

Decision: `GATHER_EVIDENCE`. Exact next owner: one Quartermaster.
