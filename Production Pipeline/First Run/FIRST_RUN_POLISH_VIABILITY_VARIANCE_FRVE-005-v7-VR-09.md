# Horizon Archive First Run Polish Viability Variance

Variance ID: `FRVE-005-v7-VR-09`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`HOLD / SOLE COMBINED SAME-PROCESS FIXTURE RESULT NOT
RETAINED TO BOUNDED ACCEPTANCE / NO RETRY / FRVE-005-v7-VR-09`**

Date: **2026-08-11**

Science source inspected:
`336675da56f90b7870b224fe55129a256bec763e`

Mission correction: `FRSH-005-v1-VR-11`

Decisive prior Science return: `FRVE-005-v7-VR-08`

Quartermaster return: `FRCA-005-v4`

Effective shell: `FRSH-005-v1` through `FRSH-005-v1-VR-11`

Effective treatment / blueprint: `FRDT-005-v1-VR-01` /
`FRPX-005-v1-VR-01`

Functional report: `FRCE-005-v1-VR-05`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Mission Captain / `mission_captain`

## Science decision

Science read the complete active intake, workflow, registry, full Science
profile, complete `FRSH-005-v1-VR-11`, `FRCA-005-v4`, complete
`FRSH-005-v1-VR-10`, complete `FRSH-005-v1-VR-09`, decisive
`FRVE-005-v7-VR-08`, complete `FRWO-005-v7`, decisive
`FRVE-005-v7-VR-03`, the complete effective shell, treatment, blueprint,
`FRCE-005-v1-VR-05`, immutable `FRAM-001-v1`, and both exact literal blocks.
Starting `HEAD` was exact `336675da...`. The three disclosed untracked
protected items remained untouched.

The independent static preflight passed completely. Science then ran exactly
one credential-cleared, no-request fixture. The parent completed every
pre-child identity, parser, Windows-limit, stage-order, synthetic response,
review, ordinal-transition, materialization, PNG-schema, success/rollback, and
identity-conditioned cleanup assertion. It invoked the exact frozen child
once. After that invocation, the parent rejected an unexpected or assertion
result and retained only this stable record:

```text
SCIENCE_COMBINED_FIXTURE_FAILURE|code=UNEXPECTED_OR_ASSERTION|childInvocations=1
```

The fixture did not retain the child's exact exit, sole diagnostic, or first
failing assertion as a bounded accepted result. Science therefore cannot
positively claim that the child reached `PH08_ROOT_CREATE_COMPLETE`, entered
the tail, completed helper compile/load/identity/cleanup, or stopped at
`PT06_CREDENTIAL_GATE`, even though the frozen control flow remains statically
consistent with that sequence. The missing retained child result is mandatory
runtime evidence and cannot be reconstructed from intent, stage order, or
final absence.

The exact-one boundary forbids a second fixture in this Science run. Science
issues **`HOLD`** and does not rerun, reinterpret, or weaken the fixture gate.
It does not authorize Quartermaster, an API call, or ordinal `2`.

Variance classification: **`REQUIRED CORRECTION / SCIENCE COMBINED-FIXTURE
CHILD RESULT RETENTION OR ASSERTION DIAGNOSTIC / OPEN`**.

## Exact static identity and parser evidence

Science extracted the literal blocks in memory and normalized only repository
CRLF framing into the frozen LF byte domain. Independent identities are exact:

```text
launcher byteLength=2001
launcher sha256=96feaf7e62fa89e8c80cc46d38425d465cf845ffbd426405a75c73c056314212
prefix byteLength=976
prefix sha256=5cd257c94bcd70b8d6ada4e0b561b2a14ed52fd9459146b1269dc93ce1bdc7d1
tail byteLength=26068
tail sha256=e9815ce8f988bca0f1f9bd14a7ba50cfa4935b3d7a47492c95bfa90986b7070c
combined byteLength=27044
combined sha256=015dfd96befad29793892f1e15dc9ff4362ff8cec0ae4ce7b9c45b5da9e125f3
```

All four values were UTF-8 without BOM, LF-only, and included the final LF.
Combined bytes `[0..975]` were byte-identical to the accepted `976`-byte
pre-helper. The complete combined block parsed with exact `0` errors.

The executable remained exact
`C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`; the exact
argument vector remained `-NoLogo -NoProfile -NonInteractive
-ExecutionPolicy Bypass -Command <launcher>`; and the sole process environment
name remained exact `HORIZON_ARCHIVE_HOST06_PREHELPER_V1`. The unchanged
launcher command and complete environment entry were independently rendered
below the applicable `8,191` and `32,767` character limits. No encoded command,
stdin, child source file, second environment name, profile, nested child shell,
or alternate transport was used.

The complete `PT01..PT18` stage identifiers were present in frozen order. This
is static contract evidence only; it does not replace the missing accepted
child-stage result.

## Synthetic non-media branch evidence

Before the sole child invocation, the same Science fixture passed all of these
synthetic non-media controls:

- strict response shape and canonical-base64 acceptance, extra-member
  rejection, and malformed-base64 rejection;
- exact review-record acceptance and objective rejection, invalid ACCEPT and
  invalid REJECT semantic rejection, and the ordinal-2 objective-rejection to
  ordinal-3 transition condition;
- in-memory strict PNG schema acceptance plus CRC, dimension, and alpha
  rejection using scalar/chunk facts only, without raster bytes, decode,
  rendering, or pixels;
- create-new, one write, `Flush(true)`, byte/hash equality, same-directory
  no-replace move, mismatch-conditioned deletion refusal, byte-identical
  success copy, rollback deletion, target cleanup, and exact empty-root
  nonrecursive cleanup using a fresh disposable `.bin` fixture only; and
- production-failure normalizer shape tests prepared for the retained child
  record.

The disposable fixture root was outside every controlled production path and
was deleted exactly and nonrecursively before the child began. No fixture file
entered the repository or product tree.

## No-request, ordinal, and cleanup result

The parent removed `OPENAI_API_KEY` from its process environment without
reading it and supplied the exact combined value only through the frozen
process-environment name. Because request construction follows the credential
gate, the credential-cleared child had no lawful path to request construction
or `SendAsync`. Exact activity for this Science run remains:

```text
credential value reads=0
request constructions=0
SendAsync calls=0
direct Image API sends=0
ordinals consumed=0
```

Historical ordinal `1` remains consumed, opaque, inaccessible, and unchanged.
Ordinals `2` and `3` remain unstarted and unconsumed.

Independent read-only postflight proved:

```text
helperRootAbsent=true
helperDllAbsent=true
liveRootAbsent=true
ordinal2StageAbsent=true
ordinal2TargetAbsent=true
ordinal2DecisionAbsent=true
ordinal3StageAbsent=true
ordinal3TargetAbsent=true
ordinal3DecisionAbsent=true
productRootAbsent=true
productRasterAbsent=true
productProvenanceAbsent=true
scienceFixtureRootsAbsent=true
```

Final absence proves cleanup state; it does not prove the missing child stage,
helper execution, or accepted diagnostic. No live root or product target was
allocated. No response, JSON from a service, base64 payload, media byte,
raster, provenance, accepted-media stream, image, or pixel was created,
opened, inspected, rendered, or retained.

## Protected state, maturity, and exact Mission handoff

The repository QA quarantine, protected PDF, training tree, Martin's real
browser/profile/save, hidden lore, accepted-media bytes/pixels, OS-temp parent,
ordinal-1 residual, real managed directory, user work, VR-65, and every opaque
residual remained untouched. All thirteen inherited process records and the
separate Commandant filename/search-scope record remain separate and **OPEN**.
This HOLD closes, cures, merges, waives, renumbers, accesses, or reclassifies
none.

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`.

Science changed no product, test, runtime, lesson, save, media, manifest,
candidate, map, scoreboard, maturity, process classification, residual,
schedule, or automation state. The temporary Science fixture harness was
removed before commit. The only repository changes authorized for this stage
are this variance and synchronized `NEXT_INSTANCE_HANDOFF.md`.

One fresh Mission Captain / `mission_captain` reads this complete HOLD,
complete `FRSH-005-v1-VR-11`, `FRCA-005-v4`, complete
`FRSH-005-v1-VR-10`, complete `FRSH-005-v1-VR-09`, decisive
`FRVE-005-v7-VR-08`, complete `FRWO-005-v7`, decisive
`FRVE-005-v7-VR-03`, the effective shell, treatment, blueprint,
`FRCE-005-v1-VR-05`, immutable `FRAM-001-v1`, and both exact literal blocks.

Mission issues exactly one new versioned `FIRST RUN SHELL READY`, `REVISE`, or
`HOLD` decision for the missing bounded child-result retention/diagnostic.
Mission may not treat static proof or final absence as the missing runtime
acceptance; silently authorize another Science fixture; route to Quartermaster;
execute the launcher/helper; allocate a live root; read a credential; construct
or send a request; consume ordinal `2`; inspect media/pixels; change product or
tests; run E2E; reveal; advance maturity; close an OPEN record; access a
residual or VR-65; schedule; automate; push; release; or call `FIRST RUN
COMPLETE`.

Office of Science Administrator signs **`HOLD /
FRVE-005-v7-VR-09`** from exact source `336675da...`.
