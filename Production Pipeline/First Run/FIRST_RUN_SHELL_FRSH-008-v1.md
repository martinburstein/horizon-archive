# Horizon Archive First Run Shell - Host 08 Severed Relay Spine

Shell ID: `FRSH-008-v1`

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-008-v1` / `FRVE-008-v1`

Disposition: **`FIRST RUN SHELL READY / HOST 08 / IMAGEGEN CLI / FIVE SEQUENTIAL ATTEMPTS`**

Date: **2026-08-12**

Shell source: `62ab7b86c067f739d19e0c9bfe8f4b6ec1281b4c`

Released predecessor: `FRAB-007-v1`, release commit
`ba671218921cfda0dd0f38dce1fb2673a3308014`

Baseline / continuity: `FRPB-001-v5` / `FRCL-008-v1`

## Fixed outcome and boundaries

After exact Host 07 and strict `L03-01` plus `L03-02` mastery, the player
reaches one dry nonhumanoid Severed Relay Spine. Stacked service laminates have
collapsed laterally. One external continuity visibly ends in fracture while a
different continuity remains materially connected and pressure-bearing toward
the basin wall. Host 07's interrupted sediment/channel relation remains the
physical handoff. LOOK is physical and write-free; TALK is silent; sole USE
owns unchanged `L03-03`. After completion, the current generic Text Analysis
continuation remains. Host 09 stays absent.

No radio, computer, network diagram, active message, protocol, robot, body,
repair invitation, route opening, branch, lesson, reward, identity, authority,
world response, hidden-lore answer, Host 09 reveal, RP-013, or successor may be
added. `Relay` remains only the Pilot's provisional hypothesis.

## Exact generation contract

Only this installed CLI may run:

```text
C:\Users\marti\.codex\skills\.system\imagegen\scripts\image_gen.py
bytes=35266
sha256=c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05
python=3.12.10
openai=3.0.0
```

Every call is `python <script> generate --prompt-file <exact prompt> --model
gpt-image-2 --size 3840x2160 --quality high --output-format png --background
opaque --out <exact ordinal path> --no-augment`. `n=1` is default. No force,
batch, edit, reference/input image, mask, variation, alternate model, direct
REST/SDK, script edit, internal retry, reveal, or sixth call exists. API-key
presence may be tested only as a boolean; its value is secret.

Attempt domain is exactly `{H8-1,H8-2,H8-3,H8-4,H8-5}`, initially unconsumed.
An ordinal is consumed when its CLI process launches. Nonzero exit stops and
returns for adjudication. Objective rejection plus exact identity cleanup and
absence alone enables the next ordinal. First PASS stops all later calls.

## Frozen custody paths

Fresh currently absent root:

```text
C:\Users\marti\AppData\Local\Temp\horizon-archive-host08-cli-89f57c93-a731-46c2-87bd-afb58586476c
```

Its only children may be `h8-1.png` through `h8-5.png`. Quartermaster creates
the root immediately before the first eligible call, proves ordinary,
non-reparse, contained, and empty, and removes it nonrecursively after all
identity-conditioned candidate cleanup.

Only a selected byte-identical source and provenance may enter:

```text
Visual Direction/Production Masters/2026-08-12-first-run-host08/
  host08-severed-relay-spine-master-v1.png
  PROVENANCE.md
```

Both product paths are currently absent. Import is create-new/no-replace.

## Acceptance and private review

Recon owns five complete independent prompt files and directorial meaning;
Tactical may tighten measurable composition without changing physical canon.
All prompt bytes and hashes freeze before Combat's candidate commit.

Quartermaster privately checks exact ordinary/non-reparse/one-link identity;
sole-child set; `1..30,000,000` bytes and SHA-256; strict static CRC-valid
`3840x2160` opaque RGB/RGBA PNG; local decode; every `FRCL-008-v1` physical
predicate; and every normalized threshold in `FRVE-008-v1`. Six layouts must
retain at least 95% of the complete relation and all essential centers; target
is at least 44px, contains the physical center, and avoids damage/water/
Host07/return/Crown/later cues; label/focus separation is at least 8px.
Ambiguity rejects. Aesthetic strength cannot override a predicate.

Rejected pixels are never reused, referenced, published, or revealed. Only
bounded reason codes may inform already-predeclared later prompts. Identity is
reproved immediately before deletion or selected copy.

## Runtime and interaction shell

Combat first builds inert null-first Host 08 source, provenance, geometry,
copy, alt, state, and tests. Exact Host 07 + strict `L03-01`/`L03-02` is the
sole predecessor. Before enablement the generic Client Bridge launcher remains.
Accepted-only enablement removes it irreversibly; any later guard failure hides
Host 08 without restoring a bypass.

State is hidden -> available -> in progress -> remediation required or
complete, derived only from sanitized strict Client Bridge evidence. LOOK,
selection, focus, media review, and presentation never mint evidence. USE
opens unchanged `L03-03`; completed USE is read-only. Keyboard, pointer, touch,
switch-like activation, semantic naming, focus, announcement, return, reload,
resume, reflow/effective-200%, forced color, and reduced motion remain
equivalent and truthful.

## Roles, validation, and budgets

1. Recon freezes direction and five prompts; no generation.
2. Tactical freezes flow/state/focus/recovery/copy/geometry/private-review
   blueprint; no generation.
3. Combat implements inert runtime/tests, freezes code and prompts, runs
   non-media gates, commits/pushes; no generation.
4. Quartermaster alone calls the CLI, reviews privately, cleans rejects,
   imports the first passing byte stream, writes provenance and accepted-only
   fields, and runs content gates.
5. Image Specialist performs code/config presentation only with unchanged
   selected bytes.
6. Intelligence independently validates and alone releases/matures Host 08.

Budgets: accepted media `<=19 / 83,320,925`; affected/global JS
`<=1,702,345 / 1,726,000`; CSS `<=119,900`; modules `<=222`; maps `0`; no
dependency change; decode `<=300ms` cold / `<=120ms` warm; handler `<=2ms`;
CLS `<=.01/.05`; full E2E `<180s`.

Run focused Host07/Host08/ClientBridge tests; related route/save/privacy/
accessibility/ending tests; validators/full suite; production and TD-012
builds; PBA/media; served identities; six-layout/input/focus/forced-color/
reduced-motion review; and one clean-start Host08-to-MH40 E2E using a fresh
explicit external QA root outside the repository. Fail before browser start
if that root is absent, in-repository, reparse, nonempty, or not cleanable.
Never access repository QA quarantine.

Rollback removes only exact Host 08 additions and this shell's scratch.
Accepted Hosts 01-07/media, lessons, saves, route, ending, protected state,
opaque residuals, and later gaps are immutable.

## Done, variances, and handoff

Done requires one byte-identical selected raster/provenance, exact measurements
and copy, accepted-only runtime source, complete `L03-03` interaction, every
gate, no rejected/scratch residue, no protected access, Intelligence release,
and synchronized main. If no candidate passes, close HOLD with five consumed,
rejected, and cleaned. Tool/auth/quota/transport failure stops at its active
ordinal.

Every variance returns to its earliest workflow owner. Preserve the OPEN
protected-QA capture-write variance, both filename-enumeration records, every
other OPEN record, and VR-65. Protected screenshots remain untouched and
unstaged.

Mission signs **`FIRST RUN SHELL READY / FRSH-008-v1 / ROUTE FRESH RECON`**.
