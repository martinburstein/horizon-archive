# Horizon Archive First Run Work Order - Host 09 Flooded Choir

Work Order ID: `FRWO-009-v1`

Stage / stable agent: Operations Planning Major / `operations_planning_major`

Disposition: **`WORK ORDER READY / HOST 09 FLOODED CHOIR / FIVE TOTAL SEQUENTIAL ATTEMPTS MAXIMUM`**

Baseline / continuity / predecessor: `FRPB-001-v6` / `FRCL-009-v1` /
`FRAB-008-v1`

Address: `FR-03 / Host 09`, immediately after released Host 08 and before
missing Host 10.

## Earliest-first outcome and maturity

Host 09 is the first missing player-facing expression after the synchronized
Host 08 release. The existing pressure-bearing continuity already supplies the
physical handoff; no later/global dependency displaces it.

After exact Host 08 and `L03-03` mastery, the player reaches one dry annular rim
beside a family of differently sized water-filled cavities. Pressure and
reflection relations are observable without implying voice or communication.
LOOK is physical/write-free, TALK is silent, and sole USE owns unchanged
`L04-01 + L04-02` Text Analysis and Speech Workloads.

Current: Host 09 missing; `FR-03` physical `FR0`, one accepted compression /
four exact / seven missing. Target: Host 09 exact/released while address-level
physical maturity remains `FR0` and Hosts 10-15 remain missing.

## Exact five-attempt generation boundary

Domain is `{H9-1,H9-2,H9-3,H9-4,H9-5}`, all unconsumed. Later Science and
Mission must freeze the exact approved imagegen route, one prompt per ordinal,
one output per call, maximum `3840x2160`, high-quality opaque PNG, and complete
key/custody/identity controls.

Calls are sequential. Nonzero generator exit consumes and stops. Objective
rejection plus exact identity-conditioned cleanup alone enables the next fixed
prompt. First PASS stops. No sixth call, rerun, internal retry, batch, edit,
input/reference image, mask, alternate model, script edit, or reveal exists.

Mission must freeze one fresh absent OS-temp root and exact `h9-1.png` through
`h9-5.png` children. Root/current/later/product absence, ordinary/nonreparse/
one-link identity, sole-child set, strict technical review, candidate hash,
identity reproof, exact rejection deletion, selected create-new/no-replace
copy, and final nonrecursive root deletion are mandatory. Any ambiguity stops.

## Product and runtime scope

At most one accepted byte-identical raster and provenance may enter:

```text
Visual Direction/Production Masters/2026-08-12-first-run-host09/
  host09-flooded-choir-master-v1.png
  PROVENANCE.md
```

Combat first builds a null-first fail-closed Host 09 source, provenance,
geometry, copy, alt, state, and tests. Exact Host 08 plus strict `L03-03`
mastery is the sole predecessor. Before accepted enablement the current generic
Text Analysis and Speech Workloads launchers remain. Accepted enablement removes
only those bypasses; invalid later guards hide Host 09 without restoring them.
Sole USE opens unchanged `L04-01` then `L04-02`; completed USE is read-only.
After both masteries, the current generic visual-analysis continuation remains
unchanged and Host 10 remains absent.

## Physical, layout, and access evidence

A candidate must prove every `FRCL-009-v1` relation plus normalized rectangles
for complete cavity family, each of at least three distinct openings, incoming
pressure continuity, reflected-light pair, no-optical-return cavity, dry rim,
wet exclusion, semantic target, label, and protected regions.

The complete essential relation must occupy a centered, crop-stable island:
family within normalized source `x=.24-.76`, `y=.20-.78`; incoming continuity
center within `x=.30-.46`, `y=.48-.68`; dry rim across lower-middle `y=.66-.88`;
target center within `x=.46-.58`, `y=.43-.61`. At least `95%` of the family,
all three cavity centers, the reflection pair, nonreturn cavity, pressure
continuity, and dry-rim path must survive desktop, laptop, narrow,
effective-200%, `320x180`, and `320x240` centered-cover layouts.

Native target is `>=44x44`; label is contained with `>=8px` focus/protected
separation; target has zero overlap with water/wet exclusion, cavity voids,
Host 08, return route, or Crown. Factual alt, forced-color focus, reduced-motion
parity, no horizontal overflow, and keyboard/pointer/touch equivalence are
required. Ambiguous mouths/faces/voices, submerged access, or answer cues are
objective rejection.

## Performance, validation, and rollback

Candidate is strict static RGB/RGBA fully opaque PNG, `1..30,000,000` bytes.
Accepted aggregate media `<=100,136,520`. Affected/global JS `<=1,705,500 /
1,730,000`, CSS `<=119,950`, modules `<=223`, maps `0`, no dependency change;
decode cold `<=300ms`, warm `<=120ms`, handler `<=2ms`, full E2E `<180s`.

Required ladder: authority/prompt/tool/key-boolean/path/ledger preflight;
focused Host08/Host09/Text Analysis/Speech tests; route/save/privacy/
accessibility/ending regressions; validators and full tests; production/TD-012
builds; minified PBA, exact media inventory, and served identity; isolated
external-QA-root E2E through Host 09 and MH-40; six-layout/keyboard/focus/
forced-color/reduced-motion review; exact cleanup, ports, ancestry, patch
integrity, and independent Intelligence release.

The E2E plan must inventory every initial launcher and re-entry control before
the first browser run. CSS must be measured after minification before the Image
Specialist commit.

Rollback removes only exact Host 09 additions and shell-owned scratch. Hosts
01-08/media, lessons, saves, route, world, ending, protected state, repository
QA, and opaque residuals are not targets.

## Science questions

Science must independently prove execution/dependency/custody viability;
five-prompt schema; exact physical/geometry/layout/access thresholds; dual
lesson ownership and privacy; null-first state; media/PBA budgets; validation;
Cloud product handling; cleanup; rollback; and external-QA-root enforcement.
It must issue READY, REVISE, or HOLD with zero live generation/media activity.

