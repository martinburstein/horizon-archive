# First Run Shell Variance Reissue - Geometry Evidence-Source Correction Only

Variance ID: `FRSH-003-v1-VR-46`

Disposition: **`FIRST RUN SHELL READY / GEOMETRY EVIDENCE-SOURCE CORRECTION
ONLY / FRSH-003-v1-VR-46`**

Stage gate remains: **`HOLD / PRODUCTION FUNCTIONAL NOT ISSUED`**

Release state remains: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / governing shell: `FRWO-003-v1` / `FRSH-003-v1`

Immediate return: `FRCE-003-v1-VR-45`

Mission source inspected:
`ccfe5157409adaffd8f45385c24e46a38433f1df`

Diagnostic-transport candidate:
`6c64eb354b7dbb467df5725e2cae4eb67092ddc7`

Transport report control:
`0ac9023037873004f7bd5d75c16f80953d770a4d`

Exact immutable product / validation / diagnostic / evidence identities:

```text
a91763e28d488f31f8cf7d40ece0b2682246ba9b
4cd7fbf31291671dd28c0743b44a7c49aaad82bb
2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
ca89a679195c11d441a76e6c02983a6436f2ccb2
```

Recorded: **2026-08-10**

## Context reuse and independent transport audit

Fresh-child capacity remains unavailable. Root expressly authorized this
Mission Captain context to perform the independent audit and issue the next
bounded shell. The reuse is disclosed, is not candidate evidence, and waives
no boundary.

Mission decoded the retained standard-base64 payload from the exact literal
VR-45 report without running E2E, preview, root, browser, build, test, product,
media, protected-path, or user-state action. The audit passed exactly:

```text
canonicalBytes=21154
failurePaths=114 unique=114 ordinalSorted=true
flattenedFailuresByLayoutMatchesPaths=true
browserClosed=true
```

The six-layout inventory is complete:

```text
desktop=0
laptop=0
narrow=57
effective-200=0
retained-320x180=57
retained-320x240=0
```

All `114` paths are geometry evidence. Owner counts are aggregate `6` and
geometry `108`. Category counts are top-level layout aggregate `4`, geometry
aggregate `4`, geometry drift `106`, and non-geometry `0`. Across both failing
layouts, drift coverage is exact:

```text
containing=8 frame=8 sceneArt=8
host04=14 label=14 labelText=14 physical=14 semantic=14
returnRidge=12
```

Value signatures are exact: `56` Boolean `false` values against expected
`true`, and `58` negative residual values against expected `0`. Each failing
layout contributes the same `57`-path shape: aggregate stability/pass cascade,
geometry aggregate cascade, then document equality, inverse-scroll, residual-
zero, and residual component failures across the same nine measured boxes.

There is no transported semantic identity, focus, input, gameplay, copy,
learning, privacy, save, route, later-rail, MH-40, null-delta, successor,
runtime-error, offline, request, media, PBA, or performance failure path.

## Classification and earliest lawful owner

The evidence does not support a product/layout repair. The product/content
candidate is unchanged; VR-42 proved exact production/fixture served identity;
the same immutable product passed the earlier accepted six-layout complete
journey; four current layouts are clean; and both failing layouts show one
whole-snapshot geometry cascade rather than isolated responsive constraints.

Mission classifies all `114` paths as **`EVIDENCE-CONTROL / POST-EPOCH
GEOMETRY SOURCE OR SCHEMA COLLAPSE / TWO LAYOUTS / NO PRODUCT FINDING`**.
Aggregate failures are downstream consequences, not separate causes. The
earliest lawful owner is **Combat Engineer**, limited to the complete-E2E
geometry evidence source in `playtest/e2e-playthrough.mjs` and its existing
static control. Tactical, product CSS/markup, Image, content, learning, and
route owners are not opened.

The VR-45 payload remains diagnostic-only, non-release, forbidden verifier
input, and no-retry evidence. Decoding it for Mission classification does not
authorize another E2E or convert it into verifier evidence.

## Exact bounded correction authority

Exact next owner is a **fresh Combat Engineer** if capacity permits; otherwise
context reuse must be disclosed. Combat may inspect and modify only:

- `playtest/e2e-playthrough.mjs`; and
- `horizon-archive-game/test/sixfoldWeir.test.js` without increasing the
  focused test count.

Correct only the earliest post-epoch geometry source/schema defect. The live
post snapshot for all nine required rectangles must come from the same
connected current DOM nodes and the same coordinate basis as the pre snapshot,
after the authorized focus sequence and layout settle. Narrow and retained
`320x180` must not receive missing/default/zero rectangle objects, stale epoch
objects, mismatched viewport sources, or post-teardown values.

If a required live rectangle/source is absent or disconnected, record one
explicit evidence-source failure at its true source. Do not manufacture zero
rectangles whose derived document/residual fields fan out into false product
geometry failures.

Do not hardcode either failing layout, any transported negative value, any box
dimension, or any expected outcome. Do not weaken/remove document equality,
inverse-scroll, lattice, residual-zero, stability, target-size, containment,
overlap, or viewport gates. Do not filter, deduplicate, reclassify, truncate,
or suppress failures. Do not change diagnostic transport, FRRC, summary,
verifier, layout order, viewport sizes, focus sequence, E2E count, product,
CSS, markup, runtime, fixture, media, or candidate identity.

Static control must prove, without browser execution, that post-epoch geometry
is sourced from live same-epoch nodes for all nine names; missing/disconnected
sources fail explicitly rather than defaulting to zero; and all existing gate
semantics remain exact. Keep the focused inventory at `68` tests.

## Exact validation and return

Run only:

1. `node --check playtest/e2e-playthrough.mjs`;
2. the exact FRRC-002 focused command, requiring `68/68` PASS; and
3. a scalar candidate-integrity proof limited to the two permitted files.

No E2E, preview, browser, fresh root, extractor, diagnostic decode, summary,
verifier, build, fixture build, related/full test, validator, PBA, served
identity, product/media/protected inspection, or cleanup action is authorized.

On success, commit/push only the two permitted evidence-control files and
return **`GEOMETRY EVIDENCE-SOURCE CORRECTION READY / NODE PASS / FOCUSED
68/68 / NO E2E / STOP / RETURN TO FRESH MISSION`**. On failure return
**`HOLD / GEOMETRY EVIDENCE-SOURCE CORRECTION OR STATIC FAILURE / NO E2E /
RETURN TO FRESH MISSION`**. Mission alone may later decide whether one fresh
diagnostic verification is lawful.

## Preserved boundaries

The cumulative VR-22 tests/validators, VR-30 production build, VR-35 fixture
build/corrected PBA, and VR-42 formal served identity remain accepted. No
product change, `PRODUCTION FUNCTIONAL`, release, or maturity advance is
authorized.

All seven OPEN divergences remain separate and OPEN: VR-17 protected-path
enumeration; VR-23 broad filename enumeration; VR-24 untracked pathname
enumeration; VR-25 and VR-28 literal control pathname output; VR-39 exact-port
listener ownership recovery; and VR-41 parser diagnostic scalar output. None
is waived, merged, closed, cured, or used as candidate evidence.

All frozen player, learning, privacy, save, accessibility, route, world,
MH-40, null-delta, `successor=null`, ending, media, diagnostic non-verifier,
and one-E2E meanings remain exact. No downstream stage, release, schedule,
automation, reveal, or `FIRST RUN COMPLETE` action is authorized.

Mission Captain signs **`FIRST RUN SHELL READY / GEOMETRY EVIDENCE-SOURCE
CORRECTION ONLY / FRSH-003-v1-VR-46`**.
