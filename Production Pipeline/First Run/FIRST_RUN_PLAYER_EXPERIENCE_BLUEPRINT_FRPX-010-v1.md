# FIRST RUN PLAYER EXPERIENCE BLUEPRINT / FRPX-010-v1

## Disposition

`PLAYER EXPERIENCE READY / ACCEPTED-ONLY NATIVE HOST 10`

Authorities: `FRSH-010-v1`, `FRDT-010-v1`, source release `FRAB-009-v2`.

## Current and target graph

Current: Host 09 mastery -> generic Visual Workloads launcher -> strict mastery
-> generic Extraction Workloads launcher -> strict mastery -> unchanged later
route.

Target before source selection is behaviorally identical. Target after source
enablement is Host 09 mastery -> available Host 10 -> USE opens Visual -> host
in progress/remediation -> USE resumes Visual -> after mastery USE opens
Extraction -> host in progress/remediation -> after both mastery host complete
and USE is read-only -> unchanged later route. The generic Visual and
Extraction launchers are absent whenever `source.enabled===true`, including
later decode or measurement failure; malformed enabled state fails closed.

## Registry and states

Combat creates one null-first frozen registry with source/provenance, hollow,
mount array, route array, phase surface, dry ring, semantic target, label,
protected regions, layouts, seven copy slots, and alt. `hidden` covers missing
predecessor mastery, source/provenance/measurement/decode failure, or malformed
evidence. A lawful source with no owned evidence is `available`; any valid
partial record is `in_progress`; either owned remediation marker is
`remediation_required`; both strict mastery records are `complete`.

## Native actions and focus

One native hotspot button owns LOOK/TALK/USE. LOOK announces only dry stone,
mounts, inert routes, shallow hollow, and blank surface. TALK announces exact
nonresponse. USE dispatches Visual first, then Extraction, and never dispatches
after complete. Pointer, Enter/Space, touch, and switch activation converge on
that button. A one-paint transition after predecessor mastery presents and
focuses it once. Terminal close, safe exit, remediation, primary completion,
mastery, reload, and route return restore focus to it when lawful. Escape closes
only the active Terminal layer.

## Responsive/accessibility contract

The image remains `cover 50% 50%`. The hotspot is derived from the selected
semantic source rectangle, clamped into the scene, and never smaller than
44x44 CSS px. Source rectangles project through the released six-layout utility;
relation retention >=.95, essential centers visible, contained target, physical
center inclusion, protected overlap zero, and stable label/focus separation are
mandatory. DOM source order remains scene art, native hotspot, status/Terminal.
Alt is truthful and noninterpretive. Forced colors supplies a visible border;
reduced motion removes transition only; effective-200% retains the same action.

## Copy placeholder ledger

Quartermaster fills exactly: `unseen`, `available`, `visualInProgress`,
`extractionInProgress`, `missed`, `mastered`, `returned`, `nextBoundary`, and
`alt`. Copy cannot name a picture, answer, correctness, response, authority, or
new observation.

## Acceptance matrix

Combat tests: null-first registry; exact four prompt identities; source and
provenance fail-close; >=3 mounts and >=2 routes; hollow/surface/ring containment;
derived six-layout evidence ignores caller attestations; source enablement
retires both legacy launchers; predecessor strict mastery; every state; Visual-
then-Extraction routing; complete read-only USE; LOOK/TALK write-free; decode
failure; focus/reload/Escape; native target; seven copy slots; immutable media
guard; and unchanged later route/MH-40. Quartermaster adds selected-byte,
measurement, browser, and private-decision evidence. Intelligence reruns the
complete release ladder.

No variance exists. Exact next owner: one Combat Engineer.

