# First Run Shell - Host 06 v8 Same-Run Helper Identity

Shell ID: `FRSH-005-v1-VR-37`

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / viability / predecessor: `FRWO-005-v8` /
`FRVE-005-v8-VR-03` / `FRCA-005-v7`

Disposition: **`FIRST RUN SHELL READY / RETAINED HELPER CLEANED BY EXACT
IDENTITY / SAME-RUN DLL IDENTITY FREEZE / COMPLETE CORRECTED SOURCES RETAINED /
CREDENTIAL-CLEARED SCIENCE PROOF REQUIRED / NO API OR A1 /
FRSH-005-v1-VR-37`**

Date: **2026-08-12**

Mission source inspected:
`e2de964fc7136b9e3cf3a922a2afa4b6bfd56fb2`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

First-run address: `FR-03 / Chapter II - Drowned Archive / Host 06`

Current / target maturity: unchanged current physical-host expression
`FR0 - 1 accepted shared compression / 1 exact / 10 missing`; this Science
subgate advances no maturity. The later bounded Work Order target remains one
accepted exact Host 06 source, subject to Quartermaster and Intelligence gates.

Exact next owner: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Mission decision and retained-object cleanup

Mission accepts `FRVE-005-v8-VR-03` as an exact fail-closed current-run DLL
identity variance. Exact helper source compilation is host/run dependent and
therefore cannot safely require a byte digest observed in a prior compile.
Source identity, compiled reflection/native surface, handle behavior,
one-link/non-reparse evidence, byte continuity, and identity-conditioned
cleanup remain mandatory.

Before changing the carrier, Mission inspected only the exact retained owned
v8 helper entries:

```text
root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-v8-5fbbd31e-8b50-4cb4-a0d3-c2f0d4b9e8aa
dll=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-v8-5fbbd31e-8b50-4cb4-a0d3-c2f0d4b9e8aa\Host06FileIdentity.dll
rootOrdinaryNonReparse=true
rootChildCount=1
rootSoleChild=Host06FileIdentity.dll
dllOrdinaryNonReparse=true
dllLength=4096
dllSha256=5fa8f9dfbb4c29dcb0469286ef27dc4008ed5342a6cb04b72c6a3545dbeb6566
```

Those exact predicates supplied cleanup authority for that retained object
only. Mission deleted the exact DLL, proved its file and directory names
absent, deleted the now-empty exact root nonrecursively, and proved its file
and directory names absent. No parent, sibling, glob, recursion, pattern,
alternate root, v7 path, residual, or protected entry was enumerated or
targeted.

## Corrected same-run DLL identity contract

The exact helper source remains immutable at `1,693 /
98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97`.
Compilation remains one exact `Add-Type -TypeDefinition` operation to the
literal v8 DLL, with warnings and diagnostics terminal and no source file.

The corrected carrier no longer compares compile output to a prior-instance
DLL digest. In the same child run it must:

1. require one ordinary non-reparse compile output at the exact DLL path,
   with byte length `1..1,048,576`;
2. read those bytes once, freeze their exact positive length and lowercase
   SHA-256 in memory, and require the `FileInfo` length to agree;
3. call only `Reflection.Assembly.Load(byte[])` on those same frozen bytes;
4. require exactly the public static `Read(SafeFileHandle) -> UInt64[]` and
   private static `GetFileInformationByHandle(SafeFileHandle, out
   BY_HANDLE_FILE_INFORMATION) -> Boolean` declared methods, with one
   `DllImport("kernel32.dll", SetLastError=true)` on the native method;
5. reopen the exact DLL exclusively, re-read its complete bytes through that
   open stream, and require reobserved length and SHA to equal the same-run
   frozen values before using its `SafeFileHandle`;
6. require the native result shape of five unsigned values, link count `1`,
   no reparse bit, and native size equal to the frozen same-run byte length;
7. only then mark helper identity accepted, delete the exact DLL, prove it
   absent, delete the exact empty root nonrecursively, and prove it absent;
   and
8. retain the same-run length/SHA only for accepted provenance if A1 later
   passes every production gate. It is never a cross-run expected digest.

Any source mismatch, compiler output/diagnostic, output count/path/type error,
empty/oversized/reparse DLL, load/reflection/PInvoke mismatch, byte
reobservation mismatch, native result/link/reparse/size mismatch, substitution,
or cleanup failure is terminal before credential access. If identity has not
been accepted, the carrier may not guess at cleanup. Science reports the exact
bounded HOLD for fresh Mission adjudication.

## Frozen complete corrected sources

The actual executable source content is retained in the repository, not only
described by a schema or digest. Exact strict-UTF-8/no-BOM identities are:

```text
builder=Production Pipeline/First Run/BUILD_HOST06_V8_A1_CARRIER.ps1
builder bytes=24785
builder SHA-256=3bf898f19fe47ddc5ea0095348de3d981a4215a84b6f29dcb80bfac3d3ee2dd6

carrier=Production Pipeline/First Run/HOST06_V8_A1_PRODUCTION_CARRIER.ps1
carrier bytes=34766
carrier SHA-256=9b1d315bc728299145d9a9582f7fd0da134403dd0a076254a8423a065da046a5

launcher=Production Pipeline/First Run/HOST06_V8_A1_LAUNCHER.ps1
launcher bytes=2637
launcher SHA-256=4eccc9abbed86b917dd987050b04340867aee023707aa063d0ecf13c6bf4bc1a

stdin parent=Production Pipeline/First Run/HOST06_V8_A1_STDIN_PARENT.ps1
parent bytes=2878
parent SHA-256=24e5dceeb7216e6571dcd30bbc74a4cb3389bb8f8d65feb50936afa7874b3f32

fixture controller=Production Pipeline/First Run/HOST06_V8_A1_STDIN_FIXTURE_CONTROLLER.ps1
controller bytes=2196
controller SHA-256=093232af889ea1a2cfc824c4898a0dafa10ea4532bf2ec1a8fddc3c4620dd804
```

All five sources parse with exact zero errors. The builder deterministically
retains the complete carrier content, produces the complete stdin launcher,
and freezes the carrier and launcher identities in the retained parent. The
launcher independently requires the complete carrier identity before parsing
or invocation. No carrier environment binding exists.

## Exact fresh Science gate

Fresh Science first proves the retained v8 helper root and DLL are absent and
that all other controlled paths are absent. It independently recomputes all
five source identities and parser-zero results, inspects the complete source
contract, and then performs exactly one credential-cleared fixture through the
retained controller, parent, launcher, and carrier.

The sole accepted result is one child reaching
`PT06_CREDENTIAL_GATE`, exit `87`, zero stdout, and the exact bounded
production diagnostic with every helper/live/active/product/provenance absence
true. It must prove exact same-run DLL byte/length/SHA freeze, byte-only load,
reflection/PInvoke/native behavior, later byte reobservation, identity-bound
cleanup, one child, credential value reads `0`, request constructions `0`,
`SendAsync` calls `0`, API sends `0`, A1 consumed `false`, and all controlled
paths absent afterward.

There is no correction, second fixture, retry, alternate transport, production
credential read, request, API call, media or pixel operation, product change,
test/E2E, or reveal in Science. PASS yields `POLISH VIABILITY READY` and
returns to one fresh Mission Captain for a later Quartermaster shell. Anything
nonexact is `HOLD`.

## Complete production boundary retained but not authorized

The sole future player-visible outcome remains the exact Host 05 fragment
handoff into one dry same-basin Stranded Lens Cradle and sole unchanged
`L02-03`, followed by the unchanged next Drowned boundary. Entry, active
states, completion, permitted exits, retry/return/resume, privacy, save,
offline, accessibility, performance, and hard stop remain exactly those of
`FRWO-005-v8` and the accepted v8 controls.

The exact one-send official ImagesResponse parser, endpoint, model, prompt,
request, response caps/diagnostics, A1-only budget, PNG/CRC/decode/physical/
crop/layout/accessibility/PBA/product/provenance gates, and no-reveal boundary
remain unchanged. This shell does not authorize Quartermaster, credential
access, request construction, `SendAsync`, API/media work, or A1 consumption.

Permitted changes in this Mission stage are only the five retained v8 source
files above, this shell, and synchronized handoff. Rollback removes this shell
and restores those sources and handoff to the preceding commit by explicit
content; it never resets the repository or touches media/product/protected
state.

Repository QA quarantine, protected PDF, training tree, Martin's real browser/
profile/save, accepted media, all v7 paths/residuals, opaque roots, VR-65,
hidden lore, and unrelated work remain inaccessible. The one-path rail, sole
unchanged `L02-03`, equal-dignity MH-40 outcomes, shared RP-012 ending, all
null deltas, and `successor=null` remain immutable. No branch, packet, lesson,
reward, access, identity, authority, world response, Machine/Builder dialogue,
hidden-lore answer, Host 07+, RP-013, successor, or post-ending content is
authorized.

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-005-v1-VR-37 /
SAME-RUN DLL IDENTITY AND EXACT CLEANUP / COMPLETE SOURCES RETAINED / FRESH
CREDENTIAL-CLEARED SCIENCE NEXT / A1 UNCONSUMED`**.
