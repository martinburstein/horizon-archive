# FIRST RUN WORK ORDER / FRWO-011-v1

## Disposition

`WORK ORDER READY / TEN PRIVATE HOST 11 ATTEMPTS`

Address: `FR-03 / Host 11 / Wall of Borrowed Light`. Predecessor:
`FRAB-010-v1`. Current physical inventory is one accepted compression plus six
exact encounters and five missing; target is one additional exact encounter
without promoting the address above `FR0`.

Execute at most ten sequential, one-output imagegen CLI attempts
`{H11-1..H11-10}`. The first complete technical, physical, responsive, and
accessibility PASS stops the ledger. Use `gpt-image-2`, `3840x2160`, high,
opaque PNG, `n=1`, no augmentation, reference, edit, variation, batch, force,
or internal retry. Science and Mission must freeze the exact prompt and CLI
identities before generation; attempts may vary composition wording only while
the continuity sentence remains exact.

Scratch root is the fresh initially absent owned OS-temp directory
`C:\Users\marti\AppData\Local\Temp\horizon-archive-host11-0a57b4f1-12d7-476a-aa76-845f6063b1dd`.
Outputs are exact `h11-1.png` through `h11-10.png`. Product path is
`Visual Direction/Production Masters/2026-08-12-first-run-host11/host11-wall-of-borrowed-light-master-v1.png`
plus `PROVENANCE.md`.

PASS requires an ordinary nonreparse opaque RGB PNG, exact `3840x2160`, <=30
MB, and strict decode. Physical review must prove one mineral receiving wall;
moving borrowed illumination crossing visible mineral film; at least three
embedded apertures; at least two visibly changed return angles; a broad,
continuous, dry foundation apron; and one off-axis service throat. The wall
must not emit its main light or show text, image, UI, glyph, answer, audience,
person, creature, portal, console, screen, or intelligent response.

Responsive proof derives actual `object-fit: cover` projection from measured
source rectangles at desktop, laptop, narrow, effective-200%, `320x180`, and
`320x240`. The complete relation must retain >=95%; every essential center and
the off-axis throat must remain visible; the interaction target must remain
contained and >=44 CSS px; label/focus/protected overlap must remain zero; and
real-browser corroboration must pass. No arbitrary absolute source band may
override an otherwise complete derived-layout PASS.

Rejected evidence records attempt, bytes/SHA, technical/physical booleans,
measured source rectangles, derived layout scalars, and bounded reasons, then
performs exact identity deletion and absence proof. Accepted-only integration
replaces the generic `L05-01 + L05-02` launchers with native LOOK/TALK/USE,
focus, recovery, decode, final copy/alt, provenance, and six-layout evidence.

Budgets: accepted media <=22 /139,046,406; JS <=1,750,000; CSS <=120,900;
modules <=231; maps 0. Focused, related, validators, full product, clean build,
served identity, isolated real-browser responsive, and one clean-start E2E are
required. Rejected media never enters the workspace.

Accepted Hosts 01-10 and all accepted media, lessons, evidence/privacy/save,
one route, RP-012, `successor=null`, protected QA, real browser/save, hidden
lore, opaque residuals, and later hosts are protected. Quartermaster alone may
make the frozen calls and import the first passing raster. Image Specialist is
runtime code/configuration only. Intelligence alone releases.

Exact next owner: one Office of Science Administrator.
