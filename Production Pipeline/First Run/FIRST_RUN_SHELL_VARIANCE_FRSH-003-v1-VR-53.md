# First Run Shell Variance Reissue - Corrected Four-Stage Complete Diagnostic-Control Verification

Variance ID: `FRSH-003-v1-VR-53`

Disposition: **`FIRST RUN SHELL READY / CORRECTED FOUR-STAGE COMPLETE
DIAGNOSTIC-CONTROL VERIFICATION / FRSH-003-v1-VR-53`**

Stage gate remains: **`HOLD / PRODUCTION FUNCTIONAL NOT ISSUED`**

Release state remains: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Stage / stable agent: Mission Captain / `mission_captain`

Mission source inspected:
`1c491a9471112d3d0b1eeef95aaebb7a62bec565`

Geometry correction / diagnostic transport / report lineage:

```text
8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97
6c64eb354b7dbb467df5725e2cae4eb67092ddc7
0ac9023037873004f7bd5d75c16f80953d770a4d
```

Exact immutable product / validation / diagnostic / evidence identities:

```text
a91763e28d488f31f8cf7d40ece0b2682246ba9b
4cd7fbf31291671dd28c0743b44a7c49aaad82bb
2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
ca89a679195c11d441a76e6c02983a6436f2ccb2
```

Recorded: **2026-08-10**

## Context reuse and VR-52 adjudication

Fresh-child capacity remains unavailable. Root expressly authorized this
Mission Captain context to adjudicate VR-52 and issue the next bounded shell.
The reuse is disclosed, is not candidate evidence, and waives no boundary.

Mission accepts VR-52 as **`DISPOSABLE SHA-256 CORRECTION CONTROL PASS / ROOT
DELETED / NO LIVE`**. Every root and identity/token predicate passed. Both
fresh `[Security.Cryptography.SHA256]::Create()` instances successfully ran
`ComputeHash([byte[]])`; both were disposed; uppercase and lowercase 64-hex
shapes passed; the recomputed lowercase digests were exactly equal; cleanup
attempted once and deleted the root; and native exit was 0.

No token, digest, path, PID, preview, browser, E2E, diagnostic, summary, or
verifier was emitted or retained. The evidence resolves the SHA operation for
future control use under the existing VR-50 OPEN correction. It is not a
candidate/product finding, does not close that recorded OPEN item, and does
not create an eleventh OPEN classification. Exactly one E2E budget remains.

## Cumulative accepted controls

Without rerun, Mission preserves VR-22 focused 68/related 74/full 972/
validator 40; VR-30 production build; VR-35 fixture build and corrected PBA
with JS `1667393`, CSS `119247`, media 17 / `37410731`, maps 0; VR-42 formal
served identity; VR-44 transport-control proof; VR-46 exact two-file live-DOM
epoch correction proof; VR-48 atomic root control; VR-51 predicate
localization; and VR-52 disposable SHA correction control.

VR-45's 114 geometry paths remain diagnostic evidence of the superseded
evidence-source epoch, not product/layout findings or verifier input.

## Exact corrected four-stage authority

Exact next owner is a **fresh Combat Engineer** if capacity permits; otherwise
context reuse must be disclosed. Execute four separate bounded PowerShell
calls in exact order. Never combine stages. Each call suppresses pathname,
body, object, exception, and diagnostic output except its ordered scalar and
the explicitly authorized safe base64 transports.

Copy completed-stage scalar values exactly into the next call. State may not
be recovered by directory/process/listener enumeration, wildcard search, or
inference. Any stage failure or tool-policy rejection exhausts this shell and
forbids reformulation/rerun. After Stage 1 succeeds, Stage 4 is independently
authorized and mandatory after every later outcome or rejection.

### Stage 1 - create root and emit corrected durable identity transport

In one small atomic root-only call:

1. Apply the exact accepted VR-48/VR-51 OS-temp normalization, lowercase GUID
   leaf, direct-parent, strict-containment, bidirectional repository-exclusion,
   predecessor-distinction, and nonexistence checks.
2. Create once with `New-Item -ItemType Directory -Path $candidate
   -ErrorAction Stop`; suppress its object. Same-call resolve and re-prove every
   legacy post-create predicate.
3. Strict-UTF-8 encode the normalized resolved identity; standard-base64
   encode/decode once; require byte-for-byte round trip.
4. Compute the digest twice using the exact VR-52 vector: a fresh disposable
   `[Security.Cryptography.SHA256]::Create()` for each
   `ComputeHash([byte[]])`, disposal in `finally`, uppercase
   `[BitConverter]` 64-hex shape, `ToLowerInvariant()` 64-hex shape, and exact
   ordinal recompute equality. Do not use static SHA, `HashData`,
   `Get-FileHash`, pipeline, external executable, or inferred overload.
5. Only after all predicates pass, retain the root and emit its standard
   base64 token plus the first lowercase digest. Emit no raw path or GUID.

Emit exactly:

```text
stage=1 rootPredicatesPass=<0|1> identityTokenPredicatesPass=<0|1|NA> sha1Created=<0|1|NA> sha1Computed=<0|1|NA> sha1Disposed=<0|1|NA> sha1HexShapesPass=<0|1|NA> sha2Created=<0|1|NA> sha2Computed=<0|1|NA> sha2Disposed=<0|1|NA> sha2HexShapesPass=<0|1|NA> digestRecomputeMatch=<0|1|NA> rootRetained=<0|1> rootTokenB64=<base64-or-empty> rootTokenSha256=<64-lowercase-hex-or-empty> failureRootCleanupAttempt=<0|1> failureRootDeleted=<0|1> stagePass=<0|1> nativeExit=<0|1>
```

Exact PASS requires every reached predicate 1, both SHA objects disposed,
nonempty canonical token/digest, retained root 1, failure cleanup 0/0, stage
pass 1, and native exit 0. On failure after lawful creation, exact containment-
safe empty-root cleanup runs in `finally`; emit empty token/digest and stop.
No port or live action is permitted in Stage 1.

### Stage 2 - verify transport, own, and ready previews

Accept the exact Stage 1 token/digest as literal inputs. Decode strict base64/
UTF-8, reject NUL/control characters, normalize once, and recompute the token
digest using one fresh disposable VR-52 SHA instance with explicit
`ComputeHash([byte[]])`, disposal, and lowercase 64-hex shape. Require exact
digest equality and re-prove root existence, leaf, parent, strict temp
containment, repository exclusion, and predecessor distinction.

Require ports 4173/4184 clear. Launch production/fixture once each using the
previously proven hidden direct Node/Vite `PassThru` forms. Require positive
returned PIDs, exact assigned-port ownership without recovery discovery, and
bounded suppressed root readiness HTTP 200/200.

Emit exactly:

```text
stage=2 rootTokenMatch=<0|1> tokenShaDisposed=<0|1|NA> prePortsClearCount=<0..2> prodLaunchAttempts=<0|1> fixtureLaunchAttempts=<0|1> prodPid=<positive-integer-or-0> fixturePid=<positive-integer-or-0> ownedListenerCount=<0..2> readiness200Count=<0..2> inCallFailurePidCleanupCount=<0..2> stagePass=<0|1> nativeExit=<0|1>
```

Exact PASS requires token match/disposal 1/1, clear ports 2, launches 1/1, two
positive owned PIDs/listeners, readiness 2, failure cleanup 0, stage pass 1,
and native exit 0. On partial failure, stop only locally returned owned PIDs,
report them and the cleanup count, then proceed only to Stage 4. Do not launch
a replacement or run E2E/build/test/PBA/served identity.

### Stage 3 - spend exactly one E2E and take one evidence branch

Accept the Stage 1 token/digest and Stage 2 PIDs literally. Repeat corrected
disposable token-hash verification and full root containment proof. Require
both transported positive PIDs live and still the exact assigned-port owners;
require one bounded root HTTP 200 readiness check per preview. Do not launch or
replace a process.

Only after every precondition passes, invoke exactly once:

```text
node playtest/e2e-playthrough.mjs
```

Use the exact FRRC-002 environment contract, retained QA root, frozen PBA
flags, immutable product `a91763e28d488f31f8cf7d40ece0b2682246ba9b`,
validation `4cd7fbf31291671dd28c0743b44a7c49aaad82bb`, diagnostic/probe
`2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc`, geometry correction
`8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97`, and transport candidate
`6c64eb354b7dbb467df5725e2cae4eb67092ddc7`. Apply the existing 180000 ms
timeout. Do not pipe, truncate, duplicate, or asynchronously defer evidence.

Synchronously read exactly one diagnostic while the root exists. Capture
`checkInventoryExact`, `failureCount`, and `browserClosed`.

If E2E exit is nonzero, inventory is not exact, failure count is positive, or
browser closed is not true: emit no summary; invoke the accepted diagnostic
transport exactly once with 60000 ms timeout; capture the full canonical
compact sorted `failurePaths` and `failuresByLayout` safe base64 plus lifecycle
scalars before cleanup; and invoke no verifier.

Only on E2E exit 0, inventory exact, failure 0, and browser closed may Combat
emit exactly one complete summary and invoke exactly one verifier. The summary
must cover all six layouts, full rail, both MH-40 outcomes, null-delta,
runtime/offline/performance, and every frozen accessibility/save/route/ending
meaning. Exact success requires verifier PASS. Diagnostic evidence is
forbidden verifier input.

Emit exactly one scalar plus the authorized failure transport or success
summary:

```text
stage=3 rootTokenMatch=<0|1> tokenShaDisposed=<0|1|NA> ownershipPass=<0|1> readiness200Count=<0..2> e2eAttemptCount=<0|1> e2eExit=<integer-or-NA> checkInventoryExact=<0|1|NA> failureCount=<integer-or-NA> browserClosed=<0|1|NA> transportAttemptCount=<0|1> transportExit=<integer-or-NA> summaryCount=<0|1> verifierAttemptCount=<0|1> verifierPassCount=<0|1> stageOutcome=<PRECONDITION_HOLD|DIAGNOSTIC_HOLD|SUCCESS> nativeExit=<0|1>
```

If E2E begins or returns, the one remaining budget is consumed regardless of
result. No retry, repair, alternate subset, second browser, second diagnostic,
extra transport, second summary, or second verifier is authorized.

### Stage 4 - mandatory independent exact cleanup

Run exactly once after every successful Stage 1, regardless of later success,
failure, or tool rejection. Accept the token/digest and any Stage 2 numeric
PIDs literally. Recompute/compare the digest with the corrected disposable SHA
vector and re-prove the root. Treat only transported positive PIDs as owned;
do not discover or substitute ownership.

Stop each transported owned PID once if live. Require ports 4173/4184 clear
without stopping an unowned listener. Revalidate exact identity, leaf, parent,
strict temp containment, repository exclusion, and predecessor distinction;
delete only that empty retained root once and require absence. Never target an
alternate, inferred, unresolved, repository, predecessor, wildcard, or
enumerated path.

Emit exactly:

```text
stage=4 rootTokenMatch=<0|1> tokenShaDisposed=<0|1|NA> ownedPidCount=<0..2> pidStopAttemptCount=<0..2> ownedPidsStoppedOrAbsentCount=<0..2> portsClearCount=<0..2> rootCleanupAttemptCount=<0|1> rootDeleted=<0|1> cleanupPass=<0|1> nativeExit=<0|1>
```

Exact cleanup requires token match/disposal 1/1, every owned PID stopped or
absent, ports clear 2, root cleanup/deletion 1/1, cleanup pass 1, native exit
0. Cleanup failure remains HOLD; return scalars and the still-valid token/
digest to Mission. No alternate cleanup or retry is authorized.

## Return dispositions and preserved boundaries

Exact success requires all four stages PASS, E2E attempt 1/exit 0, diagnostic
inventory exact/failure 0/browser closed, summary 1, verifier attempt/PASS 1,
and exact cleanup. Return **`ONE COMPLETE CORRECTED E2E PASS / DIAGNOSTIC
INVENTORY EXACT / FAILURE 0 / BROWSER CLOSED / SUMMARY 1 / VERIFIER 1 PASS /
EXACT CLEANUP / STOP / RETURN TO FRESH MISSION`**.

Diagnostic failure returns **`HOLD / DIAGNOSTIC FAILURE / FULL TRANSPORT
CAPTURED / NO VERIFIER / NO RERUN / EXACT CLEANUP RESULT / RETURN TO FRESH
MISSION`**. Pre-E2E failure/rejection returns **`HOLD / EXECUTION CONTROL / NO
E2E / NO RERUN / EXACT CLEANUP RESULT / RETURN TO FRESH MISSION`**.

All ten OPEN classifications remain separate and unchanged: VR-17 protected-
path enumeration; VR-23 broad filename enumeration; VR-24 untracked pathname
enumeration; VR-25 and VR-28 literal control-pathname output; VR-39 exact-port
ownership recovery; VR-41 parser diagnostic scalar output; VR-46 validation
command selection; VR-47 pre-root scalar transport; and VR-50 Stage-1 post-
create root proof. VR-49 remains non-executed/non-evidence; VR-51/VR-52 are
localization/correction evidence under VR-50. None is waived, merged, closed,
cured, or admitted as candidate evidence.

Diagnostic evidence remains non-release, forbidden verifier input, and no-
retry. Every frozen product, player, learning, privacy, save, accessibility,
route, world, MH-40, null-delta, `successor=null`, ending, media, and one-E2E
meaning remains exact.

No product/content/media/protected change, build, test, validator, PBA,
served-identity rerun, downstream stage, maturity advance, release, schedule,
automation, or reveal is authorized. Combat may not declare `PRODUCTION
FUNCTIONAL`.

Mission Captain signs **`FIRST RUN SHELL READY / CORRECTED FOUR-STAGE COMPLETE
DIAGNOSTIC-CONTROL VERIFICATION / FRSH-003-v1-VR-53`**.
