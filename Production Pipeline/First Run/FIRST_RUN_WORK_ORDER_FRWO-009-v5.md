# Horizon Archive First Run Work Order - Host 09 High-Aim Compensation Pass

Work Order ID: `FRWO-009-v5`

Stage / owner: Operations Planning Major / `operations_planning_major`

Disposition: **`WORK ORDER READY / FIVE FRESH HIGH-AIM ATTEMPTS`**

Predecessor closeout: `FRWO-009-v4` / `FRCA-009-v2`

Martin explicitly authorized five additional attempts. Earliest-first order
keeps the pass on Host 09. The new ledger is exactly `{H9-11..H9-15}`; all
prior candidates remain deleted and forbidden as references or inputs.

## Evidence-backed correction

The prior compact prompts reliably reduced subject size but the generated
island landed below their authored vertical target. This pass compensates for
that observed downward placement: prompts aim the small relation deliberately
high in the source, approximately center `y=.34..42`, so the generated output
can land near the unchanged runtime acceptance center `y=.43..61`.

The runtime acceptance gate is not weakened. A selected family must remain
within source `x=.24..76`, `y=.20..78`, center `x=.46..58`, `y=.43..61`, retain
`>=95%` plus every essential center across all six layouts, and satisfy every
physical/accessibility predicate from `FRVE-009-v1`. The new prompt target is
only a generator-bias compensation strategy.

## Attempt and product boundary

Mission freezes five independent ASCII prompts, a fresh absent GUID scratch
root, and literal `h9-11.png` through `h9-15.png`. Calls use the same approved
CLI/model/options, sequential one-output execution, first-PASS stop, nonzero
stop, exact identity-bound rejection cleanup, Cloud-safe accepted copy, and
no-reveal controls. No input/reference/edit/mask/variation/batch/internal
retry exists. There is no H9-16.

Accepted-only integration, PBA, accessibility, tests/builds/served identity,
external-QA-root E2E, Image presentation, Intelligence release, rollback,
protected state, canon, RP-012, and `successor=null` remain unchanged. Accepted
media stays `19 / 70,136,520`; product/provenance are absent; runtime is inert.

Operations signs **`WORK ORDER READY / FRWO-009-v5 / ROUTE FRESH SCIENCE`**.
