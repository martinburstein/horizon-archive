# Horizon Archive First Run Polish Viability Variance

Variance ID: `FRVE-005-v7-VR-02`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`HOLD / DIRECT PS5.1 FIXTURE BODY DID NOT EXECUTE / INLINE
TRANSPORT AND PARSE FAILURES LEAVE NO NATIVE-IDENTITY RESULT`**

Date: **2026-08-11**

Science source inspected:
`2f6279e1a2b1ce55cec685ea60fba90f106d9e79`

Planning controls: `FRRM-005-v7` / `FRSB-005-v7`

Prior Science envelope / variance: `FRVE-005-v7` /
`FRVE-005-v7-VR-01`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Science decision

Science read the active intake, workflow, registry, complete Science profile,
current handoff, complete `FRWO-005-v7`, complete `FRVE-005-v7`, complete
`FRVE-005-v7-VR-01`, and the exact inherited v6 fixture clauses needed for
this return. Starting `HEAD` was exactly
`2f6279e1a2b1ce55cec685ea60fba90f106d9e79`.

The current host directly confirmed Windows PowerShell `5.1.26100.8875` in a
64-bit process. No nested PowerShell or other child process was launched.
Science submitted the fixture inline to that direct host. The first bounded
submission was rejected by the command transport with Windows `Access is
denied` before PowerShell emitted output. A smaller direct-host form reached
the PowerShell parser, but parser errors rejected the complete command before
any statement executed. The parser diagnostics named only malformed compact
fixture syntax; they do not establish a helper defect or any fixture result.

Therefore no admissible evidence exists for `Add-Type` compilation, emitted
DLL byte length/SHA, byte-only load, reflection surface, x64 layout, native
return schema, exclusive handle identity, hard-link count, invalid/closed/
native-false failure behavior, reparse/substitution rejection, reopen and
pre/post-move identity, destination race, identity-conditioned deletion, or
fixture cleanup. No helper or disposable fixture root was allocated.

Exact post-attempt checks proved the fixed helper root, exact helper DLL, and
live API root absent. Because the fixture body never executed, absence is a
precondition/postcondition fact only and cannot be promoted into fixture
cleanup evidence.

`FRWO-005-v7` requires the complete native fixture before `POLISH VIABILITY
READY`. Science consequently issues **`HOLD`**. This remains an evidence-
availability HOLD, not a finding that the frozen helper source is defective.

Variance classification: **`REQUIRED CORRECTION / ONE FRESH SCIENCE CONTEXT
MUST RUN A PARSE-VALID EXACT FIXTURE DIRECTLY IN PS5.1 AND PROVE THE COMPLETE
RESULT AND CLEANUP`**.

## Preserved evidence and boundaries

The frozen helper source remains exact `1,693` UTF-8-without-BOM LF bytes with
lowercase SHA-256
`98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97`.
No DLL identity may be inferred or frozen from this invocation.

`FRVE-005-v6` remains accepted prior evidence for the unchanged direct API,
TLS, strict parser, credential boundary, `CreateNew`, `Flush(true)`, two-
argument no-replace move, and exact-path cleanup predicates. This interrupted
v7 return neither reran, contradicted, nor promoted those predicates. Direct
Image API calls remain exact `0`; no credential value or derivative, request,
response, network connection, generation, media operation, live-root
allocation, or pixel inspection occurred.

Ordinal `1` remains permanently consumed and opaque; ordinals `2` and `3`
remain unconsumed and unavailable. Conservative managed-residual count stays
exact `1`, associated only with ordinal `1`; VR-65 remains separate and
inaccessible. `FRAM-001-v1`, the frozen code/tree identities, Host 05 -> Host
06 -> unchanged `L02-03`, all technical/physical/accessibility/privacy/save/
offline/performance/no-reveal/rollback gates, the one-path canon, RP-012,
`successor=null`, maturity, inventory, and every OPEN record remain unchanged.

No product/runtime/test/media/control change, E2E, reveal, maturity advance,
OPEN-record closure, residual access, schedule, automation, push, release, or
`FIRST RUN COMPLETE` occurred.

## Exact fresh Science handoff

One fresh Office of Science Administrator must run the complete
`FRWO-005-v7` fixture directly in Windows PowerShell 5.1 without launching a
nested process. It must use only the fixed helper root and one fresh disposable
GUID OS-temp non-media fixture; freeze the DLL byte length/lowercase SHA; prove
the exact source, load, surface, layout, native snapshots, required failure,
hard-link, reparse, substitution, move-race, identity-conditioned cleanup, and
root-absence predicates; preserve exact zero API sends; and issue one new
versioned `POLISH VIABILITY READY`, `REVISE`, or `HOLD` artifact.

No Mission follows this artifact.

Office of Science Administrator signs **`HOLD / FRVE-005-v7-VR-02`** from
exact source `2f6279e1a2b1ce55cec685ea60fba90f106d9e79`.
