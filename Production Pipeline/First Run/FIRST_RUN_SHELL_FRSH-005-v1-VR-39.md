# First Run Shell - Host 06 v8 Sole Quartermaster A1

Shell ID: `FRSH-005-v1-VR-39`

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / viability / predecessor: `FRWO-005-v8` /
`FRVE-005-v8-VR-05` / `FRCA-005-v7`

Disposition: **`FIRST RUN SHELL READY / SOLE FRESH QUARTERMASTER A1 /
EXACT RETAINED STDIN SOURCES / ONE SEND / NO RETRY OR A2 / PRIVATE REVIEW /
BYTE-IDENTICAL ACCEPTED IMPORT OR EXACT CLEANUP / FRSH-005-v1-VR-39`**

Date: **2026-08-12**

Mission source inspected:
`030e547a31d9ebd737793b0453c5f3fe4105c2eb`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

First-run address: `FR-03 / Chapter II - Drowned Archive / Host 06`

Current / target maturity: current physical-host expression remains
`FR0 - 1 accepted shared compression / 1 exact / 10 missing`; Quartermaster
may establish `PRODUCTION CONTENT COMPLETE` evidence for one exact Host 06
source, but only Intelligence may advance the maturity record.

Exact next owner: one fresh Quartermaster / `quartermaster`

## Mission decision

Mission accepts `FRVE-005-v8-VR-05`. The sole credential-cleared fixture
proved exact stdin delivery, parent/launcher/carrier parsing, helper source,
same-run compiler-output identity, byte-only load, reflection/PInvoke/native
surface, exclusive byte reobservation, one-link/non-reparse/size identity,
exact helper cleanup, one child, and fail-closed PT06 behavior. It consumed no
credential value, request, send, API call, media, product state, or A1.

Mission now authorizes one fresh Quartermaster production pass through only
the retained stdin parent. This is the sole v8 paid-attempt domain `{A1}`.
There is no retry, A2, redirect, alternate endpoint/model/request/transport,
SDK, CLI, Python path, browser generation, edit, variation, input image,
second output, or fallback. Any nonexact condition closes v8.

## Frozen executable source and invocation

Quartermaster independently verifies strict UTF-8/no BOM, exact byte length,
SHA-256, and parser-zero for all five retained sources before invocation:

```text
builder 24897 / d0213fca3a1b61b086d6f82e2f218416292d7362fd0a2871e07d584f161923af
carrier 34766 / 9b1d315bc728299145d9a9582f7fd0da134403dd0a076254a8423a065da046a5
launcher 2637 / 4eccc9abbed86b917dd987050b04340867aee023707aa063d0ecf13c6bf4bc1a
stdin parent 2878 / 6b89f33bbe82595fc142e00106552096ff1f53033748df5d54742bc7fba6580b
fixture controller 2196 / 093232af889ea1a2cfc824c4898a0dafa10ea4532bf2ec1a8fddc3c4620dd804
parser errors=0 for all five
```

Production invokes exactly once:

```text
C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe
-NoLogo -NoProfile -NonInteractive -File
Production Pipeline/First Run/HOST06_V8_A1_STDIN_PARENT.ps1
```

The retained parent alone reads the exact carrier and launcher files, verifies
their identities, starts one child, and transfers the complete carrier through
one closed redirected-standard-input stream. Quartermaster may not hand-edit,
rebuild, inline, reinterpret, bypass, or invoke the launcher/carrier directly.
The fixture controller is verification evidence only and is not production.

## Literal controlled paths and preflight

Before invocation, Quartermaster proves these literal exact roots and every
named child absent without parent enumeration:

```text
helper root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-native-identity-v8-5fbbd31e-8b50-4cb4-a0d3-c2f0d4b9e8aa
helper DLL=<helper root>\Host06FileIdentity.dll
live root=C:\Users\marti\AppData\Local\Temp\horizon-archive-host06-api-v8-3f7d8a21-76c5-4d4e-9641-b2f5e73a019c
stage=<live root>\.attempt-A1-95a70af8-f9d0-49dc-87dc-89675212ed35.stage
target=<live root>\attempt-A1.png
decision=<live root>\.attempt-A1.review-v1
product root=C:\Users\marti\OneDrive\Desktop\Horizon Archive\Visual Direction\Production Masters\2026-08-10-first-run-host06
product raster=<product root>\host06-stranded-lens-cradle-master-v1.png
product provenance=<product root>\PROVENANCE.md
```

The obsolete carrier environment binding must be absent. Any pre-existing
controlled path, reparse point, alias, unexpected child, product destination,
or identity uncertainty is terminal before credential access and A1.

## Helper, credential, request, and A1 contract

The exact helper remains `1,693 /
98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97`.
The VR-38/VR-37 same-run compiler-output length/SHA freeze, byte-only
`Assembly.Load(byte[])`, exact two-method reflection/DllImport/native surface,
exclusive full-byte reobservation, one-link/non-reparse/size identity, and
exact DLL-then-empty-root cleanup are immutable. Helper cleanup completes
before credential access or live-root creation.

Credential authority is process-environment-only `OPENAI_API_KEY`. The child
may observe only boolean nonblank presence before using the value in memory as
the Bearer credential. It may not print, persist, hash, measure, classify,
return, or expose the value. Absent/blank credential stops at PT06 and consumes
no A1.

The sole request is one non-streaming `POST` to
`https://api.openai.com/v1/images/generations` with exact retained
`HOST06-GEN-PROMPT-v1`, `model=gpt-image-2`, `n=1`, `size=3840x2160`,
`quality=high`, `background=opaque`, and `output_format=png`.
`AllowAutoRedirect=false`, `ResponseHeadersRead`, exact ten-minute timeout,
one request body, and one `SendAsync` are mandatory. A1 begins only at that
sole authoritative `SendAsync`. Once begun it is consumed regardless of
result. No relaunch is allowed.

## Official response and bounded diagnostic gate

The complete response is bounded to `1..16,500,000` bytes. Success requires
status `200` and normalized media type `application/json`. The exact strict
official `ImagesResponse` parser admits only the frozen top-level fields,
requires nonnegative integral `created`, exactly one `data` item containing
only canonical `b64_json`, validates optional echoed strings and bounded
nonnegative integral usage shapes, rejects duplicate/unknown/trailing/
malformed content, and bounds canonical Base64 to `4..16,000,000` characters
and decoded bytes to `1..12,000,000`.

Every completed response retains only numeric status, normalized media type or
`absent`, bounded byte-count state, stable terminal gate, `sendStarted`, and
the frozen sanitized at-most-1,024-byte non-success diagnostic projection or
`diagnostic-unavailable`. No header, Authorization value, request JSON,
response body/JSON, Base64, generated bytes, exception/stack, account/project
identifier, or hidden metadata enters a ledger or provenance. All response,
diagnostic, and payload references are cleared after bounded use.

## Materialization and private technical review

Decoded bytes are written once with `FileMode.CreateNew`, one bounded write,
`Flush(true)`, close, exclusive reopen, handle identity checks, and a
same-directory atomic no-replace move from exact stage to exact target. Every
write/reopen/pre-move/post-move snapshot must preserve the same volume/file
index, link count `1`, no reparse bit, exact size, and SHA-256.

Before any aesthetic/physical judgment, Quartermaster privately verifies PNG
signature; bounded chunk lengths; exact CRCs; one IHDR; exact `3840x2160`;
opaque sRGB/no alpha; permitted chunks only; bounded inflate; independent
successful decode; absence of text, watermark, disallowed metadata, malformed
or trailing data; and exact file identity. A technical failure is terminal
and performs identity-conditioned exact cleanup without reveal.

Quartermaster may inspect only the exact owned A1 target privately. It must not
display, attach, publish, reveal, or place rejected/candidate media in the
workspace or repository. Review requires every frozen `PHY-01..12` predicate:
one weathered lens within one tilted conformal cradle; dry reachable
same-basin placement; at least two load/stress contacts; at least two continuous
drainage seams; Host 05 fragment continuity without entering activation; a
restrained horizon/reflected-horizon catch; physical material/process
legibility; no live-water intersection; and clear distinction from Host 05,
a return-like ridge, Crown, distant Tidal Lens, and any second lens/cradle.
No person, ship, dialogue, glyph, interface, reward, activation, recognition,
authority, world response, prior-human trace, text, or watermark may appear.

The frozen six-layout crop/mapping/hotspot/label/center/nonoverlap checks and
keyboard, pointer, touch, focus, reflow, effective-`200%`, forced-color,
reduced-motion, accessibility naming, source-retention, PBA, offline,
performance, privacy/save, and no-cross-credit predicates must all pass.

The decision file is created once at the literal decision path, bounded to the
retained grammar and exact A1 identity. ACCEPT is permitted only with
`technical=true`, `physical=true`, `layouts=true`,
`accessibility=true`, and `codes=NONE`. Otherwise Quartermaster writes one
truthful REJECT with only permitted objective codes. Any missing, malformed,
late, replaced, reparse, oversized, or contradictory decision is terminal.

## Acceptance, rejection, provenance, and cleanup

REJECT gives the candidate zero authority. The carrier identity-checks and
deletes only its exact target, decision, and owned empty live root. No product,
registry, copy, alt text, maturity, or reuse results. V8 closes; there is no
A2 or retry.

ACCEPT copies the exact selected target bytes with create-new semantics to the
sole product raster, verifies byte length/SHA identity, and creates the sole
`PROVENANCE.md`. Provenance contains only Work Order/shell, exact authorized
endpoint/model/options/prompt ID, A1, accepted byte identity, same-run helper
length/SHA, technical/physical/layout/accessibility PASS scalars, immutable
manifest identity, product path, and cleanup completion. It contains no key,
headers, request/response body, Base64, rejected identity/pixels, diagnostic,
exception, account/project identity, or hidden metadata.

After import, the carrier deletes only the identity-proved A1 target and empty
live root and proves helper/live cleanup complete. Product rollback after a
later required validation failure is limited to the exact newly owned raster,
provenance, and newly owned empty product root, each identity-conditioned and
nonrecursive. It may not replace pre-existing state or touch accepted media.

## Validation, completion, and routing

After accepted import, Quartermaster populates only the authorized Host 06
registry/provenance/measurement/copy/alt scalars required by the frozen v8
contract and runs the applicable focused tests, full product tests, clean
production and fixture builds, PBA, served identity, one bounded isolated E2E,
desktop/narrow/effective-`200%`/keyboard/reduced-motion/forced-color/focus/
containment/runtime-error review, and cleanup/process-shutdown gates. It never
uses Martin's real browser profile/save.

All required gates passing yields one `PRODUCTION CONTENT COMPLETE` ledger,
dedicated commit, push, `HEAD == origin/main`, synchronized handoff, and one
fresh Image Specialist for runtime code/configuration presentation only. A
failure yields exact HOLD and earliest-owner routing; no hidden retry or source
substitution exists.

## Immutable canon and protected boundaries

The player path remains Host 05 mastery and fragment handoff into one dry
same-basin Stranded Lens Cradle, sole unchanged `L02-03`, then the unchanged
next Drowned boundary. LOOK/TALK remain write-free; completed USE remains
read-only; remediation stays answer-free; retry/return/resume/save/privacy/
offline/Demo Tour/accessibility behavior remains unchanged.

The one-path rail, equal-dignity MH-40 outcomes, shared RP-012 ending, all null
deltas, and `successor=null` are immutable. No branch, packet, lesson, reward,
access, identity, authority, world response, Machine/Builder dialogue,
hidden-lore answer, Host 07+, RP-013, successor, or post-ending content exists.

Repository QA quarantine, protected PDF, training tree, Martin's real browser/
profile/save, immutable accepted-media bytes/pixels, all v7 paths/ordinals/
residuals, opaque roots, VR-65, hidden lore, and unrelated work remain
inaccessible. No cycle reveal is authorized.

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-005-v1-VR-39 / SOLE
QUARTERMASTER A1 / ONE SEND / PRIVATE REVIEW / BYTE-IDENTICAL ACCEPTED IMPORT
OR EXACT CLEANUP / NO RETRY OR A2`**.
