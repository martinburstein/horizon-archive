# FIRST RUN SHELL / FRSH-010-v1

## Disposition

`FIRST RUN SHELL READY / FOUR PRIVATE HOST 10 ATTEMPTS / FIRST PASS STOPS`

Source release is `FRAB-009-v2` at
`c8f85284c8d6df8c50ebdb358da8e0a1f42d011f`. Planning authorities are
`FRPB-001-v8`, `FRCL-010-v1`, `FRWO-010-v1`, and `FRVE-010-v1`. This shell
owns only `FR-03 / Host 10` and the existing `L04-03 + L04-04` entry.

## Fixed player-visible outcome

The released Host 09 route reaches one distinct shallow optic hollow on a
continuous dry annular route. The hollow visibly contains at least three
mounting points, at least two inert fluid routes, and one blank matte phase
surface. It never shows a picture, projection, view, portal, camera feed,
answer, active response, or world change.

LOOK is physical and write-free. TALK is exact nonresponse. USE enters only
the first incomplete existing owned lesson. Completion is read-only and the
same relationship remains blind. Later route, Crown, Witness, City, RP-012,
equal outcomes, and `successor=null` remain unchanged.

## Exact generation ledger and custody

The only permitted attempts are sequential `{H10-1,H10-2,H10-3,H10-4}`.
The first PASS ends the ledger; later calls remain unused. Each call uses the
bundled CLI at exact `35266` bytes / SHA-256
`c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05`
with `generate`, its exact prompt file, `gpt-image-2`, `3840x2160`, `high`,
opaque `png`, the exact output path, and `--no-augment`. No force, batch,
reference, edit, variation, internal retry, or alternate transport exists.

Prompt identities are:

- H10-1 `1459` / `c3ffdbf4626d6fc50ab0e63fbdff7659e5b7746d8dcbff80e2d1134d7bf02537`
- H10-2 `1311` / `a4089e93fc16fe2f2801e9469a802847c55c8af3481d7614822e256e6af9d33f`
- H10-3 `1315` / `e3532eb1eca9d0bc927bf6b41fbe0db7f29d7c43482ac8073a97775c1d95cc98`
- H10-4 `1259` / `cfd21a6ef66e11724b2e3d5c9d41f6dc8eec77af977954d6bd272ee3c120740a`

The sole scratch root is the initially absent literal
`C:\Users\marti\AppData\Local\Temp\horizon-archive-host10-cli-e5755a03-bdcf-48db-b867-65503fda4843`.
Its only permitted generated children are `h10-1.png` through `h10-4.png`.
Quartermaster privately reviews only the exact ordinary, nonreparse output
after technical identity proof. A rejected output receives a bounded scalar
record, is re-identified, deleted exactly, and proved absent before the next
call. No rejected pixel, derivative, reference, reveal, or workspace copy is
permitted. Exact unexpected state or cleanup failure stops the ledger opaque.

At most one PASS may be copied byte-identically to
`Visual Direction/Production Masters/2026-08-12-first-run-host10/host10-blind-camera-well-master-v1.png`
with `PROVENANCE.md`. Product import is selection, not release.

## Objective acceptance

Technical PASS requires one ordinary nonreparse opaque RGB PNG, exact
`3840x2160`, strict PNG/decode validity, and `1..30000000` bytes. Physical PASS
requires the complete shallow hollow, three mounts, two inert routes, blank
matte phase surface, and continuous dry ring to be unambiguous together, with
none of the continuity-lock rejection conditions.

Quartermaster records conservative source rectangles for the complete
relation, mounts, routes, phase surface, dry ring, semantic target, label, and
protected features. The released projection utility must derive all six
layouts—desktop, laptop, narrow, effective-200%, 320x180, and 320x240—with
relation retention >=.95, every essential center visible, one contained native
target >=44 CSS px, and protected overlap zero. Isolated browser evidence must
corroborate the same derived result. A physical PASS with a responsive FAIL is
still REJECT. First total PASS stops generation.

## Production ownership

- Recon owns dramatic reading order and composition within the fixed relation.
- Tactical owns exact hidden/available/in-progress/remediation/complete flow,
  focus/recovery, copy, native actions, and test matrix.
- Combat owns a null-first fail-closed registry and accepted-only runtime
  implementation. Before selection, released behavior is byte-equivalent in
  effect and generic launchers remain available. Source enablement permanently
  retires the two generic launchers even on later decode/measurement failure.
- Quartermaster alone owns calls, private review, selection/import,
  measurements, provenance, and accepted-source population.
- Image Specialist may polish code/configuration presentation using only the
  selected unchanged raster; no generation, edit, replacement, variation, or
  reveal belongs to Image.
- Intelligence independently validates and alone may release or advance the
  inventory.

## Runtime, access, and persistence

One native button receives pointer, keyboard, touch, and switch-like entry.
Focus returns to the host after LOOK, TALK, lesson exit, remediation, mastery,
reload, and route return. Escape closes only the active layer. Reload/resume
derives from existing evidence; no new save key, answer, prompt, candidate,
selection, media identity, or visual record is persisted. Seven final copy
slots and truthful alt text are populated only after selection. Reduced motion,
forced colors, reflow, and effective-200% remain equivalent.

## Budgets and validation

Accepted media must remain <=`21 / 121204767`; JS <=`1735000`; CSS
<=`120300`; production modules <=`227`; source maps `0`. Required gates are
focused host/predecessor/owned-lesson tests, related route/focus/privacy/ending
tests, all forty curriculum validators, full cold suite, production and fixture
builds, media/PBA inventory, served root/deep/assets/fixture identity, isolated
six-layout browser proof, and one clean-start E2E through selection-owned entry,
both existing lessons, return, reload, later route, and unchanged MH-40 hard
stop. Owned preview processes must stop and their ports prove clear.

## Protected state and rollback

Accepted Hosts 01-09 and every accepted media byte are immutable. Never inspect,
restore, stage, or use repository QA screenshots, Martin's real browser/profile/
save, the protected PDF/training tree, hidden lore, or opaque residual roots.
No branch, packet, lesson, reward, access, identity, authority, Machine/Builder
dialogue, world response, RP-013, or post-ending content is authorized.

Before product import, rollback is exact scratch cleanup. After import but before
release, rollback removes only the new raster/provenance and nulls the new
registry while preserving released behavior. Any variance returns to its
earliest owner; no downstream role silently changes this shell.

## Mission signature

`FIRST RUN SHELL READY / FRSH-010-v1`

Exact next owner: one Reconnaissance Sergeant.
