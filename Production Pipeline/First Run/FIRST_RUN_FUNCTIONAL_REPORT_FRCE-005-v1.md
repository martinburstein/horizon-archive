# First Run Functional Report - Stranded Lens Cradle

Report ID: `FRCE-005-v1`

Stage / owner: Combat Engineer / `combat_engineer`

Disposition: **`HOLD / ACCEPTED-MEDIA REPARSE GATE STOPPED BEFORE BYTE READ`**

Work Order / shell / blueprint: `FRWO-005-v2` / `FRSH-005-v1` /
`FRPX-005-v1`

Combat source: `36537eae64003205e0669caaa09ff5cbb36a676d`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Date: **2026-08-10**

## Result

Combat completed and froze the bounded no-media Host 06 product/test candidate,
but the mandatory pre-generation manifest gate failed before opening ordinal 1.
Every exact literal accepted-media path contains OneDrive components carrying
the Windows `ReparsePoint` attribute, and each literal file itself also carries
that attribute. `FRSH-005-v1` requires Combat to reject any reparse point in
any literal path component before reading. Combat therefore stopped rather than
hashing through the ambiguity or weakening the gate.

No `FRAM-001-v1` exists, no accepted-media byte was read, no tuple or canonical
digest was produced, no generator was called, and no generation ordinal was
consumed. Quartermaster may not begin.

## Implementation frozen

The code candidate implements only the authorized inert integration:

- exact null/false Host 06 source, provenance, physical, activation,
  protected-region, and six-layout registry fields;
- exactly seven null Quartermaster copy slots and one null alt slot;
- pure Host 05 sanitizer, source/provenance, finite measurement, objective
  layout, and decode guards;
- `source.enabled !== true` as the exact legacy-launcher condition;
- fail-closed enabled-but-invalid behavior that keeps Host 06 absent, retains
  Host 05 safely, and never restores the bypass;
- accepted Drowned source isolation for Hosts 04/05 and Demo Tour;
- conditional Host 05 -> Host 06 -> existing Return semantic succession;
- native LOOK/TALK/sole unchanged `openModelChoiceExercise()` USE routing;
- completed USE read-only behavior;
- one nonpersistent post-Host-05 paint/animation-frame transition token,
  connected focus, one polite announcement slot, and no replay on recovery;
- Terminal inertness and existing Close/Escape restoration;
- exact model-choice state mapping, reload/Meadow-return reconstruction, and
  no new save field or private persistence;
- one selected `<img>` future branch with centered `cover` presentation and
  no responsive duplicate; and
- machine-focused regression coverage plus `FRRC-003-v1`.

No candidate scalar was populated. No raster import, final prose, lesson,
evaluator, save schema, route, world, ending, Host 07+, dependency, or source
media changed.

## Files changed in the frozen code candidate

- `horizon-archive-game/src/App.jsx`
- `horizon-archive-game/src/drownedArchive.js`
- `horizon-archive-game/src/styles.css`
- `horizon-archive-game/test/strandedLensCradle.test.js`
- `Production Pipeline/First Run/FIRST_RUN_RELEASE_COMMAND_MANIFEST_FRRC-003-v1.json`

The two later media-owned paths remain absent. `FRAM-001-v1` remains absent.

## Deterministic validation evidence

- Pre-edit focused baseline: **43 passed / 0 failed**.
- Post-edit focused Host 06 plus adjacent contracts: **50 passed / 0 failed**.
- Additional legacy static-contract reconciliation: **29 passed / 0 failed**.
- Learning/privacy: **17 passed / 0 failed**.
- Related route, Demo Tour, responsive, and ending checks:
  **58 passed / 0 failed**.
- Exact inherited sorted curriculum/readiness validators:
  **40 / 40 passed**.
- Cold full product suite: **979 passed / 0 failed / 0 skipped** in
  `16.96s`.
- Production build: **PASS**, `217` modules.
- TD-012 fixture build: **PASS**, `57` modules.
- Combat pre-generation production PBA: JavaScript `1,676,508`; CSS
  `119,394`; accepted media `17 / 37,410,731`; source maps `0`. All Combat
  caps pass.
- Served preflight: production root `200`, deep fallback `200`, emitted JS
  `200`, emitted CSS `200`, and TD-012 fixture root `200`.
- Owned preview PIDs `19168`, `17844`, `53348`, and `54624` were stopped;
  ports `4173` and `4184` were proved clear.
- `git diff --check`: **PASS**.
- Complete E2E: **not run**, correctly. The selected source is absent and the
  shell forbids preview/E2E before source acceptance and managed/temp cleanup.

Automated accessibility evidence is not human assistive-technology
certification.

## Manifest gate evidence and exact blocker

`FRRC-003-v1` freezes the exact seventeen literal paths, raw-byte tuple schema,
canonical stream semantics, one-read-only-handle behavior, write/delete-share
denial, count/total checks, create/recompute modes, and later ladder.

The first create invocation checked component attributes before any byte-stream
open and stopped with `reparse point rejected`. A bounded diagnostic then
checked only the already-authorized exact literal components and established:

- all sixteen visual-media literals traverse `Visual Direction` and
  `Production Masters` components marked `ReparsePoint`;
- every dated visual-media child and every exact visual-media file is also
  marked `ReparsePoint`; and
- the audio literal traverses `curriculum`, `lessons`, `L-05-07`, and
  `evidence` components marked `ReparsePoint`, with `basin_audio.wav` also
  marked `ReparsePoint`.

The diagnostic did not list a parent, discover another path, open media bytes,
decode, render, thumbnail, interpret metadata, or inspect pixels. Because the
stop preceded byte reads, the only honest manifest result is:

```text
count: unavailable (gate stopped before tuple creation)
totalBytes: unavailable from this gate
entriesSha256: unavailable
candidateHead: 02d957e9d69dc7986928a391c37f899784f73ea5
FRAM-001-v1: absent
generation ordinals consumed: 0
```

The released PBA's independent inventory remains `17 / 37,410,731`; it is not
substituted for the missing per-file tuple gate.

## Variance and rollback

Classification: **`REQUIRED CORRECTION / SCIENCE MANIFEST VIABILITY`**.

The contradiction belongs to the Office of Science Administrator because the
Science viability finding assumed a lawful exact literal raw-byte source in
this OneDrive worktree, while the shell forbids every observed reparse
component. Mission must not silently weaken `FRSH-005-v1`; Science must either
provide a lawful shell-compatible read source that does not mutate/copy the
accepted media or issue a revised HOLD. Combat may not hydrate, copy, move,
replace, relabel, or otherwise mutate accepted media to cure the gate.

The inert code candidate is safe and reversible by explicit path to released
baseline `a91763e...`. No rollback was executed because the stopped candidate
imports no source and leaves released Host 05 behavior safe. No push occurred:
the shell authorizes the Combat push only at `PRODUCTION FUNCTIONAL`, which was
not reached.

## Protected state and maturity

Repository QA quarantine, protected PDF, training directory, real browser/
profile/save, hidden lore, user work, managed generation root, OS-temp
candidate roots, accepted-media bytes, and opaque residuals were not opened or
changed. Initial broad status and exact-ID locator recurrences remain
process-only under their already-OPEN records.

All thirteen inherited process records remain separate and **OPEN**: VR-17,
VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The separate Commandant filename/search-scope record
remains **OPEN**. None is waived, merged, closed, cured, or renumbered.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**, unknown and inaccessible.

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression `FR0 - 1 accepted shared compression / 1 exact / 10 missing`;
learning `FR2`; behavior/save/recovery `FR1`; content `FR2`; presentation
`FR3`; and prior bounded release proof `FR4`.

## Exact return handoff

One fresh Office of Science Administrator must read `FRCE-005-v1`, the exact
`FRSH-005-v1` manifest clauses, and `FRVE-005-v2` answer 10; corroborate the
OneDrive reparse condition using only the seventeen exact literal paths; and
issue a versioned `POLISH VIABILITY READY`, `REVISE`, or `HOLD` decision.

No Quartermaster attempt, generator call, accepted-media hydration/copy,
manifest bypass, candidate inspection, source import, Image work,
Intelligence release, reveal, push, maturity update, OPEN-record closure,
schedule, automation, or `FIRST RUN COMPLETE` may begin.
