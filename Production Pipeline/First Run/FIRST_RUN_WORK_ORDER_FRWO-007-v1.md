# Horizon Archive First Run Work Order - Sediment Abacus New Source

Work Order ID: `FRWO-007-v1`

Stage / stable agent: Operations Planning Major /
`operations_planning_major`

Disposition: **`WORK ORDER READY / HOST 07 SEDIMENT ABACUS / ONE POTENTIAL
CANONICAL SOURCE / TWO SEQUENTIAL BUILT-IN ATTEMPTS MAXIMUM`**

Date: **2026-08-12**

Control source inspected: `243edb4e`

Product baseline / continuity: `FRPB-001-v3` / `FRCL-006-v1`

Closed predecessor path: `FRWO-006-v2` / `FRVE-006-v1`

Released predecessor: `FRAB-005-v1 / Host 06 Stranded Lens Cradle`

Current maturity: Host 07 missing; `FR-03` physical `FR0`

Target evidence: one exact Host 07 physical relation, final-purpose integration,
runtime presentation, and independent host-level release without changing the
address-level `FR0` classification while Hosts 08-15 remain missing.

## Selected bounded outcome

This new Work Order owns one player-visible result:

> After exact Host 06 and unchanged `L02-03`, the player follows graded
> sediment along the existing dry clockwise shelf, encounters one distinct
> distributed field of irregular mineral nodules across several wash-channel
> depths, provisionally names it `Sediment Abacus`, and uses that physical
> discovery as the sole unchanged entry to `L03-01 + L03-02`.

This is the earliest incomplete fixed encounter. No later-address exception is
needed. The failed existing-source Work Order remains closed; `FRWO-007-v1` is
a distinct new-source pass under Martin's explicit two-attempt authority.

## Player problem and intended improvement

The released rail currently moves from exact Host 06 to generic compact
continuation and already-capable learning systems. A new player therefore has
no distinct physical Host 07, no visible sediment-to-channel relationship, and
no authored physical owner for Structured Packets and Control Flow.

The intended improvement is one credible same-basin discovery that makes the
next learning entry feel earned by observation rather than exposed by a
generic launcher. It adds no lesson, route, lore, response, reward, or later
content.

## Ordered milestones

1. Science proves built-in generation custody, source envelope, measurable
   physical/layout/accessibility gates, performance budgets, and fail-closed
   attempt disposition without consuming an attempt.
2. Mission integrates exactly two predeclared built-in prompts/calls maximum,
   staging/custody, selection, provenance, cleanup, implementation, validation,
   and rollback into one `FIRST RUN SHELL READY` contract.
3. Recon freezes one directorial interpretation and two exact prompt texts:
   prompt A for the primary source and prompt B for a more composition-constrained
   independent source. Both prompts are fixed before either call.
4. Tactical freezes complete hidden/available/in-progress/remediation/complete,
   focus, responsive, recovery, copy-slot, semantic, and attempt-review records.
5. Combat builds inert fail-closed Host 07 runtime integration and regression
   tests without enabling a source or calling generation.
6. Quartermaster performs attempt A and, only if lawfully eligible, attempt B;
   privately validates each; rejects and disposes failures; selects at most one
   passing source; imports it with exact provenance; and populates only the
   accepted runtime fields.
7. Image Specialist changes no media bytes and polishes only runtime code/
   configuration presentation using the selected unchanged source.
8. Intelligence independently validates the exact candidate, releases or
   rejects it, reconciles maturity/inventory, and leaves one synchronized next
   action.

## Exact attempt ledger

The complete domain is `{H7-A, H7-B}`.

- `H7-A` is the first and only initially eligible built-in `image_gen` call.
- `H7-B` becomes eligible only after `H7-A` reaches one exact terminal state:
  objective rejection with required output custody/cleanup complete, or a
  pre-materialization/tool-result failure that the shell explicitly classifies
  as consumed and safely terminal.
- Both prompts must describe a brand-new source and use no reference image.
- `H7-B` is an independent second call, not an edit or variation of `H7-A`.
- No third call, repeat call, batch, hidden replacement, edit, variation,
  reference-image inclusion, CLI, direct API, alternate model, or SDK runner is
  authorized.
- A passing `H7-A` ends generation; `H7-B` remains unconsumed and unavailable.
- A passing `H7-B` ends generation. If both fail, the Work Order returns HOLD.
- Only one passing raster may enter the workspace and product candidate.
- Every rejected/unselected output has zero reuse, prompt-reference, canon,
  continuity, learning, maturity, publication, or reveal authority.

The built-in image-generation skill governs the calls. Its managed default
output is not a product asset. Science must freeze how the exact returned
output is identified and lawfully copied or moved into bounded candidate
custody without parent discovery or unrelated-file access. If exact custody,
identity, disposition, or project import cannot be proved, Science must HOLD.

## Required physical acceptance rubric

Quartermaster must reject a candidate unless private original-resolution review
proves every item:

1. one coherent photorealistic first-person Drowned basin view;
2. graded sediment visibly continues from the preceding material route onto
   the dry shelf;
3. a distributed irregular field of mineral nodules occupies at least three
   visibly different wash-channel depths/bands;
4. nodule scale, spacing, orientation, and grouping invite counting while
   resisting rows, grids, beads-on-rods, a control panel, code diagram, or
   human-facing front;
5. the complete relationship is dry-reachable and observed from above present
   live flow;
6. the relation is one clear encounter, not scattered decoration or multiple
   competing candidates;
7. same-basin materials, mist, water, architecture, and restrained Crown-light
   behavior coexist without duplicating accepted Host 04-06 compositions;
8. protected Host 04-06 features, live water, Crown reflection, distant Tidal
   Lens, return ridge, and later-host cues remain outside the physical and
   semantic target;
9. no text, glyph, native label, lesson diagram, Machine/Builder message,
   prior-human trace, hidden-lore answer, invitation, recognition, reward,
   access, authority, or world response appears; and
10. the complete essential relation remains compositionally usable at desktop,
    narrow, and effective `200%` under Science's exact measurements.

Any ambiguity is rejection. Aesthetic strength cannot cure a failed physical,
layout, accessibility, provenance, or custody predicate.

## Routes, state, return, and checkpoint boundaries

- Exact Host 06 mastery and unchanged `L02-03` completion are the only entry
  predecessor.
- Before eligibility, Host 07 has no DOM, focus, semantic name, action, copy,
  evidence, or save state.
- `LOOK AT` may report only visible physical facts and writes no learning
  evidence.
- `TALK TO` remains silent.
- Sole `USE` enters `L03-01`; `L03-02` remains locked until exact `L03-01`
  mastery under current rules.
- Miss and remediation remain local and recoverable. Return, reload, and resume
  restore truthful local state without world change.
- Completed `USE` is read-only and may reopen completed human learning review
  without granting new credit.
- No new route branch, portal, vehicle, door, reward, access, authority, save
  checkpoint type, schema family, or later-host reveal is permitted.

## Learning, evidence, privacy, and offline scope

`L03-01` remains Structured Packets; `L03-02` remains Control Flow. Existing
thresholds, primary/fresh-transfer gates, answer-free remediation, confidence/
ownership, evidence allowlists, anti-forgery, no-cross-credit, sanitized
replicas, private-content stripping, Demo Tour isolation, offline behavior, and
no-exam-guarantee language remain exact.

Physical observation and image content grant zero learning evidence. The image
must contain no code, answer, score, or personal data. No network dependency is
allowed after the selected source is imported into the local product.

## Candidate, provenance, and product paths

Science and Mission must freeze exact generated-output custody before any call.
The only eventual product paths are:

```text
Visual Direction/Production Masters/2026-08-12-first-run-host07/
  host07-sediment-abacus-master-v1.png
  PROVENANCE.md
```

Both paths must be absent before selection. Import is create-new/no-replace and
only after all candidate gates pass. Provenance must record Work Order/shell,
built-in mode, attempt ID, final prompt identity, source byte length/SHA-256,
format/dimensions/color/opacity, generated-output custody identity, selection
decision, and immutable accepted-media baseline relationship without secrets
or rejected-output details.

Accepted Hosts 01-06 and all accepted media bytes remain immutable. A selected
source must be stored locally and referenced locally; no runtime URL, API,
credential, telemetry, remote fetch, or generated-images managed path may
remain in product code.

## Accessibility, responsive, presentation, and performance scope

Science must freeze candidate-specific normalized physical, semantic, label,
sediment-handoff, dry-approach, three-depth, protected-separation, and retention
records before acceptance. The semantic target must remain at least `44x44 CSS
px`, contain the physical center, avoid protected regions, and retain equivalent
meaning across the six established layout classes.

Final factual alt must describe only visible graded sediment, irregular nodules,
wash-channel depth, and dry reachability. It must not use visual-position-only
instructions or assert purpose. Keyboard, pointer, touch, switch-like
activation, focus, announcements, reflow/effective `200%`, forced color, and
reduced motion must be equivalent.

Science owns exact byte, decode, JS/CSS/module, ready-time, handler-time, CLS,
build, and test budgets. At minimum, final accepted media must remain within a
single-source cap and a whole-product aggregate cap, add no source map or
dependency, decode locally, and fail closed on missing/mismatched provenance,
measurements, source, or decode.

## Required implementation scope

Likely permitted systems after Mission approval:

- Host 07 registry/state/source/provenance/measurement records;
- Drowned route integration immediately after exact Host 06;
- final-purpose Host 07 copy and semantic/interaction surfaces;
- reuse/integration of existing Structured Packets and Control Flow systems;
- Host 07 focused tests plus adjacent Host 06/generic-continuation regressions;
- bounded source/provenance product paths above; and
- runtime presentation CSS/configuration using the unchanged selected source.

Science and Mission must name exact files. No new package/dependency, global
architecture rewrite, accepted-media edit, or unrelated cleanup is permitted.

## Validation ladder and completion evidence

Required evidence includes:

1. authority, source-control, attempt-ledger, custody, identity, provenance,
   accepted-media immutability, and forbidden-boundary checks;
2. prompt identity and objective private candidate review for each consumed
   attempt;
3. candidate byte/SHA/format/dimension/color/opacity/decode checks;
4. exact physical/layout/accessibility measurement records;
5. focused Host 07, Structured Packets, Control Flow, Host 06, route, focus,
   privacy, save, and recovery tests;
6. related and full-product tests plus readiness/curriculum validators;
7. production and fixture builds, PBA/media inventory, maps=0, and served
   root/deep/JS/CSS/media/fixture identity;
8. isolated non-overlapping E2E from exact Host 06 through Host 07 learning,
   return, reload, and completion, with representative responsive/accessibility
   review;
9. owned-process shutdown, port clearance, bounded candidate/output cleanup,
   and protected-state proof; and
10. dedicated commits, Mission/Combat/Intelligence push gates, exact candidate
    ancestry, and `HEAD == origin/main` synchronization.

Host-level release requires Intelligence PASS. Address-level `FR-03` remains
physical `FR0` while Hosts 08-15 remain missing.

## Rollback and hard stops

Before selection, any failure disposes only the exact shell-owned candidate
under its identity law and leaves product null-first. After import, any
functional, provenance, PBA, accessibility, build, E2E, or release failure
removes only the exact newly imported Host 07 raster/provenance and reverts only
Host 07 code/test/config changes to the last synchronized predecessor. Accepted
Hosts 01-06 are never rollback targets.

Stop and return to the earliest owner for continuity conflict, custody
ambiguity, tool-output ambiguity, multiple/unknown files, protected overlap,
cleanup failure, attempt-ledger ambiguity, source mismatch, or any request for
a third call/fallback. Do not enumerate or inspect unrelated managed-output,
protected, user, QA, residual, browser/profile/save, PDF, training, or hidden-
lore state.

## Maturity and inventory impact

None at planning. Host 07 remains missing; inventory remains `7 exact / 1
accepted shared compression / 31 missing / 1 unadvanced Witness expression`.
The Work Order target is evidence, not aspiration.

## Exact Science questions

Science must answer all of these before READY:

1. Can the built-in tool's exact returned output be privately inspected,
   identity-bound, copied/moved to bounded custody, and disposed without parent
   discovery, unrelated access, or revealing rejected results?
2. What exact source format, dimension/aspect, byte, color/opacity, decode, and
   aggregate-media envelope is viable for the existing runtime?
3. What normalized physical/semantic/label/handoff/approach/three-depth/
   separation records and six-layout thresholds objectively accept or reject a
   candidate?
4. How are the two fixed prompts, attempt ledger, attempt-2 eligibility,
   result custody, rejected-output zero authority, selected import, provenance,
   and rollback proven fail-closed?
5. What null-first state/source/provenance/decode/measurement guard prevents
   generic continuation after enablement failure and preserves exact Host 06?
6. Can current Structured Packets and Control Flow systems own the sole
   `L03-01 + L03-02` route without threshold, evidence, privacy, or save drift?
7. What exact accessibility, responsive, offline, performance, PBA, build,
   test, served, E2E, cleanup, and release budgets are required?
8. Are all protected exclusions, OPEN records, filename-enumeration records,
   and VR-65 preserved without access or evidentiary use?

## Exact Science handoff

Use one fresh Office of Science Administrator /
`office_of_science_administrator` context. Read its full profile, this Work
Order, `FRCL-006-v1`, `FRPB-001-v3`, closed `FRWO-006-v2` / `FRVE-006-v1`,
exact `FRAB-005-v1`, current runtime learning/Host 06 controls, accepted-media
inventory, and the installed image-generation skill.

Run only read-only/synthetic, no-generation viability checks. Issue one
versioned `POLISH VIABILITY READY`, `REVISE`, or `HOLD` artifact and synchronize
NEXT. Do not call image generation, inspect managed outputs, use CLI/API,
create candidate/product paths, edit runtime, begin Mission/production, advance
maturity, reveal media, or access protected state.

Operations signs **`WORK ORDER READY / FRWO-007-v1 / HOST 07 SEDIMENT ABACUS /
TWO BUILT-IN ATTEMPTS MAXIMUM / ROUTE FRESH SCIENCE`**.
