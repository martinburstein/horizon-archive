# First Run Polish Viability Envelope - Sixfold Weir

Envelope ID: `FRVE-003-v1`

Stage: Office of Science Administrator / `office_of_science_administrator`

Work Order: `FRWO-003-v1 / Sixfold Weir`

Baseline / lock: `FRPB-001-v1` / `FRCL-003-v1`

Release map / scoreboard: `FRRM-003-v1` / `FRSB-003-v1`

Science source inspected: `efff81ed2641fcfa673cbbbdd8ced12be52a5838`

Released First Run predecessor: `FRAB-002-v1 / FIRST RUN PASS RELEASED`

Released product/test candidate:
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`

Budget authority: `PBA-TD012-v1`

Date: **2026-08-10**

## Disposition

**`POLISH VIABILITY READY / FRVE-003-v1`**

The immutable accepted Drowned master can honestly carry one distinct Host 05
relationship without a media change. Its generation record deliberately
authors intake, separation, distribution, return flow, maintenance bypasses,
submerged counterparts, process structure at several heights, and a grounded
local coupling. Read-only inspection of the accepted plate confirms a second
coherent foreground relationship: six unequal dry conduit/spill seams cross
two differently oriented mineral lips to the right of Host 04. It is not the
lower-left return ridge, an underwater object, or the distant Tidal Lens.

The fixed registration is identical at canonical and narrow layouts:

`left 45% / top 75% / width 20% / height 25%`.

It covers source pixels `x 752.4-1086.8 / y 705.75-941` in the exact
`1672 x 941` master. It is disjoint from the existing wide Host 04 rectangle by
`10` percentage points / `167.2` source pixels and from the existing narrow
Host 04 rectangle by `1` percentage point / `16.72` source pixels. It is also
disjoint from both return-ridge rectangles. Its center is exact
`55% / 87.5%` (`x 919.6 / y 823.375`), on the dry foreground relationship.

The existing `L02-02` responsible-AI surface may be rehoused behind this
target's sole campaign `USE` without changing its content, evaluator,
thresholds, evidence, privacy, save, later Drowned order, route, world, or
ending. Mission may issue a shell only if it adopts every fixed requirement
below. Science performs no implementation and advances no maturity.

## Independent inspection basis

- Read the active First Run workflow, registry, Science profile, synchronized
  handoff, `FRWO-003-v1`, `FRPB-001-v1`, both guarded revalidations,
  `FRAB-001-v1`, `FRAB-002-v1`, `FRCL-003-v1`, and current map/scoreboard.
- Read the complete protected game-on-paper and the complete Drowned chapter,
  including the Host 04-to-05 material handoff, all later Drowned hosts, Crown
  turn, and unchanged exit.
- Read current Drowned surface, scene, visual, and provenance authorities;
  current `App.jsx`, `styles.css`, `sceneTransition.js`, Terminal focus logic,
  Workload Sort, exact responsible-AI source/evaluator/sanitizer, save
  projection, return/reload, later continuation, PBA, manifest, and MH-40
  controls.
- Read-only inspection used the accepted master only. It generated, edited,
  replaced, varied, imported, moved, removed, exported, or published no media
  and created no reveal.
- Accepted Drowned master remains exact `2,727,857` bytes / SHA-256
  `AFA0008E3F1E0CDACB2B9E58F14E9F676729EAB9E8725A58C87D73AC489C08ED`.
- Candidate-to-current product/test diff across `horizon-archive-game`,
  `playtest`, and `curriculum` is empty.
- Fresh relevant baseline passes `44/44` across responsible AI, Workload Sort
  and resume, Drowned return, Terminal focus, plate integration, and game
  restore in `146.2252 ms` Node duration.
- The required `foundry-azure-source-priority` skill is unavailable. This pass
  changes no current AI-901 objective claim, learning content, mapping, or
  evaluator, so no external objective assertion was needed. Existing
  `AI901-D1-O1` ownership remains byte- and meaning-fixed; no third-party
  source was used.
- Martin's browser/profile/save, hidden lore, and all three protected untracked
  paths were not opened or inspected.

## Answers to the ten Work Order questions

### 1. Provenance-honest distinct, dry-reachable relationship

**PASS.** The exact target covers six visibly separable conduit/spill seams
carried over two dry foreground lips at unlike angles. These are part of the
authored intake/distribution/return system. Their local center lies on the
same dry foreground ridge reached after Host 04; no swimming, live-flow entry,
or remote activation is implied.

The target does not reuse Host 04. Existing Host 04 is:

| Layout | Host 04 bounds | Host 05 bounds | Horizontal gap |
| --- | --- | --- | ---: |
| canonical | `24.375/56.94/10.625/21.11` | `45/75/20/25` | `10%` / `167.2px` source |
| narrow | `20/54/24/43` | `45/75/20/25` | `1%` / `16.72px` source |

The Meadow-return ridge ends at `17.5%` canonical and `15%` narrow, leaving
`27.5%` / `30%` before Host 05. Pairwise intersection area is exactly zero.
The distant Tidal Lens occupies the upper source field and is vertically
disjoint from a target beginning at `75%`. Submerged counterparts remain
underwater evidence surrounding the dry foreground; they are not the named or
actionable body. Host 05's stable assistive identity belongs to the foreground
six-branch relationship only.

Any implementation that shifts the target off the dry center, counts an
underwater counterpart as one of the six reachable branches, overlaps an
existing target, or names the Tidal Lens is a fail-closed `HOLD`.

### 2. Exact mapping, cover transform, retention, and target size

**PASS under one frozen rectangle.** Canonical and narrow use the same exact
`45/75/20/25` mapping. The source ratio is `1672/941`; the runtime scene is
`16/9` with `object-fit: cover`. Cover therefore retains full source width and
removes only `0.5` source pixel vertically.

- Canonical/laptop/effective-`200%` use the current `20%` vertical position:
  `0.1` source pixel is removed at top and `0.4` at bottom. Host 05 source-band
  retention is exact `0.998299681190223`.
- Narrow and retained `320` fixtures use the current top position: `0` source
  pixels are removed at top and `0.5` at bottom. Host 05 source-band retention
  is exact `0.997874601487779`.
- Horizontal retention is exact `1.0`; target center remains contained in all
  cases. Pairwise target overlap remains exactly zero.

| Required layout | Scene frame | Host 05 rendered target |
| --- | ---: | ---: |
| desktop | `1920 x 1080` | `384 x 270` |
| laptop | `1366 x 768.375` | `273.2 x 192.09375` |
| narrow | `390 x 219.375` | `78 x 54.84375` |
| effective `200%` | `768 x 432` | `153.6 x 108` |
| retained `320 x 180` | `320 x 180` | `64 x 45` |
| retained `320 x 240` viewport | `320 x 180` scene | `64 x 45` |

Every target exceeds `44 x 44`. The shell must require actual DOM/source
remeasurement at all six layouts. Any target dimension below `44`, source-band
retention below `0.95`, lost center, positive overlap, clipped label, or
horizontal escape is `HOLD`. The scene alt remains a neutral description of
visible landscape; the active button alone owns action and state semantics.

### 3. Pure post-Host-04 state and no persistence

**PASS with one sanitized view derivation.** Derive only from existing
sanitized `workloadEvidence` and `responsibleAIEvidence`:

```text
sanitizeWorkloadEvidence(...).masteryStatus != mastered -> hidden interface
workload mastered + no valid responsibleAIEvidence       -> available
workload mastered + in_progress/primary_complete/
  transfer_complete                                     -> in_progress
workload mastered + remediation_required                -> remediation_required
workload mastered + responsible AI mastered              -> complete
```

`hidden interface` means the six-branch material remains visible in the plate,
but no Host 05 DOM node, focus stop, label, status, announcement, action, or
credit exists. Exact Host 04 mastery may mount one polite Suit detection after
its Terminal closes. The derivation is nonpersisted view state. Forged IDs,
unknown forms/statuses, malformed correctness, or a forged prerequisite fail
closed through the current sanitizers.

No `sixfoldWeir`, detection, focus, dialogue, host-state, or active-session key
may enter the save. Existing `completed`, `pendingSceneId`, Drowned completion,
and every evidence field remain unchanged in meaning and schema.

### 4. Sole in-world `L02-02` entry and invariant learning

**PASS.** Remove the generic post-Host-04 Responsible AI launcher. Exactly one
campaign entry remains: `USE Sixfold Weir` after sanitized Host 04 mastery.
`LOOK AT` observes the physical relation; `TALK TO` records silence; neither
opens a Terminal or writes evidence. `USE` opens or reconstructs the existing
`EX-L0202-RESPONSIBLE-AI` session.

The following remain byte- and meaning-fixed:

- six principles; six primary and six fresh-transfer scenarios;
- four independently checked dimensions per scenario and strict `24/24` per
  form;
- all-principles coverage, misconception routing, progressive answer-free
  remediation, genuinely fresh transfer, closed-note explanation, confidence,
  and human ownership confirmation;
- `AI901-D1-O1`, exercise/activity/assessment/skill owners;
- `responsibleAIEvidence` as the sole durable record; and
- the next existing `L02-03` continuation after exact mastery.

Completed `USE` is read-only: it may report existing finalized local evidence
but cannot open fresh scored work, add an attempt/hint/check, change confidence,
downgrade mastery, or affect route/save/world state. Demo Tour remains
separate and zero-credit.

### 5. Exact focus owners and fallbacks

| Event | Required focus / recovery owner |
| --- | --- |
| Host 04 mastery / Host 05 detection | Host 05 hotspot after Host 04 Terminal unmount; one polite Suit announcement |
| `LOOK AT` / `TALK TO` | Host 05 remains focused; dialogue live region reports owner-correct meaning |
| `USE` | Existing Terminal title receives initial focus; background becomes inert |
| Primary miss / remediation | Actual failed field/status and existing ordered controls; no scene focus jump |
| Primary acknowledgement | Terminal closes; Host 05 restores focus; next `USE` reconstructs fresh transfer |
| Transfer to explanation | Existing in-dialog order; no close or focus escape |
| Close / Escape | Connected enabled Host 05 trigger; if sanitation hides it, exact Host 04 trigger; otherwise the current scene's first lawful fallback |
| Mastery acknowledgement | Host 05 trigger, then the unchanged next continuation remains the next lawful action |
| Reload unfinished | Host 05 receives scene recovery focus; clean session mounts only after deliberate `USE` |
| Reload mastered | Existing next continuation receives recovery focus; completed Host 05 remains discoverable and read-only |
| Meadow return / redepart unfinished | Navigation writes nothing; on Drowned re-entry Host 05 receives focus |
| Meadow return / redepart mastered | Navigation writes nothing; existing next continuation receives focus |
| Malformed evidence | Earliest safe sanitized Drowned boundary; no Host 05, skip, replay, or credit |

Keyboard, Enter/Space, pointer, touch, and switch-like semantic activation must
reach the same handler once. No focus landing is evidence. Disabled/hidden
surfaces receive no focus. Source order is Host 04, Host 05 when eligible,
existing secondary return only where currently ordered, then adventure
controls and the unchanged continuation; Tactical must preserve a coherent
actual tab order and may not use CSS visual order as proof.

### 6. Required meaning slots and voice owners

Quartermaster owns exact final prose. The shell must require these meanings:

| State | Required meaning | Owner |
| --- | --- | --- |
| unseen as interface | Six unequal foreground branch relations already divide the flow; no active interface or invitation is claimed | Scene |
| available | Pilot notices the repeated relation; Suit confirms one separate compatible local surface | Pilot then Suit/System |
| in progress | Human expedition work is unfinished and can resume privately; water and branch geometry remain unchanged | System plus Scene fact |
| missed | Only the actual responsible-AI dimension is unresolved; remediation is bounded and answer-free | 901 Teacher/System |
| mastered | Existing `L02-02` evidence is finalized; the six branches, basin, Crown, and route do not respond | System plus Scene fact |
| returned | Same physical relation; sanitized existing evidence reconstructs only the lawful boundary | Scene plus Suit/System |
| next boundary | Pilot chooses to follow the pre-existing material trail; the retained continuation claims no Host 06 parity | Pilot / Scene / System boundary |

Machine and Builders own no line. Copy cannot say native six principles,
lesson diagram, invitation, expected visitor, healed, unlocked, earned route,
approval, reward, identity, authority, mission, access, response, memory, or
recognition.

### 7. Narrow performance, offline, and media budget

**PASS only fail-closed.** Released evidence is JS `1,663,664`, CSS `118,981`,
modules `216`, and media exact `17 / 37,410,731`.

The Mission shell must impose both the global PBA and this narrower pass cap:

| Measure | FRWO-003 pass cap | Global hard cap |
| --- | ---: | ---: |
| aggregate emitted JavaScript | `<=1,675,664` (`+12,000`) | `<=1,703,258` |
| aggregate emitted CSS | `<=119,281` (`+300`) | `<=119,672` |
| production modules | `<=217` (`+1`) | `<=222` |
| focused / related / cold full | `<=30s / <=60s / <=60s` | same |
| production build / fixture build | `<=60s` each | same |
| complete E2E | `<=180s` | same |
| sampled Host 05 activation | `<=2ms` | total sampled task `<=100ms` |

Prefer existing hotspot, state, focus, dialog, forced-color, reduced-motion,
and containment styles. No dependency, lockfile, network request, account,
credential, endpoint, live model, Foundry/Azure call, telemetry, download,
clipboard, service worker, source map, Python/WASM runtime, or external storage
is permitted. Runtime media remains the exact accepted hash set with zero new,
changed, moved, or removed media. Any local or global overage is `HOLD`; there
is no quality waiver.

### 8. Exact proof ladder

1. Candidate ancestry, bounded diff, protected boundaries, game-on-paper and
   Drowned master hashes, exact media inventory, and forbidden-token checks.
2. Pure geometry tests freeze `45/75/20/25`, exact source bounds/center, both
   cover-retention values, six rendered sizes, `>=44`, zero pairwise overlap,
   and source-honest dry foreground identity.
3. State tests cover forged/malformed evidence, pre-mastery DOM absence,
   available/in-progress/remediation/complete derivation, no new save key, and
   zero credit from discovery/observation/focus.
4. App contracts cover Host 04-to-05 order, LOOK/TALK/USE, exactly one active
   `L02-02` entry, generic-launcher absence, completed read-only `USE`, modal
   inertness, announcements, and every focus fallback above.
5. Exact responsible-AI tests retain strict primary, actual miss, progressive
   remediation, fresh transfer, closed-note explanation, ownership,
   confidence, mastery, sanitizer, safe close/Escape, and clean restore.
6. Save/privacy tests enumerate the complete campaign projection and reject
   private choices, explanation text, feedback, hints in progress, dialogue,
   focus, detection, and content sentinels.
7. Return/resume tests prove Drowned-to-Meadow navigation and redeparture are
   write-free; unfinished/mastered states recover correctly; `L02-03` and all
   later Drowned/later-rail order remain exact; no Host 06 affordance is
   invented.
8. Demo Tour, opening, Meadow, Witness, City, later rail, full product suite,
   and exactly `40` sorted curriculum/readiness validators pass.
9. Production and exact TD-012 fixture builds pass; root/deep/JS/CSS served
   bytes equal disk; PBA/offline/media caps pass.
10. One and only one non-overlapping clean-start production E2E proves Host 04,
    deliberate Host 05, a real `L02-02` miss/remediation/safe exit/reload/fresh
    transfer/explanation/mastery, completed read-only `USE`, unchanged next
    Drowned boundary and later rail, both equal MH-40 outcomes, null deltas,
    `successor=null`, and zero runtime errors.
11. Live review proves all six layouts, keyboard/pointer/touch, exact focus,
    label/target containment, no overflow, non-color state, forced-color
    focus, reduced motion, and no sensory-only meaning; automation is not human
    assistive-technology certification.
12. Stop exact owned preview/browser PIDs, prove ports clear, remove only exact
    owned external temp roots after resolved containment checks, preserve the
    repository QA quarantine, run `git diff --check`, and freeze candidate
    identity before later synchronization.

No string-only proof, invisible duplicate button, renamed generic launcher,
second whole-game journey, or manually authored live summary supplies release
evidence.

### 9. Manifest supersession and required TUNE

**Supersede; do not revise historical release evidence.** Mission must
authorize a new checked-in
`Production Pipeline/First Run/FIRST_RUN_RELEASE_COMMAND_MANIFEST_FRRC-002-v1.json`
for `FRWO-003-v1`. `FRRC-001-v1` remains the immutable prior-cycle manifest.

`FRRC-002-v1` must retain explicit ID, workdir, argument-array command,
timeout, expected exit, owner, prerequisites, output/port ownership, and
cleanup for focused, related, full, exactly `40` sorted `--self-test`
validators, both builds/previews, served identity, PBA/media, one E2E, live
summary verification, and cleanup identity.

Its QA contract must:

- create a new GUID-suffixed directory under the resolved OS temporary root at
  execution time;
- require the directory not to exist before creation;
- prove the resolved root is outside the repository and inside the resolved
  OS temporary parent before any write or delete;
- pass the exact resolved path as `HORIZON_ARCHIVE_QA_DIR` to the sole E2E;
- never name, inspect, use, or delete `Production Pipeline/First Run/QA/`; and
- remove only that exact owned external root after evidence capture and a
  repeated containment check.

The sole E2E must emit `first-run-live-summary.json` inside that owned root.
The manifest must include a machine verifier that fails unless the JSON has:

- schema, Work Order, shell, exact candidate, and runtime-error fields;
- exactly the six required layout IDs and measured viewport/scene dimensions;
- per-layout Host 05 DOM rectangle, source bounds, center containment,
  source-band retention, Host04/return overlap areas, target-size,
  label containment, and horizontal-overflow results;
- focus order and restoration results for detection, LOOK/TALK/USE, close,
  Escape, miss, primary acknowledgement, mastery, reload, return, and next
  continuation;
- forced-color and reduced-motion computed-style results; and
- one fail-closed aggregate `pass` derived by the run, not handwritten.

The verifier prints one concise JSON result and rejects missing/extra layout
IDs, nonfinite measurements, a candidate mismatch, any false required result,
or runtime errors. The live summary is evidence only; it is not durable
campaign state or a reveal.

### 10. Exact rollback boundary

Rollback baseline is released product/test candidate
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`.

The future bounded rollback removes only the Host 05 product/test delta,
FRWO-003 E2E delta, new `FRRC-002-v1`, and FRWO-003 production reports. It
restores shell-permitted product/test files to their exact candidate blobs and
deletes only files first introduced by the bounded patch. It does not reset the
repository, rewrite unrelated First Run control history, alter `FRRC-001-v1`,
or touch protected/untracked state.

No save migration is needed because no new durable field exists. Before and
after rollback, prove the released candidate tree for `horizon-archive-game`,
`playtest`, and `curriculum`; exact Drowned/media hashes; protected boundaries;
and no owned preview/temp residue. Any candidate ambiguity returns to Mission;
no broad reset, checkout, or recursive delete is authorized.

## Reused systems and permitted production surface

Reused unchanged in authority: accepted Drowned master; normalized hotspot
renderer; Host 04 and Workload Sort; existing responsible-AI evaluator,
session, sanitizer, evidence, save projection, Terminal shell, focus trap;
Drowned return/reload and retained continuation chain; Demo Tour isolation;
all later rail; TD-012 and MH-40.

Mission may permit only:

- `horizon-archive-game/src/App.jsx` for bounded Host 05/action/focus wiring;
- one pure Drowned state/presentation module if it lowers `App.jsx` risk;
- `horizon-archive-game/src/styles.css` only within the narrow cap and only if
  existing styles cannot meet state/focus/containment requirements;
- `horizon-archive-game/src/responsibleAIExercise.js` wiring only if strictly
  unavoidable; evaluator, content, thresholds, sanitizer, and evidence meaning
  are patch-forbidden;
- exact focused/related tests, the single existing E2E, `FRRC-002-v1`, and
  current-cycle reports/handoff.

Curriculum, answer keys/scenario bank, save schema/version, dependencies and
lockfiles, canonical story/lore, accepted media, Host 06-15 content/controllers,
Witness, City, later rail, Measured Horizon product code, real storage, hidden
lore, and all protected paths are patch-forbidden.

## Hard stops, variance routing, and maturity

Return `HOLD` for new/altered media, unsupported six-branch interpretation,
lost dry center, crop/overlap/target failure, duplicate `L02-02` entry, learning
or sanitizer change, private persistence, save-schema change, Host 06
promotion, route/world response, dependency/network use, budget failure,
manifest external-root/live-summary failure, or inability to run one isolated
E2E. Return canon/voice conflict to Colonel, ordering/scope expansion to
Operations, and cross-discipline ambiguity to Mission.

No maturity changes in Science. `FR-03` remains continuity `FR2`, physical
host `FR0 - 1 accepted compression / 11 missing`, learning `FR2`, behavior/
save/recovery `FR1`, final-purpose content `FR2`, presentation `FR3`, and prior
bounded release proof `FR4` until Intelligence accepts as-built evidence.

## Exact Mission handoff

Read `First Run Agent Profiles/mission-captain.md` in full, then read
`FRWO-003-v1`, this `FRVE-003-v1`, `FRPB-001-v1`, both guarded
revalidations, `FRAB-001-v1`, `FRAB-002-v1`, `FRCL-003-v1`, current
`FRRM-003-v1` / `FRSB-003-v1`, exact runtime/evaluator/save/PBA/media controls,
and `FRRC-001-v1` plus its TUNE.

Independently reconcile every Work Order requirement and issue exactly one
versioned `FIRST RUN SHELL READY`, `REVISE`, or `HOLD`. If ready, freeze exact
`45/75/20/25` geometry, pure sanitized state, sole in-world `USE`, unchanged
learning/privacy/save/later order, focus table, meaning slots, narrow/global
budgets, immutable media, `FRRC-002-v1` external-QA/live-summary contract, one
non-overlapping E2E, hard stops, permitted files, and exact rollback before
handing Recon one production edge.

Do not write final prose, implement production, alter learning/save/route/
world/ending/media, advance maturity, begin Recon, inspect protected state,
create a reveal/schedule, or call `FIRST RUN COMPLETE`.
