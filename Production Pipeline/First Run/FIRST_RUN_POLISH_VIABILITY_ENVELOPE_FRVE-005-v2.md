# Horizon Archive First Run Polish Viability Envelope

Envelope ID: `FRVE-005-v2`

Stage: Office of Science Administrator / `office_of_science_administrator`

Work Order: `FRWO-005-v2 / Stranded Lens Cradle - One New Source and Fixed
Lesson Integration`

Disposition: **`POLISH VIABILITY READY / MANAGED ONE-FILE INGRESS AND
PRE-GENERATION IMMUTABLE 17-FILE MANIFEST GATE VIABLE`**

Date: **2026-08-10**

Science source: `4c7c8a0d0d238f07db09871061ec05224fb75266`

Baseline / continuity: `FRPB-001-v2` / `FRCL-004-v2`

Superseded Science return: `FRVE-005-v1 / HOLD`

Released authority: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Released product candidate:
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`

Planning controls: `FRRM-005-v2` / `FRSB-005-v2`

## Science finding

`FRWO-005-v2` lawfully cures the two execution blockers in `FRVE-005-v1`
without weakening the one-source authority or any retained envelope. The
built-in tool's managed `$CODEX_HOME/generated_images/...` output can be used
as one exact tool-owned ingress before a byte-identical copy enters one fresh
GUID-named OS-temporary evaluation child. Every ambiguous or unverifiable
return fails opaque, consumes the ordinal, and stops without discovery or
fallback.

The current runtime also supplies a closed manifest input set without media
enumeration. A read-only traversal beginning at the exact literal entry
`horizon-archive-game/src/main.jsx`, following only literal relative module and
CSS references, reaches exactly seventeen unique media literals. It performs
no parent listing, glob, recursive filename search, media-byte read, preview,
decode, metadata interpretation, or pixel inspection. Mission may freeze the
exact list below and Combat may later read only those exact files through the
machine contract in answer 10.

The twelve viability answers are therefore `PASS`, conditional on exact shell
adoption and later candidate proof. No asset, prompt, coordinate, candidate,
manifest tuple, or source mapping is accepted by this planning artifact. No
maturity or media inventory advances.

## Answers to the twelve Work Order questions

### 1. Generator capability, output envelope, and maximum attempts

**PASS / CONDITIONAL.** The installed built-in image-generation authority
uses `$CODEX_HOME/generated_images/...` for managed output, requires generation
before any project copy, and does not provide a dependable destination-path
control. The exact managed root currently resolves to
`C:\Users\marti\.codex\generated_images` and is available outside the Horizon
repository. Mission must freeze that exact root and the call/result adapter;
the tool-returned exact local absolute pathname is the only ingress authority.

The source envelope is:

- one brand-new `stylized-concept` landscape per call;
- no referenced image, conversation image, accepted-media input, edit, or
  variation mode;
- maximum three calls, sequential, exactly one returned raster per call;
- one shell-frozen builder-honest prompt specification; any prompt revision
  consumes the next ordinal;
- required selected result: exact `PNG`, `3840 x 2160`, `16:9`, opaque 8-bit
  sRGB, browser-decodable, no alpha/transparency, text, or executable metadata,
  and at most `12,000,000` bytes;
- wrong count, path, format, dimensions, color, opacity, decode, metadata, or
  byte cap rejects and consumes the ordinal; and
- no upscale, alternate export, crop, edit, derivative, fourth attempt, CLI,
  REST/API, browser, alternate generator, or manual-file fallback.

The prompt specification must describe a premium contemporary first-person
same-basin Drowned view; one weathered lens inside one tilted conformal cradle
on an existing dry reach; Host 05 fragment continuity; visible drainage and
stress contacts; horizon reflection; old Builder materials and multiple
stewardship eras; and quiet scientific uncertainty. It must exclude people,
body, human controls, symbols, UI, lesson diagrams, response, reward,
invitation, and accepted-image input.

### 2. Objective physical rejection rubric

**PASS.** Original-resolution Quartermaster review rejects on any failed
predicate, recorded by the stable codes `PHY-01` through `PHY-12`:

1. no single tight rectangle contains the complete lens/cradle relation;
2. less than `80%` of the visible lens lies within the projected inner cradle,
   or the lens reads beside/behind/through/in front of an empty aperture;
3. cradle principal-axis tilt is outside `12-35 degrees` from horizontal;
4. fewer than two distinct load/stress contacts couple lens, cradle, and
   supporting fabric;
5. fewer than two continuous drainage seams lead toward basin drainage without
   implying player-caused drainage;
6. relation intersects live water, lacks `2%` source-height dry clearance, or
   lacks one continuous dry-material approach;
7. fewer than three lens-like fragments cue from the Host 05 side toward but
   not into activation;
8. horizon/reflected horizon crosses less than `25%` of visible inner-lens
   width before interface;
9. view is not first-person or shows cutaway, diagram, teleport, second basin,
   body, hand, shadow, reflection, companion, ship, human path/control,
   readable text, glyph, face, or prior-human trace;
10. host cannot be separated from live water, Host 05 cue, return ridge, Crown,
    distant Tidal Lens, or a second lens/cradle candidate;
11. same-basin continuity is not independently legible through water, ceramic
    ribs, smoky phase glass, drainage, corrosion/mineral deposition, repair
    seams, and old process routing; or
12. scene resembles a native lesson diagram, indicates a best answer, or
    implies response, recognition, reward, access, authority, invitation,
    purpose certainty, or hidden lore.

One failure gives the source zero product, reuse, canon, maturity, publication,
or reveal authority.

### 3. Source bands and composition tolerance

**PASS.** Acceptance uses normalized source coordinates:

| Band | Frozen envelope |
| --- | --- |
| top status-safe context | `x=[0,1], y=[0,.10]`; no essential physical fact |
| complete Host 06 relation | wholly inside `x=[.34,.70], y=[.30,.74]` |
| tight relation rectangle | width `.30-.36`; height `.30-.40` |
| Host 05 fragment approach | wholly inside `x=[.04,.30], y=[.56,.88]` |
| same-basin distant context | `x=[.70,.98], y=[.08,.56]` |
| live-water context | may occupy `x=[.68,.98], y=[.62,.92]`; no intersection with relation or dry approach |

The relation and approach cue each retain at least `99.5%` area and measured
center in all six layouts. The host rectangle is at least `.30 x .30` of
source, supplying at least `96 x 54 CSS px` in the retained-small layout before
semantic enlargement. Essential fact outside the bands or any required crop,
raster/vector repair, synthetic geometry, overlay-created fact, or fourth
attempt is rejection.

### 4. Six-layout centered no-crop contract

**PASS.** One `<img>` uses `object-fit: cover` and
`object-position: 50% 50%` at every layout. Exact `16:9` source and world frame
mean the full `x=[0,3840], y=[0,2160]` source remains visible at `1920x1080`,
`1366x768`, `390x844`, `768x900` effective `200%`, `320x180`, and the
`320x180` world inside `320x240`. For rendered art width `Rw`, scale is
`Rw/3840` and height is `2160*scale`. Ratio drift, crop, responsive duplicate,
or non-centered override rejects. Relation, approach, anchor, label, and
physical center remain retained; the semantic target remains at least
`44 x 44 CSS px`.

### 5. Center, hotspot, label, nonoverlap, and mapping

**PASS.** Tactical freezes and Quartermaster may populate only:

```text
source = { path, sha256, byteLength, width=3840, height=2160,
           format="png", color="opaque-srgb-8", attemptOrdinal }
physical = { x, y, width, height, centerX, centerY }
activation = { x, y, width, height }
label = { insetOuterCss=3, insetTextCss=5 }
protected = { host05Cue, liveWater, returnLikeRidge, crown, tidalLens,
              secondLensCandidate }
layouts = { desktop, laptop, narrow, effective200, retained320x180,
            retained320x240 }
```

Candidate values remain `null` until one exact passing source exists. Physical
is the smallest full-relation rectangle; its arithmetic center lies in
activation with `>=4 CSS px` clearance in all layouts. Activation contains
physical, is no more than `1.50x` its area, and is at least `44 x 44 CSS px`.
Label outer/text bounds are activation inset by `3/5 CSS px`, fully contained
without scroll or overflow. Physical and activation have zero area intersection
with every present protected rectangle and at least `96` source pixels / `8
CSS px` mapped edge separation. Each absent protected feature is explicitly
`absent`.

Mapping uses the answer-4 transform, unrounded DOM rectangles, and strict
`1/64 CSS px` floor lattice. Gates prove source/DOM identity, center and label
containment, target size, anchor retention, nonoverlap, source order, and
geometry stability before and after focus traversal.

### 6. Fail-closed state and source guard

**PASS.** Combat first creates an inert registry with all source/measurement
fields `null` and `enabled=false`. Host 06 is absent unless exact sanitized
Host 05 responsible-AI mastery, enabled exact permitted path, provenance-equal
dimensions/bytes/SHA, ordinal `1..3`, finite passing measurements, and natural
dimension decode all pass.

Then absent model-choice evidence is `available`; sanitized
`remediation_required` remains that state; sanitized `in_progress`,
`primary_complete`, or `transfer_complete` maps to `in_progress`; exact
`mastered` maps to `complete`; every other state is `hidden`. Raw/private
responses, focus, observation, media load, and detection are never inputs.

No durable field, save version, migration, route/detection flag, or scene
mutation is added. Missing import fails the build. Missing/mismatched runtime
decode or metadata leaves Host 06 absent and Host 05 safe, never restoring the
generic bypass. Focus returns to lawful Host 06, otherwise Host 05, otherwise
the first lawful scene control. The unavailable boundary cannot release.

### 7. Sole unchanged `L02-03` routing

**PASS.** Sole Host 06 `USE` calls existing `openModelChoiceExercise()` only
in `available`, `in_progress`, or `remediation_required`; completed USE is
read-only; LOOK/TALK write nothing. The generic post-Host-05 launcher is absent
when the source contract is enabled.

All existing `L02-03` assets, eight primary plus eight transfer scenarios,
options, answer key, evaluator, strict `16/16 + 16/16 + 2/2` progression,
actual-dimension remediation, blank retry, closed-note explanation,
ownership/confidence, sanitizer, allowlisted `modelChoiceEvidence`, private
clearing, save projection/restore, and next boundary remain byte/behavior
exact. Microsoft Learn's current AI-901 objectives, effective April 15, 2026
and checked August 10, 2026, still include selecting an appropriate model by
capability and identifying deployment options/configuration parameters. No
learning revision is required.

### 8. Meaning, ownership, and accessibility

**PASS.** Quartermaster owns final prose in exactly seven slots: unseen scene
fact; available observation/provisional name/unresolved hypothesis; in-progress
reconstructable work; remediation limited to the actual missed tradeoff;
complete allowlisted evidence with invariant world; returned invariant relation
with sanitized restoration; and next existing structured-packet boundary with
no Host 07 expression.

Scene owns physical fact; Pilot owns provisional naming and uncertainty;
Suit/System owns compatibility/interface/evidence/recovery/save/route; the 901
Teacher owns bounded learning. Machine and Builders own no line.

The fixed accessible name is `Stranded Lens Cradle`. Native labels are exactly
`${verb in lower case} Stranded Lens Cradle, ${state}`, where state is
`available`, `in progress`, `remediation required`, or `complete`. Hidden has
no node, name, or announcement. Alt text states only first-person observable
lens, tilted cradle, dry waterline separation, fragments, drainage, stress
contacts, and surrounding basin—never purpose, correctness, invitation,
lesson answer, or Builder intent.

Enter/Space, pointer, touch, and switch-like activation converge once on the
same native button. Meaning never relies on color, sound, animation, or sight.
Forced color retains a `3px Highlight` focus outline; reduced motion disables
nonessential motion; no required audio or timed state exists.

### 9. Media, PBA, runtime, and validation budgets

**PASS.** Released predecessor: JavaScript `1,667,393`, CSS `119,247`, modules
`217`, accepted media `17 / 37,410,731`, source maps `0`.

| Measure | Affected cap | Global/result cap |
| --- | ---: | ---: |
| emitted JavaScript | `<=1,679,393` | `<=1,703,258` |
| emitted CSS | `<=119,547` | `<=119,672` |
| production modules | `<=218` | `<=222` |
| selected raster | `<=12,000,000` | exact `3840 x 2160` PNG |
| resulting media | exact `18` and `37,410,731 + selectedBytes` | `<=49,410,731` bytes |
| source maps | `0` | `0` |
| cold/warm decode | `<=250ms / <=100ms` | direct browser decode |
| local image ready | `<=750ms` | one same-origin request; zero external |
| attributable / total CLS | `<=0.01 / <=0.05` | reserved `16:9` slot |
| activation / sampled task | `<=2ms / <=100ms` | same |
| focused / related / cold full | `<=30s / <=60s / <=60s` | same |
| production / fixture build | `<=60s` each | same |
| complete E2E | one call `<=180s` | no retry |

No dependency/lockfile, service, account, credential, endpoint, live runtime
model, telemetry, download, clipboard, external storage, service worker, or
Python/WASM runtime is allowed.

### 10. Managed ingress, immutable manifest, provenance, cleanup, no reveal

**PASS.** Mission freezes one exact executable adapter implementing the ten
managed-ingress steps in `FRWO-005-v2`: predeclared fresh GUID temp child;
exact one-file direct return beneath the fixed managed root; scalar containment;
byte/SHA identity copy; immediate exact managed-file deletion and absence
proof; temporary-only rubric; exact rejected cleanup; at most one byte-identical
product copy; exact temp-child cleanup; and maximum three ordinals. Unknown,
missing, multiple, ambiguous, unowned, nonlocal, in-repository, containment-
uncertain, identity-mismatched, missing-before-cleanup, cleanup-failed, or
absence-unverifiable state stops the cycle opaque. No parent listing, search,
retry, fallback, broad cleanup, import, or reveal follows an opaque stop.

#### Exact manifest input list

Mission must copy these seventeen strings byte-for-byte, in this exact ordinal
order, into its immutable gate. They are repository-relative POSIX paths and
are the only accepted-media read authority Combat receives:

```text
Visual Direction/Production Masters/2026-07-15-glass-meadow-integrated-terminal/glass-meadow-integrated-terminal-master-v1.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/city-threshold-access-master.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/city-threshold-boundary-master.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/city-threshold-overview-master.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/drowned-archive-master.png
Visual Direction/Production Masters/2026-07-15-photorealistic-demo/witness-corridor-master.png
Visual Direction/Production Masters/2026-07-16-civic-record-district-arrival/civic-record-district-arrival-master-v1.png
Visual Direction/Production Masters/2026-07-26-rp004-three-current-runtime-master/sc05-three-current-panorama-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-27-rp005-manyfold-return-runtime/sc06-manyfold-return-detail-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-27-rp005-manyfold-return-runtime/sc06-manyfold-return-panorama-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-29-rp006-interval-works-runtime/sc07-interval-works-crosssection-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-29-rp006-interval-works-runtime/sc07-interval-works-panorama-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-29-rp007-braided-verge-runtime/sc08-braided-verge-contact-detail-runtime-master-v1.webp
Visual Direction/Production Masters/2026-07-29-rp007-braided-verge-runtime/sc08-braided-verge-panorama-runtime-master-v1.webp
Visual Direction/Production Masters/2026-08-01-rp008-offset-reach-runtime/sc09-offset-reach-panorama-runtime-master-v4.webp
Visual Direction/Production Masters/2026-08-01-rp008-offset-reach-runtime/sc09-offset-reach-relation-detail-runtime-master-v1.webp
curriculum/lessons/L-05-07/evidence/basin_audio.wav
```

#### Machine-readable schema and hash semantics

The manifest schema ID is
`horizon-archive.first-run.accepted-media-manifest/v1`. Its JSON object has
exactly these semantic fields:

```text
schema: exact schema ID
workOrder: "FRWO-005-v2"
shell: exact later Mission shell ID
releaseBaseline: "a91763e28d488f31f8cf7d40ece0b2682246ba9b"
candidateHead: exact 40-character lowercase Git commit at Combat gate
pathBase: "repository-root"
hashAlgorithm: "SHA-256"
hashEncoding: "lowercase-hex"
entries: array of exactly 17 { ordinal, path, byteLength, sha256 }
count: 17
totalBytes: 37410731
entriesSha256: SHA-256 of the canonical tuple stream below
```

`ordinal` is integer `1..17`; `path` is the corresponding exact literal above;
`byteLength` is a nonnegative base-10 JSON integer from the same stream read;
`sha256` is exactly 64 lowercase hexadecimal characters. The JSON's whitespace
and object-key order are not identity-bearing. Manifest identity is
`entriesSha256` over this canonical byte stream, in frozen ordinal order:

```text
UTF8(path) + 0x00 + ASCII(decimal byteLength, no sign/leading zero) +
0x00 + ASCII(lowercase sha256) + 0x0A
```

Concatenate all seventeen records with no BOM and no additional bytes, then
SHA-256 that stream and encode lowercase hexadecimal. Each file digest is
SHA-256 over the exact raw file bytes from one opened read-only handle; its
`byteLength` is the number of bytes read from that same handle. Before reading,
Combat must reject any reparse point in any literal path component, resolve the
opened handle inside the exact repository root, prohibit write/delete sharing,
and reject a before/after file-identity or length change. It never decodes,
renders, thumbnails, interprets metadata, or inspects pixels.

The gate fails before generation on a missing/inaccessible/duplicate/unknown/
extra path, ordinal drift, reparse ambiguity, out-of-root resolution, tuple
drift, count other than `17`, total other than `37,410,731`, malformed hash, or
canonical-set digest mismatch. Later media/PBA, build, rollback, and
Intelligence gates recompute the same seventeen tuples and canonical digest.
No mismatch authorizes repair or media mutation.

#### Provenance and workspace delta

At most these two media-owned workspace paths may be introduced:

- `Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png`;
- `Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md`.

Provenance records Work Order/shell; available tool/mode/model identity; prompt
version and explicit no-image-input declaration; attempt ordinals; bounded
pass/fail facts and `PHY-*` rejection codes; managed-root/containment, identity,
and cleanup booleans; selected dimensions, bytes, raw-byte SHA, PNG/color/
metadata/decode results; selected product path; runtime/reference status; the
accepted-media manifest `entriesSha256`; and the statement that accepted media
was neither input nor changed. It contains no pixels, rejected bytes/hash/path,
managed/temp path, prompt secret, credential, or hidden diagnostic.

A rejected candidate never enters workspace, source control, runtime, preview,
browser, report, Markdown, publication, sharing, or reveal. Only bounded scalar
facts survive; exact rejected temp cleanup is mandatory. Never emit an image,
Markdown embedding, contact sheet, preview export, cycle reveal, or publication.

### 11. Ordered validation ladder

**PASS.** The later shell requires, in order:

1. authority/ancestry, bounded diff, protected boundary, game-on-paper hash,
   exact accepted-media manifest/canonical digest, selected digest/bytes,
   provenance, attempt count, containment/identity/cleanup, forbidden-path,
   and no-reveal proof;
2. original-resolution `PHY-01..12`, PNG chunks, dimensions, opacity, color,
   metadata, byte cap, and direct-decode review;
3. machine six-layout source/physical/semantic/label/center/anchor/retention/
   nonoverlap/target geometry and focus-stability proof;
4. focused source guard, pre-mastery absence, Host 05-to-06 order, LOOK/TALK/
   sole USE, duplicate-launcher absence, state, focus, Close/Escape, return,
   reload, malformed evidence, and decode-failure tests;
5. every current model-choice case plus one actual miss, scoped answer-free
   remediation, blank retry, transfer, explanation, ownership/confidence,
   mastery, private-free persistence, and restore;
6. Hosts 04/05, Meadow return, unchanged next boundary, no Host 07,
   Witness/City/later rail, Demo Tour, both MH-40 outcomes, null deltas, and
   `successor=null` regression;
7. exact sorted `40/40` curriculum/readiness validators and cold full suite;
8. production/TD-012 fixture builds, exact JS/CSS/module/media/map PBA,
   served-to-disk identity, offline/same-origin requests, decode/task/CLS;
9. exactly one non-overlapping clean-start-to-MH-40 E2E including Hosts 04-06,
   real `L02-03` miss/recovery/mastery, reload/return, unchanged rail, equal
   outcomes, zero runtime errors, one summary, and one verifier; and
10. exact owned cleanup, protected/opaque residuals untouched, candidate
    freeze, and later push-gate synchronization.

Automated accessibility evidence is not human assistive-technology
certification.

### 12. Exact rollback

**PASS.** Rollback removes only the exact new source, `PROVENANCE.md`, bounded
Host 06 product/config/test/E2E delta, new release-command manifest, and cycle
reports. Pre-existing permitted product/test paths restore by explicit path to
their exact blobs at released product candidate `a91763e...`; no repository
reset or planning-history erasure is allowed. Files first introduced by the
candidate are removed only after exact path and digest proof.

Rollback never touches accepted media/provenance, Hosts 04/05, curriculum,
save schema, dependencies, Host 07+, Witness, City, later rail, Measured
Horizon, user/untracked work, browser/profile/save, protected roots, repository
QA quarantine, a managed file not returned by the exact generation call, or an
opaque residual. Failed managed/temp cleanup remains an opaque stopped
condition and never authorizes parent enumeration or recursive deletion.

## Reused systems, safe flexibility, and hard stops

Reusable systems are the accepted hotspot renderer, canonical `16:9` frame,
sanitized evidence projection, model-choice exercise, Terminal shell,
save/restore, Drowned return, forced-color/reduced-motion system, Demo Tour
isolation, PBA controls, served identity, and complete rail.

Mission may freeze executable filenames, scalar diagnostic names, exact prompt
text/version, and test-command composition only within this envelope. Recon may
choose emotional interpretation; Tactical may choose implementation-neutral
layout details within the fixed geometry; Quartermaster may choose final prose
inside seven slots and select at most one passing attempt. None may alter the
physical relation, manifest literals/schema, state/learning/access/PBA/
provenance/cleanup/no-reveal/rollback boundary.

Any need for more than one canonical source, more than three attempts,
accepted-media input/edit, CLI/API fallback, parent listing, unknown tool path,
cleanup weakening, derivative, second host, lesson/evaluator/save/dependency
change, network runtime, route/world/ending change, reveal, or protected/
residual access is `REVISE` or `HOLD` to the earliest owner.

## Maturity, route, process, and protected state

No maturity cell, host inventory, candidate identity, accepted-media tuple, or
release proof advances. `FR-03` remains `1 accepted shared compression / 1
exact / 10 missing`; the forty-host inventory remains `6 exact / 1 accepted
shared compression / 32 missing / 1 unadvanced Witness expression`.

Hosts 07-15, Witness Host 16, the City contradiction, Hosts 17-38, and the
Measured Horizon literary close remain separate and unadvanced. Learning,
privacy, save/restore, offline/access, Demo Tour, equal READY/NOT YET READY
dignity, null deltas, no world response, shared ending, and `successor=null`
remain exact. Accepted media remains `17 / 37,410,731`.

All thirteen inherited records remain separate and **OPEN**: VR-17, VR-23,
VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The Commandant-stage broad First Run filename/search-
scope record remains one additional separate **OPEN** record. None is waived,
merged, closed, cured, renumbered, or candidate evidence. All prior
process-only recurrences remain recorded.

This Science stage adds no closure. Its initial broad tracked filename locator
and later exact-ID locator outputs are retained as process-only recurrences
under still-OPEN VR-23 and the separate Commandant search-scope record. The
first literal-graph proof used an unavailable path-relativization API, emitted
only a method diagnostic, and was immediately replaced by a bounded string
relative-path operation; this is retained as a process-only diagnostic
recurrence under still-OPEN VR-41. No output from those attempts supplied
media, candidate, canon, maturity, or release evidence. No protected QA file
content or disclosed untracked path was opened or used.

VR-65 remains exactly **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**. Its contents and meaning remain
unknown. No protected QA content, accepted-media byte, user/browser/save path,
or residual was opened, inspected, inferred, retried, cleaned, moved, or
deleted.

## Validation performed and honest limitations

- Verified exact starting `HEAD`
  `4c7c8a0d0d238f07db09871061ec05224fb75266`.
- Read the active authorities, complete Science profile, `FRPB-001-v2`,
  `FRCL-004-v2`, complete `FRWO-005-v2`, complete `FRVE-005-v1`, current map/
  scoreboard, released PBA evidence, runtime instructions, exact package entry,
  and installed built-in image-generation authority/tool contract.
- The requested `foundry-azure-source-priority` skill was unavailable at its
  expected installed path. Used the official Microsoft Learn AI-901 study
  guide directly; its current objectives remain effective April 15, 2026 and
  the page reports last update July 13, 2026.
- Followed `165` source files from the one literal entry through literal
  module/CSS edges and recovered exactly the seventeen frozen media literals.
  This proof did not enumerate directories or read media bytes.
- Ran the focused read-only contract set `modelChoiceExercise.test.js`,
  `sixfoldWeir.test.js`, `canonicalFrame.test.js`, and
  `firstRunPackageContract.test.js`: **30 passed, 0 failed, 0 skipped**;
  Node-reported duration `167.7195 ms`.
- Did not compute the seventeen file hashes or manifest digest; Combat owns
  that pre-generation byte-stream gate. Did not run build, preview, browser,
  E2E, save, media decode, generation, edit, variation, selection, import,
  copy, movement, deletion, publication, reveal, cleanup, schedule,
  automation, push, or release.

## Disposition and exact Mission handoff

**`POLISH VIABILITY READY / FRVE-005-v2`**

One fresh Mission Captain / `mission_captain` must read the active authorities,
complete Mission profile, `FRPB-001-v2`, `FRCL-004-v2`, complete
`FRWO-005-v2`, complete `FRVE-005-v1`, and this complete envelope. Mission
must issue exactly one versioned `FIRST RUN SHELL READY`, `REVISE`, or `HOLD`
contract that incorporates all twelve answers, copies the seventeen manifest
literals exactly, freezes the machine schema/hash semantics and managed-root/
call-result adapter, and preserves every role/canon/learning/state/access/PBA/
provenance/no-reveal/rollback/process boundary.

No Reconnaissance, Tactical, Combat, Quartermaster, Image Specialist,
Intelligence, media attempt, manifest byte read, production, reveal, schedule,
automation, push, release, maturity update, OPEN-record closure, residual
inspection, or `FIRST RUN COMPLETE` begins from this envelope.
