# FIRST RUN WORK ORDER / FRWO-013-v1

## Disposition

`WORK ORDER READY / ROLLING-POOL TERMINAL 13 PASS`

Address: `FR-03 / Host 13 / Tidal Pattern Loom`. Predecessor:
`FRAB-012-v1`. Current physical inventory is one accepted compression, eight
exact encounters, and three missing; target is one additional exact encounter
without promoting the address above `FR0`.

Execute up to eight sequential, one-output imagegen CLI calls allocated from
the rolling pool. Local ordinals are `{H13-1..H13-8}`. The first complete
technical, physical, responsive, and accessibility PASS stops the local ledger;
every unused call remains in the pool for a later full First Run loop. Use
`gpt-image-2`, `3840x2160`, high, opaque PNG, `n=1`, no augmentation,
reference, edit, variation, batch, force, or internal retry. Science and Mission
must freeze the exact prompt set and CLI identity before generation.

Scratch root is the fresh initially absent owned OS-temp directory
`C:\Users\marti\AppData\Local\Temp\horizon-archive-host13-a0952fba-d2cf-41aa-a5a0-2649927367aa`.
Outputs are exact `h13-1.png` through `h13-8.png`. Product path is
`Visual Direction/Production Masters/2026-08-13-first-run-host13/host13-tidal-pattern-loom-master-v1.png`
plus `PROVENANCE.md`.

PASS requires an ordinary nonreparse opaque RGB PNG, exact `3840x2160`, <=30
MB, and strict decode. Physical review must prove one continuous dry waterline
approach; at least three distinct taut mineral-glass filament runs; visible
collars fixing both ends at materially varied depths and angles; at least three
load/strain or repaired-contact relationships; residue/mineral staining that
does not form text; and visible same-basin waterline continuity. Wind may move
or tone the spans, but no synchronized performance, writing, score, console,
portal, kiosk, readable glyph, projected UI, answer, face, person, creature,
ship, or intelligent response may appear.

Responsive proof derives actual `object-fit: cover` projection from measured
source rectangles at desktop, laptop, narrow, effective-200%, `320x180`, and
`320x240`. The complete physical sentence must retain >=95%; every filament,
collar pair, dry approach, loading contact, and waterline center must remain
visible; the target must remain contained and >=44 CSS px; label/focus/
protected overlap must be zero; and real-browser corroboration must pass. No
arbitrary absolute band may override a complete derived-layout PASS.

Rejected evidence records local ordinal, bytes/SHA, technical/physical
booleans, measured rectangles, derived layout scalars, and bounded reasons,
then performs exact identity deletion and absence proof. Accepted-only
integration replaces only the generic `L05-05 + L05-06` launchers with native
LOOK/TALK/USE, focus, recovery, decode, final copy/alt, provenance, and six-
layout evidence. Runtime attempt identity is `H13-n`.

Budgets: accepted media <=24 /170,647,485; JS <=1,772,000; CSS <=121,500;
modules <=235; maps0. Focused, related, validators, full product, clean build,
served identity, isolated real-browser responsive, and one clean-start E2E are
required. Rejected media never enters the workspace.

Accepted Hosts 01-12 and media, lessons, evidence/privacy/save, one route,
RP-012, `successor=null`, protected QA, real browser/save, hidden lore, opaque
residuals, and later hosts are protected. Quartermaster alone may make frozen
calls and import the first passing raster. Image Specialist remains runtime
code/configuration only. Intelligence alone releases and carries unused calls.

Exact next owner: one Office of Science Administrator.
