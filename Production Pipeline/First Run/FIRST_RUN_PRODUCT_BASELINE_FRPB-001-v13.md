# FIRST RUN PRODUCT BASELINE / FRPB-001-v13

## Disposition

`FIRST RUN VISION BASELINE REACTIVATED / MATERIAL REPRESENTATION MIGRATION / COLONEL NEXT`

Commandant stage: `commandant` / `DESIGN -> MIGRATE`.

Human authority: Martin's 2026-08-13 instruction, **“You now have my explicit
approval for a materially different source-production mechanism. Go wild!”**

Source/control commit read:
`795436bcd74acf5be8157655eb97377806eaa4f0`.

Best and committed release: `FRAB-013-v1` at exact candidate
`357ad6dc4184b74150173504e86e366c761cdc0e`.

Current null-first functional candidate: `FRCE-014-v1` at
`b4444afefe624ff231d986933a56c2003c0d8ac5`. The product tree at current HEAD
is byte-identical to that null-first commit and remains disabled/null, so the
released generic path remains the only player-visible path.

Predecessor baseline: `FRPB-001-v12`. Hold evidence:
`FRSH-014-v3-VR-01`. Convergence control: `FRCV-001-v1`.

Martin's instruction fires the existing media-authority and representation
reopen trigger. It does **not** reopen the audience, product category, route,
canon, learning promise, save/offline model, accessibility bar, or shared
ending. The prior prompt-only shell and its ordinal ledger remain closed. This
is a new bounded planning cycle, not H14-5 and not an inheritance of the prior
twenty-eight unused calls.

## Stable audience, promise, and finish line

The intended player remains one local browser player who may be new to Python
and is studying current AI-901 objectives without any exam, certification,
employment, access, or external-authority guarantee. Equivalent action and
meaning across keyboard, pointer, touch, semantic activation, responsive
reflow, effective `200%`, forced color, reduced motion, and assistive semantics
remain delivery requirements rather than mastery evidence.

The product promise remains one connected on-foot expedition through forty
ordered physical encounters and one expedition-owned interface, building real
recoverable Python and AI-901 method before one honest local readiness measure
at the shared Measured Horizon. Failure changes practice time, never story
dignity. The city supplies physical evidence but no answer, judgment,
permission, identity, reward, or response.

`FIRST RUN COMPLETE` still requires independently released whole-game evidence.
This reactivation targets one bounded physical-expression gap only.

## Product truth and preserved checkpoint

- The exact released product remains `FRAB-013-v1@357ad6dc...`; it is the best
  verified and committed state.
- Current HEAD contains the separately tested inert null-first scaffold from
  `FRCE-014-v1`. Its registry source, provenance, copy, geometry, layouts, and
  runtime URL remain disabled/null; generic launchers remain the only active
  player route.
- Git object inspection freshly confirmed current HEAD's product tree
  `066077823b780ba83c5e71e7b478d117f3761dcc` equals the tree at the null-first
  commit. The released product tree remains separately addressable as
  `612a7d53c4564563bfd0bdb7eeca110f715e5c82`.
- No accepted media, source, player copy, lesson, save, route, or maturity state
  changed during this Commandant stage.
- The accepted-media inventory remains `24 / 154,163,567` by reused exact
  predecessor evidence. No media pixels were opened.

## Binding gap and new representation hypothesis

The prior loop established a local representation ceiling, not a global model
ceiling. Prompt-only generation could preserve the required low nonhuman
topology or express constructed-process causality, but did not preserve both
conjunctively. More wording iterations had no independently justified causal
mechanism.

The new planning hypothesis is a guide-first representation migration:

1. deterministically author a code/native structural composition plus
   material-ID guide that encodes the required topology, three distinct
   constructed processes, pairwise interdigitation, one continuous trace and
   its distinct substrate reactions, route/crop anchors, and prohibited zones;
2. use that guide as explicit source evidence for bounded built-in image
   generation and image-to-image/reference edit passes;
3. allow deterministic local compositing, masking, upscale, crop, channel/
   opacity conversion, and output-format normalization when the later shell
   names the exact operation, inputs, outputs, hashes, and no-replace rule;
4. retain immutable intermediates and append-only provenance until acceptance
   or an exact shell-governed cleanup decision; and
5. import only an independently passing final source. No guide, intermediate,
   attractive render, or generator self-assessment counts as acceptance.

This hypothesis creates a materially more editable and verifiable action
surface than natural-language prompt search. It is planning direction, not
Commandant authority to create, inspect, edit, select, or import media.

## Authority and risk boundary

Downstream planning may freeze a bounded mechanism containing deterministic
guide creation, built-in generation, reference/image-to-image editing, and
deterministic local compositing or format normalization. It may use an
accepted-media input only if Science proves exact provenance, non-mutation,
rights, custody, and anti-leak boundaries and Mission names it explicitly.

The following remain hard constraints:

- fixed single pedestrian route, current lesson ownership, and the same
  earliest unresolved physical-expression address;
- no new branch, packet, lesson, hidden-lore answer, prior-human trace,
  protagonist body, reward, access, identity, authority, world response,
  successor, RP-013, or post-ending content;
- Machine and Builders remain silent; Scene, Pilot, Suit/System, and Teacher
  ownership remain exact;
- privacy allowlists, transient clearing, fail-closed save/resume, recoverable
  play, offline operation, no credentials, and no exam guarantee;
- responsive, focus, semantic, forced-color, reduced-motion, performance, PBA,
  provenance, cleanup, rollback, and independent release gates;
- accepted originals remain immutable; deterministic transforms create new
  hashed derivatives and never silently overwrite inputs;
- repository QA/screenshots, Martin's browser/profile/save, hidden lore,
  protected PDFs/training material, rejected/deleted sources, opaque residual
  roots, and unrelated worktree dirt remain forbidden evidence; and
- no encounter name or media path is revealed to Martin.

The final candidate must communicate physical causality without text, glyphs,
human infrastructure, lesson-answer encoding, completion response, or visual
special-casing for the verifier.

## Required planning contract before production

Colonel, Operations, Science, and Mission must complete the normal strict
planning sequence. Before any guide, generation, edit, composite, media
inspection, import, product change, or browser action, the new versioned
`FIRST RUN SHELL READY` contract must freeze:

- one bounded Work Order at the already fixed earliest-first address and the
  exact current/target maturity;
- the exact representation graph: every guide, material mask, reference,
  generator/edit invocation, deterministic transform, candidate, provenance
  record, accepted destination, and cleanup/retention path;
- separate finite budgets for guide families, generation calls, edit/reference
  calls, deterministic media operations, candidate inspections, retained
  intermediates/bytes, wall time, risk, and final verification;
- a protected final-proof reserve that search and aesthetic iteration cannot
  consume;
- exact permitted files, tools, models, sizes, formats, reference inputs,
  process concurrency, create-new/no-replace semantics, cancellation, crash
  recovery, and secret handling;
- hard guide-structure checks; technical custody; independent physical and
  anti-human/anti-message review; responsive/source mapping; accessibility;
  provenance; product/PBA/build/served/E2E/cleanup; and fresh Intelligence
  holdout authority order;
- current, best, and committed references plus deterministic rollback to the
  generic released path;
- anti-Goodhart tests proving that masks, labels, crop anchors, or guide colors
  cannot themselves pass as player-facing physical evidence;
- first full release-quality PASS stop, two-equivalent-failure diagnosis,
  three-same-family-non-improvement replan/return/stop, oscillation detection,
  and low-marginal-value stop rules; and
- exact typed outcomes: `CONTINUE`, `REPLAN`, `RETURN_TO_OWNER`, `ROLLBACK`,
  `HOLD`, `STOP_LOW_MARGINAL_VALUE`, or release handoff.

No prior call count, prompt ordinal, shell, candidate, or rejected output is
revived. Operations/Science/Mission must choose and justify the new finite
budgets from expected information value, cost, and risk under Martin's new
authority.

## Evidence reuse and validation economics

No behavior-affecting product, dependency, test, fixture, or runtime-
configuration input changed after the null-first functional commit. Therefore:

- `31/31` focused, `63/63` related, `1047/1047` cold full-suite,
  `40/40` validator, `234/57` production/fixture build, and null-first PBA
  evidence from `FRCE-014-v1@b4444afe...` are **REUSED**, not rerun or relabeled
  as fresh PASS;
- the independent released-browser/E2E holdout from
  `FRAB-013-v1@357ad6dc...` is **REUSED** only for the committed release and is
  not evidence for a future source-enabled candidate;
- fresh Commandant verification was limited to Git commit/tree identity,
  expected release-to-current product-path delta, null-first-to-HEAD zero
  product delta, and intended-control worktree status; and
- no test, build, validator, browser, E2E, generator, or media tool was run.

Any later rerun must name the verifier-relevant changed input or invalidation.
Control-only handoffs do not invalidate product evidence. The independent final
candidate holdout remains fresh and cannot be reused from the release or
null-first state.

## Risks and controls

| Risk | Required control |
| --- | --- |
| Guide becomes a new weak proxy | Deterministic structure checks are necessary but never sufficient; final physical review judges the rendered source independently |
| Editing launders failed evidence | Immutable inputs/intermediates, complete transform DAG, hashes, no overwrite, and candidate lineage |
| Repeated passes become an art loop | Finite multidimensional budgets, best-state retention, earned continuation, typed plateau stops |
| Aesthetic compositing alters canon | Colonel lock and Mission shell outrank novelty; no message, human trace, response, or answer cue |
| Format normalization becomes hidden repainting | Science freezes allowed deterministic pixel operations and adversarial equivalence checks |
| Expensive tests repeat at each handoff | Content-addressed evidence reuse; smallest affected rung; full suite only after behavior-relevant change and fresh Intelligence holdout |
| Null-first scaffold is mistaken for release | Keep current, best, and committed refs separate; Intelligence alone advances release/maturity |
| Generated or edited media leaks protected inputs | Explicit allowlist, isolated custody, no QA/browser/save/protected-document inputs, exact cleanup |

## Convergence handoff to Colonel

```yaml
convergence_handoff:
  mode: DESIGN_to_MIGRATE
  state_version_read: FRLS-MSN-004-v1
  state_version_written: FRLS-CMD-002-v1
  current_ref: FRCE-014-v1_null_first@b4444afefe624ff231d986933a56c2003c0d8ac5
  best_ref: FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e
  committed_ref: FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e
  source_control_ref: 795436bcd74acf5be8157655eb97377806eaa4f0
  binding_gap_or_hypothesis: >-
    A deterministic structure/material guide followed by bounded reference-
    conditioned render/edit and deterministic transforms can make topology and
    constructed-process causality jointly editable and independently verifiable.
  action_kind: information
  predicted_effect: >-
    Colonel will confirm that the expanded production mechanism changes only
    representation and cannot introduce a route, voice, lore, learning,
    response, or ending contradiction before Operations scopes it.
  verifier_vector:
    human_reactivation_authority: PASS_EXPLICIT
    product_baseline_reopen: MEDIA_AND_REPRESENTATION_ONLY
    released_checkpoint: PRESERVED
    current_null_first_tree_identity: PASS_FRESH_GIT
    current_player_visible_generic_rollback: REUSED_FRCE_014
    focused_related_full_validator_build_pba: REUSED_FRCE_014
    prior_release_holdout: REUSED_FRAB_013_FOR_COMMITTED_RELEASE_ONLY
    media_generation_edit_import: NOT_PERFORMED
    product_or_maturity_change: ZERO
  delta_vs_best: zero_product_zero_media_zero_maturity_positive_authority_and_representation_information
  budget_used:
    generation_calls: 0
    edit_or_reference_calls: 0
    deterministic_media_operations: 0
    media_inspections_or_imports: 0
    product_effect_actions: 0
    test_build_validator_browser_e2e_invocations: 0
  budget_remaining:
    prior_generation_pool: CLOSED_NOT_INHERITED
    new_multidimensional_budget: OPERATIONS_SCIENCE_MISSION_MUST_FREEZE
    final_verification_reserve: REQUIRED_PROTECTED
    role_order: STRICT_SEQUENTIAL
  remaining_uncertainty:
    - Colonel continuity classification of the new representation/action surface
    - Operations Work Order and exact source-production graph
    - Science feasibility, custody, verifier, accessibility, performance, and budget envelope
    - Mission conflict-free shell and exact executable authority
    - future candidate and fresh independent release proof
  decision: GATHER_EVIDENCE
  decision_evidence:
    - Martin explicitly supplied the missing materially-different-mechanism authority
    - the previous hold localized the failure to prompt-only representation
    - guide-first representation is a falsifiable new mechanism, not another wording pass
    - product, media, and maturity checkpoints remain unchanged
  next_owner: colonel
```

Exact Colonel task: read this baseline, `FRSH-014-v3-VR-01`, the current
compact state, game-on-paper, surface-canon/voice/route controls, current
release map/scoreboard, and exact runtime evidence. Issue one versioned
`CONTINUITY LOCK`, `REVISE`, or `HOLD` for the representation migration. Lock
which visual/physical facts a deterministic guide and later edits may encode,
which facts remain unknown, and every prohibited human/message/answer/world-
response inference. Do not select the Work Order, freeze a tool or budget,
create/inspect/edit/import media, run product tests, or modify runtime.
