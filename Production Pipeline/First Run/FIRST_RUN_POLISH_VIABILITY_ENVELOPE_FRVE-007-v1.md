# Horizon Archive First Run Polish Viability Envelope - Host 07 Built-In Custody

Envelope ID: `FRVE-007-v1`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-007-v1`

Disposition: **`HOLD / BUILT-IN GENERATED-OUTPUT IDENTITY AND PROJECT-CUSTODY
ADAPTER UNAVAILABLE / ZERO HOST 07 ATTEMPTS CONSUMED / MARTIN DECISION
REQUIRED`**

Date: **2026-08-12**

Control source inspected: `5a6a63c8`

Product baseline / continuity: `FRPB-001-v3` / `FRCL-006-v1`

Released predecessor: `FRAB-005-v1 / Host 06 Stranded Lens Cradle`

Exact next owner: **Martin**

## Science decision

`FRWO-007-v1` is technically and educationally viable except for one decisive
pre-call primitive: the current built-in `image_gen` boundary does not provide
a trusted project-materialization adapter.

The installed image-generation skill says built-in mode saves under managed
`$CODEX_HOME/*` storage and that project-bound output must later be copied or
moved into the workspace. It also explicitly says not to rely on a destination
argument. The callable tool returns a renderable result and optional output
hint, but exposes no same-call filesystem write, exact returned ordinary-file
handle, guaranteed direct absolute source pathname, create-new product copy, or
identity-conditioned rejected-output delete primitive.

That gap is not hypothetical on this exact project/tool family. The retained
Host 06 built-in attempt in `FRCA-005-v1` returned a multiline non-path
`output_hint`; `FRCA-005-v2` then proved the same-isolate full-payload
materialization adapter unavailable. `FRVE-005-v4` proved that listing the
managed parent and choosing a new same-owner/time-window file cannot establish
which process created it. Those findings remain applicable because the current
built-in callable contract has not gained a trusted path/handle or filesystem
materialization primitive.

Calling now would risk exactly the outcomes the Work Order forbids:

- a generated result whose managed file cannot be creator-bound, privately
  inspected, byte-identified, or imported with provable identity;
- a rejected result that cannot be exactly located and disposed without parent
  discovery or unrelated-file risk; and
- an opaque managed residual while consuming one of Martin's two attempts.

Science therefore stops before `H7-A`. No attempt, tool call, generated result,
managed output, candidate, raster, provenance file, or opaque Host 07 residual
exists. `H7-A` and `H7-B` are both unconsumed and unavailable.

The built-in skill provides exactly one lawful alternative: an explicit user
decision to use its CLI fallback. That fallback requires `OPENAI_API_KEY` and a
new planning/shell correction. The prior Host 06 CLI/API authorization was
Host-06-bounded and does not transfer to Host 07.

Variance classification: **`REQUIRED CORRECTION / HOST 07 GENERATION
TRANSPORT AUTHORITY / EARLIEST OWNER MARTIN / GATING`**.

## Exact Work Order question answers

### 1. Built-in result custody

**FAIL / GATING.** A renderable tool result is not the same as a trusted
filesystem identity. No current primitive proves exact creator-bound path,
ordinary-file identity, byte-preserving candidate custody, create-new product
copy, and exact rejected-output deletion without managed-parent discovery or a
second full-payload transport.

### 2. Source envelope

**PROSPECTIVELY VIABLE, NOT ACTIVATED.** A future lawfully materialized source
must be one local opaque raster, landscape `16:9`, at least `1536x864` and at
most `3840x2160`, `1..30,000,000` bytes, strict decodable RGB/RGBA with no
animation, embedded executable content, external reference, or metadata needed
at runtime. The eventual accepted aggregate must remain at most
`83,554,983` bytes (`53,554,983` accepted predecessor plus the 30 MB single-
source ceiling). Exact format, dimensions, color mode, alpha/opacity, length,
and SHA-256 must be recorded from the selected byte stream before import.

### 3. Physical, semantic, and six-layout measurements

**PROSPECTIVELY VIABLE, CANDIDATE VALUES NULL.** A future candidate must record
normalized source rectangles for:

- complete distributed relation;
- physical center;
- semantic target;
- factual label anchor;
- Host 06 graded-sediment handoff;
- dry approach/observation area;
- each of at least three distinct channel-depth bands;
- complete nodule group; and
- protected Host 04-06/water/Crown/Tidal-Lens/return/later-cue regions.

All candidate-specific rectangles are `null` today. Acceptance requires the
complete relation and center retained in each of the established six layout
classes, physical center inside the semantic target, semantic target at least
`44x44 CSS px`, label fully contained, zero protected overlap, and at least
`8 CSS px` separation between label/focus treatment and every protected region.
The complete relation may not rely on an off-crop fragment or depth band.

### 4. Attempt ledger, prompts, provenance, and rollback

**LEDGER VIABLE; CUSTODY BLOCKED.** Domain remains `{H7-A,H7-B}`. Two distinct
prompts must be frozen before `H7-A`; neither may use an image input. `H7-B`
requires exact terminal `H7-A` disposition and cleanup. A passing `H7-A` ends
generation. No third call/fallback/edit/variation/retry exists. Rejected
outputs have zero authority. Selected import must be create-new/no-replace and
byte-identical to its identity-bound candidate; provenance must contain no
secret or rejected-output details. Current product paths remain absent.

### 5. Null-first state/source guard

**VIABLE.** Combat may construct an inert Host 07 registry whose enabled flag,
source URL, attempt ID, byte/SHA provenance, decode state, physical/semantic/
layout measurements, final copy, and alt are null/false. The released generic
continuation remains exact only while Host 07 has never been enabled. Once
accepted-only enablement becomes true, any missing/mismatched source,
provenance, decode, or measurement must fail closed rather than restore the
legacy launcher. Exact Host 06 remains independently lawful and unchanged.

### 6. Learning reuse

**PASS.** Focused current tests prove Structured Packets, Control Flow,
model-choice, and Host 06 reuse:

```text
27 passed / 0 failed / 0 skipped
duration=108.2167ms
```

`L03-01` and `L03-02` may retain their current strict primary/transfer/
explanation, remediation, sanitization, evidence, and no-cross-credit rules.
The physical host and image grant zero evidence.

### 7. Accessibility, performance, and validation budgets

**PROSPECTIVELY VIABLE.** A future corrected shell must require:

- equivalent keyboard/pointer/touch/switch-like activation, truthful semantic
  name, visible focus, one bounded announcement, deterministic return/reload/
  resume, reflow/effective `200%`, forced-color, and reduced-motion behavior;
- attributable CLS `<=0.01`, whole-view CLS `<=0.05`, activation handler
  `<=2ms`, local ready-to-interactive `<=750ms`, selected-source cold decode
  `<=300ms` and warm decode `<=120ms` on the validation host;
- affected JS `<=1,692,160`, global JS `<=1,716,000`, CSS `<=119,700`, modules
  `<=220`, maps `0`, no new dependency/lockfile change, and media within the
  single/aggregate caps above;
- focused tests `<=30s`, related `<=60s`, validators/full `<=60s` each,
  production and fixture builds `<=60s` each, and isolated E2E `<=180s`;
- served root/deep/JS/CSS/media/fixture identity, representative six-layout/
  keyboard/focus/forced-color/reduced-motion review, owned-process shutdown,
  port clearance, exact output cleanup, and Intelligence reconciliation.

These ceilings are readiness constraints, not current candidate evidence.

### 8. Protected and process boundaries

**PASS.** Every OPEN record, both filename-enumeration records, and VR-65 remain
separate and unchanged. Science used only literal tracked authorities and
tracked-only Git status. It did not list or inspect managed generated-image
storage, protected QA, PDF, training, browser/profile/save, hidden lore, prior
attempt roots, or residuals.

## State, evidence, privacy, save, and offline contract

The future state graph remains hidden -> available -> in progress ->
remediation required or complete, with exact Host 06 + `L02-03` mastery as the
entry predecessor. LOOK is physical/write-free; TALK is silent; sole USE owns
`L03-01`, then exact `L03-02`; completed USE is read-only. Observation,
candidate review, media selection, focus, and presentation mint no evidence.

Existing sanitizer, allowlist, strict thresholds, confidence/ownership,
anti-forgery, local remediation, fresh transfer, Demo Tour isolation, no-
cross-credit, save/recovery, malformed-state fail-close, offline/no-authority,
and no-exam-guarantee behavior remain exact. No new credential, runtime
network, telemetry, external authority, personal data, or remote asset is
allowed.

## Product paths and accepted-media disposition

Both planned product paths are independently absent:

```text
Visual Direction/Production Masters/2026-08-12-first-run-host07/
  host07-sediment-abacus-master-v1.png  absent
  PROVENANCE.md                         absent
```

Accepted media remains `18 / 53,554,983` bytes. No accepted byte or pixel was
read or changed by this stage. No Host 07 source, prompt, geometry, semantic
target, label, layout, provenance, runtime state, or copy is populated.

## Hard stop and exact Martin choices

Martin must choose one mutually exclusive path:

1. **Authorize imagegen CLI fallback for Host 07's same exact two-attempt
   ceiling.** This requires `OPENAI_API_KEY` to be set locally and routes a
   fresh Commandant/Operations correction before Science and Mission freeze
   explicit output paths and the new shell. It does not itself consume an
   attempt.
2. **Keep Host 07 at HOLD.** Both built-in attempts remain unconsumed; no
   generation or production follows.

General continuation does not select the CLI fallback. Do not call built-in
generation in the hope that this invocation returns a better path hint; the
retained exact tool evidence already disproves that as a safe project custody
contract.

No Mission stage follows this HOLD.

Science signs **`HOLD / FRVE-007-v1 / BUILT-IN PROJECT-CUSTODY ADAPTER
UNAVAILABLE / H7-A AND H7-B UNCONSUMED / MARTIN DECISION REQUIRED`**.
