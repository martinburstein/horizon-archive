# Horizon Archive First Run Polish Viability Envelope - Host 08

Envelope ID: `FRVE-008-v1`

Stage / stable agent: Office of Science Administrator / `office_of_science_administrator`

Work Order: `FRWO-008-v1`

Disposition: **`POLISH VIABILITY READY / HOST 08 SEVERED RELAY SPINE / FIVE CLI ATTEMPTS AVAILABLE`**

Date: **2026-08-12**

Exact next owner: **one fresh Mission Captain / `mission_captain`**

## Decision

Science accepts the bounded Host 08 CLI plan. The installed generator is exact:
`image_gen.py`, `35,266` bytes, SHA-256
`c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05`.
The host has Python `3.12.10`, OpenAI SDK `3.0.0`, and a nonempty API-key
boolean. An exact `gpt-image-2`, `n=1`, `3840x2160`, high-quality opaque-PNG
dry run exited `0` and created no output. Science made no live request, read no
credential value, generated no image, and reviewed no media.

The attempt domain is exactly `{H8-1,H8-2,H8-3,H8-4,H8-5}`. Calls must remain
sequential and independent, with no input/reference image, edit, variation,
batch, alternate runner, script mutation, or sixth call. A technical failure
stops. An objective rejection enables the next ordinal only after exact
identity-conditioned deletion and absence proof. The first PASS ends all
generation.

## Source and custody envelope

Mission must freeze one fresh absent GUID child of the OS temporary directory
and the five literal ordinary-file children `h8-1.png` through `h8-5.png`.
Before each call, the root and all current/later output paths must have the
shell-required identity and absence state. Every returned candidate must be a
single ordinary, non-reparse, one-link file created at its exact predeclared
path. Candidate custody is strict pathname plus volume/file identity, length,
and lowercase SHA-256 before and after private review.

A lawful candidate is a static PNG with signature/chunk/CRC/IHDR validation,
exactly `3840x2160`, RGB or RGBA, fully opaque, one image, no external
reference, and `1..30,000,000` bytes. A passing source may enter only the exact
new Host 08 product path with create-new/no-replace byte identity and bounded
provenance. Accepted media must remain at most `19 / 83,320,925` bytes. A
rejected candidate has zero canon/reuse/reveal authority and must be deleted by
its exact re-proved identity. The empty scratch root must be deleted
nonrecursively after termination.

## Physical and composition acceptance

Each private candidate record must contain normalized source rectangles for:

- the complete collapsed laminate relation;
- the externally ending fracture continuity;
- the distinct materially connected pressure-bearing continuity;
- the Host 07 interrupted sediment/channel handoff;
- the stable dry outer-ridge approach;
- the damaged exclusion volume;
- the semantic target, label, and protected regions.

The complete relation must be centrally crop-safe: bounds inside source
`x=.16..84`, `y=.14..86`, width `.38..66`, height `.28..58`, with physical
center inside `x=.42..58`, `y=.38..62`. Each continuity must remain visibly
distinct and span at least `.10` source width outside its shared material
connection; the fracture end must occupy at least `.05 x .05`. The dry
approach must span at least `.12` source width, visibly connect to the
observation side, and have zero overlap with the damaged exclusion volume.
The Host 07 handoff must span at least `.08` source width without becoming a
duplicate abacus.

Across desktop, laptop, narrow, effective-200%, `320x180`, and `320x240`
centered-cover layouts, at least `95%` of the complete relation must remain;
both continuity centers, fracture end, dry approach, and physical center must
remain visible. The native semantic target must be at least `44x44 CSS px`,
contain the physical center, and have zero overlap with damage, water, Host 07,
return route, Crown, or later-host cues. The label must remain contained with
at least `8 CSS px` focus/protected separation. Forced-color and reduced-motion
behavior must remain truthful. Any ambiguity is objective rejection.

## Runtime, learning, and privacy

Combat may build Host 08 only as null-first/fail-closed registry and UI state.
Exact released Host 07 plus strict `L03-01` and `L03-02` mastery is the sole
predecessor. The current generic Client Bridge launcher remains only before
Host 08 is ever enabled. Accepted-only enablement removes that launcher
irreversibly; a later source/provenance/decode/measurement failure hides Host
08 and never restores a bypass.

Host 08 states are hidden, available, in progress, remediation required, and
complete, derived only from sanitized strict Client Bridge evidence. LOOK is
physical and write-free, TALK is silent, and sole USE owns unchanged `L03-03`.
Completed USE is read-only. After `L03-03`, the existing generic Text Analysis
continuation remains unchanged; Host 09 remains absent. The image, LOOK,
selection, focus, and presentation mint no evidence, reward, access,
authority, identity, telemetry, credential, or world response.

Focused predecessor/Client-Bridge tests pass `22/22` in `301.7896ms`.

## Performance and validation envelope

Final ceilings are affected/global JavaScript `<=1,702,345 / 1,726,000`, CSS
`<=119,900`, modules `<=222`, maps `0`, and no dependency/lockfile change.
Selected-source decode must be `<=300ms` cold and `<=120ms` warm; interaction
handler `<=2ms`; attributable/whole-view CLS `<=.01/.05`; full clean-start E2E
`<180s`.

The validation ladder is authority/path/ledger preflight; focused Host 07,
Host 08, and Client Bridge tests; route/save/privacy/accessibility/ending
regressions; validators and full tests; production and TD-012 builds; PBA and
media inventory; served root/deep/JS/CSS/media/fixture identity; six-layout,
keyboard, focus, forced-color, and reduced-motion review; then one isolated
clean-start E2E through Host 08 and MH-40. E2E must use a newly created,
explicit external QA root outside the repository and fail before browser start
if that root is absent, inside the repository, reparse, nonempty, or cannot be
identity-cleaned afterward. Repository QA quarantine is never a target.

Rollback removes only exact Host 08 additions and shell-owned scratch. It may
not touch Hosts 01-07, accepted media, route, lessons, saves, ending, protected
state, or opaque residuals.

## Preserved boundaries

The OPEN protected-QA capture-write variance, both earlier filename-enumeration
records, every other OPEN record, and VR-65 remain separate and unresolved.
Science used tracked authorities and literal paths only; it did not inspect,
restore, stage, or use protected QA screenshots, PDF/training material,
browser/profile/save, hidden lore, managed-image storage, or residual roots.

Science signs **`POLISH VIABILITY READY / FRVE-008-v1 / ROUTE FRESH MISSION / ZERO ATTEMPTS CONSUMED`**.
