# First Run Shell Variance Reissue - One Fresh Contained Corrected Diagnostic-Control Verification

Variance ID: `FRSH-003-v1-VR-49`

Disposition: **`FIRST RUN SHELL READY / ONE FRESH CONTAINED CORRECTED
DIAGNOSTIC-CONTROL VERIFICATION / FRSH-003-v1-VR-49`**

Stage gate remains: **`HOLD / PRODUCTION FUNCTIONAL NOT ISSUED`**

Release state remains: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Stage / stable agent: Mission Captain / `mission_captain`

Mission source inspected:
`f8219e44cfb798bc7243289f584ef85d6ea7157a`

Geometry correction / diagnostic transport / report lineage:

```text
8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97
6c64eb354b7dbb467df5725e2cae4eb67092ddc7
0ac9023037873004f7bd5d75c16f80953d770a4d
```

Exact immutable product / validation / diagnostic / evidence identities:

```text
a91763e28d488f31f8cf7d40ece0b2682246ba9b
4cd7fbf31291671dd28c0743b44a7c49aaad82bb
2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
ca89a679195c11d441a76e6c02983a6436f2ccb2
```

Recorded: **2026-08-10**

## Context reuse and VR-48 adjudication

Fresh-child capacity remains unavailable. Root expressly authorized this
Mission Captain context to adjudicate VR-48 and issue the next bounded shell.
The reuse is disclosed, is not candidate evidence, and waives no boundary.

Mission independently accepts the returned VR-48 scalar as **`ATOMIC
FRESH-ROOT CONTROL PASS / ROOT DELETED / NO LIVE ACTION`**. The scalar proves:

- OS temp and repository normalization succeeded;
- the generated lowercase GUID leaf shape, direct parent, strict temp
  descendant, bidirectional repository exclusion, predecessor distinction,
  and pre-creation nonexistence checks all passed;
- exactly one `New-Item -ItemType Directory -Path ... -ErrorAction Stop`
  creation was attempted and succeeded;
- the same-call post-create resolve matched the candidate exactly and retained
  the proven parent/containment identity;
- exactly one empty-root cleanup succeeded and the root was absent afterward;
  and
- `nativeExit=0`.

No preview, browser, E2E, extractor, summary, verifier, build, test, validator,
PBA, served-identity, product, media, or protected action occurred. VR-48 is a
root-control finding only and consumed no live or diagnostic-run budget. Its
authority is exhausted and may not be reused.

The VR-47 **`REQUIRED CORRECTION / EXECUTION CONTROL / PRE-ROOT SCALAR
TRANSPORT / OPEN`** classification remains OPEN; the successful localization
does not silently close or erase the recorded divergence.

## Cumulative accepted controls

The following immutable gates remain accepted without rerun:

- VR-22 focused 68, related 74, full 972, and validator 40 controls;
- VR-30 production build;
- VR-35 fixture build and corrected PBA: one module, one built asset, zero
  native assets, JS `1667393`, CSS `119247`, media 17 / `37410731`, maps 0,
  scan 0;
- VR-42 formal production/fixture served identity;
- VR-44 transport control JSON/Node/focused-68 proof; and
- VR-46 exact two-file live-DOM epoch correction Node/focused-68/integrity
  proof for candidate `8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97`.

VR-45's 114 geometry paths remain diagnostic evidence of the superseded
evidence-source epoch. They are not product/layout findings and are not
verifier input.

## Exact one-run authority

Exact next owner is a **fresh Combat Engineer** if capacity permits; otherwise
context reuse must be disclosed. Perform one contained run, in this order,
with scalar-only control output and no pathname, body, browser, directory,
process-object, exception, or repository-enumeration output.

### 1. Fresh root creation and retained identity

Use one small atomic PowerShell call before any port or live action:

1. Apply the exact VR-48 normalization, lowercase GUID leaf, direct-parent,
   strict-temp-descendant, bidirectional repository-exclusion, predecessor-
   distinction, and nonexistence predicates.
2. Create exactly once with `New-Item -ItemType Directory -Path $candidate
   -ErrorAction Stop`, suppressing the returned object.
3. In that same call, resolve with `Resolve-Path -LiteralPath -ErrorAction
   Stop`; require exact candidate equality and re-prove every parent,
   containment, exclusion, predecessor, leaf, and existence predicate.
4. Retain the exact resolved root for the run. Do not delete it at this stage.
   Emit one ordered root-control scalar and continue only on the exact VR-48
   PASS predicate, except `cleanupAttemptCount=0` and `rootDeleted=0` are
   expected because the proven root is intentionally retained.

Any root-control failure requires one `finally` cleanup attempt only if the
candidate was lawfully created and its exact identity can be revalidated.
Then stop without querying ports or starting live work. Do not generate a
second candidate or retry creation.

### 2. Owned preview lifecycle

Require exact ports 4173 and 4184 clear. Start the production preview on 4173
and the fixture preview on 4184 once each, hidden and with exact `PassThru`
ownership. Use the previously proven direct Node/Vite launcher form. Require
the returned processes to be live and require bounded, suppressed
`Invoke-WebRequest -UseBasicParsing` root readiness HTTP 200 for both.

Do not perform deep-route, asset, byte-identity, PBA, build, validator, or test
work; the accepted VR-42 served-identity and cumulative controls are inherited.
If either launch, ownership, or readiness predicate fails, capture scalar
stage evidence, perform exact cleanup, and stop without E2E.

### 3. Exactly one corrected complete E2E

Against the retained fresh root and owned previews, invoke exactly once:

```text
node playtest/e2e-playthrough.mjs
```

Use the FRRC-002 environment contract, the immutable product identity
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`, validation identity
`4cd7fbf31291671dd28c0743b44a7c49aaad82bb`, diagnostic/probe identity
`2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc`, geometry correction
`8aa1dcaf2d5a347cc21ec48a4d6022485fe7cd97`, transport candidate
`6c64eb354b7dbb467df5725e2cae4eb67092ddc7`, the exact retained QA root, and
the frozen PBA flags. Apply the existing 180000 ms E2E timeout. Do not pipe,
truncate, duplicate, or asynchronously defer diagnostic evidence.

This is exactly one complete E2E invocation. No retry, repair, alternate
layout subset, extra browser, second evidence pass, or verifier-before-gate is
permitted.

### 4. Diagnostic gate and mutually exclusive outcome

Synchronously read exactly one diagnostic after the E2E process ends and
before root cleanup. Require the diagnostic inventory to be exact and capture:

```text
checkInventoryExact=<0|1>
failureCount=<integer>
browserClosed=<0|1>
```

If E2E exit is nonzero, inventory is not exact, `failureCount` is positive, or
`browserClosed` is not true:

1. do not produce a release summary;
2. invoke the accepted diagnostic transport exactly once with its 60000 ms
   timeout while the retained root still exists;
3. synchronously capture and emit the full canonical compact sorted
   `failurePaths` and `failuresByLayout` as safe base64, plus lifecycle scalars,
   before cleanup;
4. do not invoke the verifier; and
5. return **`HOLD / DIAGNOSTIC FAILURE / FULL TRANSPORT CAPTURED / NO VERIFIER
   / NO RERUN`** after cleanup.

A tool-policy rejection or failure before E2E execution is execution-control
evidence only; it does not spend the one E2E invocation. Nevertheless this
shell is exhausted and no same-context reformulation or rerun is authorized.
If E2E begins or returns, the one-run budget is consumed regardless of result.

Only when E2E exit is 0, `checkInventoryExact=true`, `failureCount=0`, and
`browserClosed=true` may Combat emit exactly one complete summary and invoke
exactly one verifier. The summary must contain all six layouts, the full rail,
both MH-40 outcomes, null-delta/runtime/offline/performance controls, and all
frozen accessibility/save/route/ending meanings. Exact success additionally
requires exactly one verifier PASS. Diagnostic evidence itself remains
forbidden verifier input.

### 5. Unconditional exact cleanup

In `finally`, stop only the two owned preview PIDs, require ports 4173 and 4184
clear, and delete only the retained root after revalidating exact identity,
direct parent, strict temp containment, bidirectional repository exclusion,
predecessor distinction, and leaf shape. No alternate, inferred, unresolved,
repository, or predecessor path may be targeted. Require root absence.

Emit exactly one final scalar covering root-control PASS, launch/readiness
counts, E2E attempt/exit, diagnostic inventory/failure/browser lifecycle,
transport attempt/result when applicable, summary count, verifier attempt/PASS
count, owned-PID cleanup, port-clear count, root cleanup, and native exit.

Exact success is **`ONE COMPLETE CORRECTED E2E PASS / DIAGNOSTIC INVENTORY
EXACT / FAILURE 0 / BROWSER CLOSED / SUMMARY 1 / VERIFIER 1 PASS / EXACT
CLEANUP / STOP / RETURN TO FRESH MISSION`**. Combat may not declare
`PRODUCTION FUNCTIONAL`, advance maturity, or authorize a downstream stage.

## Preserved boundaries and OPEN classifications

No product, content, media, asset, lore, curriculum, save, route, ending,
accessibility, privacy, or release change is authorized. Every frozen product,
player, learning, privacy, save, accessibility, route, world, MH-40,
null-delta, `successor=null`, ending, media, and one-E2E meaning remains exact.

All nine classifications remain separate and OPEN:

1. `UNAUTHORIZED DIVERGENCE / PROTECTED PATH ENUMERATION / OPEN / VR-17`;
2. `UNAUTHORIZED DIVERGENCE / BROAD REPOSITORY FILENAME ENUMERATION / OPEN /
   VR-23`;
3. `UNAUTHORIZED DIVERGENCE / UNTRACKED PATHNAME ENUMERATION / OPEN / VR-24
   COMBAT ATTEMPT`;
4. `UNAUTHORIZED DIVERGENCE / LITERAL CONTROL PATHNAME OUTPUT / OPEN / VR-25
   MISSION`;
5. `UNAUTHORIZED DIVERGENCE / LITERAL CONTROL PATHNAME OUTPUT / OPEN / VR-28
   MISSION STAGING`;
6. `UNAUTHORIZED DIVERGENCE / EXACT-PORT LISTENER OWNERSHIP RECOVERY / OPEN /
   VR-39`;
7. `UNAUTHORIZED DIVERGENCE / PARSER DIAGNOSTIC SCALAR OUTPUT / OPEN / VR-41`;
8. `UNAUTHORIZED DIVERGENCE / VALIDATION COMMAND SELECTION / OPEN / VR-46
   COMBAT`; and
9. `REQUIRED CORRECTION / EXECUTION CONTROL / PRE-ROOT SCALAR TRANSPORT / OPEN
   / VR-47`.

None is waived, merged, closed, cured, or admitted as candidate evidence by
this shell. Diagnostic evidence remains non-release, forbidden verifier input,
and no-retry. No downstream stage or release action is authorized.

Mission Captain signs **`FIRST RUN SHELL READY / ONE FRESH CONTAINED CORRECTED
DIAGNOSTIC-CONTROL VERIFICATION / FRSH-003-v1-VR-49`**.
