# First Run Shell Variance - Exact Quartermaster Retry Authorization

Variance ID: `FRSH-005-v1-VR-10`

Disposition: **`FIRST RUN SHELL READY / SCIENCE PRE-HELPER PASS ACCEPTED /
EXACTLY ONE FRESH QUARTERMASTER RETRY AUTHORIZED FROM ORDINAL 2 /
FRSH-005-v1-VR-10`**

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / decisive Science return: `FRWO-005-v7` /
`FRVE-005-v7-VR-08`

Quartermaster return: `FRCA-005-v3`

Effective prior shell: `FRSH-005-v1` through `FRSH-005-v1-VR-09`

Effective treatment / blueprint: `FRDT-005-v1-VR-01` /
`FRPX-005-v1-VR-01`

Functional report: `FRCE-005-v1-VR-05`

Mission source inspected:
`de2cb62c355a26204ef5d8f9f43254c34d1dd48f`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Date: **2026-08-11**

Exact next recipient: one fresh Quartermaster / `quartermaster`

## Mission decision

Mission independently accepts `FRVE-005-v7-VR-08`. Science positively proved
the complete exact `FRSH-005-v1-VR-09` pre-helper fixture envelope: both
frozen byte identities, compact current-shell parent, collision-safe parent
namespace, bounded executable-plus-argument rendering, exact PS5.1 version and
bitness, ordered `PH01..PH08`, every declared first-failure branch, bounded
diagnostic normalization, identity-conditioned cleanup, complete exact-path
absence, and zero credential/request/API/ordinal activity.

The Science parent used exact `$prehelperIdentity`; no `$pid` or case variant
of the read-only automatic `$PID` variable was declared or assigned. Its
conservative executable-plus-argv rendering was exact `2,156` characters,
below both `8,191` and `32,767`. Native stderr and `$LASTEXITCODE` were
captured in memory with `$ErrorActionPreference='Continue'` scoped only around
each native invocation and the prior preference restored immediately.

The `FRCA-005-v3` pre-helper failure is therefore repaired at its earliest
shell owner without replaying, reinterpreting, or weakening the historical
failure. Mission issues **`FIRST RUN SHELL READY`** and authorizes exactly one
fresh Quartermaster retry. The retry begins with ordinal `2`; ordinal `3`
exists only under the unchanged conditional rule below.

This variance supersedes only the `FRESH SCIENCE REQUIRED` stop in
`FRSH-005-v1-VR-09` and the Science-return routing in `FRCA-005-v3`. It changes
no launcher, pre-helper, parent, helper, API, ordinal, parser, staging,
identity, cleanup, media, physical, crop/mapping, accessibility, canon,
lesson, privacy, save, offline, performance, PBA, provenance, no-reveal,
rollback, protected-state, maturity, or OPEN-record clause in the effective
shell.

Variance classification: **`REQUIRED CORRECTION RESOLVED / EXACT
PRE-HELPER EXECUTION CONTROL POSITIVELY PROVED / QUARTERMASTER RETRY
AUTHORIZED`**.

## Exact retry shell and ordinals

One fresh Quartermaster begins from the synchronized commit containing this
variance and performs exactly one bounded production action under the
effective shell. The production sequence uses the authorized compact current-
shell parent and exactly one bounded 64-bit Windows PowerShell
`5.1.26100.8875` child sequence. Every task-local parent name is checked
case-insensitively against PowerShell automatic, preference, constant,
read-only, and common system names before child entry. The pre-helper identity
object is exact `$prehelperIdentity`; `$pid` and every case variant of `$PID`
are forbidden.

Before the child begins, the parent conservatively renders the exact
executable plus exact argument array and requires the result to remain safely
below both `8,191` and `32,767` characters. It may not assume the Science
measurement in place of its own preflight. Around only the exact native child
invocation, the parent saves the prior `$ErrorActionPreference`, sets it to
`Continue`, captures stderr and `$LASTEXITCODE` in memory, and restores the
prior preference immediately in `finally`. No suppression, discard, file
redirection, transcript, or alternate capture is allowed.

The ordinal ledger is exact:

- historical ordinal `1` remains consumed, opaque, inaccessible, unchanged,
  and ineligible for retry, recovery, discovery, inspection, or cleanup;
- ordinal `2` is unstarted and unconsumed and is the sole first live request;
- ordinal `3` is unstarted, conditional, and unconsumed;
- direct Image API sends at Mission handoff remain exact `0`;
- maximum future sends remain at most `2`, sequential, with one request and
  one output per ordinal; and
- there is no retry, ordinal `4`, replacement, parallel call, alternate
  transport, or fallback.

Ordinal `3` is authorized only if ordinal `2` becomes a fully identified
**objective source rejection** after final-target handle identity, complete
technical gate, original-resolution physical and `PHY-01..12` review, exact
source-band and six-layout crop/mapping/accessibility review, and exact
identity-conditioned target/root cleanup. An API, transport, non-200,
response, parser, base64, file, handle, identity, substitution, link, reparse,
move, destination-race, or cleanup failure after `SendAsync` consumes the
active ordinal and is terminal; it never authorizes ordinal `3`. Pre-helper or
helper failure consumes no ordinal and is also terminal. No failure authorizes
a retry in the same or later heartbeat.

## Frozen launcher, pre-helper, and bounded diagnostic contract

Transport contract ID remains `HOST06-PREHELPER-PS51-v1`.

The sole inherited process-environment name remains exact:

```text
HORIZON_ARCHIVE_HOST06_PREHELPER_V1
```

Its value remains the exact pre-helper script frozen in
`FRSH-005-v1-VR-09`: UTF-8 without BOM, LF-only including final LF, exact
`976` bytes and SHA-256:

```text
5cd257c94bcd70b8d6ada4e0b561b2a14ed52fd9459146b1269dc93ce1bdc7d1
```

The exact short launcher remains UTF-8 without BOM, LF-only including final
LF, exact `2,001` bytes and SHA-256:

```text
96feaf7e62fa89e8c80cc46d38425d465cf845ffbd426405a75c73c056314212
```

It remains the sole `-Command` value to exact
`C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe` with arguments
`-NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command` in that
order. No encoded command, stdin, source file, profile, nested shell,
alternate environment name, interpolation, or second transport is permitted.

The stable predicates remain exact and ordered:

```text
PH01_ENV_RETRIEVAL
PH02_PARSE_SUCCESS
PH03_INVOCATION_ENTRY
PH04_PS51_VERSION
PH05_X64_PROCESS
PH06_ROOT_ABSENT
PH07_ROOT_CREATE_ENTRY
PH08_ROOT_CREATE_COMPLETE
```

The sole root-creation primitive remains exact
`[System.IO.Directory]::CreateDirectory($helperRoot)`. `New-Item`, `mkdir`,
`md`, `cmd`, another shell, recursive creation/deletion wrappers, and alternate
roots remain forbidden. Root deletion remains only exact
`[System.IO.Directory]::Delete($helperRoot,$false)` after this process created
the root, proved it ordinary/non-reparse, and proved the exact DLL absent.
There is no parent or sibling enumeration.

Pre-helper success is silent. A pre-helper failure emits only one ASCII stderr
line beginning `HOST06_PREHELPER_FAILURE|` with the exact frozen field order,
eight-predicate allowlist, class/FQID allowlists, and lowercase helper-root,
helper-DLL, and live-root absence booleans. Non-allowlisted class/FQID values
become literal `UNLISTED`. Any zero, multiple, missing, extra, reordered,
unknown-predicate, multiline, non-ASCII, or over-`640`-byte record becomes only
`STABLE_LOCAL_FAILURE / diagnosticRecordRejected=true` plus freshly observed
exact absence booleans. The exact `640`-byte boundary remains accepted; `641`
bytes remains rejected.

No message, stack, source excerpt, command, path, native tuple, timestamp,
username, process detail, key, header, request/response, JSON, base64, media,
payload, or opaque value may be retained. The earliest predicate alone
survives. Failure is terminal, consumes no ordinal, and authorizes no retry.
Cleanup after root creation remains nonrecursive, exact-root-only, and
conditioned on process ownership, ordinary/non-reparse identity, emptiness,
and exact DLL absence. Cleanup uncertainty is `HOLD`.

## Frozen helper, API, media, and product contract

The pre-helper is only the exact nonsecret prefix authorized by
`FRSH-005-v1-VR-09`. On its exact success, the same bounded production
sequence may continue only through every unchanged statement of
`FRSH-005-v1-VR-08`.

Helper contract `HOST06-FILE-IDENTITY-PSNET-v1` remains exact: frozen C# source
`1,693 / 98cf564b7d22da686adc204a3f6051927ac2d37ef7b5f2fe22d774cb10d5da97`;
frozen DLL `4,096 / 39e85b32b7f8437c2b5732e26093ca5bd9a9182b048c411e9dc5660ba03f10c9`;
exact temp-only root/output; in-memory `Add-Type` source and verified-byte
`Assembly.Load`; exact reflected surface/layout; owning exclusive
`FileStream.SafeFileHandle`; one-link/non-reparse/size and volume/file-index
continuity; same-directory two-argument no-replace move; identity-conditioned
cleanup; and no path/write/close/arbitrary-native surface. Helper compile,
load, reflection, and exact cleanup complete before live-root allocation or
API request.

API contract `HOST06-IMAGE-API-PSNET-v1` remains exact: one `POST` to
`https://api.openai.com/v1/images/generations`; model `gpt-image-2`; exact
`HOST06-GEN-PROMPT-v1`; `n=1`; `3840x2160`; `quality=high`;
`background=opaque`; `output_format=png`; exact seven-member request; env-only
`OPENAI_API_KEY`; no redirect; `ResponseHeadersRead`; `00:10:00`; exactly one
`SendAsync` for the active ordinal; strict bounded UTF-8/JSON/canonical-base64
response; one in-memory decode; create-new/flush/handle-bound staging; and no
SDK, CLI, Python, package, browser, alternate endpoint/model/parser, URL,
managed output, or discovery path.

All strict PNG signature/chunk/CRC/IHDR/sRGB/opacity/dimension/inflate/browser-
decode gates, original-resolution `PHY-01..12`, source bands, centered full-
source `cover / 50% 50%`, six-layout crop/mapping/accessibility thresholds,
focus, target, forced-color, reduced-motion, and factual-alt requirements
remain exact. No repair, edit, conversion, crop, derivative, alternate decode,
screenshot, preview, export, or reveal is authorized.

At most the first fully passing identity-proved target may be copied once,
byte-identically with create-new semantics, to the sole frozen product raster
path, followed only by the sole `PROVENANCE.md`. Provenance retains exactly
the shell-authorized scalars and never a key/header, request/response JSON,
base64, helper/DLL bytes, native tuple, rejected bytes/hash/path/pixels,
diagnostic, exception, residual, or opaque value. At most one raster and one
provenance file may enter the workspace.

The immutable `FRAM-001-v1` manifest remains exact
`17 / 37,410,731` with the frozen file and canonical digests. Accepted-media
bytes and pixels remain immutable and outside Mission and retry-preflight
inspection. The corrected runtime source guard remains exact ordinal `2|3`;
all source, provenance, physical, activation, protected, layout, seven copy,
and factual-alt slots remain null-first until lawful selection.

## Canon, learning, validation, PBA, and protected boundaries

The only player-visible outcome remains exact Host 05 mastery, one same-basin
dry Stranded Lens Cradle expression, sole unchanged `L02-03`, and the unchanged
next Drowned boundary. The one-path rail, both MH-40 outcomes, equal dignity,
all null deltas, shared RP-012 ending, and `successor=null` remain immutable.
No branch, packet, lesson, Host 07, reward, access, identity, authority, world
response, hidden-lore answer, successor, RP-013, or post-ending content exists.

All unchanged `L02-03` scenario, scoring, remediation, fresh-transfer,
explanation, evidence, privacy, save/restore, no-cross-credit, and Demo Tour
isolation requirements remain exact. The shipped product remains local/offline
with one same-origin selected-image request and zero external runtime requests.
All response/base64/raster, JavaScript, CSS, module, media, source-map,
decode/image-ready, CLS, activation/task, focused/related/full, build, PBA,
served-identity, and one-complete-E2E caps in `FRSH-005-v1-VR-08` remain exact.
Quartermaster does not run the complete E2E.

The repository QA quarantine, protected PDF, training tree, Martin's real
browser/profile/save, hidden lore, accepted-media pixels, OS-temp parent,
ordinal-1 residual, real managed directory, user/untracked work, VR-65, and
every opaque residual remain protected. The thirteen inherited process records
and separate Commandant filename/search-scope record remain separate and
**OPEN**. This variance closes, cures, merges, waives, renumbers, accesses, or
reclassifies none. VR-65 remains separate, opaque, non-gating, and inaccessible.

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`. Only fresh
Intelligence may advance maturity from accepted as-built evidence.

## Mission validation, Git gate, and exact Quartermaster handoff

Mission read the complete current intake, workflow, registry, full Mission
profile, `FRVE-005-v7-VR-08`, `FRVE-005-v7-VR-07` through
`FRVE-005-v7-VR-05`, `FRSH-005-v1-VR-09`, `FRCA-005-v3`, complete
`FRWO-005-v7`, decisive `FRVE-005-v7-VR-03`, the effective shell, treatment,
blueprint, `FRCE-005-v1-VR-05`, immutable `FRAM-001-v1`, and the exact passing
pre-helper evidence. Starting `HEAD` was exact `de2cb62c...`.

Mission independently reconfirmed candidate tree `92b22fc...`, the ordinal
`2|3` source guard, exact manifest file identity/count/stored-byte total, and
absence of helper root/DLL, live root, both ordinal pairs, product raster, and
provenance. Mission did not execute the launcher or pre-helper, allocate a
root, compile/load the helper, read a credential, construct/send a request,
consume an ordinal, inspect media/pixels, change product/tests, run build/
browser/E2E, reveal, advance maturity, close an OPEN record, access a residual
or VR-65, schedule, automate, release, or call `FIRST RUN COMPLETE`.

The dedicated Mission contribution contains only this variance and the
synchronized `NEXT_INSTANCE_HANDOFF.md`. The commit is pushed at this shell
gate and exact `HEAD == origin/main` synchronization is proved before the
handoff becomes active; the final commit identity is necessarily reported
outside the self-containing artifact.

After that synchronization, exactly one fresh Quartermaster / `quartermaster`
reads the complete active intake, full Quartermaster profile, this complete
variance, `FRVE-005-v7-VR-08`, `FRSH-005-v1-VR-09`, `FRCA-005-v3`, complete
`FRWO-005-v7`, decisive `FRVE-005-v7-VR-03`, complete effective shell,
treatment, blueprint, `FRCE-005-v1-VR-05`, corrected candidate `f4b2062...`,
immutable `FRAM-001-v1`, and the exact helper/direct-API/product controls.

Quartermaster independently proves synchronization, manifest identity,
selected-ordinal guard, inert slots, all controlled-path absences, and the
exact ordinal ledger. It then performs the exact bounded sequence above,
beginning with ordinal `2`, using ordinal `3` only under the unchanged lawful
objective-rejection rule, selecting at most one source, importing only the one
byte-identical raster plus provenance, populating only predeclared slots,
proving exact cleanup, and issuing exactly one versioned `PRODUCTION CONTENT
COMPLETE`, `REVISE`, or `HOLD` ledger without reveal.

Quartermaster may not retry ordinal `1`; exceed the two future sends; alter
the launcher/pre-helper/helper/API/parser/ordinal/media/physical/crop/PBA/
provenance contract; change behavior, lesson, route, save, world, ending,
accepted media/manifest, dependencies, Host 07+, or later rail; inspect or
retain rejected pixels; run the complete E2E; access a residual or VR-65;
advance maturity; release; schedule; automate; reveal; or call `FIRST RUN
COMPLETE`.

Mission Captain signs **`FIRST RUN SHELL READY / EXACTLY ONE FRESH
QUARTERMASTER RETRY AUTHORIZED FROM ORDINAL 2 / FRSH-005-v1-VR-10`** from
exact source `de2cb62c...`.
