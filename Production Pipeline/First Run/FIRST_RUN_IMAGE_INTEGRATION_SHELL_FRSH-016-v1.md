# First Run Image Integration Shell

Shell: `FRSH-016-v1`

Disposition: **FIRST RUN SHELL READY / EXISTING-MASTER RUNTIME INTEGRATION / HOST 14 FIRST**

Loop: `HA-RI-001-v1`

Source commit: `6bf7036d`

Predecessor release: `FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e`

## Bounded outcome

Wire the 27 already-produced, private Full HD environment masters into their
canonical host boundaries, earliest-first. One asset is the atomic iteration;
Host 14 at `FR-03` is first. Shared adapter work is permitted only when it
reduces duplicate integration code without changing route or state ownership.

## Frozen boundaries

- Existing lesson order, scoring, evidence, focus, retry, save, return, and
  generic fallback behavior remain authoritative.
- No new host, route branch, lesson, dialogue authority, reward, world
  response, access, successor, `RP-013`, or post-ending content.
- Production masters and their receipts are immutable; runtime code references
  them without overwriting.
- Alternative text comes from the current asset card unless a focused
  accessibility review proves a meaning-preserving correction is required.
- No image generation/editing call, candidate reveal, hidden lore, real
  browser/profile/save, repository QA screenshot, PDF, or training-file use.

## Verification and evidence reuse

Per asset, run only master identity/provenance, the affected selector/source
test, a build/emitted-identity check when bundler inputs change, and the
affected responsive/accessibility check. Reuse unchanged `FRCE-015-v1`
evidence. Reserve a cold full suite, applicable validators, served/E2E and
independent presentation/release review for the frozen aggregate candidate.

## Stop and rollback

The first focused PASS stops each asset iteration. Failed or malformed source
identity keeps the previous presentation/fallback. Two equivalent failures
require diagnosis; three non-improving iterations force replan, bounded return,
rollback, or typed stop. Reverting the host-owned adapter/import restores the
previous verified product; no master is deleted.

## First executable iteration

Host 14 may replace only the missing physical presentation at the existing
`L06-01 -> L06-02` boundary. Its approved master, card alternative text,
source identity, responsive containment, and existing launcher/focus behavior
must all pass before it becomes an integration candidate. No later host is
modified in the same iteration.
