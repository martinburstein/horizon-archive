# FIRST RUN SHELL / FRSH-013-v1

## Disposition

`FIRST RUN SHELL READY / EIGHT ROLLING-POOL CALLS / FIRST PASS STOPS`

Source release is `FRAB-012-v1` at
`5b1dd2608380b49ab2eb4c6719c4ba94c6e440b2`. Planning authorities are
`FRPB-001-v11`, `FRCL-013-v1`, `FRWO-013-v1`, and `FRVE-013-v1`. This shell
owns only `FR-03 / Host 13` and unchanged `L05-05 + L05-06`.

## Fixed player-visible outcome

The released upper route reaches a dry receding-waterline strip and one local
field of at least three taut mineral-glass spans. Every span is visibly fixed
at both ends by collars; depths and angles vary; at least three contacts show
strain, repair, or load residue; and same-basin water remains visible. The
material records physical loading only. No writing, music, code, response,
answer, intention, maker identity, wet access, or later ledger is inferred.

LOOK is physical and write-free. TALK is exact nonresponse. USE enters only the
first incomplete owned lesson. Completion remains read-only and changes no
span, collar, water, wind, residue, route, access, meaning, or later state.

## Generation, custody, and objective PASS

Only sequential `{H13-1..H13-8}` exist locally. First complete PASS ends this
ledger; unused calls remain in the rolling pool but cannot move until this full
release loop completes. Each call uses bundled CLI `35,266` /
`c2cdb05244ad9a3dcb8731988790ea6a06a59ab3c062c0f872b35f4ad7d20b05`
with its exact prompt from `FRVE-013-v1`, `generate`, `gpt-image-2`,
`3840x2160`, high, opaque PNG, exact output, and `--no-augment`. No force,
batch, reference, edit, variation, internal retry, alternate model, alternate
transport, or prompt rewrite.

Scratch root/children, product raster, and provenance are exact `FRWO-013-v1`
literals. Quartermaster privately reviews only an identity-proved ordinary
nonreparse output. A rejection records bounded identity, flags, measurements,
derived layout verdict, and reasons, then identity-deletes the output and proves
absence before another call. No rejected pixel, derivative, reference, reveal,
or workspace copy exists.

Technical PASS is opaque RGB PNG, exact `3840x2160`, strict decode, 1..30 MB.
Physical PASS is the complete dry-approach/spans/collars/load-contacts/residue/
waterline sentence with every `FRCL-013-v1` rejection absent. Responsive PASS
measures the actual source, calls production derived projection at all six
layouts, and requires >=.95 relation retention, all required centers visible,
contained target >=44 CSS px, contained label/focus, zero protected overlap,
and later real-Edge corroboration. Prompt composition is guidance, never an
acceptance shortcut or absolute-band rejection.

At most one passing byte-identical raster plus `PROVENANCE.md` enters the
workspace. Import is selection, not release.

## Role ownership and runtime contract

- Recon locks one physical reading order without spectacle or meaning.
- Tactical locks state, native actions, focus, layout, recovery, copy, and QA.
- Combat builds a null-first fail-closed registry/tests. Before selection,
  released generic behavior remains. Exact source enablement retires only the
  affected generic launchers even on later decode/layout failure.
- Quartermaster alone calls, privately reviews, selects, imports, measures,
  records provenance, and populates accepted-source fields.
- Image Specialist changes runtime code/configuration only around the unchanged
  selected raster; never image bytes or reveal.
- Intelligence independently validates and alone releases/carries the pool.

Runtime acceptance requires exact `H13-[1-8]` attempt ID, no surrogate ordinal,
source bytes 1..30000000, lowercase SHA-256, exact dimensions/opacity, final
copy/alt/provenance, measured relation/approach/span/collar/contact/waterline/
semantic rectangles, and all six layout records. One native button converges
pointer, keyboard, touch, and switch-like entry. Focus returns after LOOK,
TALK, lesson exit, remediation, mastery, reload, and route return. No prompt,
candidate, answer, media identity, or review state is persisted.

## Validation, budgets, rollback, and protection

Budgets and validation are exactly `FRWO-013-v1` / `FRVE-013-v1`. Combat
passes focused/related/full tests, validators, production/fixture builds, PBA/
media, served identity, and pushes the exact code candidate. Quartermaster
proves selection and cleanup. Image proves real six-layout runtime
presentation. Intelligence runs isolated clean-start E2E through both owned
lessons and unchanged MH-40, final budgets, cleanup, candidate identity, and
sync.

Before import, rollback is scratch cleanup. Before release after import,
rollback removes only the new raster/provenance and restores the null registry.
Identity, technical, physical, responsive, accessibility, cleanup, budget,
canon, lesson, or protected-boundary failure stops at the earliest owner.

Accepted Hosts 01-12 and media remain immutable. Never inspect, restore, stage,
or use repository QA screenshots, Martin's browser/profile/save, protected PDF/
training, hidden lore, or opaque residual roots. No branch, packet, lesson,
reward, access, identity, authority, dialogue, world response, RP-013,
post-ending content, or successor.

## Mission signature

`FIRST RUN SHELL READY / FRSH-013-v1`

Exact next owner: one Reconnaissance Sergeant.
