# FIRST RUN WORK ORDER / FRWO-015-v1

## Disposition

`WORK ORDER READY / GUIDE-FIRST MATERIAL REPRESENTATION / SCIENCE NEXT`

Operations stage: `operations_planning_major` / `MIGRATE -> DESIGN`.

Baseline: `FRPB-001-v13`. Continuity lock: `FRCL-015-v1`. Compact state
read: `FRLS-COL-002-v1`. Predecessor Work Order and terminal evidence:
`FRWO-014-v1` / `FRSH-014-v3-VR-01`.

Best and committed release: `FRAB-013-v1` at exact candidate
`357ad6dc4184b74150173504e86e366c761cdc0e`. Current inert null-first
candidate: `FRCE-014-v1` at
`b4444afefe624ff231d986933a56c2003c0d8ac5`.

Martin's explicit authority reopens a materially different source-production
mechanism, not the prior prompt-only attempt ledger. `H14-1..H14-32`, the
`4 used / 28 remaining` accounting, every `FRSH-014` call permission, and all
rejected/deleted sources are **closed historical evidence**. They do not carry
into this Work Order. The new cycle namespace is `FRM15`; no `FRM15` guide,
render, edit, transform, inspection, import, or product action is executable
until Science proves viability and Mission issues a complete versioned
`FIRST RUN SHELL READY` contract.

No product, runtime, media, lesson, save, route, maturity, test, browser, or E2E
input changed at Operations. Existing product evidence is reused exactly as
recorded; no test or build was rerun.

## Address, ordering, and bounded outcome

The selected surface remains the earliest missing physical expression at
`FR-03`, immediately after the exact released predecessor and before the
unchanged later generic boundary. The Work Order owns one distinct dry,
reachable, low constructed face and its already-owned two-lesson native entry.
It does not own the later missing host, any later address, a chapter rewrite,
or a whole-game art pass.

Current maturity at the address remains `FR0` for the missing physical
expression while released continuity, learning, behavior, content, and
presentation evidence remain unchanged. Target evidence is one independently
released bounded pass: technically valid source, physically legible canon-safe
relationship, lawful native entry and recovery, responsive/accessibility
parity, and exact-candidate release proof. Only Intelligence may advance
maturity.

Player problem: the functional rail currently collapses this physical
relationship into two generic owned-lesson launchers. The prior prompt-only
mechanism could preserve the low nonhuman topology or constructed-process
causality, but not both.

Player-visible outcome: one grounded physical relationship visibly carries
three constructed histories, their interdigitation, and one continuous trace
with three substrate reactions, while the released route, silence, lesson
order, save/privacy/offline behavior, and unchanged world remain exact.

## Required representation migration

### Artifact classes and identifiers

The shell may authorize only these classes, under the new `FRM15` namespace:

| Class | Preliminary identity | Purpose | Player-facing authority |
| --- | --- | --- | --- |
| Guide specification | `FRM15-SPEC-v*` | Machine-readable topology, process, trace, route, crop, target, and protected-zone constraints | None |
| Structural guide | `FRM15-G##-STRUCT` | Deterministic mass, recess, seam, overlap, and route geometry | None |
| Material-ID guide | `FRM15-G##-MAT` | Deterministic process regions, pairwise interlocks, trace, and reaction masks | None |
| Protection/layout guide | `FRM15-G##-PROT` | Crop anchors, semantic target, label anchor, route and canon exclusions | None |
| Guide validation record | `FRM15-G##-V` | Deterministic proof that the guide obeys the frozen graph contract | Evidence only |
| Base render | `FRM15-R##` | One guide-conditioned built-in generation result | Private candidate only |
| Reference edit | `FRM15-E##` | One local image-to-image/reference repair of the current search-best candidate | Private candidate only |
| Deterministic derivative | `FRM15-D###` | Shell-allowlisted composite, crop, upscale, channel, opacity, or format normalization | Private candidate only |
| Acceptance record | `FRM15-PA##` | Private technical/physical/accessibility/anti-message verdict and measurements | Evidence only |
| Selected source | `FRM15-A01` | First and only source to pass every pre-import gate | Candidate until release |
| Product candidate | `FRM15-PC01` | Atomic import plus exact runtime/source/provenance integration | Candidate until Intelligence |

Identifiers are append-only and never reused after an ambiguous or completed
effect. Guide colors, IDs, labels, masks, legends, anchors, and annotations are
production controls only. They must be absent from the flattened player-facing
source and cannot satisfy a physical or accessibility verifier.

### Required action graph

```text
FRM15-SPEC
  |-- deterministic --> G##-STRUCT
  |-- deterministic --> G##-MAT
  `-- deterministic --> G##-PROT
             |
             `--> G##-V (structural hard gate)
                       |
                       `--> R## (one guide-conditioned base render)
                              |
                              |--> PA## (private hard-gate observation)
                              |
                              `--> E## (only a localized diagnosed edit)
                                     |
                                     `--> PA##
                                            |
             guide masks + best passing regions ----+
                                                    v
                    D### composite/crop/upscale/channel/format normalization
                                                    |
                                                    v
                    technical + physical + anti-message + source-layout gate
                                                    |
                                  first full pass -> A01
                                                    |
                                atomic no-replace import + provenance DAG
                                                    |
                                                   PC01
                                                    |
                           functional/runtime/accessibility/release holdout
```

The graph is a directed acyclic provenance chain. Every node records its exact
parent identities, bytes, SHA-256, tool identity, parameters or deterministic
operation, time, owner, result, and disposition. A composite records every
source node and mask; normalization records the decoded input and output
properties. No node may overwrite a parent or conceal a failed ancestor.

### Guide contract

Before a stochastic call, one guide family must make these independently
inspectable and editable:

- one low, inclined, concave, nonhuman constructed face and dry route;
- older foundation, later repair, and current service-skin process regions;
- horizontal and vertical overlap plus at least two nontrivial pairwise
  interlocks/islands;
- one continuous irregular trace crossing every process region;
- three separate process-dependent reaction zones on that trace;
- at least three upward unequal nonparallel recessed seams, including one
  branch or merge;
- receded waterline adjacency without live-water route overlap;
- relation, essential centers, semantic target, label anchor, and six runtime
  layout inputs; and
- protected predecessor, later boundary, return route, water, landmark,
  Crown, Witness, narration/UI, and interaction regions.

A structurally passing guide is necessary, never sufficient. It fails if it
resembles player-facing writing, a lesson diagram, an objective grid, a human
plan, or a source that could be imported directly as finished art.

### Stochastic render/edit contract

The initial experiment is one passing guide family, one guide-conditioned base
render, and at most two local reference-edit observations. An edit is
authorized only to address a diagnosed visible delta while retaining the
immutable guide and prior candidate in the DAG. A whole-frame regeneration
presented as an edit, an unrecorded reference, an internal retry/batch, or a
candidate created outside the exact ordinal is prohibited.

The built-in generator/edit mechanism may interpret production guides, but it
may not receive repository QA captures, Martin's browser/profile/save, hidden
lore, protected documents, rejected/deleted sources, archives, opaque
residuals, or unrelated worktree files. Existing accepted media may be a
reference only if Science proves its immutable identity and Mission names the
exact allowlisted input; otherwise accepted pixels remain unopened.

### Deterministic transform contract

Science and Mission may allow create-new deterministic operations only from
this closed class:

- exact-mask compositing between recorded DAG parents;
- crop or pad to the frozen aspect and safe-layout envelope;
- deterministic upscale/downscale with a named algorithm;
- channel reorder, alpha flattening against a named fixed background, and
  color-profile conversion;
- lossless metadata stripping and final output-format normalization; and
- deterministic measurement overlays written to separate evidence artifacts,
  never into the candidate.

No transform may invent or repaint semantic geometry, clone content into a
missing physical region, remove a hard failure by cropping it away, apply an
unfrozen generative fill, overwrite an input, or launder an unattributed
intermediate. Source pixels and guide pixels remain distinct classes.

## Exact selected-source and product contract

The Work Order may yield exactly one selected flattened player-facing source:

- identity `FRM15-A01`, derived only from the complete recorded DAG;
- `3840 x 2160`, PNG, opaque 8-bit sRGB, one flattened image, no runtime alpha;
- ordinary-file, strict-decode, byte-count, SHA-256, no-link/no-reparse and
  create-new identity proof;
- no visible guide colors, mask edges, labels, legends, glyphs, writing,
  numbers, UI, objective layout, human infrastructure, protagonist body,
  answer cue, completion response, or embedded metadata that changes meaning;
- exact physical measurements for relation, three histories, interlocks,
  trace/reactions, waterline, seams, dry approach, target, anchor, essential
  centers, and protected regions;
- exact provenance record referencing every retained or dispositioned parent;
  and
- byte-exact atomic no-replace import only after every private pre-import hard
  gate passes.

Science may tighten encoding, byte, color-profile, decode, and custody
requirements, but may not weaken these output semantics. If the current
null-first runtime's closed `H14-*` attempt-ID schema cannot lawfully accept the
new namespace, Combat may make the smallest fail-closed schema migration after
shell; no old ordinal becomes valid again.

Accepted inventory remains `24 / 154,163,567` throughout search. Only a
selected and atomically imported `FRM15-A01` can make the provisional candidate
inventory `25` files, subject to Science's exact aggregate-byte and PBA caps.
Rejected guides/candidates and evidence artifacts never enter accepted media.

## Ordered production ownership

1. **Reconnaissance Sergeant:** interpret the locked physical sentence as one
   treatment and define what the guide/render must feel like without creating
   media or changing canon.
2. **Tactical Operations Specialist:** blueprint source-space structure,
   interaction, crop, focus, responsive retention, fallback, and recovery.
3. **Combat Engineer:** implement deterministic guide builders/validators and
   the smallest fail-closed runtime namespace/source-law migration. Product
   remains null-first and generic unless and until a selected source exists.
4. **Quartermaster:** own isolated custody, stochastic calls, immutable DAG,
   private inspection, deterministic derivatives, acceptance, exact import,
   copy/provenance population, and rejected-artifact cleanup.
5. **Image Specialist:** polish only runtime presentation using the accepted
   imported source; no further media-byte generation/edit is assigned to this
   role.
6. **Intelligence Officer:** independently freeze and audit the exact product
   candidate, run the fresh release holdout, classify variances, and alone
   release or return it.

No owner may silently widen a predecessor's artifact class, call pool,
transform allowlist, protected input set, or verifier.

## Player route, state, pacing, and rollback

- Entry remains exact predecessor completion and the same fixed dry adjacency.
- LOOK remains write-free. TALK remains complete nonresponse. USE opens only
  the first incomplete owned lesson after every source/predecessor/geometry
  guard passes.
- The two existing lessons remain sequential and independent. Their evaluator,
  thresholds, evidence, remediation, fresh retry/transfer, explanation,
  close/Escape, reopen, reload, resume, return, mastery, privacy sanitation,
  offline behavior, and no-exam/no-authority language remain unchanged.
- Miss and mastery alter expedition-owned evidence/time only. Nothing in the
  world moves, answers, rewards, opens, identifies, approves, or remembers.
- Player-time impact is limited to replacing the two generic launcher entries
  with one native physical arrival and its already-released lesson flow. No
  additional required lesson or route step is added.
- The next released boundary and the shared ending remain unchanged.

Rollback is exact and available at every stage:

- before import, source stays disabled/null and both generic launchers remain;
- on any candidate hard failure, retain or clean the exact scratch nodes only
  according to the shell ledger; accepted media and product remain unchanged;
- on import/integration failure, remove only the exact new product source and
  provenance under identity control, restore the null-first registry/config,
  and prove generic launcher behavior;
- on holdout failure, return to the last verified product checkpoint; and
- no rollback mutates an accepted original, user file, save, dependency, or
  unrelated worktree change.

## Physical, responsive, accessibility, and safety gates

### Physical hard gate

The final flattened source independently demonstrates every fact in
`FRCL-015-v1`: low nonhuman face, three visibly constructed process histories,
horizontal/vertical overlap, at least two pairwise interlocks, one continuous
trace crossing all three with three distinct substrate reactions, unequal
upward recessed seams with a branch/merge, dry route, and receded waterline.
Natural geology, biology, stripes, a diagram, human hardware, writing, UI,
lesson answers, or a responsive world are hard failures.

### Source-derived responsive gate

The accepted source and frozen runtime layout inputs must deterministically
derive and then corroborate in the sanctioned browser:

- desktop, laptop, narrow, effective `200%`, retained `320x180`, and retained
  `320x240` layouts;
- at least `0.95` relation retention in every layout;
- every essential center visible;
- semantic target contained and at least `44 x 44` CSS pixels;
- physical center inside the target;
- label fully contained with its frozen focus separation; and
- zero protected-region overlap or crop-based concealment of a failed fact.

### Accessibility and equivalent truth

Keyboard, pointer, touch, semantic and switch-like activation; visible focus
and focus return; assistive name/description; announcement state; narrow and
effective-`200%` reflow; forced-color containment; reduced-motion parity; and
non-color/non-sound/non-motion meaning must pass. Alt/copy may describe only
facts visibly present in a passing source and cannot rescue ambiguity. No
human assistive-technology certification is claimed.

### Security, custody, and privacy

All guide/media work is isolated, one process at a time, create-new, exact-
identity, append-only in provenance, and fail-closed on ambiguous completion.
Environment-only secrets never enter prompts, reports, logs, commits, or state.
Forbidden evidence remains unopened. Scratch cleanup requires literal frozen
targets and residual accounting; an ambiguous target produces `STOP_SAFETY`,
not broad deletion.

## Preliminary finite economics

Science and Mission must tighten or reduce this proposal before execution. They
may not increase a hard dimension without returning to Operations.

```yaml
preliminary_budget:
  cycle_namespace: FRM15
  guide_families:
    hard_max: 2
    initially_authorized_after_shell: 1
    second_family_requires: representation_replan_after_causal_diagnosis
  stochastic_calls_total:
    hard_max: 6
    initial_tranche: 3  # one base render plus at most two diagnosed edits
    earned_extension: 2
    near_pass_local_repair_reserve: 1
    concurrency: 1
    outputs_per_call: 1
  deterministic_media_operations:
    hard_max: 12
    guide_build_or_validation: 4
    composite_crop_upscale_channel_format: 8
  private_candidate_inspections:
    search_hard_max: 6
    selected_source_reconciliation: 1
    intelligence_holdout: 1
  retained_intermediates:
    hard_max_files: 16
    hard_max_bytes: 536870912
    accepted_media_during_search: 24_files_154163567_bytes
  wall_clock:
    production_search_and_selection: 4_hours
    protected_integration_and_final_proof: 2_hours
  product_effect_iterations_before_replan: 3_non_improving
  final_verification_reserve:
    - post-change focused and affected related checks
    - one cold full suite only after behavior-relevant product/config/test change
    - curriculum/readiness validators when their exact inputs require fresh proof
    - production and sanctioned fixture builds
    - selected-source PBA, served identity, six-layout browser and one clean-start E2E
    - cleanup, exact-candidate identity, rollback capacity, and independent Intelligence holdout
```

The initial three-call tranche tests the new causal mechanism, not aesthetic
variety. The two-call extension is earned only by a measurable best-state
improvement on the frozen physical vector plus a diagnosis predicting the next
local edit. The final stochastic call is not search capacity: it can be used
only when one near-pass candidate has exactly one localized, independently
measured repair and all other hard gates pass. Search may never consume final
integration/release capacity.

## Progress vector, plateau, and terminal rules

```yaml
potential_at_operations:
  hard_failures: 0_observed
  missing_physical_expression: 1
  guide_structure: NOT_CREATED
  player_facing_physical_meaning: UNKNOWN
  source_and_provenance: UNKNOWN
  runtime_integration: NULL_FIRST_REUSED
  maturity_change: ZERO
  stochastic_calls_used: 0
  final_proof_budget_remaining: FULL_PROTECTED
```

Meaningful improvement requires a candidate to reduce at least one frozen
physical or technical defect without regressing a previously passing hard
dimension. Novelty, beauty, another file, another call, guide compliance alone,
or generator self-description is not progress. The deadband is exact: a change
that remains in the same hard-failure class with no new localization is
non-improving.

- First full release-quality PASS stops all search and edit calls.
- Two materially equivalent failures require a fresh causal diagnosis before
  another call in that strategy family.
- Three non-improving iterations in one guide/render/edit family force
  `REPLAN`, `RETURN_TO_OWNER`, or a typed stop; an equivalent fourth is barred.
- A second guide family must change deterministic topology/material
  representation, not merely guide color, prompt wording, seed, or crop.
- Any hard canon, answer, human-infrastructure, privacy, custody, residual,
  route, save, accessibility, or world-response failure triggers rejection and
  rollback; it cannot be averaged into visual appeal.
- If deterministic compositing is needed to invent missing meaning rather than
  preserve passing regions, return to Reconnaissance/Tactical/Science.
- `STOP_LOW_MARGINAL_VALUE` when no falsifiable local repair has positive
  expected player value relative to call, review, storage, regression, or
  cleanup cost.
- `STOP_BUDGET` on any hard budget dimension; `STOP_SAFETY` on unresolved
  identity, secret, destructive-target, or residual ambiguity;
  `RETURN_TO_OWNER` follows the canonical earliest-owner routing.

## Regression radius and evidence reuse

The eventual selected-source candidate must cover the exact released
predecessor, native/generic launcher inversion, both owned lessons and all
miss/retry/close/reopen/reload/return/complete states, save/privacy/offline and
Demo Tour isolation, unchanged later entry, responsive scene containment,
both equal ending outcomes, and `successor=null`. It may not modify those
surfaces beyond the bounded native replacement and new source-law namespace.

At Operations, product/code/test/configuration/media inputs are unchanged.
Therefore the following evidence from
`FRCE-014-v1@b4444afefe624ff231d986933a56c2003c0d8ac5` is `REUSED`, not rerun:
focused `31/31`, related `63/63`, cold full `1047/1047`, validators `40/40`,
production/fixture builds `234/57`, and null-first PBA. The independent
`FRAB-013-v1` holdout is reused only for the committed release, not the future
source-enabled candidate. Fresh full-product execution becomes justified only
after a verifier-relevant product/config/test input changes and once for the
independent exact-candidate holdout.

Operations performed zero product tests, builds, validators, browser/E2E,
media inspections, guide operations, stochastic calls, transforms, imports,
or product effects. Repository QA/screenshots, Martin's browser/profile/save,
hidden lore, archives, protected PDFs/training files, opaque residuals, and
unrelated worktree changes were not inspected or modified.

## Required versus optional scope

Required: one guide family, structural hard gate, one base-render observation,
only evidence-earned local edits, complete DAG/provenance, one flattened exact
source, private physical/source-layout acceptance, null-first source-law
migration, atomic import, native entry, unchanged lessons/recovery, runtime
presentation, cleanup, and independent holdout.

Optional only after every required hard gate: one second guide family, one
deterministic composite preserving separately passing regions, restrained
runtime lighting/color treatment, and unused budget retention. Optional work
may never delay first PASS or widen the address.

Explicitly excluded: later hosts/addresses; new copy meaning, lesson, answer,
reward, access, identity, authority, branch, save field, world response,
successor, RP-013, post-ending content; accepted-original mutation; guide or
candidate reveal; repository QA/browser/save/protected-document evidence; and
test-count expansion without a new failure mode.

## Exact Science handoff

Science must issue one versioned `POLISH VIABILITY READY`, `REVISE`, or `HOLD`
without creating a guide or spending a call. It must freeze or reject:

1. exact built-in generation/edit tools, model/version, local-path/reference
   behavior, request/response identity, one-output/no-retry semantics, secret
   boundary, and ambiguous-completion reconciliation;
2. exact deterministic guide representation, builder, schema, dimensions,
   material IDs, structural validator, anti-diagram mutations, and tool hashes;
3. exact parent/child provenance schema, ordinal allocation, custody root,
   ordinary-file/link/reparse/handle controls, immutable retention, crash/
   cancellation recovery, selected/rejected cleanup, and residual accounting;
4. exact transform allowlist and implementation for mask compositing, crop/
   pad, upscale/downscale, alpha flattening, opaque 8-bit sRGB conversion,
   metadata stripping, PNG normalization, and adversarial proof that no
   transform invents meaning or hides failure;
5. exact technical/source-size, aggregate-media, decoded-memory, staging/
   response, retained-byte, JS/CSS/module, runtime-request, source-map,
   activation, main-thread, and wall-clock budgets;
6. exact physical measurements and independent private review protocol proving
   all `FRCL-015-v1` facts and rejecting natural geology, human infrastructure,
   writing/UI/answer cues, guide leakage, and world response;
7. exact source-derived six-layout thresholds, sanctioned browser
   corroboration, `44px` targeting, focus, semantic, forced-color,
   reduced-motion, effective-`200%`, narrow, and non-sensory equivalence;
8. exact null-first namespace/schema migration and fallback law for absent,
   malformed, stale, mismatched, decode-invalid, or disabled source state;
9. exact focused/related/full/validator/build/PBA/served/browser/E2E/cleanup/
   Intelligence cascade with content-addressed reuse and named invalidation
   conditions for every fresh run;
10. whether the proposed `2` guide-family, `6` stochastic-call, `12`
    deterministic-operation, `8` inspection, `16` file / `512 MiB`, and
    `4h + 2h` budgets are feasible and minimal; tighten them if possible;
11. exact measurable improvement vector, deadband, two-equivalent-failure
    diagnosis, three-non-improving plateau, second-family replan, near-pass
    reserve, first-PASS stop, and low-marginal-value rule; and
12. exact rollback to the null-first current candidate and committed generic
    release, including accepted inventory preservation and honest unavailable
    evidence without forbidden inputs or a human AT study.

Science may not revive any `H14` ordinal/pool, create or inspect a guide/media
artifact, make a stochastic call, transform/import media, modify product,
run browser/E2E, or advance maturity.

## Convergence handoff

```yaml
convergence_handoff:
  mode: MIGRATE_to_DESIGN
  state_version_read: FRLS-COL-002-v1
  state_version_written: FRLS-OPS-002-v1
  current_ref: FRWO-015-v1 / FRCE-014-v1_null_first@b4444afefe624ff231d986933a56c2003c0d8ac5
  best_ref: FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e
  committed_ref: FRAB-013-v1@357ad6dc4184b74150173504e86e366c761cdc0e
  binding_gap_or_hypothesis: >-
    A deterministic structural/material/protection guide followed by one
    guide-conditioned render, localized reference edits, and provenance-bound
    deterministic normalization can preserve low nonhuman topology and make
    three constructed processes plus one causal trace jointly legible.
  action_kind: information
  predicted_effect: >-
    Science will prove or reject the exact guide, tool, custody, transform,
    verifier, accessibility, performance, fallback, and economic envelope
    before Mission grants any effect authority.
  verifier_vector:
    earliest_first: PASS
    bounded_vertical_outcome: PASS
    continuity_and_lesson_ownership: PASS
    representation_graph: DEFINED_PRELIMINARY
    provenance_and_rollback: DEFINED_PRELIMINARY
    output_contract: DEFINED_SCIENCE_MUST_HARDEN
    runtime_null_first_fallback: PRESERVED_REUSED
    tool_and_transform_feasibility: UNKNOWN_SCIENCE_OWNED
    future_source_physical_and_release_proof: NOT_STARTED
  delta_vs_best: zero_product_zero_media_zero_maturity_positive_scope_representation_and_economics_information
  budget_used:
    guide_families: 0
    stochastic_calls: 0
    deterministic_media_operations: 0
    media_inspections_or_imports: 0
    product_effect_actions: 0
    test_build_validator_browser_e2e_invocations: 0
  budget_remaining:
    old_H14_pool: CLOSED_NOT_INHERITED
    new_FRM15_budget: PRELIMINARY_SCIENCE_AND_MISSION_MUST_FREEZE
    initial_stochastic_tranche_after_shell: 3
    hard_stochastic_ceiling: 6
    final_verification_reserve: FULL_PROTECTED
  remaining_uncertainty:
    - exact guide and built-in generator/edit tool feasibility
    - exact deterministic transform safety and custody implementation
    - future private physical/source-layout acceptance
    - exact performance/PBA and proof commands
    - future product candidate and independent release decision
    - human assistive-technology usability study
  decision: GATHER_EVIDENCE
  decision_evidence:
    - the prior hold localized failure to prompt-only representation
    - Martin explicitly authorized a materially different mechanism
    - FRCL-015-v1 locks guide-safe physical facts and anti-inference boundaries
    - the new DAG makes topology and material causality separately editable and auditable
    - no product or media state changed and all prior product evidence remains reusable
  next_owner: office_of_science_administrator
```

Operations signature:
**`WORK ORDER READY / FRWO-015-v1 / SCIENCE NEXT`**.
