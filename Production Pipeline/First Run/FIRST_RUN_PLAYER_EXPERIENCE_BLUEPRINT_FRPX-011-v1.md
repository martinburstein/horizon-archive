# FIRST RUN PLAYER EXPERIENCE BLUEPRINT / FRPX-011-v1

## Disposition

`PLAYER EXPERIENCE READY / ACCEPTED-ONLY NATIVE HOST 11`

Authorities: `FRSH-011-v1`, `FRDT-011-v1`, source release `FRAB-010-v1`.

## State and replacement graph

Current: Host 10's owned lessons/mastery -> generic Portal Orientation launcher
-> strict mastery -> generic Prompt Layers launcher -> strict mastery -> later
route. Before source selection, target is behaviorally identical.

After exact source enablement: Host 10 mastery -> Host 11 `available` -> USE
opens Portal Orientation -> `in_progress`/`remediation_required` -> strict
Portal mastery -> USE opens Prompt Layers -> `in_progress`/remediation -> both
mastered -> `complete` and read-only USE -> unchanged later route. Whenever
`source.enabled===true`, both generic launchers are absent even if a later
decode/measurement predicate fails; malformed enabled state fails closed.

## Registry and native interaction

Combat creates one null-first registry with source/provenance, receiving wall,
mineral film, aperture array, return-path array, dry apron, service throat,
semantic target, label, protected regions, six layout records, copy slots, and
alt. Lawfulness requires exact H11 attempt ID, dimensions, opacity, size/SHA,
physical geometry, source containment, and all derived layouts.

One native hotspot button owns LOOK/TALK/USE. LOOK announces only received
light, film, apertures, returns, dry apron, and throat. TALK announces complete
nonresponse. USE dispatches Portal first, then Prompt, and never dispatches
after complete. Pointer, Enter/Space, touch, and switch-like activation
converge. One paint after predecessor mastery presents/focuses once. Terminal
exit, primary, remediation, mastery, reload, and route return restore focus
when lawful. Escape closes only the active terminal layer.

States: `hidden` for unmet predecessor or any source/provenance/decode/layout/
evidence malformation; `available`; `in_progress`; `remediation_required`;
`complete`. State never changes world presentation.

## Responsive, accessibility, and copy

Scene uses cover projection centered on the selected source. The semantic
target is derived from measured source geometry, clamped, and >=44x44 CSS px.
Six layout records cover desktop, laptop, narrow, effective-200%, 320x180, and
320x240: relation retention >=.95, wall/film/aperture/return/apron/throat centers
visible, target contained, physical center included, label/focus separated,
protected overlap zero. DOM order is art -> hotspot -> status/terminal. Forced
colors adds a visible border; reduced motion removes transition only; 200%
retains meaning and action.

Quartermaster fills exact final-purpose slots: `unseen`, `available`,
`portalInProgress`, `promptInProgress`, `missed`, `mastered`, `returned`,
`nextBoundary`, and `alt`. Copy cannot name projected content, address,
intelligence, answer, correctness, reward, or world change.

## Combat acceptance matrix

Tests prove null-first registry; ten prompt identities; exact attempt ID and
30 MB/aggregate media guards; source/provenance fail-close; wall/film; >=3
apertures; >=2 returns; dry apron; off-axis throat; derived six-layout evidence
ignores caller attestations; source enablement retires both generic launchers;
predecessor strict mastery; all five states; Portal-then-Prompt routing;
complete read-only USE; LOOK/TALK write-free; decode failure; focus/reload/
Escape; target semantics; all copy slots; immutable accepted media; unchanged
later route and MH-40.

Quartermaster adds selected-byte, physical measurement, private decision,
cleanup, and provenance evidence. Image adds runtime presentation only.
Intelligence independently reruns the release ladder. No variance exists.

Exact next owner: one Combat Engineer.
