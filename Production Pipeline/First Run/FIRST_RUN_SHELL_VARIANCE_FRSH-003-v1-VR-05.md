# First Run Shell Variance Reissue - Truthful Static Contract Alignment

Variance ID: `FRSH-003-v1-VR-05`

Disposition: **`FIRST RUN SHELL READY / FRSH-003-v1-VR-05`**

Stage / owner: Mission Captain / `mission_captain`

Governing shell: `FIRST RUN SHELL READY / FRSH-003-v1`

Operative browser-proof shell: `FIRST RUN SHELL READY /
FRSH-003-v1-VR-04`

Functional return: `HOLD / VR-04 DETERMINISTIC CONTROL INCOMPATIBILITY /
FRCE-003-v1`

Work Order: `FRWO-003-v1 / Sixfold Weir`

Mission control source inspected:
`9ef42b45daf2fd1cef276e292ee29da5168b289c`

Frozen runtime product candidate:
`7e85154abd8dbf116c4bb84ca66afd859903d750`

Frozen browser-probe candidate:
`d9487d8205174a7b5f688cbfccbcd5f7875ac1ad`

Frozen predecessor lineages:
`a9776e337f1820776864a5690332c364d0fb2556` /
`bf58e528bc6ce4088f81f2c782ce2895259ab9fd`

Released rollback baseline:
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`

Date: **2026-08-10**

## Decision

The VR-04 hold is a shell-permission contradiction, not a product, behavior,
geometry, focus, verifier, or learning defect. The operative manifest and E2E
probe correctly implement the authorized VR-04 identities and genuine-keyboard
path:

```text
shell: FRSH-003-v1-VR-04
key path: Tab -> Shift+Tab
intermediate: LOOK AT
final: Host 05 / sixfold-weir
```

The single frozen static expectation at
`horizon-archive-game/test/sixfoldWeir.test.js:146` still asserts superseded
VR-02 literals:

```text
shell: FRSH-003-v1-VR-02
key path: Shift+Tab -> Tab
```

VR-04 required the corrected verifier while simultaneously forbidding the one
test change needed to assert it. That contract is internally unsatisfiable.
Adding decorative legacy tokens to the operative manifest or E2E would make a
stale test pass without proving the active contract and is forbidden.

Mission therefore authorizes exactly one truthful static contract-test
expectation update. No product source, E2E, manifest, runtime behavior,
meaning, threshold, media, learning, save, route, world, later rail, or ending
change is needed or authorized.

This is **`FIRST RUN SHELL READY / FRSH-003-v1-VR-05`**. It authorizes Combat
to make the one test-only alignment below, commit it as a separate validation-
control candidate, run the complete deterministic pre-live ladder, and invoke
exactly one fresh complete E2E only after every deterministic gate passes.
Quartermaster remains blocked until Combat issues a fully passing synchronized
functional report.

## Exact authorized one-line test delta

Permitted product-test control file:

```text
horizon-archive-game/test/sixfoldWeir.test.js
```

Within the existing `live-summary-verify` command assertion only, replace the
two superseded expected literals:

```text
FRSH-003-v1-VR-02  ->  FRSH-003-v1-VR-04
Shift+Tab -> Tab    ->  Tab -> Shift+Tab
```

The lawful resulting assertion must still verify the same uninterrupted
ordered command content around those literals, including
`first-run.live-summary.v1`, `productPredecessor`,
`semanticBottomAnchored`, and `sixfoldActivationMs`.

This is one focused assertion update, not authority to:

- add a legacy VR-02 or reversed-key token to the manifest, E2E, summary,
  comments, fixture, or unrelated string solely to satisfy the test;
- rename the operative manifest/E2E shell from VR-04 to VR-05;
- change another assertion, test, product file, manifest field, E2E line,
  threshold, command, count, or timeout; or
- reinterpret `Tab -> Shift+Tab` as anything other than Host 05 to `LOOK AT`
  and back to Host 05 under the frozen VR-04 graph.

The static test must assert the unchanged operative VR-04 probe. VR-05 is the
Mission permission record that resolves the frozen-file conflict; it is not a
new runtime summary identity.

## Candidate and patch identity

- Runtime product candidate remains exactly
  `7e85154abd8dbf116c4bb84ca66afd859903d750`. Its `App.jsx`, CSS, modules,
  media, copy placeholders, learning, save, route, world, and ending bytes are
  frozen.
- Browser-probe candidate remains exactly
  `d9487d8205174a7b5f688cbfccbcd5f7875ac1ad`. Its only introducing changes are
  the existing `playtest/e2e-playthrough.mjs` and `FRRC-002-v1` corrections;
  neither file may change in this return.
- Combat must commit the authorized one-file test delta as a separate
  validation-control candidate and report its full hash. That new control
  identity does not replace or absorb either frozen candidate.
- The validation-control candidate's patch from Mission source must contain
  exactly one modified file: `horizon-archive-game/test/sixfoldWeir.test.js`.
  The changed hunk must contain only the two literal substitutions above.
- All product source blobs must equal `7e85154`; both probe-file blobs must
  equal `d9487d82`; all unrelated tracked files must remain unchanged before
  execution.

## Frozen VR-04 contract inherited without amendment

Every substantive requirement in `FRSH-003-v1-VR-04` remains exact:

- authored physical Host 05 registration remains `45/75/20/25`;
- current-Chromium used-value equality remains exact `q=1/64 CSS px`,
  `Q(v)=floor(v/q)*q`, strict component equality, and no epsilon;
- semantic activation remains bottom-anchored and `>=44 x 44`;
- label placement remains inner `2px`, outer `3px` across the exact `1px`
  button border, with exact box/content dimensions and no escape or scroll;
- source mapping begins from browser-resolved physical `P` and actual
  `.scene-art`, never frame or viewport arithmetic;
- enabled order remains Host 05 -> `LOOK AT` -> `USE` -> `TALK TO` ->
  inventory return;
- genuine forced-color focus proof remains Host 05 `Tab` -> `LOOK AT`, then
  `Shift+Tab` -> Host 05, with no pointer or programmatic target focus and
  exact `3px solid` system `Highlight`;
- all six layouts, retention `>=0.95`, exact zero overlap/overflow,
  forced-color/reduced-motion, identity, performance, PBA, and cleanup gates
  remain fail-closed;
- exactly seven Quartermaster placeholders remain unchanged;
- immutable media remains exact `17 / 37,410,731`; and
- `L02-02`, evaluator, evidence/privacy, save, Host 04, later Drowned/Witness/
  City/rail, equal MH-40 outcomes, null deltas, and `successor=null` remain
  unchanged.

No maturity cell advances from this planning adjudication.

## Deterministic pre-live ladder

After committing the one-file validation-control candidate, Combat must run
the exact VR-04 deterministic ladder from the beginning. Prior partial or live
evidence does not substitute for any gate.

1. **Integrity/static:** prove the exact one-file/two-literal delta, frozen
   product/probe blobs, seven placeholders, authored geometry, min-`44`
   mechanism, both prior placeholder assertions, immutable media, JSON parse,
   `node --check`, `git diff --check`, forbidden-change scan, and no tracked
   drift.
2. **Focused:** run exact `FRRC-002-v1.entries.focused` within `30s`; require
   **`68/68`**. The corrected static assertion must pass because it truthfully
   matches the unchanged operative manifest, not because a legacy token was
   added elsewhere.
3. **Related:** run exact `entries.related` within `60s`; require `74/74`.
4. **Cold full:** run exact `entries.full` within `60s`; require `972/972`.
5. **Validators:** run the exactly forty sorted `validate*.py --self-test`
   invocations; require `40/40`.
6. **Build and served identity:** require exact production/fixture builds
   `217/57`, then exact owned previews and root/deep/JS/CSS served-to-disk
   identity.
7. **PBA/media/offline/performance:** require JavaScript `1,666,665`, CSS
   `119,247`, modules `217`, media `17 / 37,410,731`, no product/request/
   dependency drift, Host 05 `<=2ms`, and total sampled tasks `<=100ms`.

Any deterministic failure is immediate `HOLD`. Combat must not start previews
unnecessarily, create an external QA root, run the E2E, or repair another file
after a failure.

## Sole fresh E2E, summary, and verifier

Only after every deterministic gate above passes, Combat is authorized exactly
one fresh complete `FRRC-002-v1` E2E within `180s`, using:

- frozen runtime product candidate `7e85154abd8dbf116c4bb84ca66afd859903d750`;
- frozen browser-probe candidate
  `d9487d8205174a7b5f688cbfccbcd5f7875ac1ad`;
- the separately committed VR-05 validation-control candidate identity;
- operative shell identity `FRSH-003-v1-VR-04` inside the unchanged
  E2E/summary/verifier contract; and
- one new nonexisting GUID-suffixed root strictly inside resolved OS temp and
  outside the repository.

The one journey and its one machine-produced `first-run-live-summary.json`
must satisfy every unchanged VR-04 journey, six-layout, source, label, focus,
motion, later-rail, both-outcome, null, performance, runtime-error, ownership,
and no-successor gate. Only after the E2E succeeds and emits exactly one
summary may Combat invoke the unchanged independent live-summary verifier once.

If the E2E fails, aborts, times out, omits the summary, or the verifier fails,
no rerun is authorized. Preserve exact evidence, perform exact owned cleanup,
issue `HOLD`, and return to Mission.

## Permitted files and downstream report

Before execution, Combat may change only:

- `horizon-archive-game/test/sixfoldWeir.test.js` as the exact one-line
  assertion alignment above.

After execution, Combat may additionally change only:

- `Production Pipeline/First Run/FIRST_RUN_FUNCTIONAL_REPORT_FRCE-003-v1.md`
  or one versioned bounded Combat return report; and
- `NEXT_INSTANCE_HANDOFF.md`.

No App/product source, E2E, manifest, shared CSS, fixture, package, dependency,
lockfile, curriculum, validator, save, story, map, scoreboard, media, copy, or
other control file may change.

Combat may issue `PRODUCTION FUNCTIONAL / BUILD CANDIDATE READY` only if the
same frozen runtime product, frozen probe, and exact validation-control
candidate pass every deterministic and live gate; one summary is accepted;
the verifier passes once; cleanup is exact; and the Combat report/handoff
commit is pushed and synchronized. Quartermaster may begin only from that
passing synchronized handoff.

## Rollback, hard stops, and protected boundaries

Bounded rollback removes only the one test expectation change and restores the
test blob at Mission source. It does not alter either frozen candidate, rewrite
history, reset product, migrate save, or touch protected/untracked/user state.

Immediate `HOLD` applies to any product/probe/E2E/manifest/media/copy change;
extra test hunk; legacy-token workaround; threshold/tolerance/count/timeout
change; second E2E; identity drift; incomplete cleanup; or change to learning,
save, route, world, later rail, equal ending, null deltas, or `successor=null`.

The protected PDF, training directory, repository QA quarantine, Martin's
browser/profile/save, hidden lore, and all image/media operations remain
forbidden. Automation, archived workflows, schedules, reveals, Host 06-15,
City repair, RP-013, and post-ending work remain outside authority.

## Mission validation, signature, and exact Combat handoff

Mission read the current synchronized handoff, full Mission profile, complete
latest `FRCE-003-v1`, complete `FRSH-003-v1-VR-04`, exact frozen assertion at
`sixfoldWeir.test.js:146`, and the current operative manifest/E2E literals at
source `9ef42b45daf2fd1cef276e292ee29da5168b289c`.

Mission verified:

- `7e85154` is an ancestor of `d9487d82`, and `d9487d82` is an ancestor of
  current control;
- `d9487d82` introduces exactly the two authorized probe-file changes;
- current `App.jsx` and `sixfoldWeir.test.js` blobs still equal the frozen
  `7e85154` blobs;
- the manifest verifier and E2E already use exact VR-04 / `Tab -> Shift+Tab`;
- the frozen test alone asserts the superseded VR-02 / reversed path; and
- no product, threshold, meaning, media, learning, save, route, world, later-
  rail, or ending decision is required to resolve that static mismatch.

Mission ran no product test, validator, build, preview, browser, E2E, summary,
or verifier and inspected no protected/user state.

Mission Captain signs **`FIRST RUN SHELL READY /
FRSH-003-v1-VR-05`**.

Exact next owner is Combat Engineer. Read this reissue, complete operative
`FRSH-003-v1-VR-04`, complete latest `FRCE-003-v1`, and Combat profile. Change
only the one frozen static assertion as specified, freeze one validation-
control candidate, and run every deterministic gate from the beginning.
Invoke exactly one fresh E2E only after all deterministic gates pass; require
one machine summary and one passing verifier. Any failure is `HOLD` without
rerun.

Do not change product/probe/E2E/manifest/CSS/module/media/copy/learning/save/
route/world/later-rail/ending/maturity/map/scoreboard, inspect protected/user
state, begin Quartermaster, repair City, expand Host 06-15, create a schedule/
reveal, or call `FIRST RUN COMPLETE`.

The dedicated Mission commit, push, and exact `HEAD == origin/main` proof are
reported from Git after commit because this artifact cannot contain the hash
of the commit that first contains itself.
