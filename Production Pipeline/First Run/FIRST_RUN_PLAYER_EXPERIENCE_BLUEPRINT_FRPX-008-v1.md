# Horizon Archive First Run Player Experience Blueprint - Severed Relay Spine

Blueprint ID: `FRPX-008-v1`

Stage / owner: Tactical Operations Specialist / `tactical_operations_specialist`

Disposition: **`PLAYER EXPERIENCE READY / HOST 08 NULL-FIRST / FIVE CANDIDATE RECORDS`**

Shell / treatment: `FRSH-008-v1` / `FRDT-008-v1`

Source inspected: `080db44c`

## State and replacement graph

Current release: lawful Host 07 plus strict `L03-01` and `L03-02` mastery
shows the existing generic Client Bridge continuation. Inert Combat state has
`source.enabled=false` and changes nothing. Later accepted state requires the
same predecessor plus exact source/provenance/measurement/decode lawfulness;
one committed Host 07 paint then replaces the source with Host 08, focuses one
native target once, emits one factual polite notice, and removes the generic
Client Bridge launcher. Guard loss after enablement hides Host 08 and never
restores the launcher.

State derives from sanitized Client Bridge evidence only: absent ->
`available`; remediation -> `remediation_required`; any lawful primary,
transfer, retrieval, or explanation progress -> `in_progress`; strict mastery
-> `complete`; malformed/forged/contradictory -> `hidden`. LOOK and TALK retain
state and write nothing. USE opens/resumes unchanged `L03-03`; complete USE is
read-only. Close/Escape returns focus to Host 08 if lawful, otherwise Host 07
or the first lawful control. Reload/return/resume reconstruct from sanitized
evidence and never replay the arrival notice.

## Null-first registry

Combat declares source `{enabled:false,path:null,sha256:null,byteLength:null,
width:null,height:null,format:null,color:null,attemptId:null}`, separate null
provenance, rectangles for complete relation/fracture continuity/pressure
continuity/Host07 handoff/dry approach/damaged volume/semantic target/label,
six null layout records, seven null final-purpose copy slots, and null alt.

Accepted attempt ID must be exactly `H8-1`..`H8-5`; path, prompt/hash,
CLI hash, bytes/SHA, `3840x2160`, opaque PNG, provenance, geometry, layout, and
decode must agree. No ordinal scalar exists. Media cap is `1..30000000` and
aggregate `<=83320925`.

## Interaction and accessibility

There is exactly one selected image and one native Host 08 button. Pointer,
touch, Enter, Space, speech-by-name, and switch-like activation converge on one
click path guarded against mixed-input duplicate opens. Accessible name is
`${verb.toLowerCase()} Severed Relay Spine, ${state}`. Source order is image,
target, scene status, LOOK/TALK/USE, factual dialogue, existing navigation.
Hidden/unavailable states have no DOM, focus, name, announcement, action,
credit, or save presence.

One nonpersisted transition token performs one animation-frame recheck, one
source replacement, one `preventScroll:true` focus, and one polite factual
notice. Reduced motion keeps the same committed order without decorative
travel; forced colors preserve target and focus outline without image-derived
meaning. Effective-200% and narrow layouts have no horizontal escape.

## Copy and physical record

Quartermaster owns seven slots: unseen visible materials; available provisional
Pilot name plus Suit compatibility; in-progress sanitized boundary; missed one
checked learning dimension with answer-free remediation; mastered finalized
human evidence and unchanged world; returned unchanged relation; nextBoundary
only existing Text Analysis. Alt is one factual sentence describing collapsed
laminates, one fractured continuity, one attached wallward continuity, and a
dry observation ridge—no purpose, communication, correctness, or response.

Every selected source carries normalized rectangles and the six exact layout
records required by `FRVE-008-v1`. Each layout stores viewport, rendered and
visible source, retained area, every mapped physical/semantic/label/protected
rectangle, intersections, logical order, and before/after focus bounds. All
Science thresholds fail closed.

## Private candidate decision

Each launched attempt has a bounded record: attemptId, immutable prompt path/
bytes/SHA, CLI identity, launched, exit, ordinary/one-link/length/SHA, strict
PNG/decode/opacity, technical/physical/layout/accessibility booleans,
allowlisted reason codes (`TECHNICAL`, `PHYSICAL`, `LAYOUT`, `ACCESSIBILITY`),
identity reproof, cleanup, and terminal disposition. Rejected records retain no
path/hash/pixels/description. Nonzero exit stops. Objective rejection plus
cleanup enables only the next fixed attempt. First PASS stops all later calls.

## Combat acceptance matrix

Combat must prove: null-first values; exact five-ID/source/provenance guard;
independent fail-close for source, provenance, geometry, layout, and decode;
Host07+strict L03-01/L03-02 sole predecessor; generic launcher visible iff
`enabled!==true`; enabled-invalid never restores it; hidden zero-presence;
single image/target and no Host07 target on Host08 source; LOOK/TALK zero-write;
USE unchanged L03-03 and complete read-only; exact evidence-to-state mapping;
one transition/focus/notice; lawful fallback focus; close/reload recovery;
single native dispatch; six-layout thresholds; 44px target; forced-color/
reduced-motion parity; no Demo Tour node/credit; no Host08 save/private field;
fixed prompt/CLI ledger; nonzero stop; rejection cleanup; PASS stop and
byte-identical import; zero evidence from media/presentation; accepted Hosts
01-07 immutable; no canon expansion; RP-012 and `successor=null`; PBA/tests/
builds/served/E2E/cleanup gates.

Combat may change only dedicated Host 08 registry/state/view/copy/styles and
focused tests plus required App integration. It must not populate selected
values, generate, inspect, import, or reveal media. Any ambiguity is HOLD.

Tactical signs **`PLAYER EXPERIENCE READY / FRPX-008-v1 / ROUTE FRESH COMBAT`**.
