# Horizon Archive First Run Polish Viability Variance

Variance ID: `FRVE-005-v7-VR-06`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`HOLD / FRESH PARENT HARNESS REJECTED BY WINDOWS COMMAND-LINE
LIMIT BEFORE FIXTURE ENTRY / NO RETRY / FRVE-005-v7-VR-06`**

Date: **2026-08-11**

Science source inspected:
`8dd3ce6c9eba6c4ca696e91fbbcb6896ec18c041`

Mission correction: `FRSH-005-v1-VR-09`

Prior Science return / decisive viability: `FRVE-005-v7-VR-05` /
`FRVE-005-v7-VR-03`

Quartermaster return: `FRCA-005-v3`

Effective shell: `FRSH-005-v1` through `FRSH-005-v1-VR-09`

Effective treatment / blueprint: `FRDT-005-v1-VR-01` /
`FRPX-005-v1-VR-01`

Functional report: `FRCE-005-v1-VR-05`

Corrected inert code candidate:
`f4b2062508f9e0606953a4cc9bcdaff09b66ebc4`

Corrected candidate tree:
`92b22fc56d79d18cf1abf7213c5268c9e4149830`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Exact next recipient: one fresh Office of Science Administrator /
`office_of_science_administrator`

## Science decision

Science read the complete active intake, workflow, registry, full Science
profile, `FRVE-005-v7-VR-05`, `FRSH-005-v1-VR-09`, the cited prior Science,
Quartermaster, Work Order, effective-shell, treatment, blueprint, functional,
and immutable-manifest controls, and the exact pre-helper execution boundary.
Starting `HEAD` was exact
`8dd3ce6c9eba6c4ca696e91fbbcb6896ec18c041`. The tracked tree was clean;
the three previously disclosed untracked protected items were left untouched.

The fresh Science parent harness was prepared to preserve the exact frozen
launcher identity `2001 / 96feaf7e...`, exact pre-helper identity
`976 / 5cd257c9...`, exact executable and child argument order, in-memory
stderr retention, and exact-one fixture semantics. Its only intended runtime
change from `FRVE-005-v7-VR-05` was the authorized correction: set
`$ErrorActionPreference='Continue'` only around each native child invocation,
capture the child stderr object and `$LASTEXITCODE` in memory, and immediately
restore the prior preference.

Before that parent could enter, Windows rejected the Science-owned encoded
parent command at process creation with:

```text
The filename or extension is too long
```

Exact `powershell.exe` parent execution therefore never began. The frozen
launcher was not invoked even once; no success or negative child branch ran;
and no `PH01..PH08`, allowlist, `UNLISTED`, malformed/oversized-collapse, or
post-root cleanup evidence was produced. This is a fresh Science parent-
harness transport defect. It neither disproves nor changes the frozen Mission
launcher, pre-helper, diagnostic, PS5.1 root primitive, or cleanup contract.

The explicit no-retry boundary prohibits another parent transport or fixture
in this heartbeat. Science issues **`HOLD`** and does not route to Mission or
Quartermaster.

Variance classification: **`REQUIRED CORRECTION / SCIENCE FIXTURE PARENT
PROCESS TRANSPORT / OPEN`**.

## Evidence available and unavailable

Available evidence:

- exact source and intake identity were verified before the attempted launch;
- every controlled helper, live, ordinal, raster, and provenance target was
  absent before the attempted launch;
- Windows rejected the parent before PowerShell execution and before the
  exact launcher child could start; and
- the same complete exact-path absence matrix passed after the rejection.

Unavailable because the fixture never entered:

- independent execution-time recomputation of the launcher and pre-helper
  identities inside the fresh fixture;
- exact 64-bit Windows PowerShell Desktop `5.1.26100.8875` child success;
- silent ordered `PH01..PH08` success and exact ordinary/non-reparse root
  creation/cleanup;
- every locally inducible first-failure record and allowlist bound;
- class/FQID replacement to literal `UNLISTED`;
- missing, extra, reordered, multiline, non-ASCII, over-`640`-byte, zero, and
  multiple-record collapse; and
- identity-conditioned launcher cleanup after post-root failure.

No partial assertion or planned harness source is accepted as fixture proof.

## Final root, ordinal, product, and protected-state proof

Independent exact-path checks after the rejected parent launch proved:

```text
helperRootAbsent=true
helperDllAbsent=true
liveRootAbsent=true
ordinal2StageAbsent=true
ordinal2TargetAbsent=true
ordinal3StageAbsent=true
ordinal3TargetAbsent=true
productRasterAbsent=true
productProvenanceAbsent=true
```

The attempted parent launch did not begin, so credential reads, request
constructions, `SendAsync` calls, direct Image API sends, and ordinals consumed
are each exact `0`. Historical ordinal `1` remains permanently consumed,
opaque, inaccessible, and unchanged. Ordinals `2` and `3` remain unstarted
and unconsumed.

No helper was compiled or loaded. No source file, helper root, DLL, live root,
request, response, raster, provenance, media byte, or pixel was created or
opened. Repository QA quarantine, the protected PDF, training tree, Martin's
browser/profile/save, hidden lore, accepted-media pixels, OS-temp parent,
ordinal-1 residual, managed directory, user work, VR-65, and every opaque
residual remained untouched. All inherited OPEN records remain separate and
OPEN.

## Required fresh Science correction

One fresh Office of Science Administrator begins from the committed handoff
and runs exactly one new credential-cleared, no-request fixture. It must use a
bounded Science parent transport that does not exceed the Windows command-line
limit and does not introduce a child source file, stdin, diagnostic file, or
alternate environment transport. The exact launcher, pre-helper, environment
name, child executable, child arguments, controlled paths, predicates, record
schema, allowlists, and cleanup rules remain frozen.

The parent must still apply the sole runtime correction from
`FRVE-005-v7-VR-05`: scope `$ErrorActionPreference='Continue'` around only
each exact native child invocation, capture the single stderr record and
`$LASTEXITCODE` in memory, and immediately restore the prior preference. It
may not suppress, rewrite, discard, or file-redirect stderr.

That one fixture must independently repeat exact identities and full success,
then complete every locally inducible first-failure, allowlist, `UNLISTED`,
malformed/oversized collapse, ownership-conditioned cleanup, and final
absence assertion. Only a PASS may issue a new `POLISH VIABILITY READY` and
return to one fresh Mission Captain for an explicit Quartermaster retry
decision. Failure remains an exact Science `HOLD` with no retry.

## Maturity, changes, and hard stops

Maturity impact: none. `FR-03` remains continuity `FR2`; physical-host
expression remains `FR0 - 1 accepted shared compression / 1 exact / 10
missing`; learning `FR2`; behavior/save/recovery `FR1`; content `FR2`;
presentation `FR3`; and prior bounded release proof `FR4`.

Science changed no product, test, runtime, lesson, save, media, manifest,
candidate, map, scoreboard, maturity, process classification, residual,
schedule, or automation state. The only repository changes authorized for
this stage are this variance and synchronized `NEXT_INSTANCE_HANDOFF.md`.

Science did not run the fixture child, call the API, inspect media/pixels,
compile/load the helper, allocate the live root, consume an ordinal, change
product/tests, run a build/browser/E2E, reveal, advance maturity, close an OPEN
record, access a residual or VR-65, schedule, automate, push, release, or call
`FIRST RUN COMPLETE`.

Office of Science Administrator signs **`HOLD /
FRVE-005-v7-VR-06`** from exact source
`8dd3ce6c9eba6c4ca696e91fbbcb6896ec18c041`.
