# First Run Shell Variance - Strict PNG Data-URL Ingress

Variance ID: `FRSH-005-v1-VR-06`

Disposition: **`FIRST RUN SHELL READY / STRICT PNG DATA-URL INGRESS AND
BOUNDED OPAQUE BUILT-IN MANAGED RESIDUAL POLICY FROZEN /
FRSH-005-v1-VR-06`**

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-005-v3` / `FRVE-005-v3`

Superseded Work Order / prior viability: `FRWO-005-v2` / `FRVE-005-v2` /
`FRVE-005-v2-VR-01` / `FRVE-005-v2-VR-02`

Base shell / effective prior Mission variances: `FRSH-005-v1` /
`FRSH-005-v1-VR-01` / `FRSH-005-v1-VR-02` /
`FRSH-005-v1-VR-03` / `FRSH-005-v1-VR-04` /
`FRSH-005-v1-VR-05`

Quartermaster return: `FRCA-005-v1 / HOLD`

Combat functional close: `FRCE-005-v1-VR-04 / PRODUCTION FUNCTIONAL`

Mission source inspected:
`b6d2c235687020be5db0f06aba7d38958410be58`

Frozen inert code candidate:
`02d957e9d69dc7986928a391c37f899784f73ea5`

Frozen candidate tree:
`09da6293d72c8123b8d9673bd8e41329338e8d13`

Immutable accepted-media manifest: `FRAM-001-v1`, file SHA-256
`a674c337b377de113eaa4c6763bd431afc73ab613374a76501aa8c5ce37e5437`,
exact `17 / 37,410,731`, canonical digest
`c7ca95201029b490f2460a846e3dc2a64a26775b57e8c587cbc2d874df654d99`

Released predecessor: `FRAB-003-v1 / FIRST RUN PASS RELEASED`

Date: **2026-08-11**

Exact next recipient: one fresh Quartermaster / `quartermaster`

## Mission adjudication

`FRWO-005-v3` and `FRVE-005-v3` lawfully and completely answer the planning
return in `FRSH-005-v1-VR-05`. The future built-in result's direct
`image_url` value may replace the superseded direct-managed-path ingress only
when it is exactly one canonical PNG data URL. The payload is transient
production state; one index-only pass validates it, one fixed-buffer streaming
decoder writes it once to the sole create-new OS-temp candidate, and an
independent exact-file stream proves byte count and lowercase SHA-256 identity
before any PNG, browser-decode, pixel, or composition work.

This variance replaces only the base shell's direct `output_hint` pathname,
managed-file-to-temp identity, mandatory managed-file deletion, and associated
ordinal-1 opaque-stop rules. It does not reinterpret them. `FRWO-005-v3` is
the versioned Work Order authority for the replacement, and `FRVE-005-v3`
proves the replacement technically viable and fail-closed.

Every unchanged product, continuity, prompt, physical, composition, state,
learning, privacy, save, accessibility, performance, offline, provenance,
validation, rollback, no-reveal, process, route, world, ending, and protected-
state field in `FRSH-005-v1` through `FRSH-005-v1-VR-04` remains exact.
`FRSH-005-v1-VR-05` remains the historical `REVISE` that required this
complete Operations -> Science -> Mission sequence; this variance resolves
that return prospectively and does not rewrite its facts.

The shell is conflict-free and reaches **`FIRST RUN SHELL READY`**. Because
Combat already proved the frozen inert candidate and immutable accepted-media
manifest, the next stage is Quartermaster at generation ordinal `2`, not a
restart of Recon, Tactical, or Combat.

## Bounded player-visible outcome and maturity

The one bounded outcome remains:

```text
exact Host 05 / Sixfold Weir mastery
-> lens-like fragment handoff
-> one distinct local dry Host 06 / Stranded Lens Cradle
-> sole unchanged L02-03 entry and loop
-> unchanged next Drowned learning boundary
```

The changed boundary begins only after exact sanitized Host 05 / `L02-02`
mastery and hard-stops immediately after exact `L02-03` mastery. There is one
short observation/reorientation beat, no new scored work or mandatory detour,
and no Host 07 or later physical expression.

Maturity is unchanged. `FR-03` remains continuity `FR2`; physical-host
expression `FR0 - 1 accepted shared compression / 1 exact / 10 missing`;
learning `FR2`; behavior/save/recovery `FR1`; content `FR2`; presentation
`FR3`; and prior bounded release proof `FR4`. Only Intelligence may later
record `1 accepted shared compression / 2 exact / 9 missing` from accepted
as-built evidence.

## Frozen ordinal and opaque-residual ledger

- The total generation-ordinal domain remains exactly `{1,2,3}`.
- Ordinal `1` is permanently consumed. It is neither a candidate nor a
  `PHY-01..12` rejection and may never be retried, reinterpreted, recovered,
  inspected, decoded, discovered, or cleaned.
- The only remaining ordinals are exactly `2` and `3`, in that order. Maximum
  future calls is `2`. Ordinal `3` is conditional and may occur only after
  ordinal `2` becomes a fully identified objective rejection and its exact
  temp candidate and GUID child are successfully deleted and proved absent.
- Selection at ordinal `2`, an opaque stop, a cleanup failure, or any other
  terminal condition forbids ordinal `3`. Ordinal `3`, if reached, is final.
  No fourth call, retry, replacement ordinal, parallel call, or out-of-order
  call exists.
- Each consumed ordinal is conservatively charged at most one possible
  tool-owned managed residual. Ordinal `1` therefore counts as possible
  residual `1`; consuming ordinal `2` makes the conservative count `2`; and
  consuming ordinal `3` makes it `3`. The maximum total ordinal-associated
  managed-residual count is exactly `3`; a fourth count is forbidden.
- Every such possible residual is exactly **`DEFERRED LIMITATION /
  RELEASE-PROCESS ONLY / NON-GATING / OPAQUE BUILT-IN MANAGED RESIDUAL`**.
  It has zero project, asset, candidate, provenance, evidence, reuse, reveal,
  rollback, canon, maturity, release, or cleanup authority. A conservative
  count is not proof that any file exists.
- No role may resolve, list, search, parse, infer, inspect, render, preview,
  hash, copy, move, rename, delete, clean, or prove absence of the managed
  parent or an opaque managed output. Discarding result references is not
  managed cleanup.
- VR-65 remains separately **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
  NON-GATING / OPAQUE EXTERNAL QA RESIDUAL`**. It is not included in the
  three-count, and no ordinal residual may be merged with or used to infer it.
- If any later controlling policy requires zero default retention, the only
  lawful outcome is `HOLD / MARTIN DECISION REQUIRED`; managed discovery or
  cleanup is never an alternative.

## Exact future built-in call

Prompt ID: **`HOST06-GEN-PROMPT-v1`**

For each lawful future ordinal, Quartermaster invokes only the built-in
`image_gen__imagegen` generation tool with exactly this argument object:

```text
{ prompt: HOST06-GEN-PROMPT-v1 }
```

Both image-input fields are omitted: no `referenced_image_paths` and no
`num_last_images_to_include`. There is no conversation image, accepted-media
input, reference image, edit target, variation, image edit, CLI, REST/API,
browser, extension, alternate generator, manual file, upscale, derivative,
post-process, conversion, or fallback. Calls are sequential and each lawful
call seeks exactly one output and one direct payload.

### `HOST06-GEN-PROMPT-v1` exact text

```text
Use case: stylized-concept
Asset type: final runtime 16:9 first-person environment plate for Horizon Archive Host 06
Primary request: Create one premium contemporary, maximum-quality cinematic science-fiction environment view of the Stranded Lens Cradle in the existing Drowned Archive basin. Show one visibly weathered lens resting inside one tilted conformal cradle on an already-dry, above-water local reach. The scene must preserve quiet scientific uncertainty about whether the lens was stranded by a former phase or is exactly where it belongs.
Scene/backdrop: One local next reach of the same enormous flooded Builder phase-processing basin, continuous with the prior Sixfold Weir rather than a second basin, teleport, cutaway, or lesson diagram. Keep a ruler-straight horizon and reflected horizon, restrained atmosphere, and physically legible old process routing.
Subject: The complete lens-and-cradle relation is the single discovery center. At least 80 percent of the visible lens body lies within the projected inner cradle. Tilt the cradle principal axis 12 to 35 degrees from image horizontal. Show at least two distinct load or stress-control contacts coupling lens, cradle, and supporting fabric; at least two continuous drainage seams leading away toward basin drainage; at least 2 percent of source height of dry clearance above live water; and one continuous dry material approach. Show at least three lens-like fragments leading from the Host 05 side toward but not into the activation region. Make the horizon or its reflection cross at least 25 percent of the visible inner lens width.
Style/medium: Feature-film environment credibility and flagship current-generation game key art; photorealistic materials, physically convincing smoky phase glass, ceramic ribs, mineral deposition, corrosion, abrasion, dust, inclusions, refraction, internal reflection, caustics, repair seams, and several visible stewardship eras. Builder beauty must read as accumulated functional civic construction, engineered ecology, maintenance, revision, graceful failure, and nonhuman process rather than generic fantasy ornament or human industry.
Composition/framing: Exact wide 3840 by 2160 landscape intent, first-person eye level, centered full-source presentation with no required crop. Keep the top 10 percent free of essential physical fact. Place the complete Host 06 relation wholly within normalized x 0.34 to 0.70 and y 0.30 to 0.74, with a tight relation rectangle width 0.30 to 0.36 and height 0.30 to 0.40. Place the Host 05 fragment approach wholly within x 0.04 to 0.30 and y 0.56 to 0.88. Reserve same-basin distant context within x 0.70 to 0.98 and y 0.08 to 0.56. Live water may occupy x 0.68 to 0.98 and y 0.62 to 0.92 but must not intersect the relation or dry approach. Keep the relation distinct from live water, the Host 05 cue, a return-like ridge, the Crown, the distant suspended Tidal Lens, and any second lens/cradle candidate.
Lighting/mood: Restrained natural basin light, horizon reflection caught by the inner lens, sober discovery, deep atmospheric scale, no magical glow, spectacle cue, invitation, reward, activation flash, or world response.
Text: No text.
Constraints: No image input. No protagonist, body, hands, shadow, reflection, portrait, companion, ship, person, face, human path, human control, label, glyph, symbol, interface, overlay, beacon, readable mark, prior-human trace, or native educational graphic. The Machine and Builders do not speak, react, recognize, reward, authorize, invite, judge, explain, move, heal, aim, drain, illuminate, or open anything because of the player. Preserve physical ambiguity and surface-safe canon.
Avoid: Empty ring; lens beside, behind, through, or in front of an aperture; submerged or water-filled cradle; inaccessible scenic landmark; duplicate lens/cradle relation; the distant Tidal Lens as Host 06; generic ruins; terrestrial industrial shorthand; arbitrary fantasy ornament; answer key; model or deployment diagram; purpose certainty; hidden-lore answer; watermark; border; crop-dependent essential fact.
```

Prompt intent and tool success never prove count, transport, format, pixels,
composition, meaning, or acceptance.

## Exact temp creation and direct-result firewall

Before ordinal `2` or conditional ordinal `3`, Quartermaster must:

1. independently prove exact `HEAD`, shell, frozen candidate/tree, immutable
   manifest identity, current ordinal, prior disposition, residual count,
   absent product raster/provenance, null registry/copy/alt slots, and no
   tracked delta outside the stage;
2. resolve the OS temporary parent as one scalar without listing it;
3. create exactly one fresh direct child named
   `horizon-archive-frwo005-<fresh GUID in D format>` with create-new
   semantics and prove strict containment under that parent and separation
   from the repository, `$CODEX_HOME` managed root, accepted-media paths,
   protected/user/browser/profile/save/QA roots, VR-65, and every residual;
4. predeclare only the absent exact target `attempt-0N.png`: exactly
   `attempt-02.png` for ordinal `2` or exactly `attempt-03.png` for ordinal
   `3`; and
5. use no glob, recursive operation, parent enumeration, discovery, fallback
   path, or broad cleanup.

The one tool return is accepted only when all of these are true:

- it is one direct non-array result object, not an array, wrapper, iterable,
  nested candidate, accessor-bearing surrogate, or multiple-result shape;
- it has one direct own data property named `image_url` whose value is one
  primitive string; the adapter performs no traversal or coercion;
- that string is the only candidate-bearing value consumed and contains
  exactly one PNG data URL; and
- every other result field, including `output_hint`, remains completely opaque
  and unread. It is never parsed, quoted, exposed, retained, logged, searched,
  or used as a path, payload, model claim, cleanup target, or evidence.

Arrays, wrappers, multiple results/payloads, accessors, missing or non-string
`image_url`, nested alternatives, remote or alternate URL, blob, file, path-
like text, or ambiguous shape is an **opaque transport stop**. The ordinal is
consumed and the only permitted recovery is exact cleanup of the predeclared
temp file if created and the exact empty GUID child where safely provable.

## Literal data-URL grammar and bounded one-time decode

The direct string must satisfy all of these rules before decode:

1. The exact literal 22-code-unit prefix `data:image/png;base64,` occurs at
   offset zero exactly once.
2. Total string length is `26..16,000,022` UTF-16 code units. Payload length
   is therefore `4..16,000,000`, begins at offset `22`, and is divisible by
   four. The upper bound is exact:
   `4 * ceil(12,000,000 / 3) = 16,000,000` payload characters plus the
   22-code-unit prefix.
3. One index-only, non-copying validation pass permits only ASCII `A-Z`,
   `a-z`, `0-9`, `+`, `/`, and zero, one, or two final `=` characters.
   Whitespace, CR/LF, control/non-ASCII code units, percent encoding, URL-safe
   alphabet, comma, colon, semicolon, fragment/query syntax, second prefix or
   separator, early/excess padding, and trailing material reject.
4. Padding is canonical RFC 4648: `==` requires zero low four pad bits in the
   second sextet; `=` requires zero low two pad bits in the third sextet.
5. With checked integer arithmetic, compute only
   `(payloadLength / 4) * 3 - paddingCount` and require exact decoded length
   `1..12,000,000` before allocation or write.
6. The adapter may not call `split`, `substring`, `slice`, `replace`, a regular
   expression, `atob`, `Buffer.from(fullPayload)`, or another whole-payload
   copier. It may not create a second transport string or full decoded buffer.

After grammar passes, open only the exact predeclared `attempt-0N.png` target
with create-new semantics. Decode every quartet exactly once through the same
fixed lookup table used by validation and write sequentially through one
reusable buffer of at most `196,608` bytes. During that single decode, count
decoded bytes and update one SHA-256 state. Require count equal the computed
decoded length, flush, and close before any read. No second decode, second
materialization, normalization, repair, conversion, export, derivative, or
alternate file exists.

Reopen only that exact temp candidate read-only and independently stream it
once to EOF, counting bytes and computing lowercase SHA-256. Require exact
equality with decode-time byte count and digest before any PNG, metadata,
browser-decode, pixel, or `PHY-*` work. Then clear the reusable buffer and
release base64/result/decoder references. Reference disposal is in-process
only and never claims managed-output deletion.

An allocation, memory-pressure, create-new, partial-write, close, length,
digest, or identity failure is a consumed-ordinal opaque stop. Close exact
handles, prove exact child/file identity, delete only the exact created temp
file if present, prove it absent, delete only the exact empty GUID child
nonrecursively, and prove it absent. Cleanup uncertainty is terminal.

## Exact pre-pixel PNG, color, opacity, size, and decode gate

Only a payload/temp-identical candidate may enter the same exact temp file's
read-only parser. It must pass all of these gates:

1. Exact PNG signature bytes `89 50 4E 47 0D 0A 1A 0A`; exact file length
   equal the already-proved `1..12,000,000` bytes.
2. Every chunk has a checked big-endian unsigned length, four exact letter
   type bytes, in-bounds data, and valid CRC over type plus data. Reject
   arithmetic overflow, length beyond remaining bytes, chunk length above
   `2^31-1`, non-letter type bytes, a lowercase reserved/type byte, or any CRC
   mismatch.
3. Exactly one first `IHDR`, length `13`, with width `3840`, height `2160`,
   bit depth `8`, color type `2` truecolor, compression `0`, filter `0`, and
   interlace `0`. This is RGB with no alpha channel.
4. Exactly one `sRGB` before first `IDAT`, length `1`, intent `0..3`.
   Optional `gAMA` is allowed at most once before IDAT only with length `4`
   and value `45455`. Optional `cHRM` is allowed at most once before IDAT only
   with length `32` and exact values
   `31270,32900,64000,33000,30000,60000,15000,6000`. Optional `pHYs` is
   allowed at most once before IDAT only with length `9`, nonzero X/Y density,
   and unit `0` or `1`.
5. One or more consecutive `IDAT` chunks with nonzero aggregate data, then
   exactly one last empty `IEND`, with no trailing byte. Reject nonconsecutive
   IDAT, empty aggregate, duplicate required chunks, and every other chunk,
   including `PLTE`, `tRNS`, `iCCP`, `sBIT`, text, EXIF, time, unknown/private,
   or APNG chunks.
6. Stream-inflate the concatenated IDAT zlib stream once without unfiltering,
   rendering, retaining, or inspecting pixels. Require one complete zlib
   stream, no compressed trailing data, exactly `24,885,360` structural bytes
   (`2160 * (1 + 3840 * 3)`), exactly 2160 scanlines, and filter byte `0..4`
   at each `11,521`-byte scanline boundary.
7. Directly decode that exact temp file in a clean isolated headless browser
   at natural `3840 x 2160`. There is no repair, conversion, alternate file,
   screenshot, preview, user profile, emitted image, or reveal.

The combination of 8-bit color type `2`, required `sRGB`, and forbidden
`tRNS`/alpha/profile alternatives proves opaque 8-bit sRGB semantics. MIME and
prompt text are never source proof. A failure here is an **objective source
rejection**, not an opaque transport failure, because candidate identity is
already complete.

## Objective original-resolution physical and composition rubric

After every pre-pixel gate passes, Quartermaster reviews the original
`3840 x 2160` candidate and rejects it on any failed predicate:

1. `PHY-01`: no one tight rectangle contains the complete lens/cradle relation.
2. `PHY-02`: less than `80%` of visible lens lies within the projected inner
   cradle, or the lens reads beside, behind, through, or in front of an empty
   aperture.
3. `PHY-03`: cradle principal-axis tilt is outside `12-35 degrees`.
4. `PHY-04`: fewer than two distinct load/stress contacts couple lens, cradle,
   and supporting fabric.
5. `PHY-05`: fewer than two continuous drainage seams lead toward basin
   drainage without implying player-caused drainage.
6. `PHY-06`: relation intersects live water, lacks `2%` source-height dry
   clearance, or lacks one continuous dry-material approach.
7. `PHY-07`: fewer than three lens-like fragments cue from the Host 05 side
   toward but not into activation.
8. `PHY-08`: horizon/reflected horizon crosses less than `25%` of visible
   inner-lens width before interface.
9. `PHY-09`: view is not first-person or shows cutaway, diagram, teleport,
   second basin, body, hand, shadow, reflection, companion, ship, human path
   or control, readable text, glyph, face, or prior-human trace.
10. `PHY-10`: Host 06 cannot be separated from live water, Host 05 cue,
    return-like ridge, Crown, distant Tidal Lens, or a second lens/cradle
    candidate.
11. `PHY-11`: same-basin continuity is not independently legible through
    water, ceramic ribs, smoky phase glass, drainage, corrosion/mineral
    deposition, repair seams, and old process routing.
12. `PHY-12`: scene resembles a native lesson diagram, indicates a best
    answer, or implies response, recognition, reward, access, authority,
    invitation, purpose certainty, or hidden lore.

The same candidate must satisfy these normalized source bands:

| Band | Required envelope |
| --- | --- |
| top status-safe context | `x=[0,1], y=[0,.10]`; no essential physical fact |
| complete Host 06 relation | wholly inside `x=[.34,.70], y=[.30,.74]` |
| tight relation rectangle | width `.30-.36`; height `.30-.40` |
| Host 05 fragment approach | wholly inside `x=[.04,.30], y=[.56,.88]` |
| same-basin distant context | `x=[.70,.98], y=[.08,.56]` |
| live-water context | may occupy `x=[.68,.98], y=[.62,.92]`; no relation or dry-approach intersection |

One `<img>` uses `object-fit: cover` and exact `object-position: 50% 50%` at
desktop `1920x1080`, laptop `1366x768`, narrow `390x844`, effective `200%`
`768x900`, retained `320x180`, and the `320x180` world inside `320x240`.
Because both source and world are `16:9`, the full source remains visible.
Relation and approach each retain at least `99.5%` area and measured center.
Physical/activation/label/center/anchor/retention/nonoverlap/target mapping
must satisfy the unchanged `1/64 CSS px` floor-lattice contract before and
after focus; the native semantic target is at least `44 x 44 CSS px`.

One failed PNG, decode, `PHY`, source-band, six-layout, mapping,
accessibility, or meaning predicate gives the candidate zero product, reuse,
canon, maturity, publication, or reveal authority.

## Exact rejection cleanup and ordinal-3 recovery

For a fully identified objective rejection, Quartermaster may retain only the
ordinal, prompt ID, built-in tool/mode identity, gate stage, stable rejection
codes, cleanup booleans, and conservative managed-residual count/classification.
It retains no data URL, payload, result object, `output_hint`, temp/managed
path, rejected byte count, SHA, bytes, pixels, screenshot, diagnostic, or
preview.

Close every exact candidate handle, reprove the exact child/file identity,
delete only the exact `attempt-0N.png`, prove it absent, delete only the exact
empty GUID child nonrecursively, and prove it absent. No parent listing,
recursive/pattern deletion, fallback cleanup, or managed operation exists.

If ordinal `2` is an objective rejection and all exact temp cleanup proofs
pass, Quartermaster may create a new distinct GUID child and invoke ordinal
`3` once with the same unchanged prompt and adapter. The candidate is not
edited, varied, repaired, used as a reference, or carried forward. If ordinal
`2` is an opaque stop or cleanup is uncertain, ordinal `3` is forbidden.

If ordinal `3` is an objective rejection, clean only its exact temp candidate
and child as above, then issue `HOLD / ALL LAWFUL ORDINALS CONSUMED / NO MEDIA
CANDIDATE`. There is no fourth call, compromise, composite, crop, local repair,
alternate source, or return to ordinal `2`.

## Selected identity, byte-identical import, provenance, registry, and copy

At most the first candidate passing every prior gate may be selected. The two
and only two media-owned workspace paths remain:

```text
Visual Direction/Production Masters/2026-08-10-first-run-host06/host06-stranded-lens-cradle-master-v1.png
Visual Direction/Production Masters/2026-08-10-first-run-host06/PROVENANCE.md
```

Both targets must be absent. Stream-copy the selected temp candidate exactly
once to the raster target with create-new semantics, close it, and independently
require product byte length and lowercase SHA-256 to equal the selected temp
identity. Only after that equality passes may Quartermaster create the one
permitted `PROVENANCE.md`.

Provenance records Work Order/effective shell; built-in tool/mode and model
identity only if independently supplied without reading opaque result fields;
prompt ID/version and exact no-image-input declaration; ordinals consumed;
strict direct-data-URL, canonical-base64, one-decode, temp-create,
payload/temp-identity, PNG/chunk/CRC/IHDR/sRGB/opacity/dimension/size/inflate/
browser-decode results; selected product bytes and lowercase SHA; `PHY-*`,
source-band, six-layout, mapping, focus, and accessibility results; immutable
manifest digest; exact product path; accepted-media unchanged declaration;
and conservative ordinal-associated managed-residual count/classification.

Provenance contains no data URL, payload, base64, result object,
`output_hint`, managed/temp path, rejected bytes/hash/path/pixels, opaque
identity, prompt secret, credential, hidden diagnostic, or claim that a
managed residual was deleted. It explicitly says managed residuals were not
inspected, copied, moved, deleted, or used and that their count is conservative
rather than existence proof.

After provenance succeeds, populate only the predeclared Host 06 source,
physical, activation, protected, and six-layout scalars; set the source URL by
one static project import; preserve label insets `3/5`; write final-purpose
content in exactly the seven `FRPX05_COPY` slots; and write one observable-fact
`FRPX05_IDENTIFICATION.ALT`. Copy must preserve the fixed owner/meaning rules:
scene physical fact; Pilot provisional naming/uncertainty; Suit/System local
compatibility, evidence, recovery, save, and route; 901 Teacher actual missed
tradeoff only; Machine/Builders silence; no answer, purpose certainty,
response, reward, access, authority, identity, invitation, hidden lore, Host
07 expression, or changed next boundary.

Then delete only the exact selected temp candidate and exact GUID child and
prove both absent. Product-copy identity, provenance, registry/copy write, or
exact-temp cleanup failure is an opaque stop and triggers bounded rollback of
only the exact new raster, provenance, and Quartermaster product delta after
exact identity proof. Rollback never touches a managed residual, VR-65,
accepted media, a parent root, or user work.

## Candidate identity separation

| Identity | Exact meaning and authority |
| --- | --- |
| Frozen Combat code candidate | Commit `02d957e9...`, tree `09da6293...`; inert behavior/test authority only |
| Immutable accepted-media baseline | `FRAM-001-v1`, exact `17 / 37,410,731` and canonical digest; negative-control evidence only |
| Consumed invocation ordinal | Prompt ID plus ordinal `1..3` and result disposition; no candidate, media, or managed-path authority |
| Transient payload transport | One direct canonical data URL for the active ordinal; memory-only ingress, never an asset or evidence artifact |
| Temp evaluation candidate | Exact `attempt-0N.png` only after payload/temp byte-and-SHA identity; temporary rubric evidence only |
| Selected raster identity | First fully passing temp byte length/SHA copied byte-identically to the exact product path; content candidate only |
| Quartermaster content candidate | Dedicated commit containing selected raster, provenance, populated registry/geometry, seven copy slots, alt, report, and handoff; candidate only |
| Image presentation candidate | Later code/config-only presentation review commit over immutable selected bytes; candidate only |
| Intelligence release candidate | Exact frozen as-built commit and full evidence chain independently reconciled; only Intelligence may accept and advance maturity |
| Opaque managed residual | Conservative release-process count only; zero candidate, provenance, evidence, reuse, reveal, rollback, maturity, or release authority |

Copying, hashing, naming, committing, pushing, parsing, decoding, or passing a
subset of gates never promotes one identity into another.

## Fixed behavior, learning, accessibility, privacy, and world contract

The effective base shell remains exact:

- Host 06 is absent until exact sanitized Host 05 mastery, complete enabled
  source/provenance identity, finite lawful measurements, and natural
  `3840 x 2160` decode all pass. Failure leaves Host 05 safe and does not
  restore the generic bypass.
- Sole `USE Stranded Lens Cradle` calls existing `openModelChoiceExercise()`
  only in `available`, `in_progress`, or `remediation_required`; completed USE
  is read-only; LOOK/TALK write nothing.
- Existing `L02-03` assets, eight primary/eight transfer scenarios, evaluator,
  strict `16/16 + 16/16 + 2/2` progression, actual-dimension answer-free
  remediation, blank retry, explanation, ownership/confidence, sanitizer,
  private clearing, save projection/restore, no cross-credit, and next
  structured-packet boundary remain byte/behavior exact.
- Native labels remain `${verb in lower case} Stranded Lens Cradle, ${state}`;
  hidden has no node/name/announcement/focus/action/credit. Enter/Space,
  pointer, touch, and switch-like activation converge once. Focus, Close/
  Escape, return, reload, malformed-state recovery, effective `200%`, forced
  color `3px Highlight`, and reduced-motion behavior remain exact.
- No new save field/version/migration, dependency, lockfile, network runtime,
  endpoint, credential, telemetry, service worker, model call, route, world
  response, lesson, branch, reward, access, identity, authority, successor,
  RP-013, or post-ending content exists. READY and NOT YET READY retain equal
  dignity, null deltas, shared MH-40 ending, and `successor=null`.

## Performance, PBA, and no-inline-data caps

The data URL is transient production ingress only. It may not enter source,
configuration, provenance, Git, build output, browser storage, logs, reports,
previews, or reveal. A static source/diff/build gate must prove absence of
literal `data:image/png;base64,` and the selected base64 payload from all
product/build/report output. The selected PNG alone is the media delta.

| Measure | Affected cap | Global/result cap |
| --- | ---: | ---: |
| emitted JavaScript | `<=1,679,393` | `<=1,703,258` |
| emitted CSS | `<=119,547` | `<=119,672` |
| production modules | `<=218` | `<=222` |
| selected raster | `1..12,000,000` bytes | exact `3840 x 2160` PNG |
| resulting media | exact `18` and `37,410,731 + selectedBytes` | `<=49,410,731` bytes |
| source maps | `0` | `0` |
| cold/warm decode | `<=250ms / <=100ms` | direct browser decode |
| cold local image ready | `<=750ms` | one same-origin request; zero external |
| attributable / total CLS | `<=0.01 / <=0.05` | reserved `16:9` slot |
| Host 06 activation / sampled task | `<=2ms / <=100ms` | same |
| focused / related / cold full | `<=30s / <=60s / <=60s` | same |
| production / fixture build | `<=60s` each | same |
| complete E2E | one invocation `<=180s` | no retry |

Any cap overage is `HOLD`; there is no budget borrowing, data-URL inlining,
compression/edit waiver, source-map waiver, dependency workaround, or E2E
retry.

## Role freedoms and exact permitted delta

Quartermaster may choose only:

- whether ordinal `2`, and conditional ordinal `3`, objectively passes the
  frozen gates;
- the measured finite physical/activation/protected/layout scalars of the
  first passing candidate within the frozen geometry;
- final-purpose prose inside the seven frozen copy meanings and one factual
  alt slot; and
- the bounded scalar wording of its content/asset report and provenance.

Quartermaster may change only the exact selected raster, exact provenance,
`horizon-archive-game/src/drownedArchive.js`, applicable existing focused
content/source-contract tests if necessary, its exact versioned content/asset
report, and `NEXT_INSTANCE_HANDOFF.md`. It may not change behavior in
`App.jsx`, styles, lesson/evaluator/sanitizer/save code, dependencies,
accepted-media manifest or files, release-command manifests, prompt, shell,
route, world, ending, Image presentation, maturity/map/scoreboard/changelog,
or any unlisted file. A necessary behavioral or layout change returns to its
earliest owner rather than expanding Quartermaster scope.

The Image Specialist remains later and code/configuration-only around the
immutable selected bytes. Intelligence remains the only release/maturity
owner. Neither stage is pre-run by Quartermaster.

## Ordered validation ladder

Candidate-bearing work preserves this exact order:

1. authority, ancestry, exact synchronized shell, frozen candidate/tree,
   bounded diff, permitted paths, protected state, immutable manifest file
   identity/canonical digest, ordinal/residual counters, absent targets,
   parser/adapter identity, forbidden ingress, and no-reveal proof;
2. direct-result-shape and RFC 4648 boundary-vector tests for exact prefix,
   maximum `16,000,022` code units, alphabet/padding/pad bits, decoded-length
   arithmetic, one decoder invocation, bounded buffer, create-new target,
   payload/temp byte/SHA identity, reference disposal, and every partial-write
   exact cleanup branch;
3. exact PNG signature/chunk/order/CRC/IHDR/sRGB/opacity/size/inflate/browser-
   decode gates, followed only then by original-resolution `PHY-01..12`;
4. six-layout source/physical/activation/label/center/anchor/retention/
   nonoverlap/target mapping and before/after-focus stability;
5. focused Host 05-to-06 state/action/focus/return/reload/malformed/decode-
   failure tests and complete current `L02-03` learning/privacy/restore proof;
6. related Hosts 04/05, Meadow return, unchanged next boundary, no Host 07,
   Witness/City/later rail, Demo Tour, both MH-40 outcomes, null deltas, and
   `successor=null` regression;
7. exact sorted `40/40` validators, cold full suite, clean production and
   TD-012 fixture builds, exact PBA, served-to-disk identity, offline/same-
   origin, decode/task/CLS, and no-inline-data proof;
8. later, after Quartermaster and Image commits, exactly one non-overlapping
   clean-start-to-MH-40 E2E with one real `L02-03` miss/recovery/mastery,
   reload/return, equal outcomes, zero runtime errors, one summary, and one
   verifier; and
9. exact owned process/artifact/temp cleanup, opaque/protected residuals
   untouched, candidate freeze, bounded diff, and required Git synchronization.

Quartermaster may run only the tiers required to prove its content candidate;
the complete E2E remains reserved for the later release ladder and has no
retry. Automated accessibility evidence is not human assistive-technology
certification.

## Definition of done, hard stops, and rollback

Quartermaster reaches **`PRODUCTION CONTENT COMPLETE`** only when one lawful
ordinal yields one fully passing source; temp/product identity and exact
provenance pass; the one raster and one provenance file are the only media
delta; every predeclared scalar, seven copy slots, and one alt slot are lawful;
applicable focused/content/source gates pass; exact temp cleanup is proved;
no payload or managed output enters the repository or reveal; the dedicated
content candidate/report/handoff commit is exact; and the next Image stage is
synchronized. Prompt intent, tool success, partial PNG parsing, decorative
similarity, a renamed generic launcher, or incomplete cleanup is insufficient.

Immediate `HOLD` applies to a fourth call/count, nonsequential ordinal,
multiple result/payload, `output_hint` use, managed discovery/cleanup, parser
liberalization, full-payload copy, second decode/materialization, non-PNG,
alpha/non-sRGB source, chunk/CRC/metadata exception, alternate tool or
fallback, reference/edit/variation, accepted-media input/change, derivative,
identity mismatch, broad cleanup, more than one selected source, source-band/
mapping/accessibility failure, cap overage, private persistence, lesson/save/
dependency/network/route/world/ending change, protected/residual access,
reveal, E2E retry, hidden lore, or unsupported media authority.

Rollback removes only the exact new selected raster, `PROVENANCE.md`, bounded
Quartermaster registry/copy/test delta, and Quartermaster report after exact
identity proof. Restore pre-existing permitted paths only by explicit path to
frozen candidate blobs. Never reset the repository or touch an ordinal-managed
residual, VR-65, accepted media, Hosts 04/05, curriculum, save schema,
dependencies, Host 07+, later rail, user/untracked work, browser/profile/save,
repository QA quarantine, protected PDF, training directory, hidden lore, or
a parent root.

## Variance routing, protected state, and process records

Every mismatch is classified as `ACCEPTED IMPROVEMENT`, `REQUIRED
CORRECTION`, `MASTERPLAN UPDATE`, `DEFERRED LIMITATION`, or `UNAUTHORIZED
DIVERGENCE` and returned to the earliest owner. Product/media authority goes
to Commandant; canon/voice/ending to Colonel; address/scope/ordinal policy to
Operations; ingress/parser/learning/privacy/save/accessibility/performance/
offline/cleanup to Science; shell ambiguity to Mission; direction to Recon;
interaction/focus/mapping to Tactical; behavior/build defects to Combat;
copy/content/asset/provenance defects to Quartermaster; runtime presentation to
Image; and release/maturity/synchronization to Intelligence.

Repository QA quarantine, `Art Of No Mans Sky Book Scan.pdf`, `Simplilearn
Training Files/`, real browser/profile/save, hidden lore, accepted-media
pixels, managed/temp parents, user work, and every opaque residual remain
protected. No protected path is opened or changed by this Mission stage.

All thirteen inherited process records remain separate and **OPEN**: VR-17,
VR-23, VR-24, VR-25, VR-28, VR-39, VR-41, VR-46, VR-47, VR-50, VR-53 Stage 4,
VR-60 Call 9, and VR-61. The separate Commandant filename/search-scope record
remains **OPEN**. This variance closes, cures, merges, waives, or renumbers
none. Current control/status locators and the corrected literal Git-object
query are process-only recurrences under the applicable still-OPEN records;
they supplied no media, canon, candidate, maturity, or release evidence.

## Change history, Git gate, and Mission signature

- `FRSH-005-v1-VR-06` is the prospective strict data-URL ingress and bounded
  opaque managed-residual correction for `FRWO-005-v3` / `FRVE-005-v3`.
- It supersedes only the returned-path/managed-cleanup execution facts and
  ordinal-1 stop in the effective base shell. Every other shell field remains
  exact.
- Mission changed no product, test, runtime, lesson, save, media, manifest,
  candidate, map, scoreboard, maturity, process classification, residual,
  schedule, or automation state.
- Mission must commit only this variance and the synchronized Quartermaster
  handoff, push `main`, and prove exact `HEAD == origin/main == remote main`
  before Quartermaster ordinal `2` becomes active. The enclosing commit hash
  is necessarily resolved after this self-containing artifact is written.

Mission Captain signs **`FIRST RUN SHELL READY / STRICT PNG DATA-URL INGRESS
AND BOUNDED OPAQUE BUILT-IN MANAGED RESIDUAL POLICY FROZEN /
FRSH-005-v1-VR-06`** from exact source `b6d2c235...`.

Mission used the installed built-in image-generation skill only to confirm its
default `$CODEX_HOME/*` save-path policy and project-bound selected-copy rule.
Mission did not call a generator; inspect or decode a result, payload, temp
file, managed output, accepted-media path, or pixel; import media; write copy
or provenance; run a test/build/preview/browser/E2E; reveal; update maturity;
close an OPEN record; inspect VR-65; schedule; automate; release; or call
`FIRST RUN COMPLETE`.

## Exact Quartermaster handoff

After this variance and `NEXT_INSTANCE_HANDOFF.md` are committed, pushed, and
exactly synchronized, one fresh Quartermaster / `quartermaster` reads the full
active authorities, full Quartermaster profile, complete `FRWO-005-v3`,
complete `FRVE-005-v3`, complete prior viability/effective variances, complete
effective shell through this variance, `FRDT-005-v1`, `FRPX-005-v1`,
`FRCE-005-v1-VR-04`, `FRCA-005-v1`, immutable `FRAM-001-v1`, and the installed
built-in save-path policy.

Quartermaster independently proves intake, then begins with ordinal `2` only.
It follows the exact direct-result/data-URL/streaming-decode/temp-identity/PNG/
rubric/cleanup/import/provenance/registry/copy contract above; uses ordinal `3`
only after a lawful objective ordinal-2 rejection plus exact cleanup; issues
one versioned `PRODUCTION CONTENT COMPLETE` or `HOLD` ledger; commits only its
bounded delta and synchronized next handoff; and does not generate a reveal.
