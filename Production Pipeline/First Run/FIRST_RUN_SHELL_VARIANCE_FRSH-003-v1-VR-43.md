# First Run Shell Variance Reissue - One Complete Diagnostic-Control Verification

Variance ID: `FRSH-003-v1-VR-43`

Disposition: **`FIRST RUN SHELL READY / ONE COMPLETE DIAGNOSTIC-CONTROL
VERIFICATION / FRSH-003-v1-VR-43`**

Stage gate remains: **`HOLD / PRODUCTION FUNCTIONAL NOT ISSUED`**

Release state remains: **`HOLD / RELEASE GATE FAILURE / FRAB-003-v1`**

Stage / stable agent: Mission Captain / `mission_captain`

Work Order / governing shell: `FRWO-003-v1` / `FRSH-003-v1`

Command authority: `FRRC-002-v1`

Immediate return / predecessor authority: Combat VR-42 corrected formal
pre-live served-identity PASS / `FRSH-003-v1-VR-42`

Mission predecessor source:
`a6376d040798ecd11fc4d279f3d55e914012dadf`

Exact immutable product candidate:
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`

Validation control: `4cd7fbf31291671dd28c0743b44a7c49aaad82bb`

Exact diagnostic candidate:
`2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc`

Accepted evidence predecessor:
`ca89a679195c11d441a76e6c02983a6436f2ccb2`

Recorded: **2026-08-10**

## Context reuse and cumulative adjudication

Fresh-child capacity remains unavailable. Root expressly authorized this
already-active Mission Captain context to adjudicate the cumulative checkpoint
and issue this bounded shell. The reuse remains disclosed, is not candidate
evidence, and waives no validation, independence, divergence, or release
boundary.

Mission accepts the following immutable cumulative ladder without rerun:

- VR-22 focused `68/68`, related `74/74`, cold full `972/972`, and validators
  `40/40`;
- VR-30 production build `moduleCount=1 builtSubstringCount=1 nativeExit=0`;
- VR-35 TD-012 fixture build `fixtureModuleCount=1
  fixtureBuiltSubstringCount=1 nativeExit=0` and corrected immutable PBA
  JavaScript `1,667,393`, CSS `119,247`, media `17 / 37,410,731`, source maps
  `0`;
- VR-42 formal production and fixture served identity PASS: exact owned
  launches/readiness, root/deep/JavaScript/CSS HTTP and SHA-256 disk/served
  equality counts, owned cleanup, and both ports clear.

The product candidate remains byte-unchanged. The seven OPEN divergences are
process/control classifications only, are not candidate evidence, and do not
alter the frozen product, validation, diagnostic, or predecessor identities.

The cumulative ladder is sufficient to authorize exactly one complete
diagnostic-control verification. It does not itself issue `PRODUCTION
FUNCTIONAL`, release acceptance, or maturity advance.

## Exact fresh-root boundary

Create exactly one QA root named
`horizon-archive-frrc002-<lowercase-D-GUID>` as a direct child of the resolved
OS temporary directory. Before creation and again before deletion, require all
of these internal checks while emitting only Boolean/count scalars:

1. normalized direct parent equals normalized resolved OS temp;
2. normalized candidate is a strict descendant of OS temp;
3. candidate is outside the normalized repository in both directions;
4. leaf matches exact anchored GUID-name shape;
5. candidate does not already exist before creation;
6. candidate differs ordinal-ignore-case from disclosed predecessor root
   `C:\Users\marti\AppData\Local\Temp\horizon-archive-frrc002-3d574750-951c-4029-b6b6-531f7d012015`.

Create with `[System.IO.Directory]::CreateDirectory`. Resolve and freeze that
exact created path once. Emit no root, parent, repository, or predecessor path.
Do not inspect, reuse, mutate, move, or delete the disclosed predecessor or any
other temp entry.

## Exact one-E2E authority

Exact next owner is a **Combat Engineer**. A fresh context remains preferred;
if thread limits require reuse, disclose that reuse in the return.

Run one fail-closed wrapper in this exact order:

1. Require exact loopback ports `4173/4184` clear. Use the accepted hidden,
   no-redirection, PassThru form to start the exact manifest production and
   TD-012 fixture Node/Vite previews once each. Require two positive owned
   PIDs. Obtain exact root readiness `200/200` with the bounded suppressed IWR
   transport. Do not rerun formal served identity; VR-42 is inherited.
2. Set the complete-E2E environment exactly:

```text
HORIZON_ARCHIVE_URL=http://127.0.0.1:4173/
HORIZON_ARCHIVE_QA_DIR=<exact frozen fresh root>
HORIZON_ARCHIVE_PRODUCT_CANDIDATE=a91763e28d488f31f8cf7d40ece0b2682246ba9b
HORIZON_ARCHIVE_PROBE_CANDIDATE=2cccbfe104e0dc88b17343fe7a2950afe0c2a9cc
HORIZON_ARCHIVE_PBA_NARROW=true
HORIZON_ARCHIVE_PBA_GLOBAL=true
HORIZON_ARCHIVE_MEDIA_IDENTITY=true
```

3. From repository root invoke exactly once, with timeout `180000ms`, the
   manifest `complete-e2e` vector:

```text
node playtest/e2e-playthrough.mjs
```

   Capture and suppress the complete stream. Record only run count, native
   exit, and duration scalar. No second browser or E2E invocation is permitted.
4. After E2E completion, address only the two exact literal fresh-root children
   `first-run-live-diagnostic.json` and `first-run-live-summary.json`. Do not
   enumerate the root. Require exactly one diagnostic. Parse it read-only and
   emit only `checkInventoryExact` and integer `failureCount`. Exact diagnostic
   success is `true` and `0`.
5. If E2E exit is nonzero, diagnostic is absent/invalid, inventory is not exact,
   or failure count is nonzero: do not inspect or verify any summary; set
   verifier run count `0`; proceed directly to cleanup; return diagnostic-only
   HOLD. No rerun is authorized.
6. Only after E2E exit `0` and exact diagnostic success, require exactly one
   literal summary. Parse only required scalar fields and require the complete
   accepted contract: exactly six ordered layouts; full later rail; both equal
   MH-40 outcomes; equal dignity; null deltas; `successor=null`; zero runtime
   errors; focus/input/PBA/media/runtime-request/offline gates true; Host 05
   activation `<=2ms`; maximum sampled main-thread task `<=100ms`; and summary
   pass true. Require exact product candidate, diagnostic/probe candidate,
   validation control, manifest, and fresh-root identities.
7. Only after every preceding success, invoke exactly once with timeout
   `60000ms` the exact manifest `live-summary-verify` vector, supplying the
   exact literal summary path plus the product and diagnostic candidates above.
   Capture and suppress its complete output. Exact success requires verifier
   run count `1`, exit `0`, and its parsed PASS scalar true. No verifier may run
   on any failure path.
8. In an unconditional final block, stop only the two PassThru-owned preview
   PIDs, require both stopped, and check exact ports `4173/4184` clear. Require
   the E2E-owned browser closed. Then re-run all frozen containment/equality/
   predecessor-distinctness checks against the exact created root and delete
   only that root with `[System.IO.Directory]::Delete(<exact-root>,$true)`.
   Require it absent afterward. Do not use discovery, globbing, or an alternate
   cleanup target.

Initialize absent PIDs/counts to `0`, unreached numeric/status fields to `-1`,
and Boolean fields to `0`. Emit exactly one ordered scalar line after cleanup
and nothing else:

```text
rootContainmentPass=<0|1> rootDistinctPass=<0|1> rootCreated=<0|1> prePortClearCount=<0..2> productionPid=<int> fixturePid=<int> readinessStatusCount=<0..2> e2eRunCount=<0|1> e2eExit=<int> e2eDurationMs=<int> diagnosticCount=<0|1> checkInventoryExact=<0|1> failureCount=<int> summaryCount=<0|1> sixLayoutsPass=<0|1> fullRailPass=<0|1> bothMh40Pass=<0|1> nullOutcomePass=<0|1> runtimeOfflinePass=<0|1> performancePass=<0|1> verifierRunCount=<0|1> verifierExit=<int> verifierPass=<0|1> cleanupAttemptCount=<0..2> productionPidStopped=<0|1> fixturePidStopped=<0|1> browserClosed=<0|1> portClearCount=<0..2> rootDeleted=<0|1> nativeExit=<0|1>
```

Exact PASS requires root containment/distinct/create `1/1/1`; pre-port and
readiness counts `2/2`; two positive owned PIDs; E2E run `1`, exit `0`, and
positive duration within timeout; diagnostic `1/1/0`; summary count `1`; six
layout/full rail/both MH-40/null/runtime-offline/performance scalars all `1`;
verifier `1/0/1`; preview cleanup `2` and stopped `1/1`; browser closed `1`;
ports clear `2`; root deleted `1`; and native exit `0`.

On exact PASS return **`ONE COMPLETE DIAGNOSTIC-CONTROL VERIFICATION PASS /
STOP / RETURN TO FRESH MISSION`**. On any other result return **`HOLD / ONE
COMPLETE DIAGNOSTIC-CONTROL VERIFICATION FAILURE / DIAGNOSTIC ONLY / NO
RERUN / RETURN TO FRESH MISSION`**. A failure must have verifier run count `0`
unless every E2E, diagnostic, summary, identity, journey, runtime, offline, and
performance precondition passed before the verifier itself failed.

Stop after the one scalar. Do not diagnose, repair, retry, relaunch, reverify,
or run a post-cleanup command. No product, test, E2E, manifest, content, CSS,
module, dependency, package, lockfile, curriculum, evaluator, save, story,
route, map, scoreboard, maturity, or media write is authorized. The only writes
are the exact fresh QA-root artifacts owned by the one E2E and their exact
cleanup.

## Preserved boundaries and classifications

These seven classifications remain distinct and OPEN; none is waived, merged,
closed, cured, or used as candidate evidence:

- **`UNAUTHORIZED DIVERGENCE / PROTECTED PATH ENUMERATION / OPEN / VR-17`**;
- **`UNAUTHORIZED DIVERGENCE / BROAD REPOSITORY FILENAME ENUMERATION / OPEN /
  VR-23`**;
- **`UNAUTHORIZED DIVERGENCE / UNTRACKED PATHNAME ENUMERATION / OPEN / VR-24
  COMBAT ATTEMPT`**;
- **`UNAUTHORIZED DIVERGENCE / LITERAL CONTROL PATHNAME OUTPUT / OPEN / VR-25
  MISSION`**;
- **`UNAUTHORIZED DIVERGENCE / LITERAL CONTROL PATHNAME OUTPUT / OPEN / VR-28
  MISSION STAGING`**;
- **`UNAUTHORIZED DIVERGENCE / EXACT-PORT LISTENER OWNERSHIP RECOVERY / OPEN /
  VR-39`**; and
- **`UNAUTHORIZED DIVERGENCE / PARSER DIAGNOSTIC SCALAR OUTPUT / OPEN /
  VR-41`**.

All frozen candidate, threshold, player, learning, copy/owner, `L02-02`, strict
`24/24`, evaluator, remediation, evidence/privacy, save/reload/return,
accessibility, focus, responsive, forced-color, reduced-motion, offline,
request, dependency, source-map, PBA, performance, route, world, equal MH-40,
null-delta, `successor=null`, ending, immutable-media `17 / 37,410,731`,
diagnostic non-evidence/non-verifier, and one-E2E meanings remain exact.

No image/media operation or reveal is authorized. No branch, packet, lesson,
hidden-lore answer, reward, access, identity, authority, world response,
successor, RP-013, or post-ending content may be added or changed. Protected
repository QA, PDF, training, browser/profile/save, hidden lore, media, user
state, predecessor roots, and unrelated external roots remain forbidden to
inspect, enumerate, reuse, modify, move, or delete.

No Quartermaster, Image Specialist, Intelligence, reveal, maturity advance,
release, schedule, automation, or `FIRST RUN COMPLETE` action is authorized.

Mission Captain signs **`FIRST RUN SHELL READY / ONE COMPLETE DIAGNOSTIC-
CONTROL VERIFICATION / FRSH-003-v1-VR-43`**.

Commit and push are required only for this administrative variance and the
synchronized exact handoff. No product file may be staged.
