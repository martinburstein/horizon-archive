# First Run Shell Variance Reissue - External Cleanup Deferred Limitation

Variance ID: `FRSH-003-v1-VR-11`

Disposition: **`FIRST RUN SHELL READY / FRSH-003-v1-VR-11`**

Cleanup classification: **`DEFERRED LIMITATION / RELEASE-PROCESS ONLY /
NON-GATING FOR PRODUCTION CONTENT`**

Stage / owner: Mission Captain / `mission_captain`

Governing shell: `FIRST RUN SHELL READY / FRSH-003-v1`

Operative evidence shell: `FIRST RUN SHELL READY / FRSH-003-v1-VR-07`

Exact-identity replay chain: `FRSH-003-v1-VR-08` through
`FRSH-003-v1-VR-10`

Quartermaster return: `PRODUCTION CONTENT HOLD / FRCA-003-v1 / VR-10`

Work Order: `FRWO-003-v1 / Sixfold Weir`

Mission control source inspected:
`823d883bff915f52938027963775b627ea59162f`

Exact content product/test candidate:
`a91763e28d488f31f8cf7d40ece0b2682246ba9b`

Runtime product predecessor:
`7e85154abd8dbf116c4bb84ca66afd859903d750`

Validation-control candidate:
`4cd7fbf31291671dd28c0743b44a7c49aaad82bb`

Passing evidence-control candidate:
`ca89a679195c11d441a76e6c02983a6436f2ccb2`

Exact disclosed external root:
`C:\Users\marti\AppData\Local\Temp\horizon-archive-frrc002-3d574750-951c-4029-b6b6-531f7d012015`

Released rollback baseline:
`3e3da60dc2ffa097a3499a5e2f5fa6ce2273f602`

Date: **2026-08-10**

## Decision

Mission classifies the remaining exact external-root cleanup as a truthful,
bounded, release-process-only **`DEFERRED LIMITATION`** that is non-gating for
Quartermaster production-content completion.

Every product, repository identity, served-identity, browser behavior,
six-layout, summary, verifier, performance, PBA, media, journey, later-rail,
and ending gate passed against the exact immutable content candidate. Owned
preview PIDs are stopped and ports are clear. The sole remaining condition is
that the execution layer rejected the already containment-proven recursive
literal deletion command before it ran. The root is outside the repository,
candidate, runtime, media inventory, and Git and creates no product or content
variance.

Mission therefore does not require or authorize another cleanup attempt,
alternate deletion mechanism, destructive bypass, E2E, summary, verifier,
preview, build, test, or tracked product change. Quartermaster may issue
`PRODUCTION CONTENT COMPLETE / BUILD CANDIDATE READY / FRCA-003-v1` by
updating only its content ledger/return and the synchronized handoff, while
preserving the exact root disclosure and deferred-limitation classification.

This is content-stage acceptance only. Image Specialist remains blocked until
Quartermaster commits, pushes, and proves that exact PASS. Intelligence retains
independent final variance/release authority. No maturity advances from this
Mission decision.

## Exact accepted VR-10 evidence

- Repeated synchronized Git/object/blob/ancestry/slot/media/port preflight
  passed at `fb9d217b9db7470d799c1c7292ebc3934d7c1db0`.
- Separator-normalized pre/post root containment passed: exact direct parent
  inside resolved OS temp, descendant true, and both repository-containment
  directions false.
- Native creation and postvalidation passed for exactly one fresh GUID root.
- Served production/fixture identity passed `2/2`.
- The sole exact-identity E2E ran exactly once for `58.8s` with exact product
  `a91763e28d488f31f8cf7d40ece0b2682246ba9b` and evidence candidate
  `ca89a679195c11d441a76e6c02983a6436f2ccb2`.
- All six ordered layouts, same-node/geometry/direct/focus gates, complete
  later rail, both equal MH-40 outcomes, null deltas, `successor=null`, and
  zero runtime errors passed.
- Maximum Sixfold activation and sampled main-thread task were each
  `0.20000004768371582ms`.
- Exactly one machine summary named the actual product/evidence identities;
  the independent verifier ran exactly once and passed exact repository
  identity, schema, geometry, focus, journey, performance, PBA, and media.
- Owned preview PIDs `50292` / `45096` were stopped and ports `4173` / `4184`
  are clear.
- No tracked product/content/test/copy/probe/manifest/media/presentation/
  learning/save/route/ending/maturity or protected/user byte changed; no
  image/media operation or reveal occurred.

No replay or reconstructed evidence is required. The passing machine summary
and verifier result remain the sole accepted VR-10 live evidence.

## Exact cleanup finding

Before deletion, Quartermaster reconstructed and resolved the exact frozen
target and encoded normalized root equality, direct-parent/temp containment,
bidirectional repository exclusion, and exact literal recursive removal in one
PowerShell command. The execution layer rejected the command because it
contained recursive `Remove-Item`; rejection occurred before execution.

The exact root remains, and Mission independently confirmed only its directory
existence and resolved path without enumerating or reading its contents:

```text
C:\Users\marti\AppData\Local\Temp\horizon-archive-frrc002-3d574750-951c-4029-b6b6-531f7d012015
```

This decision grants no authority to inspect, enumerate, reuse, mutate, move,
or delete that root. It must remain disclosed by exact path in the content
ledger and later Intelligence evidence. A future cleanup may occur only under
separate explicit authority and a policy-supported exact method; it is not a
condition for the current content-stage handoff and creates no entitlement to
rerun or change the release candidate.

## Consistency with accepted First Run precedent

This classification applies the established First Run boundary rather than a
new waiver:

- `FRCA-001-v1` advanced a content-complete candidate to Image with one
  policy-blocked isolated external QA directory disclosed outside repository,
  candidate, and runtime and with ports clear.
- `FRCA-002-v1` advanced a content-complete candidate to Image with three
  policy-blocked Quartermaster-owned external temp capture directories
  classified as a deferred cleanup limitation; processes and ports were clear.
- `FRPR-001-v1` completed runtime presentation while disclosing a policy-
  blocked isolated external QA directory after owned process/port cleanup.
- `FRAB-001-v1` independently classified policy-blocked isolated QA deletion
  as `DEFERRED LIMITATION`: release-process only, candidate and Git unaffected.
- `FRSH-003-v1` requires exact owned cleanup and reporting of any exact cleanup
  limitation; it does not authorize misrepresenting policy rejection as
  deletion or altering a proven candidate to work around tooling policy.

The current condition is at least as bounded: exact external path, proven
containment, no active PID/port, fully passing summary/verifier, no candidate
or Git effect, and truthful disclosure.

## Exact Quartermaster completion authority

Quartermaster must use a fresh Quartermaster context and verify from the new
synchronized VR-11 source:

1. `HEAD == origin/main == remote main` and no tracked drift;
2. content candidate `a91763e`, validation control `4cd7fbf`, and evidence
   control `ca89a679` remain exact ancestors and current frozen blobs remain
   unchanged;
3. the VR-10 E2E, summary, and single verifier PASS evidence is recorded
   exactly, without replay, edit, substitution, or re-emission;
4. PIDs remain stopped and ports `4173` / `4184` remain clear; and
5. the exact external-root path and this non-gating deferred limitation remain
   explicitly disclosed.

Quartermaster may then change only:

- `Production Pipeline/First Run/FIRST_RUN_CONTENT_ASSET_LEDGER_FRCA-003-v1.md`
  or one versioned Quartermaster content-completion return; and
- `NEXT_INSTANCE_HANDOFF.md`.

The content return must issue exact **`PRODUCTION CONTENT COMPLETE / BUILD
CANDIDATE READY / FRCA-003-v1`**, retain all exact candidate/evidence IDs and
the full root path, classify cleanup as `DEFERRED LIMITATION / RELEASE-PROCESS
ONLY / NON-GATING`, state that no deletion or replay occurred, and route the
next synchronized action to Image Specialist under the existing-media-only
runtime-presentation boundary.

No product/content/test/copy/E2E/probe/manifest/summary/verifier/media/
presentation file may change. Quartermaster may not run a test, build,
preview, browser, E2E, summary, verifier, or cleanup command; alter the prior
root; retry deletion; or begin Image/Intelligence itself.

## Frozen scope, downstream stop, and rollback

Exactly seven final-purpose content slots remain accepted. Host 05 geometry,
states/actions/focus/recovery, `L02-02`, evaluator, sanitizer, evidence/privacy,
save, Host 04, route, later Drowned/Witness/City/rail, both equal MH-40
outcomes, null deltas, and `successor=null` remain unchanged.

Immutable media remains exact `17 / 37,410,731`. No image/audio/media
generation, edit, replacement, variation, import, movement, deletion,
publication, or reveal is authorized. No Host 06 work, City repair, new
meaning, reward, access, authority, response, branch, successor, RP-013, or
post-ending content is authorized.

Image Specialist and Intelligence remain blocked until the exact Quartermaster
completion docs are committed, pushed, and synchronized. Thereafter only the
normal Image Specialist runtime-presentation stage may begin; this Mission
variance does not begin or waive it.

Released rollback remains `3e3da60`; content candidate remains `a91763e`;
there is no product/content rollback or migration. Protected PDF/training/
repository-QA/browser/profile/save/hidden-lore state, prior roots, automation,
archived workflows, schedules, and reveals remain forbidden.

## Mission validation, signature, and exact handoff

Mission read the current synchronized handoff, full Mission profile, complete
current `FRCA-003-v1` including VR-10 evidence, relevant cleanup controls, and
accepted First Run cleanup precedents at source
`823d883bff915f52938027963775b627ea59162f`.

Mission verified exact current candidate/manifest blob equality, content
candidate ancestry, clear ports, and the exact external root's existence and
resolved path without reading its contents. Mission ran no test, build,
preview, browser, E2E, summary, verifier, or cleanup; changed no product;
deleted nothing; and inspected no protected/user state.

Mission Captain signs **`FIRST RUN SHELL READY /
FRSH-003-v1-VR-11`** with cleanup classified **`DEFERRED LIMITATION /
RELEASE-PROCESS ONLY / NON-GATING FOR PRODUCTION CONTENT`**.

Exact next owner is Quartermaster. Read its profile, this variance, complete
current `FRCA-003-v1`, complete `FRSH-003-v1-VR-10`, and exact VR-10 evidence.
Without rerun, cleanup, or tracked product change, issue the content-completion
return and synchronized handoff exactly above. Image remains blocked until
that Quartermaster PASS is committed, pushed, and synchronized.

The dedicated Mission commit, push, and exact `HEAD == origin/main` proof are
reported from Git after commit because this artifact cannot contain the hash
of the commit that first contains itself.
