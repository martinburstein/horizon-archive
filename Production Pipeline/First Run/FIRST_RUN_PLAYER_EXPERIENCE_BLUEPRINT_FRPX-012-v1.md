# FIRST RUN PLAYER EXPERIENCE BLUEPRINT / FRPX-012-v1

## Disposition

`PLAYER EXPERIENCE READY / ACCEPTED-ONLY NATIVE HOST 12`

Authorities: `FRSH-012-v1`, `FRDT-012-v1`, source release `FRAB-011-v1`.

## State and replacement graph

Current: predecessor Prompt mastery -> generic Client Boundaries -> generic SDK
Route Chooser -> generic Single Agent -> later route. Before source selection,
behavior remains identical.

After exact source enablement: predecessor mastery -> unchanged Client
Boundaries -> Host 12 `available` -> USE opens SDK Route -> strict mastery ->
USE opens Single Agent -> strict mastery -> `complete` read-only USE ->
unchanged later route. Whenever selected source enablement is true, generic SDK
and Single Agent launchers are absent even if later decode/measurement fails;
malformed enabled state fails closed. Client Boundaries stays in its exact
existing place and is not claimed as Host 12 ownership.

## Registry and native interaction

Combat creates one null-first registry with source/provenance, dry approach,
upper crown, bypass-seam array, sealed-chamber array, drowned-return array,
water boundary, semantic target, label, protected regions, six layout records,
copy slots, and alt. Lawfulness requires exact H12 attempt ID, dimensions,
opacity, size/SHA, physical geometry, source containment, and derived layouts.

One native hotspot owns LOOK/TALK/USE. LOOK announces only slab, crown, seams,
sealed chambers, water, and drowned returns. TALK announces complete
nonresponse. USE dispatches SDK Route first, then Single Agent, and never
dispatches after complete. Pointer, Enter/Space, touch, and switch-like
activation converge. One paint after Client Boundaries mastery presents and
focuses once. Terminal exit, primary, remediation, mastery, reload, and route
return restore focus when lawful. Escape closes only the active terminal layer.

States: `hidden` for unmet predecessor or source/provenance/decode/layout/
evidence malformation; `available`; `in_progress`; `remediation_required`;
`complete`. State never changes world presentation or water access.

## Responsive, accessibility, and copy

Scene uses centered cover projection. Semantic target derives from measured
source geometry, is clamped, and remains >=44x44 CSS px. Six records cover
desktop, laptop, narrow, effective-200%, 320x180, and 320x240: complete relation
retention >=.95; approach/crown/seam/chamber/return centers visible; target
contained; physical center included; label/focus separated; protected overlap
zero. DOM order is art -> hotspot -> status/terminal. Forced colors adds a
visible border; reduced motion removes transition only; 200% preserves meaning.

Quartermaster fills final-purpose slots: `unseen`, `available`,
`sdkInProgress`, `agentInProgress`, `missed`, `mastered`, `returned`,
`nextBoundary`, and `alt`. Copy cannot imply underwater access, opening,
activation, address, intelligence, answer, correctness, reward, or world change.

## Combat acceptance matrix

Tests prove null-first registry; nine prompt identities; exact attempt ID and
30 MB/aggregate guards; source/provenance fail-close; dry approach/crown; >=3
bypass seams; >=2 sealed chambers; >=2 drowned returns; explicit water boundary;
derived six-layout evidence ignores caller attestations; source enablement
retires generic SDK/Single Agent launchers but never Client Boundaries;
predecessor strict mastery; all five states; SDK-then-Agent routing; complete
read-only USE; LOOK/TALK write-free; decode failure; focus/reload/Escape; target
semantics; all copy slots; immutable accepted media; unchanged later route and
MH-40.

Quartermaster adds selected-byte, private physical measurement/decision,
cleanup, and provenance evidence. Image adds runtime presentation only.
Intelligence independently reruns the release ladder. No variance exists.

Exact next owner: one Combat Engineer.
