# FIRST RUN CONTENT AND ASSET LEDGER / FRCA-012-v1

## Disposition

`PRODUCTION CONTENT COMPLETE / H12-1 SELECTED / EIGHT CALLS UNUSED`

Shell `FRSH-012-v1`; Combat candidate `12efa77a`; functional gate `f1f4748e`.

Exact H12-1 was invoked once. It is an ordinary nonreparse opaque RGB PNG,
`3840x2160`, `14,744,405` bytes, SHA-256
`d8b5c6abe887fa0c17321f7f426a0bea2f6bfbcddfdaca6875b219d51a8292fc`.
Strict decode passed. Private review passed the complete dry approach, fused
crown, three bypass seams, two sealed chambers, two drowned returns, water
boundary, and every canon rejection. Derived six-layout evidence passed with
complete relation retention, essential centers visible, contained >=44 CSS-px
target, and zero protected overlap. First PASS stopped the ledger; H12-2..H12-9
remain unused in the rolling pool.

The selected byte was copied exactly once to the frozen product path. Source
and destination SHA matched. The exact scratch output was deleted, absence was
proved, and the now-empty owned root was deleted nonrecursively and proved
absent. No rejection, derivative, reference, edit, reveal, or alternate source
exists.

Registry/provenance now bind H12-1, prompt `1285` /
`a5b2af07451e0a562986a8b59314d9acda57979615c9ac3d4034fade4dd9411d`,
CLI identity, selected bytes, measured geometry, semantic target, and all final
copy/alt slots. Copy remains physical, silent, local, offline, and write-free;
it implies no underwater access, opening, answer, correctness, reward, or
world response. Native USE owns SDK Route then Single Agent only; Client
Boundaries and every later launcher remain unchanged.

Focused content/lesson/state tests pass `26/26`. Production build passes `231`
modules; JS `1,741,218`, CSS `120,356`, accepted media `23 / 140,647,485`,
maps0. Full suite is cumulative `1027/1027`: initial integrated run `1025/1027`
plus exact served-inventory corrections `25 -> 26` and isolated `2/2` PASS.
Those literal count updates change no product behavior or threshold.

Exact next owner: one Image Specialist for runtime code/configuration
presentation only. The selected raster is immutable and remains unrevealed.
