# First Run Polish Viability Variance

Variance ID: `FRVE-005-v7-VR-28`

Stage / stable agent: Office of Science Administrator /
`office_of_science_administrator`

Work Order: `FRWO-005-v7 / Stranded Lens Cradle - Direct Image API Source,
Native Handle Identity, and Fixed Lesson Integration`

Disposition: **`POLISH VIABILITY READY / EXACT RETAINED-OUTPUT CLEANUP PASS /
SOLE CREDENTIAL-CLEARED NO-REQUEST PROOF ACCEPTED AT PT06 / ONE PARENT / ONE
CHILD / ALL CONTROLLED PATHS ABSENT / ZERO REQUEST OR API ACTIVITY / NO RETRY /
FRVE-005-v7-VR-28`**

Date: **2026-08-11**

Science source inspected: `a2ef74832f229da55fb96621081fb8aa4be3fd3c`

Mission shell: `FRSH-005-v1-VR-31`

Prior Science return: `FRVE-005-v7-VR-27`

Effective shell: `FRSH-005-v1` through `FRSH-005-v1-VR-31`

Exact next recipient: one fresh Mission Captain / `mission_captain`

## Science decision

Science read the complete active intake, workflow, registry, full Science
profile, complete `FRSH-005-v1-VR-31`, `FRVE-005-v7-VR-27`,
`FRSH-005-v1-VR-30`, `FRVE-005-v7-VR-26`, both retained sources, complete
`FRWO-005-v7`, and the cited effective controls.

Before any outer, parent, or child execution, Science applied the sole cleanup
authorization exactly. The literal retained root was an ordinary non-reparse
directory whose sole direct entry was the literal ordinary non-reparse DLL.
The DLL identity was exact:

```text
length=4096
sha256=5092fcfd97f14c71841213a59c895d528bfb7b387808fadf6db9be7f9e3fe25d
```

Science deleted only that literal DLL with `File.Delete`, proved file and
directory absence at the DLL path, proved the root empty, deleted only the
literal root with `Directory.Delete(root,false)`, and proved file and directory
absence at the root path. No parent, sibling, glob, recursive target, cache,
repository path, live root, managed output, residual, or protected path was
inspected or changed.

## Retained source and carrier adjudication

The retained sources matched Mission's identities and form:

```text
parentBytes=58512
parentSha256=943d3e83da37d3cba45f35833e2e283b24e9e1434ed137144a7df31ae6169c39
outerBytes=15559
outerSha256=55a4cf00b76cee30ebca0718e0f26341957ee72727ceaa620f0a20d48c1a317f
strictAscii=true
lfOnly=true
finalLF=true
parserErrors=0
```

The outer-to-parent freeze, canonical stored Base64 carriers, launcher
`2001 / 96feaf7e...`, stored combined carrier `27044 / 015dfd96...`, corrected
runtime combined carrier `27690 / 91bcba2d...`, prefix
`976 / 5cd257c9...`, tail `26714 / 69a70f77...`, sole helper-source LF seam,
dynamic current-run DLL freeze, byte-only load, later exclusive full-byte
reobservation, stage order, credential removal, one-parent/one-child capture,
exact PT06 acceptance, and zero-activity result schema were present and
parser-valid. All `13/13` controlled paths were absent before execution.

The child dynamically computed its current compile output length and SHA-256,
loaded only those frozen bytes, and required an exclusive later full-byte
observation to match them before native identity acceptance and cleanup. The
accepted controller deliberately projects no DLL scalar; therefore this
Science report records the authoritative same-run equality proof without
inventing or promoting a compile-instance digest into a future predicate.
Any later production run must record its own current-run compile length/SHA in
provenance as required by the effective shell.

## Sole authoritative execution

Science invoked the exact retained outer controller exactly once through
Windows PowerShell 5.1 x64 with `OPENAI_API_KEY` removed from the outer process
environment. No correction, alternate runner, cleanup, or retry followed
start.

```text
controllerExecutions=1
controllerExit=0
classification=ACCEPTED_PARENT_RESULT
parentExecutions=1
parentExit=0
parentStdout=EXACT_ACCEPTED_V2
parentStderr=EMPTY
childInvocations=1
postflightAbsent=true
```

`EXACT_ACCEPTED_V2` is the outer controller's exact byte-for-byte acceptance
of the parent result that requires child exit `87`, child stdout zero, one
exact PT06 diagnostic, `ACCEPTED_NO_REQUEST_STOP`, earliest stage
`PT06_CREDENTIAL_GATE`, code `CREDENTIAL_ABSENT`, and exact zero
credential-value reads, request constructions, `SendAsync` calls, direct
sends, and ordinal consumption. The accepted parent also requires every exact
local and postflight absence boolean true.

Science independently confirmed all `13/13` controlled paths absent after the
sole run. The helper DLL/root were cleaned by the identity-proved child path;
the live root, both ordinal staging/target/decision trios, product root/raster/
provenance, and Science fixture root remained absent.

## Activity, viability, and protected state

API sends remain exact `0`; ordinal `1` remains opaque and consumed; ordinals
`2` and `3` remain unstarted and unconsumed. No credential value, request,
response, media, pixel, product, build, browser, E2E, managed residual, opaque
residual, or protected state was accessed. This stage changes no product,
runtime, test, lesson, copy, media, manifest, map, scoreboard, or maturity.

The exact helper/current-run byte-freeze and PT06 no-request boundary are
technically viable. Science therefore returns **`POLISH VIABILITY READY`** for
one fresh Mission Captain to issue the next exact versioned shell decision.
This result does not itself authorize Quartermaster, an API call, ordinal
consumption, media inspection, generation, import, or production work.

The one-path rail, sole unchanged `L02-03`, equal-dignity MH-40 outcomes,
shared RP-012 ending, and `successor=null` remain immutable. Repository QA
quarantine, protected PDF, training tree, Martin's real browser/profile/save,
accepted media, opaque residuals, VR-65, and hidden lore remained untouched.

Science changes only this result and `NEXT_INSTANCE_HANDOFF.md`, commits them,
and does not push.

Office of Science Administrator signs **`POLISH VIABILITY READY / EXACT
CLEANUP PASS / SOLE FRSH-005-v1-VR-31 OUTER RUN ACCEPTED AT PT06 / ONE PARENT
/ ONE CHILD / ALL PATHS ABSENT / ZERO API / NO RETRY /
FRVE-005-v7-VR-28`** from exact source
`a2ef74832f229da55fb96621081fb8aa4be3fd3c`.
