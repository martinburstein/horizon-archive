# FIRST RUN FUNCTIONAL REPORT / FRCE-011-v1

## Disposition

`PRODUCTION FUNCTIONAL / INERT HOST 11 SLOT / QUARTERMASTER READY`

Shell `FRSH-011-v1`; blueprint `FRPX-011-v1`; source release `FRAB-010-v1`.

Combat added `borrowedLightWall.js`, an exact null-first registry, ten prompt
identity guards, 30 MB source cap, provenance/measurement/decode lawfulness,
derived six-layout projection, state derivation, hotspot projection, and
generic Portal/Prompt retirement guard. `App.jsx` now gates both legacy
launchers on `source.enabled!==true`; because source remains false/null, current
released behavior is unchanged. No raster import or candidate field exists.

Tests added for null-first behavior, attempt domain/cap, responsive evidence,
fail-closed state, and both launcher guards. Focused Host10/Host11/Portal/Prompt
suite passes `22/22`. Production build passes `228` modules, JS `1,724,220`
bytes, CSS `120,020`, current media `21 / 109,046,406`, maps0. Full suite passes
`1019/1021`; the two failures were stale exact production-asset counts in
TD010/TD011 served-identity tests caused by the added source module chunk. The
exact expectations were corrected `23 -> 24`, and both isolated served tests
then pass `2/2`. No behavior or acceptance threshold was weakened.

No generation, API call, media inspection, candidate, product raster,
provenance, copy, alt, geometry, preview, browser profile, save, or protected QA
action occurred. Ledger `{H11-1..H11-10}` remains unstarted. Rollback is the
new module/test plus the two launcher guards and served inventory corrections.

Exact Quartermaster placeholder ledger: source/provenance; wall, film,
apertures, returns, apron, throat, semantic target, label, protected regions,
six layouts; nine final-purpose copy slots plus name/alt. Quartermaster must
populate only after first total PASS and preserve exact cleanup/private custody.

Exact next owner: one Quartermaster.
