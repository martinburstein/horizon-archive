# First Run Shell — The Third Coupling

Shell ID: `FRSH-002-v1`

Disposition: **`FIRST RUN SHELL READY / FRSH-002-v1`**

Stage / owner: Mission Captain / `mission_captain`

Work Order / viability: `FRWO-002-v1` / `FRVE-002-v1`

Planning authorities: `FRPB-001-v1`, `FRPB-001-v1-GR-01`,
`FRCL-002-v1`, `FRRM-002-v1`, `FRSB-002-v1`

Mission source inspected: `04e1cd039b554058005ba7cbba368a0214e29974`

Released First Run predecessor: `FRAB-001-v1 / FIRST RUN PASS RELEASED`

Accepted product/test baseline: `d37103b46f257cb61ef99f76413611ee31d0fab2`

Budget authority: `PBA-TD012-v1`

Date: **2026-08-10**

## Disposition and felt improvement

The planning authorities are compatible and the pass is ready for production.
After mastering the Route Marker, the player discovers that the suit has found
one compatible coupling in the low repair stock they could easily have walked
past. Deliberate `USE` opens the already-existing optional calibration through
that physical relationship; skip, failure, safe exit, mastery, departure,
reload, and return all preserve the same already-open route. The chapter lands
on the recognition that three unlike local bodies shared the expedition's
human interface without implying that any surface spoke, healed, invited, or
responded.

This shell adds one distinct runtime host relationship over immutable accepted
media. It adds no lesson, scored requirement, route, save field, state of the
world, asset, access, reward, authority, identity, ending, or successor.

## Planning reconciliation and provenance decision

Mission independently reconciles the fixed target as follows:

1. The accepted integrated master is exactly `1672 x 941`, `2,688,247` bytes,
   24-bit RGB PNG, SHA-256
   `6B27AC8233A5C29583450064DA3D51C504E16A513759D52E5651B9C60EC031D2`.
2. Original provenance explicitly authors annealing, cutting collars, rejected
   cloudy growths, salvage, repairs, feed channels, and underground routing.
   Integrated provenance preserves that plate/camera/composition and separately
   adds only the central field-linked Terminal and right Route Marker. The
   low-left repair stock is therefore source-authored material, not a new
   object fabricated by prose.
3. Fracture Nursery geometry is frozen as `left 0% / top 52% / width 24% /
   height 48%`, with identical narrow geometry. At source resolution its bounds
   are `x 0-401.28`, `y 489.32-941`.
4. First Signal starts at `25%` (`x 418`), leaving an exact `1%` / `16.72`
   source-pixel gap. Route Marker starts at `77.0313%`. Pairwise rectangle
   intersection area is zero for all three targets.
5. Centered `16/9` cover preserves the horizontal band. The target renders
   `460.8 x 518.4`, `327.84 x 368.82`, `93.6 x 105.3`, and
   `184.32 x 207.36` at the four required scene widths, and `76.8 x 86.4`
   in the retained `320 x 180` fixture. Tactical must still prove the actual
   DOM/source mapping, center containment, `>=0.85` source-band overlap,
   `>=44 x 44`, and zero positive overlap; arithmetic alone is not release
   evidence.
6. The broad image alt may mention visible low repair stock but may not call
   the raster interactive, awake, responsive, or native-named. The semantic
   button owns the stable name `Fracture Nursery coupling`.
7. Current `App.jsx`, `pixelMeadow.js`, `calibrationExercise.js`, and
   `sceneTransition.js` confirm that the required systems exist. Production is
   integration work: a third registered target/state, one in-world entry,
   removal of the duplicate generic launcher, focus/recovery wiring, final
   owned copy, tests, and release orchestration.

No provenance conflict remains. Any later visual finding that the registered
rectangle does not honestly cover one coherent repair-stock relationship is a
fail-closed `HOLD`; no raster, crop export, overlay, replacement, variation,
import, or media workaround is authorized.

## Address, maturity, entry, completion, and hard stop

- **Address:** `FR-02 / Chapter I — Glass Meadow` only.
- **Current:** continuity `FR0`; physical-host expression `FR0` with two exact
  hosts and one missing; learning `FR2`; behavior/save/recovery `FR1`;
  final-purpose content `FR1`; presentation `FR3`; prior bounded `FR4` applies
  only to `FRWO-001-v1`.
- **Target evidence:** continuity, physical-host expression, and final-purpose
  content sufficient for `FR2`; affected presentation sufficient for `FR3`;
  existing learning and behavior remain exact. Intelligence alone may change
  the scoreboard or record `FR4`.
- **Clean entry:** unchanged title/opening reaches Glass Meadow and completes
  First Signal then Route Marker exactly as released.
- **Changed active boundary:** begins only after sanitized Route Marker mastery
  makes the Nursery's compatibility available.
- **Completion:** the Nursery has an honest distinct target, its optional
  calibration is available solely through in-world `USE`, both optional
  outcomes return safely, and the unchanged Drowned departure remains usable.
- **Permitted exits:** close/Escape, retry/remediation, decline/skip, immediate
  Drowned departure, Drowned-to-Meadow return, reload/resume, and existing
  later write-free returns.
- **Hard stop:** stable entry into existing `FR-03 / Drowned Archive`. No
  Drowned host, controller, learning, copy, pacing, or presentation is changed.
- **Pacing:** zero new mandatory scored work and zero net new mandatory
  activation.

## Fixed host and state contract

Register one `fracture-nursery` secondary hotspot after Route Marker in source
and canonical focus order. Its physical rectangle is exact:

```text
left: "0%"
top: "52%"
width: "24%"
height: "48%"
narrow: identical
```

Availability is pure, sanitized, nonpersisted view state:

```text
routeMarkerMastery != mastered       -> hidden/inert (no button/affordance)
routeMarkerMastery == mastered
  && calibrationMastery == null      -> available
  && calibrationMastery in progress  -> in_progress
  && calibrationMastery mastered     -> complete
```

Both inputs use existing sanitizers. Malformed, forged, missing, reordered, or
unsupported evidence fails closed. The derived state may exist beside
`MEADOW_PIXEL_HOTSPOTS`; it is not a save property, completion key, evidence
class, route prerequisite, scene ID, or world state.

The low-left material remains physically present before detection, but there
is no Nursery button, active affordance, announcement, or interaction. After
exact Route Marker mastery, one polite Suit/System announcement may report
newly classified compatibility exactly once after the Route Terminal unmounts.
The announcement creates no evidence, does not claim physical change, and does
not repeat on render.

`completed`, `pendingSceneId`, `pendingAdvance=true`, the completed Meadow
hold, and immediate `Depart: Drowned Archive` remain exact. During that hold,
only the enabled Nursery hotspot and the verbs needed to select its
`LOOK AT` / `TALK TO` / `USE` actions may remain active among scene targets.
First Signal and Route Marker remain completed and nonactive.

## Sole calibration entry and optional-route contract

- Remove the generic departure-row `Optional calibration` / `Resume optional
  calibration` launcher completely.
- Exactly one campaign activation may open `L01-03`:
  **`USE Fracture Nursery coupling`** after sanitized Route Marker mastery.
- `LOOK AT` reports visible repair-stock fact. `TALK TO` reports silence.
  Neither action opens a Terminal, creates evidence, or changes state.
- `USE` opens the existing `EX-L0103-CALIBRATION-DEBUG` workspace and no other
  exercise. Pointer, touch, Enter, Space, and switch-like semantic activation
  dispatch the same nonduplicated intent once.
- Departure is enabled before, during, after, or without calibration. Choosing
  practice never closes or creates the route.
- Close/Escape, decline, actual miss, remediation, and unfinished work return
  to the same open route. Retries remain unlimited and answer-free.
- Same-scene close/reopen may keep the current in-memory working session. Full
  reload, departure, or return reconstructs clean working content from exact
  sanitized mastery only.
- If mastery is already complete, `USE` may expose a bounded read-only summary
  of existing evidence or report completion, but must not open a fresh scored
  form, increment an attempt, expose an answer, show a hint, request confidence,
  downgrade mastery, or change save/route/world state.
- Demo Tour remains separately named and zero-credit. Nursery discovery,
  focus, appearance, or interaction cannot mint campaign or Tour evidence.

## Immutable learning, privacy, save, route, world, and ending firewall

The complete existing `L01-03` contract is byte/meaning fixed:

- lesson `L-01-03`, activity and assessment ownership, skill `PY-007`,
  exercise `EX-L0103-CALIBRATION-DEBUG`;
- current prompts, starters, tracebacks, forms, answers, diagnosis rules,
  check IDs, strict `8/8 + 8/8 + 4/4`, confidence, misconception tags,
  actual-miss-only remediation, hint ladder, unlimited retry, and safe exit;
- `calibrationMastery` as the sole durable calibration record; and
- exact current evidence allowlist and sanitizer.

Curriculum files and `calibrationExercise.js` are regression-only. No role may
change an exercise prompt, source, answer, evaluator, threshold, check, owner,
mapping, objective, prerequisite, lesson status, score, or remediation route.

The campaign save keeps its current schema/version and full projection.
Working source, traceback, output, choices, notes, dialogue, detection, focus,
pointer path, timing, modality, and content sentinels never enter persistence.
No new save key, version, migration, checkpoint, attempt record, or scene state
is authorized. Reload and Drowned return derive availability from sanitized
existing evidence and perform no new write or replay.

Route order, `completed`, `pendingSceneId`, Drowned entry/return, all later
learning/evidence, and MH-40 remain exact. No host interaction causes physical
healing, motion, illumination, acknowledgement, invitation, access, reward,
identity, authority, world/city/external delta, ending change, successor,
RP-013, or post-ending content. `successor=null` remains exact.

## Content meanings and voice slots

Quartermaster owns exact final prose. The shell freezes these meanings:

| State | Owner | Required meaning | Forbidden meaning |
| --- | --- | --- | --- |
| pre-detection | Scene | rejected/fractured repair stock is already present | active host, invitation, hidden label |
| detected | Suit/System | one compatible local coupling is now classified | material moved, woke, opened, answered |
| optional unstarted | Suit/System | scored calibration is available here; route already open | required work, route gate, reward |
| in progress/remediation | System / 901 Teacher | bounded human working-copy gap; actual misses only | object fault, shame, answer exposure |
| mastered | System + Scene fact | existing evidence finalized; cracks/field unchanged | healing, earned road, approval |
| declined/departed | Pilot / Scene | deliberate use of pre-existing ridge; no penalty | refusal, loss, closed route |
| returned | Scene + Suit/System | same stock/coupling; unfinished work resumes here | recall, new response, replay |
| chapter turn | Pilot | three unlike bodies shared the expedition interface | Machine/Builder speech or native Python |

Machine and Builders own no line. Final copy may not say `healed`, `crowned
passage earned`, `expected`, `accepted`, `invited`, `approved`, `mission`,
`native Python`, `successor packet`, or any equivalent claim that calibration
earned the already-open route. The complete location turn must be compact
enough for play; no bulk game-on-paper paste is authorized.

## Focus, announcements, accessibility, and responsive contract

Source/focus order is First Signal, Route Marker, Fracture Nursery, then
adventure controls, regardless of the Nursery's visual-left position. All
actions are native semantic controls with one accessible name including
action, `Fracture Nursery coupling`, and a non-color state.

| Event | Required focus / recovery |
| --- | --- |
| Route Marker mastery/detection | Nursery hotspot after Route Terminal unmounts; one polite announcement |
| Nursery `LOOK AT` / `TALK TO` | Nursery hotspot remains focused; owner-correct result reaches the existing polite region |
| Nursery `USE` | existing Terminal title receives initial focus; containment and Escape remain exact |
| close/Escape/failed-practice close | enabled Nursery trigger; if sanitation makes it unavailable, Drowned departure |
| active failure/remediation | existing actual-failed control/status inside Terminal; no focus escape |
| mastery acknowledgement | Nursery hotspot, then owner-correct completion announcement; departure next reachable |
| reload unfinished | Nursery hotspot; clean session only after deliberate `USE` |
| reload unstarted/mastered | Drowned departure; Nursery remains next in canonical scene order |
| Drowned return unfinished | Nursery hotspot |
| Drowned return unstarted/mastered | Drowned departure; Nursery remains discoverable |
| malformed evidence | existing earliest safe fallback; no Nursery, credit, or skipped prerequisite |

Focus is zero evidence. Repeated-key, pointer/keyboard crossover, or simultaneous
activation cannot create duplicate attempts. Hidden/disabled targets cannot
receive focus. Terminal background remains inert; close restores focus once.

Prove `1920x1080`, `1366x768`, `390x844`, and `768x900` effective `200%`, plus
retained `320 x 180` and `320 x 240` contracts. The actual target is at least
`44 x 44 CSS px`, retains `>=0.85` source-band overlap, contains its center,
and has zero positive overlap with both existing targets. Longest final copy,
status, and controls wrap without clipping, target occlusion, horizontal
escape, or required outer-page scroll. Forced colors and grayscale retain
identity/state/focus; reduced motion removes nonessential motion without
changing order or meaning. No sound, color, animation, hover, or timing is
required for comprehension. Automated evidence is not human AT certification.

## Presentation and immutable-media contract

Recon may define attention, discovery rhythm, silence, and concept-level use
of the existing plate. Tactical may freeze semantic order, DOM/source mapping,
focus, containment, non-color states, and responsive behavior. Combat may
implement authorized code/state wiring. Quartermaster may finalize owned copy.
Image Specialist may adjust only authorized code/configuration-level
composition, color/lighting treatment, existing sound, motion, typography,
hierarchy, spacing, focus, containment, forced color, and reduced motion.

No role may generate, edit, replace, vary, crop-export, import, move, remove,
publish, or reveal an image or other media asset; alter a media byte; add a
runtime media request; or treat concept art as integrated evidence. The exact
accepted media inventory remains `17 files / 37,410,731 bytes` with zero new,
changed, moved, or removed files.

## Performance, offline, and fail-closed budgets

Current accepted evidence leaves exactly `41,875` JavaScript bytes, `10` CSS
bytes, and `6` modules under `PBA-TD012-v1`.

| Measure | Hard cap |
| --- | ---: |
| aggregate JavaScript | `1,703,258` bytes |
| aggregate CSS | `119,672` bytes |
| production modules | `222` |
| accepted runtime media | exact `17 / 37,410,731` bytes |
| new runtime media | exact `0 / 0 bytes` |
| build / focused / related / full | `<=60s / <=30s / <=60s / <=60s` |
| complete E2E | `<=180s` |
| sampled main-thread task | `<=100ms` |

Reuse existing hotspot, status, focus, modal, responsive, forced-color, and
reduced-motion rules. New CSS is allowed only when same-candidate removal of
demonstrably redundant/equivalent CSS keeps aggregate emitted CSS under the
cap without weakening any other surface. One byte over any cap is `HOLD`; no
waiver, quality reduction, or media escape is permitted.

Normal play stays local/offline. Add no dependency, network request, service
worker, Python/WASM runtime, source map, account, sign-in, telemetry,
credential, endpoint, SDK/model/agent call, upload, external storage, or
external action.

## Exact permitted files and systems

Product/test changes are limited to:

- `horizon-archive-game/src/App.jsx`;
- `horizon-archive-game/src/pixelMeadow.js` for the exact registered geometry
  and pure derived view state only;
- `horizon-archive-game/src/sceneTransition.js` for bounded Meadow
  presentation/return/focus derivation only, with no route/state change;
- `horizon-archive-game/src/styles.css` only under the exact aggregate cap and
  with no unrelated presentation regression;
- `horizon-archive-game/test/calibrationExercise.test.js`;
- `horizon-archive-game/test/pixelMeadow.test.js`;
- `horizon-archive-game/test/meadowRouteMarker.test.js`;
- `horizon-archive-game/test/routeMarkerExercise.test.js`;
- `horizon-archive-game/test/sceneTransition.test.js`;
- `horizon-archive-game/test/sceneReturn.test.js`;
- `horizon-archive-game/test/demoTour.test.js`;
- `horizon-archive-game/test/terminalFocus.test.js`;
- `horizon-archive-game/test/photorealisticWorldPlates.test.js`;
- `horizon-archive-game/test/signalCouplerProduction.test.js`;
- `horizon-archive-game/test/openingFlow.test.js` and
  `horizon-archive-game/test/gameLogic.test.js` for related regression only;
- optional new `horizon-archive-game/test/fractureNursery.test.js`;
- the single existing `playtest/e2e-playthrough.mjs`;
- `Production Pipeline/First Run/FIRST_RUN_RELEASE_COMMAND_MANIFEST_FRRC-001-v1.json`;
- the current role's exact First Run report and `NEXT_INSTANCE_HANDOFF.md`;
  Intelligence may also update the release map, scoreboard, and changelog.

`calibrationExercise.js`, curriculum JSON/Markdown/Python, save schema/version,
dependencies/lockfiles, canonical story/lore, Drowned and later controllers,
fixture behavior, accepted media, real storage, hidden lore, and every path not
listed above are patch-forbidden. A downstream variance must return before
touching any additional file.

## Role freedoms and ownership

- **Recon:** define one compelling discovery/attention/pacing interpretation
  over the existing plate; no exact prose, control graph, code, media, or reveal.
- **Tactical:** freeze actual target geometry proof, semantic/source order,
  action graph, focus/announcement/recovery, responsive containment, and copy
  slots; no final prose or implementation.
- **Combat:** implement the exact host/view state, sole `USE` entry, duplicate
  launcher removal, focus/recovery wiring, tests, E2E, and the command manifest.
  Preserve content slots explicitly for Quartermaster.
- **Quartermaster:** write final owner-correct copy and accessible labels inside
  the frozen states; change no mechanics, learning, route, save, or media.
- **Image Specialist:** review and, only if needed, adjust authorized runtime
  code/configuration presentation; a no-change PASS is valid and no media
  operation is permitted.
- **Intelligence:** independently run/reproduce the manifest ladder, reconcile
  the exact candidate, update maturity only from accepted as-built evidence,
  and release or return without specialist repair.

Every owner writes one dedicated report/commit. Combat is the next production
push gate after Mission; Intelligence is the final push gate.

## `FRRC-001-v1` release-command manifest contract

Combat must create the checked-in JSON manifest at the exact permitted path.
It is release control, not runtime code. It must have a versioned schema and,
for every entry, explicit `id`, `workdir`, argument-array `command`, timeout,
expected exit, owner, prerequisites, output/port ownership, and cleanup.

It must contain these nonempty entries with exact meaning:

1. `focused`: explicit affected test files covering calibration, geometry,
   Nursery, Route Marker, transition, return, privacy, focus, and Tour.
2. `related`: explicit early-rail/opening/Drowned/Witness/City and affected
   accessibility/save files; no empty test-name filter.
3. `full`: `node --test` from `horizon-archive-game`.
4. `validators`: sorted enumeration of exactly the current `40`
   `curriculum/**/validate*.py` files, each called with `--self-test`; a count
   other than `40` requires reconciliation before execution.
5. `production-build`: `npm run build`.
6. `fixture-build`: `npm run build:td012-fixture`.
7. `production-preview`: exact Vite preview of production `dist` at
   `127.0.0.1:4173` with strict owned PID/port.
8. `fixture-preview`: exact
   `review-fixtures/td012-measured-horizon/vite.config.js` preview at
   `127.0.0.1:4184`; generic preview defaults are forbidden.
9. `served-identity`: root, one deep fallback, emitted JS, and emitted CSS for
   both previews return `200`; root/fallback and served/disk bytes match.
10. `complete-e2e`: one production preview, one resolved contained isolated QA
    directory, exact `HORIZON_ARCHIVE_URL` and `HORIZON_ARCHIVE_QA_DIR`, and
    exactly one invocation of `playtest/e2e-playthrough.mjs`.
11. `pba-media`: aggregate every emitted JS/CSS chunk, exact module count,
    exact accepted media inventory/hash, forbidden-runtime-asset scan, and all
    caps.
12. `cleanup-identity`: stop only recorded owned PIDs; prove `4173` and `4184`
    clear; remove only exact manifest-created fixture/QA outputs after resolved
    path containment checks; report blocked cleanup; run `git diff --check`,
    protected-boundary checks, and record candidate commit before/after.

Quartermaster and Image may append only their named focused test files without
changing command meaning. Intelligence must execute the manifest or
independently reproduce each exact entry from the frozen candidate. A shell
command that diverges from the checked-in manifest supplies no release evidence
until the manifest is versioned and the exact gate rerun. Broad process kills,
broad temporary deletion, real browser/profile/save paths, protected paths,
validator runs without `--self-test`, and generic fixture previews are forbidden.

## Executable validation ladder

Every gate records command/manifest entry, candidate identity, elapsed time,
result, and honest unavailable evidence.

1. **Integrity:** exact authority/source ancestry, game-on-paper hash, patch
   integrity, permitted paths, protected boundaries, forbidden-token scan,
   accepted master/media hashes, and zero media changes.
2. **Focused (`<=30s`):** exact geometry, `>=44`, `>=0.85`, center
   containment, zero overlap; pre-Marker absence; sanitized available/in-
   progress/complete derivation; no new save key; LOOK/TALK/USE; sole active
   calibration path; completed-use non-regression; departure in every state;
   exact calibration/privacy/focus/Tour contracts.
3. **Related (`<=60s`):** clean/restore Meadow; miss, hint, exit, mastery,
   skip-departure, Drowned write-free return, unfinished/mastered return,
   malformed state, exact redeparture, no duplicate/cross-credit; opening,
   Drowned, Witness, City, later rail, and ending invariant radius.
4. **Learning/privacy (`<=60s`):** cold full `node --test`; exact forty
   validator `--self-test` invocations; byte/meaning fixed `L01-03`; save
   projection enumeration; no private/transient persistence or Tour credit.
5. **Build/budget/identity:** clean production and exact TD-012 fixture builds;
   all PBA caps; exact media; HTTP preflight; root/deep/JS/CSS served-to-disk
   identity; no dependency or runtime request.
6. **One E2E (`<=180s`):** one and only one non-overlapping production journey
   from clean start to MH-40. In the same deterministic design it proves the
   optional calibration can first be skipped for Drowned departure, returns
   write-free, then completes calibration from the Nursery after at least one
   real miss plus exit/reload recovery, redeparts, and reaches both equal MH-40
   outcomes with zero runtime errors. No second whole-game journey is claimed.
7. **Live affected states:** desktop/laptop/narrow/effective `200%`; keyboard,
   pointer/touch-equivalent, target/source mapping, non-color states, focus,
   announcement uniqueness, longest copy, overflow, forced color, grayscale,
   reduced motion, requests/errors, sampled task. Do not overclaim human AT.
8. **Cleanup:** restore QA capture state; remove only owned contained outputs;
   close owned contexts/processes; clear owned ports; preserve user state and
   report any blocked cleanup honestly.
9. **Identity:** dedicated commits; Mission/Combat/Intelligence pushes; exact
   ancestry and `HEAD == origin/main` at every push gate.

## Definition of done

One visually distinct, provenance-honest, non-overlapping Fracture Nursery
relationship exists at exact geometry; it is absent before exact Route Marker
mastery and available afterward from sanitized view state only; `LOOK AT`,
`TALK TO`, and sole in-world `USE` preserve their owners; the generic launcher
is gone; exact optional `L01-03` works and recovers without new persistence;
skip, failure, mastery, departure, reload, and return preserve the same route;
final content lands the three-bodies/one-expedition-interface turn without a
response; accessibility, responsive, PBA, offline, privacy, media, E2E,
cleanup, and Git gates pass; every variance is classified; and Intelligence
accepts only the exact candidate.

String presence, an invisible button, arithmetic without live mapping, or test
depth without player-visible host expression is insufficient.

## Rollback, hard stops, and variance routing

Rollback is the complete bounded `FRWO-002-v1` product/test patch, manifest,
and downstream reports. Reverting it restores the exact accepted
`FRAB-001-v1` product/test baseline. No migration exists because no durable
field is authorized. User state and accepted media are never rollback tools.

Record every mismatch as `ACCEPTED IMPROVEMENT`, `REQUIRED CORRECTION`,
`MASTERPLAN UPDATE`, `DEFERRED LIMITATION`, or `UNAUTHORIZED DIVERGENCE` and
return it to the earliest owner. No downstream role silently changes this
shell.

Return through Mission to Science for any crop/source mismatch, positive target
overlap, source-band overlap below `0.85`, target below `44 x 44`, CSS/JS/module/
time overage, unsupported provenance reinterpretation, save-schema change,
route/calibration coupling, privacy leak, Demo Tour credit, new dependency,
media need, or inability to run the single isolated E2E. Return canon/voice to
Colonel, address/scope/order to Operations, and product promise to Commandant.

Immediate `HOLD` applies to hidden lore, Martin's browser/profile/save,
protected-path conflict, image/media/reveal work, invisible host expression,
new lesson/branch/route/save field, Machine/Builder dialogue, world response,
reward, access, identity, authority, exam promise, successor, RP-013,
post-ending content, destructive action, or external operation.

## Mission signature and exact Recon handoff

Mission Captain signs **`FIRST RUN SHELL READY / FRSH-002-v1`** from source
`04e1cd039b554058005ba7cbba368a0214e29974`.

### Change history and shell-gate proof

- `FRSH-002-v1` — initial shell for `FRWO-002-v1`; it does not supersede the
  released `FRSH-001-v1`, which remains historical authority for
  `FRWO-001-v1` only.
- Mission changed no product, test, learning, story, save, media, build, map,
  scoreboard, or maturity file.
- The dedicated Mission commit is the Git commit introducing this shell and
  synchronized Recon handoff. Mission pushes `main` and reports the resolved
  commit plus equality of `git rev-parse HEAD` and `git rev-parse origin/main`;
  that post-commit proof does not alter this contract.

Reconnaissance Sergeant must read this shell, `FRWO-002-v1`, `FRVE-002-v1`,
the current handoff, relevant Glass Meadow runtime and immutable provenance,
then inspect the current wide/narrow Meadow presentation without changing it.
Issue exactly one Directorial Treatment for `FRSH-002-v1` that defines the
felt discovery, attention path, pacing, silence, and existing-media concept.
Do not write final prose, freeze exact interaction/focus implementation, edit
code or media, generate/reveal an image, alter learning/route/save/world/ending,
advance maturity, or begin Tactical.
