# FIRST RUN AS-BUILT RECONCILIATION / FRAB-009-v2

## Disposition

`FIRST RUN PASS RELEASED / DISTINCT HOST 09 SOURCE AND NATIVE ENTRY`

## Independent reconciliation

The released candidate accepts the first corrected-gate attempt and stops the
five-call ledger. The selected raster and provenance identities are exact;
the other four authorizations were unused. Source, measurements, responsive
projection, copy, decode, focus, native hotspot, dual unchanged lesson entry,
and completed read-only behavior match FRSH-009-v5. The world remains silent
and unchanged.

Independent review found and returned one stale E2E expectation for the retired
generic Text Analysis launcher. Combat corrected the clean-start traversal to
require the native Host 09 hotspot after Client Bridge mastery and added an
anti-regression assertion. No product meaning or media changed.

## Release evidence

- Exact selected raster: `21,068,247` bytes / SHA-256
  `a36b6d863cb309b11c35d28261e8d7bb4e25570f120e8eef8a0fe86499ae3370`.
- Focused Host 09: `9/9 PASS`; focused/adjacent projection: `15/15 PASS`.
- Cold full suite after E2E correction: `1010/1010 PASS`.
- Curriculum/readiness validators: `40/40 PASS`.
- Isolated Edge responsive evidence: `6/6`, forced colors `1`, reduced motion
  `1`.
- Production build: `225 modules`; JavaScript `1,714,803`, CSS `119,757`,
  source maps `0`; accepted media `20 / 91,204,767`.
- Served-identity fixture asset count: `23`, with root/deep fallback and every
  emitted byte verified by the cold suite.
- Complete isolated clean-start E2E: PASS in `128.4s`; native Host 09 entry,
  primary/transfer/reload re-entry, both owned lessons, read-only completion,
  both MH-40 outcomes, null deltas, and `successor=null` all passed.
- Scratch candidate and exact owned root are absent. Product/provenance exist
  only at their accepted paths.

## Variances

- `ACCEPTED IMPROVEMENT`: corrected responsive evidence uses derived projection
  and live browser corroboration rather than source-center preferences.
- `REQUIRED CORRECTION / RESOLVED`: clean-start E2E now traverses the native
  hotspot rather than the retired generic launcher.
- Existing protected QA screenshot modifications remain outside the release:
  unstaged, uninspected, unrestored, and unused.

## Maturity and inventory

The `FR-03` physical-detail cell remains address-level `FR0`, but advances from
`1 accepted shared compression / 1 exact / 10 missing` to
`1 accepted shared compression / 5 exact / 6 missing` after the already
released Hosts 05-08 plus this bounded release. Whole-game inventory advances
to `13 exact / 1 accepted shared compression / 25 missing / 1 unadvanced
Witness expression`. No other cell advances.

## Retrospective

`TUNE`: keep the derived projection and browser gate. For every accepted source
integration, inspect the clean-start E2E immediately after native launcher
replacement, before the first full-suite run; this prevents an expected
served-asset-count correction from obscuring a separate stale journey edge.

No reveal was created or shown. Canon, lesson ownership, one-path order,
RP-012, `successor=null`, protected exclusions, and all OPEN records remain.
