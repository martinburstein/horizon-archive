Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal engineering diagram.

Hard gates only:

1. Three materially distinct live currents must remain separately readable as they approach from different directions and converge into one capped return; an image implying they are identical, share an origin, transform into one another, or reduce to ordinary pipes fails.
2. A low nearby host cluster must contain three sealed manufactured replica throats around one junction, each materially corresponding to a different current without becoming botanical, biological, anatomical, or an open conduit.
3. All replica throats must be visibly isolated from live flow, with no cable, pipe, beam, bridge, sampling path, suction, discharge, or active transfer connecting them.
4. The replica junction must be safely reachable from a broad dry outer foundation that skirts all three systems without crossing a live current and provides a clear onward route.
5. No current may change speed or react; no replica may open, signal, reward, answer, reveal, track, acknowledge, or respond, and no humanoid, text, UI, marker, or biological subject may dominate the scene.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact current geometry, replica scale, material color, cap depth, haze, and degree of radial symmetry are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0-20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
