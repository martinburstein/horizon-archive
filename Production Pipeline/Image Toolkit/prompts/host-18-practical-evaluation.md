Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal forensic illustration.

Hard gates only:

1. The main spectacle must read as abstract civic light crossing steam and an angled mineral membrane, producing a wall-scale moving relationship without a projected image, writing, performance, or audience-facing interface.
2. A small grounded off-axis service throat must be visible at the phenomenon's edge along a reachable dry margin, and must not read as a projector, screen, console, or identity scanner.
3. Light, clearing steam, heat routing, and any maintenance cycle must read as pre-existing ambient operation rather than a player-triggered presentation or response.
4. An already-lit rightward civic bridge or equivalent continuous civic crossing must remain the clear open physical onward route.
5. No dominant human architecture, humanoid character, text, UI, marker, beacon, projected symbol, greeting, refusal, reward, tracking, acknowledgement, or world-response cue.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0–4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact membrane angle, steam density, shadow-edge shape, maintenance-form count, distant identity-aperture visibility, amber/violet balance, and precise repair topology are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0–20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
