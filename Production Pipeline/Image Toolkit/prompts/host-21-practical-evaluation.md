Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal diagram or forensic checklist.

Hard gates only:

1. Two long exposed process bands must visibly correspond in broad rhythm while remaining distinct, including one bounded chamber or structural interval present in only one band; perfectly identical duplication or a mismatch portrayed as breakage/error fails.
2. A later conductive laminate must physically stitch across both bands while clearly preserving rather than erasing, merging, or correcting their bounded difference.
3. A small flush local coupling must be visible at the stitched joint and safely reachable from stable dry foundation exposure without reading as a human console, switch, screen, or security control.
4. A third route must remain opaque and fused with no visible interior, while an exterior carrier-skin bypass visibly continues around it without becoming a human road, rail, cable, pipe, or conduit.
5. Nothing may open, judge, correct, reward, answer, reveal, track, acknowledge, or react; no humanoid, text, UI, marker, alarm, invitation, refusal, or response cue may dominate the reading.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact band count beyond two, chamber shape, stitch color, joint pattern, bypass path, vapor density, and degree of asymmetry are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0-20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
