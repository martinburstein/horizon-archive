Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal forensic illustration.

Hard gates only:

1. The scene must read as an immense alien city threshold already operating before arrival, with pre-existing heat, vapor, and bridge light rather than player-triggered activation.
2. A small oblique environmental coupling must be visibly grounded along a reachable dry condensation-return edge in the near field.
3. The coupling must read as ordinary return infrastructure joining old foundation fabric to later exchange ribs, not as a user-facing console, identity scanner, or welcoming interface.
4. The already-lit rightward bridge or equivalent continuous civic crossing must remain the clear physical onward route.
5. No dominant human city architecture, humanoid character, text, UI, marker, beacon, greeting, refusal, reward, tracking, or world-response cue.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0–4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact maintenance-form count, precise rib topology, exact seam placement, distant closed-aperture visibility, mist volume, and amber/violet balance are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0–20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
