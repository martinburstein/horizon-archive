Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a sound diagram or literal checklist.

Hard gates only:

1. The focal host must be a manufactured alien pressure-relief manifold embedded in the detour, with multiple cavities visibly oriented in different directions; a musical instrument, speaker, mouth/face, biological organ, or absent manifold fails.
2. Connected pressure paths and cavity geometry must make a structured physical environmental cadence plausible without relying on text, UI, glowing waves, or explanation.
3. The cadence must remain nonlinguistic: any word, speaker, voice, writing, semantic symbol, translation, message, answer, or implied recovered language fails.
4. Pressure continuity must visibly extend through and beyond the host along persistent outer joints after the local overlay; activation, pause-as-word, correctness cue, reward, acknowledgement, or player-caused response fails.
5. A stable dry route and continuing exterior structure must remain compositionally usable for onward travel; no humanoid, dominant biology, text, UI, marker, or response device may dominate.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact cavity count, orientation angles, channel count, dust amount, flex visibility, material palette, joint spacing, route curvature, and haze are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0-20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
