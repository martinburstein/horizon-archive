Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal material-science diagram.

Hard gates only:

1. A nonhuman carrier-skin bypass must show a translucent sacrificial outer layer peeling in controlled sheets while a clearly intact continuous replacement remains underneath; collapse, structural damage, biological molting, or an exposed interior fails.
2. A much smaller dry exchange collar must be visible at the bypass edge, safely reachable from stable ground and distinct from the largest moving sheets without reading as a human console, switch, screen, scanner, or airlock.
3. The bypass must remain continuous and visibly rejoin three materially distinct stable continuities beyond the margin, including a clear broad safe onward route; a broken or blocked passage fails.
4. Shedding must read as ordinary pre-existing material renewal, not alarm, rejection, correction, reward, player activation, or moral comparison between old and new layers.
5. No humanoid, biological subject, text, UI, marker, answer, reveal, tracking, acknowledgement, or world-response cue may dominate the scene.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact sheet count, translucency, collar shape, carrier color, route cadence, vapor density, and degree of weathering are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0-20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
