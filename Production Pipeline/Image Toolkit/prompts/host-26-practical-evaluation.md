Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal checklist illustration.

Hard gates only:

1. One broad branch must remain fully sealed beneath a continuous opaque service laminate, with no visible interior, opening, window, hatch, handle, door seam, keyhole, portal, reward, or implied access.
2. A reachable low cradle must sit beside the sealed branch on its exterior service skin and stable dry footing, never inside the branch.
3. The cradle must hold only isolated inert material replicas; a live sample, book/library/letters, treasure or puzzle device, or any physical connection through the sealed laminate fails.
4. A separate external bypass must remain visibly active and continuous around the sealed boundary, rejoining the carrier beyond the receiver field.
5. Nothing may answer, rank, reward, acknowledge, pulse, spotlight, track, or react; no humanoid, dominant biology, text, UI, marker, or response cue may dominate.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact replica count, material vocabulary, cradle silhouette, receiver count, bypass curvature, haze, and maintenance-light color are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0-20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
