Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal forensic illustration.

Hard gates only:

1. The near discovery must visibly contain exactly three nested blank mineral leaves as physical alien construction, with no text, imagery, symbols, page-like human content, book, tablet, library, or archive furniture.
2. The surrounding mass must communicate an old fixed trace, later repair fabric crossing it, and a carefully bordered empty absence as distinct physical conditions without turning them into an inscription, answer, portal, or puzzle reward.
3. A small grounded compatible coupling must be visible on a stable heat-return margin, separate from the leaves and isolated from live district machinery; it must not read as a console, archive terminal, or identity scanner.
4. A separate identity-bearing closure must remain closed and noninviting while a safe dry perimeter route remains physically open.
5. Heat, light, maintenance, and every world process must remain pre-existing and indifferent, with no humanoid, text, UI, marker, greeting, refusal, reward, tracking, reveal, or response cue.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0–4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact repair color, trace shape, leaf thickness, distant repetition, border geometry, steam density, coupling orientation, and closed-interlock scale are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0–20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
