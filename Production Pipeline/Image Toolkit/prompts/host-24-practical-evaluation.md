Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal optical diagram.

Hard gates only:

1. Three materially distinct live feeders must remain separately readable as they enter one deep return; identical pipes, merged flow before entry, or an implication of shared origin/equivalence fails.
2. The return must remain completely covered by a thick opaque cap with no visible contents, interior, destination, portal, window, tunnel, cutaway, or reveal.
3. A thick refractive prism host must sit on the dry outer service skin and be safely reachable without crossing, stepping over, bridging, or touching live flow.
4. Refraction may reveal only the geometry of convergence through bent or duplicated edges; the prism must not read as a transparent window, magic crystal, hologram, scanner, screen, projector, or control surface.
5. An ordinary outbound carrier and dry margin must continue on the far side, while no current, cap, prism, or carrier opens, rewards, answers, tracks, acknowledges, or reacts; no humanoid, text, UI, marker, or creature may dominate.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact prism shape, refraction strength, cap scale, feeder color, outbound carrier position, haze, and weathering are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0-20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
