Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a statistical chart or literal checklist.

Hard gates only:

1. A branching incoming carrier must feed a large field of visibly unequal manufactured receiver forms; identical ranked rows, a crowd, biological growth, or no legible branching field fails.
2. Most exposed receiver cuffs must share a recurring mineral-trace span while several clear outliers remain equally maintained and neutrally lit; warning colors, damage, rejection, quarantine, praise, or ranking of outliers fails.
3. A low dry underfield coupling must be visibly reachable beneath several receivers and read as isolated replica infrastructure rather than live output, screen, chart, console, voting device, microphone, or speaker.
4. One branch must disappear beneath an opaque service laminate with no interior reveal while a separate external bypass remains visibly active and continuous around it.
5. Nothing may vote, sing, praise, reject, dim, reward, answer, track, acknowledge, or react; no humanoid, text, UI, marker, or biological subject may dominate.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact receiver count, cuff-trace width, outlier count, branch pattern, material color, haze, and repair detail are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0-20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
