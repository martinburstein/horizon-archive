Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a camera diagram or literal checklist.

Hard gates only:

1. The focal host must be a dark exterior mineral projection cavity on the bypass of a fully opaque closed junction; a camera, eye, portal, interior view, open junction, or absent cavity fails.
2. Ambient exterior light must enter through two ordinary separate openings and cast two distinct material-edge silhouettes into the cavity; a photograph, hologram, screen, symbolic image, or no legible projections fails.
3. A reachable low alien coupling must sample only the projected edges and must not read, record, infer, x-ray, reveal, or access the hidden junction interior.
4. The projection must change only with ambient light; activation, tracking, reward, answer, acknowledgement, memory playback, message, unity, shared cause, or player response fails.
5. Both material continuities must remain separate and leave through a broad working cut beside a stable safe onward margin; no humanoid, dominant biology, text, UI, marker, or surveillance device may dominate.
6. The image must be technically and compositionally usable as a 16:9 game environment plate.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact aperture shape, projection sharpness, cavity depth, coupling size, ambient color, edge count beyond the required pair, dust, haze, repair detail, and working-cut width are preferences, not automatic failures.

Return JSON only with the standard evaluator keys plus:

- `hard_gate_failures`: array
- `category_scores`: object with five integer scores
- `practical_score`: integer 0-20
- `disposition`: `select`, `minor_polish_optional`, `hold_as_best`, or `new_concept`

Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses.
