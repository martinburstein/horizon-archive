Evaluate the candidate as a complete player-facing game environment using `HA-IMAGE-RUBRIC-v1`, not as a literal diagram or checklist illustration.

Terminology: `petal-like` describes nonbiological mechanical lamellae around shallow blind sampling cups. The openings must terminate locally but do not need identical counts or exact radial symmetry.

Hard gates only:

1. The primary scene must clearly contain a reachable nonhuman cluster of radial sample sleeves on a maintained bypass beside a dense opaque alien margin. A flower/organism, portal array, weapon, turbine, antenna, shrine, ordinary human sampler, wrong place category, or absent sleeve cluster fails.
2. The petal-like sleeve mouths must visibly terminate as shallow blind local openings. Any doorway, tunnel, passage, socket invitation, open interior, or route through the margin fails. Exact sleeve count, lamella count, and angle are preferences.
3. The adjacent folded anchor margin must remain opaque, closed, and unavailable while the stable bypass crosses only an exterior scar. A cutaway, transparent body, hidden chamber, revealed destination, or accessible interior fails.
4. The image must contain no meaning-changing text/chart/matrix, answer, reward, activation, response, humanoid, dominant biology, or UI.
5. The image must be technically and compositionally usable as a 16:9 game environment plate with a stable dry approach.

Score 0-4 in each category: immediate scene read; composition/runtime usability; art direction/world fit; physical/story support; finish/production quality. A total of 14+ with no hard-gate failure is a practical pass. Exact materials, sleeve count, wear, residue, haze, asymmetry, and secondary exterior cue are soft criteria, not automatic failures.

Return JSON only with the standard evaluator keys plus `hard_gate_failures`, `category_scores`, `practical_score`, and `disposition`. Set `hard_pass` true when there is no hard-gate failure and practical_score is at least 14. Report no more than three meaningful weaknesses. The evaluator is advisory and may not invent requirements.
