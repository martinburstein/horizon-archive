# Demo Increments

Store one Working Team implementation and release record per file using `Production Pipeline/templates/DEMO_INCREMENT_TEMPLATE.md`.

Naming convention: `DI-###-short-slug.md`.

The active increment is shared sequentially by Player, Coder in `bug-repair` mode, Aesthetic, and the same Coder in `aesthetic-polish` mode.
