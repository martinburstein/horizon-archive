# RP-002 — Civic Record Encounter

## Packet identity

- Status: `SEED`
- Route order: 2
- Predecessor: `RP-001 — City Threshold`, ending with the expedition-only local anchor recorded and the reversible civic-district route available.
- Surface-safe purpose: first bounded civic-record encounter inside the City Beneath.

## Contiguity lock

RP-002 must begin from `cityThresholdAnchorRecorded=true` and `civicDistrictRouteAvailable=true` while preserving `continuation` unchanged and `city_state_delta=None`. It may not reinterpret the RP-001 route as welcome, recognition, consent, identity acceptance, Builder speech, or city response.

## Next Advance action

Lore Builder Agent performs A1 and defines the first surface-safe civic-record beat without opening hidden lore or changing any accepted RP-001 lock.
