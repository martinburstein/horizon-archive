# TD-005 Playable Slice Shell — Manyfold Return

## Document control

| Field | Released construction value |
|---|---|
| Stage | Mission Captain |
| Agent ID | `mission_captain` |
| Shell ID / version | `SS-RP005-MANYFOLD-RETURN-v1` |
| Slice ID | `TD-005-RP005-MANYFOLD-RETURN-v1` |
| Campaign address | `RP-005 / SC-06 / MF-00-MF-30` |
| Route contract | `TD005-RTA-001` |
| Product baseline | `GDB-TD005-v1` |
| World baseline | `WNMP-TD005-v1` |
| Floor stack | `CFS-TD005-v2` |
| Viability envelope | `VE-TD005-v2` |
| Budget authority | `PBA-TD005-v1` |
| Released predecessor | `TD-004 / SS-RP004-THREE-CURRENT-v1 / TR-40 VERIFY + RETURN` |
| Preceding stage commit | `57f220d569e1b75334ff39070b0d4558c605cfd7` |
| Mission disposition | **`SHELL READY`** |

This versioned shell supersedes historical stop certificate
`MC-TD005-HOLD-v1`. The hold correctly prevented construction while route
authority was absent. Martin's standing adjacent-transition policy,
`CFS-TD005-v2`, and `VE-TD005-v2` resolve that one defect without reopening
the Commandant or Colonel baselines.

This shell authorizes construction, not release. The accepted playable
boundary remains TD-004 until the Marines complete this exact contract and
the Intelligence Officer independently issues `AS BUILT RELEASED`.

## Mission reconciliation

The Mission Captain independently reconciled the following:

- Martin's standing authority permits only the next already-mapped adjacent
  transition after Colonel derivation, Operations adjacency proof, Science
  viability, and this conflict-free shell;
- the selected candidate is the existing surface-canon
  `RP-005 / SC-06 / MF-00-MF-30`, directly after released RP-004;
- `TD005-RTA-001` is a fresh Pilot-owned expedition navigation decision and
  is not emitted by the apparent common return, notation, record, Scene,
  Builders, city, Machine, or world;
- exact TD-004 raw bytes remain immutable and RP-005 receives its own strict
  record;
- protected RP-005 code is reference evidence, not the production adapter;
- frozen learning, evidence ownership, privacy, atomic recovery,
  accessibility, responsive, offline, Tour, world-invariance, asset, budget,
  and release requirements agree; and
- `MF-30` remains a hard stop before any RP-006 route or later exposure.

No unresolved Colonel conflict remains.

## Current source and build identity

Mission began from exact local Science commit
`57f220d569e1b75334ff39070b0d4558c605cfd7`.

Fresh Mission evidence against that source:

| Measure | Result |
|---|---:|
| Focused protected/predecessor boundary suite | `38/38 PASS` |
| RP-005 mapping validator | `PASS` |
| Production modules | `183` |
| Vite build | `23.39s` |
| Build wall time | `26.323s` |
| Aggregate JavaScript | `1,247,724` bytes |
| JavaScript SHA-256 | `875A2E9FDD9F3F38BCE3B2CD29556678AA3D6F638D61923B30D2FE9917608409` |
| Aggregate CSS | `85,151` bytes |
| CSS SHA-256 | `9222AAE71766D4E9DC7ACFA8D2FEB16F958D5D1CDC36C158982075C602D4F985` |
| Runtime media | `21,536,123` bytes across the exact accepted TD-004 media set |
| `PBA-TD005-v1` baseline mode | `PASS` |

Normal RP-005 route, controller, persistence, UI, SC-06 runtime images, and
release integration do not yet exist. Marines must construct them within this
shell; the evidence above does not claim they are live.

## Bounded player-facing purpose

### Purpose

The Pilot deliberately continues to one repeated-receiver field and records:

1. a recurring exposed trace range;
2. one bounded divergent trace;
3. one sealed branch as unavailable evidence; and
4. compatible stewardship across visible material eras.

The Pilot then uses sanitized precomputed replicas for ordinary Python work
and separate course-authored text-analysis practice, preserves a bounded
expedition note, verifies it without replay, and returns safely.

### Emotional promise

The slice should create quiet awe at a vast process whose recurrence,
difference, sealed absence, and long care can be described precisely without
becoming a verdict or answer.

The world remains larger than the expedition's interpretation. Nothing
invites, grades, recognizes, rewards, rejects, or responds to the Pilot.

## Exact entry contract

### Source state

Entry begins only from exact released TD-004:

- `TR-40 VERIFY + RETURN` or
  `TR-40 VERIFIED + CONTINUATION NOTED`;
- scene identity `SC-05` using the released SC-05 runtime master;
- strict released key
  `horizon-archive-rp004-three-current-save-v1`;
- record version `rp004.three-current-save.v1`;
- shell `SS-RP004-THREE-CURRENT-v1`;
- controller `rp004.three-current-controller.v1`; and
- the complete strict packet, mapping, checkpoint, continuation, null deltas,
  null successor, ordered note, common-return boundary, correspondence,
  purpose-null, and eight-record evidence contract in `CFS-TD005-v2`.

The normal entry adapter must reuse:

- `THREE_CURRENT_REACH_SAVE_KEY`;
- `THREE_CURRENT_REACH_RECORD_VERSION`;
- `THREE_CURRENT_REACH_SHELL_VERSION`;
- `THREE_CURRENT_REACH_CONTROLLER_VERSION`;
- `sanitizeThreeCurrentReachSave`;
- `createThreeCurrentReachStorageAdapter`; and
- `resolveThreeCurrentReachWorldScene`.

It may not replace, loosen, migrate, or fork those released controls.

### Sole route action

The only new transition is:

```text
TD005-RTA-001
PILOT // EXPEDITION NAVIGATION
PILOT // FOLLOW EXPEDITION-MARKED ADJACENT SURVEY TO MANYFOLD RETURN
```

`Manyfold Return` is an expedition label only. The common return remains
observational and non-traversable. Optional TD-004 destinationless notation
remains `routeOpened=false`, `destination=null`, and `successor=null`; it is
neither necessary nor sufficient for entry.

### Validation and one-hit result

Before token consumption, the System must validate:

1. exact normal campaign mode and Tour exclusion before storage access;
2. exact strict TD-004 record and canonical raw read-back;
3. exact TR-40 state and released SC-05 scene identity;
4. absence of private, answer, source, feedback, credential, endpoint,
   payload, pointer, focus, token, and other transient material;
5. one active `PILOT // EXPEDITION NAVIGATION` group;
6. exact route ID, source and target identities, Pilot owner, approved
   modality, and active group; and
7. one opaque fresh unused event token.

Only then may the token be consumed. One accepted token mounts exactly one
transient, zero-evidence `MF-00 ARRIVE + IDLE / SC-06` state. It writes no
TD-004 or RP-005 record, replays nothing, grants no evidence, access,
permission, reward, authority, external action, or world effect, and keeps
continuation unchanged with null city/external deltas and `successor=null`.

A duplicate token is a no-op. Every entry after a return, reload, or unsaved
interruption requires a fresh Pilot action and token.

## State graph and scene identity

```text
TR-40 ROUTE CHOICE / SC-05
  -> MF-00 ARRIVE + IDLE / SC-06 panorama
  -> MF-10 INSPECT DISTRIBUTION / SC-06 forensic detail
  -> MF-20 SUMMARIZE + SAVE / SC-06 panorama + expedition interface
  -> MF-30 VERIFY + RETURN / SC-06 panorama
  -> exact TR-40 / SC-05 OR existing City Threshold anchor
```

| State | Scene and required behavior |
|---|---|
| Exact TR-40 | Released SC-05; new Pilot action, optional notation, and both released TD-004 returns remain independent peers |
| Notation-recorded TR-40 | Same SC-05; notation is Recorded/inert and changes no route availability |
| Invalid/missing/stale/duplicate TD-005 intent | Same TR-40/SC-05; no token consumption, scene change, write, evidence, or response |
| Invalid TD-004 dependency | Released TD-004 deterministic recovery identity; never mount SC-06 |
| Accepted route / MF-00 | SC-06 panorama; exactly one zero-evidence arrival and explicit orientation |
| MF-10 | Invariant SC-06 forensic detail; four equal-peer observations in any of 24 orders |
| MF-20 | Invariant SC-06 panorama plus separate expedition interface; strict learning, explanations, review, and save |
| Exact saved MF-30 restore | Invariant SC-06 panorama; heading-first, replay-free restored controls |
| Unsaved interruption or invalid RP-005 record | Exact TR-40/SC-05; clear RP-005 transient/invalid material and require fresh entry |
| Return to Three-Current Reach | Exact TR-40/SC-05; write-free and replay-free |
| Return to City Threshold | Existing accepted City Threshold identity; write-free and replay-free |
| TD-004 return to Calibration Margin | Existing exact CM-50/SC-04 behavior, reachable only through released TD-004 |

State identity determines scene identity. Record presence, focus, notation,
route labels, invalid data, and navigation history cannot select SC-06.

## Required interactions

### TR-40 route choice

- Present `TD005-RTA-001` beside, not instead of, the existing Calibration
  Margin return, City Threshold return, and optional destinationless note.
- Preserve each action's independent owner, fresh token, and one-hit meaning.
- Invalid route attempts remain at heading-first TR-40 and preserve all valid
  future choices.

### MF-00 — arrive and orient

- Establish the invariant panorama, provenance limit, replica-only boundary,
  inspection action, and safe returns.
- Orientation is explicit and zero-credit.
- Arrival, LOOK, and presentation cannot record any of the four observations.

### MF-10 — inspect distribution

- Provide four equal semantic actions: recurring exposed range, bounded
  divergent trace, sealed branch boundary, and layered stewardship.
- All `4! = 24` orders converge on the same MF-20 boundary.
- A deliberate first inspection records its one physical observation;
  repeat inspection is Recorded/idempotent.
- Unsupported or duplicate input advances nothing. Recovery names only the
  incomplete physical class and focuses the first incomplete peer.
- Do not use normal/outlier, good/bad, correct/failed, color, position,
  frequency, glow, sound, or motion as the sole accessible name or meaning.

### MF-20 — summarize, review, and save

- Keep physical observations, Python, AI-901, and the two explanations
  visually and semantically separate.
- Use only sanitized precomputed replicas for executable Builder work.
- Require the complete independent evidence conjunction before bounded
  review and save.
- Present explicit provenance and save intents.
- Save success requires strict read-back. Failure reports honestly and
  grants no progression.

### MF-30 — verify and return

- Restore only the exact sanitized bounded note and finalized evidence.
- Replay no route, observation, learning, explanation, save, world, or
  return event.
- Focus the MF-30 heading first.
- Permit exact return to Three-Current Reach or City Threshold.
- Optional serviced-continuation notation remains destinationless and opens
  no later route.

### Pacing and optional scope

- Clean first play target: `30-41 minutes`.
- Mastered replay target: `18-25 minutes`.
- Protected reference smoke target: under five minutes through real
  evaluators; this is validation evidence, not a player speed target.

Optional, trimmable elements are Recorded-observation revisit, stewardship
and replica-provenance inspection, answer-free progressive hints, confidence,
clear-work, early safe return, and destinationless post-restore notation.
They may be removed for clarity or budget but may not replace evidence,
weaken recovery/accessibility, or create a later route.

## Ownership contract

| Owner | Owns | Must never own |
|---|---|---|
| `PILOT` | Fresh route decision, provisional expedition labels, deliberate observations, cautious note, explicit save/return intents | Native fact, permission, access, route grant from the world, sealed contents, authority, or response |
| `SCENE` | Visible carrier, receivers, traces, sealed boundary, stewardship layers, maintenance paths, and invariant ambient process | Learning score, correctness, preference, failure, answer, reward, invitation, or response |
| `SYSTEM` | Mode isolation, semantic tokens, sanitation, evidence boundaries, status/focus, local persistence, rollback, restore, and route reconstruction | Builder/Machine judgment, native category, world memory, permission, reward, exam authority, or physical causation |
| `BUILDER WORK` | The exposed incomplete Python fragment over sanitized supplied replicas | Live field access, ranking, correction, authentication, save ownership, world control, or expectation of the Pilot |
| `TEACHER / COURSE` | Neutral course-authored AI-901 cases, answer-free remediation, retrieval, transfer, and explanations | SC-06 truth, visual answer key, route, permission, world response, or native meaning |

## Frozen learning and evidence

### Python

Freeze `PY-012` exactly:

- primary `8/8`;
- answer-free remediation for actual failed checks only;
- delayed closed-note six-dimension function trace; and
- genuinely blank distinct transfer `8/8`.

The learner defines `build_summary` with two parameters, returns one exact
four-key nonjudgmental dictionary, and calls it once with supplied sanitized
inputs. Supplied counts, `None`, field names, and keys are unscored.
Hardcoding, mutation, ranking, computation beyond the contract, live access,
primary reuse, or truth/quality/purpose inference fails closed.

### AI-901

Freeze `RP005-TEXT-01 / AI901-D1-O5` exactly:

- primary `8/8`;
- per-case/per-dimension answer-free remediation;
- delayed retrieval `6/6`;
- genuinely blank distinct transfer `8/8`; and
- separate requested-output and truth-boundary explanations.

Neutral course cases distinguish keyword extraction, entity detection,
sentiment analysis, and summarization. They contain no SC-06 crop, trace
frequency, receiver/branch form, sealed/stewardship icon, Builder-world
referent, or presentation-derived deciding signal.

### Evidence firewall

The save conjunction is:

1. all four physical observations;
2. three finalized Python records;
3. three finalized text-technique records; and
4. two finalized explanations.

No conjunct cross-credits another. Scene, route, observation order,
frequency, color, sound, motion, position, navigation, focus, modality,
timing, confidence, hints, Tour, execution display, or save/restore
presentation grants learning evidence.

Misses remain local, unlimited, answer-free, and wholly blank on retry.

## Exact RP-005 persistence

### Identities

| Field | Exact value |
|---|---|
| Storage key | `horizon-archive-rp005-manyfold-return-save-v1` |
| Record version | `rp005.manyfold-return-save.v1` |
| Shell | `SS-RP005-MANYFOLD-RETURN-v1` |
| Controller | `rp005.manyfold-return-controller.v1` |
| Packet | `RP-005` |
| Mapping | `RP005-A3-MANYFOLD-RETURN` |
| Checkpoint | `manyfold_return_complete` |

The record has exactly ten ordered top-level keys:

```text
version
packetId
mappingId
checkpoint
continuation
cityStateDelta
externalStateDelta
successor
note
evidence
```

`continuation="continuation"`; city/external deltas and `successor` are
`null`.

The note has exactly nine ordered keys:

```text
observations
recurrence
divergence
unavailable
stewardship
replicas
truth
purpose
destination
```

Observations contain each frozen ID once in canonical contract order.
Required values are
`exposed_recurring_range_observed`,
`one_bounded_divergence_observed`,
`sealed_branch_unavailable`,
`layered_stewardship_observed`, and
`sanitized_precomputed_only`; truth, purpose, and destination are `null`.

The evidence array has exactly these eight ordered finalized records:

1. `PY-012 / primary`;
2. `PY-012 / trace`;
3. `PY-012 / transfer`;
4. `RP005-TEXT-01 / primary`;
5. `RP005-TEXT-01 / retrieval`;
6. `RP005-TEXT-01 / transfer`;
7. `RP005-TEXT-01 / requested_output_explanation`; and
8. `RP005-TEXT-01 / truth_boundary_explanation`.

Each record uses the frozen packet/mapping identity, exact dimension set,
all dimensions true, attempt count `1-99`, hint level `0-3`, confidence
`null|low|medium|high`, allowlisted misconception tags, and
`mastery_status="mastered"`.

### Privacy and sanitation

Reject the whole record for any unapproved key or serialized learner source,
raw answer, free reasoning, feedback, case/source text, private note,
identity content, credential, secret, endpoint, URL, payload, request,
response, exam item, external-action request, pointer/focus history, event
token, route intent, live reading, or sealed content.

Clear unsubmitted code, choices, feedback, case answers, private/transient
work, tokens, pointer history, and focus history at retry, transition, save,
return, and resume boundaries as applicable.

### Atomic save and recovery

The dedicated RP-005 adapter must:

1. retain exact prior RP-005 raw bytes or verified absence;
2. reject a malformed prior record rather than overwrite it;
3. strictly sanitize the complete candidate;
4. verify exact TD-004 raw bytes before writing;
5. perform one replacement at the RP-005 key only;
6. read back raw bytes and the strictly sanitized object;
7. accept only exact canonical raw/object equality plus unchanged TD-004
   bytes;
8. otherwise restore exact prior RP-005 bytes or verified absence;
9. verify rollback and unchanged TD-004 bytes; and
10. report failure without progression if rollback cannot be proven.

Strict read-back alone defines success. Entry, save attempt, recovery,
restore, and return may never rewrite TD-004 bytes.

Unsaved reload or invalid RP-005 data returns to exact TR-40/SC-05 and
requires fresh entry. Exact valid save restores only replay-free MF-30.
Same-session partial progress may guide first-incomplete focus but may not be
persisted in the RP-005 save key.

## Accessibility and responsive contract

- Seven equivalent modalities: pointer, touch, Enter, Space, switch, speech,
  and screen reader.
- Validate all fields before one-hit token consumption.
- Exactly one active owner/message/content/action group.
- Semantic headings, persistent labels, field-associated errors, and one
  polite atomic status.
- Deterministic focus to the group heading, first invalid control, or next
  required control.
- Every required control at least `44 x 44 CSS px`.
- No meaning or credit relies on color, motion, sound, position, frequency,
  or timing alone.
- Reduced motion uses still/direct replacement and
  `scroll-behavior:auto`.
- Forced colors preserve borders, focus, grouping, ownership, and native
  controls.
- Grayscale retains recurrence, divergence, sealed unavailability,
  stewardship, and interface distinctions.
- No time limit, speed score, forced animation, or dexterity evidence.

Required DPR-1 review layouts:

- `1920 x 1080`;
- `1366 x 768`;
- `390 x 844`; and
- `768 x 900` width-equivalent effective-`200%` text zoom.

Wide layouts keep the complete shell in `100dvh`, the world dominant, no
outer horizontal/vertical scroll, and the required action visible.
Narrow/zoom layouts use one natural vertical column with no horizontal
escape or clipped owner, limits, feedback, recovery, save, or return.

## Closed fixture

Create one storage-free, production-excluded fixture with an exact allowlist
covering at least:

- route-ready and notation-recorded TR-40;
- invalid, stale, and duplicate route attempts;
- accepted MF-00;
- every MF-10 observation class and the completed set;
- dense and repair states for Python and text technique;
- bounded review;
- save-write and read-back failures;
- exact MF-30 restore;
- early and final Three-Current Reach returns; and
- City Threshold return.

The fixture must reject URL, query, browser storage, campaign save, arbitrary
JSON, and runtime-state injection. It must be absent from production imports
and output. Review includes exact layouts, forced colors, reduced motion,
grayscale, focus/status, target sizes, containment, crop/hotspot registration,
local requests, console/page errors, scene/alt identity, a bounded capture
set, and a compact machine-readable summary.

## Scene and production assets

### Existing retained asset

The released SC-05 runtime master remains unchanged:

- `sc05-three-current-panorama-runtime-master-v1-VoJlZWmR.webp`;
- `2,163,752` bytes; and
- SHA-256
  `B6E0F34A917732DBB7B66B65968198CFC068BC650AC00CD8A01F095A6109F63F`.

All accepted TD-004 media hashes must remain present.

### Exactly two new runtime image roles

1. `SC-06-PANORAMA-MASTER` — maximum-quality cinematic photorealistic
   first-person 16:9 world identity.
2. `SC-06-DETAIL-MASTER` — spatially consistent forensic detail containing
   all four peer observation zones.

Each source is preferably at least `6144 x 3456` and never below
`3840 x 2160`. Both require provenance, direct runtime import, emitted
byte/hash identity, accessible alternative treatment, registered responsive
crops, grayscale review, artifact cleanup, and explicit placeholder
retirement.

Only those two new image files may enter production. Review derivatives,
masks, contact sheets, and source packages remain non-runtime evidence;
runtime crops must sample the two registered masters without emitting extra
image payloads. No new audio, font, video, or network-fetched asset is
permitted.

### Fixed visual canon

- incoming carrier, unequal receiver field, recurring exposed range,
  neutrally maintained bounded divergence, opaque sealed boundary, three
  visible stewardship eras, embedded replica-only coupling, independent
  maintenance, and destinationless serviced continuation;
- one panorama for arrival/work/save/restore/return and one registered detail
  for all observations;
- first-person clean plates with no protagonist, ship, hands, body, shadow,
  reflection, portrait, prior human trace, readable Builder text, human room,
  console, chart, graph, score, warning treatment, doorway, or portal;
- physically causal materials, light, atmosphere, effects, and monotonic
  environmental clocks;
- equal exposure and credibility for recurring and divergent traces; and
- no world-state success/failure highlight or response.

## Offline, Tour, authority, and invariant world

- Local/offline course practice only.
- No live Azure, Foundry, Language, service, terminal, landscape, or sealed
  branch call.
- No credential, resource, endpoint, payload, request, response, external
  action, authentication, consent, permission, access, ranking, correction,
  sampling, exam guarantee, or official authority.
- Demo Tour rejects before campaign storage, stays storage-free and
  zero-credit, and cannot dispatch, save, restore, or unlock.
- SC-05 and SC-06 remain invariant through entry, observation, learning,
  remediation, save, restore, return, and resume.
- No city, Builder, Machine, route, physical, evidence, or external delta is
  caused by learning or save state.
- Truth, quality, value, identity, native category, purpose, audience,
  ownership, sealed contents, reward, recognition, and response remain
  unknown or absent.

## Performance and budget

`PBA-TD005-v1` is one-time and non-compounding:

| Measure | Cap |
|---|---:|
| Aggregate JavaScript | `1,310,110` bytes |
| Aggregate CSS | `89,408` bytes |
| Production modules | `192` |
| New runtime image assets | `2` |
| New runtime image bytes | `8,388,608` |
| Total runtime media | `29,924,731` bytes |
| Production build | `60s` |
| Focused suite | `30s` |
| Related suite | `60s` |
| Full suite | `60s` |
| Complete E2E | `180s` |
| Sampled main-thread task | `100ms` |

Every emitted JS/CSS chunk and runtime asset counts. The validator must retain
every accepted TD-004 media identity and reject overages, disallowed classes,
hidden payload, and missing release timings.

No quality, canon, learning, evidence, privacy, sanitation, accessibility,
responsive, save/recovery, offline, visual, or release requirement may be
weakened to satisfy a cap. Overage requires a formal Science/Mission
variance.

## Permitted exits and hard stop

Permitted exits:

- early or final `RETURN TO THREE-CURRENT REACH` reconstructs exact
  TR-40/SC-05 without write or replay;
- early or final `RETURN TO CITY THRESHOLD` uses the existing accepted
  write-free anchor; and
- from restored TD-004 only, the existing Calibration Margin return remains
  available under its released contract.

Do not create a direct RP-005-to-Calibration-Margin or Civic shortcut.

The hard stop is `MF-30 VERIFY + RETURN`. Optional serviced-continuation
notation is LOOK/record only, destinationless, zero-evidence, and non-routing.

Forbidden exposure includes:

- any RP-006 route, destination, bearing, opening, successor, or content;
- a traversable apparent common return or route-bearing notation;
- native identity, category, purpose, answer, truth, quality, correctness,
  preference, failure, audience, ownership, permission, reward, access,
  judgment, authority, or Machine relation;
- sealed-branch contents, result, owner, audience, or reason;
- live landscape/service access, credential, endpoint, payload, external
  action, sampling, ranking, correction, or world response;
- city, Builder, or Machine invitation, acceptance, refusal, attention,
  memory, recognition, or reply;
- RP-013, successor, post-ending content, changed ending, or hidden lore; and
- Martin-owned browser storage, campaign save, or protected user work.

## Marine ownership and bounded freedoms

### Reconnaissance Sergeant

Own one creative treatment: emotional rhythm, player fantasy, attention
hierarchy, concept-level materials, lighting, atmosphere, sound restraint,
motion restraint, and contrast with recent scenes. Preserve the exact scene
content and all evidence/authority boundaries. Sound may use only existing
runtime capability; this shell authorizes no new audio asset.

### Tactical Operations Specialist

Own exact interface geometry, responsive layout, action grouping, focus IDs,
copy placement, hotspot registration, fixture state manifest, and interaction
blueprint. May refine non-semantic visible copy but cannot change owner,
route, graph, evidence, persistence, returns, or hard stop.

### Combat Engineer

Own normal controller/adapter implementation, module factoring, semantic
token mechanics, tests, fixture implementation, and performance-safe code.
May choose internal function and file names except the frozen public
identities. Must not import the protected journey as the normal adapter.

### Quartermaster

Own final player-facing structural copy, both production image masters,
provenance package, alt text, asset ledger, direct imports, placeholder
retirement, and content consistency. May refine wording inside fixed meaning;
may not add a third runtime image or new audio/font/video/network payload.

### Image Specialist

Own final presentation polish, grade, crops, registered detail treatment,
effect timing, forced-color/reduced-motion/grayscale parity, responsive
visual review, and exactly one spoiler-safe cycle-reveal candidate after
choosing and comparing one accepted decision with visual canon. May not alter
geometry, evidence, state, asset count, or invariant-world meaning.

### Intelligence Officer

Own independent release validation, line-by-line shell reconciliation,
variance classification, as-built master updates, retrospective, exact
reveal acceptance/rejection, synchronized handoff, and release disposition.

## Validation ladder

### Tier 1 — contract

- standing route authority, exact endpoints, owner, graph, and hard stop;
- TD-004 schema, sanitizer, adapter, raw bytes, scene resolver, choices,
  notation, and returns;
- RP-005 frozen mapping, record schema, denylist, evidence order,
  atomicity, recovery, scene matrix, visual canon, and budget;
- forbidden-boundary and protected-work scans.

### Tier 2 — focused

- seven route modalities, validation-before-consumption, token freshness,
  invalid/duplicate/interruption/reload, and one zero-effect arrival;
- immutable TD-004 bytes and exact SC-05 identity;
- all 24 observation orders and idempotent revisit;
- exact Python/AI-901 chains, both explanations, no-cross-credit,
  answer-free retry, and Tour isolation;
- RP-005 sanitizer, commit/read-back/rollback, restore, and returns;
- scene resolver, asset identity, alt text, focus/status, and fixture states.

### Tier 3 — related

- TD-003/TD-004 save, rollback, restore, route, scene, interruption, return,
  fixture, UI, accessibility, and production-copy regression;
- protected RP-005 reference and curriculum self-tests;
- no protected journey imported into production.

### Tier 4 — full product

- full game suite and every readiness validator;
- production build and `PBA-TD005-v1` in candidate/release mode;
- exact served root/deep-link/JS/CSS/SC-04/SC-05/SC-06 bytes and hashes;
- fixture marker/path/scenario/source-map exclusion from production;
- local-only request scan and patch integrity.

### Tier 5 — release

- build completes before one complete non-overlapping E2E;
- isolated coordinator-owned preview and exact served identity;
- complete E2E with every gate true and no runtime error;
- exact four-layout fixture matrix plus forced-color, reduced-motion, and
  grayscale review;
- human visual inspection of both world masters, crops, registration,
  invariance, and artifact cleanup;
- incidental QA restoration and owned-process shutdown only;
- shell-to-as-built reconciliation, variance classification, exact reveal
  validation, synchronized push, and `HEAD == origin/main`.

## Definition of done

The shell is complete only when all of the following are independently
proven:

1. Exact TD-004 TR-40 record, bytes, choices, returns, and SC-05 identity are
   unchanged.
2. `TD005-RTA-001` alone mounts one zero-evidence MF-00/SC-06 arrival after
   full validation and fresh token consumption.
3. Invalid, duplicate, stale, Tour-derived, interrupted, and reloaded entry
   paths fail closed.
4. MF-00 through MF-30 work in order; all 24 observation orders converge.
5. Recurrence, divergence, sealed unavailability, and stewardship remain
   equal, nonjudgmental physical evidence.
6. `PY-012`, `RP005-TEXT-01`, and both explanations pass their frozen strict
   chains independently.
7. No scene, route, presentation, Tour, modality, focus, timing, or save
   display grants learning evidence.
8. RP-005 saves only the exact ten-key, nine-note-key, eight-record allowlist.
9. Private/transient material is rejected and cleared at every required
   boundary.
10. One atomic replacement, exact read-back, verified rollback, unchanged
    TD-004 bytes, deterministic recovery, and replay-free MF-30 restore pass.
11. Returns to TR-40 and City Threshold are write-free and replay-free; no
    shortcut or later route exists.
12. Seven modalities, focus, status, errors, target sizes, forced colors,
    reduced motion, grayscale, desktop/laptop/narrow/zoom layouts, and closed
    fixture pass.
13. Offline/no-authority/no-exam-guarantee, no-credit Tour, local-only
    requests, and invariant SC-05/SC-06 pass.
14. Both and only both SC-06 runtime image roles are directly imported,
    provenance-complete, accessible, responsive, artifact-clean, and visually
    maximum quality.
15. Every budget, full-suite, validator, served-identity, E2E, visual,
    cleanup, patch, and synchronization gate passes.
16. No purpose/identity/access/reward/authority/world response, RP-006 route,
    RP-013, successor, post-ending content, or hidden lore is exposed.
17. Intelligence classifies every variance and issues
    `AS BUILT RELEASED`; until then TD-004 remains the released boundary.

## Variance protocol

No Marine may silently alter this shell. Record:

```text
Variance ID:
Discovering role:
Shell requirement:
Observed evidence:
Requested classification:
Proposed correction or bounded change:
Earliest owning return stage:
Canon / learning / privacy / accessibility / save / budget / world impact:
Validation needed:
```

Return product conflicts to Commandant; canon/world/mystery to Colonel;
campaign/route/checkpoint to Operations; systems/learning/evidence/privacy/
accessibility/save/offline/recovery/performance/budget to Science; shell
ambiguity to Mission; presentation roles to their owning Marine. Intelligence
owns final classification.

## Protected files and systems

- Never open `DO_NOT_READ_HORIZON_ARCHIVE_HIDDEN_LORE_VAULT.md`.
- Never inspect, alter, stage, move, delete, or commit
  `Art Of No Mans Sky Book Scan.pdf` or `Simplilearn Training Files/`.
- Never inspect or mutate Martin's browser storage, campaign save, cookies,
  profile, or session to create evidence.
- Use deterministic source, tests, closed fixtures, agent-owned isolated
  previews, and served bundles.
- Preserve unrelated user work and the archived two-team infrastructure.

## Shell change history

| Version | Date | Change |
|---|---|---|
| `MC-TD005-HOLD-v1` | 2026-07-26 | Correctly stopped construction because no current transition authority existed |
| `SS-RP005-MANYFOLD-RETURN-v1` | 2026-07-26 | Standing adjacent-transition authority, `CFS-TD005-v2`, `VE-TD005-v2`, and `PBA-TD005-v1` resolve the route defect and freeze the complete construction contract |

## Mission signature and disposition

**Mission Captain / `mission_captain`**

**`SHELL READY — SS-RP005-MANYFOLD-RETURN-v1`**

The shell is conflict-free, integration-sized, measurable, and bounded by
existing surface authority. Marine deployment may begin only after the
dedicated Mission commit is pushed and synchronization is verified.

## Exact Reconnaissance Sergeant handoff

Read `AGENTS.md`, `NEXT_INSTANCE_HANDOFF.md`,
`SKYSCRAPER_AGENT_WORKFLOW.md`, the registry, the Reconnaissance Sergeant
profile, and this exact shell in full. Then read only the shell-cited
Commandant, Colonel, scene, visual-charter, released TD-004, recent-reveal,
and current-control sections needed for the creative pass.

Create one bounded `TD-005` Playable Slice Creative Treatment for
`SS-RP005-MANYFOLD-RETURN-v1`. Define:

- one-sentence emotional promise and player fantasy;
- arrival, developing tension, understanding, and exit emotions;
- attention hierarchy and `30-41` minute clean / `18-25` minute mastered
  pacing;
- the relationship between invariant SC-06 world, expedition interface,
  sanitized Builder work, and neutral course practice;
- concept-level materials, lighting, atmosphere, existing-audio use, and
  reduced-motion direction;
- contrast with recent SC-04/SC-05 scenes and reveals;
- fixed invariants, flexible staging choices, risks, and any formal variance.

Preserve the exact route, graph, evidence, record, privacy, save/recovery,
accessibility, responsive, asset-count, budget, offline/Tour, invariant-world,
return, and MF-30 hard-stop contracts. Do not imply correctness, failure,
purpose, identity, access, reward, authority, response, or an RP-006 route.

Produce `TD-005/06-PLAYABLE-SLICE-CREATIVE-TREATMENT.md`, update the metrics
and current handoff, commit one dedicated Reconnaissance stage, and leave an
exact Tactical Operations Specialist handoff with `CREATIVE LOCK`, `REVISE`,
or `HOLD`.
