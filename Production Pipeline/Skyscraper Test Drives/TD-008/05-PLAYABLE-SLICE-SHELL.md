# TD-008 Playable Slice Shell - Offset Reach

## Document control

| Field | Value |
|---|---|
| Stage | Mission Captain |
| Agent ID | `mission_captain` |
| Shell ID | `SS-RP008-OFFSET-REACH-v1` |
| Slice | `TD-008-RP008-OFFSET-REACH-v1` |
| Campaign address | `RP-008 / SC-09 / OR-00-OR-30` |
| Sole route | `TD008-RTA-001` |
| Graph | `BV-30 / SC-08 -> OR-00 -> OR-10 -> OR-20 -> OR-30 / SC-09` |
| Released predecessor | `TD-007 / SS-RP007-BRAIDED-VERGE-v1 / BV-30 / SC-08` |
| Product authority | `GDB-TD008-v1` |
| World authority | `WNMP-TD008-v1` |
| Operations authority | `CFS-TD008-v1` |
| Science authority | `VE-TD008-v1` |
| Budget authority | `PBA-TD008-v1` |
| Source commit | `91d62301e6e9a239fe8bd9a82e28a3638dd7d409` |
| Disposition | **`SHELL READY`** |

This is the sole construction contract for TD-008. It reconciles the four
preceding Colonel gates into one integration-sized normal-play slice. It does
not implement the route, controller, UI, fixture, art, or content; it does not
release RP-008 to players. Marines may build only this shell. Intelligence
alone may release the as-built result.

## Mission reconciliation

Mission independently reconciled:

- `GDB-TD008-v1`: one contiguous, player-visible, recoverable advance with
  strict evidence, privacy, accessibility, offline, no-authority, and
  invariant-world behavior;
- `WNMP-TD008-v1`: one bounded scope-revision study that preserves the valid
  local TD-007 account while treating recurrence, non-contact, cross-family
  contact, and unavailability as limited observations rather than answers;
- `CFS-TD008-v1`: exact structural adjacency, fresh Pilot-owned
  `TD008-RTA-001`, `OR-00` through `OR-30`, exact returns, and hard stop;
- `VE-TD008-v1`: buildable normal identities, independent learning/evidence,
  sanitation, atomic persistence, replay-free recovery, seven modalities,
  four layouts, the closed 64-scenario fixture, two provisional image roles,
  and complete validation requirements; and
- `PBA-TD008-v1`: a fresh non-compounding budget measured from the exact
  released TD-007 build.

No product, canon, route, learning, evidence, privacy, accessibility, save,
recovery, offline, performance, visual, or world-state conflict remains.
There is no unresolved duplicated ownership and no requirement needs hidden
lore, Martin-owned browser state, a live service, or an external decision.

The protected `OffsetReachProtectedJourney` and RP-008 curriculum package are
reference evidence only. They must remain absent from App, main, normal route,
normal storage, the production fixture surface, and the production bundle.
Normal implementation must use new distinct identities and may reproduce only
the frozen behavior defined here.

## Current source and build identity

Mission began from exact Science commit
`91d62301e6e9a239fe8bd9a82e28a3638dd7d409` with the tracked worktree clean.
Fresh Mission evidence is:

| Measure | Result |
|---|---:|
| RP-008 focused protected suite | `14/14 PASS`; Node `0.306s`; wall `0.363s` |
| RP-008 mapping self-test | `PASS` |
| Production modules | `198` |
| Vite build | `7.04s` |
| Build wall | `8.051s` |
| Aggregate JavaScript | `1,392,211` bytes |
| JavaScript identity | `index-DZHephx8.js`; SHA-256 `37FC8DDA6435EAB878410746C6EA989C5A7141E359C330F1A0B6BCC8AC4ADCE6` |
| Aggregate CSS | `95,573` bytes |
| CSS identity | `index-C7RpNj2r.css`; SHA-256 `AB0B450E5CE4C6890F197BA4D7E82B8797919010FC2BFFEE8E1747E5A2043C98` |
| Runtime media | `33,421,873` bytes across `15` exact TD-007 files |
| `PBA-TD008-v1` baseline | `PASS`; no TD-008 normal media or modules exist |

Normal RP-008 route, controller, persistence, UI, closed fixture, SC-09
runtime images, E2E, and release integration remain unbuilt. The released
normal boundary remains TD-007 BV-30 plus its exact returns.

## Player-facing purpose and emotional promise

The Pilot enters one immense Offset Reach and deliberately records six equal
physical observations:

1. both familiar continuities remain separately traceable;
2. one familiar bounded contact recurs;
3. one comparable exposed passage shows both familiar continuities without
   visible contact;
4. one third material continuity makes one comparable bounded contact with
   only one familiar continuity;
5. one further case remains externally bounded and unavailable; and
6. layered compatible stewardship remains visible without a single author,
   institution, hierarchy, rule, or purpose.

The Pilot then performs bounded JSON work over sanitized replicas, completes
separate course-authored information-extraction practice and two explanations,
reviews every independent responsibility, saves one expedition-owned note,
verifies it without replay, and returns.

Emotional promise:

> New evidence can narrow the reach of a valid local account without making
> that account false or turning comparison into explanation.

Nothing in the scene, learning, save, or return recognizes, judges, invites,
rewards, rejects, authorizes, opens, corrects, or responds to the Pilot.

Clean first play targets `35-48 minutes`; mastered replay targets `21-30
minutes`. There is no time limit, speed score, timing credit, forced motion,
or forced audio.

## Exact entry and sole transition

### Released source state

Normal entry begins only from exact, replay-free `BV-30 VERIFY + RETURN /
SC-08` with all of these simultaneously true:

- campaign mode, never Demo Tour;
- storage key `horizon-archive-rp007-braided-verge-save-v1`;
- record version `rp007.braided-verge-save.v1`;
- shell `SS-RP007-BRAIDED-VERGE-v1`;
- controller `rp007.braided-verge-controller.v1`;
- packet `RP-007`, mapping `RP007-A3-BRAIDED-VERGE`, checkpoint
  `braided_verge_complete`;
- exact canonical raw/object equality for the released ten-root-key,
  fourteen-note-key, eight-record RP-007 save;
- `continuation="continuation"`, null city and external deltas, and
  `successor=null`;
- exact released owner, group, focus, status, and SC-08 scene identity with
  zero replay;
- cleared learner source, report, cases, answers, feedback, private notes,
  identity, credential, endpoint, payload, response, token history, and
  temporary work; and
- exact immutable TD-006, TD-005, and TD-004 byte strings.

Construction must reuse, not fork, migrate, weaken, annotate, or rewrite:

- `BRAIDED_VERGE_SAVE_KEY` and `BRAIDED_VERGE_RECORD_VERSION`;
- `BRAIDED_VERGE_SHELL_VERSION` and
  `BRAIDED_VERGE_CONTROLLER_VERSION`;
- `sanitizeBraidedVergeSave` and
  `createBraidedVergeStorageAdapter`;
- the released BV-30 resolver and both released return paths; and
- every accepted TD-004 through TD-007 record and media identity.

The RP-007 record is immutable predecessor evidence. No RP-008 action may
prefill, cross-credit, replace, supersede, or invalidate it.

### Sole route action

```text
TD008-RTA-001
PILOT // EXPEDITION NAVIGATION
PILOT // FOLLOW EXPEDITION-MARKED ADJACENT SURVEY TO OFFSET REACH
```

The active group is `bv30_offset_reach_route_choice`. The route uses one
opaque, fresh, private-free, one-hit event token and exactly one of the seven
approved modalities.

The following never expose, synthesize, authorize, or dispatch the route:
BV-30 alone; recurrence; association; non-contact; relative order; saved
completion; continuation text; destinationless notation; imagery; sound;
focus; restore; scenery; planning order; packet or scene names; and any Tour
state.

### Validation before consumption

The normal adapter performs this order exactly:

1. isolate and reject Demo Tour before campaign storage access;
2. read the released RP-007 record once;
3. run the released sanitizer and require exact canonical raw/object equality;
4. require exact replay-free BV-30/SC-08 state and identities;
5. prove TD-006, TD-005, and TD-004 raw bytes unchanged;
6. reject and clear private, transient, extra-key, partial, combined, or
   contaminated material;
7. validate exact mode, source shell/controller, packet, mapping, checkpoint,
   group, Pilot owner, action, modality, and token shape;
8. prove the token is fresh and unused;
9. consume it exactly once; and
10. atomically mount only `OR-00 ARRIVE + ORIENT / SC-09`.

One accepted intent yields exactly one zero-evidence OR-00 arrival. It writes
no predecessor or candidate record; grants no observation or learning
evidence; replays nothing; preserves null deltas and `successor=null`; and
changes no physical surface, environmental clock, authority, or world state.

## Fixed state graph and ownership

```text
BV-30 OFFSET ROUTE CHOICE / SC-08
  -> OR-00 ARRIVE + ORIENT / SC-09 panorama
  -> OR-10 INSPECT OFFSET EVIDENCE / SC-09 panorama + relation detail
  -> OR-20 REVISE SCOPE + SAVE / invariant SC-09 + separate work/course UI
  -> OR-30 VERIFY + RETURN / invariant SC-09 panorama
  -> exact BV-30 / SC-08 OR exact City Threshold anchor
```

At every instant there is exactly one active owner/message/content/action
group. `SCENE` owns only observable physical facts. `PILOT` owns expedition
labels and provisional interpretation. `SYSTEM` owns route validation,
replica provenance, availability, sanitation, progress, save, restore, and
limits. `BUILDER WORK` owns only the exposed incomplete executable fragment.
`TEACHER` owns course-authored guidance. No owner may speak as another.

### State, world, alternative, focus, and status matrix

| State/group | World role and required alternative identity | Owner / deterministic focus | Required status and behavior |
|---|---|---|---|
| exact released BV-30 restore | exact released SC-08 panorama and unchanged released alternative | released saved-controls owner / released heading | zero replay; existing note and both returns intact |
| `bv30_offset_reach_route_choice` | same SC-08 panorama and alternative | `PILOT // EXPEDITION NAVIGATION` / `td008-route-choice-heading` | one route peer beside both existing returns; scenery and notation inert |
| invalid intent with valid RP-007 | same SC-08 identity | Pilot / same route heading | no scene, write, evidence, token, focus, or world change |
| invalid RP-007 dependency | released TD-007 recovery to exact `IW-30 / SC-07` | released recovery owner/focus | never mount or announce SC-09 |
| `or00_orientation` | `SC-09-PANORAMA-MASTER`: the familiar laminated ribbon and cellular mantle remain separately traceable through an immense working reach; one recurring contact, one open passage, one third filament continuity, and one opaque unavailable case remain visible; nothing reacts or opens | `SCENE // OFFSET REACH` / `or00-orientation-heading` | one zero-credit arrival; explicit orientation or safe return |
| `or10_observations` | SC-09 panorama plus registered relation detail: all six physical roles remain equal, structurally distinct, and unordered; exposed non-contact proves no separation, cross-family contact proves no equivalence, and unavailable depth remains unseen | `SCENE // SENSOR RECORD` / `or10-observation-heading` | exact `n of 6` text plus observation ID; revisits idempotent; no learning credit |
| `or20_python_primary`, `or20_python_trace`, `or20_python_transfer` | invariant SC-09 panorama plus a separate Builder-work interface; world alternative unchanged | `BUILDER WORK` then `TEACHER` / first incomplete field or form heading | truthful memory-only JSON label; local answer-free status only |
| `or20_ai_primary`, `or20_ai_retrieval`, `or20_ai_transfer` | invariant SC-09 panorama plus neutral course interface with no world referent | `TEACHER` / first incomplete case control | one scored result only after all four cases; no scene-derived remediation |
| `or20_selection_explanation`, `or20_inference_explanation` | invariant SC-09 panorama plus separate course authoring | `TEACHER` / first incomplete explanation field | only the applicable explanation dimensions; no cross-credit |
| `or20_review` | invariant SC-09 panorama plus separate expedition review | `PILOT // EXPEDITION REVIEW` / `or20-review-heading` | every conjunct shown independently; no inferred completion |
| `or20_save` | invariant SC-09 panorama plus separate save confirmation | `PILOT // EXPEDITION RECORD` / `or20-save-heading` | focus contained until save, cancel, or Escape; one atomic polite result |
| exact saved `or30_restore` | invariant SC-09 panorama; both bounded notes available separately as text | `SYSTEM // RESTORED EXPEDITION NOTES` / `or30-restore-heading` | zero replay; only two returns and optional inert notation |
| invalid/unsaved RP-008 re-entry | exact BV-30/SC-08 | released predecessor owner/focus | clear candidate work and require fresh route intent |
| return to Braided Verge | exact BV-30/SC-08 | released BV-30 owner/focus | write-free and replay-free |
| return to City Threshold | existing accepted City Threshold identity and alternative | existing accepted owner/focus | write-free and replay-free |
| any RP-009 or later state | no world or alternative identity | none | may not mount, render, announce, dispatch, or receive focus |

The exact longest required heading/label sample is:

```text
Review retained local association and independent offset responsibilities
```

Tactical may choose shorter compliant copy elsewhere, but may not change or
shorten this named sample. Image Specialist must live-render its exact text at
all four shell layouts before `PRESENTATION COMPLETE`; source-only or fixture-
only assertions are insufficient. Intelligence must independently reproduce
the same four-layout result.

## Required interactions and actions

### OR-00 - arrive and orient

- Show the retained RP-007 account as separately valid and unchanged.
- Use `ORIENT TO OFFSET REACH` to acknowledge only that three material
  continuities and four exposed case silhouettes are present.
- Orientation grants zero observation, learning, save, route, authority, or
  world evidence.
- `RETURN TO BRAIDED VERGE` and `RETURN TO CITY THRESHOLD` remain safe.

### OR-10 - inspect six equal observations

The six IDs occur exactly once in the final canonical observation order,
regardless of the player's chosen order:

```text
familiar_continuity_trace
recurring_familiar_contact
comparable_non_contact
cross_family_contact
unavailable_case
layered_stewardship
```

All `6! = 720` orders converge. Revisit is idempotent. Each deliberate action
records only its matching physical ID, presents exact `n of 6` text, returns
focus to the peer chooser, and changes no scene or world. Duplicate,
unsupported, universal, exclusive, separation, equivalence, unity, causal,
purpose, ownership, or unavailable-content claims do not advance.

No order, image, crop, contact placement, material, brightness, glow, motion,
sound, timing, focus, modality, navigation, Tour, save display, or
accessibility behavior grants observation or learning evidence.

### OR-20 - independent learning, review, and save

After all six observations, deterministic first-incomplete ownership is:

1. `PY-016 / primary`;
2. `PY-016 / trace`;
3. `PY-016 / transfer`;
4. `RP008-INFORMATION-EXTRACTION-01 / primary`;
5. `RP008-INFORMATION-EXTRACTION-01 / retrieval`;
6. `RP008-INFORMATION-EXTRACTION-01 / transfer`;
7. technique-selection explanation;
8. inference-limit explanation;
9. bounded review; and
10. explicit atomic save.

Each transition replaces the whole active group and clears the prior private
work. A conjunction may open review/save but may never manufacture or repair a
missing conjunct.

### OR-30 - verify and return

Only exact verified normal RP-008 bytes restore OR-30. Restore presents the
unchanged retained RP-007 summary and bounded offset note separately, proves
private/transient work absent, and replays no route, arrival, observation,
evaluator, JSON plan, case, explanation, review, save, animation, sound,
focus event, or world event.

Permitted exits are exactly:

- `RETURN TO BRAIDED VERGE` -> exact BV-30/SC-08; and
- `RETURN TO CITY THRESHOLD` -> exact accepted City Threshold anchor.

Both are write-free, replay-free, reversible, and clear unsaved candidate
work. Optional `RECORD DESTINATIONLESS OFFSET` records only
`destination=null` and `routeOpened=false`; it opens nothing.

## Frozen learning and evidence

### Python - `PY-016`

The chain is exact:

```text
8/8 primary
-> failed-check-only answer-free remediation
-> delayed closed-note 8/8 trace
-> genuinely blank 8/8 transfer
```

The eight checks are:

```text
json_import_and_call_shape
records_decoded_exactly
summary_exact_keys
counts_and_retained_local
unavailable_case_preserved
unsupported_limits_remain_none
round_trip_matches_sorted_json
inputs_unchanged_and_no_forbidden_operations
```

The approved source shape imports `json`, decodes the supplied sanitized JSON
once, derives the exact ten-key summary from unchanged input records, encodes
once with `json.dumps(..., sort_keys=True)`, and decodes that serialization
once. The exact summary keys are:

```text
retained_local_association
recurring_familiar_contact
comparable_non_contact
cross_family_contact
unavailable_case
universal
exclusive
unity
cause
purpose
```

Retained local association is true; the four supported counts are each `1`;
and universal, exclusive, unity, cause, and purpose remain `None`/null. Input
order and bytes remain unchanged. The unavailable record remains explicit.
Primary cannot prefill transfer.

Normal browser implementation may use pure JavaScript, `JSON.parse`, and a
deterministic serializer only over session-memory sanitized replicas. It may
not use `eval`, `Function`, arbitrary Python, Python/WASM, Worker, file/path
APIs, browser persistence, network, package, environment, process, clipboard,
download, file picker, or external input.

Exact truthful label:

> The course validator checks the approved Python source shape and performs
> the supplied JSON decode, bounded derivation, sorted encode, and decode plan
> over session-only sanitized replicas. It does not execute arbitrary Python
> or contact a live service.

A miss exposes only actually failed check IDs, provides no solution code or
expected JSON, clears all source/JSON/output/diagnostic work, and opens a
genuinely blank unlimited retry focused at the first failed control.

### Information extraction - `RP008-INFORMATION-EXTRACTION-01 / AI901-D1-O8`

This independent neutral course chain is exact:

```text
8/8 primary
-> actually-scored-tag-only answer-free remediation
-> delayed 8/8 retrieval
-> genuinely blank 8/8 transfer
-> two separately scored explanations
```

Each form uses four course-authored text cases and two dimensions per case:
`technique` and `deciding_signal`. The choices distinguish OCR, field
extraction, multimodal structured extraction, and extract/enrich/index for
search. Cases contain no SC-09 or Builder-world referent.

Remediation may derive only from an actually emitted allowlisted misconception
tag. It may identify source modality and requested output shape, contrast
techniques, remove scene or inferred-weakness cues, restate the missing-value
boundary, and use one neutral guided case. It never supplies an answer.

Required separate explanation meanings are:

1. source modality and requested output contract select the technique; and
2. extracted or missing values do not establish truth, universality,
   exclusivity, separation, equivalence, unity, cause, or purpose.

A miss clears choices, answers, reasoning, feedback, cases, and diagnostics;
announces one complete bounded System status; and opens a wholly blank
unlimited retry at the first incomplete scored control.

### Evidence firewall

The retained RP-007 summary, six observations, three Python records, three
AI-901 records, two explanations, review, and save are separate conjuncts.
None may be prefilled, inferred, compensated, selected, repaired, or finalized
by another. Route, scene, presentation, focus, timing, modality, confidence,
navigation, Tour, save/restore display, and world state are zero credit.

## Exact normal persistence and sanitation

### Distinct identities

| Field | Exact value |
|---|---|
| Storage key | `horizon-archive-rp008-offset-reach-save-v1` |
| Record version | `rp008.offset-reach-save.v1` |
| Controller | `rp008.offset-reach-controller.v1` |
| Route controller | `td008.route-controller.v1` |
| Fixture | `td008-offset-reach-v1` |
| Packet | `RP-008` |
| Mapping | `RP008-A3-OFFSET-REACH` |
| Checkpoint | `offset_reach_complete` |

None may alias `rp008.protected-journey.v1`.

Exactly eleven ordered top-level keys:

```text
version
packetId
mappingId
checkpoint
continuation
cityStateDelta
externalStateDelta
successor
retainedRp007Summary
note
evidence
```

`continuation="continuation"`; city and external deltas and successor are
null.

`retainedRp007Summary` has exactly these ordered keys and values:

```text
checkpoint = "braided_verge_complete"
continuities = "distinct_visible_continuities"
association = "recurrent_exposed_association"
difference = "one_bounded_difference"
junction = "closed_junction_unavailable"
unity = null
cause = null
purpose = null
```

`note` has exactly these ordered keys:

```text
observations
retained_local_association
recurring_familiar_contact
comparable_non_contact
cross_family_contact
unavailable_case
universal
exclusive
unity
cause
purpose
stewardship
replicas
```

The observation IDs use the exact six-ID canonical order. Retained local
association is `true`; each supported count is `1`; the five unsupported
limits are null; `stewardship="layered_stewardship_observed"`; and
`replicas="sanitized_precomputed_only"`.

Exactly eight ordered finalized evidence records:

1. `PY-016 / primary`;
2. `PY-016 / trace`;
3. `PY-016 / transfer`;
4. `RP008-INFORMATION-EXTRACTION-01 / primary`;
5. `RP008-INFORMATION-EXTRACTION-01 / retrieval`;
6. `RP008-INFORMATION-EXTRACTION-01 / transfer`;
7. `RP008-INFORMATION-EXTRACTION-01 / selection_boundary_explanation`; and
8. `RP008-INFORMATION-EXTRACTION-01 / inference_boundary_explanation`.

Each record permits only packet, mapping, form, skill/objective ID, exact
dimension correctness, bounded attempt count `0-99`, hint level `0-3`,
confidence `null|low|medium|high`, actually scored allowlisted misconception
tags, and `mastery_status="mastered"`.

### Privacy and clearing

Never persist or log learner source or fragments; supplied, derived, encoded,
or restored JSON; raw cases; active choices; answers; reasoning; feedback;
hint text; diagnostics; copied output; private notes; identity; focus,
pointer, or token history; credentials; secrets; endpoints; payloads;
responses; source content; prompts; exam items; unavailable-case content;
external-action requests; or unsupported conclusions.

Clear all such work on miss, retry, owner-group replacement, scene change,
early return, interruption, failed write, invalid restore, successful save,
verified restore, and Tour exit. Browser work is session-memory-only and
sanitized-replica-only. It uses no durable browser storage and no external
operation.

### Atomic save, rollback, restore, and recovery

One explicit save must:

1. retain exact prior RP-008 raw bytes or verified absence;
2. snapshot exact RP-007, TD-006, TD-005, and TD-004 raw bytes;
3. sanitize the complete candidate in memory;
4. reject before writing unless every conjunct, key order, value, and
   allowlist passes;
5. write one canonical serialization only at the dedicated RP-008 key;
6. read raw bytes back exactly once;
7. parse, re-sanitize, and require exact canonical byte equality;
8. prove every predecessor byte string unchanged;
9. on any failure restore prior RP-008 bytes or absence;
10. read back and prove rollback plus predecessor equality; and
11. issue `HOLD` if rollback cannot be verified.

Failure and reload behavior is exact:

- Tour rejects before campaign storage access and exposes no route or SC-09.
- Invalid RP-007 exposes no TD-008 identity and uses released TD-007 recovery
  to exact IW-30/SC-07.
- Wrong group, owner, action, modality, or token remains at BV-30, consumes
  and writes nothing, and focuses the route heading.
- Missing, duplicate, stale, forged, combined, partial, private, or
  contaminated intent never spends a future valid token.
- Duplicate intent after OR-00 is a no-op at the current state.
- Interruption before consumption restores BV-30 and requires fresh intent.
- Interruption after OR-00 but before verified save clears RP-008 work,
  reconstructs BV-30, and requires fresh intent.
- Invalid or absent RP-008 save mounts no SC-09 restore, clears candidate
  work, preserves all predecessor bytes, and reconstructs BV-30.
- A contiguous finalized same-session pre-save prefix may select the first
  incomplete scored boundary, but all six physical observations must be
  deliberately re-observed and no raw/private work survives.
- Failed write returns to deterministic first incomplete only after verified
  rollback; unverified rollback is a hard `HOLD`.
- Only exact verified bytes restore OR-30, and restore replays nothing.

## Accessibility, responsive, and modality contract

- Seven equivalent modalities: pointer, touch, Enter, Space, switch, speech,
  and screen reader.
- Validate before one-hit consumption; keyboard repeat and multi-event bursts
  remain one hit.
- Exactly one owner/message/content/action group is active.
- Whole-group replacement focuses its heading; observation return focuses the
  unordered peer chooser; recovery announces one complete polite atomic
  System status and then focuses the first incomplete control.
- Persistent labels, native semantics, `aria-describedby`, `aria-invalid`,
  and complete help/error/result association are required.
- Every required target is at least `44 x 44 CSS px`.
- Meaning cannot depend on color, left/right, position, sequence, motion,
  sound, brightness, glow, layout, or material alone.
- Forced colors use system colors, explicit borders, and visible focus.
- Reduced motion uses registered stills or direct replacement and
  `scroll-behavior:auto` without semantic change.
- Grayscale preserves all six physical identities through geometry, material
  structure, and complete text.
- No time limit, speed score, forced animation, or sensory-only cue exists.

Required DPR-1 live matrix:

| Layout | Requirement |
|---|---|
| desktop | `1920 x 1080`; complete `100dvh` shell, world dominant, active group and actions visible, no outer scroll |
| laptop | `1366 x 768`; no horizontal escape; internal panel scroll, if needed, is evident and keyboard reachable |
| narrow | `390 x 844`; world above one natural interface column, no clipping or horizontal escape |
| effective 200% | `768 x 900` CSS viewport; natural reflow and complete labels, errors, status, and actions |

The full presentation matrix includes those layouts plus forced colors,
reduced motion, grayscale, focus, status, targets, crop registration,
alternative text, containment, local-only requests, runtime logs, and the
exact longest-copy sample. Product and fixture control counts remain separate.

## Closed storage-free fixture

Create exactly one fixture, `td008-offset-reach-v1`, absent from production.
Its versioned public allowlist contains exactly these 64 IDs and no others:

```text
route_pointer
route_touch
route_keyboard_enter
route_keyboard_space
route_switch
route_speech
route_screen_reader
route_invalid_predecessor
route_invalid_intent
route_duplicate_or_stale
route_private_contaminated
route_tour_isolated
or00_arrive
or10_inspect
or20_revise
or30_restore
observation_familiar_continuity_trace
observation_recurring_familiar_contact
observation_comparable_non_contact
observation_cross_family_contact
observation_unavailable_case
observation_layered_stewardship
python_primary_blank
python_primary_miss
python_primary_pass
python_trace_blank
python_trace_pass
python_transfer_blank
python_transfer_pass
ai_primary_blank
ai_primary_miss
ai_primary_pass
ai_retrieval_blank
ai_retrieval_pass
ai_transfer_blank
ai_transfer_pass
selection_explanation_blank
selection_explanation_pass
inference_explanation_blank
inference_explanation_pass
resume_contiguous_prefix
resume_first_gap
interruption_before_consumption
interruption_after_or00_pre_save
private_work_cleared
json_work_cleared
feedback_work_cleared
atomic_polite_status
review_ineligible
review_eligible
save_committed
save_rollback_verified
restore_replay_free
return_braided_verge
return_city_threshold
continuation_inert
layout_desktop
layout_laptop
layout_narrow
layout_effective_200
mode_forced_colors
mode_reduced_motion
mode_grayscale
longest_copy_contained
```

The fixed groups are 12 route, 4 checkpoint, 6 observation, 18 learning, 8
recovery/privacy, 8 review/save/return, and 8 presentation scenarios. The
launch manifest and scenario module must enumerate them exactly; tests reject
addition, omission, duplication, arbitrary JSON, URL/query/hash injection,
campaign adapters, browser storage, network, service worker, clipboard, file
picker, and Martin-owned state.

Harness controls/output remain separate from product controls/content. The
fixture uses frozen public memory only, makes no production import, and leaves
no marker, ID, scenario copy, style, or asset in the production bundle. A
bounded capture set covers world roles rather than all 720 permutations.

## Scene, assets, and visual canon

### Exactly two provisional runtime roles

1. `SC-09-PANORAMA-MASTER` - invariant panoramic Offset Reach identity.
2. `SC-09-RELATION-DETAIL-MASTER` - spatially registered relation detail
   containing all six evidence responsibilities in one coherent outcrop.

No other new runtime image, audio, font, video, source map, network payload,
Python/WASM runtime, or visual role is permitted. The two roles are planned,
not accepted media. Quartermaster must replace exactly two truthful structural
placeholders only after Combat Engineer has completed behavior.

Each role requires:

- a lossless 16:9 source, preferably generator-native `6144 x 3456` or
  larger and never below `3840 x 2160`;
- honest separate reporting of native dimensions and any deterministic
  enlargement;
- direct normal import and exact source/runtime byte and SHA-256 identities;
- complete prompt, model, mode, reference, attempt, selection, edit, cleanup,
  derivative, and approval provenance;
- registered responsive crops, all required semantic zones, exact
  alternative treatment, grayscale, forced-color, reduced-motion,
  thumbnail, full-resolution, and artifact-cleanup review; and
- explicit retirement of its matching placeholder.

The detail may be a spatially consistent registered view; if it is not a
literal panorama crop, provenance and player-facing claims must say so.
Derivative enlargement may not be described as native resolution.

### Fixed visual canon

- First-person, maximum-quality cinematic photorealism; no Pilot, hands,
  body, shadow, reflection, ship, occupant, portrait, or prior human trace.
- The familiar laminated ribbon and cellular mantle remain separately
  traceable from SC-08; a third refractory filament fan remains materially
  distinct.
- One familiar saddle contact recurs without central confirmation treatment.
- One comparable open passage shows both familiar structures at comparable
  exposure while preserving uncertainty about hidden depth.
- One comb-like filament-to-ribbon contact is comparable but not equivalent.
- One opaque accreted casing exposes only boundary, approach seams, and
  bypass; it has no door, reward, access affordance, interior, or portal.
- Foundation fabric, later additions, pair-specific interphases, sacrificial
  wear, casing/bypass, and present maintenance show compatible stewardship
  without one institution, hierarchy, author, rule, or purpose.
- World plates contain no baked UI, text, labels, arrows, node/link diagrams,
  columns, binary panels, checks/crosses, verdict, progress trail, answer key,
  causal cue, or prescribed order.
- Maintenance, light, sound, heat, deposition, and material clocks remain
  monotonic and never react, synchronize, separate, converge, pause, or grade.
- Replica coupling remains low, off-axis, and isolated; it is not a console,
  live path, comparison authority, route control, face, or eye.

Hard visual rejection includes human ergonomics, networks, flowcharts, Venn
diagrams, truth tables, graphs, laboratories, specimen displays, diptychs,
yes/no staging, readable pseudo-writing, road/rail/bridge/platform, portal,
door, monitor, keyboard, kiosk, altar, generic neon science fiction,
face-like forms, impossible interpenetration, copied composition, binary
contact/non-contact treatment, separation/failure styling, equivalence
styling, locked-reward styling, success reaction, or any RP-009 cue.

### Cycle reveal boundary

Image Specialist must archive exactly one accepted spoiler-safe reveal after
selecting one accepted visual or lore decision and checking it against recent
reveals. The reveal is reference-only unless a separately proven runtime role
already exists; it never counts as gameplay evidence.

Attempt budget is exactly one initial private generation. Only if it fails
documented Image QA before entering the workspace may Image make one targeted
recovery. A second failure is `HOLD`; no third attempt. Rejected bytes remain
outside workspace, canon, runtime, provenance package, commits, and the
user-facing reveal.

## Offline, Tour, authority, and invariant world

No account, sign-in, identity, credential, Azure or Foundry resource,
endpoint, SDK/API call, upload, live extraction, model, service, package,
file, network request, external content, or external action is required or
authorized. The learning cases are course-authored and offline. The result
claims no Microsoft authorship, official exam item, certification, service
truth, access, permission, or exam guarantee.

Tour isolates before campaign storage. It creates no route, token,
observation, learning, remediation, save, mastery, world, authority, or
successor state.

SC-08, SC-09, all geometry, continuities, contacts, passage, casing, bypass,
maintenance, light, sound, and clocks remain invariant. Observation,
execution, miss, retry, review, save, restore, return, and completion cannot
relight, join, separate, equate, repair, open, privilege, suppress, reroute,
or change them.

## Performance and budget

`PBA-TD008-v1` is executable authority. It is a one-time non-compounding
rebaseline from the exact TD-007 release, not inherited unused headroom.

| Measure | TD-008 cap |
|---|---:|
| aggregate JavaScript | `<=1,461,821` bytes |
| aggregate CSS | `<=100,351` bytes |
| production modules | `<=207` |
| new runtime media | at most `2` image-only assets / `<=8,388,608` bytes |
| total runtime media | `<=41,810,481` bytes |
| production build | `<=60s` |
| focused suite | `<=30s` |
| related suite | `<=60s` |
| full suite | `<=60s` |
| complete E2E | `<=180s` |
| sampled main-thread task | `<=100ms` |

Every emitted JS/CSS chunk and every media file counts. All 15 TD-007 media
hashes must remain. Build warnings do not waive the caps. Budget pressure
never permits weaker evidence, privacy, accessibility, recovery, visual
quality, offline behavior, or validation.

## Hard stop and forbidden exposure

The exact hard stop is `OR-30 VERIFY + RETURN / SC-09`, followed only by the
two exact write-free/replay-free returns and optional inert destinationless
notation.

No RP-009 name, location, scene, image, sound, event, route, token, cue,
objective, lesson, destination, successor-facing copy, or next action may
mount, render, announce, dispatch, persist, or receive focus.

The shell forbids universal or exclusive truth; separation; independence;
incompatibility; equivalence; interchangeability; unity; coordination;
communication; dependence; cause; ownership; identity; native category;
purpose; author; audience; unavailable contents; reward; access; permission;
authority; judgment; response; world effect; live read/join/route/open/
correct/separate/equate/control; RP-013; successor; and post-ending content.

The Machine, Builder disappearance, continuity mechanism, intended ending,
and every central mystery remain unresolved. The hidden-lore vault is neither
needed nor permitted.

## Marine ownership and freedoms

Every freedom below is subordinate to every fixed shell requirement.

### Reconnaissance Sergeant

May choose the emotional treatment, attention rhythm, restrained palette,
material emphasis, atmosphere, sound reuse, concept-level motion, reduced-
motion treatment, and contrast with recent slices/reveals. May not change the
route, graph, evidence, record, fixture, assets, canon meaning, or hard stop.

### Tactical Operations Specialist

May choose component hierarchy, compliant copy within source budgets, exact
panel arrangement, crop coordinates, and focusable grouping implementation.
Must freeze every group/action/focus/status/error transition, all 64 fixture
scenarios, and all four layouts. The named longest-copy sample is fixed.

### Combat Engineer

May choose pure helper/component factoring, deterministic serializer design,
test organization, style organization, and internal names not frozen here.
Must create distinct normal route/controller/storage/fixture identities,
preserve predecessor adapters, exclude the protected journey, and implement
the entire shell before `FUNCTIONALLY COMPLETE`.

### Quartermaster

May choose generation prompts, compliant production method, selection,
artifact cleanup, deterministic derivatives, file names, and final crops for
exactly the two approved runtime roles. May use at most two private attempts
per runtime role only under the downstream production rules and must report
native versus derived dimensions honestly. May not add a third role or alter
behavior, copy meaning, evidence, or world state.

### Image Specialist

May make bounded presentation-only corrections, tune crop/grade/contrast/
typography/spacing/focus visibility inside the shell, and select one reveal
subject that varies recent composition. Must independently inspect original
resolution, registration, grayscale, alternatives, four layouts, exact
longest copy, forced colors, reduced motion, focus/status/targets, containment,
and request/log hygiene. May not generate replacement runtime art or change
mechanics, evidence, canon, assets count, or hard stop.

### Intelligence Officer

Has no implementation freedom. Intelligence independently reproduces the
entire release ladder, compares shell to as-built evidence, classifies every
variance, accepts or returns the exact reveal, and alone issues `PASS`,
`REVISE`, or `HOLD` plus synchronized next action.

## Validation ladder

### Tier 1 - contract

- source hashes, exact predecessor/route predicate/order, entry/target/return
  states, owner/focus/status/alternative matrix, record keys/order/values,
  evidence dimensions, truthful execution copy, privacy denylist, rollback,
  fixture list, asset roles, PBA, and hard stop;
- protected journey and frozen curriculum remain unchanged and absent from
  production imports; and
- forbidden-boundary, protected-work, and later-marker scans.

### Tier 2 - focused

- strict RP-007 dependency and immutable TD-007 through TD-004 bytes;
- seven route modalities, validation-before-consumption, freshness, one hit,
  invalid/duplicate/stale/forged/combined/private/Tour/interruption/reload,
  and zero-effect OR-00;
- all 720 observation orders and idempotent revisits;
- exact PY-016 primary/trace/blank-transfer behavior and forbidden-operation
  probes;
- exact independent AI primary/retrieval/blank-transfer behavior, both
  explanations, misconception-tag-only remediation, and zero cross-credit;
- deterministic first-incomplete recovery, complete sanitation, strict
  sanitizer, atomic success/read-back/failure/rollback/rollback-unverified,
  replay-free restore, and both exact returns;
- scene resolver, direct asset roles, alternatives, focus/status/targets, and
  all 64 fixture scenarios.

### Tier 3 - related

- complete TD-004 through TD-007 normal/protected/record/scene/asset/route/
  return/evidence/restore/sanitation regression;
- RP-008 protected reference and curriculum self-test; and
- all protected journeys, fixture source, Node-only utilities, and later
  content absent from production.

### Tier 4 - full

- complete game suite and all applicable readiness validators;
- production build and `PBA-TD008-v1` baseline/candidate/release modes;
- exact served root, deep link, chunks, and all 15 predecessor plus two SC-09
  media byte/hash identities;
- fixture/source-map/private/later-marker exclusion, local-only requests,
  patch integrity, and QA restoration.

### Tier 5 - release

- build completes before one non-overlapping complete E2E;
- isolated coordinator-owned production preview and exact served identity;
- E2E gates true with no runtime errors;
- exact desktop/laptop/narrow/effective-200, forced-color, reduced-motion,
  grayscale, focus, status, target, crop, alternative, longest-copy,
  local-request, and log review;
- one named live scenario per image role and separate product/fixture counts;
- human full-resolution/crop/registration/invariance/artifact review;
- owned-process shutdown and restoration of incidental QA artifacts;
- shell-to-as-built reconciliation, variance classification, exact reveal
  validation, synchronized push, and
  `HEAD == origin/main == remote refs/heads/main`.

## Definition of done

1. Exact TD-007 through TD-004 bytes, BV-30 identity, SC-08 role, and both
   released returns remain unchanged.
2. `TD008-RTA-001` alone mounts one zero-evidence OR-00/SC-09 arrival.
3. Invalid, duplicate, stale, forged, combined, partial, private, Tour,
   interrupted, and reload paths fail closed without spending a valid token.
4. OR-00 through OR-30 work in order and all 720 observation orders converge.
5. Six observations remain equal and retain local truth, bounded recurrence,
   non-contact without separation, cross-family contact without equivalence,
   unavailable evidence, and layered stewardship.
6. PY-016 primary/trace/transfer pass independently with exact ten-key sorted
   round trip and five null limits.
7. AI-901 primary/retrieval/transfer and both explanations pass independently
   with actually-scored-tag-only answer-free blank recovery.
8. Scene, order, route, focus, modality, timing, Tour, presentation, save
   display, and accessibility grant no evidence or remediation signal.
9. Only the exact eleven-root-key, retained-summary, thirteen-note-key, and
   eight-record allowlist persists.
10. Every private/transient source, JSON, case, answer, reasoning, feedback,
    service, identity, and control field is rejected and cleared.
11. Atomic replacement, exact read-back, verified rollback, predecessor
    preservation, invalid recovery, and replay-free OR-30 restore pass.
12. Only Braided Verge and City Threshold returns exist; notation is inert.
13. Seven modalities, deterministic focus/status/errors, `>=44px` targets,
    forced colors, reduced motion, grayscale, four layouts, exact longest
    copy, and the closed 64-scenario fixture pass.
14. Offline/no-authority/no-exam-guarantee, Tour isolation, local-only
    requests, and invariant SC-08/SC-09 pass.
15. Both and only both SC-09 runtime roles are direct, provenance-complete,
    accessible, responsive, registered, artifact-clean, maximum quality, and
    exact in source/rendered/served/regression evidence.
16. PBA, suites, validators, served identity, E2E, live review, cleanup,
    patch, and synchronization gates pass.
17. Exactly one accepted reveal is archived and shown under the one-recovery,
    no-third-attempt rule.
18. No universal/exclusive truth, separation, equivalence, unity, cause,
    purpose, identity, reward, access, authority, response, RP-009 cue,
    RP-013, successor, post-ending content, or hidden lore appears.
19. Intelligence classifies every variance and issues `AS BUILT RELEASED`;
    until then TD-007 remains the released boundary.

## Variance protocol

No Marine may silently alter this shell. Record:

```text
Variance ID:
Discovering role:
Shell requirement:
Observed evidence:
Requested classification:
Proposed correction or bounded change:
Earliest owning return stage:
Canon / learning / privacy / accessibility / save / budget / world impact:
Validation needed:
```

Return product conflicts to Commandant; canon/world/mystery to Colonel;
campaign/route/checkpoint to Operations; systems/learning/evidence/privacy/
accessibility/save/offline/recovery/performance/budget to Science; shell
ambiguity to Mission; and presentation conflicts to the owning Marine.
Intelligence owns final classification.

## Protected files and systems

- Never open `DO_NOT_READ_HORIZON_ARCHIVE_HIDDEN_LORE_VAULT.md`.
- Never inspect, alter, stage, move, delete, or commit
  `Art Of No Mans Sky Book Scan.pdf` or `Simplilearn Training Files/`.
- Never inspect or mutate Martin's browser storage, campaign save, cookies,
  profile, or session to manufacture evidence.
- Use deterministic source/tests, the closed storage-free fixture, isolated
  agent-owned previews, and served bundles only.
- Preserve unrelated user work and archived two-team infrastructure.

## Shell change history

| Version | Change |
|---|---|
| `SS-RP008-OFFSET-REACH-v1` | Initial conflict-free Mission integration of GDB/WNMP/CFS/VE/PBA TD-008 authorities; freezes normal OR-00-OR-30 construction, retained RP-007 validity, independent JSON/information-extraction evidence, exact atomic record, 64-scenario fixture, two-role SC-09 plan, four-layout longest-copy gate, and OR-30 hard stop |

## Mission signature and disposition

**Mission Captain / `mission_captain`**

**`SHELL READY - SS-RP008-OFFSET-REACH-v1`**

The shell is conflict-free, integration-sized, measurable, and bounded by
existing surface authority. No Marine creative, implementation, content, or
image work was performed in this stage.

## Exact Reconnaissance Sergeant handoff

Read `AGENTS.md`, `NEXT_INSTANCE_HANDOFF.md`,
`SKYSCRAPER_AGENT_WORKFLOW.md`, the registry,
`Skyscraper Agent Profiles/reconnaissance-sergeant.md`, and this shell in
full. Then read the shell-cited TD-008 product/world/operations/science
authorities, the TD-007 Intelligence release, SC-09 brief, photorealistic
charter, and current visual/reveal controls.

Create one bounded
`TD-008/06-PLAYABLE-SLICE-CREATIVE-TREATMENT.md` for exact shell
`SS-RP008-OFFSET-REACH-v1`. Define:

- one-sentence emotional promise and player fantasy;
- arrival, tension, understanding, and exit emotions;
- attention hierarchy and `35-48` clean / `21-30` mastered pacing;
- invariant-world, retained-summary, Builder-work, memory-only JSON, neutral
  course-interface, and no-cross-credit relationships;
- concept-level materials, light, atmosphere, existing-audio use, motion,
  and reduced-motion direction;
- contrast with SC-07/SC-08 and recent reveal compositions; and
- fixed invariants, flexible staging, risks, and any formal variance request.

Preserve exact `TD008-RTA-001`, OR-00-OR-30, state/alternative/focus/status
matrix, six equal peers and all 720 orders, strict independent learning and
explanations, exact eleven-key record, privacy clearing, atomic recovery,
seven modalities, four layouts, the exact longest-copy sample, closed
64-scenario fixture, exactly two provisional image roles, PBA-TD008-v1,
offline/Tour/invariant-world behavior, exact returns, reveal accounting, and
the OR-30 hard stop.

Do not imply universality, exclusivity, separation, equivalence, unity,
coordination, cause, purpose, identity, unavailable contents, reward, access,
authority, response, RP-009, or successor content. Issue `CREATIVE LOCK`,
`REVISE`, or `HOLD`, update metrics/handoff, make one dedicated Recon commit,
and hand exactly to Tactical Operations Specialist.
