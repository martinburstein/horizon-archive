# TD-002 Playable Slice Shell

## Document control

| Field | Value |
|---|---|
| Stage | Mission Captain |
| Agent ID | `mission_captain` |
| Shell ID | `SS-RP003-IE01` |
| Version | `v1` |
| Full contract identity | `SS-RP003-IE01-v1` |
| Test drive | `TD-002` |
| Campaign position | `RP-003 / SC-04 / CM-30-CM-34` |
| Product brief | `GDB-TD002-v1` |
| World baseline | `WNMP-TD002-v1` |
| Floor stack | `CFS-TD002-v1` |
| Viability envelope | `VE-TD002-v1` |
| Accepted predecessor release | `TD-001 / SS-RP003-PY010-v1 / 20ac6c505208fb97ccd5440318581cc7a101f026` |
| Pre-shell source commit | `c7f1d46427d837b5842372ad36628d7e0da89a0d` |
| Branch | `main` |
| Disposition | `SHELL READY` |

This document is the single construction contract for the TD-002 Marine field
unit. The four TD-002 Colonel authorities reconcile without an unresolved
contradiction, omission, duplicated owner, or untestable requirement. A Marine
may make only the choices explicitly left flexible below. Any mismatch with a
fixed boundary uses the variance procedure in this shell.

The shell contains no private learner answer material. Frozen case prompts,
expected values, evaluator truth, and exact explanation value remain in their
existing curriculum and evaluator authorities and are not restated here.

## Change history

| Version | Change | Authority | Result |
|---|---|---|---|
| `v1` | First issue: integrates the guarded product/world deltas, contiguous extraction floor, and viability envelope into one bounded normal-route information-extraction slice | `GDB-TD002-v1`, `WNMP-TD002-v1`, `CFS-TD002-v1`, `VE-TD002-v1` | `SHELL READY` |

Any change to a fixed field creates a successor shell version after return to
the named owner. Interpretation inside a stated Marine freedom does not change
the shell version.

## Current source and build identity

### Accepted source boundary

- The accepted release is `TD-001`, commit
  `20ac6c505208fb97ccd5440318581cc7a101f026`.
- Normal RP-003 play ends at exact no-action `PY010-P3`.
- The TD-001 controller/checkpoint chain finalizes only `PY-010` after the
  accepted three-observation gate, strict primary, retrieval, and transfer.
- `PY010-P3` exposes no extraction, review/save, route, authority, or world
  action in the accepted release.
- The normal runtime has no `CM-30-CM-34` controller, checkpoint, renderer, or
  production review fixture.
- The protected reference journey contains the frozen
  `RP003-IE-01` evaluator, remediation projection, evidence shape, cases, and
  explanation authority, but the complete protected journey is not normal
  route authority and may not be dispatched from normal play.
- The existing Python checkpoint and all three observation decisions are
  predecessor evidence only. They must remain byte-stable and cannot become IE
  evidence.

### Pre-shell source verification

The runtime and curriculum are unchanged between the accepted TD-001 release
and the pre-shell source commit:

```text
git diff --name-only
  20ac6c505208fb97ccd5440318581cc7a101f026
  c7f1d46427d837b5842372ad36628d7e0da89a0d
  -- horizon-archive-game curriculum

result: empty
```

The four intervening TD-002 commits are strategic documents only. The current
implementation therefore matches the predecessor claimed by all four Colonel
artifacts.

### Accepted release measurements

These are inherited independent TD-001 measurements, not a claim that the
Mission Captain rebuilt the game:

| Measure | Accepted identity |
|---|---|
| Production modules | `173` |
| JavaScript | `index-BHYcpGmM.js`, `1,138,689` raw bytes, SHA-256 `7D580F7BC58C7E7075D0851E85917F68B7408C94770F7693E602079DA7C0C41A` |
| CSS | `index-BPUl5qmV.css`, `77,814` raw bytes, SHA-256 `0E099AABEC927D829AEFCF81CC7304A4A65771805BE0B9D74897EE5A1BA30BF3` |
| World plate | `2,626,795` bytes, SHA-256 `1D727694FA1DBB4311F9D7974A017D6165E66F10080114E5F81FE3CFD44EFF6D` |
| Full release evidence | `737/737`, `15/15` validator self-tests, production build, served identity, complete `101.06s` E2E |

The Combat Engineer establishes the TD-002 candidate identity at
`FUNCTIONALLY COMPLETE`. The Intelligence Officer independently re-establishes
it at release.

## Colonel reconciliation

### Sources reconciled

1. `GDB-TD002-v1` fixes the unchanged product promise, exact predecessor,
   bounded extraction purpose, quality order, measurable success, and hard
   stop.
2. `WNMP-TD002-v1` fixes SC-04 identity, human expedition ownership, the
   emotional envelope, unresolved mysteries, and invariant-world meaning.
3. `CFS-TD002-v1` fixes the contiguous `CM-30-CM-34` floor, phase order,
   checkpoints, permitted returns, first-incomplete recovery, pacing, and
   no-action landing.
4. `VE-TD002-v1` fixes the frozen evaluator/evidence contract, exact prefix
   allowlist, atomicity, privacy, accessibility, offline, performance,
   storage-free fixture, diagnostics, and validation ladder.

### Conflict audit

| Audit | Result | Resolution |
|---|---|---|
| Product promise versus story meaning | PASS | Human capability may increase; SC-04, the Machine, the Builders, and every world process remain unchanged and uncategorized. |
| Accepted no-action predecessor versus new entry | PASS | TD-001 `PY010-P3` remains byte-stable. TD-002 may expose exactly one new fresh Pilot activation only after the complete accepted predecessor conjunction is revalidated. |
| Python/observations versus IE evidence | PASS | They are prerequisite evidence only. The new adapter stores only exact IE records and never copies or rewrites predecessor records. |
| Campaign order versus evidence order | PASS | `CM-30 -> CM-32 -> CM-33 -> CM-34`; `CM-31` occurs only after an actual miss and returns to the same blank form. |
| Zero-credit interlude versus resume | PASS | `IE-P1` reconstructs `CM-32`; no durable interlude marker exists. A fresh continuation mounts blank retrieval in memory. |
| Transfer versus explanation atomicity | PASS | `IE-P2 -> IE-P3` appends transfer and explanation records together or not at all. |
| Deterministic recovery versus privacy | PASS | Only strict finalized prefixes persist. Active/failed work, values, answers, prose, feedback, tokens, and focus state never persist. |
| Internal checkpoint versus expedition-note save | PASS | `IE-P0-P3` are sanitized local learning prefixes only. They create no save eligibility, note, restore action, or bearing. |
| Protected evaluator reuse versus protected journey exposure | PASS | Frozen pure evaluator truth may be invoked or extracted with byte-equivalent proof; the complete protected smoke journey, save adapter, later floors, and answers remain non-routable. |
| CSS headroom versus unchanged quality | PASS | The cap is the same `+5%` per-shell measured policy recalculated once from the accepted TD-001 release, while every visual/accessibility gate remains mandatory. It is not a permanent rolling entitlement. |
| Gated visual evidence versus user storage | PASS | The fixture is test-only, closed-scenario, storage-free, outside every production import graph, and must be absent from `dist`. |
| Temporary SC-04 plate versus reveal | PASS | The accepted plate remains a classified limitation. No new runtime media is required; the cycle reveal remains separate reference production. |
| Ownership | PASS | Every fixed family has one primary owner and one acceptance method below. |

### CSS-cap decision

`VE-TD002-v1` uses the independently released CSS size, `77,814` bytes, as
the evidence baseline and retains the same `+5%` policy:

```text
77,814 * 1.05 = 81,704.7
integer raw-byte ceiling = 81,705
available TD-002 headroom = 3,891 bytes
```

The `81,705`-byte cap is therefore evidence-backed and does not weaken the
quality gate:

- the allowed percentage is unchanged;
- the baseline is a released candidate, not an aspirational or pre-build size;
- responsive, focus, forced-color, reduced-motion, blankness, and cinematic
  requirements remain pass/fail requirements independent of byte count;
- reuse and refactoring remain preferred;
- hiding overage, deleting required behavior, or shifting bytes into a runtime
  asset is forbidden; and
- this shell does not authorize a future automatic baseline reset. A later
  shell requires a new Science measurement and Mission decision.

### Fixture production-exclusion decision

The fixture is construction work, not a production feature. It is authorized
only when all of these are true:

- all fixture source and host files live outside `src`;
- App, main, production routes, and production imports contain no fixture
  import, marker, switch, query hook, or scenario injection;
- production `npm run build` produces no fixture path, marker, scenario ID,
  or test host in `dist`;
- the fixture uses only closed named scenarios and public controller/component
  interfaces;
- the fixture never reads or writes browser/campaign storage; and
- the fixture is launched only by an explicit test/review command on loopback
  with an owned process and cleanup.

Failure of any condition is `REVISE`, not an accepted production backdoor.

No issue requires return to an earlier Colonel. No hidden lore, external
authority, unavailable source evidence, or Martin product decision is needed.

## Player-facing purpose and emotional promise

From the accepted no-action Python landing, the player deliberately begins one
fresh expedition-owned information-extraction check. They distinguish supplied
inputs, accountable structured output/provenance, and unsupported values;
recover from an actual miss without receiving an answer; retain the boundary
after a zero-credit delay; and transfer it to a distinct missing-input case
before finalizing only `RP003-IE-01`.

The emotional movement is:

```text
renewed purpose
  -> productive uncertainty
  -> precise humility when needed
  -> quiet reorientation
  -> durable discipline
  -> restraint under missing evidence
  -> earned epistemic responsibility
```

The player becomes more reliable. The City, Machine, Builders, sealed source,
coupling, route, light, material, sound source, maintenance process, and
independent environmental clocks do not react.

## Exact entry contract

### Required predecessor conjunction

Normal entry is permitted only when all of these are exact:

- mode is ordinary campaign, never Demo Tour;
- packet is `RP-003`, board is `SC-04`, and the accepted source phase is
  `PY010-P3`;
- the source controller/checkpoint is the sanitized TD-001 P3 form under
  `SS-RP003-PY010-v1`;
- `PY-010` is finalized through its strict ordered primary, retrieval, and
  transfer prefix;
- the three approved observations remain the exact prerequisite conjunction
  proven by the accepted TD-001 entry route;
- there is no `RP003-IE-01` record or IE prefix beyond exact empty `IE-P0`;
- Python evidence and observation authority are byte-stable;
- `continuation=continuation`;
- `cityStateDelta`, `worldStateDelta`, and `accessStateDelta` are `null`;
- `successor=null`;
- private/transient work is cleared;
- no Tour, stale, partial, combined, contaminated, private-bearing, malformed,
  forged, replayed, asserted-later, review/save, route, authority, external
  action, or world-change state exists; and
- the TD-001 landing has not automatically spent or synthesized an extraction
  intent.

The accepted P3 route provenance is the observation prerequisite proof. The IE
adapter may not invent a second copy of observation records or rewrite the
Python checkpoint to make that proof more convenient.

### Fresh entry intent

After the conjunction passes, the existing System-owned finalized landing may
expose exactly one control whose semantic action ID is
`BEGIN_EXTRACTION`. The action is explicitly framed as a fresh Pilot choice;
its presence is not a System, Scene, Builder, City, or Machine invitation.

The semantic intent contains exactly:

- `shellVersion=SS-RP003-IE01-v1`;
- `packetId=RP-003`;
- current active group ID;
- exact expected owner;
- allowlisted action ID;
- one of the seven supported modalities; and
- one opaque fresh event token.

Validation occurs before token consumption. Wrong shell, packet, group, owner,
action, modality, token shape, duplicate token, Tour mode, missing predecessor,
malformed prefix, private extra key, stale group, passive visibility, focus,
timing, key repeat, or later-state assertion rejects without spending a future
valid token.

One accepted P0 intent mounts only a genuinely blank `CM-30` primary. With a
valid retained P1/P2 prefix, the same fresh semantic activation reconstructs
the exact first incomplete boundary rather than replaying completed evidence.

## Exact state, action, ownership, and evidence contract

### State graph

```text
accepted no-action PY010-P3 + exact IE-P0
  -> fresh valid BEGIN_EXTRACTION
  -> CM-30 / ie_primary / wholly blank P01
       -> incomplete
            -> textual required errors only; token unspent
       -> actual miss
            -> clear every submitted/private value and result
            -> CM-31 / ie_primary_repair / actual failed IDs only
            -> fresh RETRY_BLANK_EXTRACTION_PRIMARY
            -> CM-30 wholly blank P01
       -> simultaneous current-attempt strict 3/3
            -> atomic primary record
            -> IE-P1
            -> CM-32 / ie_interlude / zero evidence

CM-32
  -> fresh CONTINUE_TO_EXTRACTION_RETRIEVAL
  -> CM-33 / ie_retrieval / wholly blank R01
       -> incomplete
            -> textual required errors only; token unspent
       -> actual miss
            -> clear every submitted/private value and result
            -> CM-31 / ie_retrieval_repair / actual failed IDs only
            -> fresh RETRY_BLANK_EXTRACTION_RETRIEVAL
            -> CM-33 wholly blank R01 with no P01 answers present
       -> simultaneous current-attempt strict 3/3
            -> atomic retrieval record
            -> IE-P2
            -> CM-34 / ie_transfer / wholly blank T01 plus blank explanation

CM-34
  -> incomplete
       -> textual required errors only; token unspent
  -> any failed dimension and/or failed explanation
       -> clear every submitted/private value and result
       -> CM-31 / ie_transfer_repair
          / actual failed dimension IDs and/or explanation boundary only
       -> fresh RETRY_BLANK_EXTRACTION_TRANSFER
       -> CM-34 wholly blank T01 plus blank explanation
  -> simultaneous current-attempt strict 3/3
       AND separately valid unsupported-input explanation
       -> atomically append transfer + explanation records
       -> IE-P3
       -> finalize only RP003-IE-01
       -> ie_finalized no-action landing
```

No transition may skip the primary, interlude, retrieval, transfer, or
explanation; combine attempts; retain partial correctness; compensate one
dimension with another; prefill a later form; treat P1/P2 as mastery; or
finalize from anything except exact atomic P3.

### Canonical groups and actions

Player-facing wording is a Marine freedom. The semantic structure is fixed.

| Boundary | Active group | Owner | Editable fields | Exact semantic actions |
|---|---|---|---|---|
| Accepted `PY010-P3` | `python_finalized` | `SYSTEM` | none | `BEGIN_EXTRACTION` only after the full conjunction passes |
| `CM-30` | `ie_primary` | `PILOT` | `input_boundary`, `output_contract`, `unsupported_rule` | `SUBMIT_EXTRACTION_PRIMARY`, `CLEAR_EXTRACTION_WORK`, `RETURN_TO_PYTHON_LANDING` |
| `CM-31` primary repair | `ie_primary_repair` | `TEACHER` | none | `RETRY_BLANK_EXTRACTION_PRIMARY`, `RETURN_TO_PYTHON_LANDING` |
| `CM-32` | `ie_interlude` | `SYSTEM` | none | `CONTINUE_TO_EXTRACTION_RETRIEVAL`, `RETURN_TO_PYTHON_LANDING` |
| `CM-33` | `ie_retrieval` | `PILOT` | `input_boundary`, `output_contract`, `unsupported_rule` | `SUBMIT_EXTRACTION_RETRIEVAL`, `CLEAR_EXTRACTION_WORK` |
| `CM-31` retrieval repair | `ie_retrieval_repair` | `TEACHER` | none | `RETRY_BLANK_EXTRACTION_RETRIEVAL` |
| `CM-34` | `ie_transfer` | `PILOT` | the three fixed dimensions plus `unsupported_explanation` | `SUBMIT_EXTRACTION_TRANSFER`, `CLEAR_EXTRACTION_WORK` |
| `CM-31` transfer repair | `ie_transfer_repair` | `TEACHER` | none | `RETRY_BLANK_EXTRACTION_TRANSFER` |
| Final `IE-P3` | `ie_finalized` | `SYSTEM` | none | none |

At any moment exactly one owner/message/content/action group is mounted and
operable in visual, DOM, pointer, keyboard, and accessibility order. Replaced
groups unmount or become fully inert. The P3 entry control must identify its
activation as the Pilot's choice without implying that the System or world
requested the lesson.

### Fixed learning contract

- Objective: existing `AI901-D2-O7`.
- Check ID: existing `RP003-IE-01`.
- Primary: frozen P01 and simultaneous current-attempt strict `3/3`.
- Retrieval: frozen delayed closed-note R01 and simultaneous current-attempt
  strict `3/3`, with no P01 answer set present.
- Transfer: frozen distinct audio/video missing-input T01 and simultaneous
  current-attempt strict `3/3`.
- Transfer explanation: a separate valid unsupported-input boundary.
- Dimensions: `input_boundary`, `output_contract`, `unsupported_rule`.
- Retry: unlimited, untimed, answer-free, and wholly blank.
- The controller imports or invokes the single frozen evaluator truth. It may
  not duplicate expected values in a second hand-maintained evaluator.
- The case prompts, expected values, thresholds, misconception tags,
  explanation value, and remediation map remain unchanged in their existing
  authorities.

### Actual-miss repair

- Only dimensions that evaluated false may appear in repair.
- If all three transfer dimensions pass but the explanation fails, only the
  explanation boundary appears.
- If both fail, the stable ordered union of actual failures appears once.
- Repair contains a human-authored contrast or question, never an expected
  value, completed field, correct option, source response, or inferred answer.
- Submitted values, prose, evaluator input, and evaluator result objects clear
  before the repair group mounts.
- Repair state stores only failed IDs and answer-free display metadata.
- Retry reconstructs every field in the same form as a native empty value.
- Attempt count, hint level, repair, retry, focus, timing, confidence, status,
  and interlude grant zero evidence.

### Evidence firewall

None of these may prefill, satisfy, compensate for, or cross-credit an IE
dimension or explanation:

- `PY-010` evidence, Python code/output, or Python checkpoint status;
- any observation or observation order;
- navigation, entry, return, continue, reload, resume, focus, timing,
  confidence, modality, color, motion, audio, layout, status, or Tour;
- partial/failed IE attempts, repair, hints, diagnostics, or interlude;
- review/save status, route state, screenshots, release tests, or presentation;
  or
- hidden/later evidence.

Conversely, IE evidence cannot change or refinalize Python evidence,
observations, review/save eligibility, route, destination, authority, or world
state.

## IE-P0 through IE-P3 checkpoint contract

`IE-P0-P3` are sanitized local learning prefixes, not expedition-note saves,
rewards, permissions, player-visible scores, or additional skill IDs.

| Prefix | Exact meaning | Exact ordered IE evidence | Reconstructed boundary |
|---|---|---|---|
| `IE-P0` | Exact accepted `PY010-P3`; no IE form finalized | none | no-action Python landing; fresh `BEGIN_EXTRACTION` required |
| `IE-P1` | Strict P01 primary finalized | primary | zero-credit `CM-32`; fresh continue required |
| `IE-P2` | Strict P01 primary and R01 retrieval finalized | primary, retrieval | wholly blank `CM-34` transfer plus blank explanation |
| `IE-P3` | Strict T01 transfer and separate explanation finalized atomically | primary, retrieval, transfer, unsupported explanation | finalized-`RP003-IE-01` no-action landing |

### No durable interlude marker

There is no durable `CM-32` continue marker. `IE-P1` reconstructs the
interlude. A valid fresh continue mounts blank retrieval in memory. Reload
before P2 returns to the interlude and requires another fresh continue. This
repeats no evidence, answer, world event, or credit.

### Exact checkpoint envelope

The extraction checkpoint contains only:

- `version`;
- `shellVersion=SS-RP003-IE01-v1`;
- `packetId=RP-003`;
- `mappingId=RP003-A3-CALIBRATION-MARGIN`;
- `checkpoint` in `IE-P0|IE-P1|IE-P2|IE-P3`;
- `continuation=continuation`;
- `cityStateDelta=null`;
- `worldStateDelta=null`;
- `accessStateDelta=null`;
- `successor=null`; and
- `evidence`.

Each evidence record contains only the existing contract keys:

- `packet_id`;
- `mapping_id`;
- `form`;
- `skill_or_objective_id=RP003-IE-01`;
- exact all-true allowlisted `dimension_correctness`;
- integer `attempt_count` clamped to `1-99`;
- integer `hint_level` clamped to `0-3`;
- `confidence` in `null|low|medium|high`;
- allowlisted `misconception_tags` (v1 may use the stricter empty array); and
- `mastery_status=mastered`.

The explanation record uses `form=unsupported_explanation` and the existing
single explanation correctness dimension. Its expected learner answer is not
stored in the shell, renderer, checkpoint, diagnostics, or fixture.

Unknown keys; false/partial dimensions; wrong IDs/forms; duplicate,
out-of-order, or extra records; transfer without explanation; explanation
without transfer; Python/observation evidence; private work; review/save/note/
bearing/route content; or later-state content rejects the whole candidate.

### Atomicity

- Sanitize the accepted predecessor, current exact IE prefix, and candidate
  record set before any write.
- One success atomically replaces only the previous IE prefix with the next
  exact prefix.
- P2-to-P3 transfer and explanation records commit together or not at all.
- Rejection, exception, false commit, malformed candidate, or duplicate leaves
  the previous exact prefix byte-for-byte stable.
- Failure never downgrades valid P1/P2, upgrades mastery, or changes Python or
  observation evidence.
- P0 need not create a storage write merely to represent empty IE evidence.
- If a distinct browser key is used, its sole allowed purpose is the sanitized
  extraction prefix; it may not share or mutate the campaign save or Python
  checkpoint.

## Permitted returns, exits, resume, and recovery

### Permitted outputs

The slice may produce only:

1. a wholly blank primary retry after bounded answer-free primary repair;
2. a wholly blank retrieval retry after bounded answer-free retrieval repair;
3. a wholly blank transfer/explanation retry after bounded answer-free repair;
4. a write-free, private-cleared return from `CM-30`, primary repair, or
   `CM-32` to accepted `PY010-P3`;
5. an exact sanitized P1/P2 prefix sufficient only to reconstruct the required
   first incomplete boundary;
6. fail-closed recovery to P0/Python landing or the last valid prefix; or
7. `IE-P3`, a no-action landing with only `RP003-IE-01` newly finalized.

There is no new late-chain return from `CM-33` or `CM-34`. Those phases may
clear work, enter actual-miss repair, retry their own blank form, or recover
from the last exact prefix.

Returning from P1 may preserve only exact P1. Re-entry requires a new valid
semantic activation and reconstructs `CM-32`; it does not replay primary.

### Resume and sanitation

| Sanitized condition | Recovery target | Initial focus |
|---|---|---|
| Exact predecessor + `IE-P0` | no-action Python landing; fresh entry required | landing heading, then entry action |
| Exact `IE-P1` | zero-credit `CM-32` | interlude heading, then continue |
| Exact `IE-P2` | wholly blank `CM-34` | transfer heading, then first dimension |
| Exact `IE-P3` | finalized no-action IE landing | landing heading |
| Missing/malformed/private/non-prefix IE state with exact predecessor | clear IE work and downgrade to `IE-P0` | Python landing recovery heading, then fresh entry |
| Invalid predecessor | fail closed to the existing accepted Python recovery owner | existing first-incomplete target |

Active partial or failed work never resumes. Return, replacement, reload,
malformed recovery, and finalization clear private/transient work. Recovery
emits only bounded nonprivate reason IDs and never replays an evidence event,
answer, status spectacle, world event, or external action.

### No-save boundary

This shell performs no expedition-note save, save-eligibility computation,
save confirmation, save failure, verified note restore, onward bearing, or
destination selection. Internal strict learning prefixes do not represent or
imply any of those features.

## Privacy and diagnostic contract

### Always private/transient

The following remain memory-only and clear on evaluated miss, repair entry,
retry, clear, return, group replacement, reload sanitation, malformed-state
recovery, checkpoint failure, and P3 finalization:

- P01/R01/T01 selections and native field values;
- explanation prose or selection;
- raw prompt, case, source, image, audio, or video material when mutable;
- evaluator inputs/results, partial correctness, submitted and expected values;
- failed submitted values and answer-bearing feedback;
- local hint/guide text beyond allowlisted numeric metadata;
- event tokens, handled-token sets, focus target/history, timing, and live
  region history;
- Tour work; and
- identity, credentials, endpoints, payloads, responses, service data, private
  notes, sealed content, or external-action requests.

These values never enter browser/campaign storage, URL/query state, analytics,
logs, production diagnostics, QA captures, screenshots, release reports, or
the storage-free fixture.

### Allowed diagnostics

Diagnostics may contain only bounded reason IDs such as:

- `exact_python_p3_required`;
- `fresh_extraction_intent_required`;
- `tour_extraction_closed`;
- `intent_rejected`;
- `one_hit_only`;
- `primary_remediation_required`;
- `retrieval_remediation_required`;
- `transfer_remediation_required`;
- `unsupported_explanation_required`;
- `checkpoint_write_rejected`;
- `resume_sanitized_to_ie_p0`; and
- `later_state_closed`.

They may include actual failed dimension IDs. They may never include submitted
or expected values, explanation text, case/source/media content, event tokens,
focus history, identity, credentials, endpoints, payloads, responses, or
sealed content.

## Accessibility, input, focus, and responsive contract

### Seven semantic modalities

Pointer, touch, keyboard Enter, keyboard Space, switch, speech, and screen
reader activation converge on one semantic dispatch and identical state/
evidence result. Native form/button behavior is preferred. Modality cannot
alter scoring, repair, checkpoint order, evidence, focus destination, or
one-hit semantics.

### Blankness

Primary, every post-repair retry, retrieval, and transfer/explanation are
empty in:

- visible value;
- native control value/default;
- component/controller state;
- programmatic value;
- accessibility name, description, and value;
- placeholder and option default;
- data attributes;
- serialized checkpoint/controller state; and
- focus/live-region history.

Labels may identify a dimension but may not expose an expected value. The
renderer may not import expected-answer exports.

### One active group and focus

- Exactly one active owner group is mounted and operable.
- Fresh CM-30 entry focuses its complete owner/heading, then exposes the first
  blank dimension in source order.
- Incomplete submit associates textual `required` feedback with each empty
  field and focuses the first invalid field without consuming the token.
- Actual miss clears submitted work, then focuses the repair heading or first
  failed-ID explanation.
- Retry focuses the first field of the wholly blank owning form.
- Pass focuses the complete next-group heading.
- CM-32 entry/resume focuses its heading, then continue.
- Final P3 focuses the no-action landing heading.
- Bounded return restores the exact Python landing heading/action.
- Focus, history, timing, and announcement history are transient and zero
  credit.

Every input has a persistent visible label, unique native/programmatic name,
required state, and associated help/error text. One polite atomic status
region reports current nonprivate status without stale or duplicate
announcements.

### Targets and reflow

- Every required action/control is at least `44 x 44 CSS px`.
- At representative `1920 x 1080`, the dominant SC-04 world and complete
  current required group/actions fit the dynamic viewport without outer
  horizontal or vertical scroll.
- At narrow width and effective `200%` text zoom, the world precedes one
  natural source-ordered interface column.
- Narrow/zoom may scroll vertically but has no page-level horizontal escape,
  clipping, overlap, or inaccessible action.
- Long labels, errors, provenance/unsupported copy, and wrapped controls keep
  semantic/source order.
- The three dimensions carry equal semantic weight; layout is never an answer
  key.
- Unavailable input is textual evidence, never a disabled/denied permission
  cue.

### Non-sensory parity

- Blank, required, failed, repaired, finalized, unavailable, owner, and
  disabled states are explicit in text and semantics.
- Meaning never depends on color, position, sound, glow, or animation alone.
- Forced colors retain boundaries, native states, visible focus, and error
  association.
- Reduced motion uses direct group replacement with identical status, focus,
  order, and evidence.
- Audio is optional and zero credit; all meaning has a silent equivalent.
- No timer exists.

The fixture may provide deterministic rendered-state evidence. It does not by
itself prove human screen-reader speech order, physical switch hardware, or
platform-specific forced-color rendering.

## Performance, offline, and invariant-world contract

### TD-002 production budgets

| Measure | Acceptance |
|---|---|
| New runtime media/font/network payload | `0` bytes |
| Raw JavaScript | `<=1,195,624` bytes |
| Raw CSS | `<=81,705` bytes |
| Production modules | `<=182` without a documented factoring variance |
| Production build | `<=60s` on the existing gate host |
| Focused extraction controller/route/UI suite | `<=30s` |
| Protected RP-003 reference smoke | `<300s` |
| Complete isolated E2E | `<=180s`, never overlapping build |
| Sampled local evaluate/dispatch | no main-thread task over `100ms` |
| Served identity | root/assets HTTP `200` and byte-identical to candidate `dist` |

An overage is a variance. It cannot be hidden by changing the baseline,
dropping required accessibility/presentation behavior, excluding valid bytes
from measurement, or moving payload into a runtime asset.

### Offline and no-authority

The slice is a course-authored local simulation. It uses no fetch, network,
service-worker fetch path, SDK, REST/CLI, Azure/Foundry call, Content
Understanding submission, account, authentication, credential, resource,
endpoint, external file/process, payload, response, or live analyzer.

It grants no official exam item, exam credit, exam guarantee, certification,
Microsoft authority, service configuration proof, permission, consent,
authentication, access, recognition, identity, reward, refusal, denial,
external action, or automated decision. Demo Tour remains isolated and zero
credit.

### Canon and world invariants

- `Calibration Margin` remains a provisional expedition term.
- SC-04 remains a civilization-scale working cross-section, not a classroom,
  lab, shrine, factory floor, kiosk, screen, door, or human workstation.
- The sealed source remains unavailable and unread.
- Unavailable is not false, negative, empty, absent, unequal, refused, denied,
  failed, or permission to infer.
- Course cases and media are expedition learning material, not city, Builder,
  Machine, or sealed-source evidence.
- The Machine, Builders, sealed source, Python correspondence, local purpose,
  and intended ending remain unresolved.
- No geometry, crop, route, material, light, sound source, maintenance,
  coupling, seal, clock, city process, or world state changes.
- Independent environmental clocks remain unsynchronized.
- `continuation` stays unchanged.
- `cityStateDelta`, `worldStateDelta`, and `accessStateDelta` remain `null`.
- `successor` remains `null`.
- The inherited SC-04 plate remains a temporary runtime limitation. It is not
  `SC-04-MASTER`.
- Human-readable learning UI remains a separate expedition layer.
- The cycle reveal is separate canonical reference production and does not
  become a runtime asset without a later accepted shell.

## Storage-free gated-review fixture

### Required scenarios

The fixture uses a closed named scenario allowlist and exposes at least:

- `blank-primary`;
- one repair state for each of the three dimensions;
- `interlude`;
- `blank-retrieval`;
- `blank-transfer`;
- `explanation-only-repair`; and
- `finalized-p3`.

It accepts no arbitrary JSON, checkpoint, URL/query state, browser seed, or
developer-entered record.

### Construction boundary

- All fixture source lives under
  `horizon-archive-game/review-fixtures/td002-extraction/` or an equivalently
  explicit test-only directory outside `src`.
- It imports the public production controller/component and creates states
  only through exact public constructors, semantic intents, and sanitized
  checkpoint adapters.
- It may use closed test-only invalid inputs to reach a repair state, but no
  submitted or expected value appears in rendered output, logs, URLs, captures,
  or provenance.
- It never reads/writes `localStorage`, `sessionStorage`, IndexedDB, cookies,
  campaign adapters, Martin's save, URL/query state, or signed-in browser data.
- It creates no production action, route, debug menu, query flag, storage seed,
  or arbitrary checkpoint injection seam.
- An explicit test/review script may be added only to start this separate
  loopback host. The production build command and entry remain unchanged.
- The host binds to loopback only, records its owned PID/liveness, and is
  stopped by the owning validator.
- Import-graph and production-bundle scans prove fixture code, scenario IDs,
  test host markers, and review entry are absent from `dist`.

If the fixture cannot meet every condition, the discovering role returns
`REVISE`. It may not manipulate Martin's campaign save as a fallback.

## Exact permitted systems, assets, and files

### Read/reuse authorities

- `curriculum/readiness/RP-003/contract.json`;
- the existing pure information-extraction evaluator, unsupported-explanation
  authority, failed-ID projection, and evidence record shape in
  `CalibrationMarginProtectedJourney.js`;
- accepted TD-001 Python checkpoint/controller/normal composition patterns;
- `CalibrationMarginNormalEntry.js`;
- `CalibrationMarginEntry.jsx`;
- relevant RP-003 composition in `App.jsx`;
- relevant RP-003 and command-panel rules in `styles.css`;
- `CanonicalGameFrame` and current accessibility/frame tests;
- existing Node/Vite/readiness/build/E2E infrastructure; and
- the inherited SC-04 world plate already used by normal RP-003.

No answer-bearing value may be copied into player-facing code, diagnostics,
fixture state, documentation, data attributes, or tests that inspect rendered
content.

### Product surfaces that may be edited without variance

- `horizon-archive-game/src/CalibrationMarginNormalEntry.js`;
- `horizon-archive-game/src/CalibrationMarginEntry.jsx`;
- `horizon-archive-game/src/CalibrationMarginPythonFloor.js`;
- `horizon-archive-game/src/CalibrationMarginPythonFloor.jsx`;
- `horizon-archive-game/src/App.jsx`;
- `horizon-archive-game/src/styles.css`; and
- `horizon-archive-game/src/CalibrationMarginProtectedJourney.js` only for a
  byte-equivalent extraction of shared pure IE evaluator truth.

### Product files that may be created

- `horizon-archive-game/src/CalibrationMarginExtractionCheckpoint.js`;
- `horizon-archive-game/src/CalibrationMarginExtractionFloor.js`;
- `horizon-archive-game/src/CalibrationMarginExtractionFloor.jsx`; and
- one bounded pure shared IE evaluator module only if direct reuse would pull
  protected full-journey authority into normal dispatch.

Equivalent names require no variance when the responsibility and import
boundary are identical and the stage report maps them explicitly.

### Focused tests that may be created or edited

- extraction checkpoint tests;
- extraction controller/state-graph tests;
- normal-route composition tests;
- extraction UI/accessibility tests;
- fixture isolation/scenario tests;
- current TD-001 Python/normal-entry tests only when the new terminal entry
  changes an exact prior hard-stop assertion; and
- RP-003 protected reference tests only to prove byte-equivalent shared
  evaluator behavior.

### Fixture/configuration envelope

- `horizon-archive-game/review-fixtures/td002-extraction/**`;
- one test-only review-host helper outside `src`;
- one test-only configuration file outside `src`, if necessary; and
- `horizon-archive-game/package.json` only to add an explicit fixture
  test/review command.

No production build target, App entry, route configuration, persistence
technology, asset manifest, curriculum source, art/audio/font, or unrelated
test may change without a variance.

### Forbidden systems

Normal play and the fixture must not invoke or expose:

- `runCalibrationMarginProtectedJourneySmoke`;
- the protected full persistence/save adapter;
- bounded review, save/restore, bearing, later safe returns, or later packets;
- expected-answer exports in DOM, accessibility text, placeholders, data
  attributes, logs, captures, or persisted state;
- arbitrary state injection, developer backdoor, query flag, or production
  fixture route;
- a live Foundry/Azure/Content Understanding path; or
- Martin's campaign save or browser storage as review material.

## Fixed, optional, and forbidden scope

### Fixed

- Exact accepted `PY010-P3` predecessor and one fresh seven-modality one-hit
  entry.
- Complete `CM-30-CM-34` extraction chain in one integration tranche.
- Frozen P01/R01/T01, three dimensions, strict `3/3`, separate explanation,
  evaluator, failed-ID remediation, and evidence schema.
- Answer-free actual-miss-only repair, full clearing before repair, and wholly
  blank same-form retry.
- Zero-credit `CM-32`, fresh continue, delayed answer-free retrieval, distinct
  transfer, and atomic transfer-plus-explanation completion.
- Exact ordered IE-P0-P3 prefix sanitation and deterministic recovery.
- Finalize only `RP003-IE-01`; preserve Python and observations byte-stable.
- No-credit Tour, offline operation, no authority/exam guarantee, invariant
  world, accessibility/responsive parity, performance caps, storage-free
  fixture, and the hard stop.

### Optional

- Answer-free hints that fill no active dimension or explanation.
- Optional CM-32 source-boundary reinspection that grants zero credit.
- Confidence recording inside the exact allowlist and at zero credit.
- `CLEAR_EXTRACTION_WORK` on active learner forms.
- Restrained suit-layer presentation using existing runtime means.
- CSS reuse/refactoring inside the cap.
- Pure evaluator factoring with byte-equivalent protected and normal results.
- A dedicated extraction renderer or bounded extension of the RP-003 renderer.

Optional work cannot gate completion, add evidence, widen the route, extend
duration artificially, create a durable interlude marker, or consume the
reveal requirement.

### Forbidden

- New objective, case, expected value, dimension, threshold, remediation map,
  misconception, evidence key, checkpoint field, native category, story fact,
  scene, route, puzzle, observation, persistence technology, backend, account,
  SDK, REST/CLI call, endpoint, credential, network path, world asset, runtime
  media/font/audio, or later campaign state.
- Prefilled controls, primary-to-retrieval/transfer carryover, partial-credit
  accumulation, answer-bearing repair, expected values in UI/fixture/logs, or
  intermediate mastery.
- Production-accessible fixture/debug/state injection.
- `CM-40`, review/save/eligibility/note/restore, bearing, destination, RP-004,
  RP-013, successor, or post-ending content.
- Reward, access, identity, recognition, authentication, permission,
  authority, official exam standing, external action, live service behavior,
  or physical/world response.
- Hidden lore or inference from the hidden-lore vault.
- Inspection or mutation of Martin's campaign save/browser storage to
  manufacture a gated state.

## Marine freedoms

### Reconnaissance Sergeant - creative treatment

May choose:

- the surface-safe emotional throughline and restrained tone;
- answer-free owner/status/instruction language;
- how SC-04's quiet presence frames the separate expedition learning layer;
- hierarchy and pacing emphasis across entry, uncertainty, repair, interlude,
  retrieval, transfer, and landing;
- a bounded copy envelope for unavailable/provenance concepts without
  exposing expected values; and
- one spoiler-safe visual decision for later Image Specialist execution.

May not alter state, ownership meaning, case, answer, threshold, evidence,
route, physical response, canon, fixture boundary, or hard stop.

### Tactical Operations Specialist - experience blueprint

May choose:

- exact component-level arrangement inside one active source-ordered group;
- native radio/select/other accessible choice controls, provided all begin
  genuinely blank and expected values do not leak;
- wide columns versus vertical groups and exact responsive breakpoints;
- field grouping, copy placement, focus wording, live-region phrasing, error
  association, and recovery affordance inside the required focus classes;
- how accepted Python P3 visually hands off to and receives the extraction
  floor;
- exact test-only fixture filename/host composition inside its boundary; and
- restrained interlude pacing that grants no credit.

May not add an alternate entry, concurrent group, new late-chain return,
prefill, answer browser, hidden focus target, durable interlude marker,
arbitrary fixture state, or later action.

### Combat Engineer - implementation

May choose:

- pure reducer/controller/checkpoint factoring inside the file envelope;
- a dedicated extraction renderer or bounded RP-003 extension;
- exact storage-agnostic adapter composition and the separately allowlisted IE
  checkpoint key;
- test organization;
- CSS refactoring within the cap; and
- byte-equivalent extraction of shared pure IE evaluator truth if needed to
  keep the protected complete journey non-routable.

May not change evaluator semantics, cases, answers, dimensions, threshold,
explanation, evidence keys, modality list, focus destination class, budgets,
storage technology, protected journey results, fixture production exclusion,
or hard stop.

### Quartermaster - content and assets

May choose:

- final surface-safe instructions, labels, blank-state, repair, interlude,
  retrieval, transfer, explanation, finalization, and negative-authority copy;
- answer-free wording mapped to existing remediation concepts;
- course provenance wording that keeps all media/cases expedition-owned;
- reuse of existing local symbols/styles where ownership remains clear; and
- documentation/provenance for the inherited temporary world plate.

May not add a runtime media/font/audio asset, expose an expected answer, change
a case, add lore, imply Builder/Machine teaching, make unavailable mean a
negative result, or make the seal an access state.

### Image Specialist - presentation and reveal

May choose:

- final CSS/material/light/atmosphere/interface polish using the inherited
  world asset;
- restrained local motion only with exact reduced-motion parity;
- visible focus, forced-color, narrow, zoom, and desktop refinements;
- exactly one spoiler-safe reveal candidate that varies scale, subject, or
  composition from recent reveals; and
- provenance plus one named visual checklist item.

May not change function/evidence, generate more than one candidate, add a
required runtime media asset, imply world response or synchronized clocks, or
claim reference integration without evidence.

## Protected files and systems

Never inspect, alter, stage, move, delete, or commit:

- `Art Of No Mans Sky Book Scan.pdf`;
- `Simplilearn Training Files/`.

Never open or infer from
`DO_NOT_READ_HORIZON_ARCHIVE_HIDDEN_LORE_VAULT.md` without Martin's explicit
authorization.

Never inspect or mutate Martin's browser storage, campaign save, cookies,
profile, or session merely to reach a gated state.

## Requirement ownership and acceptance

| Requirement family | Primary owner | Acceptance method |
|---|---|---|
| Product promise and priority order | Commandant | shell-to-brief contract trace |
| Canon, mystery, emotional boundary, ownership, invariant world | Colonel | shell-to-masterplan and copy/visual review |
| Campaign position, sequence, checkpoints, returns, landing | Operations Planning Major | route/state graph and predecessor/successor tests |
| Evaluator, evidence, privacy, accessibility, offline, recovery, performance, fixture | Office of Science Administrator | focused controller/UI/privacy/isolation tests, validator, measurements |
| Cross-discipline shell clarity | Mission Captain | Tier 1 shell audit and variance routing |
| Emotional treatment | Reconnaissance Sergeant | creative treatment trace |
| Flow, layout, focus, responsive and fixture blueprint | Tactical Operations Specialist | blueprint trace and focused UI/accessibility/isolation tests |
| Functional behavior | Combat Engineer | Tiers 2-4 |
| Final copy and asset completeness | Quartermaster | content/asset ledger and placeholder scan |
| Presentation and reveal | Image Specialist | presentation review, visual evidence, provenance |
| Release and variance classification | Intelligence Officer | independent Tier 5 and line-by-line shell reconciliation |

## Validation ladder

### Tier 1 - shell contract

Before Marine deployment:

- all five Colonel artifacts agree on shell ID, address, exact predecessor,
  chain, landing, and hard stop;
- source/curriculum difference since accepted TD-001 is empty;
- frozen objective, cases, dimensions, strict thresholds, explanation,
  remediation, and evidence keys are unchanged;
- P0-P3 record sets are exact ordered allowlists;
- CM-32 has no durable marker and grants zero credit;
- transfer and explanation commit atomically;
- Python/observation evidence is byte-stable and cannot cross-credit IE;
- no private answer value appears in this shell;
- the `81,705` CSS cap traces to the accepted `77,814`-byte release and keeps
  every quality/accessibility gate;
- the storage-free fixture has an explicit production-exclusion contract;
- every fixed requirement has an owner/acceptance method; and
- no save, network, service, route, authority, world, or later state appears.

### Tier 2 - focused construction

Prove directly:

- exact Python P3 plus fresh entry and every rejection class;
- all seven modalities and validation-before-token-consumption;
- native/visible/semantic/programmatic/serialized blankness on entry and every
  retry;
- strict primary `3/3`, delayed retrieval `3/3`, transfer `3/3`, and separate
  explanation;
- no primary answer set in retrieval and no primary/retrieval carry into
  transfer;
- actual-failed-dimension/explanation-only repair after immediate private clear;
- zero-credit interlude and explicit fresh continue;
- exact P0/P1/P2/P3 sanitation, atomicity, no downgrade, and resume;
- atomic P2-to-P3 two-record commit;
- byte-stable Python and observation predecessor evidence;
- Tour isolation and complete evidence firewall;
- heading/field focus, labels, required errors, status, target size, reflow,
  forced-color, and reduced-motion;
- one active group and final no-action landing;
- closed fixture scenarios, no browser-storage access, and production
  exclusion; and
- absence of CM-40, review/save, bearing, later rail, service, authority, and
  world response.

### Tier 3 - related regression

Run:

- all new extraction checkpoint/controller/route/UI/fixture tests;
- `calibrationMarginProtectedJourney.test.js`;
- `calibrationMarginPythonFloor.test.js`;
- `calibrationMarginPythonCheckpoint.test.js`;
- `calibrationMarginPythonNormalRoute.test.js`;
- `calibrationMarginPythonUi.test.js`;
- `calibrationMarginNormalEntry.test.js`;
- `calibrationMarginProtectedEntry.test.js`;
- `calibrationMarginProtectedSurvey.test.js`;
- connected canonical-frame and RP-002 return tests; and
- `python curriculum/readiness/RP-003/validate_mapping.py --self-test`.

### Tier 4 - `FUNCTIONALLY COMPLETE`

The Combat Engineer independently runs:

- all focused and related checks;
- full `npm test`;
- every current readiness-validator self-test;
- `npm run build`;
- JS/CSS/module/media budget measurement;
- fixture/test-marker production exclusion;
- inherited plate identity;
- source/dist/served identity and forbidden-reachability scans;
- offline/no-network proof;
- privacy, storage, campaign/Tour separation;
- clean patch/status checks; and
- commit, push, and `HEAD == origin/main`.

### Tier 5 - independent release

The Intelligence Officer independently runs:

- full suite and every readiness-validator self-test;
- production build;
- isolated production preview with root/assets HTTP `200`, owned PID/liveness,
  reload-stability preflight, and served identity;
- one complete non-overlapping E2E;
- storage-free fixture review at desktop, narrow, and effective `200%`;
- blank, repair, retry, interlude, retrieval, transfer, explanation-repair,
  and P3 rendered/semantic states;
- keyboard/focus, disabled/native state, error association, live region,
  forced-color, reduced-motion, console, network, overflow, asset, and hash
  checks;
- fixture absence from production;
- QA restoration and owned-process shutdown;
- line-by-line shell reconciliation and variance classification; and
- final commit, push, and synchronization.

No role may claim a live gated, assistive-technology, save, or storage result
without direct evidence.

## Definition of done

`SS-RP003-IE01-v1` is complete only when:

1. exact accepted no-action `PY010-P3` and empty IE-P0 are the sole normal
   entry boundary;
2. one fresh semantic `BEGIN_EXTRACTION` opens a wholly blank CM-30 primary;
3. simultaneous current-attempt strict `3/3` is the sole primary pass;
4. every actual miss clears private work before failed-ID-only answer-free
   repair and a wholly blank same-form retry;
5. exact IE-P1 reconstructs zero-credit CM-32 and requires a fresh continue;
6. delayed blank CM-33 is strict `3/3` with no P01 answer set;
7. distinct blank CM-34 is strict `3/3` plus a separately valid explanation;
8. P2-to-P3 transfer and explanation records commit atomically;
9. only IE-P3 finalizes `RP003-IE-01`;
10. Python and observation evidence remains byte-stable and grants zero IE
    credit;
11. private/transient values clear or remain outside durable state;
12. only exact ordered IE-P0-P3 prefixes survive and failed writes preserve
    the prior prefix byte-for-byte;
13. return, reload, invalid recovery, and resume reconstruct the correct blank
    first-incomplete boundary without replay, prefill, cross-credit, or
    downgrade;
14. Tour, interlude, navigation, focus, presentation, timing, confidence,
    repair, and later evidence grant zero credit;
15. seven modalities converge and one active group owns each state;
16. labels, focus, errors, live status, targets, desktop, narrow, zoom, forced
    colors, and reduced motion pass;
17. the storage-free fixture covers the closed scenario allowlist, touches no
    browser/campaign storage, and is absent from production;
18. offline/no-authority/no-exam-guarantee, invariant world, independent
    clocks, zero new runtime media, and all production budgets pass;
19. CM-40, review/save, bearing, later packets, RP-013, successor, reward,
    access, authority, live service, external action, and physical/world
    response remain unreachable;
20. focused, connected, full, validator, build, served, E2E, visual-review,
    cleanup, patch, and sync gates pass at their owning stages;
21. every variance is classified and no unauthorized divergence remains; and
22. the Intelligence Officer issues `AS BUILT RELEASED`.

## Variance procedure

A Marine stops the affected work and appends:

```text
VARIANCE ID:
DISCOVERING STAGE / AGENT:
SHELL ID / VERSION:
FIXED REQUIREMENT:
OBSERVED CONDITION:
EVIDENCE:
PLAYER / PRODUCT IMPACT:
PROPOSED CLASSIFICATION:
MINIMUM REQUESTED CHANGE:
FILES / SYSTEMS AFFECTED:
VALIDATION NEEDED:
RETURN OWNER:
WORK SAFE TO CONTINUE:
```

Return ownership:

- product purpose -> Commandant;
- canon, mystery, world, or ownership meaning -> Colonel;
- campaign order, dependency, pacing, route, or checkpoint -> Operations
  Planning Major;
- learning, evidence, privacy, accessibility, save/checkpoint, offline,
  recovery, performance, fixture, or official source -> Office of Science
  Administrator;
- shell ambiguity or cross-discipline conflict -> Mission Captain;
- emotional treatment -> Reconnaissance Sergeant;
- flow, layout, interaction, focus, responsive, or fixture blueprint ->
  Tactical Operations Specialist;
- functional defect -> Combat Engineer;
- content or major asset defect -> Quartermaster;
- presentation defect -> Image Specialist; and
- release evidence or reconciliation -> Intelligence Officer.

The discovering role may continue only on work demonstrably independent of the
variance. It may not silently weaken or reinterpret a fixed requirement. The
Intelligence Officer finally classifies each variance as:

- `ACCEPTED IMPROVEMENT`;
- `REQUIRED CORRECTION`;
- `MASTERPLAN UPDATE`;
- `DEFERRED LIMITATION`; or
- `UNAUTHORIZED DIVERGENCE`.

## Mission Captain validation

| Check | Result |
|---|---|
| Every workflow/profile shell field is present | PASS |
| Current source matches the accepted predecessor claim | PASS |
| All Colonel identities, versions, address, entry, chain, landing, and disposition agree | PASS |
| No unresolved contradiction or duplicated owner remains | PASS |
| Scope is one integration-sized `CM-30-CM-34` chain | PASS |
| Frozen learning truth is referenced without private answers in the shell | PASS |
| Entry, groups, actions, exits, prefixes, recovery, and hard stop are measurable | PASS |
| CSS cap is mathematically traceable to accepted release evidence and retains quality gates | PASS |
| Fixture is mechanically test-only and production-excluded by contract | PASS |
| Allowed files/systems/assets and fixed/optional/forbidden scope are explicit | PASS |
| Privacy, no-save, accessibility, responsive, performance, offline, canon, and world invariants are testable | PASS |
| Every Marine has meaningful bounded freedom | PASS |
| Hidden lore, later content, and protected user work are named and closed | PASS |

Issuance evidence:

- pre-shell runtime/curriculum difference from accepted TD-001: empty;
- current RP-003 focused authority suite reported by Science: `61/61 PASS`;
- RP-003 readiness validator reported by Science: `SELF-TEST PASS`;
- accepted TD-001 independent release: `737/737`, `15/15`, build, served
  identity, and complete E2E;
- no current normal CM-30-CM-34 integration, live fixture, save behavior, or
  TD-002 release is claimed by this document.

## Hard stop and forbidden exposure

The shell stops immediately after exact finalized `RP003-IE-01` at `IE-P3`.
The final state exposes no action.

Normal play, fixture, tests, copy, and presentation must not mount, preload as
reachable UI, dispatch, score, imply, or reveal:

- `CM-40 BOUNDED REVIEW`;
- review/save eligibility, provenance review, expedition-note save, atomic
  save, save confirmation/failure, verified restore, or first-incomplete save
  routing;
- onward bearing, destination, route opening, or RP-004;
- RP-013, successor, post-ending content, or a central-mystery answer;
- reward, access, authentication, permission, identity, recognition,
  authority, official exam standing, external action, or live Microsoft/
  Azure/Foundry behavior;
- sealed-source content, a negative inferred from missing input, native schema,
  official truth, or service result; or
- any physical, environmental, route, light, material, sound-source,
  maintenance, clock, city, coupling, Builder, Machine, or world response.

Protected later source may remain unimported authority evidence. It cannot
become normal integration or fixture content.

## Disposition and signature

**Disposition:** `SHELL READY`

**Mission Captain:** `mission_captain`

**Issued contract:** `SS-RP003-IE01-v1`

**Construction order:** Reconnaissance Sergeant -> Tactical Operations
Specialist -> Combat Engineer -> Quartermaster -> Image Specialist ->
Intelligence Officer.

The Marines are authorized to construct only this version. The shell stays
fixed until an accepted variance creates a successor version.

## Exact Reconnaissance Sergeant handoff

- **Stage / agent:** Reconnaissance Sergeant / `reconnaissance_sergeant`
- **Shell:** `SS-RP003-IE01-v1`
- **Starting authority:** this `SHELL READY` contract plus `GDB-TD002-v1`,
  `WNMP-TD002-v1`, `CFS-TD002-v1`, `VE-TD002-v1`, and accepted TD-001 release
- **Bounded objective:** produce one compelling, surface-safe treatment for
  exact Python P3 -> fresh blank extraction primary -> actual-miss-only
  answer-free repair/blank retry -> zero-credit interlude -> blank delayed
  retrieval -> blank distinct audio/video missing-input transfer plus separate
  explanation -> finalized-IE no-action landing
- **Permitted file:** `Production Pipeline/Skyscraper Test Drives/TD-002/06-CREATIVE-TREATMENT.md` only
- **Validation tier:** Tier 1 creative trace against this shell
- **Stop boundary:** do not write runtime code, blueprint, final production
  copy, generated art, new canon, case, answer, state, route, asset, fixture,
  or later-floor content
- **Required output:** `06-CREATIVE-TREATMENT.md` containing the emotional arc,
  player-readable hierarchy, owner separation, answer-free language envelope,
  visual/audio intent using existing runtime assets, flexible versus fixed
  choices, risk checks, one reveal decision candidate, and one exact Tactical
  Operations Specialist handoff
- **Required disposition:** `CREATIVE LOCK`, `REVISE`, or `HOLD`
- **Next recipient:** Tactical Operations Specialist /
  `tactical_operations_specialist`

If a compelling treatment appears to require a world response, access/
authority implication, expected answer, new runtime asset, late-chain return,
production fixture route, or later action, record a variance and return it to
the named earliest owner instead of changing the shell.
