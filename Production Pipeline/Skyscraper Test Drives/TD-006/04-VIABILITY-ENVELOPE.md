# TD-006 Viability Envelope - Interval Works

## Document control

| Field | Value |
|---|---|
| Stage | Office of Science Administrator |
| Agent ID | `office_of_science_administrator` |
| Certificate ID | `VE-TD006-v1` |
| Product brief | `GDB-TD006-v1` |
| World baseline | `WNMP-TD006-v1` |
| Floor stack | `CFS-TD006-v1` |
| Route contract | `TD006-RTA-001` |
| Released predecessor | `TD-005 / SS-RP005-MANYFOLD-RETURN-v1 / MF-30` |
| Selected slice | `TD-006-RP006-INTERVAL-WORKS-v1` |
| Campaign address | `RP-006 / SC-07 / IW-00-IW-30` |
| Budget authority | `PBA-TD006-v1` |
| Preceding stage commit | `87fedd7e28fc4d8d89974ec6d7be4ac76b5eec5e` |
| Disposition | **`VIABILITY READY - ROUTE, RECORD, EVIDENCE, RECOVERY, ACCESSIBILITY, AND BUDGET VALIDATED`** |

Science independently finds `TD006-RTA-001` compatible with the exact
released TD-005 record, frozen RP-006 learning, a separate normal RP-006
record, deterministic atomic recovery, seven-modality access, invariant-world
limits, and a fresh non-compounding production budget.

This certificate authorizes Mission reconciliation only. It does not issue a
shell or authorize Marine construction. The protected RP-006 journey remains
unimported reference evidence and cannot substitute for a normal adapter,
controller, persistence boundary, UI, fixture, art package, or release proof.

## Authority certificate

| Authority | SHA-256 or identity |
|---|---|
| `AGENTS.md` | `DD96BE91D0BD8DACD2A7D0EF7A3949047658F36C577F1A177A9CCABBB2622349` |
| `NEXT_INSTANCE_HANDOFF.md` at stage start | `D1A1A7FF82B03841ACDA5BF79736A6B7710B421E97073A89255B12F0E94DDFD7` |
| `SKYSCRAPER_AGENT_WORKFLOW.md` | `CA3D2868DB35D82C08B42EB83BB69A5C49483C60E281476EE79D74359CCA719C` |
| `Skyscraper Agent Profiles/README.md` | `78170827BD7BA4D420EC985937810396C3ED241A81E9ED6F697B0D9C24666D13` |
| `office-of-science-administrator.md` | `CB5C3A8D1699A9A91829FC403359A59B01EE5F95805794E8C2298EB4EBFD6B44` |
| `TD-006/01-GAME-DEVELOPMENT-BRIEF.md` | `209E320AEB127923CE81E090D7414B27F4286368895B5FE36D964A20A8CE3D0C` |
| `TD-006/02-WORLD-NARRATIVE-MASTERPLAN.md` | `05CB8A30AEF81BF90B15D9926326DA5B261605AB6533070DA1DA0694ACF9C37B` |
| `TD-006/03-CAMPAIGN-FLOOR-STACK.md` | `DEE4703994031C1B739BA00E4F7FCE0CAA639125B6BB453635D6F7C6D433E940` |
| `TD-005/11-AS-BUILT-RECONCILIATION.md` | `5C5C5FDA7C8AE9EEDCAA71FCC9329571DC8EF458C78B9CBD076E6BA585AA7364` |
| `curriculum/readiness/RP-006/contract.json` | `AB24B5F5D948A461D18F585DF7F9D265A100B189C37089E94AC457D8DB61B252` |
| `RP-006-interval-works.md` | `7DD14D2268BA2A05A107CA3246FBB35187C62D6F00938A27DD83F9EB58B2D278` |
| `SC-07-interval-works.md` | `5CD1863FF397BC3D6881DEC1A52F95D56DDF876A3EC20F780E1C6437652A34F6` |
| released `ManyfoldReturnNormal.js` | `3F88B01507EB008957E5D5D187608DF35C45BBC3D673D1C6912BB6F9D4678CA2` |
| protected `IntervalWorksProtectedJourney.js` | `4EF204D0764A518CC0870D774F469E574AF35526A7960F9B6021BD64C62D32E9` |
| `04A-PRODUCTION-BUDGET-AUTHORITY.json` | `992EB4EDB75121BD31453E111BE664FB9CB88DC2B7BF5EF2C5DF14EA92C07CB1` |
| `scripts/validate_td006_budget.py` | `DDB51C0198C8778E5774233BDFE06F2EC922F47035A748188CFEB46F6BEBA841` |

## Official Microsoft source review

The named `foundry-azure-source-priority` skill is not available in this
runtime. Science followed its required source priority directly and used no
third-party source:

- [AI-901 study guide](https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/ai-901):
  the April 15, 2026 objectives still include identifying features and
  capabilities of speech recognition and speech synthesis.
- [Azure Speech overview](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/overview):
  speech-to-text converts audio to text; text-to-speech converts input text
  to synthesized speech.
- [Microsoft Foundry documentation](https://learn.microsoft.com/en-us/azure/foundry/),
  [SDK and endpoint overview](https://learn.microsoft.com/en-us/azure/foundry/how-to/develop/sdk-overview),
  [Agent Service](https://learn.microsoft.com/en-us/azure/foundry/agents/overview),
  and [Content Understanding](https://learn.microsoft.com/en-us/azure/ai-services/content-understanding/overview)
  remain supporting official context only.

RP-006 uses course-authored offline cases. It does not configure, call,
simulate, authenticate to, or claim authority over Azure Speech, Foundry, a
model, an agent, a resource, an endpoint, or an external service.

## Route and adapter viability

### Sole transition

`TD006-RTA-001` is the only route from released TD-005:

```text
PILOT // EXPEDITION NAVIGATION
PILOT // FOLLOW EXPEDITION-MARKED ADJACENT SURVEY TO INTERVAL WORKS
```

`Interval Works` is an expedition planning label. The route is not emitted by
the serviced continuation, receiver field, cuffs, frequency, bypass, packet
row, scene sheet, saved record, Builder work, Machine, city, or world. It
grants no invitation, access, permission, identity, reward, authority,
recognition, response, or external action.

### Released predecessor that must be reused

The normal entry implementation must import and reuse these released TD-005
exports rather than duplicate their logic:

- `MANYFOLD_RETURN_SAVE_KEY` =
  `horizon-archive-rp005-manyfold-return-save-v1`;
- `MANYFOLD_RETURN_RECORD_VERSION` =
  `rp005.manyfold-return-save.v1`;
- `MANYFOLD_RETURN_SHELL_VERSION` =
  `SS-RP005-MANYFOLD-RETURN-v1`;
- `MANYFOLD_RETURN_CONTROLLER_VERSION` =
  `rp005.manyfold-return-controller.v1`;
- `sanitizeManyfoldReturnSave`;
- `createManyfoldReturnStorageAdapter`; and
- `resolveManyfoldReturnWorldScene`.

The entry adapter also retains exact released TD-004 raw bytes through the
released TD-005 predecessor boundary. It may read those bytes only for
strict identity verification; it may not rewrite, migrate, merge, annotate,
or remove either predecessor record.

### Exact availability predicate

The action exists only when every condition passes:

1. Demo Tour is rejected before campaign storage access.
2. mode is exact normal `campaign`;
3. the released TD-005 key yields one exact strictly sanitized record;
4. raw read-back equals the canonical serialization of that sanitized record;
5. version, shell, controller, packet `RP-005`, mapping
   `RP005-A3-MANYFOLD-RETURN`, checkpoint `manyfold_return_complete`,
   continuation, null deltas, null successor, note, four observations, and
   all eight ordered evidence records are exact;
6. retained TD-004 bytes are unchanged;
7. mounted state is exact no-replay `mf30_restore` or
   `mf30_restore_recorded`;
8. the scene resolver returns released `SC-06`;
9. learner source, raw answers, feedback, reasoning, identity material,
   credentials, endpoints, payloads, responses, tokens, pointer/focus
   history, Tour state, and transient work are absent;
10. exactly one active `PILOT // EXPEDITION NAVIGATION` group owns the route,
    optional destinationless notation, and released returns;
11. route ID, source and target identities, owner, active group, action,
    modality, and opaque event-token shape are exact; and
12. the token is fresh and unused.

The restored record proves prior completion only. It is neither permission
nor a navigation dispatch. The fresh Pilot intent is the sole route owner.
Availability is identical before and after optional serviced-continuation
notation; that notation remains inert and non-routing.

### Validation before token consumption

The System performs, in order:

1. isolate Tour;
2. strict-read and sanitize TD-005;
3. verify raw canonical read-back and retained TD-004 bytes;
4. verify exact MF-30 state and SC-06 identity;
5. clear or reject private/transient material;
6. validate source, target, group, owner, action, modality, and token shape;
7. prove the token is fresh and unused; then
8. consume it and mount only `IW-00 ARRIVE + IDLE / SC-07`.

One accepted intent creates one transient arrival, writes no record, grants
zero observation or learning evidence, replays no predecessor event, changes
no world surface, and preserves `continuation="continuation"`, both null
deltas, and `successor=null`. A duplicate token is a no-op and cannot replay
arrival, write, erase work, move focus, or change scene identity.

Return, reload, or an unsaved interruption requires a fresh Pilot intent and
token.

## State-to-scene identity

| Exact state | Required scene identity | Contract |
|---|---|---|
| TD-005 `MF-30 VERIFIED RESTORE` | released SC-06 panorama | route, notation, and returns remain independent |
| TD-005 `MF-30 CONTINUATION NOTED` | same released SC-06 panorama | notation is inert and non-routing |
| invalid/missing/stale/duplicate TD006 intent with valid TD-005 | `SC-06 / MF-30` | no scene, write, token, focus, or evidence change |
| invalid TD-005 or retained TD-004 dependency | released predecessor recovery scene | never mount SC-07 |
| accepted `TD006-RTA-001` | `SC-07 / IW-00` panorama | one zero-evidence arrival |
| `IW-10 INSPECT MATERIAL ORDER` | SC-07 registered cross-section | four equal observations; display/order is zero-credit |
| `IW-20 SEQUENCE + SAVE` | invariant SC-07 panorama plus separate work/course surface | independent chains, explanations, review, sanitation, atomic save |
| exact saved `IW-30 VERIFY + RETURN` | invariant SC-07 panorama | heading-first replay-free restore |
| interrupted or invalid unsaved RP-006 | exact `MF-30 / SC-06` | clear RP-006 work and require fresh route intent |
| return to Manyfold Return | exact `MF-30 / SC-06` | write-free and replay-free |
| return to City Threshold | existing accepted City Threshold identity | write-free and replay-free |

SC-07 may mount only from one accepted route intent or one exact valid RP-006
restore. Labels, scenery, record presence, continuation notation, focus,
invalid data, and navigation history never select SC-07. No scene reacts to
route, observation, evaluation, save, restore, or return.

## Normal RP-006 persistence contract

### New identities

| Field | Required value |
|---|---|
| Storage key | `horizon-archive-rp006-interval-works-save-v1` |
| Record version | `rp006.interval-works-save.v1` |
| Planned shell | `SS-RP006-INTERVAL-WORKS-v1` |
| Controller version | `rp006.interval-works-controller.v1` |
| Packet | `RP-006` |
| Mapping | `RP006-A3-INTERVAL-WORKS` |
| Checkpoint | `interval_works_complete` |

The normal identities are distinct from protected
`rp006.protected-journey.v1`. The protected in-memory adapter is not a
production storage adapter.

### Exact record shape

The normal record has exactly these ten ordered top-level keys:

```text
version
packetId
mappingId
checkpoint
continuation
cityStateDelta
externalStateDelta
successor
note
evidence
```

`continuation` is exactly `continuation`; city and external deltas and
`successor` are `null`.

The note has exactly these eleven ordered keys:

```text
observations
relativeOrder
changed
persistent
unavailable
stewardship
replicas
gap
cause
purpose
destination
```

Required note values:

- observations contain each of the four frozen IDs exactly once in canonical
  contract order:
  `overlap_crosscut_relative_order`,
  `changed_persistent_feature_pair`,
  `closed_interval_unavailable`,
  `layered_stewardship_visible`;
- `relativeOrder="exposed_source_order_preserved"`;
- `changed="one_visible_change_observed"`;
- `persistent="one_feature_persists"`;
- `unavailable="closed_interval_unavailable"`;
- `stewardship="layered_stewardship_observed"`;
- `replicas="sanitized_precomputed_only"`;
- `gap="explicit_unavailable_record"`; and
- cause, purpose, and destination are `null`.

### Exact ordered evidence

The record contains exactly eight independently finalized records:

1. `PY-013 / primary`;
2. `PY-013 / trace`;
3. `PY-013 / transfer`;
4. `RP006-SPEECH-01 / primary`;
5. `RP006-SPEECH-01 / retrieval`;
6. `RP006-SPEECH-01 / transfer`;
7. `RP006-SPEECH-01 / direction_boundary_explanation`; and
8. `RP006-SPEECH-01 / causation_boundary_explanation`.

Every record has exactly the frozen packet/mapping, owner, form, complete
dimension set, every dimension `true`, attempt count `1-99`, hint level
`0-3`, confidence `null|low|medium|high`, an allowlisted misconception-tag
array, and `mastery_status="mastered"`.

No predecessor record, observation, route, scene, transcript order, sound,
presentation, focus, modality, timing, confidence, hint, Tour, execution
display, save display, or story state can manufacture, prefill, compensate
for, or cross-credit a missing record.

### Privacy and sanitation

Reject the whole record for any missing, extra, reordered, wrong-type, false,
partial, duplicate, forged, or contaminated field. Reject any key or
serialized content containing:

- learner source, raw case answers, free reasoning, feedback, copied source
  or case content, private notes, identity content, exam items;
- credentials, secrets, resource IDs, endpoints, URLs, payloads, requests,
  responses, external-action requests;
- pointer, focus, modality, token, route-intent, timing, or Tour history;
- live readings, inferred cause/duration/authorship/purpose/category/value;
- closed-interval contents; or
- any unapproved key.

After scene change, remediation, save, return, reload, and resume, clear
unsubmitted code, choices, answers, feedback, prompts, controls, event tokens,
and focus/pointer history. Only the exact finalized record is durable.

### Atomic write, rollback, and read-back

The normal adapter must:

1. retain exact prior RP-006 raw bytes or verified absence;
2. reject a malformed prior record without overwriting it;
3. retain exact TD-005 and TD-004 predecessor raw bytes;
4. strictly sanitize the complete candidate before any write;
5. write once only to the dedicated RP-006 key;
6. read back raw bytes and strict sanitized object;
7. accept only when raw bytes equal canonical serialization, the strict object
   equals the candidate, and both predecessor byte strings remain exact;
8. otherwise restore exact prior RP-006 bytes or verified absence;
9. verify rollback and both predecessor byte strings; and
10. report failure honestly without progression when rollback cannot be
    proven.

A write throw, malformed read-back, predecessor change, invalid prior record,
or unverified rollback grants no save, route, evidence, world, or authority
effect. Exact valid save restores only heading-first `IW-30 VERIFY + RETURN`
with zero replay. No valid save means reload reconstructs exact MF-30/SC-06
and requires a fresh route.

## Learning and evidence firewall

### Entry readiness

The scored terminal holds until strict existing `L-03-03 / PY-013` readiness
and `L-04-02` plus `L-06-01 / AI901-D1-O6` readiness pass. Story returns
remain available. Confidence or narrative progress never satisfies the gate.

### Python chain

- learner imports `itertools` and calls
  `itertools.chain.from_iterable(record_groups)` exactly once;
- supplied sanitized groups and wrapper are unscored;
- primary is strict `8/8`;
- answer-free remediation addresses only actual failed checks;
- delayed closed-note trace is strict `8/8`;
- transfer is distinct, genuinely blank, and strict `8/8`;
- order, exact keys, changed/persistent fields, explicit unavailable interval,
  and `cause=None` remain exact; and
- hardcoding, sorting, reversing, filling the gap, inferring cause/duration/
  authorship/purpose, mutation, live reads, forbidden operations, or carried
  primary work fail closed.

### Speech chain

- primary has four course-authored cases x two dimensions = `8/8`;
- remediation is per actual case/dimension and answer-free;
- delayed retrieval has two distinct cases x two dimensions = `4/4`;
- transfer is genuinely blank with four distinct cases x two dimensions =
  `8/8`;
- one separate explanation proves input/output direction selects recognition
  versus synthesis; and
- one separate explanation proves transcript order/content does not establish
  causation.

The neutral case surface contains no SC-07, scene order, scene sound,
landscape, Builder, layer, repair, gap, changed/persistent, closed interval,
or stewardship answer channel.

### Conjunction

Four observations, three Python records, three speech records, and two
explanations are separate owners. Only the complete conjunction permits
review and save. A miss never erases or awards another owner. Retry is
unlimited, answer-free, and wholly blank.

## Checkpoints, recovery, and returns

| Checkpoint | Required completion | Recovery |
|---|---|---|
| `MF-30 ROUTE CHOICE` | fresh validated `TD006-RTA-001` | invalid intent stays MF-30; source recovery remains TD-005-owned |
| `IW-00 ARRIVE + IDLE` | explicit orientation | zero evidence; return to MF-30 or City Threshold |
| `IW-10 INSPECT MATERIAL ORDER` | all four equal observations in any of 24 orders | idempotent revisit; first-unrecorded focus |
| `IW-20 SEQUENCE + SAVE` | observations + all eight evidence records + strict read-back | blank answer-free remediation; deterministic first incomplete; byte-stable failed write |
| `IW-30 VERIFY + RETURN` | exact sanitized no-replay restore | return to MF-30 or City Threshold; destinationless notation opens nothing |

Failure rules:

- Tour rejects before storage and cannot dispatch.
- Invalid/contaminated TD-005 mounts no SC-07 and uses released recovery.
- Wrong source/controller/group/owner/action/modality/token remains MF-30,
  consumes and writes nothing, and focuses the route-choice heading.
- Missing or duplicate pre-transition token requires a fresh token.
- Duplicate post-transition token is a no-op at the current RP-006 state.
- Pre-consumption interruption reconstructs MF-30/SC-06.
- Post-arrival pre-save reload clears RP-006 work and reconstructs MF-30.
- Invalid RP-006 save mounts no SC-07 restore, preserves predecessors, clears
  invalid RP-006 material, and reconstructs MF-30.
- A verified saved record restores only IW-30 with private work absent.
- A failed write returns to the deterministic first incomplete scored
  boundary only after exact rollback is verified.
- Early return clears RP-006 transient work and reconstructs MF-30 or City
  Threshold write-free.
- Re-entry always requires fresh Pilot intent.

RP-006 has only:

- `RETURN TO MANYFOLD RETURN`, restoring exact no-replay MF-30/SC-06; and
- `RETURN TO CITY THRESHOLD`, restoring the existing anchor.

There is no direct shortcut to Three-Current Reach, Calibration Margin, or
Civic Comparison. No RP-007 route or destination exists.

## Accessibility, responsive, and fixture contract

### Semantic access

- seven equivalent modalities: pointer, touch, Enter, Space, switch, speech,
  and screen reader;
- validation before one-hit token consumption;
- exactly one active owner/message/content/action group;
- persistent visible labels, field-associated errors, semantic headings, and
  one polite atomic status;
- deterministic heading-first, first-invalid, next-required, recovery, save,
  and return focus;
- every action target at least `44 x 44 CSS px`;
- no meaning or credit from color, motion, sound, frequency, position,
  sequence, timing, or dexterity;
- forced-color borders, grouping, native controls, and focus;
- reduced-motion still/direct replacement and `scroll-behavior:auto`;
- grayscale-legible material relations plus full text alternatives; and
- no time limit, speed score, forced animation, or sensory-only cue.

### Exact DPR-1 layouts

- `1920 x 1080` desktop;
- `1366 x 768` laptop;
- `390 x 844` narrow; and
- `768 x 900` effective-`200%` text-zoom equivalent.

Wide layouts contain the complete shell in `100dvh` with the world dominant,
no outer horizontal or vertical scroll, and the current action visible.
Narrow/zoom layouts reflow into one natural vertical column with no
horizontal escape or clipped owner, limit, feedback, recovery, save, or
return.

### Closed storage-free fixture

The fixture must use an exact allowlisted manifest and cover at least:

- route-ready and notation-recorded MF-30;
- invalid, stale, duplicate, Tour, and predecessor-contaminated route cases;
- accepted IW-00;
- each IW-10 observation and all-observed state;
- Python primary/trace/transfer blank, miss, repair, and pass boundaries;
- speech primary/retrieval/transfer blank, miss, repair, and pass boundaries;
- both explanation boundaries;
- bounded review;
- write, read-back, rollback, and rollback-unverified failures;
- exact IW-30 restore;
- unsaved interruption/reload;
- early/final Manyfold Return; and
- City Threshold return.

It accepts no URL, query, browser storage, campaign save, arbitrary JSON, or
runtime injection; it is absent from production imports and output. The
machine summary measures player-surface targets/containment separately from
harness controls/output. Independent whole-document containment and human
visual review remain mandatory.

The matrix includes all four layouts, forced colors, reduced motion,
grayscale, focus/status, target size, crop/hotspot registration, overflow,
local requests, console/page errors, scene/alt identity, fixture exclusion,
and a bounded screenshot set.

## Offline, Tour, authority, and invariant world

- all practice is local and offline;
- no live Azure, Foundry, Speech, terminal, field, sealed-interval, or world
  call;
- no account, credential, resource, endpoint, payload, request, response,
  authentication, consent, access, permission, ranking, correction,
  causation, replay, exam credit, or exam guarantee;
- Tour isolates before campaign storage, stays storage-free and zero-credit,
  and cannot dispatch, save, restore, or unlock;
- SC-06 and SC-07 remain invariant under entry, observation, evaluation,
  remediation, save, restore, return, and resume;
- ambient clocks are monotonic and independent of player results;
- no city, Builder, Machine, physical, route, or external delta;
- absolute chronology, duration, cause, authorship, native category,
  identity, importance, purpose, audience, ownership, closed contents,
  reward, recognition, and response remain unknown; and
- hard stop is IW-30 before RP-007, RP-013, a successor, or post-ending
  content.

## Production budget

`PBA-TD006-v1` is a fresh one-time non-compounding five-percent rebaseline
from the exact accepted TD-005 production identity:

| Measure | Baseline | TD-006 cap | Headroom |
|---|---:|---:|---:|
| Aggregate JavaScript | `1,295,773` | `1,360,561` | `64,788` |
| Aggregate CSS | `87,267` | `91,630` | `4,363` |
| Production modules | `188` | `197` | `9` |
| New runtime images | `0` | `2` | `2` |
| New image-only media | `0` | `8,388,608` | `8,388,608` |
| Total runtime media | `26,078,751` | `34,467,359` | `8,388,608` |

The two planned runtime image roles are the SC-07 panoramic identity and its
registered material cross-section identity. They are allowances, not quality
targets. Maximum-quality cinematic photorealism, first-person framing,
physical credibility, direct imports, provenance, accessible alternatives,
responsive crops, grayscale review, source/full-resolution inspection, and
placeholder retirement remain mandatory. Byte compliance never excuses
visible degradation.

No new runtime audio, font, video, source-map, or network-fetched payload is
permitted.

Timing caps:

- build `<=60s`;
- focused suite `<=30s`;
- related suite `<=60s`;
- full suite `<=60s`;
- complete non-overlapping E2E `<=180s`; and
- sampled main-thread task `<=100ms`.

`scripts/validate_td006_budget.py` aggregates all emitted JS/CSS chunks,
hashes and counts every media file, requires all eleven accepted TD-005 media
hashes, limits new media to two image files/8 MiB, rejects unapproved media,
enforces modules/build/timing, and requires every release timing in release
mode. It passes the exact TD-005 baseline and rejects module, build,
missing-release-timing, audio, and three-image probes.

## Connected validation ladder

### Mission and contract

- authority hashes, route predicate/order, exact states/scenes, record keys,
  evidence dimensions, privacy denylist, rollback, returns, accessibility,
  assets, budget, and hard stops reconcile without conflict;
- protected-reference identity remains excluded from normal production; and
- shell is `SHELL READY` only when every requirement is testable.

### Combat and related regression

- focused normal route/controller/persistence/UI tests;
- all seven modalities and one-hit/duplicate/stale/Tour/private probes;
- all 24 observation orders;
- strict PY-013 and RP006-SPEECH-01 independent pass/miss/remediation paths;
- exact record/atomic success, write/read-back failure, malformed prior,
  rollback-unverified, restore, and return tests;
- TD-005 normal/protected/integration/UI and RP-006 protected regressions;
- state-only scene resolver and alt/crop role tests; and
- route/protected/fixture non-integration and later-content exclusions.

### Content, presentation, and release

- all readiness self-tests;
- full game suite;
- production build and `PBA-TD006-v1`;
- exact emitted/served identity and local-only requests;
- closed fixture across all layouts/parity modes;
- full-resolution, grayscale, crop, focus, target, overflow, console/page,
  forced-color, reduced-motion, and human visual review;
- build and one complete E2E strictly non-overlapping;
- incidental QA restoration and owned-preview shutdown; and
- independent shell-to-build Intelligence reconciliation.

## Fresh Science evidence

| Gate | Result |
|---|---|
| focused protected RP-006 | `13/13 PASS`; Node `0.381s`; wall `0.533s` |
| related TD-005/RP-006 | `42/42 PASS`; Node `0.447s`; wall `0.590s` |
| full game | `825/825 PASS`; Node `10.463s`; wall `12.064s` |
| readiness self-tests | `11/11 RP-002-RP-012 PASS` |
| production build | `PASS`; Vite `18.74s`; wall `21.765s`; `188` modules |
| released identity | JS `1,295,773` / `D26D8288...`; CSS `87,267` / `20F300C1...`; media `26,078,751` / 11 files |
| budget baseline | `PBA-TD006-v1 PASS` |
| budget negative probes | module, build, missing release timings, unapproved audio, and three images all rejected |
| patch/protected boundary | hidden lore unopened; browser save/storage uninspected; protected user work untouched |

Science does not claim live SC-07, assistive-technology hardware, fixture,
normal RP-006, E2E, or visual evidence before those artifacts exist.

## Risks, flexibility, and hard stops

### Controlled risks

- **Route integration:** the released TD-005 controller has no TD-006 action.
  Mitigation: a narrow normal entry composition imports released TD-005
  identities and validates without rewriting them.
- **Protected adapter weakness:** protected RP-006 persistence is in-memory
  and less strict than the normal requirement. Mitigation: do not reuse it;
  build the exact normal sanitizer and atomic storage adapter above.
- **Large evidence surface:** six scored forms plus two explanations can leak
  cross-credit. Mitigation: exact ordered owners, complete dimension sets,
  conjunctive save, and targeted negative probes.
- **Scene semantics:** relative order can be mistaken for cause or absolute
  time. Mitigation: permanent material relations, neutral copy, grayscale/
  alt review, and two explicit limits.
- **Asset growth:** two premium masters can consume the media allowance.
  Mitigation: direct role-specific masters and derived crops only; no quality
  downgrade or hidden duplicate asset.

### Downstream flexibility

Mission and Marines may choose code/file boundaries, local immutable-data
structures, exact responsive crop coordinates, non-answer-bearing copy, and
visual topology within the approved SC-07 brief. They may not change route
ownership, record/evidence identities, state/scene mapping, observation
equality, recovery, returns, budgets, or hard stops.

### Immediate return conditions

Return to Operations for any adjacency, source/target, route, return, or
checkpoint conflict. Return to Colonel for any canon, mystery, identity,
purpose, causal, world-response, or successor implication. Return to Science
for any evidence, privacy, storage, accessibility, offline, performance,
asset, or budget conflict. Mission records `HOLD` rather than weakening a
contract.

## Disposition and exact Mission Captain handoff

**`VIABILITY READY / VE-TD006-v1`.**

Mission Captain must reconcile `GDB-TD006-v1`, `WNMP-TD006-v1`,
`CFS-TD006-v1`, `VE-TD006-v1`, and `PBA-TD006-v1` into exactly one
conflict-free `SS-RP006-INTERVAL-WORKS-v1` shell.

The shell must freeze:

1. exact released TD-005/TD-004 byte identity and the sole
   validation-before-consumption `TD006-RTA-001`;
2. one zero-evidence IW-00 arrival and the IW-00-IW-30 checkpoint graph;
3. exact SC-06/SC-07 state-to-scene identity;
4. the distinct normal key/version/controller and exact 10-key,
   11-note-key, eight-record allowlist;
5. strict sanitation, atomic replace/read-back/rollback, valid IW-30 restore,
   invalid/unsaved MF-30 recovery, and fresh re-entry;
6. four equal observations, independent strict `PY-013`,
   `RP006-SPEECH-01`, two explanations, and zero cross-credit;
7. seven modalities, deterministic focus/status, exact layouts, forced
   colors, reduced motion, grayscale, closed fixture, offline/Tour/world
   invariance, and exact returns;
8. `PBA-TD006-v1` and exactly two planned SC-07 image roles; and
9. hard stop at IW-30 before RP-007, RP-013, successor, identity, purpose,
   access, reward, authority, response, or post-ending content.

Mission issues `SHELL READY` only if all requirements are mutually compatible
and independently testable. Otherwise it returns `REVISE` or `HOLD` to the
earliest responsible owner. No Marine deploys before the Mission commit/push
gate.
