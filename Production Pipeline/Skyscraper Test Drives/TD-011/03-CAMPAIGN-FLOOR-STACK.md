# TD-011 Campaign Floor Stack - Route and Placement Certificate

## Document control

| Field | Value |
|---|---|
| Stage | Operations Planning Major |
| Agent ID | `operations_planning_major` |
| Certificate ID | `CFS-TD011-v1` |
| Product brief | `GDB-TD011-v1` |
| World certificate | `WNMP-TD011-v1` |
| Released predecessor | `TD-010 / SS-RP010-COUNTERFIELD-v1 / CF-30 / SC-11` |
| Selected campaign address | `RP-011 / SC-12 / UR-00-UR-30` |
| Sole transition | `TD011-RTA-001` |
| Shell | Unissued; Mission owns shell versioning after Science |
| Preceding-stage commit | `db2684c` |
| Disposition | `FLOOR STACK READY - ROUTE AUTHORIZED` |

This certificate selects the nearest mapped contiguous packet and authorizes
one narrow conditional route at planning level. It does not implement the
route, import protected code, issue a shell, change normal runtime, generate or
import an image, open RP-012, define the ending, or authorize a successor,
reward, access, identity, permission, authority, response, or world effect.

## Current whole-building and playable positions

- Intended story rail: complete through protected final railhead `RP-012`;
  no `RP-013`, successor, or post-ending floor exists.
- Accepted normal Demo: released through exact
  `RP-010 / SC-11 / CF-00-CF-10-CF-20-CF-30`.
- Released stop: CF-30, exact Occluded Fold or City Threshold return, or inert
  LOOK with `destination=null`, `routeOpened=false`, and `successor=null`.
- Nearest unfinished normal edge: the already-mapped and coordinator-promoted
  `RP-011 / Unborrowed Reach`, directly after RP-010.
- Implementation distance: RP-011 story, scene, curriculum, puzzle, and pure
  protected journey exist; normal route, normal controller, App rendering,
  browser persistence, closed fixture, served release evidence, and accepted
  Demo behavior do not.

No structural predecessor is skipped. RP-011 is selected because it is the
only approved contiguous packet after released RP-010 and before the protected
ending railhead.

## Authority and predecessor verification

The Operations starting authority is exact Colonel commit `db2684c`, which
contains `WNMP-TD011-v1`, the stage ledger, and the Colonel-to-Operations
handoff. The candidate is derived from approved `RP-011` surface material and
remains within the product envelope in `GDB-TD011-v1`.

The released normal predecessor is not the protected planning journey. Normal
entry must validate the canonical browser-save record:

- `version = "rp010.counterfield-save.v1"`;
- `packetId = "RP-010"`;
- `mappingId = "RP010-REQUEST-CLIENT-BOUNDARY-01"`;
- `checkpoint = "counterfield_complete"`;
- `continuation = "continuation"`;
- `cityStateDelta = null`, `externalStateDelta = null`, `successor = null`;
- exact retained RP-007, RP-008, and RP-009 records;
- exact seven-observation Counterfield scope register and bounded exchange;
- exact eight finalized, private-free learning-evidence records; and
- canonical raw bytes under the released Counterfield save key while every
  required predecessor save byte remains unchanged.

`rp010.protected-journey.v1` and `UnborrowedReachProtectedJourney` are
protected reference authorities only. They are unimported and non-routable and
must be explicitly rejected as normal bridge inputs. Protected success,
planning order, test fixtures, LOOK, scenery, saved scopes, or queue status
cannot substitute for exact released normal bytes and state.

Focused source/runtime evidence passed `20/20`: five released-normal
Counterfield tests, including all `5,040` observation orders and exact save/
restore/hard stop; and fifteen RP-011 protected-contract tests, including all
`720` fresh-observation orders, all `24` reopen orders, strict independent
learning, two byte-stable checkpoints, recovery, returns, null LOOK, isolation,
and no production import.

## Selected slice and player purpose

### Slice identity

- Packet: `RP-011`.
- Location handle: `SC-12 / Unborrowed Reach`.
- Internal graph: `UR-00 -> UR-10 -> UR-20 -> UR-30`.
- Campaign position: Phase IV capstone and resolution, slot 2 of 3, directly
  after RP-010 and before protected ending railhead RP-012.

### Player purpose

Make one defensible fresh record without viewing or copying prior expedition
conclusions, then reopen the four complete prior scopes for a separate
method-and-limit reconciliation that does not merge locations or claims.

The slice tests transfer of inquiry method, not transfer of conclusion. It is
not a memory test, readiness verdict, final answer, access gate, or ending.

## Sole route contract - `TD011-RTA-001`

### Route group

- Owner: `PILOT // EXPEDITION NAVIGATION`.
- Group ID: `td011-unborrowed-reach-route`.
- Action ID/copy: `FOLLOW THE EXPOSED FIELD MARGIN`.
- Intent kind: `td011.unborrowed-reach-route-intent.v1`.
- Mode: `normal` only.
- Permitted activation modalities: pointer, touch, keyboard Enter, keyboard
  Space, switch, speech, and screen reader.
- Event token: opaque, transient, private-free, at least 16 characters, fresh,
  one-hit, never persisted, never reconstructed from focus or browser history.

### Exact preconditions

The route group may mount only after all of the following validate without
mutation:

1. exact released `SS-RP010-COUNTERFIELD-v1` normal controller state at
   `CF-30 VERIFY + RETURN / SC-11`;
2. exact `packetId=RP-010`, `activeGroup=cf30_restore`, System expedition-ledger
   owner, deterministic CF-30 heading/status/focus, and current LOOK plus both
   released returns;
3. exact canonical normal `rp010.counterfield-save.v1` raw/object and exact
   eight-record learning evidence;
4. unchanged canonical predecessor save bytes required by the RP-010 adapter;
5. `continuation="continuation"`, null city/external deltas, null successor,
   no authority, no external action, no world-state change, and no replay;
6. accepted campaign mode rather than Demo Tour, fixture, protected reference,
   debug, preview harness, or historical save; and
7. a separate fresh explicit Pilot route activation matching owner, group,
   action, modality, and unconsumed token.

Validation occurs before token consumption or dispatch. The existing LOOK and
returns remain semantically unchanged and cannot synthesize a route intent.

### Accepted transition

One valid intent consumes its token exactly once and atomically replaces the
route group with:

- `RP-011 / UR-00 ARRIVE + ISOLATE / SC-12`;
- owner `SYSTEM // INDEPENDENT RECORD MODE`;
- four complete prior scopes present only as hidden-retained references;
- no observation, learning, reconciliation, save, route, readiness, authority,
  or world evidence granted;
- cleared private/transient work and empty new-record workspace;
- unchanged `continuation`, null city/world/external deltas, null successor;
- no predecessor event replay; and
- deterministic heading-first focus.

The transition itself writes no RP-011 progress. It does not change or delete
any RP-010 or earlier byte.

### Rejection and interruption

Missing, malformed, partial, extra, stale, duplicate, consumed, wrong-owner,
wrong-group, wrong-action, wrong-version, wrong-packet, private-bearing,
Tour-derived, fixture-derived, protected-version, successor-bearing,
authority-bearing, or external-action-bearing state/intent rejects before
token consumption and leaves exact CF-30 unchanged.

- Interruption before validation/consumption leaves the current token unspent
  only if the same live event remains current; reload never reconstructs it.
- Interruption after consumption may yield only complete UR-00 entry or exact
  unchanged CF-30; no partial route, half-mounted group, observation, learning,
  save, or later state may persist.
- Duplicate activation with the same token is inert and cannot replay entry.
- Failure requires fresh explicit Pilot intent; scenery, LOOK, focus, timing,
  saved scopes, or a previous valid token cannot retry automatically.

## Ordered floor stack

### `UR-00 - ARRIVE + ISOLATE`

- Verify exact RP-010 entry and show one System-owned independent-record mode.
- Hide but do not delete the four prior scopes.
- Require one explicit Pilot isolation action before fresh inspection.
- Grant zero observation, learning, save, reconciliation, authority, or route
  evidence.
- Permit safe return to exact Counterfield without a write.

### `UR-10 - INSPECT FRESH EVIDENCE`

- Present six equal, deliberate, idempotent physical observation actions in
  any of all `720` orders.
- Keep prior scopes hidden-retained and unavailable as task answers.
- Revisit changes no count, order, rank, route, or world state.
- Only the complete six-observation set permits movement to UR-20; no
  observation cross-credits learning.

### `UR-20 - REVIEW LOCAL WORK + FINALIZE FRESH RECORD`

- Run strict independent `PY-019` primary, closed-note trace, and genuinely
  blank transfer over sanitized fresh-reach replicas only.
- Run strict independent `RP011-SINGLE-AGENT-SURFACES-01 / AI901-D2-O4`
  primary, delayed retrieval, genuinely blank transfer, and two separate
  explanations.
- Only actually failed public checks or scored case/dimension tags select
  answer-free remediation; retries begin blank and private work clears.
- Observation, Python, AI-901, both explanations, review, and explicit
  finalization remain separate responsibilities. Their full conjunction alone
  permits one atomic `rp011_fresh_finalized` checkpoint.
- That checkpoint retains five separate records, keeps RP-007 through RP-010
  hidden-retained, stores no private/transient work, and permits no
  reconciliation yet.

### `UR-30 - REOPEN + RECONCILE + VERIFY + RETURN`

- Only exact verified fresh finalization permits reopening.
- Reopen all four complete prior scopes individually in any of all `24`
  orders; no side-by-side answer matrix and no memory of hidden content.
- Evaluate one separate exact six-method/twelve-`None`-limit reconciliation
  with actually-missed-item-only answer-free remediation and blank retry.
- Explicit review plus save commits one atomic
  `rp011_reconciliation_saved` checkpoint retaining five separate records and
  the separate reconciliation.
- Restore is replay-free, private/transient work is cleared, and deterministic
  focus targets the first incomplete responsibility or saved controls.
- Completion changes no physical or external state and issues no readiness
  verdict.

## Routes, returns, checkpoints, and hard stop

### Permitted returns

- `RETURN TO COUNTERFIELD`: write-free navigation to exact released CF-30 /
  SC-11, preserving all validated bytes and requiring fresh Pilot intent to
  re-enter RP-011.
- `RETURN TO CITY THRESHOLD`: existing reversible navigation only from
  shell-specified safe UR states; it writes no new evidence and opens no
  successor.
- Neither return bypasses unfinished learning, finalizes a record, performs
  reconciliation, or consumes/creates a route token automatically.

Mission must freeze exact per-state return availability and focus. A return
from an invalid or interrupted RP-011 state fails closed to the earliest exact
recoverable boundary without inventing evidence.

### Checkpoints

1. RP-010 released save remains immutable predecessor proof.
2. `rp011_fresh_finalized`: first atomic RP-011 checkpoint; prior scopes still
   hidden-retained; no reopened scopes or reconciliation.
3. `rp011_reconciliation_saved`: final atomic checkpoint; all five records
   separate, four prior scopes reopened, exact reconciliation present.

Both RP-011 writes require canonical serialization, exact read-back, unchanged
predecessor bytes, rollback verification, and replay-free restore.

### Hard stop

Stop at UR-30 verified restore, either exact permitted return, or one inert
LOOK-only outward bearing with `destination=null`, `routeOpened=false`,
`persisted=false`, `evidenceGranted=false`, and `successor=null`.

No RP-012 route, scene, readiness outcome, ending, credits, successor,
RP-013, reward, access, identity, permission, authority, response, or world
effect is reachable.

## Puzzle and learning placement

Operations places but does not alter the already SOLIDIFIED learning:

- `PY-019`: offline API/SDK/endpoint-role evidence; exact primary, trace, and
  fresh transfer.
- `RP011-SINGLE-AGENT-SURFACES-01 / AI901-D2-O4`: distinct portal authoring,
  portal testing, client invocation, and client result-handling surfaces;
  primary, retrieval, fresh transfer, and two boundary explanations.

Science owns exact source freshness, cases, answers, dimensions, thresholds,
evidence schema, privacy, and readiness. Mission owns the final integrated
shell. Operations authorizes no live Foundry/Azure call, credential, endpoint,
resource, network action, or exam guarantee.

## Required dependencies

- released canonical Counterfield normal controller/save/restore/return
  authorities and unchanged predecessor bytes;
- approved RP-011 packet, protected journey, curriculum contract, validator,
  canonical forms, and frozen answers;
- existing observation, evaluator, remediation, evidence, atomic transaction,
  focus, accessibility, Tour-isolation, and return patterns;
- one new normal-only route adapter and one normal-only RP-011 controller,
  renderer, browser persistence adapter, closed storage-free fixture, and
  focused/related/release tests; and
- code-native or already accepted immutable presentation only.

The protected RP-011 module remains reference evidence and cannot be imported
into App/main or used as browser authority.

## Required versus optional scope

### Required

- sole `TD011-RTA-001` route and fail-closed token discipline;
- complete UR-00 through UR-30 ownership path;
- all six observations and both strict learning chains;
- fresh-finalization-before-reopening order;
- all four complete prior scope reopens;
- separate six-method/twelve-limit reconciliation;
- two atomic checkpoints, rollback, restore, recovery, privacy clearing;
- exact returns, hard stop, responsive/accessibility parity, fixture and
  release evidence; and
- honest code-native/immutable-media presentation.

### Optional and not authorized for this slice

- additional observations, lore, branches, hints beyond answer-free recovery,
  side quests, inventory, audio dependency, new images, edited images, boards,
  variants, replacement assets, runtime image imports, cycle reveal, RP-012
  setup, readiness verdict, credits, or ending content.

## Pacing and duration

- Clean first play: `46-64 minutes`.
- Mastered replay: `29-41 minutes`.
- No timer, speed score, timing credit, forced motion, or forced audio.
- Approximate pacing: 2-4 minutes entry/isolation; 7-10 fresh inspection;
  10-14 Python; 15-21 agent-surface learning; 4-6 fresh review/finalization;
  8-11 reopening/reconciliation/save/restore.

The slice is integration-sized because it closes one complete normal packet
from route through verified restore and returns. It is not a micro-seam, and it
does not absorb RP-012.

## Operations validation

| Check | Result |
|---|---|
| Commandant and Colonel authority chain verified | PASS |
| RP-011 is exact nearest mapped successor to released RP-010 | PASS |
| Released normal and protected predecessor versions distinguished | PASS |
| Sole route requires fresh explicit Pilot intent | PASS |
| LOOK, retained scopes, scenery, focus, timing, and Tour cannot route | PASS |
| Invalid/duplicate/stale/private/interrupted state fails closed | PASS |
| Entry writes no evidence and preserves predecessor bytes | PASS |
| Six observations, learning, finalization, reopening, reconciliation, and save remain separate | PASS |
| Two checkpoints are distinct and rollback/replay-free | PASS |
| Returns do not bypass evidence or open later state | PASS |
| UR-30 hard stop excludes RP-012 and ending | PASS |
| Image Specialist/generation/edit/board/import/reveal remain disabled | PASS |
| Focused existing normal/protected tests | `20/20 PASS` |
| Protected user paths and hidden lore untouched | PASS |

## Disposition and handoff

**`FLOOR STACK READY - ROUTE AUTHORIZED`**

Exact `RP-011 / SC-12 / UR-00-UR-30` is the selected contiguous slice. Sole
`TD011-RTA-001` is authorized at planning level only. Construction remains
closed until Science issues a viable envelope and Mission issues a versioned
shell.

### Exact Office of Science Administrator handoff

Read `GDB-TD011-v1`, `WNMP-TD011-v1`, `CFS-TD011-v1`, the final TD-010
release and budget evidence, the released normal Counterfield save/controller,
the full frozen RP-011 curriculum/readiness package, the protected journey and
focused tests, current Microsoft-only source authorities where freshness is
required, and the Science profile. Independently validate the complete route,
learning/evidence, privacy, atomic save/rollback/restore, recovery, responsive/
accessibility/focus, fixture isolation, offline/no-authority, non-generative
presentation, and fresh non-compounding budget envelope. Explicitly reject
protected RP-010/RP-011 versions as normal bridge authority. Issue
`VIABILITY READY`, `REVISE`, or `HOLD`, with a fresh executable budget contract
and exact Mission inputs. Do not write the shell, code, copy, art, board,
image, reveal, or ending content.

## Report envelope

- **Stage / agent:** Operations Planning Major /
  `operations_planning_major`
- **Selected slice:** `RP-011 / SC-12 / UR-00-UR-30`
- **Sole route:** `TD011-RTA-001`
- **Focused validation:** `20/20 PASS`
- **Files changed:** this certificate, stage metrics, current handoff
- **Disposition:** `FLOOR STACK READY - ROUTE AUTHORIZED`
- **Next:** Office of Science Administrator only
