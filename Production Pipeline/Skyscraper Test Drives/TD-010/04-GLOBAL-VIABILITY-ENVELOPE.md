# TD-010 Global Viability Envelope - Counterfield

## Document control

| Field | Value |
|---|---|
| Stage | Office of Science Administrator |
| Agent ID | `office_of_science_administrator` |
| Certificate ID | `VE-TD010-v1` |
| Product brief | `GDB-TD010-v1` |
| World baseline | `WNMP-TD010-v1` |
| Floor stack | `CFS-TD010-v1` |
| Route contract | `TD010-RTA-001` |
| Released predecessor | `TD-009 / SS-RP009-OCCLUDED-FOLD-v1 / OF-30 / SC-10` |
| Selected slice | `TD-010-RP010-COUNTERFIELD-v1` |
| Campaign address | `RP-010 / SC-11 / CF-00-CF-30` |
| Budget authority | `PBA-TD010-v1` |
| Preceding-stage commit | `381fb5175de6490e15f1ae30a0457d0e9c3256df` |
| Disposition | **`VIABILITY READY`** |

Science independently finds the selected floor viable. One integration-sized
normal slice can bridge exact released RP-009 bytes into blank Counterfield,
keep seven observations equal across all `5,040` orders, run strict independent
`PY-018` and `RP010-FOUNDRY-CLIENT-FLOW-01 / AI901-D2-O3` chains with two
explanations, review four separately attributable scopes, commit one
private-free record atomically, restore without replay, and remain within one
fresh non-compounding TD-010 budget.

This certificate authorizes Mission reconciliation only. It does not issue a
shell, create the normal bridge, implement UI or storage, generate or approve
art, inspect Martin's browser/profile/save, or expose RP-011. The existing
`CounterfieldProtectedJourney` is non-routable design evidence. Its protected
RP-009 predecessor version is not the released normal RP-009 record and may
not be imported, aliased, or silently adapted into production.

## Authorities independently read and verified

| Authority | SHA-256 or identity |
|---|---|
| `AGENTS.md` | `DD96BE91D0BD8DACD2A7D0EF7A3949047658F36C577F1A177A9CCABBB2622349` |
| stage-start handoff | `D1AAB91BAA3D16592E30A37B524E116C6F668F5888178C0FB9D903BB39FB46CE` |
| workflow / registry / Science profile | `CA3D2868...` / `78170827...` / `CB5C3A8D...` |
| `TD-010/01-GAME-DEVELOPMENT-BRIEF.md` | `2017337D59AF6E8F9F6A236CDCD8A3E474AAEC02462B6D5BFF30A875266FC527` |
| `TD-010/02-WORLD-NARRATIVE-MASTERPLAN.md` | `B76209F2161ED320E010FCAC9F39AC72044CD23E6E0FDE3D977A384D25BA38B1` |
| `TD-010/03-CAMPAIGN-FLOOR-STACK.md` | `C84792D8CD5F4C4F4FC213669E45FA30BBE5911662FA1E248216132A1975DBAB` |
| TD-009 Intelligence release | `48AB535B26BAC369620EF42E6D5985785160FA450836482FB5E51589BD0E8648` |
| frozen RP-010 packet | `AB533EEA2F2C85F84C8781AEF8D1C2FB808E76D1AA1D87C52CE1C525F455455A` |
| RP-010 curriculum contract | `367B540AC2E5C176565555FBF6729CF850598E0410602AA46331E29BF0A59E71` |
| RP-010 validator | `1CF60FF4564C4E68FA32C05A59E45A18D012FA8778CC231DEE3CFB315CA089F2` |
| protected RP-010 journey | `541A28EA41EEC1E294A167673338369427D1430F44BA1A65D2892A44F3B8EF03` |
| protected RP-010 tests | `5988A6D8A71EB0AFF5F17D19B7548A970D04C312763FC530F1D724C9DF45A47C` |
| released RP-009 normal authority | `9EA5924056CB131901C259ADD3207DF88C6CB0E0EF23C3D3D750A32A2EDF32A0` |
| released RP-009 normal tests | `CEED5692CA3DE50287F510C666B787B604F86534E6832CA5DC4E5705F30052AD` |
| `SC-11` scene authority | `A87286852ABDB4E4DA2944B9F6BABB344276708DE5161C9340347EB15B640992` |
| executable budget validator | `DDB51C0198C8778E5774233BDFE06F2EC922F47035A748188CFEB46F6BEBA841` |

The Operations commit is exact `HEAD`, has sole parent
`8fcef9b2509faccd88ddd8154d45de8368f7b2a5`, and changes only the floor stack,
stage ledger, and synchronized handoff. Current curriculum, gameplay,
readiness, architecture/state, normal RP-009 storage, privacy, accessibility,
tests, RP-010 packet/puzzle/protected journey, SC-11 asset authority, budget
patterns, and latest TD-009 variances were inspected directly. Hidden lore,
protected user files, and Martin-owned browser/profile/save were not opened or
inspected.

## Official Microsoft source review

The repository-mandated `foundry-azure-source-priority` skill is unavailable
in this session. Science followed the required fallback: local official
Microsoft captures first, then current `learn.microsoft.com` verification.
No third-party source was used.

- The current [AI-901 study guide](https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/ai-901),
  last updated 2026-07-13 and listing skills measured from 2026-04-15, still
  includes creating a lightweight chat client with the Foundry SDK.
- The current [Foundry SDKs and endpoints overview](https://learn.microsoft.com/en-us/azure/foundry/how-to/develop/sdk-overview)
  keeps project-client construction from project endpoint and credential,
  derivation of an OpenAI-compatible client, `responses.create` model/input
  submission, and returned `response.output_text` handling as distinct steps.
- The [Foundry documentation hub](https://learn.microsoft.com/en-us/azure/foundry/),
  [Agent Service overview](https://learn.microsoft.com/en-us/azure/foundry/agents/overview),
  and [Content Understanding overview](https://learn.microsoft.com/en-us/azure/ai-services/content-understanding/overview)
  describe real cloud resources, authenticated endpoints, managed agent
  capabilities, and multimodal extraction. TD-010 performs none of them.

The frozen `AI901-D2-O3` mapping remains supportable as an offline,
course-authored client-flow responsibility exercise. It makes no Microsoft
authorship, exam-item, service execution, credential, endpoint, resource,
permission, output-truth, external-action, certification, or exam-guarantee
claim. Reopen only if official objectives materially change, a validator or
playtest proves a mismatch or bypass, or Martin changes the learning goal.

## Released-versus-protected predecessor boundary

The released normal predecessor is exactly:

- save key `horizon-archive-rp009-occluded-fold-save-v1`;
- version `rp009.occluded-fold-save.v1`;
- controller `rp009.occluded-fold-controller.v1`;
- shell `SS-RP009-OCCLUDED-FOLD-v1`;
- checkpoint `occluded_fold_complete` at `OF-30 / SC-10`; and
- one canonical 12-key raw/object record accepted by
  `sanitizeOccludedFoldSave` with null deltas and `successor=null`.

The protected RP-010 reference instead calls
`deriveOccludedFoldResume` and requires protected predecessor version
`rp009.protected-journey.v1`. That object is useful design evidence only. A
normal TD-010 bridge must read the released raw string, parse once, run the
released normal sanitizer, require `raw === JSON.stringify(sanitizedObject)`,
and copy its three bounded scopes only through new normal sanitizers. It must
not accept the protected version, call a protected adapter, import
`CounterfieldProtectedJourney`, or substitute protected test fixtures for
released bytes.

The released OF-30 LOOK remains inert with `destination=null`,
`routeOpened=false`, and `successor=null`. Released returns to Offset Reach and
City Threshold remain write-free and replay-free; neither is an alternate
Counterfield entry.

## Sole normal route and byte-proof contract

`TD010-RTA-001` is the only normal transition. It belongs to a distinct
`PILOT // EXPEDITION NAVIGATION` route group at exact verified OF-30. Mission
must freeze its route controller, owner/group, action, modality, and token
identities separately from TD-009 and all protected identities.

Before consuming a token, the normal adapter validates in order:

1. Campaign mode, rejecting Demo Tour before any campaign read or mutation.
2. Exact released TD-009 shell, controller, OF-30 state, SC-10 scene, active
   owner/group, null deltas, null successor, and inert outward LOOK.
3. Exact fresh Pilot route group, allowlisted action, and one of the seven
   supported modalities.
4. One syntactically safe opaque one-hit token containing no private or answer
   payload and unused by any prior event.
5. One current raw read of the released RP-009 key, one parse, one released
   `sanitizeOccludedFoldSave` call, canonical raw/object equality, exact
   version/packet/mapping/checkpoint/continuation, exact retained RP-007 and
   RP-008 summaries, exact RP-009 edge ledger/eight evidence records, null
   deltas, and `successor=null`.
6. Current raw snapshots for exact TD-004, TD-005, TD-006, TD-007, TD-008,
   and TD-009 keys; every object is independently sanitized by its released
   normal authority and every raw string equals its canonical object bytes.
7. Absence or clearing of learner source, request/response replicas, derived
   exchange, cases, choices, answers, reasoning, feedback, hints,
   diagnostics, credentials, secrets, endpoints, headers, payloads,
   responses, identity data, token/focus/pointer history, temporary files,
   external-action requests, and Tour state.
8. One atomic zero-credit mount of genuinely blank `CF-00 / SC-11`.

Only then is the route token consumed. Missing, invalid, duplicate, stale,
forged, combined, private, contaminated, wrong-owner/group/action/modality,
Tour-derived, protected-version, changed-byte, partial, or interrupted intent
fails closed to exact OF-30 without write, replay, SC-11 exposure, or token
expenditure. Interruption before consumption restores OF-30. Interruption
after blank CF-00 but before verified save clears all candidate work, restores
OF-30, and requires a fresh intent.

## Normal identities and state graph

Mission must freeze distinct normal identities at or more specific than:

- save key `horizon-archive-rp010-counterfield-save-v1`;
- record version `rp010.counterfield-save.v1`;
- controller `rp010.counterfield-controller.v1`;
- route controller `td010.route-controller.v1`;
- route group `td010-counterfield-route`; and
- fixture `td010-counterfield-v1`.

None may alias `rp010.protected-journey.v1` or protected RP-009 identities.

```text
OF-30 COUNTERFIELD ROUTE CHOICE
  -> CF-00 ARRIVE + ORIENT / SC-11-00
  -> CF-10 SURVEY SEPARATE DISTRICTS / SC-11-10
  -> CF-20 BOUND EXCHANGE + SAVE / SC-11-20
  -> CF-30 VERIFY + RETURN / SC-11-30
```

State-to-scene identity is exact. No scene phase may mount early, no
observation or learning result changes a master, and CF-30 retains the same
registered world truth with no completion effect.

## Seven equal observations and deterministic recovery

The exact observation IDs are:

```text
recurrent_adjacency
incomplete_ordered_change
cross_scale_correspondence
ordinary_unmatched_feature
multi_candidate_relation
unavailable_margin
layered_stewardship
```

All `7! = 5,040` orders converge; revisits are idempotent. No pair, rank,
map, matching, common-clock, topology, chronology, cause, shared purpose, or
failure mechanic is permitted. Crop, scale, position, color, brightness,
motion, sound, timing, focus, modality, confidence, save state,
accessibility behavior, Tour, and world state grant zero observation credit.

After all seven observations, deterministic first-incomplete ownership is:
PY-018 primary, trace, transfer; client-flow primary, retrieval, transfer;
client-flow-boundary explanation; truth/authority explanation; four-scope
review; save. A partial sanitized prefix may identify only the first
incomplete scored boundary. Physical observations are re-observed after an
unsaved interruption, and no raw or private work survives.

## Strict independent learning and evidence firewall

### PY-018 request/response boundary

The frozen chain is exact `8/8 primary -> actually-failed-check-only
answer-free remediation -> delayed closed-note 8/8 trace -> genuinely blank
8/8 transfer`. The learner derives one bounded exchange summary from supplied
sanitized request and response replicas while preserving:

- request method, route label, content type, record IDs, provenance, and
  scope as request-owned;
- response status, content type, and the separate supported,
  counterevidence, ambiguous, and unavailable classes as response-owned; and
- `identity`, `topology`, `continuity`, `transformation`, `unity`,
  `synchronization`, `chronology`, `cause`, and `purpose` as nine `None`
  limits.

The browser path may use only pure deterministic approved-source-shape
validation over supplied session-only replicas. It may not use `eval`, the
`Function` constructor, arbitrary Python, Python/WASM, Worker, import,
network, SDK, endpoint, credential, file/path, browser persistence,
clipboard, download, package, process, or external input. Required label:

> The course validator checks the approved Python source shape and derives the
> bounded exchange summary from session-only sanitized request/response
> replicas. It does not execute arbitrary Python, contact a network or
> Foundry service, use a credential or endpoint, or perform an external
> action.

Primary and transfer use different records and blank forms. A miss exposes
only actually failed public check IDs, clears private/transient work, opens a
wholly blank unlimited retry, announces one complete System status, and
focuses the first failed control.

### RP010-FOUNDRY-CLIENT-FLOW-01 / AI901-D2-O3

The independent chain is exact `8/8 primary -> per-case/per-dimension
answer-free remediation from actually scored misconception tags -> delayed
closed-note 8/8 retrieval -> genuinely blank 8/8 transfer`, plus two
separately scored explanations:

1. project-client setup, compatible-client derivation, Responses API
   model/input submission, and returned-output processing are distinct; and
2. a valid client flow neither proves output truth nor authorizes a live or
   external action.

All cases are neutral course-authored text. SC-11, retained scopes, scene
evidence, inferred weakness, success display, and save state cannot select a
case, answer, or remediation. No model, agent, endpoint, credential, SDK, API,
tool, resource, content, network, or live request occurs.

The curriculum contract's record `mappingId` remains exactly
`RP010-REQUEST-CLIENT-BOUNDARY-01`; the independently scored AI record ID is
exactly `RP010-FOUNDRY-CLIENT-FLOW-01`. Mission must not collapse or rename
those two authorities.

### No cross-credit and four scopes

The seven observations, three PY-018 records, three client-flow records, two
explanations, four-scope review, and save are separate conjuncts. No conjunct
may be inferred, compensated, repaired, or finalized by another. Route,
presentation, focus, timing, modality, confidence, accessibility, Tour,
scene, world, save/restore display, and retained predecessor completion remain
zero credit.

The review order is fixed and separately attributable:

1. retained read-only RP-007 bounded scope;
2. retained read-only RP-008 bounded scope;
3. retained read-only RP-009 bounded scope; and
4. candidate RP-010 bounded scope, available only after the full current
   conjunction.

Review and save may preserve all four; they may not replay, overwrite, erase,
merge, correct, reinterpret, universalize, or cross-credit any scope.

## Privacy, durable record, and sanitation

The normal durable record has exactly 13 root keys in this order:

```text
version, packetId, mappingId, checkpoint, continuation,
cityStateDelta, externalStateDelta, successor,
retainedRp007Summary, retainedRp008Summary, retainedRp009Ledger,
scopeRegister, evidence
```

Fixed values are `rp010.counterfield-save.v1`, `RP-010`,
`RP010-REQUEST-CLIENT-BOUNDARY-01`, `counterfield_complete`,
`continuation="continuation"`, null city/external deltas, and
`successor=null`. Retained scopes are copied only through released normal
sanitizers and remain ordered, named, read-only, and byte-verifiable.

`scopeRegister` contains exactly `observations` and `exchange`. Observations
canonicalize to the seven IDs above. Exchange preserves the exact request,
response, four evidence classes, and nine `None` limits from the accepted
primary record. `evidence` contains exactly eight ordered finalized records:
PY-018 primary/trace/transfer; client-flow primary/retrieval/transfer; then
the two explanations. Evidence fields are limited to packet, mapping, form,
skill/objective ID, dimension correctness, bounded attempt count, bounded
hint level, optional bounded confidence, actually scored allowlisted tags,
and mastery status.

Never persist or log learner source/fragments; request or response replicas;
derived exchange; raw cases; choices; answers; reasoning; feedback; hints;
diagnostics; private notes; identity data; credentials; secrets; endpoints;
headers; payloads; responses; source content; prompts; exam items; focus,
pointer, route-token, or timing history; unavailable contents; external-action
requests; or Tour state. Clear them on miss, retry, group replacement, early
return, interruption, rejection, failed write, invalid restore, successful
save, verified restore, and Tour exit.

## Atomic save, rollback, restore, and returns

One explicit normal save must:

1. retain prior RP-010 raw bytes or verified absence;
2. snapshot exact canonical TD-004 through TD-009 raw bytes;
3. sanitize the 13-key candidate in memory and reject unless every conjunct,
   key order, value, scope, and evidence allowlist passes;
4. write exactly one canonical RP-010 serialization;
5. read raw bytes back once, parse, re-sanitize, and require exact canonical
   raw/object equality;
6. reread and prove every TD-004 through TD-009 byte unchanged;
7. on any failure restore prior RP-010 bytes or absence and verify exact byte
   equality together with all predecessor bytes; and
8. enter an inert `HOLD` with only safe returns if rollback cannot be verified.

Only exact verified bytes restore `CF-30 VERIFY + RETURN / SC-11`. Restore is
heading-first, private-free, and replays no route, arrival, observation,
evaluator, request/response derivation, case, explanation, review, save,
animation, sound, clock, focus, or world event. Invalid or absent candidate
state clears candidate work and reconstructs exact OF-30.

`RETURN TO OCCLUDED FOLD` lands only at exact released OF-30. `RETURN TO CITY
THRESHOLD` lands only at the released City Threshold anchor. Both are
write-free, replay-free, reversible, clear unfinished work, preserve all
predecessor bytes and null deltas, and grant no evidence or authority. After
return to OF-30, its separately released Offset Reach return remains available
under unchanged TD-009 authority. No direct Counterfield-to-Offset-Reach
shortcut exists.

## Accessibility, focus, responsive, and rendered fixture contract

- Pointer, touch, keyboard Enter, keyboard Space, switch, speech, and screen
  reader converge on one semantic action. Validation precedes consumption;
  repeat and multi-event bursts remain one-hit.
- Exactly one owner/message/content/action group is active. Normal replacement
  focuses its heading. Recovery announces one complete polite atomic
  owner-prefixed System status, then focuses the first invalid or incomplete
  control. Save confirmation contains focus until save, cancel, or Escape.
- All required targets are at least `44 x 44 CSS px`. Native semantics,
  persistent labels, assistive names, and associated help/error text remain
  complete. DOM order equals reading and focus order.
- Meaning never depends on district, position, scale, color, brightness,
  motion, sound, timing, sequence, layout, silhouette, or focus alone.
- Forced colors use system colors, boundaries, selection, and visible focus.
  Reduced motion uses registered still/direct equivalents and
  `scroll-behavior:auto`. Grayscale preserves every observation and state by
  structure, geometry, and text.

Required DPR-1 matrix:

| Layout | Requirement |
|---|---|
| desktop | `1920 x 1080`; complete `100dvh` shell, dominant world, active group and required actions visible, no outer scroll |
| laptop | `1366 x 768`; no horizontal escape; internal scroll evident and keyboard reachable |
| narrow | `390 x 844`; world above one natural interface column; no clipping or horizontal escape |
| effective 200% | `768 x 900` CSS viewport; natural reflow; complete labels, errors, status, scopes, and actions contained |

Mission freezes exact UTF-8 owner, heading, status, focus target, longest
label, longest error, longest retained-scope row, and execution label for each
state. Those strings render at all four layouts.

Create exactly one closed fixture, `td010-counterfield-v1`, absent from
production. It accepts only a versioned, public, private-free allowlist of
exactly 66 scenarios: 13 route/entry, 4 CF checkpoints, 7 observations, 18
learning boundaries, 8 privacy/recovery, 8 review/save/return, and 8
presentation states. Route coverage includes a named protected-RP-009-version
rejection. Every scenario contract declares owner and focus; browser fixture
tests must compare the declared owner with rendered owner text and the
declared focus with actual `document.activeElement.id` for all 66, not merely
source-state strings. One status region and one product main landmark are
allowed. Harness controls/output remain semantically and visually separate.

The exact public IDs are:

```text
route_pointer, route_touch, route_keyboard_enter, route_keyboard_space,
route_switch, route_speech, route_screen_reader,
route_invalid_released_predecessor, route_protected_predecessor_rejected,
route_invalid_intent, route_duplicate_or_stale, route_private_contaminated,
route_tour_isolated,
cf00_arrive, cf10_survey, cf20_exchange_save, cf30_restore,
observation_recurrent_adjacency, observation_incomplete_ordered_change,
observation_cross_scale_correspondence, observation_ordinary_unmatched_feature,
observation_multi_candidate_relation, observation_unavailable_margin,
observation_layered_stewardship,
python_primary_blank, python_primary_miss, python_primary_pass,
python_trace_blank, python_trace_pass,
python_transfer_blank, python_transfer_pass,
client_primary_blank, client_primary_miss, client_primary_pass,
client_retrieval_blank, client_retrieval_pass,
client_transfer_blank, client_transfer_pass,
client_flow_explanation_blank, client_flow_explanation_pass,
truth_authority_explanation_blank, truth_authority_explanation_pass,
resume_contiguous_prefix, resume_first_gap, interruption_before_consumption,
interruption_after_cf00_pre_save, private_work_cleared,
request_response_work_cleared, tour_work_cleared, atomic_polite_status,
review_ineligible, review_four_scopes, save_committed,
save_rollback_verified, restore_replay_free, return_occluded_fold,
return_city_threshold, continuation_inert,
layout_desktop, layout_laptop, layout_narrow, layout_effective_200,
mode_forced_colors, mode_reduced_motion, mode_grayscale,
longest_copy_contained
```

The fixture rejects additions, omissions, duplicates, arbitrary JSON,
query/hash injection, campaign adapters, browser storage, IndexedDB, service
workers, network, clipboard, file pickers, and Martin-owned state. Its source,
identity, scenario IDs, routes, and emitted chunks must be absent from the
production source graph and bundle.

## SC-11 asset, provenance, and bounded generation obligations

At most two direct SC-11 runtime image roles may be authorized:

1. `SC-11-COUNTERFIELD-PANORAMA-MASTER` - invariant panoramic world identity;
2. `SC-11-DISTRICT-DETAIL-MASTER` - spatially consistent continuous-landscape
   detail.

Lossless 16:9 sources are preferably generator-native `6144 x 3456` or larger
and never below `3840 x 2160`. Each role requires stable direct import,
source/runtime byte and SHA-256 identity, complete prompt/generator/model/
reference/edit/selection/derivative provenance, truthful native and output
dimensions, registered crops/masks/effects, exact state/crop/alternative-text
assignment, responsive and hotspot proof, grayscale/forced-color/
reduced-motion/thumbnail review, artifact cleanup, and explicit placeholder
retirement.

Original-resolution QA must prove physically separate districts; seven equal
non-diagram observations; ordinary unmatched and multi-candidate evidence;
one unavailable margin without access affordance; layered stewardship;
independent clocks; one sealed replica-only coupling; and invariant world
geometry. Reject cross-field connectors, grids, maps, paths, matching lines,
rank, answer keys, synchronized emissions, access/route affordances, readable
writing, baked UI, protagonist, ship, human trace, world response, and RP-011
cues. Deterministic enlargement is disclosed and never called native.
Contextual registration is not a literal crop unless pixel identity proves it.

The cycle reveal remains exactly one accepted spoiler-safe candidate.
Generation attempt budget is one initial private generation. Only a documented
Image Specialist QA rejection before workspace entry permits one targeted
recovery; a second failure is `HOLD` and no third attempt exists. Rejected
bytes remain outside workspace, canon, runtime, provenance, commits, and the
user-facing reveal. Reveal acceptance does not itself prove runtime
integration; each runtime role still requires its own complete gates.

## Offline, Tour, authority, and invariant-world boundary

No account, sign-in, identity, credential, Microsoft/Azure/Foundry resource,
endpoint, API, SDK, model, agent, tool, request, response, upload, network,
external content, or external action is required or authorized. All replicas
and cases are course-authored and local. The slice grants no access,
permission, identity, authority, response, service truth, readiness judgment,
certification, or exam guarantee.

Demo Tour rejects before campaign storage and creates no route, token,
observation, learning, remediation, save, mastery, scope, world, authority, or
successor state. SC-11 geometry, materials, light, sound, pressure,
maintenance, and all local clocks remain invariant through observation,
learning, execution, review, save, leave, return, and restore.

## Fresh non-compounding production budget

`PBA-TD010-v1` rebaselines once from exact accepted TD-009 output, not from
unused TD-009 headroom. Fresh production evidence reproduces the released
identity: one JavaScript chunk `1,509,968` bytes, one CSS chunk `104,035`
bytes, `206` modules, and 17 runtime media files totaling `37,410,731` bytes.

| Measure | TD-010 cap | Fresh headroom |
|---|---:|---:|
| aggregate JavaScript | `1,585,466` bytes | `75,498` bytes |
| aggregate CSS | `109,236` bytes | `5,201` bytes |
| production modules | `216` | `10` |
| new runtime images | `2` | `2` |
| new runtime image bytes | `8,388,608` | `8,388,608` |
| total runtime media | `45,799,339` bytes | `8,388,608` |

All emitted chunks and all new runtime assets count. The accepted 17 media
hashes must remain present. No new audio, font, video, source-map payload,
Python/WASM runtime, service worker, or network-fetched payload is permitted.
Build cap is 60s; focused/related/full suites 30/60/60s; complete E2E 180s;
sampled main-thread task 100ms. Any overage returns to Science and Mission;
quality and contract gates cannot be weakened to meet a cap.

At the Operations commit, a fresh build transformed `206` modules in `6.83s`
(`8.313s` wall) and reproduced exact released TD-009 JavaScript, CSS, and all
17 media identities. The reusable executable validator must pass baseline at
Mission and candidate/release modes later.

## Validation ladder and fresh evidence

| Gate | Science result |
|---|---|
| Predecessor commit, parent, dedicated file set | PASS |
| Focused released-normal RP-009 plus protected RP-010 | `20/20 PASS`; Node `1.425s`; wall `1.486s` |
| All `5,040` observation orders and revisits | PASS in protected executable evidence |
| RP-010 mapping self-test | PASS: request/response boundary, client flow, scope locks, bypass probes |
| Current readiness validators | `15/15 PASS`; wall `2.073s` |
| Full product suite | `905/905 PASS`; Node `10.303s`; wall `11.800s` |
| Fresh production build | PASS; `206` modules; Vite `6.83s`; wall `8.313s` |
| Exact released production identity | JS `1,509,968` / `4929...11A3`; CSS `104,035` / `1C24...EFB`; media `17 / 37,410,731` PASS |
| Protected/reference isolation | protected RP-010 absent from App/main/storage/bundle; protected RP-009 version remains non-normal PASS |
| Planning-only boundary | no shell, route implementation, fixture, storage, asset, generation, or browser/profile/save work performed PASS |

Mission contract checks must prove all named identities, state/scene mapping,
13-key order, evidence order, fixture count/IDs, asset roles, source claims,
and hard stops. Combat focused tests exhaust route ordering, all seven
modalities, token one-hit semantics, released/protected predecessor rejection,
all `5,040` orders/revisits, learning chains and bypasses, no-cross-credit,
sanitation, four scopes, atomic write/read-back/rollback, TD-004-TD-009 byte
stability, replay-free restore, returns, Tour, fixture isolation, invariant
world, and CF-30 absence.

Related regression covers TD-004 through TD-009 records, controllers, routes,
returns, media, privacy, persistence, fixtures, and exact OF-30 behavior.
Release requires the full suite; all readiness validators; production and
fixture builds; `PBA-TD010-v1` release mode; exact served root/deep/file/chunk/
media identity; source/bundle leakage and RP-011 absence scans; one complete
non-overlapping E2E; all 66 rendered owner/focus contracts; four-layout,
forced-color, reduced-motion, grayscale, target, longest-copy, local-request,
runtime-log, and sampled-main-thread review; QA restoration; stable committed
candidate freeze; patch/object integrity; cleanup; and independent
Intelligence reconciliation.

No live visual, assistive-technology, normal Counterfield, browser storage,
served candidate, E2E, or final-art result is claimed at Science.

## Risks, controls, and variances

| Risk | Control / return owner |
|---|---|
| Protected RP-009 reference silently becomes normal predecessor | Exact released version/raw/object gate; named protected-version rejection; return to Science/Operations |
| OF-30 LOOK or a released return becomes entry | Sole fresh `TD010-RTA-001`; LOOK and returns unchanged; return to Operations |
| Seven observations become map, rank, clock, or identity | Equal-peer all-order tests, source/runtime/text/asset scans; canon conflict to Colonel |
| Request and response ownership collapses | Strict PY-018 checks and separate trace; return to Science |
| Valid client flow is presented as truth or authority | Independent explanation and no-live-action boundary; return to Science |
| Retained records merge or cross-credit | Four ordered named scopes, separate conjunction, exact predecessor bytes |
| Save failure corrupts a prior packet or candidate | Canonical read-back, TD-004-TD-009 proof, verified rollback, hard HOLD on unverifiable rollback |
| Fixture reports source strings rather than live product truth | Render production surface; compare declared owner/focus to DOM text/activeElement for 66/66 |
| Long copy or four scopes break layout | Freeze exact UTF-8 strings and render at all four layouts |
| SC-11 implies a connector, answer, access, or successor | Original-resolution required/forbidden QA, registered crops, hard absence scans |
| Budget compounds from prior allowance | One-time 5% rebaseline from exact accepted TD-009 output only |

Latest TD-009 `TD009-FUNC-001/002/003`, `TD009-CONT-002`, and
`TD009-FIX-001/002` are resolved and become regression requirements. Historical
`TD009-PROC-001`, `TD009-PRES-001`, and `TD009-CONT-001` remain TD-009-only
deferred limitations; none relaxes TD-010. No new formal variance is required.

## Flexible implementation areas and hard stop

Mission may choose compliant copy, pacing, component factoring, visual
hierarchy, local simulation organization, focus wording, and at most the two
approved SC-11 image treatments. These choices may not change route order,
normal/protected identity, evidence, observation equality, first-incomplete
order, save shape, scope order, privacy, accessibility, fixture obligations,
asset roles, budget caps, returns, canon, authority meaning, or hard stop.

Hard stop is `CF-30 VERIFY + RETURN / SC-11`, exact Occluded Fold or City
Threshold returns, and one inert LOOK-only field-margin continuation with
`destination=null`, `routeOpened=false`, zero persistence/evidence, and
`successor=null`. No RP-011 name, location, event, scene, objective, route,
token, asset, cue, focus target, save field, unavailable contents,
reconciliation, readiness judgment, access, reward, permission, identity,
authority, response, Machine relation, world effect, RP-013, successor,
ending extension, or post-ending content may mount, render, announce,
persist, or become focusable.

## Disposition

**`VIABILITY READY`**

`VE-TD010-v1` and `PBA-TD010-v1` are the complete Science contract for Mission
reconciliation. No Science-owned contradiction, missing utility, or
unacceptable risk prevents one integration-sized shell. Normal integration,
browser persistence, fixture construction, production art, served identity,
live review, and release evidence remain downstream work.

## Exact Mission Captain handoff

Run **Mission Captain / `mission_captain` only**. Reconcile
`GDB-TD010-v1`, `WNMP-TD010-v1`, `CFS-TD010-v1`, this `VE-TD010-v1`, and
`PBA-TD010-v1` into one conflict-free versioned shell for exact
`RP-010 / SC-11 / CF-00-CF-30` and sole `TD010-RTA-001`, or issue
`REVISE`/`HOLD`.

Freeze the released-versus-protected RP-009 distinction; exact released
raw/object and TD-004-TD-009 byte proof; normal IDs; route validation before
token consumption; CF state-to-scene identities; seven equal observations/all
`5,040` orders; strict independent PY-018 and client-flow chains plus both
explanations; mapping/check ID distinction; no-cross-credit; scored-miss-only
blank recovery; four ordered scopes; 13-key record; privacy clearing; atomic
write/read-back/rollback; replay-free restore; exact returns; seven
modalities; deterministic status/focus; four layouts; exact UTF-8 copy;
66-scenario storage-free production-absent fixture with rendered owner/actual
focus proof; two conditional SC-11 roles with provenance/original-resolution
QA; one-initial/one-recovery generation boundary; offline/no-authority/
no-exam-guarantee; PBA gates; served identity; stable-candidate and validation
ladder; and CF-30 hard stop.

Do not implement, generate assets, deploy Marines, inspect browser/profile/
save, expose RP-011, or issue `SHELL READY` unless all five Colonel contracts
reconcile without conflict.

## Report envelope

- **Systems/evidence:** one new normal adapter/controller/storage boundary;
  protected journey remains reference-only; strict independent learning and
  four scopes are conjunctive without cross-credit.
- **Privacy/save/recovery:** exact allowlists, private/transient clearing,
  TD-004-TD-009 byte stability, canonical read-back, verified rollback, and
  replay-free restore are mandatory.
- **Accessibility/performance:** seven modalities, `>=44px`, deterministic
  owner/status/focus, four layouts, assistive modes, and 66 rendered contracts
  fit `PBA-TD010-v1`.
- **Validation:** focused `20/20`, readiness `15/15`, full `905/905`, fresh
  206-module build, and exact TD-009 production identity pass.
- **Official sources:** local and current Microsoft-only authorities support
  `AI901-D2-O3`; unavailable skill documented; no third party used.
- **Variances:** none; TD-009 image waiver does not carry forward.
- **Files:** this envelope, `04A`, stage metrics, and synchronized handoff.
- **Synchronization:** dedicated local Science commit; no push at this gate.
- **Next role:** Mission Captain.
