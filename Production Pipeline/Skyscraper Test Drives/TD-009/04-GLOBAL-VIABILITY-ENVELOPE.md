# TD-009 Global Viability Envelope - Occluded Fold

## Document control

| Field | Value |
|---|---|
| Stage | Office of Science Administrator |
| Agent ID | `office_of_science_administrator` |
| Certificate ID | `VE-TD009-v1` |
| Product brief | `GDB-TD009-v1` |
| World baseline | `WNMP-TD009-v1` |
| Floor stack | `CFS-TD009-v1` |
| Route contract | `TD009-RTA-001` |
| Released predecessor | `TD-008 / SS-RP008-OFFSET-REACH-v1 / OR-30 / SC-09` |
| Selected slice | `TD-009-RP009-OCCLUDED-FOLD-v1` |
| Campaign address | `RP-009 / SC-10 / OF-00-OF-30` |
| Budget authority | `PBA-TD009-v1` |
| Preceding-stage commit | `b3e1f7f461f05766b88278784ec05b1be7c0c615` |
| Disposition | **`VIABILITY READY`** |

Science independently finds the conditional floor viable. Normal construction
can validate one fresh Pilot intent only after the exact released RP-008 record
and all predecessor bytes pass, keep six observations equal across all `720`
orders, run strict independent PY-017 and prompt-boundary chains over sanitized
session-only replicas, save one private-free ledger atomically, restore three
ordered records without replay, and fit a fresh non-compounding TD-009 budget.

This certificate authorizes Mission reconciliation only. It does not issue a
shell, deploy Marines, implement a route, generate or approve art, inspect
browser storage, or expose RP-010. `OccludedFoldProtectedJourney` remains
non-routable design evidence and may not be imported into App, main, normal
routing, browser persistence, the closed fixture, or the production bundle.

## Authorities independently read

| Authority | SHA-256 or identity |
|---|---|
| `AGENTS.md` | `DD96BE91D0BD8DACD2A7D0EF7A3949047658F36C577F1A177A9CCABBB2622349` |
| stage-start handoff | `4CD8D715E1BC6D52DD09A40434D40703E4D48534693412FBEEE5D5AAB4CFDEAA` |
| workflow / registry / Science profile | `CA3D2868...` / `78170827...` / `CB5C3A8D...` |
| `TD-009/01-GAME-DEVELOPMENT-BRIEF.md` | `250C54366F59F6F7C43EEA2697558EFD94E3FDC71B853D18BC95FA7792328DB2` |
| `TD-009/02-WORLD-NARRATIVE-MASTERPLAN.md` | `4EACEF4EEE4FCEB0F92EAB646097F8A414F982DDA0A7214A022BED94CE3AB137` |
| `TD-009/03-CAMPAIGN-FLOOR-STACK.md` | `4B6C2D4CBA8DEB958B36788AF0C085E8A6A7F7026B5F9DE1AD41AE7D9E125367` |
| TD-008 Intelligence release | `10A6EC562D92AAC423930D0CF21A340CFF8EBE29ADF5277E001F4D5F04A6D494` |
| frozen RP-009 packet | `826BAD86BF088B5DF3C822BDFFE66DE5801EAB539CF0781330DFB84D15374903` |
| RP-009 curriculum contract | `F2C983A9B1B16601BF3DDDEE19303734E2392D75C0FC50D05BAD057C5C3050AC` |
| RP-009 validator | `5C0CA230357784B1DDBFDC9ED009511BF68BD52673D5BB49A1D9E97F85550E0E` |
| protected RP-009 journey | `5D2A3A1F04D6DED391139E2912E7B6D37A65FE25D50223900D8AF7AD75DC5B04` |
| protected RP-009 tests | `CDC1A01BB5BAF123649F8B26F34369BF43A4D83E9A754763C9D8B5543EFE5C0F` |
| executable budget validator | `DDB51C0198C8778E5774233BDFE06F2EC922F47035A748188CFEB46F6BEBA841` |

The current curriculum, gameplay-systems, and production-readiness controls,
the exact RP-008 save/controller boundary, current App/main/bundle absence,
privacy allowlists, accessibility metadata, save adapters, tests, and current
production output were inspected directly. The hidden-lore vault, Martin's
browser/profile/save, and protected user files were not opened or inspected.

## Official Microsoft source review

The repository-mandated `foundry-azure-source-priority` skill is unavailable
in this session. Science used the prescribed fallback: current official
Microsoft sources first, then frozen repository authority. No third-party
source was used.

- The [Microsoft Foundry documentation hub](https://learn.microsoft.com/en-us/azure/foundry/)
  presents Foundry as the platform for apps, models, agents, tools,
  observability, evaluation, and governance.
- The [SDKs and endpoints overview](https://learn.microsoft.com/en-us/azure/foundry/how-to/develop/sdk-overview)
  separates Foundry project, OpenAI-compatible, Agent Framework, and
  tool-specific endpoints and requires real resource configuration and
  authentication for service use.
- The [Agent Service overview](https://learn.microsoft.com/en-us/azure/foundry/agents/overview)
  defines agents through models, instructions, and tools and describes managed
  endpoints, identity, Responses API calls, state, tools, and observability.
- The [Content Understanding overview](https://learn.microsoft.com/en-us/azure/ai-services/content-understanding/overview)
  describes real multimodal content processing and structured extraction in
  Foundry Tools; TD-009 performs none of those service operations.
- The [AI-901 study guide](https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/ai-901),
  checked 2026-08-01, lists skills measured as of April 15, 2026 and explicitly
  includes creating effective system and user prompts under Foundry
  implementation.
- Microsoft's [system message design guidance](https://learn.microsoft.com/en-us/azure/foundry/openai/concepts/advanced-prompt-engineering)
  assigns persistent role, boundary, safety, and output-format guidance to the
  system message and states that such guidance influences but does not
  guarantee model compliance.

The frozen `AI901-D2-O1` mapping remains supportable as an offline,
course-authored prompt-ownership and deciding-signal exercise. It makes no
Microsoft authorship, current-service execution, resource, endpoint,
deployment, credential, permission, external-action, certification, or exam
guarantee claim. Reopen only if official objectives materially change or a
validator/playtest proves a mismatch or bypass.

## Route, predecessor, and sanitation viability

`TD009-RTA-001` is the sole normal transition. It is owned by
`PILOT // EXPEDITION NAVIGATION` at exact verified
`OR-30 VERIFY + RETURN / SC-09`. Mission must freeze distinct normal route,
controller, group, action, and one-hit token identities; none may alias the
protected journey.

Before token consumption the adapter must validate, in order:

1. Campaign mode, rejecting Demo Tour before any campaign read or mutation.
2. Exact released shell/controller/state and active OR-30 boundary.
3. Exact Pilot route-owner group, allowlisted action, and supported modality.
4. One syntactically safe opaque fresh token, with no answer/private payload.
5. One strict current read of the exact RP-008 record version, packet,
   mapping, checkpoint, continuation, null deltas, null successor, ordered
   retained RP-007 summary, bounded note, and eight learning records.
6. Raw/object canonical equality and exact byte stability of TD-004 through
   TD-008 predecessor records.
7. Absence or clearing of learner source, environment values, answer prose,
   feedback, prompts, credentials, secrets, endpoints, payloads, responses,
   identity data, transient controls, temporary files, and Tour state.
8. One atomic zero-evidence mount of blank `OF-00 / SC-10`.

Only then is the token consumed. Missing, duplicate, stale, forged, combined,
partial, private, contaminated, wrong-owner/action/group/modality, Tour,
changed-predecessor, or interrupted intent fails closed at exact OR-30,
writes and replays nothing, reveals no SC-10 identity, and does not spend a
future valid token. Interruption before consumption restores OR-30;
interruption after blank OF-00 but before verified save clears candidate work,
reconstructs OR-30, and requires fresh intent.

## Normal identities and state graph

Mission must freeze distinct normal identities for:

- save key `horizon-archive-rp009-occluded-fold-save-v1`;
- record version `rp009.occluded-fold-save.v1`;
- controller `rp009.occluded-fold-controller.v1`;
- route controller `td009.route-controller.v1`; and
- fixture `td009-occluded-fold-v1`.

They may not alias `rp009.protected-journey.v1`.

```text
OR-30 OCCLUDED-FOLD ROUTE CHOICE
  -> OF-00 ARRIVE + ORIENT
  -> OF-10 SURVEY EXPOSED EDGES
  -> OF-20 RECONCILE BOUNDARIES + SAVE
  -> OF-30 VERIFY + RETURN
```

The six equal observation IDs are `three_near_margins`,
`bounded_signature_correspondences`, `unmatched_exposed_record`,
`ambiguous_candidate_record`, `unavailable_outer_margin`, and
`layered_edge_stewardship`. Every `6! = 720` order converges and revisits are
idempotent. Order, view, crop, signature, silhouette, motion, sound, timing,
focus, navigation, modality, confidence, save display, accessibility, Tour,
and world state grant zero observation or learning credit.

After the six observations, deterministic first-incomplete ownership is:
PY-017 primary, trace, transfer; prompt-boundary primary, retrieval, transfer;
system/user explanation; truth/authority explanation; bounded review; save.

## Strict independent learning and evidence firewall

### PY-017 environment and secret boundary

The frozen chain is exact `8/8 primary -> failed-check-only answer-free
remediation -> delayed closed-note 8/8 trace -> genuinely blank 8/8 transfer`.
The learner imports `os`, reads exactly one allowlisted nonsecret local mode
with one `os.environ.get`, requires value `bounded`, preserves the environment
and supplied sanitized records, derives exactly four distinct evidence
classes, leaves identity/topology/continuity/transformation/cause/purpose
`None`, and performs no output, file, network, secret, credential, endpoint,
process, package, or external operation.

The normal browser implementation may use a pure deterministic approved-source
shape validator and supplied sanitized in-memory replicas. It must not use
`eval`, `Function`, arbitrary Python, Python/WASM, Worker, file/path APIs,
browser persistence, environment/process access, packages, network, clipboard,
downloads, or external input. Required truthful label:

> The course validator checks the approved Python source shape and derives the
> bounded ledger from session-only sanitized replicas. It does not execute
> arbitrary Python, read a real process environment or secret, or contact a
> live service.

Primary and transfer use different records and allowlisted nonsecret names;
primary may not prefill transfer. A miss reveals only actual failed check IDs,
clears all transient/private work, opens a wholly blank unlimited retry,
announces one bounded System status, and focuses the first failed control.

### RP009-PROMPT-BOUNDARY-01 / AI901-D2-O1

The independent chain is exact `8/8 primary -> per-case/per-dimension
answer-free remediation -> delayed closed-note 8/8 retrieval -> genuinely
blank 8/8 transfer`, plus two separately scored explanations:

1. system prompts own persistent role, rules, boundaries, fallback, safety,
   and output contracts while user prompts own the current task and supplied
   input; and
2. prompt quality and grounding neither prove output truth nor authorize live
   action.

All cases are neutral course-authored text. SC-10, retained summaries, scene
evidence, inferred weakness, success display, and save state cannot select a
case, answer, or remediation. No model, agent, endpoint, credential, SDK, API,
tool, content, resource, or live prompt call occurs. A miss exposes only actual
scored allowlisted misconception tags, never an answer, and returns blank.

### No cross-credit

The two predecessor records, six observations, three Python records, three
prompt records, two explanations, review, and save are separate conjuncts.
No conjunct may be prefilled, repaired, inferred, compensated, or finalized by
another. Presentation, route, focus, timing, modality, confidence, Tour,
save/restore display, scene, and world state remain zero credit. Conjunction
permits review and save; it never creates cross-credit.

## Privacy, persistence, and durable record

The durable record has exactly twelve root keys in this order:

```text
version, packetId, mappingId, checkpoint, continuation,
cityStateDelta, externalStateDelta, successor,
retainedRp007Summary, retainedRp008Summary, edgeLedger, evidence
```

Fixed identities are `rp009.occluded-fold-save.v1`, `RP-009`,
`RP009-A3-OCCLUDED-FOLD`, `occluded_fold_complete`,
`continuation="continuation"`, null city/external deltas, and `successor=null`.
The RP-007 and RP-008 summaries must be copied only through their exact
sanitizers and remain ordered, separately named, read-only, and independently
verifiable.

`edgeLedger` permits only the six canonical observations and one reconciliation
with exact ordered keys: `mode`, `correspondence`, `unmatched`, `ambiguous`,
`unavailable`, `identity`, `topology`, `continuity`, `transformation`, `cause`,
`purpose`. Mode is `bounded`; the four classes stay separate; all six limits
remain null. `evidence` contains exactly eight ordered finalized records:
PY-017 primary/trace/transfer; prompt primary/retrieval/transfer; then the two
explanations. Evidence fields are limited to packet, mapping, form,
skill/objective ID, dimension correctness, bounded attempt count, bounded hint
level, optional bounded confidence, actually scored allowlisted tags, and
mastery status.

Never persist or log source/fragments, environment names selected by the
learner, environment snapshots or values, supplied/derived edge records or
working ledger, active choices, answers, reasoning, feedback, hints,
diagnostics, private notes, identity data, focus/pointer/token history,
credentials, secrets, endpoints, payloads, responses, source content, prompts,
exam items, unavailable contents, external-action requests, or unsupported
conclusions. Clear them on miss, retry, group replacement, early return,
interruption, rejection, failed write, invalid restore, save, verified restore,
and Tour exit.

## Atomic save, recovery, resume, and return

One explicit save must:

1. retain prior RP-009 bytes or verified absence;
2. snapshot exact TD-004 through TD-008 predecessor bytes;
3. sanitize in memory and reject unless every conjunct and allowlist passes;
4. write one canonical RP-009 serialization;
5. read raw bytes back once, parse, re-sanitize, and require exact canonical
   equality;
6. prove every predecessor byte unchanged;
7. on any failure restore prior RP-009 bytes or absence and verify exact byte
   equality; and
8. issue `HOLD` if rollback cannot be verified.

Only exact verified bytes restore `OF-30 VERIFY + RETURN / SC-10`. Restore
replays no route, arrival, observation, evaluator, environment read, ledger
derivation, case, explanation, review, save, animation, sound, focus event, or
world event. Invalid/absent candidate state clears candidate work and
reconstructs exact OR-30. A contiguous finalized pre-save learning prefix may
identify the first incomplete scored boundary only after sanitation; all six
physical observations must be deliberately re-observed and no raw/private
work survives. Both exact returns—to Offset Reach and City Threshold—are
write-free, replay-free, reversible, clear unsaved candidate work, and restore
safe deterministic focus.

## Accessibility, responsive, and modality contract

- Pointer, touch, keyboard Enter, keyboard Space, switch, speech, and screen
  reader converge on one semantic action; validation precedes token
  consumption and repeat/multi-event bursts remain one-hit.
- Exactly one owner/message/content/action group is active. Replacement
  focuses its heading. Recovery announces one complete polite atomic
  owner-prefixed System status, then focuses the first incomplete control.
- All required targets are at least `44 x 44 CSS px`; native semantics,
  persistent labels, and associated help/error text remain complete.
- Meaning never depends on color, near/far position, sequence, silhouette,
  brightness, motion, sound, focus, layout, or binary arrangement alone.
- Forced colors use system colors, borders, and visible focus. Reduced motion
  uses still/direct equivalents and `scroll-behavior:auto`. Grayscale keeps
  every record class distinguishable by structure, geometry, and text.

Required DPR-1 live matrix:

| Layout | Requirement |
|---|---|
| desktop | `1920 x 1080`; complete `100dvh` shell, dominant world, active group/actions visible, no outer scroll |
| laptop | `1366 x 768`; no horizontal escape; internal scroll evident and keyboard reachable |
| narrow | `390 x 844`; world above one natural interface column; no clipping or horizontal escape |
| effective 200% | `768 x 900` CSS viewport; natural reflow; all labels/errors/status/actions contained |

Mission must freeze exact state-to-SC-09/SC-10 role, crop, alt, owner, focus,
status, longest heading, longest label, and longest retained-summary row. Those
exact UTF-8 strings must be live-rendered at all four layouts. Planned SC-10
roles are not accepted runtime media.

## Asset and provenance viability

At most two direct SC-10 runtime image roles may be authorized: one invariant
panoramic world identity and one spatially consistent exposed-edge detail.
Each needs a source master, stable role, direct import, emitted byte and hash
identity, prompt/generation and derivative provenance, truthful source and
output dimensions, exact state/crop/alt assignment, grayscale and responsive
review, and explicit placeholder retirement.

Original-resolution QA must inspect required and forbidden readings: three
distinct near margins; exposed far records; correspondence without identity
or drawn paths; an ordinary unmatched record; unranked multi-candidate
ambiguity; one physically opaque unavailable margin; layered stewardship; no
cutaway, opening, route, map, matching diagram, hidden contents, protagonist,
ship, human trace, readable writing, or answer key. Deterministic enlargement
must be disclosed and never called native resolution. A detail may be a
truthful contextual registration only if explicitly labeled; it may not claim
to be a literal crop unless pixel identity proves it.

## Storage-free closed fixture and validation

Create exactly one fixture, `td009-occluded-fold-v1`, absent from production.
It accepts only a versioned public, private-free allowlist of exactly 64 named
scenarios in fixed groups: 12 route/entry; 4 OF checkpoints; 6 equal
observations; 18 learning boundaries; 8 privacy/recovery; 8 review/save/return;
and 8 presentation states.

The exact public IDs are:

```text
route_pointer, route_touch, route_keyboard_enter, route_keyboard_space,
route_switch, route_speech, route_screen_reader, route_invalid_predecessor,
route_invalid_intent, route_duplicate_or_stale, route_private_contaminated,
route_tour_isolated,
of00_arrive, of10_survey, of20_reconcile, of30_restore,
observation_three_near_margins, observation_bounded_signature_correspondences,
observation_unmatched_exposed_record, observation_ambiguous_candidate_record,
observation_unavailable_outer_margin, observation_layered_edge_stewardship,
python_primary_blank, python_primary_miss, python_primary_pass,
python_trace_blank, python_trace_pass, python_transfer_blank,
python_transfer_pass,
prompt_primary_blank, prompt_primary_miss, prompt_primary_pass,
prompt_retrieval_blank, prompt_retrieval_pass, prompt_transfer_blank,
prompt_transfer_pass,
system_user_explanation_blank, system_user_explanation_pass,
truth_authority_explanation_blank, truth_authority_explanation_pass,
resume_contiguous_prefix, resume_first_gap, interruption_before_consumption,
interruption_after_of00_pre_save, private_work_cleared,
environment_work_cleared, prompt_work_cleared, atomic_polite_status,
review_ineligible, review_eligible, save_committed, save_rollback_verified,
restore_replay_free, return_offset_reach, return_city_threshold,
continuation_inert,
layout_desktop, layout_laptop, layout_narrow, layout_effective_200,
mode_forced_colors, mode_reduced_motion, mode_grayscale,
longest_copy_contained
```

Manifest and scenario module must enumerate those IDs exactly. Tests reject
additions, omissions, duplicates, arbitrary JSON, query/hash injection,
campaign adapters, browser storage, network, service workers, clipboard, file
pickers, and Martin-owned state. Harness controls/output stay semantically and
visually separate from product controls/content. Captures are bounded by world
role; no 720-layout capture explosion is required.

Focused tests exhaust route validation, seven modalities, interruptions, all
720 observation orders/revisits, both learning chains and bypasses, no-cross-
credit, sanitation, atomic write/read-back/rollback, replay-free restore,
returns, Tour, fixture isolation, invariant world, and hard stop. Related
regression covers TD-004 through TD-008 bytes, routes, returns, evidence,
media, sanitation, persistence, and fixtures. Release requires the full suite,
all readiness validators, build, `PBA-TD009-v1`, exact served root/deep/chunk/
media identity, one non-overlapping E2E, four-layout live review, assistive
modes, local-only requests, clean logs, cleanup, QA restoration, stable
candidate freeze, patch integrity, and independent Intelligence reconciliation.

## Fresh non-compounding production budget

`PBA-TD009-v1` rebaselines once from exact accepted TD-008 output, not from the
unused TD-008 cap. Current production evidence is exact: one JavaScript chunk
`1,450,367` bytes, one CSS chunk `99,210` bytes, `203` modules, and 17 runtime
media files totaling `37,410,731` bytes. The fresh caps are:

| Measure | TD-009 cap | Fresh headroom |
|---|---:|---:|
| aggregate JavaScript | `1,522,885` bytes | `72,518` bytes |
| aggregate CSS | `104,170` bytes | `4,960` bytes |
| production modules | `213` | `10` |
| new runtime images | `2` | `2` |
| new runtime image bytes | `8,388,608` | `8,388,608` |
| total runtime media | `45,799,339` bytes | `8,388,608` |

All output chunks and all new runtime assets count. Existing 17 media hashes
must remain present. No new audio, font, video, source-map payload,
Python/WASM runtime, or network-fetched payload is allowed. Build cap is 60s;
focused/related/full suites 30/60/60s; complete E2E 180s; sampled main-thread
task 100ms. Any overage returns to Science and Mission; quality and contract
gates cannot be weakened to meet a cap.

At the Operations commit, a fresh production build passed in `7.53s`
(`9.012s` wall), transformed `203` modules, and reproduced exact accepted
TD-008 JavaScript, CSS, and all 17 media identities. The focused RP-008 normal
plus RP-009 protected set passed `22/22` in `0.441s`; RP-009 mapping self-test
passed; the full product suite passed `882/882` in `9.167s`. The executable
validator is reusable with `--authority` pointing at `04A` and is required in
baseline, candidate, and release modes as applicable.

## Risks and controls

| Risk | Control / return owner |
|---|---|
| Route inferred from notation or scenery | Only the exact fresh validated route token mounts blank OF-00; return to Operations on conflict |
| PY-017 misrepresented as real environment execution | Pure approved-shape/in-memory simulation plus mandatory truthful label; return to Science |
| Prompt quality presented as truth or authority | Independent explanation and hard no-live-action boundary; return to Science |
| Prior records merge or cross-credit | Three ordered, separately named immutable scopes and exact evidence conjunction |
| Save failure loses predecessor or partial state | Canonical write/read-back, predecessor byte proof, verified rollback, hard HOLD on unverifiable rollback |
| Occlusion becomes access or hidden contents | Source, runtime, copy, alt, focus, and asset hard-stop scan; canon conflict returns to Colonel |
| Visual correspondence becomes topology | Original-resolution required/forbidden review and no matching lines or cutaway |
| Long copy fails reflow | Freeze exact UTF-8 copy and render longest cases at four required layouts |
| Fixture leaks into production or reads storage | Exact 64-ID closed allowlist, source/bundle absence, storage/network prohibition |
| Bundle allowance compounds | One-time 5% rebaseline from exact accepted TD-008 output only |

No unresolved conflict or formal variance was found. The normal route and UI
remain unimplemented by design; live presentation, browser persistence, final
assets, served identity, E2E, and assistive review remain Mission/Marine/
Intelligence obligations, not claims of this Science pass.

## Flexible areas and hard stops

Mission may choose compliant copy, grouping, pacing, component factoring,
visual hierarchy, implementation organization, focus wording, local-only
simulation structure, and at most two approved asset treatments within this
envelope. Those choices may not change evidence, state order, save shape,
route ownership, record scope, privacy, accessibility, asset roles, caps,
returns, canon, authority meaning, or the hard stop.

Hard stop: `OF-30 / SC-10`, the two exact returns, and one inert LOOK-only
`destination=null`, `routeOpened=false`, `successor=null` notation. No RP-010
name, event, destination, route, cue, asset, objective, learning, save field,
focus target, unavailable content, access, reward, permission, identity,
topology, authority, response, world effect, RP-013, successor, ending
extension, or post-ending content may mount, render, announce, persist, or
become focusable.

## Disposition

**`VIABILITY READY`**

`VE-TD009-v1` and `PBA-TD009-v1` are the complete Science contract for
Mission reconciliation. No Science-owned contradiction, missing utility, or
unacceptable risk prevents one integration-sized shell.

## Exact Mission Captain handoff

Run **Mission Captain / `mission_captain`** only. Reconcile
`GDB-TD009-v1`, `WNMP-TD009-v1`, `CFS-TD009-v1`, this
`VE-TD009-v1`, and `PBA-TD009-v1` into one conflict-free versioned shell for
exact `RP-009 / SC-10 / OF-00-OF-30` and sole `TD009-RTA-001`, or issue
`REVISE`/`HOLD`.

Freeze the exact predecessor/token/sanitation order; normal identities; six
equal observations/all 720 orders; strict independent PY-017 and prompt-
boundary chains plus both explanations; no-cross-credit; privacy clearing;
the 12-key record and ordered separate RP-007/RP-008/RP-009 scopes; atomic
write/read-back/rollback; replay-free restore; deterministic focus and
returns; seven modalities; four layouts and exact longest UTF-8 copy; forced
colors/reduced motion/grayscale; two conditional SC-10 asset roles and
original-resolution QA; exact 64-scenario storage-free fixture; offline/no-
authority/no-exam-guarantee; fresh PBA gates; served identity; stable-candidate
freeze; release ladder; and OF-30 hard stop. Do not implement, generate,
inspect browser storage/save, expose later content, or deploy Marines before
an explicit versioned `SHELL READY` contract.

## Report envelope

- **Systems/evidence:** reusable one-hit route, pure evaluators, strict
  conjunctive evidence, sanitizers, atomic adapter, focus/recovery, and closed
  fixture; normal TD-009 identities remain to be built.
- **Privacy/save/recovery:** exact allowlists, private/transient clearing,
  predecessor byte stability, read-back, verified rollback, and replay-free
  restore are mandatory.
- **Accessibility/performance:** seven modalities, >=44px, deterministic
  focus/status, four layouts and assistive modes fit `PBA-TD009-v1`.
- **Validation:** focused `22/22`, RP-009 validator pass, full `882/882`, and
  current exact 203-module production baseline pass.
- **Official sources:** Microsoft-only current sources reviewed before frozen
  repository authority; unavailable priority skill documented.
- **Variances:** none.
- **Files:** this envelope, `04A`, stage metrics, and synchronized handoff.
- **Synchronization:** dedicated local Science commit; no push at this gate.
- **Next role:** Mission Captain.
